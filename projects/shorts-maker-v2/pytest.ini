[pytest]
testpaths = tests
markers =
    smoke: live smoke tests that call external APIs
    slow: slow tests that render real video files (golden render)
addopts = --capture=no --cov=src/shorts_maker_v2 --cov-report=term-missing --cov-report=xml --cov-fail-under=45
