# TASKS - AI Kanban Board

# TASKS - AI Kanban Board

> Read this file at session start and update it before ending the session.

## TODO

| ID | Task | Owner | Priority | Auto | Created |
| --- | --- | --- | --- | --- | --- |
| T-251 | `[hanwoo-dashboard]` Run Prisma 7 live CRUD E2E once a real Supabase `DATABASE_URL` is configured. Root cause: Supabase password desynchronization. Retried on 2026-05-20 with `npm.cmd run db:prisma7-test -- --live`; local Prisma/client/adapter checks passed (`15 passed`) but live connection health still failed with Prisma `P2010`, raw code `XX000`, message `(ENOTFOUND) tenant/user postgres.fuemeqmigptwfzqvrpjf not found`. **Action Required**: The user MUST manually reset the database password via the Supabase Dashboard (Project Settings > Database) to resynchronize the control plane pooler credentials, then update `.env` if it changes. | User | Medium | approval | 2026-05-08 |
| T-372 | `[workspace]` 모노레포 마이그레이션 마무리(pnpm+turbo+biome+uv). (구 T-368 — DONE의 Codex T-368과 ID 충돌해 재번호) 현재 WIP(untracked): 루트 `package.json`/`pnpm-workspace.yaml`/`turbo.json`/`biome.json`/`.npmrc`/`pyproject.toml`/`uv.lock`, 수정된 `setup.bat`·`.github/workflows/full-test-matrix.yml`, 삭제된 knowledge-dashboard `package-lock.json`. **2026-05-20 진단(Claude)**: ① `pnpm install`이 이 로컬(Windows + 한글 홈 `박주호`)에서 linking 단계에 `exit 127`·에러 출력 없이 6회 연속 중단 — `--lockfile-only`(linking 없음)만 exit 0. 한글 경로 툴링 취약성(메모리 `windows_korean_path_encode_strict`) 의심. → **로컬 검증 불가**, CI(ubuntu-latest)는 정상일 가능성 높음. ② `pnpm-lock.yaml` 부재였으나 `pnpm install --lockfile-only`로 생성됨(루트, untracked, 336KB) — `.gitignore`엔 lockfile 제외 없음. ③ 미해결 결정: (a) `biome.json`이 `recommended` 규칙 + 전 코드베이스 대상 `biome check .` → **2026-05-20(Claude) blast radius 측정 완료**: `npx @biomejs/biome@2.4.15 check .`로 263파일에서 **796 진단(555 errors / 218 warnings / 23 infos)**. 최다 항목 — organizeImports 121·noCommaOperator 103·useButtonType 20·noUnusedVariables 19·useArrowFunction 18·noDescendingSpecificity 17·noLabelWithoutControl 16·noDoubleEquals 15·noArrayIndexKey 14·noUnusedImports 13. organizeImports/useArrowFunction/useConst/useImportType/useTemplate/noUnusedImports 등은 `biome check --write`로 자동수정 가능하나 useButtonType/noLabelWithoutControl/noDoubleEquals/noArrayIndexKey/noInnerDeclarations는 수동 판단 필요. → `pnpm lint`(`turbo run lint && pnpm lint:biome`)에 biome을 blocking으로 넣으면 즉시 적색 확정. **권장: advisory로 시작하거나 `biome check --write` 선처리 후 잔여만 blocking** — 사용자 결정 필요. (b) hanwoo `package.json`에서 `postinstall: prisma generate` 제거됨 → CI fresh build 시 prisma client 미생성 위험. **2026-05-20(Claude) 확인: `.github/workflows/`에 명시적 `prisma generate` 스텝 없음, hanwoo `build`는 `next build --webpack`뿐 — 현재 prisma client 생성은 오직 `postinstall` 의존. 따라서 postinstall 제거 시 CI fresh build가 확정적으로 깨짐.** 복원하거나 turbo build pipeline/CI에 `prisma generate` 단계 추가 필수. ④ 잔존 정리: suika-game-v2·word-chain `package-lock.json` 미삭제(pnpm 모노레포에선 제거 대상), CI `actions/setup-node@v6→v4` 다운그레이드. **로컬 검증 불가 + 미해결 설계 결정 + infra/CI 범위라 신중 진행 필요 → approval.** | Any | Medium | approval | 2026-05-20 |
| T-407 | `[workspace/blind-to-x/shorts-maker-v2]` VibeDebt RED 진짜 부채 감축 프로그램. 측정 정확화(T-406) 후 overall TDR **33.9% RED**, principal 342h. 잔여는 측정 오류가 아닌 진짜 부채 — 최악 채무자: `audio_mixin.py`(51.9, dup=100), `ai_tech_shorts.py`(51.3), `text_engine.py`(46.5, dup=71), `blind.py`·`ppomppu.py`(cx=100 거대 스크래퍼), `orchestrator.py`(maxfn 798줄·maxcc 74). 주 동인은 cyclomatic 복잡도(거대 함수 분해)·코드 중복(dedup)·실제 테스트 부재. **파일 단위로 하나씩, 해당 프로젝트 QC 검증과 함께 진행 — 멀티 AI 도구가 같은 프로젝트를 동시 편집 중일 땐 충돌 위험이 크므로 단독 작업 구간에서 진행할 것.** 다중 세션 작업. | Any | Low | approval | 2026-05-20 |

## IN_PROGRESS

| ID | Task | Owner | Started | Notes |
|---|---|---|---|---|

## DONE (Latest 5)

| ID | Task | Completed By | Completed |
|---|---|---|
| T-619 | `[hanwoo-dashboard]` Normalized dashboard building and cattle collection payloads. `DashboardClient` now routes `initialBuildings`/state through `normalizeDashboardBuildings()`, derives `safeBuildings`, routes paged/full cattle through `normalizeDashboardCattleList()`, filters malformed rows, supplies Korean missing-name copy, and passes safe collections into home cards, pen grids, drag/drop/export, setup progress, alert banners, forms, detail modal, Feed/Calving/Sales/Analysis/Settings tabs. Verification: focused home-market source test passed (`28 passed`), empty-state source test passed (`16 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 257, lint, build), and `code_review_gate --base HEAD~1 --json` emitted JSON pass (`risk_score 0.0`; cp949 reader-thread noise only). Commit `1dd1f846`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-618 | `[hanwoo-dashboard]` Normalized pen card cattle payloads. `PenCard` now routes incoming `cattle` through `normalizePenCattle()`, filters malformed rows, supplies stable fallback ids plus Korean missing-name copy, and renders from `visibleCattle`, preventing malformed caller/cache data from crashing the pen card through raw `.some()`, `.length`, or `.map()` access. Verification: focused cards accessibility/source test passed (`3 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed after one real concurrent Next build lock retry (`test` 255, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `bd5adf04`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-617 | `[hanwoo-dashboard]` Normalized notification system payloads. `NotificationSystem.js` and `NotificationSystem.tsx` now route `initialNotifications` through `normalizeSystemNotifications()`, ignore malformed rows, supply stable fallback ids plus Korean title/message defaults, and use functional state updates for mark-read actions, preventing malformed caller/cache data from crashing the notification dropdown through raw `.filter()`, `.length`, or `.map()` access. Verification: focused notification-system source test passed (`9 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 255, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `3411278f`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-616 | `[hanwoo-dashboard]` Normalized profitability widget payloads. `ProfitabilityWidget` now routes incoming `data` through `normalizeProfitabilityItems()`, filters malformed rows, supplies stable fallback ids, and renders from `visibleData`, preventing malformed caller/cache data from crashing the profitability recommendation widget through raw `.length` or `.map()` access. Verification: focused profitability copy/source test passed (`7 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 253, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `e4097a36`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-615 | `[hanwoo-dashboard]` Normalized settings building payloads. `SettingsTab` now routes incoming `buildings` through `normalizeSettingsBuildings()`, filters malformed rows, supplies safe fallback building names and pen counts, and renders from `safeBuildings`, preventing malformed caller/cache data from crashing the settings building list through raw `.map()` access. Verification: focused settings plus home-market source tests passed (`37 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 252, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`). Commit `0272bd19`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-614 | `[hanwoo-dashboard]` Normalized sales tab payloads. `SalesTab` now routes incoming `saleRecords`, `cattleList`, and `expenseRecords` through `normalizeSalesItems()`, then uses `safeSaleRecords`, `safeCattleList`, and `safeExpenseRecords` for processed sales, cattle lookup, expense aggregation, select options, submit disabled state, and empty-state actions, preventing malformed caller/cache data from crashing the tab through raw spread, `.find()`, `.filter()`, `.map()`, or `.length` access. Verification: focused home-market copy/source test passed (`26 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 252, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `ab0183f2`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-613 | `[hanwoo-dashboard]` Labeled settings submit states. `SettingsTab` now derives `farmSubmitButtonLabel` and `buildingSubmitButtonLabel` from `isSavingFarm`/`isSavingBuilding` and applies them to the farm settings and building create submit `PremiumButton` `aria-label` and `title`, so settings save actions expose their in-flight state while async saves are locked. Verification: focused settings accessibility test passed (`10 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 250, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `1b4e16e7`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-612 | `[hanwoo-dashboard]` Normalized analysis tab payloads. `AnalysisTab` now routes incoming `saleRecords`, `feedHistory`, `cattleList`, and `expenseRecords` through `normalizeAnalysisItems()`, then uses `safeSaleRecords`, `safeFeedHistory`, `safeCattleList`, and `safeExpenseRecords` for monthly chart aggregation, cost structure, top sales, feed average, and cattle count, preventing malformed caller/cache data from crashing the tab through raw `.forEach()`, spread, `.reduce()`, or `.length` access. Verification: focused analysis copy/source test passed (`3 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 250, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `1e72ad25`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-611 | `[hanwoo-dashboard]` Normalized calving tab payloads. `CalvingTab` now routes incoming `cattle` and `buildings` props through safe collection normalizers, ignores malformed rows, derives `pregnantCows` from `safeCattle`, and uses `safeCattle`/`safeBuildings` for submit and building lookups, preventing stale cache or caller data from crashing the tab through raw `.filter()` or `.find()` access. Verification: focused Calving accessibility test passed (`5 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC test/lint passed (`test` 250, lint), targeted build retry passed after a real concurrent Next build lock, and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `4535546c`; commit hook WARN was the known graph/test-gap heuristic while direct tests and build covered the changed files. | Codex | 2026-05-21 |
| T-610 | `[hanwoo-dashboard]` Labeled sales submit state. `SalesTab` now derives `submitButtonLabel` from `isSaving` and applies it to the sales submit `PremiumButton` `aria-label` and `title`, matching the visible 판매 기록 등록 state so the 판매 기록 등록 action exposes its in-flight state. Verification: focused home-market copy/source test passed (`25 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 250, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `4974e238`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-609 | `[hanwoo-dashboard]` Labeled schedule submit state. `ScheduleTab` now derives `submitButtonLabel` from `isSaving` and applies it to the schedule submit button `aria-label` and `title`, matching the visible 일정 등록/등록하기 state so the 일정 등록하기 action exposes its in-flight state. Verification: focused Schedule accessibility test passed (`7 passed`), current Cattle/Schedule focused regression pass (`18 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC test/lint passed (`test` 249, lint), initial full QC build hit a real concurrent Next build lock, targeted build retry passed, and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `69c19b22`; commit hook WARN was the known graph/test-gap heuristic while direct tests and build covered the changed files. | Codex | 2026-05-21 |
| T-608 | `[hanwoo-dashboard]` Normalized cattle form building payloads. `CattleForm` now routes incoming `buildings` props through `normalizeCattleFormBuildings()`, ignores non-array/non-object malformed payloads, supplies stable fallback ids plus Korean missing-building copy, and renders/reset/defaults from `safeBuildings`, preventing stale cache or caller data from crashing the cattle form through raw `.map()` or value derivation. Verification: focused cattle detail/form wiring test passed (`11 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 249, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `d76f1058`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-607 | `[hanwoo-dashboard]` Labeled inventory submit state. `InventoryTab` now derives `submitButtonLabel` from `isSaving` and applies it to the create submit `PremiumButton` `aria-label` and `title`, matching the visible 재고 등록/등록하기 state so the 재고 등록하기 action exposes its in-flight state. Verification: focused empty-state/home-market source tests passed (`41 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC test/lint passed (`test` 248, lint), initial full QC build hit a real concurrent/stale Next build lock, targeted build retry passed, and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `d1a2b83f`; commit hook WARN was the known graph/test-gap heuristic while direct tests and build covered the changed files. | Codex | 2026-05-21 |
| T-606 | `[hanwoo-dashboard]` Covered inventory create submit lock. `empty-state-wiring.test.mjs` now asserts `InventoryTab` uses `saveInFlightRef`, `isSaving`, `submitButtonLabel`, `aria-busy`, `aria-label`, and `title` on the create submit button so the existing duplicate-save guard and accessible pending copy cannot regress silently. Verification: focused empty-state/feed/inventory wiring test passed (`16 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 248, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `3cf67f96`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed file. | Codex | 2026-05-21 |
| T-605 | `[hanwoo-dashboard]` Normalized feed tab payloads. `FeedTab` now routes incoming `cattle`, `feedStandards`, `feedHistory`, and `buildings` props through safe collection normalizers, ignores non-array/non-object malformed payloads, supplies stable fallback building/record ids plus Korean missing-building copy, and renders from `safeCattle`, `safeFeedStandards`, `safeFeedHistory`, and `safeBuildings`, preventing stale cache or caller data from crashing feed summaries, filters, charts, or recent records through raw `.filter()`, `.forEach()`, spread, or slice access. Verification: focused empty-state/feed wiring test passed (`15 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 247, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `186a031f`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-604 | `[hanwoo-dashboard]` Labeled feed submit state. `FeedTab` now derives `submitButtonLabel` from `isSaving` and applies it to the submit `PremiumButton` `aria-label` and `title`, matching the visible 급여 기록 저장/저장하기 state so the 급여 기록 저장하기 action exposes its in-flight state. Verification: focused empty-state/feed wiring test passed (`14 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 246, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `88413c59`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-603 | `[hanwoo-dashboard]` Labeled calving submit state. `CalvingTab` now derives `submitButtonLabel` from `isSaving` and applies it to the submit button `aria-label` and `title`, matching the visible 분만 기록 저장/등록 copy so the 분만 완료 및 송아지 등록 action exposes its in-flight state. Verification: focused Calving accessibility test passed (`4 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 246, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `42caac0a`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-602 | `[hanwoo-dashboard]` Normalized schedule tab payloads. `ScheduleTab` now routes incoming `events` props through `normalizeScheduleEvents()`, ignores non-array/non-object malformed payloads, supplies stable fallback ids plus Korean missing-title copy, defaults unknown event types to `General`, and renders from `safeEvents`, preventing stale cache or caller data from crashing calendar/upcoming-event rendering through raw `.filter()` or `event.date` access. Verification: focused tab-header accessibility/source test passed (`7 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 246, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `225a216f`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-601 | `[hanwoo-dashboard]` Normalized inventory tab payloads. `InventoryTab` now routes incoming `inventory` props through `normalizeInventoryItems()`, ignores non-array/non-object malformed payloads, supplies stable fallback ids plus Korean inventory-name/unit defaults, and renders from `safeInventory`, preventing stale cache or caller data from crashing the inventory tab through raw `.map()` or `.length` access. Verification: focused empty-state/inventory wiring test passed (`14 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC test/lint passed (`test` 245, lint), initial full QC build hit a real concurrent Next build, targeted build retry passed, and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `0e6bed60`; commit hook WARN was the known graph/test-gap heuristic while direct tests and build covered the changed files. | Codex | 2026-05-21 |
| T-600 | `[hanwoo-dashboard]` Labeled cattle form submit state. `CattleForm` now derives `submitButtonLabel` from `isSaving` and applies it to the submit button `aria-label` and `title`, matching the visible pending copy and existing cancel-button state labels so the 개체 등록/수정 save action exposes its in-flight state. Verification: focused cattle detail/form wiring test passed (`10 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 245, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `17962ff1`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-599 | `[hanwoo-dashboard]` Normalized alert banner payloads. `AlertBanners` now routes incoming alert props through `normalizeAlertNotifications()` and `normalizeBuildings()`, ignores non-array/non-object malformed payloads, supplies stable fallback ids plus Korean cattle-name copy, and uses safe building lookup so stale cache/caller data cannot crash estrus or calving banners through raw `.filter()` or `.find()` calls. Verification: focused alert banner source test passed (`3 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC test/lint passed (`test` 244, lint), initial full QC build hit a real concurrent Next build, targeted build retry passed, and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `1cf25d5d`; commit hook WARN was the known graph/test-gap heuristic while direct tests and build covered the changed files. | Codex | 2026-05-21 |
| T-598 | `[hanwoo-dashboard]` Labeled login submit states. `app/login/page.js` now derives `loginSubmitLabel` for missing 아이디, missing 비밀번호, submitting, and ready states, then applies it to the login submit button `aria-label` and `title`, so the first-run login CTA explains why it is disabled or what action will run. Verification: focused error/login wiring test passed (`6 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 244, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `7cedaa17`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-597 | `[hanwoo-dashboard]` Normalized notification widget payloads. `NotificationWidget` now routes props through `normalizeNotifications()`, ignores non-array/non-object malformed payloads, supplies stable fallback ids plus Korean title/message copy, and renders from `visibleNotifications`, preventing stale cache or caller data from crashing the widget through raw `.length`, `.map()`, or `note.type` access. Verification: focused notification system copy test passed (`8 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 243, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `558c4f17`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-596 | `[hanwoo-dashboard]` Labeled cattle-detail breeding record busy actions. `CattleDetailModal` now derives labels for the 발정/수정 record open buttons plus the breeding form cancel/submit buttons and applies them through `aria-label`, `title`, and aligned `aria-busy`, so disabled breeding controls explain whether detail processing or breeding-record save work is in flight. Verification: focused cattle detail/form wiring test passed (`10 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 242, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `5a98fda7`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-595 | `[hanwoo-dashboard]` Normalized alert banner D-day labels. `EstrusAlertBanner` and `CalvingAlertBanner` now route `daysLeft` through a shared `normalizeDaysLeft()` helper backed by `toFiniteNumber()`, preventing stale or malformed notification payloads from rendering `D-abc`, `D-Infinity`, negative D-day labels, or incorrect today counts. Verification: focused alert banner source test passed (`2 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 242, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `d9d7c796`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-594 | `[hanwoo-dashboard]` Labeled cattle detail busy actions. `CattleDetailModal` now derives `editButtonLabel` and `archiveButtonLabel` from `isDetailBusy` and uses them for `aria-label` and `title`, so the edit/archive buttons explain that actions are unavailable while detail archive or breeding-record work is in flight. Verification: focused cattle detail/form wiring test passed (`10 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 241, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `5abdf575`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-593 | `[hanwoo-dashboard]` Normalized admin diagnostics metrics. `DiagnosticsPageClient` now routes database record counts, process uptime, and memory heap values through `toFiniteNumber()` before rendering model counts, uptime minutes, and MB labels, preventing malformed diagnostics payloads from leaking `NaN MB` or non-finite counts into the operations screen. Verification: focused diagnostics source test passed (`2 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 241, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `6c1c8312`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-592 | `[hanwoo-dashboard]` Labeled payment button states. `PaymentWidget` now derives `paymentButtonLabel` from checkout submitting/widget-ready state and reuses it for the visible button text, `aria-label`, and `title`, so assistive technology and hover text explain whether payment methods are still loading or payment preparation is in flight. Verification: focused payment UX/source test passed (`5 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `06cd994b`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-591 | `[hanwoo-dashboard]` Normalized weather widget numbers. `WeatherWidget` now routes visible weather temperatures, apparent temperature, humidity, wind, high/low, precipitation, forecast temperatures, and THI inputs through `toFiniteNumber()` before rendering, preventing malformed weather snapshots from leaking `NaN` or non-finite values into the home weather card. Verification: focused home/market/weather source test passed (`25 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `a5513311`. | Codex | 2026-05-21 |
| T-590 | `[hanwoo-dashboard]` Labeled cattle form cancel busy state. `CattleForm` already disabled the bottom cancel button while `isSaving`; it now derives `cancelButtonLabel` and applies `aria-busy`, `aria-label`, and `title`, so assistive technology and hover text distinguish the normal cancel action from the save-in-flight locked state. Verification: focused cattle detail/form wiring test passed (`10 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `977956b8`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-589 | `[hanwoo-dashboard]` Guarded schedule event dates. `ScheduleTab` now round-trips `YYYY-MM-DD` date keys in `toValidDate()` before calendar grouping, upcoming-list filtering, sorting, and D-day labels, so impossible schedule dates such as `2026-02-31` cannot roll forward through JavaScript `Date` parsing into false March calendar/upcoming entries. Verification: focused tab-header accessibility/source test passed (`6 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `1dccff1e`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-588 | `[hanwoo-dashboard]` Labeled cattle pagination loading state. `DashboardClient` already disabled the cattle load-more button and exposed `aria-busy` while `cattlePagination.isLoading`; it now derives `cattleLoadMoreLabel` and applies it to `aria-label` and `title`, matching the Sales pagination behavior so assistive technology and hover text distinguish the normal action from the in-flight loading state. Verification: focused cattle pagination feedback test passed (`1 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo test/lint passed (`test` 240, lint), initial full QC build hit a concurrent Next build lock, targeted build retry passed, and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `984f4592`; commit hook WARN was the known graph/test-gap heuristic while direct tests and build covered the changed files. | Codex | 2026-05-21 |
| T-587 | `[hanwoo-dashboard]` Guarded feed history dates. `FeedTab` now round-trips `YYYY-MM-DD` date keys in `toValidFeedDate()` before sorting and labeling feed history, so impossible feed record dates such as `2026-02-31` cannot roll forward through JavaScript `Date` parsing into false chart groups or recent-history labels. Verification: focused empty-state/feed wiring test passed (`13 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `2ccee108`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-586 | `[hanwoo-dashboard]` Labeled sales pagination loading state. `SalesTab` already disabled the load-more button and exposed `aria-busy` while `salesPagination.isLoading`; it now derives `loadMoreLabel` and applies it to `aria-label` and `title`, so assistive technology and hover text distinguish the normal action from the in-flight loading state. Verification: focused sales pagination feedback test passed (`1 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `4fc89c93`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-585 | `[hanwoo-dashboard]` Guarded financial chart fallback dates. `FinancialChartWidget` now uses strict `YYYY-MM-DD` round-trip validation in its fallback `toMonthKey()`, matching `AnalysisTab` and dashboard summary behavior so impossible sale/expense dates such as `2026-02-31` cannot roll forward through JavaScript `Date` parsing and pollute the client-side 6-month financial chart fallback. Verification: focused analysis/financial widget source test passed (`3 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed on latest HEAD (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `6f56e6de`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-584 | `[hanwoo-dashboard]` Labeled feed filter busy state. `FeedTab` already disabled building filter chips while feed records are saving; `FilterChip` now derives an `actionLabel`, exposes `aria-busy={disabled}`, and uses the pending Korean label/title so assistive technology receives the same save-in-flight state. Verification: focused empty-state/feed wiring test passed (`13 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `b9361301`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-583 | `[hanwoo-dashboard]` Labeled calving cancel busy state. `CalvingTab` already disabled the cancel button while `isSaving`; the cancel action now also exposes `aria-busy` plus conditional Korean `aria-label`/`title` so assistive technology receives the same save-in-flight state as the submit action. Verification: focused Calving accessibility test passed (`4 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `9ed077f3`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-582 | `[hanwoo-dashboard]` Labeled Excel export busy state. `ExcelExportButton` already blocked duplicate downloads with `isPreparing`; it now switches `aria-label` and `title` between `엑셀 다운로드` and `엑셀 다운로드 준비 중`, matching the visible pending copy and `aria-busy` state. Verification: focused Excel export button copy test passed (`2 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `90f31a18`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-581 | `[hanwoo-dashboard]` Announced AI chat send busy state. `AIChatWidget` already disabled duplicate sends through `canSend` and `sendInFlightRef`; the send icon button now exposes `aria-busy={isStreaming}` while the assistant response is streaming, matching the visible spinner/title with assistive-technology state. Verification: focused AI chat widget copy test passed (`2 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `e05a92be`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-580 | `[hanwoo-dashboard]` Showed QR print progress copy. `QRCodeWidget` now changes the print button accessible label, title, and visible text while `isPrinting` is true (`QR 라벨 인쇄 준비 중` / `인쇄 준비 중...`), keeping the duplicate-window guard and `aria-busy` state aligned with operator-facing copy. Verification: focused QR widget copy test passed (`2 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `1eb569b3`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-579 | `[hanwoo-dashboard]` Kept notification modal locked during SMS test sends. `NotificationModal` already disabled the test-send action; it now also guards Escape dismissal, overlay click dismissal, and the close button while `isTestingSMS` is true, with close button `aria-busy` so operators do not lose send state mid-flight. Verification: focused notification modal copy test passed (`7 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `f7f23d4d`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-578 | `[hanwoo-dashboard]` Announced market-price refresh state consistently. `MarketPriceWidget` now marks the initial loading state as a polite status region, exposes `aria-busy` on the refresh button while KAPE data is refreshing, and hides the decorative refresh icon from assistive technology. Verification: focused home/market copy test passed (`25 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `0c7fa6e6`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-577 | `[hanwoo-dashboard]` Locked all cattle detail actions while busy. `CattleDetailModal` now derives `isDetailBusy` from archive/delete and breeding-record save state, then applies it consistently to Escape dismissal, top close/back, edit, archive, and breeding-record open actions so no alternate action can race while destructive or record-saving work is in flight. Verification: focused cattle detail/form wiring test passed (`10 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `76a4471a`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-576 | `[hanwoo-dashboard]` Kept cattle detail modal locked while busy. `CattleDetailModal` now ignores Escape dismissal and disables the top close/back icon with `aria-busy` while archive/delete or breeding-record save work is in flight, preventing alternate-path dismissal during destructive or record-saving flows. Verification: focused cattle detail/form wiring test passed (`10 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `9db2090b`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-575 | `[hanwoo-dashboard]` Kept cattle form locked while saving. `CattleForm` already disabled the bottom cancel/submit controls during async save; it now also ignores Escape dismissal and disables the top back icon with `aria-busy` while save is in flight, preventing alternate-path dismissal during cattle create/update. Verification: focused cattle detail/form wiring test passed (`10 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `14ad83cf`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-574 | `[hanwoo-dashboard]` Ignored stale cattle tag lookup responses. `CattleForm` now tracks lookup request ids and mounted state so late MTRACE lookup responses cannot write into a new cattle form context after the modal switches records or unmounts; form reset also clears lookup loading and in-flight state. Verification: focused cattle detail/form wiring test passed (`10 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `b621bf3b`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-573 | `[hanwoo-dashboard]` Localized weather timeout copy. `weather-state.mjs` now exports `WEATHER_TIMEOUT_MESSAGE`, and both `DashboardClient` plus `useWeather` pass that Korean product copy into `fetchWithTimeout` instead of the inline English `Weather lookup timed out after 5000ms.` timeout message. Verification: focused weather/home copy tests passed (`34 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `75e22762`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-572 | `[hanwoo-dashboard]` Stabilized offline sync exception copy. `syncManager.js` now stores a stable Korean retry message for thrown sync exceptions instead of persisting raw `Error.message` values into the retry/dead-letter queue state, while keeping explicit action failure messages intact. Verification: focused sync-manager copy test passed (`1 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 240, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `31928831`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-571 | `[hanwoo-dashboard]` Stabilized dashboard validation error responses. `/api/dashboard/cattle` and `/api/dashboard/sales` now map `DashboardQueryValidationError` 400s to stable Korean operator-facing messages instead of echoing `error.message` from the route layer, keeping pagination/list API validation failures consistent with the rest of the dashboard fallback surface. Verification: focused home/market copy test passed (`25 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 239, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `e7e099e0`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-570 | `[hanwoo-dashboard]` Guarded AI chat recent-sale dates. `/api/ai/chat` `formatSaleDateForContext()` now round-trips string `YYYY-MM-DD` date keys before returning ISO text, so impossible recent-sale values such as `2026-02-31T00:00:00.000Z` cannot roll forward through JavaScript `Date` parsing into false AI assistant context and instead fall back to `출하일 미등록`. Verification: focused AI chat API/source test passed (`8 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 239, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `7546863f`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-569 | `[hanwoo-dashboard]` Guarded notification timing dates. `notification-timing.mjs` now round-trips `YYYY-MM-DD` date keys in `parseDate()` before scheduling 발정/분만 알림, so impossible source dates such as `2026-02-31T00:00:00.000Z` cannot roll forward through JavaScript `Date` parsing into false notification dates. Verification: focused notification timing test passed (`5 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo test/lint passed (`test` 239, lint), build initially hit a concurrent Next build lock and passed on targeted retry, and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `23398040`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-568 | `[hanwoo-dashboard]` Hardened profitability estimate inputs. `profitability-service.js` now lets `normalizeCachedMarketPrice()` handle snapshot dates directly instead of calling raw `toISOString()`, skips cattle with invalid birth dates before month-age math, and normalizes purchase price/weight through `toFiniteNumber()` before revenue and margin calculations. Verification: focused profitability source test passed (`6 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 239, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `dfa2e926`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-567 | `[hanwoo-dashboard]` Guarded payment approval dates. `payment-confirmation.mjs` now parses `approvedAt` through `parseApprovedAt()` before recording payment approval and subscription renewal dates, so impossible gateway timestamps such as `2026-02-31T10:00:00.000Z` cannot roll forward through JavaScript `Date` parsing into false payment approval dates. Verification: focused payment-confirmation test passed (`11 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo test/lint passed (`test` 237, lint), build initially hit a concurrent Next build lock and passed on targeted retry, and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `27ff97b4`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-566 | `[hanwoo-dashboard]` Guarded dashboard summary financial-series dates. `summary-service.js` now routes server-side dashboard summary sales/expense month aggregation through strict `YYYY-MM-DD` round-trip validation in `toMonthKey()`, so malformed or impossible dates such as `2026-02-31` cannot roll forward through JavaScript `Date` parsing and pollute cached dashboard financial series. Verification: focused analysis/summary source test passed (`3 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 237, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `5d5230a8`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-565 | `[hanwoo-dashboard]` Guarded analysis chart dates. `AnalysisTab` now routes monthly 매출/비용 chart aggregation dates through `toMonthKey()` with strict `YYYY-MM-DD` round-trip validation, so malformed or impossible sale/expense dates cannot roll forward through JavaScript `Date` parsing and pollute the annual analysis chart. Verification: focused analysis source test passed (`3 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 236, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `d03370f1`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-564 | `[hanwoo-dashboard]` Rejected impossible fallback monthly sales dates. `DashboardClient` now routes fallback monthly sales count dates through `toValidCalendarDate()`, so impossible sale-date strings such as `2026-02-31` cannot roll forward through JavaScript `Date` parsing and inflate the home monthly 출하 count. Verification: focused home/market source test passed (`25 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 236, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `82a855dd`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-563 | `[hanwoo-dashboard]` Tolerated partial market-price snapshots. `MarketPriceWidget` now normalizes incoming price snapshots so missing `bull` or `cow` side objects become empty objects before rendering, preventing partial cached/live market data from crashing the home price widget. Verification: focused home/market source test passed (`25 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo test/lint passed (`test` 236, lint), build initially hit a concurrent Next build lock and passed on targeted retry, and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `0c3247f9`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-562 | `[hanwoo-dashboard]` Guarded dashboard list cursor dates. `list-queries.js` now builds next-page cursor sort values through `toCursorSortValue()` and closes pagination gracefully when the last item has a malformed sort date, so corrupted `updatedAt`/`saleDate` values cannot throw through raw `new Date(lastItem[sortField]).toISOString()` and turn cattle/sales list pagination into a 500. Verification: focused dashboard list cursor date source test passed (`1 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 236, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `a76a1898`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-561 | `[hanwoo-dashboard]` Guarded market-price update timestamps. `MarketPriceWidget` now normalizes `fetchedAt` through `toValidUpdatedAt()` before setting the visible update time, so malformed market snapshot timestamps cannot render browser-default `Invalid Date` copy in the home price widget. Verification: focused home/market source test passed (`25 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 235, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `fa6b1903`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-560 | `[hanwoo-dashboard]` Guarded dashboard calving submit dates. `DashboardClient` now strict-validates the calving submit date before optimistic calf creation, so malformed or impossible 분만일 values cannot throw `RangeError: Invalid time value` through raw `new Date(calvingDate).toISOString()` or enqueue a false offline calving update. Verification: focused dashboard calving date submit source test passed (`1 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 235, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `d26302cf`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-559 | `[hanwoo-dashboard]` Rejected impossible cattle-detail breeding record dates. `CattleDetailModal` now parses 발정/수정 record dates through strict `YYYY-MM-DD` round-trip validation before storing `lastEstrus` or `pregnancyDate`, so impossible inputs such as `2026-02-31` cannot roll forward through JavaScript `Date` parsing into a false breeding record. Verification: focused cattle detail modal wiring test passed (`10 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 234, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `cf93524d`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |
| T-558 | `[hanwoo-dashboard]` Guarded cattle form submit date conversion. `CattleForm` now converts birth and purchase dates through `toIsoDateOrNull()` before submitting, so malformed date values cannot throw `RangeError: Invalid time value` through raw `new Date(...).toISOString()` calls; invalid required birth dates abort the submit path defensively. Verification: focused cattle form date submit source test passed (`1 passed`), targeted ESLint passed, path-limited `git diff --check` passed, full Hanwoo QC passed (`test` 234, lint, build), and staged `code_review_gate --json` passed (`risk_score 0.0`; cp949 reader-thread noise only). Commit `93705d02`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files. | Codex | 2026-05-21 |

- `safe`: can proceed immediately on a short "continue/go" command.
- `approval`: requires explicit user confirmation before execution.
- `safe` limit: at most 7 files changed, roughly 250 added lines, same feature/module boundary, and no package install, DB schema, auth/security, public API, stack, infra, or CI changes.
- If only `approval` tasks remain, stop and ask which one the user wants next.
