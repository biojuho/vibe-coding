# TASKS - AI Kanban Board

> Read this file at session start and update it before ending the session.

## TODO

| ID | Task | Owner | Priority | Auto | Created |
| --- | --- | --- | --- | --- | --- |
| T-251 | `[hanwoo-dashboard]` Run Prisma 7 live CRUD E2E once a real Supabase `DATABASE_URL` is configured. Root cause: Supabase password desynchronization. Retried on 2026-05-20, 2026-05-28 and again during an earlier continuation with `npm.cmd run db:prisma7-test -- --live`; local Prisma/client/adapter checks passed (`15 passed`) but live connection health still failed with Prisma `P2010`, raw code `XX000`, message `(ENOTFOUND) tenant/user postgres.fuemeqmigptwfzqvrpjf not found`. 2026-06-06 launch loops through T-1334 did **not** retry the live check; release authorization is a separate publish boundary, selector should remain `blocked_publish_only` while the branch is ahead of origin, readiness score should remain `96`, local blockers should remain `0`, and completion audit remains incomplete with explicit blockers for publish/Actions, release authorization, selector publish boundary, external/user-owned T-251, and direct Hanwoo readiness. **Action Required**: The user MUST manually reset the database password via the Supabase Dashboard (Project Settings > Database) to resynchronize the control plane pooler credentials, then update `.env` if it changes; rerun `npm.cmd run db:prisma7-test -- --live` immediately after. | User | Medium | approval | 2026-05-28 |


## IN_PROGRESS

| ID | Task | Owner | Started | Notes |
|---|---|---|---|---|


## DONE (Latest 5)

| ID | Task | Completed By | Completed |
|---|---|---|---|
| T-1335 | `[blind-to-x/notion]` Split Notion upload page assembly in `pipeline/notion/_upload.py` into focused helpers for upload memo construction, X publish properties, draft body properties, analysis properties, base page properties, initial media children, regulation status, and child block assembly while preserving `upload()` behavior. Added direct regression coverage for `_build_upload_properties()` preserving review/X/draft/analysis fields. Verification passed Notion upload pytest `43/43` with repo-local basetemp, ruff check, ruff format check, `py_compile`, `git diff --check`, blind-to-x project QC (`1744 passed, 9 skipped` plus lint), VibeDebt (`_upload.py` score `43.2 -> 34.4`, max complexity `24 -> 20`, max function length `128 -> 49`, project TDR `34.8 -> 34.6`), code-review gate advisory WARN `risk_score=0.40` covered by direct/project tests, graph refresh current at commit `a1c5a000`, and A/B `adopt_candidate` (`score_delta=0.25058404887338076`). Commit `a1c5a000` is local only. Release packet is currently `blocked_dirty_worktree` because unrelated `shorts-maker-v2` WIP remains unstaged. | Codex | 2026-06-06 |
| T-1334 | `[shorts-maker-v2/media]` Tightened `fallback_mixin.py` image fallback control flow by extracting `_try_image_provider()` and `_try_dalle_image()` so `_try_image_chain()` builds ordered Imagen/Gemini/Pollinations attempts while preserving DALL-E policy retry, stock-video fallback, cache, and placeholder behavior. Added regressions for skipped stock-mix attempts not recording failures and for stock-video recovery after image-provider failures. Verification passed focused media-step pytest `27/27`, related media fallback pytest `6/6`, ruff, format check, `py_compile`, `git diff --check`, full shorts-maker-v2 project QC (`1584 passed, 12 skipped, 1 warning` plus lint), VibeDebt (`fallback_mixin.py` score `40.5 -> 36.3`, max complexity `19 -> 13`, max function length `103 -> 93`, duplicate blocks `2 -> 2`, project TDR `33.47 -> 33.39`), code-review gate advisory WARN `risk_score=0.35` covered by focused/related/full project tests, and A/B `adopt_candidate` (`score_delta=1.3466666666666667`). Commit `bd2fa637` is local only. | Codex | 2026-06-06 |
| T-1333 | `[blind-to-x/content-intelligence]` Split 6D scoring and calibration in `pipeline/content_intelligence/scoring_6d.py` into focused helpers for freshness, social signal, hook strength, trend relevance, audience targeting, viral potential, weight loading, weighted score, source boost, calibration cutoff/rows, engagement values, dimension proxies, correlations, and normalized calibration weights while preserving `calculate_6d_score()` and `calibrate_weights()` behavior. Added direct regression coverage for weighted dimension scoring and source quality boost behavior. Verification passed focused pytest `6/6`, related unit pytest `52/52`, integration pytest `23/23`, ruff, format check, `py_compile`, `git diff --check`, blind-to-x project QC (`1743 passed, 9 skipped` plus lint), AST metrics (`scoring_6d.py` max complexity `22 -> 7`, max function length `143 -> 30`, `calculate_6d_score` `22/143 -> 5/27`, `calibrate_weights` `19/83 -> 3/30`), VibeDebt project TDR `34.97 -> 34.8`, code-review gate advisory WARN `risk_score=0.45` covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=0.6502586776752242`). Commit `7492047f` is local only. | Codex | 2026-06-06 |
| T-1332 | `[blind-to-x/content-intelligence]` Split editorial-fit scoring in `pipeline/content_intelligence/scoring_editorial.py` into focused signal, dimension, weighted-score, hard-reject, reason-label, and publishability brief helpers while preserving public scoring and publishability brief behavior. Added direct regression coverage for scoring dimensions and brief wiring. Verification passed focused pytest `5/5`, related pytest `74/74`, ruff, format check, `py_compile`, `git diff --check`, full blind-to-x project QC (`1742 passed, 9 skipped` plus lint), AST metrics (`scoring_editorial.py` max complexity `50 -> 8`, max function length `169 -> 49`), VibeDebt project TDR `34.97`, code-review gate advisory WARN `risk_score=0.45` covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=1.4935507`). Commit `c6bed861` is local only. | Codex | 2026-06-06 |
| T-1331 | `[shorts-maker-v2/script]` Split script prompt locale override handling in `pipeline/script_prompts.py` into focused normalizers/mergers for tone presets, CTA words, channel persona, persona keywords, prompt field names, and review criteria while preserving prompt/review behavior. Added malformed locale bundle regression coverage so invalid entries do not corrupt defaults and valid entries still apply. Verification passed focused script pytest `30/30` with one upstream Google GenAI deprecation warning, ruff, format check, `py_compile`, `git diff --check`, full shorts-maker-v2 project QC (`1582 passed, 12 skipped, 1 warning` plus lint), VibeDebt (`script_prompts.py` score `40.6 -> 34.9`, max complexity `39 -> 11`, max function length `99 -> 93`, project TDR `33.53 -> 33.47`), code-review gate advisory WARN `risk_score=0.45` covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=0.2579619`). Commit `d4ca6be4` is local only. | Codex | 2026-06-06 |
| T-1330 | `[knowledge-dashboard/sync]` Split NotebookLM fetch token/client/payload helpers in `scripts/sync_data.py` while preserving sync behavior. Added fake-client regression coverage for payload simplification, empty NotebookLM source lists, and client credential argument wiring. Verification passed Knowledge sync pytest `16/16`, ruff, format check, `py_compile`, `git diff --check` (CRLF warnings only), Knowledge project QC (`61 passed` plus lint/build/smoke), VibeDebt (`sync_data.py` score `21.0 -> 12.0`, max complexity `11 -> 9`, max function length `50 -> 45`, project TDR `25.06 -> 20.88`), code-review gate advisory WARN `risk_score=0.55` covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=0.2237566`). Commit `da6381c1` is local only. | Codex | 2026-06-06 |
| T-1329 | `[shorts-maker-v2/tts]` Split TTS provider routing and fallback guards in `providers/tts_factory.py` while preserving behavior. Added direct regressions for premium provider success, premium failure stale-output unlink plus Edge fallback, OpenAI signature routing, and missing OpenAI client failure. Verification passed focused TTS/provider pytest `17/17` with one upstream Google GenAI deprecation warning, ruff, format check, `py_compile`, `git diff --check`, full shorts-maker-v2 QC (`1581 passed, 12 skipped, 1 warning` plus lint), and A/B `adopt_candidate` (`score_delta=1.5712114`). | Codex | 2026-06-06 |
| T-1328 | `[knowledge-dashboard/sync]` Split NotebookLM token-template detection helpers in `scripts/sync_data.py`. Added script/app regressions for non-string cookie values and identity placeholder fields so template credentials are treated as unavailable instead of real NotebookLM identity. Verification passed focused Knowledge sync pytest `15/15`, ruff, format check, `py_compile`, `git diff --check`, full Knowledge project QC (`61 passed` plus lint/build/smoke), and A/B `adopt_candidate` (`score_delta=1.1957691`). | Codex | 2026-06-06 |
| T-1327 | `[blind-to-x/process]` Split `generate_review_stage.py` into focused helpers while preserving orchestration behavior. `run_generate_review_stage()` now delegates missing-generator failure, screenshot upload, draft splitting, Twitter reply fallback, generation failure, quality-gate retries, post-generation components, Twitter quality failure, and completion; the main flow shrank `230 -> 47` lines and branch points `46 -> 7`, with private helpers `4 -> 18`. Added regression coverage for readable Korean Twitter reply fallback copy. Verification passed process-stage pytest `42/42`, full blind-to-x project QC (`1740 passed, 9 skipped` plus lint), ruff, format check, `py_compile`, `git diff --check`, code-review gate advisory WARN (`risk_score=0.30`) covered by direct/full tests, and A/B `adopt_candidate` (`score_delta=1.0158915`). | Codex | 2026-06-06 |
| T-1326 | `[workspace/github]` Added a local release-workflow definition guard to `workspace/tests/test_github_workflow_hygiene.py`. The test now verifies that the two required publish gates, `root-quality-gate` and `active-project-matrix`, are defined in local workflow YAML with matching `name:` values and both `push` and `pull_request` triggers, preventing workflow name/trigger drift before the explicit push authorization boundary. Verification passed workflow hygiene pytest `3/3`, release-boundary related pytest `67/67`, ruff, format check, `py_compile`, `git diff --check`, staged code-review gate advisory WARN (`risk_score=0.30`) with no impacted nodes for the staged workflow-test file, and A/B `adopt_candidate` (`score_delta=2.294872`). | Codex | 2026-06-06 |
| T-1325 | `[workspace/auto-research]` Added direct Knowledge Dashboard target-product readiness evidence to `launch_objective_audit.py`. Knowledge launch readiness now requires score `100`, ready state, nonstale QC `PASS`, README/package/page artifacts, env checks complete, no unresolved tasks, and no dirty target paths; regressions cover complete and stale/failing evidence paths. Verification passed focused launch/completion tests `45/45`, ruff, format check, `py_compile`, current graph refresh, code-review gate advisory WARN `risk_score=0.40` covered by direct tests, A/B `adopt_candidate` (`score_delta=0.918273`), release packet `ready_for_authorization`, selector `blocked_publish_only`, readiness score `96`, and completion audit `9/14` complete with 5 blocked issues. | Codex | 2026-06-05 |
| T-1324 | `[workspace/auto-research]` Refined launch-audit graph freshness handling. `launch_objective_audit.py` now blocks stale code-review graph evidence only when graph-relevant files changed since the graph build, or when the stale range cannot be inspected; context-only stale ranges no longer create false launch blockers. Verification passed focused launch-audit tests `39/39`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=1.232601`), full active-project QC refresh (blind-to-x `1739 passed, 9 skipped`; shorts-maker-v2 `1577 passed, 12 skipped, 1 warning`; Hanwoo `500 passed` plus lint/build/smoke; Knowledge `61 passed` plus lint/build/smoke), readiness score `96`, release packet `ready_for_authorization`, selector `blocked_publish_only`, and completion audit `8/13` complete with 5 blocked issues. | Codex | 2026-06-05 |
| T-1323 | `[workspace/readiness]` Tracked canonical project-QC artifact git freshness. `project_qc_runner.py` schema `3` now records artifact branch/head/dirty metadata, and `product_readiness_score.py` compares the artifact head with the live head using relevant project/global QC paths so `.ai` context-only commits do not stale valid full-QC evidence. Verification passed readiness/QC focused tests `44/44`, ruff, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=1.503968`), and the final full active-project QC refresh. | Codex | 2026-06-05 |
| T-1322 | `[workspace/auto-research]` Added current-code triage evidence to launch-objective audits. Dependency/code freshness evidence now includes `session_orient.py --json` graph freshness plus `code_review_gate.py` review-gate output, so current-code staleness and nonpassing gate states are visible in completion audits. Verification passed focused launch-audit tests `37/37`, related auto-research tests `56/56`, runtime evidence generation, and A/B `adopt_candidate` (`score_delta=1.155124`). | Codex | 2026-06-05 |
| T-1321 | `[blind-to-x/scrapers]` Extracted Ppomppu post helpers. `PpomppuScraper.scrape_post()` now delegates post fetch failure classification, route fulfillment, post container discovery, direct-navigation fallback, title/content/count/image extraction, and screenshot saving into focused helpers while preserving behavior. Added regressions for comment-span title cleanup, content selector fallback, comment-line count fallback, image URL normalization/filtering, and fetch-failure reason preservation. Verification passed Ppomppu tests `11/11`, related scraper tests `43/43`, full blind-to-x QC `1739 passed, 9 skipped` plus lint, ruff, format check, `py_compile`, `git diff --check`, VibeDebt (`ppomppu.py` debt `44.8 -> 40.9`, max complexity `53 -> 19`, `scrape_post` complexity `53 -> 9`, length `255 -> 85`), code-review gate advisory WARN `risk_score=0.50` covered by direct/full tests, and A/B `adopt_candidate` (`score_delta=1.827920`). | Codex | 2026-06-05 |
| T-1320 | `[blind-to-x/scrapers]` Extracted Ppomppu scraper feed helpers. `PpomppuScraper` now delegates feed URL selection, feed navigation, row title/comment/view/like/image extraction, feed candidate construction, and feed candidate collection into focused helpers while preserving ranking, dedupe, and metadata behavior. Added regression coverage for candidate sort/filter/dedupe plus image metadata. Verification passed Ppomppu scraper tests `19/19`, related scraper tests `71/71`, ruff, format check, `py_compile`, `git diff --check`, staged code-review gate advisory WARN `risk_score=0.45` covered by direct tests, and A/B `adopt_candidate` (`score_delta=2.314408`). | Codex | 2026-06-05 |
| T-1319 | `[blind-to-x/scrapers]` Extracted Blind scraper feed/post helpers. `BlindScraper` now delegates feed URL selection, feed row metadata, feed candidate construction/collection, post content/count extraction, fetch failure classification, navigation, container discovery, Crawl4AI fallback handling, screenshot saving, and category keyword matching into focused helpers while preserving behavior. Added regressions for feed candidate engagement sorting/deduplication and category fallback coverage. Verification passed graph detect-changes `risk_score=0.0`, Blind scraper tests `11/11`, related scraper tests `75/75`, full blind-to-x QC `1733 passed, 9 skipped` plus lint, ruff, format check, `py_compile`, `git diff --check`, staged code-review gate advisory WARN `risk_score=0.65` covered by direct/full tests, and A/B `adopt_candidate` (`score_delta=0.528523`). | Codex | 2026-06-05 |
| T-1318 | `[blind-to-x/scrapers]` Extracted selector repair helpers from `BaseScraper._suggest_selectors()`. Selector repair now delegates selector part scoring, scored dedupe, unique append, class fallback, similar-id fallback, and tag content fallback into focused helpers while preserving candidate order and max-3 behavior. Added regressions for duplicate class/id/tag candidates and fallback ordering. Verification passed focused selector/scraper tests `46/46`, ruff check, format check, `py_compile`, VibeDebt audit (`base.py` debt `43.1 -> 43.0`, `_suggest_selectors` complexity `35 -> 7`, length `92 -> 50`), `git diff --check`, code-review gate advisory WARN `risk_score=0.40` covered by direct tests, full blind-to-x project QC `1732 passed, 9 skipped` plus lint, and A/B `adopt_candidate` (`score_delta=0.693839`). | Codex | 2026-06-05 |
| T-1317 | `[blind-to-x/draft-generator]` Extracted best-of-N draft generation helpers. `generate_drafts()` now delegates cache writes, recent-failure provider filtering, candidate generation, and reviewer selection into focused helpers while preserving behavior. Added regressions for selected comment-trigger persistence, single-candidate cache writes, and reviewer-failure fallback cache writes. Verification passed project `.venv` best-of-N tests `15/15`, related draft generation tests `36/36`, blind-to-x project QC `1729 passed, 9 skipped` plus lint, ruff, format check, `py_compile`, `git diff --check`, code-review gate advisory WARN `risk_score=0.40` covered by direct tests, and A/B `adopt_candidate` (`score_delta=0.773171`). | Codex | 2026-06-05 |
| T-1316 | `[workspace/session-orient]` Surfaced code-review graph freshness in session orientation. `session_orient.py --json` now compares graph `built_at_commit` with the current git head and reports `current_head`, `freshness`, `stale`, and `stale_reason`; text/Rich diagnostics show stale or unknown graph freshness so agents do not treat old graph context as current launch evidence. Verification passed focused session-orient tests `33/33`, ruff, format check, `py_compile`, `git diff --check`, runtime JSON stale-graph evidence, staged code-review gate advisory WARN `risk_score=0.60` covered by direct tests, and A/B `adopt_candidate` (`score_delta=1.472549`). | Codex | 2026-06-05 |
| T-1315 | `[workspace/qc]` Preserved full-workspace project-QC readiness evidence. Partial/focused `project_qc_runner.py` runs now write `.tmp/project_qc_runner_partial_latest.json` instead of overwriting `.tmp/project_qc_runner_latest.json` unless every active project has complete check coverage, preventing focused QC from degrading launch readiness evidence. Verification passed project-QC runner tests `14/14`, ruff, format check, `py_compile`, `git diff --check`, full active-project QC, and A/B `adopt_candidate` (`score_delta=0.993639`). | Codex | 2026-06-05 |
| T-1314 | `[blind-to-x/notion]` Consolidated Notion review metric/fact/emotion formatting helpers in `_upload.py` while preserving reviewer-facing output. Added focused optional-signal regression coverage in `test_notion_upload.py`. Verification passed Notion upload tests `42/42`, related Notion review/schema tests `51/51`, ruff, format check, `py_compile`, `git diff --check`, full blind-to-x QC (`1726 passed, 9 skipped` plus lint), and A/B `adopt_candidate` (`score_delta=1.346602`). | Codex | 2026-06-05 |
| T-1313 | `[workspace/auto-research]` Made the current-head release authorization packet a direct launch-objective audit item. Completion audits now expose clean-ahead publish authorization as first-class evidence instead of relying only on selector/proxy signals. Verification passed focused release/launch audit tests, related auto-research tests, ruff, format check, `py_compile`, `git diff --check`, runtime audits, and A/B `adopt_candidate` (`score_delta=0.981177`). | Codex | 2026-06-05 |
| T-1312 | `[workspace/auto-research]` Added a deterministic release authorization packet for clean-ahead current HEADs and made publish-only selector state explicit. `release_authorization_packet.py` reports dirty/synced/authorization-ready states without pushing, includes ahead commits, required workflow status, suggested push command only when clean, and guardrails. `next_experiment_selector.py` now emits `blocked_publish_only` for `current_head_release_checks_unproven` and requires the packet before push authorization; dirty worktrees route back to GitHub inventory follow-up. `launch_objective_audit.py` now includes direct `hanwoo-dashboard` readiness evidence so T-251 is a complete-coverage target blocker. Required workflow test/ruff lists include the new packet script/test. Verification passed focused selector/launch/completion/release-packet pytest `51/51`, workflow-hygiene plus packet pytest `6/6`, ruff, format check, `py_compile`, `git diff --check`, release packet `ready_for_authorization`, selector `blocked_publish_only`, launch completion audit `8/12` complete with 4 blocked issues, A/B `adopt_candidate` (`score_delta=0.998026`), and code-review gate advisory WARN `risk_score=0.40` covered by direct tests. | Codex | 2026-06-05 |
| T-1311 | `[workspace/auto-research]` Classified current-head publish/Actions boundaries in launch audits as complete evidence with explicit blockers instead of partial evidence gaps. Clean-state launch audit now reports publish-boundary readiness and selector items with `coverage=complete` while completion audit remains `incomplete` for the actual blockers: publish/Actions, selector publish boundary, and external/user-owned T-251. Verification passed launch+completion pytest `34/34`, auto-research related pytest `44/44`, ruff, `py_compile`, `git diff --check`, code-review gate advisory WARN `risk_score=0.35` covered by direct tests, clean-state launch/completion audits, and A/B `adopt_candidate` (`score_delta=0.695612`). | Codex | 2026-06-05 |
| T-1310 | `[workspace/readiness]` Prioritized the current-head publish/Actions blocker in `product_readiness_score.py` `next_actions` before user-owned project waits. Clean-state readiness remains score `96`, `local_blocker_count=0`, `publish_blocker_count=1`, and the first next action now requires explicit push authorization/user push plus current-head Actions before the Hanwoo T-251 wait. Verification passed product-readiness pytest `28/28`, readiness+selector pytest `38/38`, ruff, `py_compile`, `git diff --check`, code-review gate `pass`, runtime readiness output, and A/B `adopt_candidate` (`score_delta=0.725932`). | Codex | 2026-06-05 |
| T-1309 | `[workspace/session-orient]` Added a clean-ahead relay closeout note. `session_orient.py` now keeps clean-but-unpushed HANDOFF closeout priorities `ok` while adding `latest_next_priority_note` when a "commit this relay update" step is already satisfied locally but publish/CI/user blockers still remain. This prevents continuation agents from repeating completed relay commits while preserving push/Actions/T-251 boundaries. Verification passed focused session-orient pytest `30/30`, broader session/selector/launch pytest `68/68`, ruff, pre-commit ruff format check, `py_compile`, `git diff --check`, staged code-review gate advisory WARN `risk_score=0.55` covered by direct tests, and A/B `adopt_candidate` (`score_delta=0.949127`). | Codex | 2026-06-05 |
| T-1308 | `[workspace/auto-research][shorts-maker-v2]` Added a launch-audit guard for the `shorts-maker-v2` feature acceptance checklist. `launch_objective_audit.py` now parses Markdown checkbox items and requires `projects/shorts-maker-v2/FEATURE.md` to contain a non-empty, fully closed checklist before direct target-product readiness can be complete; `FEATURE.md` now records implemented status and marks all seven acceptance criteria complete. Runtime dirty launch audit evidence showed `shorts-maker-v2 FEATURE checklist complete 7/7, open=0`, and regression coverage proves open checklist items block completion. Verification passed focused launch-audit pytest `28/28`, broader auto-research/readiness pytest `69/69`, full active-project QC (blind-to-x `1723 passed, 9 skipped`; shorts-maker-v2 `1577 passed, 12 skipped`; Hanwoo `500/500` plus lint/build/smoke; Knowledge `61/61` plus lint/build/smoke), ruff, `py_compile`, `git diff --check`, staged code-review gate `risk_score=0.0`, and A/B `adopt_candidate` (`score_delta=0.994212`). | Codex | 2026-06-05 |
| T-1307 | `[workspace/launch][hanwoo-dashboard][auto-research]` Hardened public credential surfaces and launch selector snapshot consistency. Hanwoo seed now requires configured `ADMIN_PASSWORD` with length `>= 12` instead of seeding `admin/admin123`; the login page no longer exposes demo credentials and now shows an operator credential notice; `launch_objective_audit.py` derives next-experiment selection from the same collected readiness/GitHub/browser/dependency snapshot instead of launching a second snapshot subprocess. Verification passed Hanwoo Node tests `500/500`, Hanwoo project QC test/lint/build/smoke, focused auto-research pytest `37/37`, ruff, format check, `py_compile`, `git diff --check`, browser `/login` click QA with demo credentials absent and console errors/warnings `0/0`, and A/B `adopt_candidate` (`score_delta=1.124169`). | Codex | 2026-06-05 |
| T-1306 | `[workspace/readiness][auto-research]` Classified clean ahead-of-origin current-head Actions as a publish blocker instead of a local code blocker. `product_readiness_score.py` now reports GitHub release `ahead_count`, `publish_required`, `blocker_type=publish`, and overall `publish_blocker_count`, so a clean unpublished HEAD with missing required Actions keeps `local_blocker_count=0` while still blocking launch completion. `launch_objective_audit.py` now records `workspace/local/publish/agent blockers=...` evidence. Verification passed focused product-readiness + launch-audit pytest `53/53`, broader auto-research/readiness pytest `67/67`, active-project QC evidence complete in readiness, ruff, format check, `py_compile`, `git diff --check`, and A/B `adopt_candidate` (`score_delta=0.998045`). | Codex | 2026-06-05 |
| T-1305 | `[workspace/auto-research][shorts-maker-v2]` Added direct `shorts-maker-v2` target launch readiness evidence to the launch-objective audit and ignored Playwright MCP local artifacts. `launch_objective_audit.py` now includes a `shorts-maker-v2` target item that requires readiness score `100`, state `ready`, QC `PASS`, launch docs present, env checks passing, no unresolved tasks, and no dirty target paths. Runtime audit shows `shorts-maker-v2` complete with QC `1577 passed`, `12 skipped`, docs `5/5`, env `1/1`, tasks `0`, dirty paths `0`. `.gitignore` now ignores `.playwright-mcp/`, and `next_experiment_selector.py` now reports a clean ahead-of-origin branch with missing required Actions as `current_head_release_checks_unproven` blocked on explicit push authorization/user push plus current-head Actions. Verification passed focused auto-research pytest `35/35`, broader auto-research/GitHub pytest `76/76`, ruff, format check, `py_compile`, Hanwoo Node tests `499/499`, `shorts-maker-v2` QC (`1577 passed`, `12 skipped`, lint passed), `git diff --check`, `git check-ignore`, A/B `adopt_candidate` (`score_delta=1.285459`), and code-review gate exit `0` with advisory WARN `risk_score=0.4`. | Codex | 2026-06-05 |
| T-1304 | `[workspace/auto-research]` Refreshed active launch evidence and reconfirmed the external-blocker boundary without retrying Hanwoo T-251. Current evidence: `session_orient.py --json` clean/synced `main` at `a6535544`, open PRs `0`, current-head `root-quality-gate` and `active-project-matrix` success; `product_readiness_score.py --json` score `96` with workspace/local/agent blockers `0/0/0` and external blocker count `1`; GitHub inventory found 7 projects, 3 workflows, open PRs `0`, dirty count `0`; browser QA inventory reports coverage `4/4` and fresh usable/nonblank screenshots `4/4`; dependency freshness reports direct candidates `0` with only peer-blocked ESLint 10 major migrations deferred; selector returned `blocked_external_only` for T-251; launch completion audit remains `8/9` complete with only T-251 blocked. `py -3.13 execution\code_review_gate.py --base HEAD~1 --json` passed with `risk_score=0.0`. | Codex | 2026-06-05 |
| T-1303 | `[workspace/auto-research]` Exposed next-experiment selector required gates in launch-objective audit evidence. `launch_objective_audit.py` now carries `selected.required_gates` from `next_experiment_selector.py` into the selector requirement evidence as `Selector required gates: ...`, so launch audits preserve the exact resume gates for T-251 and local selector candidates directly. Regression coverage proves required-gate evidence is emitted alongside selector guardrails and normalized action punctuation. Verification passed focused launch audit pytest `21/21`, broader auto-research launch/completion/selector pytest `34/34`, ruff, format check, `py_compile`, `git diff --check`, runtime selector required-gate evidence, A/B `adopt_candidate` (`score_delta=0.9881422924901185`), T-1303 completion audit `complete` (`3/3`), code-review gate exit `0` with advisory WARN (`risk_score=0.4`) covered by direct tests, and required Actions on `dc0d11e8` passed (`root-quality-gate` `26997046879`, `active-project-matrix` `26997046881`). | Codex | 2026-06-05 |
| T-1302 | `[workspace/auto-research]` Exposed next-experiment selector guardrails in launch-objective audit evidence. `launch_objective_audit.py` now sentence-normalizes selector action evidence so trailing punctuation is not duplicated, and carries `selected.guardrails` into the selector requirement evidence so launch audits preserve the T-251 retry boundary directly. Regression coverage proves the T-251 action renders with one terminal punctuation mark and multi-entry guardrails are emitted as operator-readable evidence. Verification passed focused launch audit pytest `21/21`, broader auto-research launch/completion/selector pytest `34/34`, ruff, format check, `py_compile`, `git diff --check`, runtime launch audit guardrail evidence, A/B `adopt_candidate` (`score_delta=0.9849618229539489`), T-1302 completion audit `complete` (`3/3`), code-review gate exit `0` with advisory WARN (`risk_score=0.4`) covered by direct tests, and required Actions on `ae9d9e59` passed. | Codex | 2026-06-05 |
| T-1301 | `[workspace/auto-research]` Added live next-experiment selector-state evidence to launch-objective audits. `launch_objective_audit.py` now collects `next_experiment_selector.py --root . --json`, inserts a selector requirement item, treats clean `blocked_external_only` as complete evidence without duplicating the user-owned T-251 blocker, and treats local selector candidates such as `local_readiness_blocker` as completion blockers. Regression coverage locks external-only no-duplicate behavior and local-candidate blocking. Verification passed focused launch audit pytest `20/20`, broader auto-research launch/completion/selector pytest `33/33`, ruff, format check, `py_compile`, `git diff --check`, runtime launch audit selector evidence, A/B `adopt_candidate` (`score_delta=0.9910837857979347`), T-1301 completion audit `complete` (`4/4`), code-review gate exit `0` with advisory WARN (`risk_score=0.35`) covered by direct tests, and required Actions on `43bd0f8e` passed. | Codex | 2026-06-05 |
| T-1300 | `[workspace/auto-research]` Stabilized launch-objective A/B manifest evidence selection. `launch_objective_audit.py` now extracts T-IDs from local `.tmp/ab-manifest-*.json` filenames/manifest fields, ranks task-id-bearing artifacts by highest T-ID before falling back to mtime/name, and supports `--ab-manifest` so final launch audits can pin the intended A/B evidence. Regression coverage proves `ab-manifest-t1300.json` wins over a newer-touched `ab-manifest-t1299.json`, and explicit override can select `.tmp/ab-manifest-t1299.json`. Verification passed focused launch audit pytest `18/18`, broader auto-research launch/completion/selector pytest `31/31`, ruff, format check, `py_compile`, `git diff --check`, code-review gate `pass` (`risk_score=0.0`), A/B `adopt_candidate` (`score_delta=1.2387226869238648`), T-1300 completion audit `complete` (`4/4`), runtime launch audit with T-1300 metrics `5` and gates `8/8`, and required Actions on `b59e3006` passed. | Codex | 2026-06-05 |
| T-1299 | `[workspace/session-orient]` Refined stale latest-HANDOFF `Next Priorities` detection with current-HEAD CI awareness. T-1298's git-only rule could mark a post-push closeout instruction stale while `active-project-matrix` was still running on the clean/synced head. `session_orient.py` now requires git clean/synced **and** current-head `root-quality-gate` plus `active-project-matrix` success before stale post-push closeout priority warnings fire. Added current-head SHA prefix matching and regressions for green required checks, in-progress required checks, and clean/synced closeout priorities that remain actionable until release checks are green. Verification passed focused session-orient pytest `28/28`, broader session/auto-research audit pytest `57/57`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.8818502202643175`), completion audit `complete` (`4/4`), and required Actions on `0bac5859` (`root-quality-gate` `26994295341`, `active-project-matrix` `26994295347`). | Codex | 2026-06-05 |
| T-1298 | `[workspace/session-orient]` Added stale latest-HANDOFF `Next Priorities` detection for already-finished post-push closeout work. `session_orient.py` now extracts the newest addendum's `Next Priorities` cell, computes `latest_next_priority_status`, and reports `stale_next_priority_reason` when git is clean/synced but the relay still asks agents to commit/push/rerun post-push closeout. Dirty or ahead/behind worktrees keep such closeout instructions actionable, avoiding false positives during an active closeout. Text and Rich renderers surface the warning. Runtime proof after feature push reported clean/synced `main` and `latest_next_priority_status=stale` for the prior stale T-1297 closeout instruction. Verification passed focused session-orient pytest `25/25`, broader auto-research selector/launch/session pytest `50/50`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.9451472980300019`), and required Actions on `d7719bbf` (`root-quality-gate` `26994128473`, `active-project-matrix` `26994128458`). | Codex | 2026-06-05 |
| T-1297 | `[workspace/auto-research]` Hardened skill evidence linting and selector input-artifact routing. `skill_lint.py` now treats generated `input/` and `output/` artifact examples as non-bundled workflow files, clearing the false broken-reference warnings in `nature-paper2ppt`; accessibility and SEO docs no longer link to unavailable local skill folders, and the Shorts Maker V2 TTS skill no longer points at a stale unit-test path. `next_experiment_selector.py` now routes missing, unreadable, invalid, non-object, or schema-incomplete readiness/GitHub/browser/dependency evidence to an explicit `input_evidence_unavailable` candidate instead of falling through to completion audit, with UTF-16 JSON artifact support for Windows resilience. Verification passed skill-lint pytest `9/9`, focused selector+skill-lint pytest `18/18`, broader auto-research/GitHub/skill-lint pytest `75/75`, runtime skill lint `pass` score `100`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=1.154267543156432`), and T-1297 completion audit `complete` (`4/4`). Feature commit `7984696c` is pushed; required Actions passed on that head (`root-quality-gate` `26993403449`, `active-project-matrix` `26993403451`). | Codex | 2026-06-05 |
| T-1296 | `[workspace/auto-research]` Added deterministic next-experiment selection and richer blocked-audit evidence for the launch loop. `next_experiment_selector.py` now ranks local readiness blockers, GitHub follow-ups, browser QA refreshes, dependency candidates, stale QC refreshes, and external/user-owned blockers before a new cycle is chosen. Regression coverage locks clean external-only T-251 to `blocked_external_only` with selected blocker `T-251` and the guardrail not to retry until Supabase Dashboard credentials are reset; runtime selector during the candidate dirty worktree selected `local_readiness_blocker` while preserving T-251 as a blocked ranked candidate. `SKILL.md` documents the selector, `launch_objective_audit.py` now treats it as the 8th required auto-research artifact, `completion_audit.py` now includes requirement and blocker details on blocked issues, and root CI workflow slices include the selector script/test. Verification passed selector pytest `6/6`, completion audit pytest `4/4`, launch audit pytest `16/16`, broader auto-research/GitHub pytest `63/63`, ruff, format check, `py_compile`, runtime selector, runtime launch audit, A/B `adopt_candidate` (`score_delta=1.468470559113834`), T-1296 completion audit `complete` (`4/4`), and `git diff --check` with CRLF warnings only. Feature commit `4a1d1dbc` is pushed; required Actions passed on that feature head (`root-quality-gate` `26992731140`, `active-project-matrix` `26992731162`). Final clean selector returns `blocked_external_only`, final readiness is score `96` with workspace/local/agent blockers `0/0/0`, and launch-objective completion audit remains `7/8` complete with only external/user-owned T-251 blocked. | Codex | 2026-06-05 |
| T-1295 | `[workspace/auto-research]` Hardened launch-loop resumability and browser QA evidence validity. `.ai/GOAL.md` now points at the active product-launch auto-research objective instead of the stale completed Hanwoo quality goal, and `launch_objective_audit.py` now requires `.ai/GOAL.md` to exist, use an active launch-loop status, and mention the current product-launch/auto-research objective. `browser_qa_inventory.py` now validates retained PNG screenshots, detects blank screenshots, records width/height and valid/usable/nonblank counts, and recommends refreshing invalid or blank screenshots. `launch_objective_audit.py` now requires fresh usable and fresh nonblank screenshot coverage before claiming browser QA complete. Runtime browser inventory reports 4/4 browser projects with fresh valid usable nonblank screenshots; runtime launch audit reports fresh usable/nonblank 4/4 and GOAL mapped true. Verification passed launch audit pytest `16/16`, browser inventory pytest `13/13`, broader auto-research/GitHub pytest `57/57`, ruff, format check, `py_compile`, runtime browser inventory, runtime launch audit, A/B `adopt_candidate` (`score_delta=0.9822995235306066`), T-1295 completion audit `complete` (`3/3`), and `git diff --check` with CRLF warnings only. Feature commit `2c5ad69e` is pushed and required Actions passed on that head (`root-quality-gate` `26992093689`, `active-project-matrix` `26992093691`). Full launch completion remains blocked only by external/user-owned T-251. | Codex | 2026-06-05 |
| T-1294 | `[workspace/auto-research]` Added latest npm peer metadata evidence to dependency freshness and launch audit routing. `dependency_freshness_inventory.py` now checks latest `version` and `peerDependencies` for installed peer blockers on deferred major migrations, annotates each blocker with latest peer support fields, and summarizes latest-supported/blocked/unavailable blocker counts. Recommendations now expose those counts, and `launch_objective_audit.py` includes them in dependency evidence. Runtime inventory reports direct dependency candidates `0`, outdated count `3`, deferred dependencies `2`, latest peer support `1`, latest blocked `6`, unavailable `0`; Hanwoo ESLint 10 has `1/4` latest-supported blockers while Knowledge ESLint 10 remains `0/3`. Verification passed dependency freshness pytest `11/11`, launch-objective pytest `14/14`, broader auto-research pytest `53/53`, ruff, format check, `py_compile`, runtime dependency inventory, launch-objective audit generation, A/B `adopt_candidate` (`score_delta=0.9919931369745496`), and T-1294 completion audit `complete` (`3/3`). Post-push closeout confirmed clean/synced state, open PRs `0`, required Actions green (`root-quality-gate` `26991455712`, `active-project-matrix` `26991455721`), readiness score `96`, workspace/local/agent blockers `0/0/0`, and launch-objective audit `7/8` complete with only external/user-owned T-251 blocked. | Codex | 2026-06-05 |
| T-1293 | `[workspace/auto-research]` Hardened GitHub inventory dirty-state confirmation for the launch loop. `github_project_inventory.py` now refreshes the Git index before status collection, preserves raw `status_dirty_count`, confirms tracked dirty status with `git diff --quiet` and `git diff --cached --quiet`, reports confirmed `dirty_paths`, and labels `dirty_confirmation` so stale tracked status does not become a false launch-audit workspace blocker while untracked files still count as real dirty work. Regression coverage locks stale tracked status cleanup, real tracked dirty preservation, and untracked evidence preservation. Verification passed focused GitHub inventory pytest `12/12`, broader auto-research pytest `51/51`, ruff, format check, `py_compile`, runtime GitHub inventory generation, `git diff --check` with CRLF warnings only, A/B `adopt_candidate` (`score_delta=0.9966832504145935`), and T-1293 completion audit `complete` (`3/3`). Post-push closeout confirmed clean/synced state, open PRs `0`, required Actions green (`root-quality-gate` `26990896929`, `active-project-matrix` `26990896922`), readiness score `96`, browser QA fresh `4/4`, direct dependency candidates `0`, and launch-objective audit `7/8` complete with only external/user-owned T-251 blocked. | Codex | 2026-06-05 |
| T-1292 | `[workspace/auto-research]` Hardened shared relay and inventory evidence for the continuous launch loop. `session_orient.py` now checks the newest HANDOFF addendum for explicit `current ... head ... <sha>` claims, compares them with live git head evidence, and exposes `latest_head_claims`, `stale_head_claims`, `current_head`, and `head_claim_status` in JSON plus a text advisory line, catching the T-1291-style stale relay wording after context commits. `github_project_inventory.py` and `browser_qa_inventory.py` now support `--output` to persist ASCII-safe JSON evidence files while preserving stdout behavior, and the auto-research skill documents the retained output paths. Verification passed focused pytest `42/42`, ruff, format check, `py_compile`, runtime generation of `.tmp/github-project-inventory-t1292.json` and `.tmp/browser-qa-inventory-t1292.json`, ASCII-safe output checks, runtime stale-claim detection, A/B `adopt_candidate` (`score_delta=1.1248447204968943`), and T-1292 completion audit `complete` (`3/3`). Post-push closeout confirmed clean/synced state, open PRs `0`, required Actions green (`root-quality-gate` `26990435906`, `active-project-matrix` `26990435930`), readiness score `96`, and launch-objective audit `7/8` complete with only external/user-owned T-251 blocked. | Codex | 2026-06-05 |
| T-1291 | `[workspace/auto-research]` Clarified dependency freshness evidence in the launch-objective audit. `launch_objective_audit.py` now includes `outdated_dependency_count` in the dependency item and reports `current_prerelease_channel` packages such as Hanwoo `next-auth` beta with channel/current/stable-latest versions, explaining why npm outdated count can exceed deferred major migrations without implying a direct upgrade candidate. Regression coverage verifies the `next-auth` beta evidence while keeping dependency coverage complete and blockers empty. Verification passed focused launch audit pytest `14/14`, broader launch/completion audit pytest `18/18`, ruff, format check, `py_compile`, `git diff --check`, code-review gate, runtime launch audit generation with `outdated_dependency_count=3` plus `next-auth beta=5.0.0-beta.31`, A/B `adopt_candidate` (`score_delta=0.705308775731311`), and T-1291 completion audit `complete` (`3/3`). Feature commit `6fbc74ae` is pushed and required Actions passed on that head (`root-quality-gate` `26989894129`, `active-project-matrix` `26989894138`). | Codex | 2026-06-05 |
| T-1290 | `[workspace/auto-research]` Hardened launch-objective A/B manifest handling against unreadable and BOM-encoded local artifacts. `launch_objective_audit.py` now wraps manifest `stat()` access in `_ab_manifest_sort_key()`, ignores unreadable `.tmp/ab-manifest-*.json` files with explicit evidence, reads local manifest JSON with `utf-8-sig`, and still selects the newest readable valid manifest. `ab_decision.py` also reads manifests with `utf-8-sig` so Windows/PowerShell-generated BOM JSON does not break A/B adoption. Regression coverage verifies stat-failure selection, BOM launch-audit evidence, and BOM A/B decision parsing. Verification passed focused A/B/launch/completion audit pytest `18/18`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.847887323943662`), T-1290 completion audit `complete` (`3/3`), runtime candidate launch audit generation with BOM-written `.tmp/ab-manifest-t1290.json` cited in A/B evidence with candidate metrics `5` and required gates `4/4`, and required Actions passed on feature commit `f3391ffe` (`root-quality-gate` `26989496985`, `active-project-matrix` `26989497031`). | Codex | 2026-06-05 |
| T-1289 | `[workspace/auto-research]` Hardened launch-objective A/B evidence so the audit inspects the newest valid local `.tmp/ab-manifest-*.json` instead of relying only on `.ai` relay prose. The A/B launch item now includes the latest manifest artifact, records candidate metric count plus required-gate pass count, and keeps coverage partial when candidate metrics are missing or required gates fail. Regression coverage in `test_auto_research_launch_objective_audit.py` locks local manifest inclusion and failed-gate blocking. Verification passed focused launch audit pytest `11/11`, broader launch/completion audit pytest `15/15`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.6666666666666666`), T-1289 completion audit `complete` (`4/4`), and runtime candidate launch audit generation with `.tmp/ab-manifest-t1289.json` cited in the A/B item evidence with required gates `7/7`. | Codex | 2026-06-05 |
| T-1288 | `[workspace/auto-research]` Improved launch-objective audit precision for external/user-owned blockers. Known external blockers with resolved user task ids, currently Hanwoo T-251, now report `coverage: complete` while still carrying the unresolved blocker, so completion audit fails only on `blocked` instead of adding generic `incomplete_coverage` noise. Unknown external blockers remain `coverage: partial` with `unknown` blocker text so missing task mapping is not overclaimed; user-owner matching is case-insensitive. Verification passed focused launch/completion audit pytest `13/13`, ruff, format check, `py_compile`, runtime candidate launch audit generation, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.6071428571428571`), and completion audit `complete` (`4/4`). Feature commit `9025f5fc` is pushed; required GitHub Actions passed on that head (`root-quality-gate` `26988553084`, `active-project-matrix` `26988553088`). Final readiness remains score `96` with workspace/local/agent blockers `0/0/0`; launch audit is `7/8` complete with only external/user-owned T-251 blocked and no `incomplete_coverage` issue for that known blocker. | Codex | 2026-06-05 |
| T-1287 | `[workspace/readiness]` Added mixed-owner regression coverage for T-1286 readiness blocker routing. The new test locks that when a project has both an agent-owned blocker and a user-owned blocker, product readiness recommends resolving the agent-owned blocker first and then waiting on user-owned T-251. Updated shared T-1286 evidence to focused pytest `25/25`, A/B `adopt_candidate` (`score_delta=0.9090909090909091`), and completion audit `complete` (`5/5`). Verification passed focused product-readiness pytest, ruff, format check, `py_compile`, runtime readiness JSON, `git diff --check`, and staged code-review gate exit `0` with advisory WARN (`risk_score=0.35`) from graph helper-gap heuristics covered by the focused tests. Post-push required GitHub Actions passed on `31faa96e`: `root-quality-gate` `26988113562` and `active-project-matrix` `26988113556`; final readiness remains score `96` with workspace/local/agent blockers `0/0/0` and only external/user-owned T-251 incomplete in the launch audit. | Codex | 2026-06-05 |
| T-1286 | `[workspace/readiness]` Made product readiness recommendations owner-aware for task blockers. User-owned blockers such as Hanwoo T-251 now surface as `Wait for 1 user-owned task blocker(s) before rerunning local launch checks: T-251.` in the project recommendation and `next_actions`, instead of the previous generic `Resolve 1 open task blocker(s)` wording that could steer auto-research loops toward retrying an external Supabase credential-reset blocker. Mixed user-owned and agent-owned project blockers now keep agent-owned work first, then wait on user-owned blockers. Readiness score and blocker counts remain unchanged apart from normal dirty-worktree state before commit. Verification passed focused product-readiness pytest `25/25`, ruff, format check, `py_compile`, runtime readiness JSON, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.9090909090909091`), and completion audit `complete` (`5/5`). | Codex | 2026-06-05 |
| T-1285 | `[workspace/session-orient]` Aligned HANDOFF rotation recommendations with the deterministic handoff rotator. `session_orient.py` now reports `rotation_cutoff` and `archivable_addendum_count`, and only sets `rotation_suggested` when a Current Addendum entry is actually older than the seven-day retention cutoff. This removes the false warning where a long but fully recent HANDOFF (`593` lines, `83` current addenda, oldest age `1` day) suggested rotation even though `handoff_rotator.py --check --json` returned `noop` with `kept=83`, `archived=0`, cutoff `2026-05-29`. Added regression coverage for stale addenda still suggesting rotation and long recent HANDOFF files not suggesting rotation. Verification passed focused pytest `31/31`, ruff, format check, `py_compile`, runtime session-orient/rotator checks, `git diff --check`, A/B `adopt_candidate` (`score_delta=1.0`), and completion audit `complete` (`4/4`). | Codex | 2026-06-05 |
| T-1284 | `[workspace/auto-research]` Hardened auto-research evidence JSON for Windows PowerShell default reads under the Korean user profile path. All six auto-research JSON emitters now use ASCII-escaped JSON (`ab_decision.py`, `browser_qa_inventory.py`, `completion_audit.py`, `dependency_freshness_inventory.py`, `github_project_inventory.py`, `launch_objective_audit.py`), preventing valid UTF-8 evidence files from becoming unparsable when read with default `Get-Content` before `ConvertFrom-Json`. Added `test_cli_output_file_is_ascii_safe_for_powershell_default_reads` with a Korean-path temp root and documented the rule in `auto-research/SKILL.md`. Runtime proof regenerated launch/dependency JSON files and parsed both via default PowerShell reads; browser inventory still reports fresh screenshots `4/4`, stale `0`; dependency inventory still reports direct candidates `0` and peer-blocked Hanwoo/Knowledge ESLint 10. Verification passed focused launch-audit pytest `7/7`, broader auto-research/GitHub pytest `38/38`, ruff, format check, `py_compile`, skill lint score `100`, runtime PowerShell parse checks, `git diff --check`, A/B `adopt_candidate` (`score_delta=3.0604133545310015`), and completion audit `complete` (`3/3`). No app package manifests changed, and T-251 was not retried. | Codex | 2026-06-05 |
| T-1283 | `[workspace/auto-research]` Improved launch-loop evidence routing so remaining blocked work is not reselected as generic upgrade work. `dependency_freshness_inventory.py` now turns fully peer-blocked deferred major migrations into an upstream-peer-support wait recommendation; current runtime inventory still reports direct dependency candidates `0` and deferred dependencies `2`, but now recommends waiting for upstream peer support before Hanwoo ESLint 10 (`4` peer blockers) and Knowledge ESLint 10 (`3` peer blockers). `browser_qa_inventory.py` now records retained screenshot `modified_at`, `age_days`, fresh/stale summary counts, and refresh recommendations for screenshots older than 14 days; current runtime browser QA inventory reports coverage `4/4`, fresh screenshots `4/4`, stale `0`. `launch_objective_audit.py` now includes peer-blocked deferred dependency evidence plus fresh screenshot coverage. No app package manifests or lockfiles changed, and T-251 was not retried. Verification passed auto-research pytest slice `37/37`, ruff, format check, `py_compile`, runtime dependency/browser/launch inventories, skill lint score `100`, `git diff --check`, A/B `adopt_candidate` (`score_delta=1.4835309617918315`), and completion audit `complete` (`5/5`). Feature commit `37a68afe` is pushed; required GitHub Actions passed on that head (`root-quality-gate` `26986800302`, `active-project-matrix` `26986800326`). Post-push readiness is score `96` with workspace/local/agent blockers `0/0/0` and only external/user-owned T-251 remaining. | Codex | 2026-06-05 |
| T-1282 | `[workspace/auto-research]` Added lockfile peer-blocker context to dependency freshness inventory. `dependency_freshness_inventory.py` now inspects `package-lock.json` package `peerDependencies` for deferred major migrations and records peer blocker evidence instead of leaving ESLint 10 as an opaque major deferral. Baseline inventory reported direct candidates `0`, deferred dependencies `2`, and generic Hanwoo/Knowledge ESLint 10 deferrals; candidate inventory still reports direct candidates `0` and deferred dependencies `2`, but now annotates Hanwoo `peer_blocker_count=4` and Knowledge `peer_blocker_count=3`. No Hanwoo or Knowledge package manifests/lockfiles changed. Verification passed focused dependency freshness pytest `9/9`, broader auto-research/GitHub pytest `33/33`, ruff, format check, `py_compile`, runtime inventory, `git diff --check`, A/B `adopt_candidate` (`score_delta=6.222222222222222`), and completion audit `complete` (`4/4`). Do not force ESLint 10 until upstream plugin peer support changes. | Codex | 2026-06-05 |
| T-1281 | `[workspace/auto-research]` Added npm prerelease dist-tag context to dependency freshness inventory. `dependency_freshness_inventory.py` now checks `npm view <package> dist-tags --json` for prerelease/stable channel mismatches and reclassifies packages already current on their matching channel as `current_prerelease_channel`; Hanwoo `next-auth@5.0.0-beta.31` is now recognized as current on the npm `beta` tag instead of a false deferred blocker. The Knowledge Dashboard ESLint 10 candidate was tested but rejected because current Next transitive plugins still peer only through ESLint 9 and lint fails under ESLint 10 with `react/display-name` `TypeError: contextOrFilename.getFilename is not a function`; package state was restored to valid `eslint@9.39.4`. Verification passed focused pytest `8/8`, broader auto-research/GitHub inventory pytest `32/32`, ruff, format check, `py_compile`, runtime inventory deferred count `3 -> 2`, valid Knowledge `npm ls`, Knowledge lint, audit high/critical `0`, skill lint no-error status, `git diff --check`, completion audit `complete` (`4/4`), and A/B `adopt_candidate` (`score_delta=1.2549019607843137`). Remaining deferred dependency items are Hanwoo ESLint 10 and Knowledge ESLint 10; T-251 remains user-owned external Supabase work. | Codex | 2026-06-05 |
| T-1280 | `[workspace/auto-research]` Corrected launch-objective audit evidence and relay records. `launch_objective_audit.py` now reports that `SKILL.md` documents the launch objective audit command, and `workspace/tests/test_auto_research_launch_objective_audit.py` locks that evidence string. The T-1279 `.ai` records now remove an unsupported Knowledge Dashboard ESLint 10 recheck claim, correct A/B to `score_delta=5.25`, and state that the generated launch-objective completion audit remains intentionally `incomplete` while dirty work or external/user-owned T-251 exists. Verification passed focused pytest `4/4`, broader auto-research/GitHub inventory pytest `30/30`, ruff, format check, `py_compile`, runtime manifest generation with `7/7` skill artifacts, expected incomplete completion audit before clean commit, A/B `adopt_candidate` (`score_delta=2.0`), and `git diff --check` with CRLF warnings only. | Codex | 2026-06-05 |
| T-1279 | `[workspace/auto-research]` Added a deterministic launch-objective audit helper for the product-launch self-improvement loop. `.agents/skills/auto-research/scripts/launch_objective_audit.py` now collects current readiness, GitHub project/PR inventory, browser QA inventory, and dependency freshness evidence, then builds an 8-item completion-audit manifest covering the explicit launch objective. `auto-research/SKILL.md` documents the helper, and both required workspace-quality workflows include the helper plus `workspace/tests/test_auto_research_launch_objective_audit.py`. Verification passed focused pytest `4/4`, broader auto-research/GitHub inventory pytest `30/30`, ruff, `py_compile`, runtime manifest generation, and A/B `adopt_candidate` (`score_delta=5.25`). The generated launch-objective completion audit intentionally remains `incomplete` while dirty local work or external/user-owned T-251 exists, so it is not a false launch-complete signal. No Knowledge Dashboard ESLint 10 recheck was run in this cycle; Knowledge ESLint 10 remains a separate deferred migration. | Codex | 2026-06-05 |
| T-1278 | `[hanwoo-dashboard/dependencies]` Adopted TypeScript 6 on Hanwoo Dashboard's Next 16/React 19/Prisma 7 stack without mixing ESLint 10, next-auth channel changes, or T-251 live DB work. Updated `projects/hanwoo-dashboard/package.json` to `typescript` `^6.0.3`, synchronized `projects/hanwoo-dashboard/package-lock.json` and root `pnpm-lock.yaml`, and confirmed current peers: `typescript-eslint@8.60.1` accepts `typescript >=4.8.4 <6.1.0`, `eslint-config-next@16.2.7` accepts TypeScript and ESLint 9, and Prisma/Serwist peers accept TypeScript 6. Verification passed package listing, Hanwoo tests `499/499`, lint, offline Prisma 7 test (`14` passed, live skipped), build, smoke, Hanwoo project QC, frozen root pnpm lockfile check, `npm.cmd outdated typescript --json` `{}`, audit high/critical `0`, dependency inventory total deferred count `4 -> 3`, browser-click QA screenshot `output/playwright/hanwoo-t1278-typescript6-browser-qa.png` with manifest/API health `200` and no console/page/request/status issues, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.4375`), and completion audit `complete` (`7/7`). Remaining deferred items are Hanwoo ESLint 10, Hanwoo next-auth beta/stable channel mismatch, and Knowledge ESLint 10; T-251 remains user-owned external Supabase work. | Codex | 2026-06-05 |
| T-1277 | `[workspace/readiness]` Refreshed full active-project QC evidence after the latest single-project Knowledge QC artifact made readiness temporarily show blind-to-x, shorts-maker-v2, and Hanwoo QC as unavailable. `python execution/project_qc_runner.py --json` passed across all active projects and regenerated `.tmp/project_qc_runner_latest.json`: blind-to-x tests `1723` passed / `9` skipped plus ruff, shorts-maker-v2 tests `1577` passed / `12` skipped plus ruff, Hanwoo tests `499/499` plus lint/build/smoke, and Knowledge tests `61/61` plus lint/build/smoke. `python execution/product_readiness_score.py --json` then returned score `96` with blind-to-x, shorts-maker-v2, and Knowledge ready, Hanwoo blocked only by user-owned T-251, and no project QC recommendations remaining. Dependency freshness inventory `.tmp/dependency-freshness-t1277.json` still reports zero direct patch/minor candidates and four deferred major/channel items. | Codex | 2026-06-05 |
| T-1276 | `[knowledge-dashboard/dependencies]` Adopted TypeScript 6 on Knowledge Dashboard's Next 16/React 19 stack without mixing Hanwoo or ESLint 10 migrations. Updated `projects/knowledge-dashboard/package.json` to `typescript` `^6.0.3`, synchronized `projects/knowledge-dashboard/package-lock.json` and root `pnpm-lock.yaml`, and kept `eslint` on the compatible `^9` line after the first install attempt pulled ESLint 10 out of scope. Current npm metadata reports `typescript@6.0.3` with Node `>=14.17`; `typescript-eslint@8.60.1` peers on `typescript >=4.8.4 <6.1.0`; local `npm.cmd ls typescript eslint eslint-config-next typescript-eslint @typescript-eslint/parser --depth=2` reports `typescript@6.0.3`, `eslint@9.39.4`, `eslint-config-next@16.2.7`, `typescript-eslint@8.60.1`, and `@typescript-eslint/parser@8.60.1`. Verification passed Knowledge `npm.cmd test` (`61/61`), `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run smoke`, `python execution/project_qc_runner.py --project knowledge-dashboard --json`, `pnpm.cmd install --lockfile-only --frozen-lockfile --ignore-scripts`, `npm.cmd outdated typescript --json` `{}`, browser-click QA screenshot `output/playwright/knowledge-t1276-typescript6-browser-qa.png` with console errors/warnings `0`, dependency inventory `.tmp/dependency-freshness-t1276-candidate.json` with total deferred dependency count `5 -> 4`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.5625`), and completion audit `complete` (`7/7`). Knowledge ESLint 10 remains a separate deferred major migration. | Codex | 2026-06-05 |
| T-1275 | `[word-chain/dependencies]` Adopted `@vitejs/plugin-react` 6 on the existing Vite 8 Word Chain stack after current official research and app verification. Official plugin-react changelog confirms v6 removes Babel-related features and drops Vite 7-and-below support, while 6.0.2 is type-only; Word Chain's `vite.config.js` uses plain `react()` with no Babel options, and npm metadata reports Node `^20.19.0 || >=22.12.0` plus Vite `^8.0.0` peer compatibility. Updated `projects/word-chain/package.json`, `projects/word-chain/package-lock.json`, and root `pnpm-lock.yaml`; `npm.cmd ls @vitejs/plugin-react vite --depth=0` reports `@vitejs/plugin-react@6.0.2` and `vite@8.0.16`. Verification passed `npm.cmd test` (`23/23`), `npm.cmd run lint`, `npm.cmd run build` through the known ASCII fallback after direct Vite `3221226505`, frozen lockfile-only pnpm, `npm.cmd audit --json` with vulnerabilities total `0`, `npm.cmd outdated @vitejs/plugin-react --json` `{}`, dependency inventory with Word Chain `clean` and total deferred dependency count reduced `6 -> 5`, Playwright production browser-click QA with Korean input `사과`, console errors/warnings `0`, all requests `200`, screenshot `output/playwright/word-chain-t1275-plugin-react6-qa.png`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.875`), and completion audit `complete` (`6/6`). Hanwoo T-251 and remaining Hanwoo/Knowledge major/channel migrations stay separate. | Codex | 2026-06-05 |
| T-1274 | `[workspace/dependencies]` Applied the root npm patch candidates surfaced by T-1273: `@biomejs/biome` `^2.4.15 -> ^2.4.16` and `turbo` `^2.9.14 -> ^2.9.16`, with `pnpm-lock.yaml` synchronized and `biome.json` schema updated to 2.4.16. Verification passed root `pnpm.cmd list` showing Biome `2.4.16` and Turbo `2.9.16`, `pnpm.cmd exec biome --version`, `pnpm.cmd exec turbo --version`, `pnpm.cmd install --lockfile-only --frozen-lockfile --ignore-scripts`, root-filtered `pnpm.cmd install --filter . --frozen-lockfile --ignore-scripts`, root `pnpm.cmd outdated --json` returning `{}`, dependency freshness inventory `.tmp/dependency-freshness-t1274.json` reporting root `clean` and patch/minor candidates `0`, workspace-quality pytest `162/162`, workspace-quality ruff, Turbo Word Chain test `23/23`, Turbo Word Chain lint, `git diff --check`, A/B `adopt_candidate`, and completion audit `complete`. Local full-workspace `pnpm install --frozen-lockfile --ignore-scripts` still exits `-1073740791` during Windows node_modules linking, so final full-install confidence is delegated to required Linux GitHub Actions after push. Deferred major/channel migrations remain separate. | Codex | 2026-06-05 |
| T-1273 | `[workspace/auto-research]` Added deterministic npm dependency freshness inventory for the launch loop. `.agents/skills/auto-research/scripts/dependency_freshness_inventory.py` discovers package projects, runs `npm outdated --json`, classifies direct patch/minor candidates separately from major migrations and prerelease/stable channel mismatches, and writes JSON via `--output`. `SKILL.md` documents the helper, and both required workspace-quality workflows now include the helper plus `workspace/tests/test_auto_research_dependency_freshness_inventory.py`. Live runtime inventory wrote `.tmp/dependency-freshness-t1273.json` and reported root patch candidates `@biomejs/biome 2.4.15 -> 2.4.16` plus `turbo 2.9.14 -> 2.9.16`, with Hanwoo/Knowledge/Word Chain major or channel migrations deferred. Verification passed focused pytest `6/6`, combined auto-research pytest `18/18` with repo-local basetemp, ruff, runtime inventory, A/B `adopt_candidate` (`score_delta=1.325`), and completion audit `complete` (`3/3`). | Codex | 2026-06-05 |
| T-1272 | `[hanwoo-dashboard/charts]` Hardened all six Hanwoo Recharts 3 `ResponsiveContainer` chart surfaces with explicit `minWidth={0}`, `minHeight={0}`, and `initialDimension={{ width: 1, height: 1 }}` guards so the charts avoid the initial `width(-1)/height(-1)` sizing warning and still resize into the real container. Updated source coverage in `analysis-copy`, `cattle-detail-modal-wiring`, `empty-state-wiring`, and `home-market-copy` tests. Verification passed Hanwoo `npm.cmd test` (`499/499`), `npm.cmd run lint`, `npm.cmd run smoke`, `npm.cmd run build`, top-level `npm.cmd ls recharts react-is --depth=0`, direct outdated/audit review, browser QA `.tmp/hanwoo-t1272-browser-qa.json` with production login click QA and Recharts 3 render harness console/page/network/server errors `0`, A/B `adopt_candidate` (`score_delta=3.090909090909091`), and completion audit `complete` (`5/5`). T-251 remains separate. | Codex | 2026-06-05 |
| T-1271 | `[dashboard/dependencies]` Refreshed safe dashboard dependency drift: Hanwoo now declares `bullmq` `^5.78.0`, `ioredis` `^5.11.1`, `react-hook-form` `^7.77.0`, and `zod` `^4.4.3`; Knowledge refreshed its existing `eslint` `^9` lock to `9.39.4`; npm locks and root `pnpm-lock.yaml` are synchronized. Verification passed root frozen pnpm lock, target `npm ls`, Hanwoo test/lint/build/smoke (`499` tests), Knowledge test/lint/build/smoke (`61` tests), selected outdated checks, audit review with no high/critical findings, `git diff --check`, staged code-review gate pass (`risk_score=0.0`), A/B `adopt_candidate` (`score_delta=4.0`), completion audit `complete`, feature commit `7383776b`, and required GitHub Actions `root-quality-gate` 26978670413 plus `active-project-matrix` 26978670027 passed. Remaining audit findings are moderate-only with unsuitable downgrade fixes; deferred major/incompatible work stays separate. | Codex | 2026-06-05 |
| T-1270 | `[frontend/dependencies]` Aligned the selected React/Next patch stack across `word-chain`, `hanwoo-dashboard`, and `knowledge-dashboard`: Word Chain now uses `react`/`react-dom` `^19.2.7` and `@types/react` `^19.2.16`; Hanwoo now uses `next`/`eslint-config-next` `^16.2.7` and `react`/`react-dom` `^19.2.7`; Knowledge now uses `next`/`eslint-config-next` `16.2.7`. Synchronized all three npm package locks plus root `pnpm-lock.yaml`. Verification passed root frozen pnpm lock, selected-package `npm ls` and `npm outdated` checks, Word Chain tests `23/23`, lint, audit `0`, production build through the known ASCII-workspace Vite fallback after direct `3221226505`, Hanwoo/Knowledge project QC test/lint/build/smoke (`499` and `61` tests), direct browser-click QA for Word Chain submit/retry plus Hanwoo and Knowledge login interactions, A/B `adopt_candidate` (`score_delta=5.777777777777778`), and completion audit `complete`. Dashboard npm audit still reports existing moderate transitive advisories with unsuitable downgrade fixes; no broad audit fix was applied in this scoped patch cycle. T-251 remains separate. | Codex | 2026-06-05 |
| T-1269 | `[browser-apps/dependencies]` Aligned `projects/suika-game-v2` and `projects/word-chain` on the selected latest Vitest family patch. Updated `vitest` and `@vitest/coverage-v8` to `^4.1.8` in both app manifests, synchronized both npm package locks plus the root `pnpm-lock.yaml`, added `.gitignore` coverage-folder ignores for generated reports, and confirmed installed top-level versions are `4.1.8`. Verification passed Suika coverage tests (`61` tests), Word Chain coverage tests (`23` tests), lint for both apps after removing generated coverage output, builds for both apps through the known ASCII-workspace Vite fallback after direct `3221226505`, root frozen pnpm lock, npm audit `0` vulnerabilities in both apps, selected-package outdated checks returning `{}`, `git diff --check`, A/B `adopt_candidate` (`score_delta=1.4285714285714286`), and completion audit `complete`. | Codex | 2026-06-05 |
| T-1268 | `[auto-research/github-inventory]` Accepted the `--json` CLI flag for `.agents/skills/auto-research/scripts/github_project_inventory.py` while preserving JSON as the default output, updated the auto-research skill command example, and wired `workspace/tests/test_github_project_inventory.py` plus the inventory helper into the required workflow quality lists. Verification passed focused inventory pytest (`8` tests with repo-local basetemp), ruff, `py_compile`, and required GitHub Actions on commit `5849691e` (`root-quality-gate` and `active-project-matrix`). | Codex | 2026-06-05 |
| T-1267 | `[workspace/browser-qa]` Closed the T-1266 screenshot-retention recommendation for the two browser apps that only had historical/log evidence. Built `projects/suika-game-v2` and `projects/word-chain` with the known ASCII-workspace fallback after direct Vite `3221226505`, ran production `vite preview` for both, and executed direct Playwright browser-click QA via a temporary Node script using the npx Playwright package cache. Suika passed `Game Start` click, ArrowLeft/ArrowRight/Space input, nonblank canvas pixel check, console warnings/errors `0`, failed requests `0`, and retained local screenshot `output/playwright/suika-t1267-browser-click-qa.png`. Word Chain passed Korean input `사과` submit, visible submitted word assertion, console warnings/errors `0`, failed requests `0`, and retained local screenshot `output/playwright/word-chain-t1267-browser-click-qa.png`. Screenshot file checks reported both images `1280x900` with nonblank samples. Browser QA inventory now reports `browser_project_count=4`, `covered_count=4`, `missing_count=0`, `current_screenshot_project_count=4`, and `recommendations=[]`. A/B selected `adopt_candidate` (`score_delta=1.25`) and completion audit is `complete`. No app source changes were needed; preview servers were stopped. | Codex | 2026-06-05 |
| T-1266 | `[workspace/auto-research]` Added deterministic browser QA evidence inventory for the launch loop. `.agents/skills/auto-research/scripts/browser_qa_inventory.py` now discovers browser app package projects, matches verified direct-click QA evidence from `.ai` logs and current `output/playwright` screenshots, avoids cross-project false positives from generic tokens/mixed Handoff lines, and reports covered/missing projects plus screenshot-retention recommendations. `SKILL.md` now documents the inventory command. CI workflow lists now include the new helper and test in `root-quality-gate` and `active-project-matrix`. Runtime inventory reports browser QA coverage `4/4` covered, `0` missing; current screenshots are present for Hanwoo and Knowledge, while Suika and Word Chain still have log evidence but no current `output/playwright` screenshot. Verified focused pytest `8/8`, workflow-focused pytest `14/14`, ruff, py_compile, skill lint score 100/pass, diff-check, staged code-review gate `pass` risk `0.0`, A/B `adopt_candidate` (`score_delta=1.6666666666666667`), completion audit `complete`, pushed feature commit `94abd514` plus ai-context head `b0765352`, and GitHub Actions `root-quality-gate` 26974181977 plus `active-project-matrix` 26974182044 passed on `b0765352`. | Codex | 2026-06-05 |
| T-1265 | `[workspace/github-actions]` Fixed checkout/submodule hygiene after Actions logs showed `fatal: No url found for submodule path 'nature-skills' in .gitmodules` during checkout cleanup. Added `.gitmodules` for the tracked `nature-skills` gitlink, upgraded workflow checkout steps to `actions/checkout@v6`, set `persist-credentials: false`, and added `workspace/tests/test_github_workflow_hygiene.py` coverage wired into both required workflow lists. Verified submodule status/cleanup, focused pytest 2/2, root-quality equivalent 140 passed, ruff, diff-check, code-review gate pass risk 0.0, A/B `adopt_candidate`, pushed `a94136b2`, and GitHub Actions `root-quality-gate` 26973845848 plus `active-project-matrix` 26973845884 passed with no repeated checkout fatal/128 warnings in logs. | Codex | 2026-06-05 |
| T-1264 | `[knowledge-dashboard]` Added a local workspace inventory fallback for dashboard sync so GitHub project cards remain useful when `GITHUB_PERSONAL_ACCESS_TOKEN` is unavailable or the GitHub API path fails. `sync_data.py` now imports the deterministic auto-research project inventory, maps local projects into GitHub-card-compatible rows with stable ids, local/tree URLs, inferred language, README/test/workflow metadata, and falls back to that inventory from no-token/httpx/API-error paths. `build_product_readiness()` now uses the default current project-QC readiness contract instead of forcing the legacy public QA/QC artifact. Added focused Python regressions for remote URL normalization, local repo mapping, no-token fallback, and readiness invocation. Also ran direct Playwright CLI browser-click QA across login, operations, knowledge/search/modal/export menu, QA/QC, activity, refresh, and logout; screenshot artifact `output/playwright/knowledge-t1264-browser-click-qa.png`. Verified focused pytest 8/8 with repo-local basetemp, ruff, py_compile, diff-check, `npm.cmd test` 61/61, Knowledge project QC test/lint/build/smoke, full active-project QC (`3860` passed / `21` skipped), A/B `adopt_candidate` (`score_delta=4.666666666666667`), completion audit `complete`, pushed `7b656d3a`, and GitHub Actions `root-quality-gate` 26973233204 plus `active-project-matrix` 26973233352 passed. | Codex | 2026-06-05 |
| T-1263 | `[workspace/readiness]` Versioned project-QC readiness artifacts so stale parser-era evidence cannot remain complete launch evidence. `project_qc_runner.py` now writes `schema_version=2`, and `product_readiness_score.py` compares stored `project_qc_runner` artifacts with the current runner contract, downgrading missing-schema artifacts to `PARTIAL` with `contract_mismatches=["artifact_schema_version"]`. Regenerated full active-project QC now records Hanwoo 499 and Knowledge 61 Node tests in readiness. Verified project-QC runner pytest 12/12, product-readiness pytest 24/24, combined focused pytest 36/36, ruff/format/py_compile/diff-check, full active-project QC (`3860` passed / `21` skipped), A/B `adopt_candidate`, pushed `3df8642c`, and GitHub Actions `root-quality-gate` 26971365112 plus `active-project-matrix` 26971365069 passed. | Codex | 2026-06-05 |
| T-1262 | `[workspace/github-inventory]` Fixed launch-evidence false negatives in GitHub project inventory and Node test-count parsing. `github_project_inventory.py` now detects colocated JS/TS/MJS test files and usable package test scripts, skips generated/dependency directories, handles disappearing scan paths, and avoids duplicating child project tests in the root summary; Hanwoo, Suika, and Word Chain now report tests correctly. `project_qc_runner.py` now parses Node test-runner summary lines with non-word prefixes so readiness artifacts keep real passed/skipped counts. Verified focused inventory pytest 5/5, project-QC runner pytest 12/12, live inventory, Hanwoo/Knowledge runner test checks, ruff/format/py_compile/diff-check, A/B `adopt_candidate`, pushed `fb0268eb`, and GitHub Actions `root-quality-gate` 26970050492 plus `active-project-matrix` 26970050500 passed. | Codex | 2026-06-05 |
| T-1261 | `[workspace/readiness]` Hardened product readiness against stale project-QC artifacts after the runner contract changes. `execution/product_readiness_score.py` now compares stored `project_qc_runner` observed checks with current `execution/project_qc_runner.py` expected checks, so old artifacts missing newly required `smoke` checks are downgraded to `PARTIAL` instead of scoring as complete. Added focused regression coverage; verified readiness pytest 23/23, ruff/format/py_compile/diff-check, full active-project QC with smoke, A/B `adopt_candidate`, completion audit `complete`, pushed `c72e0ce8`, and GitHub Actions `root-quality-gate` 26969298890 plus `active-project-matrix` 26969298914 passed. | Codex | 2026-06-05 |
| T-1260 | `[workspace/readiness]` Added launch-blocker ownership breakdown and runtime smoke coverage to the release gates. `execution/product_readiness_score.py` now reports per-project `blocker_breakdown` plus overall `external_blocker_count`, `local_blocker_count`, `agent_task_count`, and environment/workspace-gate counts, so the current T-251 state can be read as user-owned external work while local blockers stay separate after commit. `execution/project_qc_runner.py` now includes `smoke` checks for Hanwoo and Knowledge dashboards. Verification passed focused readiness pytest (`22` passed), project QC runner pytest (`11` passed), direct T-251 ownership regressions (`2` passed), `py_compile`, ruff, diff-check, Hanwoo and Knowledge `npm run smoke`, runner smoke (`passed`), A/B `adopt_candidate`; code-review gate returned advisory WARN only for a graph-reported stale helper test gap covered by direct tests. | Codex | 2026-06-05 |
| T-1259 | `[workspace]` Hardened the duplicate task-ID commit guard so no-edit amend/fixup flows that keep the existing task ID are allowed while normal duplicate task IDs remain blocked. Touched `execution/task_id_gate.py` and `workspace/tests/test_task_id_gate.py`; pushed `a1956ec6`; GitHub Actions `root-quality-gate` run `26967317966` and `active-project-matrix` run `26967317897` passed. | Codex | 2026-06-05 |
| T-1258 | `[workspace/readiness]` Hardened the product-readiness GitHub release gate so non-Git fixture roots return a non-blocking unavailable watch state instead of inheriting parent repo GitHub status. Added regression coverage in `workspace/tests/test_product_readiness_score.py`; focused readiness tests passed 21/21, ruff/py_compile/diff-check passed, and the patch was pushed in `5403fb31` before T-1259. | Codex | 2026-06-05 |
| T-1257 | `[workspace/readiness]` Added launch-governance gates to `execution/product_readiness_score.py`: dirty worktree paths, open GitHub PRs, and required `root-quality-gate`/`active-project-matrix` status for current HEAD now block launch readiness. Added focused regressions for dirty workspace, open PR, and non-green workflow blockers. Pushed `ccf3d29e`; later CI passed on follow-up heads. | Codex | 2026-06-05 |
| T-1256 | `[workspace]` Added deterministic duplicate task-ID governance. `execution/task_id_gate.py` blocks normal commits that reuse an existing exact `T-####`, preserves `[ai-context]` follow-up commits and suffix fallback, and is wired into `.githooks/commit-msg` plus workspace CI coverage. Pushed `f7d513fb`; GitHub Actions passed on that head. | Codex | 2026-06-05 |
| T-1255 | `[blind-to-x/workspace]` Migrated the active Gemini SDK path from deprecated `google-generativeai` to current `google-genai`. `projects/blind-to-x/pyproject.toml` now declares `google-genai>=1.0.0`, the root workspace `uv.lock` resolves active `google-genai 2.8.0`, and `workspace/execution/content_writer.py` now uses `google.genai` Client generation with regression coverage in `workspace/tests/test_content_writer.py`. Verified content writer tests 8/8, ruff, py_compile, `uv lock --project projects/blind-to-x --check`, project venv import smoke (`google.genai` available, legacy `google.generativeai` absent), focused blind-to-x FutureWarning repro with `-W error::FutureWarning`, blind-to-x lint, blind-to-x project QC 1723/9, full active-project QC, readiness score 96 with only Hanwoo T-251 blocked, A/B `adopt_candidate` score_delta 1.4, completion audit complete, and GitHub Actions `root-quality-gate` plus `active-project-matrix` passed on `401b77b7`. Feature commit `401b77b7` is pushed; its commit message says T-1254 due a numbering collision, but this board records the cycle as T-1255. | Codex | 2026-06-05 |
| T-1253 | `[hanwoo-dashboard]` Ran Playwright CLI browser QA without changing code. Local dev server on `127.0.0.1:4316` rendered `/login` with readable Korean copy and demo credentials, admin/admin123 submit showed the expected Korean invalid-credentials alert, browser console errors/warnings were 0, auth provider/csrf/callback requests returned 200, `/manifest.json` returned 200 JSON, `/` redirected to `/login`, `/privacy` returned 200 with readable policy copy, and screenshot artifact was saved to `output/playwright/hanwoo-t1252-privacy.png`. `/api/health` returned 200 JSON with the known T-251 Supabase `P2010 / XX000 / ENOTFOUND tenant/user postgres.fuemeqmigptwfzqvrpjf not found` warning. Dev server and browser session were stopped. | Codex | 2026-06-05 |
| T-1252 | `[workspace/knowledge-dashboard]` Added a runtime auth readiness gate for `knowledge-dashboard` without exposing secret values. `execution/product_readiness_score.py` now checks the first existing `projects/knowledge-dashboard/.env.local` or `.env`, blocks missing/empty/placeholder `DASHBOARD_API_KEY`, reports the optional `DASHBOARD_SESSION_SECRET` fallback state, and treats the dashboard API key as a real launch-readiness requirement. Generated a gitignored local `.env.local` after the new gate detected it was missing; the file remains untracked/ignored. Added focused regression coverage for the required env check and placeholder API-key blocker path. Verified focused pytest (`16` passed), ruff, `ruff format`, `py_compile`, readiness score `96` overall with `knowledge-dashboard` score `100`, Dashboard API key configured, dedicated session secret configured, `git diff --check`, A/B `adopt_candidate` score_delta 2.875, completion audit complete, pushed `53563abf`, and GitHub Actions `root-quality-gate` plus `active-project-matrix` passed. | Codex | 2026-06-05 |
| T-1251 | `[workspace/blind-to-x]` Added a launch env readiness gate for `blind-to-x` without exposing secret values. `execution/product_readiness_score.py` now checks Notion review queue credentials (`NOTION_API_KEY`, `NOTION_DATABASE_ID`) and at least one usable LLM provider key, reports only configured-key counts, strips quoted env values, and marks stale QC as `needs-review` even at the numeric ready threshold. Added focused regression coverage for the required env check, Notion placeholder blocker path, and missing provider-key blocker path. Verified focused pytest (`14` passed), ruff, `py_compile`, readiness score `96` overall with `blind-to-x` score `100` and `8` LLM provider key(s) configured, `git diff --check`, A/B `adopt_candidate` score_delta 2.514473684210526, completion audit complete, pushed `694491a2`, and GitHub Actions `root-quality-gate` plus `active-project-matrix` passed. | Codex | 2026-06-05 |
| T-1250 | `[workspace/shorts-maker-v2]` Added a provider-key readiness gate for `shorts-maker-v2` without exposing secret values. `execution/product_readiness_score.py` now parses project `.env` assignments, checks `shorts_provider_keys`, reports only the count of configured generation provider keys, and blocks launch readiness when no usable provider key is configured. Existing Supabase env parsing reuses the same parser while preserving T-251 behavior. Added focused regression coverage for the requirement and placeholder/missing-key blocker path. Verified focused pytest (`11` passed), ruff, `py_compile`, readiness score `95` overall with `shorts-maker-v2` score `100` and `8` provider key(s) configured, `git diff --check`, A/B `adopt_candidate` score_delta 1.8894876819708846, and completion audit complete. | Codex | 2026-06-05 |
| T-1249 | `[shorts-maker-v2/workspace]` Added a GitHub-visible root launch README and made that entrypoint part of readiness. `projects/shorts-maker-v2/README.md` now documents quick start, single/batch generation, operations commands, verification commands, and related docs. `execution/product_readiness_score.py` now requires `README.md` for `shorts-maker-v2`, and `github_project_inventory.py` now recommends adding a root README when a project only has nested docs. Added regression coverage in `workspace/tests/test_product_readiness_score.py` and `workspace/tests/test_github_project_inventory.py`. Verified focused pytest (`11` passed), inventory reports `shorts-maker-v2 has_readme=true`, readiness reports `projects/shorts-maker-v2/README.md` present/state ready, `git diff --check`, A/B `adopt_candidate` score_delta 1.0, completion audit complete, pushed `848e83a0`, and GitHub Actions `root-quality-gate` plus `active-project-matrix` passed. | Codex | 2026-06-05 |
| T-1248 | `[knowledge-dashboard]` Ran browser-click QA against the readiness/operations surface and hardened Korean copy regression coverage. Playwright CLI login and tab clicks for operations, knowledge, QA/QC, and activity passed; `/api/auth/session` plus dashboard/qaqc/readiness/skills data routes returned 200; console warnings/errors were 0; readiness API reported score 94 with only Hanwoo T-251 blocked. `encoding-guard.test.mts` now detects common Korean mojibake fragments and asserts core operations/readiness Korean copy remains readable, while `.gitignore` ignores `.playwright-cli/` and `output/playwright/` QA artifacts. Verified Knowledge Dashboard project QC (`61` tests, lint, build), `git diff --check`, A/B `adopt_candidate` score_delta 0.4666666666666667, and completion audit complete. | Codex | 2026-06-04 |
| T-1247 | `[workspace]` Bridged active-project QC results into product readiness scoring. `execution/project_qc_runner.py` now writes `.tmp/project_qc_runner_latest.json` by default after real runs, including per-project status, parsed test counts, check metadata, expected/observed/missing checks, and coverage so partial runs stay partial. `execution/product_readiness_score.py` now reads that artifact first, falls back to legacy knowledge-dashboard QA/QC data, and only treats project-QC evidence as available when coverage is complete. Verified focused workspace tests 19/19, ruff, py_compile, full active-project QC, regenerated complete-coverage artifact, readiness score 71 -> 94, A/B `adopt_candidate` score_delta 1.9964788732394365, completion audit complete, diff check, and post-push GitHub Actions passed on `8b6322ef` for `root-quality-gate` plus `active-project-matrix`. | Codex | 2026-06-04 |
| T-1246 | `[blind-to-x]` Repaired packaging metadata needed for reproducible local project QC installs. `pyproject.toml` now includes setuptools package discovery for `pipeline*` and `scrapers*`, plus `ruff>=0.11.0` in dev extras because the canonical lint command is `python -m ruff check .`. Rebuilt the local blind-to-x `.venv` with Python 3.11.15 after the previous 3.14 venv hit an `lxml 5.4.0` wheel/build-tools failure. Verified editable install, project-root import smoke for `pipeline`/`scrapers`/`ruff`, `git diff --check`, full `project_qc_runner.py --json` across active projects, A/B `adopt_candidate` (`score_delta=1.0`), and completion audit `complete`. | Codex | 2026-06-04 |
| T-1245 | `[workspace/shorts-maker-v2]` Hardened `execution/project_qc_runner.py` for product-readiness loops. Python project pytest commands now use `-o addopts=` plus unique repo-local `--basetemp`, set repo-local `TMP`/`TEMP` on Windows and non-Windows, choose project/root Python only when the required `-m` module exists, and report the resolved command. Added/updated `workspace/tests/test_project_qc_runner.py` coverage and wired that test plus `execution/project_qc_runner.py` ruff coverage into both `root-quality-gate` and `active-project-matrix` workspace-quality. Actual project QC passed for `blind-to-x` test (`1723 passed, 9 skipped`) and `shorts-maker-v2` test (`1577 passed, 12 skipped`). The shorts QC run exposed a real `JsonlLogger` parent-directory deletion failure, so `projects/shorts-maker-v2/src/shorts_maker_v2/pipeline/orchestrator.py` now recreates `log_path.parent` under the write lock before opening, with regression coverage in `test_orchestrator_unit.py`. Verified focused workspace pytest 122/122, project QC runner tests 9/9, focused shorts logger/autofix tests 5/5, ruff, ruff format, compileall, diff check, A/B `adopt_candidate` score_delta 1.200667, completion audit complete, and post-push GitHub Actions passed on `8a1f3d18` for `root-quality-gate` plus `active-project-matrix`. Unrelated dirty `projects/blind-to-x/pyproject.toml` was preserved outside the commit scope. | Codex | 2026-06-04 |
| T-1244 | `[workspace/skills]` Strengthened the existing `auto-research` self-improvement skill with a deterministic completion audit helper instead of creating a duplicate skill. Added `completion_audit.py` to validate objective-to-artifact evidence manifests, added `references/completion-audit.md`, wired the workflow into `SKILL.md`, and updated the skill metadata prompt to include browser QA plus completion audits. While verifying the skill, fixed `execution/skill_lint.py` so relative `--skills-root` values resolve from the repo root; added regression coverage and wired the new skill/tool tests plus ruff targets into `root-quality-gate` and `active-project-matrix` workspace-quality. Verified focused helper pytest 12/12, CI-equivalent workspace focused pytest 113/113 with repo-local basetemp, ruff on workflow-listed files, compileall, diff check, auto-research-only skill lint score 100/pass, full skill lint had existing unrelated broken-reference warnings only and 0 errors, completion audit returned complete for this cycle, A/B returned `adopt_candidate` with score_delta 0.78125, staged code-review gate returned advisory warn risk 0.35 with direct regression coverage, and post-push GitHub Actions passed on `d136a91a` for `root-quality-gate` plus `active-project-matrix`. | Codex | 2026-06-04 |
| T-1243 | `[hanwoo-dashboard]` Closed Dependabot PR #122 without adopting ESLint 10 because the current Hanwoo Next lint stack is not ESLint 10-compatible. Local trial `npm.cmd install eslint@10.4.1 --save-dev --ignore-scripts` produced peer override warnings for `eslint-plugin-import@2.32.0`, `eslint-plugin-jsx-a11y@6.10.2`, and `eslint-plugin-react@7.37.5` from `eslint-config-next@16.2.6`; plain `npm.cmd run lint` first failed with `brace_expansion_1.expand is not a function` because Hanwoo's existing `brace-expansion` override forced `brace-expansion@2.1.1` under `minimatch@10.2.5`. A narrow local override trial allowing `minimatch@10.2.5 -> brace-expansion@5.0.6` plus refreshing `typescript-eslint` to `8.60.1` still failed lint with `scopeManager.addGlobals is not a function`, and `npm.cmd ls` marked `eslint@10.4.1` invalid under the current Next plugin peer ranges. Restored tracked files; `hanwoo-dashboard` remains on `eslint@9.39.4`, `npm.cmd run lint` passes, and PR #122 was commented/closed as not currently adoptable. | Codex | 2026-06-04 |
| T-1242 | `[knowledge-dashboard]` Closed Dependabot PR #117 without adopting ESLint 10 because the current Next lint plugin stack is not ESLint 10-compatible. `eslint-config-next@16.2.6` declares `eslint >=9.0.0`, but current npm metadata shows `eslint-plugin-import@2.32.0`, `eslint-plugin-jsx-a11y@6.10.2`, and `eslint-plugin-react@7.37.5` only peer through ESLint 9; local trial `npm.cmd install eslint@10.4.1 --save-dev --ignore-scripts` produced peer override warnings and `npm.cmd run lint` failed with `react/display-name` / `contextOrFilename.getFilename is not a function` from bundled `eslint-plugin-react`. Restored the failed lockfile churn; `knowledge-dashboard` remains on `eslint@9.39.4`, `npm.cmd run lint` passes, and PR #117 was commented/closed as not currently adoptable. | Codex | 2026-06-04 |
| T-1241 | `[word-chain]` Superseded Dependabot PR #115 by pairing `eslint` `^9.39.1` -> `^10.4.1` with `@eslint/js` `^9.39.1` -> `^10.0.1` and `eslint-plugin-react-hooks` `^7.0.1` -> `^7.1.1`, because `@eslint/js@10.0.1` peers on ESLint 10 and `eslint-plugin-react-hooks@7.0.1` does not. Synced `package-lock.json` and root `pnpm-lock.yaml`, and fixed the one ESLint 10 finding in `scripts/prebuild-check.js` by removing an unused fallback `status` initializer while preserving catch-path failure status. Verified root lock sync plus frozen pnpm lock, `npm ci`, `npm ls` at ESLint 10 stack versions, lint pass after cleanup, tests 23/23, audit 0, build through the known ASCII fallback after direct Vite `3221226505`, Playwright preview QA with Korean input `사과`, score 60/game-over/high-score state, console/network issues 0, screenshot `.tmp/word-chain-t1241-eslint10.png`, and A/B `adopt_candidate` score delta 1.352941. | Codex | 2026-06-04 |
| T-1240 | `[suika-game-v2]` Superseded Dependabot PR #102 and PR #96 together by pairing `@eslint/js` `^9.39.1` -> `^10.0.1` with `eslint` `^9.39.1` -> `^10.4.1`, because `@eslint/js@10.0.1` peers on `eslint ^10.0.0`. Synced `package-lock.json` and root `pnpm-lock.yaml`, and fixed the one ESLint 10 finding in `src/renderer.js` by replacing unused final `x/y` assignments in `drawStar()` with block-scoped outer/inner coordinates. Verified root frozen pnpm lock, `npm ls @eslint/js eslint` at 10.0.1/10.4.1, lint pass after cleanup, tests 61/61, audit 0, build through the known ASCII fallback after direct Vite `3221226505`, Chrome CDP preview QA with start overlay hidden, 480x800 canvas, sampled nonzero pixels increasing from 144 to 1089 after Space drop, PWA assets 200, same-origin/network/console issues 0, and A/B `adopt_candidate` score delta 0.263158. | Codex | 2026-06-04 |
| T-1239 | `[suika-game-v2]` Superseded Dependabot PR #104 on current `main` by bumping Vite from `^7.2.4` / lock `7.3.3` to `^8.0.16`, regenerating `package-lock.json`, and syncing root `pnpm-lock.yaml`. Current npm metadata reports Vite 8.0.16 as latest, with Node engine `^20.19.0 || >=22.12.0` satisfied by local Node 24.13.0; PR changelog includes Windows UNC/alternate-path hardening and Rolldown 1.0.3/request-timeout fixes. Verified root frozen pnpm lock, `npm ls vite` 8.0.16, suika tests 61/61, lint, audit 0, build through the known ASCII fallback after direct Vite `3221226505`, Playwright CLI preview QA with `Game Start`, 480x800 canvas, opaque pixels increasing from 2665 to 4251 after Space drop, Pause/Resume toggle, static requests all 200, console errors 0, and A/B `adopt_candidate` score delta 1.0. | Codex | 2026-06-04 |
| T-1238 | `[knowledge-dashboard]` Superseded Dependabot PR #114 on current `main` after concurrent T-1237 landed by bumping `react` and `react-dom` from `19.2.3` to `19.2.7`, tightening `@types/react` from `^19` to `^19.2.16`, syncing `package-lock.json`, and updating root `pnpm-lock.yaml`. Current npm metadata reports React/ReactDOM 19.2.7, `@types/react` 19.2.16, and `@types/react-dom` 19.2.3 as latest; `react-dom@19.2.7` peers on `react ^19.2.7`, so the runtime pair was updated together rather than adopting PR #114's partial runtime patch. Verified root frozen pnpm lock, `npm ls` React/ReactDOM 19.2.7 plus `@types/react` 19.2.16 plus `@types/react-dom` 19.2.3, knowledge-dashboard project QC 59 tests/lint/build after the transient Next build lock cleared, standalone smoke, Chrome CDP browser QA with API-key login plus knowledge/qaqc/activity/operations tab clicks plus all four data routes and `/api/health` 200 plus SVG count 13 plus console/network issues 0, unchanged audit total 7, and A/B `adopt_candidate` score delta 0.486486. | Codex | 2026-06-04 |
| T-1237 | `[word-chain]` Superseded Dependabot PR #87 on current `main` by bumping Vite from `^7.2.4` / lock `7.3.3` to `^8.0.16`, syncing `package-lock.json`, and updating root `pnpm-lock.yaml` without unrelated workspace lock churn. Current npm metadata reports Vite 8.0.16 as latest, with Node engine `^20.19.0 || >=22.12.0` satisfied by local Node 24.13.0; PR changelog includes Windows UNC/alternate-path hardening and Rolldown 1.0.3/request-timeout fixes. Verified root frozen pnpm lock, `npm ls vite` 8.0.16, word-chain tests 23/23, lint, audit 0, build through the known ASCII fallback after direct Vite `3221226505`, Chrome CDP browser click QA with Korean input `사과`, submit, max score 60, SVG count 2, same-origin/network/console issues 0, and A/B `adopt_candidate` score delta 0.5625. | Codex | 2026-06-04 |
| T-1236 | `[knowledge-dashboard]` Superseded Dependabot PR #84 on current `main` by bumping `@types/node` from `^20`/20.19.x to `^25.9.1`, syncing `package-lock.json`, and updating root `pnpm-lock.yaml` to the `@types/node@25.9.1` / `undici-types@7.24.6` resolver path. Current npm metadata reports 25.9.1 as latest and the TypeScript 5.9 dist-tag. Verified root frozen pnpm lock, `npm ls @types/node` 25.9.1, knowledge-dashboard project QC 59 tests/lint/build, standalone smoke, Chrome CDP login plus operations/knowledge/qaqc/activity tab clicks plus data APIs and health 200 plus SVG render with console/network issues 0, unchanged audit total 7, and A/B `adopt_candidate` score delta 0.333333. | Codex | 2026-06-04 |
| T-1235 | `[hanwoo-dashboard]` Fixed public health route proxy behavior discovered by browser QA: `/api/health` no longer matches the Auth.js proxy matcher, so production health checks return 200 JSON instead of a 307 login redirect that can fail under non-default local ports. Added matcher regression coverage for `api/health`, `manifest.json`, and `api/auth`. Verified focused proxy/PWA test 2/2, full Hanwoo tests 499/499, lint, production build, direct `curl` health 200 JSON, Chrome CDP `/login` render plus password-toggle click plus `/api/health` 200 plus `/privacy` navigation with console/network issues 0, and A/B `adopt_candidate` score delta 0.636364. T-251 live Supabase DB credential blocker remains separate. | Codex | 2026-06-04 |
| T-1234 | `[hanwoo-dashboard]` Superseded Dependabot PR #120 on current `main` by aligning `prisma`, `@prisma/client`, and `@prisma/adapter-pg` from `^7.6.0` to `^7.8.0`, syncing `package-lock.json`, and updating the root `pnpm-lock.yaml` importer specifiers. Current npm metadata reports Prisma `7.8.0` as latest and local Node `24.13.0` satisfies the engine range; official Prisma 7.7.0 and 7.8.0 release notes were reviewed. Verified root frozen pnpm lock, npm install with scripts ignored, Prisma/client version 7.8.0, adapter import smoke, Prisma Client 7.8.0 generation, offline Prisma runtime smoke 14/14 with live CRUD skipped, Hanwoo QC 498 tests/lint/build, Hanwoo smoke, Playwright CLI login/input/auth-callback browser QA with console errors/warnings 0, unchanged audit count of 7 existing advisories, and A/B `adopt_candidate` score delta 0.285714. T-251 live Supabase credential blocker remains separate. | Codex | 2026-06-04 |
| T-1233 | `[suika-game-v2]` Superseded Dependabot PR #98 on current `main` by bumping `jsdom` from `^28.1.0` to `^29.1.1` in `projects/suika-game-v2/package.json`, syncing `package-lock.json`, and updating the root `pnpm-lock.yaml` without unrelated workspace lock churn. Current npm metadata reports `29.1.1` as latest; official jsdom v29.0.0, v29.1.0, and v29.1.1 release notes were reviewed, with the main compatibility point being the Node 22 engine floor moving to `22.13.0+` while local Node `24.13.0` remains supported. Verified root frozen pnpm lock, suika tests 61/61, lint, build through the known ASCII fallback after direct Vite `3221226505`, jsdom `29.1.1` import/DOM smoke, `npm audit` 0 vulnerabilities, Playwright CLI browser QA with Game Start/nonblank canvas/drop pixel increase/static requests 200/no console errors, and A/B `adopt_candidate` score delta 0.4. | Codex | 2026-06-04 |
| T-1232 | `[blind-to-x]` Superseded Dependabot PR #80 on current `main` by bumping `notion-client` from `2.2.1` to `3.1.0` in `projects/blind-to-x/pyproject.toml` and syncing the root workspace `uv.lock`. Local package metadata reports `3.1.0` with `Requires-Python >=3.8, <4`, compatible with blind-to-x Python `>=3.11`; official notion-sdk-py v2.7.0, v3.0.0, and v3.1.0 release pages were reviewed. The relevant breaking change removes private/helper APIs `is_full_page_or_database` and `is_api_error_code`, which blind-to-x does not use. Verified `uv lock --project projects/blind-to-x --check`, Notion `3.1.0` import/API smoke for the used `AsyncClient` namespaces, focused Notion upload/schema/persist tests 163/163, extra Notion analytics/query/backfill tests 99/99, blind-to-x lint, and A/B `adopt_candidate` score delta 0.55. | Codex | 2026-06-04 |
| T-1231 | `[word-chain]` Superseded Dependabot PR #97 on current `main` by bumping `jsdom` from `^28.1.0` to `^29.1.1` in `projects/word-chain/package.json`, syncing `package-lock.json`, and updating the root `pnpm-lock.yaml`. Current npm metadata reports `29.1.1` as latest; official jsdom v29.0.0, v29.1.0, and v29.1.1 release notes were reviewed, with the main compatibility point being the Node engine floor moving from `^22.12.0` to `^22.13.0` for Node 22 while this local Node `24.13.0` remains supported. Verified root frozen pnpm lock, word-chain tests 23/23, lint, build through the known ASCII fallback after direct Vite `3221226505`, jsdom `29.1.1` import/DOM smoke, `npm audit` 0 vulnerabilities, and A/B `adopt_candidate` score delta 0.5. | Codex | 2026-06-04 |
| T-1230 | `[blind-to-x]` Superseded Dependabot PR #110 on current `main` by bumping `openai` from `2.37.0` to `2.41.0` in `projects/blind-to-x/pyproject.toml` and syncing the root workspace `uv.lock`. Official PyPI metadata reports `2.41.0` as latest with `requires_python >=3.9`; official openai-python v2.38.0..v2.41.0 release notes were reviewed and are additive API/spec, workload identity/additional Responses tools, Bedrock Responses support, and moderation endpoint surface updates. Verified `uv lock --project projects/blind-to-x --check`, OpenAI `2.41.0`/`AsyncOpenAI` import smoke, focused OpenAI provider/image/runtime tests 152/152, blind-to-x lint, and A/B `adopt_candidate` score delta 0.55. | Codex | 2026-06-04 |
| T-1229 | `[blind-to-x]` Superseded Dependabot PR #111 on current `main` by bumping `anthropic` from `0.104.1` to `0.105.2` in `projects/blind-to-x/pyproject.toml` and syncing the root workspace `uv.lock`. Official anthropic-sdk-python v0.105.0, v0.105.1, and v0.105.2 release pages were reviewed; changes are additive API/model/file-cap support or publishing/changelog-only chores, with no incompatible blind-to-x usage found. Verified `uv run` installed anthropic 0.105.2, `AsyncAnthropic` import smoke passed, focused Anthropic provider/cost/prompt-cache tests passed 70/70, blind-to-x lint passed, and A/B selected `adopt_candidate` score delta 0.9. | Codex | 2026-06-04 |
| T-1228 | `[blind-to-x]` Superseded Dependabot PR #107 on current `main` by bumping `pytest-asyncio` from `1.3.0` to `1.4.0` in `projects/blind-to-x/pyproject.toml` and syncing the root workspace `uv.lock`. Official pytest-asyncio v1.4.0 release notes were reviewed; blind-to-x does not use the deprecated `event_loop_policy` override or new loop-factory hooks, and current pytest is `8.4.2`, satisfying the new minimum. Verified `uv run` installed pytest-asyncio 1.4.0, full unit+integration without coverage passed 1791/1791 with 5 skipped when the known unrelated curl_cffi CAfile external test was ignored, blind-to-x lint passed, and A/B selected `adopt_candidate` score delta 0.9. | Codex | 2026-06-04 |
| T-1227 | `[suika-game-v2]` Superseded Dependabot PR #100 on current `main` by bumping `globals` from `^16.5.0` to `^17.6.0`, syncing `package-lock.json` and the root `pnpm-lock.yaml`, and confirming npm latest metadata still requires `node >=18`. Browser QA also exposed missing runtime asset requests, so CSS no longer references missing `background.png` and renderer image loading no longer requests absent fruit PNG sprites before using emoji fallback. Verified root frozen lock, suika tests 61/61, lint, build through the known ASCII fallback after direct Vite `3221226505`, `globals.browser`/`globals.node` shape smoke, audit 0, Python CDP browser QA with free-play start/nonblank canvas/drop/pause/console-network health, clean temp root pnpm install plus suika `npm ci`, and A/B `adopt_candidate` score delta 0.7. | Codex | 2026-06-04 |
| T-1226 | `[word-chain]` Superseded Dependabot PR #108 on current `main` by bumping `globals` from `^16.5.0` to `^17.6.0`, syncing `package-lock.json` and the root `pnpm-lock.yaml`, and confirming the npm latest dist-tag still requires `node >=18`. Browser QA also exposed missing PWA manifest icons, so `public/icon.svg` was added, the manifest now points at that real SVG icon, and `mobile-web-app-capable` meta was added. Verified root frozen lock, word-chain tests 23/23, lint, build through the known ASCII fallback after direct Vite `3221226505`, `globals.browser` shape smoke, audit 0, Python CDP browser click QA with Korean input/score/SVG/console-network health, clean temp worktree pnpm install, and A/B `adopt_candidate` score delta 0.7. | Codex | 2026-06-04 |
| T-1225 | `[blind-to-x]` Superseded Dependabot PR #113 on current `main` by bumping `playwright` from `1.59.0` to `1.60.0` in `projects/blind-to-x/pyproject.toml` and syncing the root workspace `uv.lock`. Official Playwright Python v1.60.0 release notes were reviewed; the only listed breaking change removes the long-deprecated `handle` option on `expose_binding`, and repository search found no `expose_binding`/`handle=` usage in blind-to-x. Verified `uv run` installed Playwright 1.60.0, import smoke passed, focused scraper/config unit tests passed 16/16 with repo-local basetemp, blind-to-x lint passed, and A/B selected `adopt_candidate`. | Codex | 2026-06-04 |
| T-1224 | `[knowledge-dashboard]` Fixed standalone smoke/data-dir drift exposed by final `active-project-matrix`: `start-standalone.mjs` copied `data/` into the standalone server cwd, so the missing-file smoke case removed `skill_lint.json` from source `data/` but the API still read the copied standalone file and returned 200. Added `KNOWLEDGE_DASHBOARD_DATA_DIR`, `getDashboardDataDir()`, and `dashboardDataFile()`; routed `/api/data/*` and `/api/health` through the helper; changed standalone start to set the data-dir env instead of copying stale data; documented Docker `/app/data`; and made smoke remove/restore fixtures across source/root/nested standalone data candidates for stale-copy regression coverage. Verified local standalone smoke, knowledge-dashboard project QC test/lint/build, and standalone smoke again after build. | Codex | 2026-06-04 |
| T-1223 | `[knowledge-dashboard]` Hardened standalone deploy/start path. Replaced `npm run start`'s `next start` path with `scripts/start-standalone.mjs`, which resolves root or nested `.next/standalone/**/server.js`, copies current `public/`, `.next/static`, and `data/` into the generated server cwd, and runs the standalone server directly. Updated smoke to use the same helper, hardened Docker for root/nested standalone output plus `/app/data` mount visibility, and cleaned deployment README/env/verifier/prebuild logs to ASCII-safe operator copy. Verified missing-key `verify-deploy` failure, keyed `verify-deploy` pass, knowledge-dashboard project QC test/lint/build, standalone smoke, Playwright login plus QA/QC and Knowledge tab clicks with console errors/warnings 0, data APIs 200, and A/B `adopt_candidate` score delta 0.7. | Codex | 2026-06-04 |
| T-1222 | `[word-chain]` Superseded Dependabot PR #112 on current `main` by bumping `lucide-react` from `^0.563.0` to `^1.17.0` in `package.json` and `package-lock.json`, syncing the root `pnpm-lock.yaml`, and fixing the existing `prebuild-check.js` ESLint `no-control-regex` blocker with `isAsciiPath()`. Verified current npm metadata (`latest=1.17.0`, React 19 peer compatible), root frozen lock, word-chain tests 23/23, lint, build via ASCII fallback after direct Vite `3221226505`, lucide import smoke (`Cpu`/`Send`/`Terminal`, 0 missing), audit 0, Chrome/Edge CDP browser QA for SVG render/input/submit/score/console-network health, clean temporary root pnpm install, A/B `adopt_candidate` score delta 0.7, and main `root-quality-gate` plus `active-project-matrix`. PR #112 was commented and closed as superseded. | Codex | 2026-06-04 |
| T-1221 | `[workspace/skills]` (/goal "필요한 스킬 검색·도입") 워크스페이스 운영 SOP 스킬 4종 신규 저작 — 반복 실수 대부분이 "스크립트는 있는데 제때 부르는 Layer-1 SOP가 없어서" 발생한다는 진단에 따라 기존 `execution/` 스크립트를 감싸는 스킬을 만듦. `.agents/skills/`에 `session-close`(세션종료 체크리스트 + handoff/session_log rotator), `task-id`(`next_task_id.py` 강제 + `T-####b` 폴백), `windows-safe-scripting`(.NET API 우선/`sys.stdout` UTF-8/cp949 mojibake), `session-start`(`session_orient.py` + `.ai/` 필독순서) 추가. 사용자 확정 범위 커스텀만(외부 마켓 설치 없음). 검증: `skill_lint.py` error 0, skill_count 52→56, 신규 4종 전부 healthy(broken_reference 0). 도그푸딩으로 본 세션 종료를 session-close SOP대로 수행. | Claude Opus 4.8 (1M context) | 2026-06-04 |
| T-1219 | `[knowledge-dashboard]` Hardened malformed GitHub repository display data surfaced during browser QA after T-1218. Added `getGithubRepoDisplayName()` so missing/blank repo names fall back to a URL slug or Korean "unnamed repository" copy before rendering, tag extraction, or search filtering. Routed repo card title and aria-label through the helper to eliminate `undefined 저장소 열기` and prevent `repo.name.toLowerCase()` crashes on loose runtime payloads. Verified focused dashboard-view regression, lint, knowledge-dashboard project QC test/lint/build, root frozen lock, production smoke, Playwright login plus Knowledge tab browser QA with console errors/warnings 0, data APIs 200, SVG render, selected tab `지식 현황`, and `hasUndefinedText=false`; A/B helper selected `adopt_candidate` with score delta 2.0. | Codex | 2026-06-04 |
| T-1218 | `[knowledge-dashboard]` Superseded Dependabot PR #119 on current `main` by bumping `lucide-react` from `^0.563.0` to `^1.17.0` in `package.json` and `package-lock.json`. Updated root `pnpm-lock.yaml` so the knowledge-dashboard importer and lucide snapshot match `1.17.0`. Verified npm metadata (`latest=1.17.0`, React peer includes `^19.0.0`), root frozen lock, 29 named lucide imports with 0 missing exports, knowledge-dashboard project QC test/lint/build, production smoke, Chrome CDP browser QA for API-key login/lucide SVG render/tabs/data APIs/console-network health, clean temporary worktree pnpm frozen install, A/B `adopt_candidate` with score delta 0.45, and main CI `root-quality-gate` plus `active-project-matrix`. PR #119 was commented and closed as superseded. | Codex | 2026-06-04 |
| T-1217 | `[hanwoo-dashboard]` Superseded Dependabot PR #121 on current `main` by bumping `lucide-react` from `^0.563.0` to `^1.17.0` in `package.json` and `package-lock.json`. Updated root `pnpm-lock.yaml` so the Hanwoo importer and lucide snapshot match `1.17.0`. Verified npm metadata (`latest=1.17.0`, React peer includes `^19.0.0`), root frozen lock, 58 named lucide imports with 0 missing exports, Hanwoo project QC test/lint/build, Chrome CDP browser QA for login lucide SVG render/input/password toggle/invalid credential/protected-route redirect, clean temporary worktree pnpm frozen install, and A/B `adopt_candidate` with score delta 0.45. `npm audit` still reports the existing unrelated Prisma/Hono/Next/PostCSS transitive advisories. | Codex | 2026-06-04 |
| T-1216 | `[github-actions]` Superseded Dependabot PR #95 on current `main` by updating `.github/workflows/full-test-matrix.yml` frontend job `pnpm/action-setup` from `@v4` to `@v6`. This targets the repeated GitHub Actions annotation that `pnpm/action-setup@v4` runs on deprecated Node.js 20. Verified PR release notes and latest upstream release (`v6.0.8`), path-limited workflow assertion that `@v6` is present and `@v4` absent, `git diff --check`, and staged code-review gate. Final validation is main GitHub Actions after push. | Codex | 2026-06-04 |
| T-1215 | `[hanwoo-dashboard]` Superseded Dependabot PR #118 on current `main` by bumping `@hookform/resolvers` from `^5.2.2` to `^5.4.0` in `package.json` and `package-lock.json`. Updated root `pnpm-lock.yaml` so the Hanwoo importer and resolver snapshot match `5.4.0`. Verified npm metadata (`latest=5.4.0`, peer `react-hook-form ^7.55.0` compatible with current `^7.76.1`), root frozen lock, clean temporary worktree pnpm frozen install, Hanwoo project QC test/lint/build, and Chrome CDP browser QA for login input, password toggle, invalid credential error, protected-route redirect, console issue 0, and serious failed request 0. A/B helper selected `adopt_candidate` with score delta 0.5. `npm audit` still reports the existing unrelated Prisma/Hono/Next/PostCSS transitive advisories. | Codex | 2026-06-04 |
| T-1214 | `[hanwoo-dashboard]` Superseded Dependabot PR #116 on current `main` by bumping `tailwind-merge` from `^3.5.0` to `^3.6.0` in `package.json` and `package-lock.json`. Updated root `pnpm-lock.yaml` so both Hanwoo and knowledge-dashboard importers now match `^3.6.0`, repairing the main CI `pnpm install --frozen-lockfile` failure introduced after T-1213. Verified lock consistency with root `pnpm.cmd install --lockfile-only --frozen-lockfile --ignore-scripts`, clean temporary worktree `pnpm.cmd install --frozen-lockfile --ignore-scripts`, and Hanwoo project QC test/lint/build. `npm audit` still reports unrelated Prisma/Hono/Next/PostCSS transitive advisories, so no broad audit fix was applied in this scoped minor dependency bump. | Codex | 2026-06-04 |
| T-1213 | `[knowledge-dashboard]` Superseded Dependabot PR #109 on current `main` by bumping `tailwind-merge` from `^3.4.0` to `^3.6.0` in `package.json` and `package-lock.json`. The npm install also synchronized stale package-lock root metadata with package version `1.1.0` and Node engine `>=20`. Verified knowledge-dashboard project QC test/lint/build and production smoke. `npm audit` still reports unrelated transitive/Next advisories; the Next suggested fix is not suitable for this scoped minor dependency bump. | Codex | 2026-06-04 |
| T-1212 | `[workspace]` Reapplied the useful core of draft/conflicting GitHub PR #123 on current `main`. `execution/product_readiness_score.py` now distinguishes missing Hanwoo `.env`, missing `DATABASE_URL`, empty `DATABASE_URL`, placeholder credentials, and present values instead of treating absent credentials as configured. Project recommendations now reuse the first concrete env blocker message. Added regression coverage for the missing `.env` case and tightened placeholder message expectations. Verified ruff, py_compile, and focused pytest 6/6 with repo-local basetemp. | Codex | 2026-06-04 |
| T-1211 | `[knowledge-dashboard]` Auto-research browser QA polish and release verification. Committed the existing product WIP as a verified release-quality bundle after fixing the current browser findings: first unauthenticated load now checks `/api/auth/session` before protected data routes, the session status route returns `{authenticated:false}` with HTTP 200 for missing sessions, the login screen no longer shows a failed-key alert before a user submits, the API-key password input has proper autocomplete context, Recharts panels use stable chart container dimensions to avoid tab-switch measurement warnings, and `rate-limit` tests use `const` where values are not reassigned. Verified project QC test/lint/build, production smoke, Playwright first-load/login/QA-QC tab browser flow, clean browser console, and data API 200 network path. | Codex | 2026-06-04 |
| T-1210 | `[workspace/skills]` Created project-level `auto-research` skill for bounded Karpathy-style self-improvement loops. Added `.agents/skills/auto-research/SKILL.md`, UI metadata, `references/karpathy-autoresearch.md`, `references/loop-contract.md`, deterministic `scripts/ab_decision.py`, and `scripts/github_project_inventory.py`. Hardened `.agents/skills/skill-creator/scripts/quick_validate.py` to read UTF-8 so Korean trigger text validates on Windows. Verified skill validation, Python compilation, A/B helper decision, GitHub/project inventory including open PRs, and scoped diff check. | Codex | 2026-06-04 |
| T-1209 | `[workspace]` Recovered Hermes CLI command resolution and prepared Grok OAuth default config. Added `%APPDATA%\npm` cmd shims for `hermes`, `hermes-agent`, and `hermes-acp`; updated the current PowerShell profile to prepend `%LOCALAPPDATA%\hermes\hermes-agent\venv\Scripts`; verified `hermes --version` and `hermes doctor`; set Hermes config to `model.provider=xai-oauth`, `model.default=grok-4.3`, and `model.base_url=https://api.x.ai/v1`. Started a visible `hermes auth add xai-oauth --timeout 900` flow for user browser approval, but `hermes auth status xai-oauth` still reports logged out until the user completes OAuth. | Codex | 2026-06-04 |
| T-1208 | `[workspace]` Control-plane log rotation hardening. Rotated oversized `HANDOFF.md` with existing `execution/handoff_rotator.py` (458 stale addenda archived, 73 kept), added deterministic `execution/session_log_rotator.py` with focused unit coverage, then rotated `.ai/SESSION_LOG.md` (241 stale table rows and 218 detailed sections archived before `2026-05-28`). Verified focused pytest 10/10, `py_compile`, and post-rotation dry-run noops. | Codex | 2026-06-04 |
| T-1207 | `[knowledge-dashboard]` (/goal "프로젝트 완성품화" → knowledge-dashboard 선택) **출시 가능 v1.0.0 완성품화**. 출시 차단 버그 3건 + 신뢰성 공백 해소: ① **mojibake** — `src/app/page.tsx`의 cp949 깨진 한국어 fallback 에러 문자열 5건(`"?몄뀡???앹꽦..."` 등)을 올바른 UTF-8로 교정. ② **QA/QC 인증 우회/staleness** — `scripts/sync_data.py`가 인증 경로 `data/qaqc_result.json`으로 미러 작성 → `/api/data/qaqc`가 항상 최신 제공(검증: Apr 1→Jun 4 신선화). `public/qaqc_result.json` git 추적 해제 + `.gitignore`에 `public/*.json`(public/은 무인증 노출 — ADR-023 위반). ③ **a11y** — TabBar를 WAI-ARIA tablist/tab(roving tabindex + 화살표/Home/End) + `role=tabpanel`, 검색 입력 `aria-label`, 로그아웃 버튼 type/focus-ring, `html lang ko`. ④ **테스트 3→16** — `dashboard-auth.test.mts`(HMAC/TTL/skew/변조/401·503·Bearer·쿠키 10건), `encoding-guard.test.mts`(mojibake 회귀 가드), insights 빈/널 엣지 2건. ⑤ **ship 정리** — v1.0.0, `app/icon.svg` favicon + metadata(noindex/OG), 보일러플레이트 SVG 5종 제거, README 배포 섹션. 검증: project_qc_runner test 16/16 + lint clean + build OK(`/icon.svg` 라우트 확인), code_review_gate risk 0.0. commit `3c20d53e`. | Claude Opus 4.8 (1M context) | 2026-06-04 |
| T-1206 | `[blind-to-x]` (/goal "프로젝트 완성품화" → blind-to-x 선택) 이미지 폴백 URL **버그 수정 + config 외부화 + 토픽 taxonomy 완전 커버**. `pipeline/image_generator.py::_get_fallback_image_url`이 AI 이미지 생성 전부 실패 시 쓰는 정적 폴백 URL 5개 중 **3개(연봉/회사문화/연애)가 실제 HTTP 404 dead** — 해당 토픽 콘텐츠에 깨진 이미지 링크 발행. 기존 테스트는 도메인만 검사해 미검출. 또 17개 토픽 클러스터 중 12개가 generic 기본 이미지로 폴백. 수정: ① 폴백 매핑을 `config.image.fallback_images`(토픽→URL)로 외부화 — README가 명시한 CDN/S3 운영 경로 충족 + "하드코딩 금지" 컨벤션 준수, ② 코드 기본값 `_FALLBACK_PHOTO_IDS`를 전체 17개 토픽 전부 커버하도록 확장하되 **모든 URL curl HTTP 200 검증**(후보 21개 검증, dead 3개 교체), ③ 운영자 오버라이드 우선 + 비dict/빈값/비문자열 방어. `config.example.yaml`에 `fallback_images` 문서화. 신규 테스트 8종(`TestFallbackImageUrl`: taxonomy 완전성 — `get_topic_rules()` 라이브 대조 / 검증 URL / dead ID 회귀 가드 / 오버라이드 / default 폴백 / 방어). 검증: image_generator 55/55, **full blind-to-x unit 1732/1732 pass 0 fail (5m15s)**, ruff clean. | Claude Opus 4.8 (1M context) | 2026-06-04 |
| T-1205 | `[workspace]` Resolved Codex startup warnings for invalid HeyGen skill metadata and broken Figma/Notion MCP startup. Shortened `heygen-avatar` and `heygen-video` skill descriptions in the Codex curated plugin cache and mirrored `.tmp` plugin cache to under 1024 bytes. Disabled the standalone `[mcp_servers.figma]` remote entry in `C:\Users\박주호\.codex\config.toml` because it requires OAuth client credentials. Hardened `infrastructure/notion-mcp/start_notion_mcp.ps1` to avoid unavailable PowerShell cmdlets, map `NOTION_API_KEY`/`NOTION_TOKEN`, and launch `@notionhq/notion-mcp-server --transport stdio`. Verified TOML parse, HeyGen description lengths, Notion launcher `-Check`, Notion MCP JSON-RPC initialize via the launcher, and `codex debug prompt-input` with clean stderr. | Codex | 2026-06-04 |
| T-1204 | `[workspace]` Patched global npm .ps1 wrapper scripts (`claude`, `codex`, `pnpm`, `pn`, `pnpx`, `pnx`) to use static .NET methods (`[System.IO.Path]::GetDirectoryName` and `[System.IO.File]::Exists`), bypassing any PowerShell module/cmdlet loading issues. Tested and verified that both commands execute flawlessly. | Gemini 3.5 Flash (High) | 2026-06-04 |
| T-1203 | `[workspace]` Fix PowerShell cmdlet error (Split-Path, Test-Path, Get-Content not found) when running npm globally installed wrappers like `codex` by creating and configuring the global PowerShell profile to explicitly import core modules (`Microsoft.PowerShell.Management`, `Microsoft.PowerShell.Utility`, `Microsoft.PowerShell.Diagnostics`, `Microsoft.PowerShell.Security`). Verified successful profile loading, cmdlet availability, and `codex` execution. | Gemini 3.5 Flash (High) | 2026-06-04 |
| T-1200 | `[blind-to-x]` `cost_db.py` schema/migration 분리 (행동 변화 0). 1074줄 단일 모듈에 schema DDL + 마이그레이션 컨스턴트 + 검증자 + runtime queries가 다 섞여 있어 schema-only 테스트 불가능. 신규 `pipeline/cost_db_schema.py` (165줄)로 분리: `MIGRATION_COLUMNS`/`PRAGMA_TABLE_INFO_SQL`/`ALTER_TABLE_ADD_SQL`/`CREATE_TABLES_SCRIPT` 상수, `validate_allowed_name`/`validate_migration_column`/`ensure_column`/`init_db` 순수 함수. `cost_db.py`는 1074→954줄(-120) 슬림화하고 `_MIGRATION_COLUMNS`/`_validate_*` 등은 backwards-compat 별칭으로 재수출(`noqa: F401`). `CostDatabase._init_db`/`_ensure_column`은 새 모듈 함수에 한 줄 위임. 신규 `tests/unit/test_cost_db_schema.py` (6 테스트) — in-memory SQLite에 `init_db(conn)`이 base table 6개 + MIGRATION_COLUMNS 전체 적용/idempotency/validator rejection 직접 검증(0.16s). 외부 호출자(`test_cost_db_security.py`의 `CostDatabase._ensure_column(...)` 등) 변경 0. 검증: focused 27/27 pass, 풀 스위트 1777/1777 pass 0 fail (6m01s), ruff clean. | Claude Opus 4.7 (1M context) | 2026-05-28 |
| T-1199 | `[hanwoo-dashboard]` AI Insight 라우트 structured metric 로그 (`ead4aa35`). T-1112 cacheBackend 응답 동봉만으로는 프로덕션 hit rate 회복 확인 불가. Langfuse SDK 추가 부담 회피하고 lightweight 대안 — Vercel/CloudWatch 가 `[ai-insight-metric]` prefix 로 grep 해 대시보드화할 한 줄 structured JSON 로그. 신규 `emitInsightMetric(payload)` (try/catch fail-safe) + 6 return path 전부 emit: `unauthenticated` / `heuristic_no_api_key` / `cache_hit`(backend+ageSeconds 포함) / `gemini_success` / `gemini_parse_failure` / `gemini_timeout`(timeoutMs 포함) / `gemini_error`(errorName 포함). 모든 페이로드에 `durationMs: Date.now() - startedAt` 동봉. **PII 잠금**: userId/summary 절대 미로그, source-grep contract 테스트로 regression 방지. 1 신규 widget-copy contract test, focused 15/15 pass, 전체 hanwoo `npm test` 498/498, lint clean, build exit 0. | Claude Opus 4.7 (1M context) | 2026-05-28 |
| T-1197 | `[blind-to-x]` Weekly report에 Best-of-N tuner 자동 임베드 (`a4951f9a`). T-1105 tuner CLI가 수동 실행만 가능했던 걸 weekly report 빌드 파이프라인에 통합. `scripts/build_weekly_report.py` 의 `_render_report()` 끝에 신규 `_render_best_of_n_section(days)` append — `tune_best_of_n_weight.load_recent_rows` + `build_report` 호출 후 markdown 코드 블록 래핑. `run()`은 `max(days, 30)` 윈도우로 sweep 표본 확보 유리. tuner 실패는 swallow → 빈 문자열 → weekly report 본문 fail-open. 10 신규 unit test(`test_build_weekly_report.py`), focused 10/10 + full blind-to-x unit suite 1713/1713, ruff clean. T-1198이 같은 작업의 검증·README 문서화로 병행 진행됨(멀티툴 race). | Claude Opus 4.7 (1M context) | 2026-05-28 |
| T-1198 | `[blind-to-x]` `/goal "blind-to-x weekly report에 Best-of-N tuner sweep 결과 자동 임베드"` 검증 & 문서화. 코드는 T-1197로 이미 완전 구현됨 — `scripts/build_weekly_report.py:15-39` `_render_best_of_n_section()`가 `tune_best_of_n_weight.build_report()` 호출 후 markdown 코드펜스로 임베드, try/except로 fail-open. 검증: focused tests 25/25 pass (test_build_weekly_report 10 + test_tune_best_of_n_weight 15), end-to-end 실 DB 호출 결과 표본 부족(0<10) → "기본값 0.5 유지" 메시지 임베드 + 본문 무손상. 문서 부재 발견 — README "추천 주간 운영" 1번 항목 아래에 임베드 동작과 fail-open 1문단 추가(`build_weekly_report.py --days 7` 호출 후 사용자에게 무엇이 추가로 보일지 안내). | Claude Opus 4.7 (1M context) | 2026-05-28 |
| T-1196 | `[blind-to-x]` Test pollution fix — `clear_runtime_state` promoted to `tests/conftest.py`. T-1108 검증 중 한 풀 스위트 run에서 `test_cost_db_extended` + `test_cost_tracker_extended` 18건 transient fail 목격(개별/소그룹은 다 pass). 원인: unit conftest의 SQLite 격리 fixture가 `tests/unit/**`만 보호 — integration test가 `draft_generator`/`scoring_6d` 통해 `get_cost_db()` 간접 호출하면 실 `.tmp/btx_costs.db` 오염시켜 후속 unit test 깨짐. 수정: `clear_runtime_state` (db path/싱글톤 monkeypatch + ml_scorer cleanup)을 부모 `tests/conftest.py`로 이동, unit conftest엔 alias 코멘트만. Integration이 cost_db 직접 안 쓰는 것은 grep으로 확인(`tests/integration/`에 `CostDatabase`/`get_cost_db`/`btx_costs.db` 0건). 검증: `py -3.14 -m pytest tests/unit tests/integration -q --no-cov --ignore=tests/integration/test_curl_cffi.py` 1767/1767 pass 0 fail (6m29s, +43s overhead 수용). | Claude Opus 4.7 (1M context) | 2026-05-28 |
| T-1195 | `[hanwoo-dashboard]` Profitability service DB row hardening. Tightened profitability calculations so malformed active cattle, feed expense, and sales collections are normalized before farm-metric adjustment and candidate ranking, preventing non-array/array-shaped rows from reaching recommendation math while preserving existing Korean operator-facing copy, window filters, and estimate behavior. | Codex | 2026-05-28 |
| T-1195 | `[hanwoo-dashboard]` AI Insight 캐시 Redis 백킹 (`97447aca`). T-1104 in-memory Map 캐시는 서버리스/다중 인스턴스 cold start 마다 초기화돼 실 hit rate ~0%. 기존 `src/lib/redis.js`(isRedisConfigured/ensureRedisConnection) 재사용해 Redis 격상. 새 async API `loadCachedInsight/saveCachedInsight/dropCachedInsight` — REDIS_URL 시 `ai-insight:<key>` 24h TTL, 미설정 시 기존 Map 자동 폴백, Redis 실패는 catch → null(fail-open). 응답에 `cacheBackend: "redis"\|"memory"` 동봉. 기존 sync API(getCachedInsight/setCachedInsight/clearCacheKey) 도 호환 유지. 5 신규 케이스(총 19), 전체 `npm test` 503/503, lint clean, build exit 0. | Claude Opus 4.7 (1M context) | 2026-05-28 |
| T-1194 | `[hanwoo-dashboard]` Raw diagnostics server-action DB row hardening. Tightened the admin diagnostics raw-data action so malformed dynamic model DB/mock results are normalized before returning to the diagnostics UI. This prevents direct/action/reuse callers from receiving non-array results or array-shaped rows from `getRawData()` while preserving authenticated server action behavior, allowed-model enforcement, existing Korean diagnostics failure copy, 50-row diagnostic limit, created-date ordering semantics, and diagnostics page safe rendering. Also aligned AI insight source coverage with the current Redis-aware async cache helpers so the full suite reflects the live route/cache contract. Verified focused action/diagnostics/AI tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1193 | `[hanwoo-dashboard]` Notification action cattle DB row hardening. Tightened the notification server action so malformed live cattle DB/mock results are normalized before building dashboard alerts. This prevents direct/action/reuse callers from feeding non-array results or array-shaped rows from `getNotifications()` into notification generation while preserving authenticated server action behavior, fresh read-model cache reuse, notification summary persistence, existing Korean fallback behavior, estrus/calving notification builder semantics, and dashboard notification consumers. Strengthened action source coverage for safe notification cattle row normalization. Verified focused action-copy tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1192 | `[hanwoo-dashboard]` Cattle list server-action DB row hardening. Tightened active and archived cattle list server actions so malformed cattle DB/mock results are normalized before returning to the dashboard. This prevents direct/action/reuse callers from receiving non-array results or array-shaped rows from `getCattleList()` and `getArchivedCattle()` while preserving authenticated server action behavior, existing Korean cattle failure copy, archive semantics, dashboard cattle normalization, cattle pagination/export consumers, and active/archive ordering semantics. Strengthened action source coverage for safe cattle list row normalization. Verified focused action-copy tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1191 | `[hanwoo-dashboard]` Sales list server-action DB row hardening. Tightened the sales list server action so malformed sales DB/mock results are normalized before returning to the dashboard. This prevents direct/action/reuse callers from receiving non-array results or array-shaped rows from `getSalesRecords()` while preserving authenticated server action behavior, existing Korean sales failure copy, sale creation validation, cattle history recording, dashboard sales normalization, profitability/analysis consumers, sales list cache invalidation, and sale-date ordering semantics. Strengthened action source coverage for safe sales row normalization. Verified focused action-copy tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1190 | `[hanwoo-dashboard]` Expense list server-action DB row hardening. Tightened the expense list server action so malformed expense DB/mock results are normalized before returning to the dashboard. This prevents direct/action/reuse callers from receiving non-array results or array-shaped rows from `getExpenseRecords()` while preserving authenticated server action behavior, existing Korean expense failure copy, expense filter parsing, expense creation validation, expense aggregation normalization, date ordering semantics, and dashboard expense rendering paths. Strengthened action source coverage for safe expense list and aggregation row normalization. Verified focused action-copy tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1189 | `[hanwoo-dashboard]` Schedule server-action DB row hardening. Tightened the schedule server action so malformed schedule DB/mock results are normalized before returning to the dashboard. This prevents direct/action/reuse callers from receiving non-array results or array-shaped rows from `getScheduleEvents()` while preserving authenticated server action behavior, existing Korean schedule failure copy, schedule creation and completion-toggle behavior, dashboard schedule normalization, upcoming schedule countdown labels, and date ordering semantics. Strengthened action source coverage for safe schedule row normalization. Verified focused action-copy tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1188 | `[hanwoo-dashboard]` Building server-action DB row hardening. Tightened the building server action so malformed building DB/mock results are normalized before returning to the dashboard. This prevents direct/action/reuse callers from receiving non-array results or array-shaped rows from `getBuildings()` while preserving authenticated server action behavior, existing Korean building failure copy, building creation validation, delete guard behavior, dashboard building normalization, and name ordering semantics. Strengthened action source coverage for safe building row normalization. Verified focused action-copy tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1187 | `[hanwoo-dashboard]` Inventory server-action DB row hardening. Tightened the inventory server action so malformed inventory DB/mock results are normalized before returning to the dashboard. This prevents direct/action/reuse callers from receiving non-array results or array-shaped rows from `getInventory()` while preserving authenticated server action behavior, existing Korean inventory failure copy, item creation and quantity validation, dashboard inventory-tab normalization, and category ordering semantics. Strengthened action source coverage for safe inventory row normalization. Verified focused action-copy tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1186 | `[hanwoo-dashboard]` Feed server-action DB row hardening. Tightened feed server actions so malformed feed-standard and feed-history DB/mock results are normalized before returning to the dashboard. This prevents direct/action/reuse callers from receiving non-array results or array-shaped rows from `getFeedStandards()` and `getFeedHistory()` while preserving authenticated server action behavior, existing Korean feed failure copy, feed record creation validation, dashboard feed-tab normalization, and feed-history ordering/limit semantics. Strengthened action source coverage for safe feed row normalization. Verified focused action-copy tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1185 | `[hanwoo-dashboard]` AI chat farm-context DB row hardening. Tightened the AI chat route's farm-context builder so malformed `groupBy()` and recent-sales results are normalized before status summary and Gemini context generation. This prevents direct/API/test reuse from crashing on non-array DB/mock results or leaking array-shaped rows and nested cattle payloads into AI prompt context while preserving Korean AI assistant instructions, date fallback copy, status summary fallback, recent-sales fallback, and numeric sale-price coercion. Strengthened AI chat route source coverage for safe farm-context row normalization. Verified focused AI chat API tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1184 | `[hanwoo-dashboard]` Expense aggregation row and amount hardening. Tightened the expense aggregation server action so malformed expense collections and array-shaped expense rows are normalized before category aggregation. This prevents direct/action/reuse callers from crashing on non-array `findMany()` results or leaking array-attached row fields into category totals, while finite numeric coercion prevents string/invalid amounts from corrupting totals and invalid categories fall back to `Other`. Strengthened expense action source coverage for row, category, and amount normalization. Verified focused action-copy tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1183 | `[hanwoo-dashboard]` Livestock weather alert utility forecast hardening. Tightened the shared weather alert utility so malformed forecast collections and array-shaped forecast rows are normalized before heat/cold/rain livestock alert generation. This prevents direct/reuse callers from crashing on null or primitive forecast input and prevents array-attached row fields from leaking into weather alert labels, thresholds, messages, or icons while preserving WeatherWidget's existing safe forecast flow, Korean weather copy, date formatting fallback, and livestock alert behavior. Strengthened utility source coverage for safe forecast normalization. Verified focused utility tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1182 | `[hanwoo-dashboard]` Notification builder cattle payload hardening. Tightened the shared notification builder so malformed cattle collections and array-shaped cattle rows are normalized before estrus/calving alert generation. This prevents direct/action/reuse callers from crashing on null or primitive cattle input and prevents array-attached row fields from leaking into notification ids, messages, cattle metadata, timing, or alert sorting while preserving existing estrus/calving timing, operator-facing notification copy, dashboard notification rendering, and notification-system state behavior. Strengthened notification source coverage for safe cattle payload normalization. Verified focused notification tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1181 | `[hanwoo-dashboard]` Payment API request-body hardening. Tightened payment prepare and confirm API routes so malformed request JSON bodies are normalized before reading payment fields. Null, primitive, and array-shaped bodies now fall into the existing Korean validation/error paths instead of throwing or leaking array-attached fields into customer-key, order, amount, or checkout metadata handling. Strengthened payment route source coverage to keep request-body normalization on both routes while preserving Toss payment preparation/confirmation behavior, Korean payment copy, authentication handling, payment-log failure normalization, and subscription creation flow. Verified focused payment UX tests, full Hanwoo tests, eslint, Next production build, path-limited diff check, and root project QC runner. | Codex | 2026-05-28 |
| T-1180 | `[hanwoo-dashboard]` AI chat request dependency hardening. Tightened the AI chat request helper so malformed or partial dependency payloads are normalized and rejected with the existing Korean JSON error envelope before authentication, API-key lookup, farm-context construction, or stream creation are invoked. This prevents direct/API/reuse callers from crashing through `deps` destructuring or missing callback invocation while preserving authenticated chat flow, validation errors, missing API-key copy, Gemini history normalization, SSE stream delegation, and existing chat route behavior. Strengthened focused AI chat API coverage for malformed dependency input. Verified focused AI chat tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1179 | `[hanwoo-dashboard]` AI/market/history helper array-object hardening. Tightened AI chat history, AI insight summary/response parsing, market-price state, and cattle-history metadata helpers so malformed array objects are rejected before chat history normalization, heuristic/AI insight aggregation, KAPE market price normalization, or cattle weight-history metadata extraction. This prevents direct/API/cache/reuse callers from leaking array-attached fields into Gemini context, insight recommendations, live/cache market prices, or cattle history chart evidence while preserving existing Korean validation copy, strict JSON insight parsing, market stale/unavailable fallbacks, metadata parse-error reporting, and current dashboard behavior. Strengthened focused helper tests for array-object guards. Verified focused helper tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1178 | `[hanwoo-dashboard]` Dashboard calculation utility array-row hardening. Tightened setup-progress, today-focus, and farm-metrics calculation helpers so malformed array rows are ignored before setup completion counts, feed-depletion consumption estimates, feed-expense samples, sales weight-gain samples, or cattle lookup values affect dashboard guidance and profitability adjustment evidence. This prevents direct/cache/reuse callers from leaking array-attached row fields into onboarding progress, today's work recommendations, feed forecasts, or farm-specific profitability calculations while preserving existing option normalization, Korean dashboard copy, date/window filtering, numeric coercion, and default fallback behavior. Strengthened focused dashboard utility tests for array-row guards. Verified focused dashboard utility tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1177 | `[hanwoo-dashboard]` Operational UI collection array-row hardening. Tightened remaining operator-facing collection normalizers in calving, schedule, settings, cattle form/detail, notification system, alert banners, ear-tag scanner, field mode, pen cards, and weather forecast rendering so malformed array rows are ignored before form select options, pregnancy/calving lists, schedules, settings building/widget controls, detail fallback charts, notification dropdowns, alert banners, scanner targets, field search/checklists, pen cattle cards, or weather forecast cards render. This prevents direct/cache/reuse callers from leaking array-attached row fields into operational UI paths while preserving existing Korean copy, async save locks, focus/a11y behavior, alert countdowns, scanner/field-mode animation guards, and card rendering semantics. Strengthened focused source coverage for these array-row guards. Verified focused operational UI tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1176 | `[hanwoo-dashboard]` Dashboard and pagination array-row hardening. Tightened DashboardClient home/helper, building, cattle, notification, full-list fetch, and shared/cattle/sales pagination row normalizers so malformed array rows are ignored before dashboard home rendering, pen lists, notification fan-out, full export registries, or paginated list state updates. This prevents direct/cache/API reuse callers from leaking array-attached row fields into dashboard panels, building grids, cattle registries, notification widgets/modals/banners, cursor pagination state, and load-more accumulation while preserving existing Korean dashboard copy, SSR pagination wiring, full-list preload behavior, stale-unmount guards, and pagination retry feedback. Strengthened focused dashboard and pagination source coverage for array-row guards. Verified focused dashboard/pagination tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1175 | `[hanwoo-dashboard]` Collection row array hardening. Tightened dashboard analysis, feed, sales, inventory, financial chart, profitability, notification widget/modal, dashboard summary, today-focus, and cattle CSV collection normalizers so malformed array rows are ignored before rendering, aggregation, CSV export, dashboard summary calculation, alert/today-focus selection, or profitability/notification display. This prevents cache/direct/reuse callers from leaking array-attached row fields into user-facing tables, charts, recommendations, notifications, CSV exports, or summary cards while preserving existing object-row normalization, numeric coercion, Korean fallback copy, pagination/render behavior, and dashboard cache semantics. Strengthened focused source coverage for array-row guards. Verified focused collection tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1174 | `[hanwoo-dashboard]` AI insight summary/request and action invalidation array-object hardening. Tightened AI insight summary normalization, AI insight widget stable-summary normalization, AI insight API request-body normalization, and shared action cache invalidation option normalization so malformed array inputs are ignored before reading farm summary, weather, force-refresh, cache-key, or cache-invalidation fields. This prevents direct/test/reuse callers from leaking array-attached fields into heuristic insight generation, AI insight request caching/refresh behavior, widget fallback summaries, or dashboard cache invalidation while preserving existing Gemini fallback flow, Korean insight copy, same-day AI cache behavior, Next cache tag revalidation, and dashboard mutation cache invalidation. Strengthened focused AI insight and action helper coverage for array-object guards. Verified focused AI/action tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1173 | `[hanwoo-dashboard]` Auth, dashboard cache/query/read-model, and notification timing array-option hardening. Tightened shared auth guard, dashboard cache key, dashboard list query, dashboard read-model cache, and notification timing option normalizers so malformed array inputs are ignored before reading redirect, filter, pagination, cache-bypass, invalidation, or notification reference-time fields. This prevents direct/test/reuse callers from leaking array-attached option fields into authentication redirects, cache keys, list query cache bypasses, read-model cache decisions, cache invalidation, or estrus timing calculations while preserving existing Korean auth copy, dashboard cache segmentation, list pagination validation, read-model cache behavior, and notification timing fallbacks. Strengthened focused source and behavior coverage for array-option guards. Verified focused helper tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1172 | `[hanwoo-dashboard]` Weather, fetch, queue, and offline utility array-option hardening. Tightened shared weather, fetch timeout, BullMQ queue, and offline sync state normalizers so malformed array inputs are ignored before reading timeout, message, location, queue, retry, permanent-failure, or metadata fields. This prevents direct/test/reuse callers from leaking array-attached option fields into user-facing weather fallback copy, timeout behavior, queue option wiring, or offline retry/dead-letter state while preserving Open-Meteo normalization, guarded fetch timeout scheduling/cleanup, Redis configuration checks, offline retry accounting, Korean fallback copy, and existing dashboard behavior. Strengthened focused utility coverage for array-option and array-item guards. Verified focused utility tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1171 | `[hanwoo-dashboard]` Market price and payment helper option hardening. Tightened the market price state helpers so malformed array options are ignored before reading fallback time/message fields, and the internal available-market-price builder normalizes top-level options before reading price sides, source metadata, freshness flags, or dates. Tightened the payment confirmation failure-log helper so malformed top-level options are normalized before reading `orderId`, `paymentKey`, or `amount`. This prevents direct/test/reuse callers from crashing helper setup or leaking malformed array option fields while preserving KAPE live/cache normalization, unavailable market fallback copy, Toss payment confirmation flow, Korean payment error copy, and existing persistence behavior. Strengthened market-price and payment route coverage for array-option and payment failure-log helper guards. Verified focused market/payment tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1170 | `[hanwoo-dashboard]` AI Gemini route helper option hardening. Tightened the AI insight and AI chat route-level Gemini provider helpers so malformed top-level helper options are normalized before reading API keys, prompts, messages, history, or system instructions. This prevents direct/test/reuse callers from crashing the AI provider setup through parameter destructuring while preserving authenticated route flow, Gemini model configuration, Korean insight timeout fallbacks, chat farm-context wiring, SSE stream delegation, AI cache behavior, and existing offline/configuration fallback behavior. Strengthened AI chat and AI insight route source coverage for safe Gemini helper option normalization. Verified focused AI route tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1169 | `[hanwoo-dashboard]` Dashboard summary service option and row hardening. Tightened the cached dashboard summary service so malformed top-level payload options are normalized before reading `farmId` or `client`, malformed financial-series options are normalized before reading sales, expenses, month count, or generated date, and malformed status/building/financial rows are filtered before reductions, maps, month aggregation, and occupancy calculations. This prevents direct/test/reuse callers and partial Prisma result shapes from crashing summary payload generation while preserving cached query wiring, current-month rollups, six-month financial series, numeric coercion, farm settings payloads, and existing dashboard summary API behavior. Strengthened analysis/financial source coverage for safe summary option, row, month, generated-date, aggregate, and building normalization. Verified focused analysis tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1168 | `[hanwoo-dashboard]` AI chat stream option and callback hardening. Tightened the AI chat SSE stream helper so malformed top-level stream options are normalized before reading `chat`, `message`, or `encoder`, allowing malformed direct/reuse calls to emit the existing Korean SSE error envelope instead of throwing before the stream is created. Tightened the AI chat widget stream helper so malformed top-level stream options and malformed chunk/done/error callbacks fall back to safe no-op handlers before fetch, chunk dispatch, completion, or error handling. This prevents direct/test/reuse callers from crashing AI chat streaming setup while preserving authenticated chat validation, Gemini history normalization, Korean configuration/error copy, abort handling, mounted-state guards, accessible launcher/dialog behavior, and existing offline fallback behavior. Strengthened AI chat API/widget coverage for malformed stream options and callback guards. Verified focused AI chat tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1167 | `[hanwoo-dashboard]` Dashboard setup and today-focus helper option hardening. Tightened the home dashboard setup-progress and today-focus helpers so malformed top-level options are normalized before reading farm setup inputs, notification/schedule/inventory/feed collections, sales counts, online state, or reference dates. This prevents direct/test/reuse callers from throwing on null, array, or primitive options before setup progress, today-focus cards, or feed-depletion projections can fall back to safe empty/default dashboard guidance. Strengthened setup-progress and today-focus tests for malformed top-level option payloads. Verified focused dashboard helper tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1166 | `[hanwoo-dashboard]` Farm metrics option hardening. Tightened the farm-specific dashboard metric helpers so malformed top-level options are normalized before estimating monthly feed cost, monthly weight gain, or combined profitability adjustments, and malformed `defaults` payloads are ignored before fallback values are read. This prevents direct/test/reuse callers and profitability-service reuse from throwing on null, array, or primitive options while preserving existing feed-expense filtering, sales-window filtering, farm-specific adjustment evidence, and default fallback behavior. Strengthened farm metrics tests for malformed top-level options and malformed defaults. Verified focused farm-metrics tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1165 | `[hanwoo-dashboard]` Dashboard client top-level prop option hardening. Tightened the main dashboard client so malformed top-level props are normalized before reading SSR cattle/sales pages, summary, notifications, feed standards, inventory, schedule, feed history, buildings, farm settings, expenses, market price, or profitability payloads. This prevents direct/test/reuse callers from throwing during dashboard render setup before existing pagination hooks, collection normalizers, Korean fallback copy, widget wiring, modal state, and mutation guards can run. Strengthened home dashboard source coverage for safe dashboard client option normalization. Verified focused home-market tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1164 | `[hanwoo-dashboard]` Cattle detail modal prop, cattle-payload, callback, helper, and fallback chart hardening. Tightened the cattle detail modal so malformed top-level props are normalized before reading cattle, buildings, busy state, or detail callbacks; malformed cattle payloads now return no modal instead of flowing into detail rendering; malformed close/edit/delete/update callbacks fall back to safe no-op/async no-op handlers before Escape, header close, edit, archive, or breeding-record save actions invoke them. Also normalized malformed fallback `weightHistory` chart rows and the modal helper option payloads before `SectionTitle`/`InfoItem` destructuring. This prevents direct/test/reuse callers from throwing during detail render, close, edit, archive, Escape, breeding-record save setup, helper rendering, or fallback chart rendering while preserving building payload normalization, history fallback, async breeding save locks, archive busy locking, Korean detail copy, focus management, and dashboard cattle detail behavior. Strengthened cattle detail source coverage for safe detail option, cattle, callback, helper, and fallback chart normalization. Verified focused cattle detail/form tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1163 | `[hanwoo-dashboard]` Field mode view prop, list, and callback hardening. Tightened the field-mode view so malformed top-level props are normalized before reading cattle rows, background full-list loaders, select callbacks, or close callbacks. Malformed cattle list payloads are filtered before search, critical-alert counters, total herd count, and the embedded ear-tag scanner; malformed full-list loader/select/close callbacks fall back to safe null/no-op handlers before load, search-result selection, scanner selection, or mode-close actions invoke them. This prevents direct/test/reuse callers from throwing during field-mode render, search, stats, scanner handoff, close, or background loading while preserving checklist persistence, stale-load cleanup, celebration animation guards, Korean field-mode copy, loading announcements, and dashboard field-mode behavior. Strengthened field-mode source coverage for safe option, list, and callback normalization. Verified focused field/scanner tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1162 | `[hanwoo-dashboard]` Ear tag scanner modal prop, list, and callback hardening. Tightened the ear tag scanner modal so malformed top-level props are normalized before reading open state, cattle rows, close callbacks, or select callbacks. Malformed cattle list payloads are filtered before simulated target selection, manual choice rendering, retry, and lookup; malformed close/select callbacks fall back to safe no-ops before header close, footer close, or confirm actions invoke them. This prevents direct/test/reuse callers from throwing during scanner render, scan setup, manual selection, retry, close, or confirm handling while preserving scanner animation guards, Korean task labels, live result announcements, missing birth-date copy, tactile/audio feedback, and dashboard scanner behavior. Strengthened scanner modal source coverage for safe option, list, and callback normalization. Verified focused scanner modal tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1161 | `[hanwoo-dashboard]` Financial chart widget prop option hardening. Tightened the home financial chart widget so malformed top-level props are normalized before reading sale, expense, or series data. This prevents direct/test/reuse callers from throwing during financial chart render setup while preserving existing collection row filtering, strict month parsing, numeric coercion, fallback six-month aggregation, Korean chart labels, accessible chart summary, and dashboard financial widget behavior. Strengthened analysis source coverage for safe financial chart option normalization. Verified focused analysis tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1160 | `[hanwoo-dashboard]` Cattle form prop and callback hardening. Tightened the cattle form so malformed top-level props are normalized before reading cattle, building, submit, or cancel options. Malformed submit/cancel callbacks now fall back to safe async/no-op handlers before save, cancel-button, or Escape-key handling. This prevents direct/test/reuse callers from throwing during cattle form render, cancel, Escape, or submit setup while preserving building payload normalization, tag lookup guards, date conversion, async save locks, focus management, validation wiring, Korean form copy, and dashboard cattle create/edit behavior. Strengthened cattle form/detail source coverage for safe form option and callback normalization. Verified focused cattle form/detail tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1159 | `[hanwoo-dashboard]` Analysis tab prop option hardening. Tightened the analysis tab so malformed top-level props are normalized before reading sales, feed, cattle, or expense payloads. This prevents direct/test/reuse callers from throwing during analysis render setup while preserving existing collection row normalization, KPI card option normalization, monthly revenue/cost/profit aggregation, feed-average calculation, chart labeling, Korean analysis copy, and dashboard analysis behavior. Strengthened analysis source coverage for safe analysis tab option normalization. Verified focused analysis tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1158 | `[hanwoo-dashboard]` Settings tab prop, widget-control, and mutation-callback hardening. Tightened the settings tab so malformed top-level props are normalized before reading building payloads, farm settings, theme state, widget registry/visibility, quick-action intent, or settings/building/theme/widget callbacks. Malformed create-building, delete-building, farm-settings, theme-toggle, and widget-toggle callbacks now fall back to safe no-ops before submit/delete/switch handling; malformed widget registry and visibility payloads are normalized before `.length`, `.map()`, and switch-state access. This prevents direct/test/reuse callers from throwing during settings render, widget interaction, farm save, building create, or building delete setup while preserving building payload normalization, Korean settings copy, async save/delete locks, mounted-state cleanup, validation wiring, diagnostics link, and existing dashboard settings behavior. Strengthened settings and empty-state source coverage for safe settings option, widget, and callback normalization. Verified focused settings/empty-state tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1157 | `[hanwoo-dashboard]` Feed tab prop and mutation-callback hardening. Tightened the feed tab so malformed top-level props are normalized before reading cattle, feed standards, feed history, buildings, or the feed-record mutation callback. Malformed feed-record callbacks now fall back to a safe async no-op before submit handling, preventing direct/test/reuse callers from throwing during feed render or mutation setup while preserving existing cattle/feed/building payload normalization, feed aggregation, Korean feed copy, async save locks, mounted-state cleanup, validation wiring, chart labeling, and dashboard feed behavior. Strengthened empty-state/feed source coverage for safe feed tab option and callback normalization. Verified focused feed/empty-state tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1156 | `[hanwoo-dashboard]` Sales tab prop, mutation-callback, and pagination-control hardening. Tightened the sales tab so malformed top-level props are normalized before reading sales/cattle/expense payloads, market data, pagination state, quick-action intent, or the sale-create callback. Malformed sale-create callbacks now fall back to a safe async no-op before submit handling, and malformed pagination/load-more callbacks fall back to a safe no-op before the load-more button can invoke them. This prevents direct/test/reuse callers from throwing during sales render, create submit, or pagination interaction while preserving collection normalization, profit aggregation, Korean empty-state/form/pagination copy, async save locks, mounted-state cleanup, validation wiring, chart labeling, and existing dashboard sales behavior. Strengthened sales, empty-state, and pagination source coverage for safe sales option, callback, and pagination-control normalization. Verified focused sales/empty-state/pagination tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1155 | `[hanwoo-dashboard]` Calving tab prop and mutation-callback hardening. Tightened the calving tab so malformed top-level props are normalized before reading cattle/building payloads or the calving mutation callback, and malformed record-calving callbacks fall back to a safe async no-op before the submit handler invokes them. This prevents direct/test/reuse callers from throwing during calving render or mutation setup while preserving cattle/building payload normalization, pregnancy-date ordering, Korean calving form copy, async save locks, mounted-state cleanup, validation wiring, countdown labels, and existing dashboard calving behavior. Strengthened calving source coverage for safe calving tab option and callback normalization. Verified focused calving tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1154 | `[hanwoo-dashboard]` Schedule tab prop and mutation-callback hardening. Tightened the schedule tab so malformed top-level props are normalized before reading event payloads, mutation callbacks, or quick-action intent, and malformed create/toggle callbacks fall back to safe async no-ops before submit/toggle handlers invoke them. This prevents direct/test/reuse callers from throwing during schedule render, create submit, or completion-toggle setup while preserving event payload normalization, Korean calendar/form copy, async save locks, mounted-state cleanup, validation wiring, countdown labels, and existing dashboard schedule behavior. Strengthened schedule and empty-state source coverage for safe schedule tab option and callback normalization. Verified focused schedule/empty-state tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1153 | `[hanwoo-dashboard]` Inventory tab prop and mutation-callback hardening. Tightened the inventory tab so malformed top-level props are normalized before reading inventory payloads, mutation callbacks, or quick-action intent, and malformed add/update callbacks fall back to safe async no-ops before submit/update handlers invoke them. This prevents direct/test/reuse callers from throwing during inventory render, create submit, or inline quantity update setup while preserving inventory row normalization, Korean empty-state/form copy, async save locks, mounted-state cleanup, validation wiring, and existing dashboard inventory behavior. Strengthened empty-state and home-market source coverage for safe inventory tab option and callback normalization. Verified focused inventory/home tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1152 | `[hanwoo-dashboard]` Subscription fallback prop option hardening. Tightened the subscription success/fail fallback status components so malformed top-level props are normalized before reading `message`. This prevents direct/test/reuse callers from throwing during payment result loading-state render setup while preserving Korean loading/failure/success copy, polite busy status semantics, retry navigation fallbacks, guarded success timers, and existing payment confirmation behavior. Strengthened payment UX source coverage for safe subscription fallback option normalization on both result pages. Verified focused payment UX tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1151 | `[hanwoo-dashboard]` Root layout prop option hardening. Tightened the app root layout so malformed top-level props are normalized before reading `children`. This prevents direct/test/reuse callers from throwing during root shell render setup while preserving the Korean metadata/PWA copy, font variable setup, FeedbackProvider wrapping, Suspense boundary, `lang="ko"`, and hydration-warning behavior. Strengthened app metadata source coverage for safe root-layout option normalization. Verified focused app metadata test, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1150 | `[hanwoo-dashboard]` App error boundary prop and reset hardening. Tightened the route and global error boundaries so malformed top-level props are normalized before reading `error` or `reset`, and malformed reset callbacks fall back to safe no-ops before retry buttons invoke them. This prevents direct/test/reuse callers from throwing during app-level failure UI render or retry handling while preserving client-boundary requirements, Korean retry/home copy, route home action, global-error html/body contract, and existing error logging. Strengthened error-page source coverage for safe error-boundary option and reset normalization. Verified focused error-page tests, full Hanwoo tests, eslint, Next production build, and root project QC runner after rerunning once without a concurrent build lock. | Codex | 2026-05-28 |
| T-1149 | `[hanwoo-dashboard]` Legal document layout prop option hardening. Tightened the shared legal document layout so malformed top-level props are normalized before reading `eyebrow`, `title`, `subtitle`, `lastUpdated`, or `children`. This prevents direct/test/reuse callers from throwing during legal page layout render setup while preserving privacy/terms support-channel copy, back-home link semantics, decorative back icon hiding, and existing legal page shell styling. Strengthened legal pages source coverage for safe layout option normalization. Verified focused legal pages test, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1148 | `[hanwoo-dashboard]` Dashboard helper panel prop and collection hardening. Tightened the home dashboard's internal `TodayFocusPanel`, `QuickActionPanel`, `SetupProgressPanel`, and `PenCattleList` helpers so malformed top-level props are normalized before reading item/action/progress/cattle collections or callbacks. Panel item collections now filter malformed rows before `.length`, `.map()`, or `.filter()` access; malformed callbacks fall back to safe no-ops; setup progress numbers are finite/clamped before progressbar labels and width; quick actions fall back to a safe icon. This prevents direct/test/reuse callers from throwing during home panel render or button handling while preserving today-focus labels, quick-action labels, setup progress semantics, pen cattle filtering, Korean empty-pen copy, and existing dashboard navigation behavior. Strengthened home-market source coverage for safe dashboard helper normalization. Verified focused home-market tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1147 | `[hanwoo-dashboard]` Feed helper prop and input option hardening. Tightened the Feed tab's internal `FilterChip` and `Field` helpers so malformed top-level props are normalized before reading selected state, labels, callbacks, disabled state, suffix, errors, or input props. `FilterChip` now ignores malformed click handlers before wiring button interaction, and `Field` normalizes `inputProps` with a safe fallback field id before spreading input attributes. This prevents direct/test/reuse callers from throwing during feed filter or feed input render setup while preserving filter chip labels, busy/pressed semantics, feed validation wiring, Korean feed copy, and existing feed aggregation behavior. Strengthened Feed source coverage for safe helper option normalization. Verified focused Feed/empty-state tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1146 | `[hanwoo-dashboard]` Analysis KPI card prop option hardening. Tightened the Analysis tab's internal `KpiCard` helper so malformed top-level props are normalized before reading `title`, `value`, `icon`, or `accent`. This prevents direct/test/reuse callers from throwing during KPI card render setup while preserving KPI labels, money formatting, decorative icon semantics, accent styling, Korean analysis copy, and existing financial aggregation behavior. Strengthened analysis source coverage for safe KPI card option normalization. Verified focused analysis tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1145 | `[hanwoo-dashboard]` Cattle detail helper prop option hardening. Tightened the cattle detail modal's internal `SectionTitle` and `InfoItem` helpers so malformed top-level props are normalized before reading heading icon/title/color or info label/value/highlight/delay fields. This prevents direct/test/reuse callers from throwing during modal helper render setup while preserving heading semantics, decorative icon hiding, highlight typography, animation delays, hover styling, Korean detail copy, and existing cattle form/detail focus behavior. Strengthened cattle detail source coverage for safe helper option normalization. Verified focused cattle detail tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-28 |
| T-1144 | `[hanwoo-dashboard]` Market price panel prop and rows hardening. Tightened the market price widget's internal `PricePanel` helper so malformed top-level props are normalized before reading `title` or `rows`, and malformed row collections are filtered before rendering price rows. This prevents direct/test/reuse callers from throwing during market price panel render setup or raw `rows.map()` access while preserving Korean market copy, kg unit labels, source badges, stale/unavailable states, polling guards, refresh controls, and price formatting. Strengthened home-market source coverage for safe price-panel option and row normalization. Verified focused home-market tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1143 | `[hanwoo-dashboard]` AI insight badge prop option hardening. Tightened the AI insight widget's internal `PriorityBadge`, `SourceBadge`, and `CacheBadge` helpers so malformed top-level props are normalized before reading `priority`, `source`, or `ageSeconds`. This prevents direct/test/reuse callers from throwing during badge render setup while preserving priority fallback styling, AI-vs-rule source labels, cached-AI metadata visibility, cache-age formatting, timeout fallback copy, and manual refresh behavior. Strengthened AI insight source coverage for safe badge option normalization. Verified focused AI insight tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1142 | `[hanwoo-dashboard]` Diagnostics status-card prop option hardening. Tightened the admin diagnostics `StatusCard` helper so malformed top-level props are normalized before reading `title`, `value`, `sub`, `icon`, or `status`. This prevents direct/test/reuse callers from throwing during diagnostics card render setup while preserving neutral fallback status styling, Korean operations copy, numeric diagnostics normalization, loading announcements, and dashboard-return feedback. Strengthened diagnostics source coverage for safe status-card option normalization. Verified focused diagnostics tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1141 | `[hanwoo-dashboard]` Feedback provider prop and toast-option hardening. Tightened `FeedbackProvider` so malformed top-level props are normalized before reading `children`, and malformed `notify()` option input falls back to safe toast defaults before reading title, description, variant, or duration. This prevents direct/test/reuse callers from throwing during provider render or feedback notification scheduling while preserving live-region toast semantics, mounted-state guards, timeout cleanup, Korean dismiss labels, confirmation dialog labels, and existing `useAppFeedback()` contract. Strengthened feedback provider source coverage for safe provider and toast option normalization. Verified focused feedback provider tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1140 | `[hanwoo-dashboard]` Notification modal prop and close-callback hardening. Tightened `NotificationModal` so malformed top-level props are normalized before reading `id`, `notifications`, `onClose`, or `onTestSMS`, and malformed `onClose` callbacks fall back to a safe no-op before overlay, Escape, or close-button dismissal. This prevents direct/test/reuse callers from throwing during modal render or dismissal while preserving notification payload filtering, focus management, SMS busy locking, Korean modal copy, and safe optional SMS test handling. Strengthened notification modal source coverage for safe option and callback normalization. Verified focused notification modal tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1139 | `[hanwoo-dashboard]` Alert banner prop option hardening. Tightened `EstrusAlertBanner` and `CalvingAlertBanner` so malformed top-level props are normalized before reading `notifications` or `buildings`. This prevents direct/test/reuse callers from throwing during render setup before notification filtering, building lookup normalization, remaining-day labels, Korean fallback copy, and date formatting can run. Strengthened alert banner source coverage for safe top-level option normalization while preserving existing malformed notification/building payload guards. Verified focused alert banner tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1138 | `[hanwoo-dashboard]` Shared cattle cards prop and callback hardening. Tightened reusable `StatCard`, `PenCard`, and `CattleRow` props so malformed top-level options are normalized before reading labels, values, cattle payloads, delay values, drag state, or callbacks. Added safe no-op guards for malformed `onSelect`, `onDrop`, and `onClick` callbacks before user interaction, preventing future dashboard/pen/cattle row reuse from throwing during render or click/drop handling while preserving native button semantics, existing payload normalization, alert labels, card styling, drag/drop payload normalization, and accessible labels. Strengthened cards source coverage for safe option and callback normalization. Verified focused cards tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1137 | `[hanwoo-dashboard]` Premium input prop option hardening. Tightened the reusable `PremiumInput`, `PremiumTextarea`, `PremiumSelect`, and `PremiumLabel` primitives so malformed top-level props are normalized before reading class names, input type, error state, children, or passthrough props. This prevents future feed, sales, inventory, and settings form reuse from throwing during render setup while preserving refs, display names, premium input styling, date monospace styling, error-state styling, children rendering, and passthrough attributes. Extended UI primitive source coverage for safe premium-input option normalization. Verified focused UI primitive tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1136 | `[hanwoo-dashboard]` Shared dialog prop option hardening. Tightened the reusable `DialogOverlay`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, and `DialogDescription` primitives so malformed top-level props are normalized before reading class names, children, close labels, or passthrough props. This prevents future modal/confirmation UI reuse from throwing during render setup while preserving Radix dialog refs/display names, contextual Korean close labels, overlay/content styling, class merging, and passthrough attributes. Extended UI primitive source coverage for safe dialog option normalization and restored the default close label as readable Korean. Verified focused UI/dialog tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1135 | `[hanwoo-dashboard]` Shared select prop option hardening. Tightened the reusable `SelectTrigger`, `SelectContent`, `SelectItem`, `SelectSeparator`, and `SelectLabel` primitives so malformed top-level props are normalized before reading class names, children, position, or passthrough props. This prevents future form/filter select reuse from throwing during render setup while preserving Radix select refs/display names, decorative icons, popper positioning, item text, class merging, and passthrough attributes. Extended UI primitive source coverage for safe select option normalization. Verified focused UI/dialog tests, full Hanwoo tests, eslint, Next production build, and root project QC runner after a one-time concurrent build-lock retry. | Codex | 2026-05-27 |
| T-1134 | `[hanwoo-dashboard]` Shared tabs prop option hardening. Tightened the reusable `TabsList`, `TabsTrigger`, and `TabsContent` primitives so malformed top-level props are normalized before reading class names or passthrough props. This prevents future dashboard/tab UI reuse from throwing during render setup while preserving Radix tabs refs/display names, tab styling, class merging, and passthrough attributes. Extended UI primitive source coverage for safe tabs option normalization. Verified focused UI primitive tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1133 | `[hanwoo-dashboard]` Avatar and skeleton prop option hardening. Tightened the reusable `Avatar`, `AvatarImage`, `AvatarFallback`, and `Skeleton` primitives so malformed top-level props are normalized before reading class names or passthrough props. This prevents future account/status/loading UI reuse from throwing during render setup while preserving Radix avatar refs/display names, skeleton styling, class merging, and passthrough attributes. Extended UI primitive source coverage for safe avatar/skeleton option normalization. Verified focused UI primitive tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1132 | `[hanwoo-dashboard]` Form primitive and progress prop option hardening. Tightened the reusable `Input`, `Label`, and `Progress` primitives so malformed top-level props are normalized before reading class names, input type, progress value, or passthrough props. `Progress` now also coerces non-finite values to `0` and clamps finite values to `0..100` before computing the indicator transform, preventing malformed reuse from rendering `NaN` or out-of-range progress offsets. Preserved refs, display names, existing styling, and passthrough attributes. Extended UI primitive source coverage for safe form/progress option and value normalization. Verified focused UI primitive tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1131 | `[hanwoo-dashboard]` Premium card prop option hardening. Tightened the reusable `PremiumCard`, `PremiumCardHeader`, `PremiumCardTitle`, `PremiumCardDescription`, `PremiumCardContent`, `PremiumCardFooter`, and `PremiumInfoCard` primitives so malformed top-level props are normalized before reading class names, header title/icon/description/children, info-card values, or passthrough props. This prevents direct/test/reuse callers from throwing during render setup while preserving refs, display names, premium card styling, profitability widget header rendering, class merging, and passthrough attributes. Extended UI primitive and profitability source coverage for safe premium-card option normalization. Verified focused UI/profitability tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1130 | `[hanwoo-dashboard]` Shared card prop option hardening. Tightened the reusable `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, and `CardFooter` primitives so malformed top-level props are normalized before reading class names or passthrough props. This prevents direct/test/reuse callers from throwing during render setup while preserving refs, display names, class merging, passthrough attributes, and existing dashboard/card layout styling. Extended UI primitive source coverage for safe card option normalization. Verified focused UI primitive tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1129 | `[hanwoo-dashboard]` Shared badge prop option hardening. Tightened the reusable `Badge` primitive so malformed top-level props are normalized before reading class names, variants, or passthrough props. This prevents direct/test/reuse callers from throwing during render setup while preserving badge variant styling, class merging, passthrough attributes, and existing dashboard badge usage. Added dedicated UI primitive source coverage for safe badge option normalization. Verified focused UI primitive tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1128 | `[hanwoo-dashboard]` Shared button prop option hardening. Tightened the reusable `Button` and `PremiumButton` primitives so malformed top-level props are normalized before reading class names, variants, sizes, slot mode, type, or passthrough props. This prevents direct/test/reuse callers from throwing during render setup while preserving default non-submit semantics, explicit submit overrides, Slot/asChild behavior, variant styling, refs, and existing confirmation/payment/empty-state actions. Strengthened shared button source coverage for safe option normalization. Verified focused button tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1127 | `[hanwoo-dashboard]` Shared dropdown menu prop option hardening. Tightened the reusable dropdown menu primitives so malformed top-level props are normalized before reading children, class names, click handlers, or passthrough props. Invalid dropdown triggers now render nothing instead of throwing through `React.cloneElement`, and non-function item click handlers are ignored before choosing button semantics or wiring `onClick`. This prevents direct/test/reuse callers from crashing notification and shared dropdown flows while preserving native button semantics, focus styling, passthrough props, labels, and existing notification menu actions. Strengthened notification/dropdown source coverage for safe option, trigger-child, and click-handler normalization. Verified focused notification/dropdown tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1126 | `[hanwoo-dashboard]` Shared empty-state prop option hardening. Tightened the reusable empty-state component so malformed top-level props are normalized before reading icon, title, description, action label, action callback, or disabled state, and non-function action callbacks are ignored before wiring the action button. This prevents direct/test/reuse callers from throwing during render setup or passing malformed callbacks into the shared action button while preserving operational empty-state copy, native action semantics, disabled/busy state, and existing inventory/sales/schedule/settings flows. Strengthened empty-state source coverage for safe option and callback normalization. Verified focused empty-state tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1125 | `[hanwoo-dashboard]` Payment widget prop and amount hardening. Tightened the subscription payment widget so malformed top-level props are normalized before reading payment keys, amount, order name, customer name, or customer email, and malformed payment amounts fall back to a finite safe value before button label formatting, widget rendering, and prepare-request payloads use `price`. This prevents direct/test/reuse callers from throwing during render setup or payment button labeling while preserving Korean checkout copy, Toss widget timeout handling, duplicate payment request guards, redirect URL safety, and payment API fallback behavior. Strengthened payment UX source coverage for safe widget option and amount normalization. Verified focused payment UX tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1124 | `[hanwoo-dashboard]` Excel export button prop option hardening. Tightened the cattle CSV export button so malformed top-level props are normalized before reading `cattleList` or `resolveCattleList`. This prevents direct/test/reuse callers from throwing during render setup before duplicate-download locking, async list resolution, empty-list warning feedback, CSV generation, temporary link cleanup, URL revocation, and Korean button/status copy can run. Strengthened Excel export source coverage for safe button option normalization. Verified focused Excel export tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1123 | `[hanwoo-dashboard]` QR widget prop option hardening. Tightened the QR label widget so malformed top-level props are normalized before reading `value` or `label`, non-empty string/finite numeric values are converted to safe QR text, and missing labels fall back to Korean QR label copy. This prevents direct/test/reuse callers from throwing during render setup or printing while preserving Korean print button/status copy, document title/name, QR rendering, and duplicate-print guards. Strengthened QR source coverage for safe widget option and QR text normalization. Verified focused QR tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1122 | `[hanwoo-dashboard]` Notification widget prop option hardening. Tightened the home notification widget so malformed top-level props are normalized before reading `notifications`. This prevents direct/test/reuse callers from throwing during render setup before notification row filtering, Korean fallback title/message copy, priority alert heading, critical badge rendering, and empty-state null rendering can run. Strengthened notification source coverage for safe widget option normalization. Verified focused notification tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1121 | `[hanwoo-dashboard]` Tab navigation prop and callback hardening. Tightened the shared dashboard tab bar so malformed top-level props are normalized before reading `activeTab` or `onTabChange`, and missing/non-function tab-change callbacks become a safe no-op. This prevents direct/test/reuse callers from throwing during tab navigation render or click handling while preserving Korean action labels, selected-state semantics, and existing dashboard tab switching. Strengthened home-market source coverage for safe tab bar option and callback normalization. Verified focused home-market tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1120 | `[hanwoo-dashboard]` AI insight widget prop option hardening. Tightened the home AI insight widget so malformed top-level props are normalized before reading `summary`. This prevents direct/test/reuse callers from throwing during render setup before deterministic heuristic cards, AI request fallback, cache metadata, manual refresh, timeout handling, and Korean accessible copy can run. Strengthened AI insight widget source coverage for safe widget option normalization. Verified focused AI insight tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1119 | `[hanwoo-dashboard]` Profitability widget prop option hardening. Tightened the shipment profitability widget so malformed top-level props are normalized before reading `data`, `isLoading`, `error`, or `meta`. This prevents direct/test/reuse callers from throwing during render setup before loading/error/empty states, recommendation row filtering, finite-number rendering, customized assumption labels, and Korean operator copy can run. Strengthened profitability source coverage for safe widget option normalization. Verified focused profitability tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1118 | `[hanwoo-dashboard]` Weather widget prop option hardening. Tightened the home weather widget so malformed top-level props are normalized before reading `weather`. This prevents direct/test/reuse callers from throwing during render setup before unavailable-weather fallback, numeric weather normalization, THI labels, forecast filtering, livestock alerts, and Korean loading/unavailable copy can run. Strengthened home-market source coverage for safe weather widget option normalization. Verified focused home-market tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1117 | `[hanwoo-dashboard]` Market price widget prop option hardening. Tightened the market price widget so malformed top-level props are normalized before reading `initialData`. This prevents direct/test/reuse callers from throwing during render setup before market snapshot normalization, loading fallback, KAPE source badges, manual refresh, guarded polling, and Korean unavailable copy can run. Strengthened home-market source coverage for safe market widget option normalization. Verified focused home-market tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1116 | `[hanwoo-dashboard]` Cattle-row malformed payload hardening. Tightened the reusable cattle row component so malformed direct `cow` props are normalized before rendering, drag payload creation, status-color lookup, accessible labels, genetic-grade fallback, and click callbacks. This prevents direct/reuse callers from crashing on null, arrays, missing nested genetic info, missing names, missing tag numbers, or missing weight values while preserving normal dashboard row behavior. Strengthened card source coverage for safe cattle-row payload normalization. Verified focused card tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1115 | `[hanwoo-dashboard]` Pen-card drag-drop payload hardening. Tightened pen-card drop handling so external drag payloads are normalized before moving cattle. Malformed JSON payloads, arrays, objects without a usable `cattleId`, empty string ids, and non-finite numeric ids are ignored instead of reaching the dashboard move callback, while valid string or finite numeric ids still move through the existing confirmation flow. Strengthened card source coverage for safe drop payload normalization. Verified focused card tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1114 | `[hanwoo-dashboard]` Admin diagnostics raw-data malformed response hardening. Tightened the admin diagnostics raw-data loader so `getRawData()` responses are normalized before reading `success`, `data`, or `message`. This prevents malformed or null raw-record responses from throwing generic client errors before the operations page can show safe Korean retry feedback, while preserving null-safe raw-data state. Strengthened diagnostics source coverage for safe raw-data response normalization. Verified focused diagnostics tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1113 | `[hanwoo-dashboard]` Admin diagnostics malformed response hardening. Tightened the admin diagnostics client so `getSystemDiagnostics()` responses are normalized before rendering database, record-count, memory, uptime, and Node fields. This prevents malformed or partial diagnostics payloads from crashing the operations page through missing `database`/`memory` objects or non-object `recordCounts`, while preserving Korean fallback copy and finite numeric rendering. Strengthened diagnostics source coverage for safe diagnostics response normalization. Verified focused diagnostics tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1112 | `[hanwoo-dashboard]` Cattle detail fallback weight chart payload hardening. Tightened the cattle detail modal fallback chart path so legacy `cattle.weightHistory` values are normalized to object rows after direct array input or JSON string parsing. This prevents malformed cached/legacy weight history payloads from passing objects, scalars, or primitive array entries into the Recharts `LineChart` data prop while preserving history-event based chart points and existing invalid JSON fallback behavior. Strengthened cattle-detail source coverage for safe fallback chart normalization. Verified focused cattle-detail/history tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1111 | `[blind-to-x]` Best-of-N `comment_trigger_avg` 영속화 → tuner sweep 분기 활성화 (`cc37acff`). 직전 세션 T-1105 의 `tune_best_of_n_weight.py` sweep 분기가 컬럼 부재로 항상 dead 였던 문제 정비. 5위치 동시 — `cost_db._MIGRATION_COLUMNS` 화이트리스트 + 컬럼 + `update_draft_comment_trigger_avg` 메서드, `draft_analytics.record_draft_event` 파라미터, `draft_generator` Best-of-N picker 가 `drafts_dict["_comment_trigger_avg"]` 영속화, `persist_stage` 가 그걸 추출해 record 에 전달, tuner docstring 정확도. 발행분마다 자동 누적, 5건 이상 모이면 sweep 자동 활성화(코드 변경 0). 5 신규 테스트, focused 54/54 pass, full blind-to-x unit 1703/1703 pass, ruff format/check clean. 커밋 메시지 내 "T-1107" 라벨은 동시 작업 중인 Codex T-1107 과 ID 충돌이 발생해 TASKS 에서는 T-1111 로 정리. | Claude Opus 4.7 (1M context) | 2026-05-27 |
| T-1111 | `[hanwoo-dashboard]` Notification system prop option hardening. Tightened the layout notification dropdown so malformed top-level props are normalized before reading `initialNotifications`. This prevents direct/test/reuse callers from throwing during render setup before notification payload normalization, unread counts, Korean accessible labels, mark-read actions, and safe empty-state rendering can run. Strengthened notification-system source coverage for safe prop option normalization while preserving existing malformed notification-row filtering and default Korean title/message fallbacks. Verified focused notification tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1110 | `[hanwoo-dashboard]` Pagination hook constructor option hardening. Tightened the shared cursor pagination hook plus cattle and sales pagination hooks so malformed top-level hook options are normalized before reading `endpoint`, `initialItems`, or `initialPageInfo`. This prevents direct/test/reuse callers from throwing during hook setup before pagination can preserve normalized initial item state, timeout protection, in-flight guards, Korean retry feedback, and safe load-more behavior. Strengthened focused pagination source coverage for safe constructor option normalization across shared, cattle, and sales hooks. Verified focused pagination tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1109 | `[hanwoo-dashboard]` Pagination load-more parameter hardening. Tightened the shared cursor pagination hook plus cattle and sales pagination hooks so malformed load-more parameter input is normalized before `Object.entries()` or destructuring. This prevents direct/retry callers from throwing before pagination can preserve timeout protection, in-flight guards, Korean retry feedback, and page-info safety checks. Strengthened focused pagination source coverage for safe parameter normalization across shared, cattle, and sales load-more paths. Verified focused pagination tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1108 | `[hanwoo-dashboard]` Dashboard full-list preload option hardening. Tightened the complete cattle/sales list preload helpers so malformed option input is normalized before reading `silent`. This prevents direct retry/preload callers from throwing during parameter destructuring before the full-list load path can reuse in-flight requests, show retry feedback, or log non-silent failures. Strengthened home dashboard source coverage for the safe option normalization while preserving existing full-list loading, retry, busy-status, and stale-unmount guards. Verified focused home dashboard tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1108 | `[blind-to-x]` `/goal "프로젝트 개선안을 만들고 완료해줘"` — 기술부채/리팩터 정리 (행동 변화 0). 3개 Explore agent 보고서의 과장(source-grep 회귀 6건/CI test step 부재/skip 14건)을 실측 검증 후 진짜 부채만 손봄. (1) stale `@pytest.mark.skip` 1건 삭제 — `test_multi_platform.py:244` naver_blog는 config `output_formats==["twitter"]`와 정책 모순인 dead test. (2) silent `pytest.skip("No tone_mapping/golden_examples")` 2건을 `assert tone_map, "policy contract"`로 전환(`test_p0_enhancements.py`) — YAML 키 회귀 시 silent skip 대신 fail. (3) `pyproject.toml`에 `[tool.ruff]`(루트 `ruff.toml` `extend`로 명시 상속 — 안 그러면 ruff upward 검색 멈춤) + `[tool.mypy]`(파일 화이트리스트, `pipeline.draft_contract`/`pipeline.harness_guard` strict) + `mypy>=1.10.0` dev dep. (4) orphan `projects/blind-to-x/.github/workflows/blind-to-x.yml` (GitHub Actions가 안 읽음, 실 CI는 root `full-test-matrix.yml`) 의도 헤더 코멘트 + `test_config_workflow_sync.py` 모듈 docstring. 검증: 1759/1759 pass 0 skip 0 fail (베이스라인 1680u + 1skip → 회귀 0), ruff clean. 후속: `pipeline/_archive/` 774줄 dead newsletter 코드 삭제는 auto-mode classifier 권한 보류. | Claude Opus 4.7 (1M context) | 2026-05-27 |
| T-1107 | `[hanwoo-dashboard]` AI Insight cache API/UI contract regression coverage. Strengthened `ai-insight-widget-copy.test.mjs` so the existing same-day AI insight cache behavior is locked down at the source-contract level: the widget must send `forceRefresh` with the summary payload, render cache metadata only for cached AI responses, normalize finite cache age values for `CacheBadge`, and the API route must build per-user cache keys, reuse cached AI responses with `cachedAt`/`ageSeconds`, clear cache entries on force refresh, store successful AI responses, and mark fallback/refresh responses as uncached. Verified focused AI insight cache/API/widget tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1106 | `[workspace]` `/goal "개선안 만들고 완성품 만들어줘"` — 3개 커밋 출하: Codex T-1008~T-1100 하드닝 94개 파일 검증·일괄 커밋(`ecdfcca5`), hanwoo AI Insight 일일 캐시(`1bbe34ba`), blind-to-x Best-of-N comment-weight 튜너 dry-run CLI(`12f16a04`). 최종 검증: hanwoo `npm test` 439/439, lint clean, build exit 0; blind-to-x `pytest test_tune_best_of_n_weight.py` 15/15, ruff clean. T-251은 user-owned로 유지. | Claude Opus 4.7 (1M context) | 2026-05-27 |
| T-1105 | `[blind-to-x]` Best-of-N comment-weight 튜닝 dry-run CLI 신규. `scripts/tune_best_of_n_weight.py` 가 최근 `draft_analytics` published 로우의 `engagement_rate` 와 hook/virality/fit/final_rank 각 축 Pearson r 계산 후, comment_trigger_avg 데이터가 있으면 `w∈{0..1}` sweep으로 `llm.best_of_n_comment_weight` 추천(config 미변경). 데이터 부족 시 0.5 유지, final_rank 상관 기반 ±0.1 자동 권장. 15개 신규 unit test (`tests/unit/test_tune_best_of_n_weight.py`) + `--json` 자동화 모드. ruff format/check clean. | Claude Opus 4.7 (1M context) | 2026-05-27 |
| T-1104 | `[hanwoo-dashboard]` AI Insight 일일 in-memory 캐시 신규. 같은 사용자가 같은 농장 summary로 하루 동안 페이지를 여러 번 열어도 Gemini 호출은 1회. `src/lib/ai-insight-cache.mjs` — userId + Asia/Seoul YYYY-MM-DD + summary SHA-256(16) 결합 키, AI 응답만 캐싱(휴리스틱 제외), 256엔트리 prune. `route.js`는 cache hit 시 `cached:true / cachedAt / ageSeconds` 반환, `forceRefresh:true`는 키 삭제 후 재호출. 위젯엔 "N분 전 캐시" 미니 배지. 14개 신규 unit test. | Claude Opus 4.7 (1M context) | 2026-05-27 |
| T-1103 | `[hanwoo-dashboard]` AI insight cache helper malformed-input hardening. Tightened the new AI insight cache helpers so malformed cache-key input, circular or non-JSON summary values, malformed cache timestamps, and malformed cache options are normalized before hashing, storing, pruning, or calculating cache age. This prevents cache metadata bugs from throwing or surfacing non-finite cache age values while preserving same-day per-user AI insight reuse and force-refresh behavior. Added focused regression coverage for malformed AI insight cache helper inputs. Verified focused AI insight cache/API/widget tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1102 | `[hanwoo-dashboard]` Expense record filter malformed-input hardening. Tightened `getExpenseRecords()` so malformed filter input is normalized before reading cattle, building, category, or date filter fields. This prevents caller bugs from falling into the generic expense fetch catch/log path before the action can build a safe empty query filter and apply guarded date parsing. Strengthened existing expense filter source regression coverage for safe filter normalization. Verified focused expense/action copy tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1101 | `[hanwoo-dashboard]` Auth credential dependency-input hardening. Tightened `authorizeCredentials()` so malformed dependency-injection input is normalized before reading `loadPrisma` or `loadBcrypt`. This prevents caller/test harness bugs from throwing generic TypeErrors before the login credential helper can preserve its safe invalid-credential `null` fallback. Added focused regression coverage for malformed auth dependency input. Verified focused auth credential/guard tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1100 | `[hanwoo-dashboard]` Dashboard pagination guard malformed-helper-input hardening. Tightened shared dashboard pagination guard helpers so malformed top-level helper input, malformed page-info payloads, and malformed `seenCursors` input are normalized before page transition checks. This prevents caller/cache bugs from throwing generic TypeErrors before pagination can stop safely, report loop/cursor errors, or continue with a valid next cursor. Added focused regression coverage for malformed pagination guard helper input. Verified focused pagination guard and pagination hook tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1099 | `[hanwoo-dashboard]` Payment confirmation malformed-helper-input hardening. Tightened payment confirmation helper functions so malformed caller input is normalized before reading gateway payloads, raw response text, fallback messages, status codes, parse errors, expected amounts, or clock providers. This prevents caller bugs from throwing generic TypeErrors before the payment flow can return safe Korean fallback responses, pending verification states, or amount-mismatch failures. Added focused regression coverage for malformed payment confirmation helper inputs. Verified focused payment confirmation tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1098 | `[hanwoo-dashboard]` Cattle history event-date hardening. Tightened shared cattle history recording so malformed `eventDate` input is normalized through a guarded helper and falls back to the current time instead of passing an invalid `Date` into Prisma. This preserves audit/history records for successful cattle and sales mutations even when optional history date input is malformed, while keeping the existing metadata serialization fallback intact. Added focused source regression coverage for safe cattle history event-date normalization. Verified focused cattle-history/helper tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1097 | `[hanwoo-dashboard]` Cattle history metadata serialization hardening. Tightened shared cattle history recording so optional metadata is serialized through a guarded helper; unserializable metadata now logs a targeted error and falls back to `null` metadata instead of preventing the cattle history row from being written. This preserves audit/history records for successful cattle and sales mutations even when optional metadata is malformed. Added focused source regression coverage for safe cattle history metadata serialization. Verified focused cattle-history/helper tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1096 | `[hanwoo-dashboard]` Home cache invalidation malformed-options hardening. Tightened shared mutation cache invalidation so `invalidateHomeCaches()` normalizes null or otherwise malformed options input before spreading options into dashboard read-model invalidation or reading `cattleListPages` / `salesListPages` for Next cache tag revalidation. This prevents caller bugs from throwing generic TypeErrors after DB mutations but before cache invalidation can complete. Added focused source regression coverage for safe helper option access while preserving existing use-cache tag wiring checks. Verified focused helper/use-cache tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1095 | `[hanwoo-dashboard]` Auth guard malformed-options hardening. Tightened `requireAuthenticatedSession()` so null or otherwise malformed options input is normalized before reading `redirectToLogin`. This prevents caller bugs from throwing generic TypeErrors before the shared authentication guard can run auth, degrade infrastructure failures to `AuthenticationError`, or redirect unauthenticated page callers. Added focused source regression coverage for safe auth-guard option destructuring. Verified focused auth-guard tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1094 | `[hanwoo-dashboard]` Dashboard list query malformed-input hardening. Tightened dashboard cattle/sales list query parsing so malformed search params fall back to default query values instead of throwing TypeErrors before validation, and list page loaders normalize malformed input before destructuring. This preserves normal API route validation while making internal reuse safer. Added focused source regression coverage for safe search-param access and safe list-page input destructuring. Verified focused list-query tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1093 | `[hanwoo-dashboard]` Dashboard read-model malformed-options hardening. Tightened dashboard read-model helpers so null or otherwise malformed cache options and cache invalidation input are normalized before reading `bypassCache`, farm ids, cache flags, or snapshot delete flags. This prevents caller bugs from throwing generic TypeErrors before cache fallback, Redis no-op behavior, or targeted invalidation can run. Added focused read-model source regression coverage for safe option/input access. Verified focused read-model tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1092 | `[hanwoo-dashboard]` Dashboard cache malformed-input hardening. Tightened dashboard cache helpers so malformed cattle/sales filter objects fall back to stable default cache keys, and malformed cache key/prefix delete inputs fall back to empty lists instead of throwing before Redis configuration checks or cache invalidation can complete. Added focused dashboard cache regression coverage for null filters and null delete lists. Verified focused cache tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1091 | `[hanwoo-dashboard]` BullMQ queue malformed-options hardening. Tightened server queue helpers so null or otherwise malformed options input is normalized before reading queue prefixes, default job options, event options, or enqueue job options. This prevents caller bugs from producing generic TypeErrors before the existing Redis/BullMQ configuration checks can return actionable setup errors. Added focused queue regression coverage for malformed options with Redis intentionally unset. Verified focused queue tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1090 | `[hanwoo-dashboard]` Offline queue persisted timestamp hardening. Tightened offline queue item normalization so non-finite persisted queue timestamps fall back to the current time and trigger storage rewrite instead of surviving as `Infinity` through JSON parsing. This prevents corrupted offline queue rows from carrying impossible timestamps into sync retry/recovery paths while preserving valid queue ids, actions, args, and retry metadata. Strengthened offline queue storage regression coverage with a real browser-storage mock for `1e999` persisted timestamps. Verified focused offline queue tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1089 | `[hanwoo-dashboard]` Offline sync malformed-state hardening. Tightened offline sync state helpers so malformed queue metadata items and malformed failure-state options fall back to safe defaults instead of throwing during retry/dead-letter preparation. This prevents corrupted persisted offline queue rows or caller bugs from breaking sync recovery paths before retry metadata can be normalized. Strengthened offline sync regression coverage for null queue items and null failure options. Verified focused offline sync tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1088 | `[hanwoo-dashboard]` Shared fetch timeout malformed-options hardening. Tightened `fetchWithTimeout()` so null or otherwise malformed options input falls back to safe defaults before reading timeout settings or error copy. This prevents shared dashboard, payment, weather, KAPE/MTRACE, and API proxy fetch paths from crashing before the guarded timeout/abort wrapper is installed. Strengthened fetch timeout regression coverage with a real malformed-options call. Verified focused fetch timeout tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1087 | `[hanwoo-dashboard]` Market and weather state malformed-options hardening. Tightened market-price and weather state helpers so null or otherwise malformed options input falls back to safe defaults instead of throwing while building live/cache/unavailable/stale states. This prevents malformed caller fallback options from crashing KAPE market-price or Open-Meteo weather state preparation while preserving existing location, message, freshness, and date behavior. Strengthened market-price and weather state regression coverage for malformed options. Verified focused state tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1086 | `[hanwoo-dashboard]` Notification timing malformed-options hardening. Tightened `getNotificationTargetDate()` so null or otherwise malformed options input falls back to safe defaults instead of throwing during parameter destructuring. Estrus and calving notification date calculations remain intact, and regression coverage now keeps malformed options from crashing notification timing preparation. Verified notification timing tests through the full Hanwoo Node test suite, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1085 | `[hanwoo-dashboard]` Cattle weight-history non-array extraction hardening. Tightened `extractWeightHistoryPoints()` so non-array history payloads return an empty list before filtering, preventing corrupted caller/cache history objects from crashing cattle detail weight-chart preparation. Valid array history extraction, positive numeric weight normalization, and malformed row filtering remain intact. Strengthened cattle-history regression coverage for non-array weight-history payloads. Verified focused cattle-history tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1084 | `[hanwoo-dashboard]` Cattle history malformed-row hardening. Tightened `normalizeCattleHistoryRows()` so non-array history payloads return an empty list and malformed/non-object rows are ignored before metadata parsing and row spreading. This prevents corrupted caller/cache history collections from leaking malformed entries into cattle detail history and weight-chart preparation while preserving valid rows and corrupt-metadata diagnostics. Strengthened cattle-history regression coverage for non-array and malformed row collections. Verified focused cattle-history tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1083 | `[hanwoo-dashboard]` Cattle weight-history point hardening. Tightened `extractWeightHistoryPoints()` so empty, null, undefined, zero, or negative weight metadata cannot become visible weight chart points through `Number()` coercion or finite-only checks. Valid positive numeric strings and numbers still render normally. Strengthened cattle-history regression coverage for empty and non-positive weight metadata. Verified focused cattle-history tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1082 | `[hanwoo-dashboard]` Inventory missing-quantity display hardening. Tightened `InventoryTab` normalization so null, undefined, or empty inventory quantities stay unavailable instead of being coerced to 0 by `Number()`. Missing quantities now render as `수량 미등록`, open the inline editor with an empty field, and do not trigger false low-stock warnings, while valid finite numeric values still render and compare normally. Strengthened inventory source regression coverage for missing-quantity normalization and display behavior. Verified focused empty-state/inventory tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1081 | `[hanwoo-dashboard]` Weather current null/empty value hardening. Tightened weather payload normalization so null, undefined, or empty required Open-Meteo current fields are treated as unavailable instead of being coerced to 0 by `Number()`, while still accepting finite numeric strings. This prevents incomplete weather payloads from rendering misleading 0-degree/0-percent livestock weather states. Strengthened weather-state regression coverage for null and empty required current values. Verified focused weather-state tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1080 | `[hanwoo-dashboard]` AI insight weather numeric-string hardening. Tightened `summarizeFarmForInsight()` so weather `thi`, `temp`, and `humidity` accept finite numeric strings while still treating null/undefined/empty values as unavailable. This prevents cached/API weather payloads with stringified numbers from hiding heat-stress signals in AI insight prompts and heuristic cards. Strengthened AI insight regression coverage for string weather numbers and preserved partial-weather fallback copy. Verified focused AI insight tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1079 | `[hanwoo-dashboard]` Farm metrics malformed window hardening. Tightened farm-specific feed-cost and weight-gain estimators so malformed `windowMonths` values fall back to safe positive defaults before date-window and denominator calculations. This prevents invalid windows from producing non-finite feed-cost evidence or suppressing valid shipment weight-gain samples. Strengthened farm-metrics regression coverage for malformed feed and weight window inputs. Verified focused farm-metrics tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1078 | `[hanwoo-dashboard]` Today focus malformed collection hardening. Tightened `buildTodayFocusItems()` so notifications, schedule events, and inventory payloads are normalized to non-null object rows before filtering, sorting, low-stock checks, or feed-depletion projections. This prevents malformed cached/caller collections or non-array payloads from crashing the home priority panel while preserving valid alert, schedule, stock, and sales prompts. Strengthened today-focus regression coverage for malformed rows and non-array collections. Verified focused today-focus tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1077 | `[hanwoo-dashboard]` Setup progress malformed collection row hardening. Tightened setup-progress item counting so only non-null object rows count as completed operating data, preventing malformed cached/caller rows in buildings, cattle, inventory, or schedule collections from falsely completing onboarding steps. Strengthened setup-progress regression coverage to keep malformed collection rows ignored while preserving valid setup completion. Verified focused setup-progress tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1076 | `[hanwoo-dashboard]` Profitability zero purchase-price handling hardening. Tightened the profitability estimate service so a valid `purchasePrice` of `0` is preserved as the base cost instead of being replaced by the default calf cost, preventing self-bred or zero-cost cattle from being over-costed in shipment profitability estimates. Strengthened profitability regression coverage to keep `0` distinct from malformed/missing purchase-price input. Verified focused profitability tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1075 | `[hanwoo-dashboard]` Weather zero-coordinate handling hardening. Tightened the dashboard weather effect and shared `useWeather()` hook so farm weather coordinates are treated as present when they are non-null/non-undefined, allowing valid zero latitude or longitude values to flow into the existing coordinate validator instead of being misclassified as missing coordinates. Strengthened home/weather regression coverage to prevent truthiness-based coordinate checks from returning. Verified focused home/weather tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1074 | `[hanwoo-dashboard]` Weather geolocation stale-callback cleanup hardening. Tightened the dashboard weather effect and shared `useWeather()` hook so geolocation success/error callbacks return after effect cleanup before starting fallback or coordinate weather fetches, avoiding post-unmount network work while preserving existing stale-state update guards. Strengthened home/weather regression coverage to keep cancellation checks in fallback, coordinate, and position callback paths. Verified focused home/weather tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1073 | `[hanwoo-dashboard]` Field Mode delayed celebration firework cleanup hardening. Tightened the Field Mode celebration effect so delayed secondary firework timer callbacks check the existing cleanup cancellation flag before mutating the local particle queue, preventing stale timer callbacks from doing post-cleanup animation work if timer cleanup is unavailable in a restricted browser environment. Strengthened Field Mode regression coverage to keep the delayed firework cancellation guards. Verified focused Field Mode tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1072 | `[hanwoo-dashboard]` QR print completion callback scheduling failure hardening. Tightened the QR label print flow so if both print-window load listener registration and fallback timer scheduling fail in a restricted browser environment, the print completion cleanup runs immediately instead of leaving the duplicate-print lock and busy state stuck. Strengthened QR widget regression coverage to keep the guarded scheduling return values and immediate cleanup fallback. Verified focused QR tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1071 | `[hanwoo-dashboard]` Widget settings unknown-id guard hardening. Tightened `useWidgetSettings()` so only ids registered in `WIDGET_REGISTRY` can be toggled, preventing accidental caller bugs or stale UI events from writing unknown widget keys into local widget visibility state/storage. Strengthened settings regression coverage to keep registry-id guarding before state/storage mutation. Verified focused settings tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1070 | `[hanwoo-dashboard]` Online-status listener partial-registration cleanup hardening. Tightened `useOnlineStatus()` so if browser event listener registration fails after one online/offline listener was already attached, the hook removes any partial registration before returning. Strengthened online-status regression coverage to keep partial cleanup and normal unmount cleanup guarded. Verified focused online-status tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1069 | `[hanwoo-dashboard]` Market price polling fallback hardening. Tightened the market price widget hourly refresh path so restricted browsers where `setInterval` throws fall back to a cleanup-aware `setTimeout` polling loop instead of permanently losing automatic KAPE price refreshes. Strengthened home/market regression coverage to keep the fallback loop and cleanup. Verified focused home/market tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1068 | `[hanwoo-dashboard]` Pagination hook timeout scheduling failure hardening. Tightened cattle, sales, and shared cursor pagination hooks so timeout scheduling failures immediately mark the request timed out and abort, instead of silently losing timeout protection in restricted browser environments. Added shared cursor pagination regression coverage and strengthened cattle/sales pagination tests to keep immediate abort fallback behavior. Verified focused pagination tests, full Hanwoo tests, eslint, Next production build, and root project QC runner. | Codex | 2026-05-27 |
| T-1067 | `[hanwoo-dashboard]` Shared fetch timeout scheduling failure hardening. Tightened the shared `fetchWithTimeout()` utility so timeout scheduling failures immediately abort with the existing `TimeoutError` instead of silently losing timeout protection across dashboard refreshes, payment confirmation, KAPE/MTRACE lookups, weather, and API proxy calls. Strengthened fetch timeout regression coverage to keep immediate timeout behavior. Verified focused fetch timeout tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1066 | `[hanwoo-dashboard]` Payment and AI insight timeout scheduling failure hardening. Tightened timeout scheduling failure paths so payment widget timeouts immediately reject with `TimeoutError`, AI insight client timeout scheduling failures immediately abort into the timeout fallback, and the AI insight API timeout wrapper rejects with `InsightTimeoutError` instead of silently losing timeout protection. Strengthened payment and AI insight regression coverage to keep immediate timeout fallback behavior. Verified focused payment/AI insight tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1065 | `[hanwoo-dashboard]` Payment and AI insight deferred reset fallback hardening. Added guarded deferred-task helpers to the payment checkout widget and AI insight widget reset paths, routing widget-ready/error reset and heuristic insight refresh reset through Promise-backed fallback scheduling so restricted browser environments where `queueMicrotask` throws still preserve cancellation and abort guards. Strengthened payment and AI insight regression coverage to keep the helpers and prevent raw reset microtasks from returning. Verified focused payment/AI insight tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1064 | `[hanwoo-dashboard]` Cattle form/detail deferred reset fallback hardening. Added guarded deferred-task helpers to the cattle edit form and cattle detail modal reset paths, routing save-state and breeding-action resets through Promise-backed fallback scheduling so restricted browser environments where `queueMicrotask` throws still preserve cancellation guards. Strengthened cattle form/detail regression coverage to keep the helpers and prevent raw reset microtasks from returning. Verified focused cattle form/detail tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1063 | `[hanwoo-dashboard]` Admin diagnostics deferred loading fallback hardening. Centralized diagnostics loading-state deferral behind a guarded helper and routed both system diagnostics and raw-data loading resets through it, so restricted browser environments where `queueMicrotask` throws still fall back safely without bypassing cancellation and latest-request guards. Strengthened diagnostics regression coverage to keep the guarded helper and prevent raw initialization microtasks from returning. Verified focused diagnostics tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1062 | `[hanwoo-dashboard]` Ear-tag scanner deferred task fallback hardening. Centralized scanner microtask scheduling behind a guarded deferred-task helper and routed both initial target selection and deferred no-match fallback through it, so restricted browser environments where `queueMicrotask` throws still fall back safely without bypassing cleanup guards. Strengthened scanner regression coverage to keep the guarded helper and prevent raw initialization microtasks from returning. Verified focused scanner tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1061 | `[hanwoo-dashboard]` Field Mode celebration timer cleanup hardening. Added cleanup-aware guards to Field Mode celebration close timers and animation-frame failure timers so delayed celebration shutdown callbacks no longer update state after the field-mode celebration effect has cleaned up. Strengthened FieldMode regression coverage to keep guarded no-canvas, frame-failure, and normal close timer paths. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1060 | `[hanwoo-dashboard]` Ear-tag scanner deferred no-match cleanup hardening. Added cleanup-aware guards to the scanner modal's deferred no-match fallback so missing canvas contexts or animation-frame scheduling failures no longer update scan state after the modal closes. Strengthened scanner regression coverage to keep guarded microtask fallback and frame-failure cleanup paths. Verified focused scanner tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1059 | `[hanwoo-dashboard]` Feedback provider unmount cleanup hardening. Added mounted-state guards to feedback toast dismissal and notification scheduling, guarded delayed toast callbacks after provider cleanup, and resolved any pending confirmation dialog promise as `false` during provider unmount so awaiting dashboard actions cannot hang on stale confirmation state. Strengthened feedback provider regression coverage to keep the stale callback and pending confirmation cleanup guards. Verified focused feedback tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1058 | `[hanwoo-dashboard]` Subscription success timer cleanup hardening. Added cleanup-aware cancellation guards to subscription success timer callbacks so invalid-amount status updates, delayed dashboard redirects, redirect-failure status updates, and pending-confirmation retry callbacks do not run after the payment success component has cleaned up. Strengthened payment UX regression coverage to keep the stale timer callback guards. Verified focused payment tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1057 | `[hanwoo-dashboard]` Dashboard drag move confirmation unmount cleanup hardening. Added a mounted-state guard after the drag-and-drop cattle-move confirmation dialog resolves so stale confirmation completions after dashboard unmount return before building the update payload or entering the cattle update/offline mutation path. Strengthened home dashboard regression coverage to keep the post-confirmation guard. Verified focused home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1056 | `[hanwoo-dashboard]` Dashboard summary and notification refresh unmount cleanup hardening. Added mounted-state guards to dashboard summary and notification refresh completions so stale API/getNotifications completions avoid summary, summary metadata, and notification state updates after dashboard unmount while preserving latest-request ordering for mounted refreshes. Strengthened home dashboard regression coverage to keep guarded refresh completion paths. Verified focused home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1055 | `[hanwoo-dashboard]` Dashboard cattle create/update unmount cleanup hardening. Added mounted-state guards to the dashboard cattle create and update online server-action completion paths so stale completions avoid cattle state, modal/edit state, selected-cow state, dashboard refresh, and toast updates after dashboard unmount while still returning the server-action result. Strengthened home dashboard regression coverage to keep guarded cattle create/update success/error/catch paths. Verified focused home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1054 | `[hanwoo-dashboard]` Dashboard farm settings mutation unmount cleanup hardening. Added mounted-state guards to the dashboard farm settings save completion path so stale server-action completions avoid farm settings state and toast updates after dashboard unmount while still returning the server-action result. Strengthened home dashboard regression coverage to keep guarded farm settings success/error paths. Verified focused home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1053 | `[hanwoo-dashboard]` Dashboard calving mutation unmount cleanup hardening. Added mounted-state guards to the dashboard calving server-action completion path so stale completions avoid calving cattle state, dashboard refresh, and toast updates after dashboard unmount while still returning the server-action result. Strengthened home dashboard regression coverage to keep guarded calving success/error paths. Verified focused home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1052 | `[hanwoo-dashboard]` Dashboard sales and feed mutation unmount cleanup hardening. Added mounted-state guards to dashboard sales-record create and feed-record create handlers so stale server-action completions avoid sales/feed state, dashboard refresh, and toast updates after dashboard unmount while still returning the server-action result. Strengthened home dashboard regression coverage to keep guarded sales/feed success/error paths. Verified focused home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1051 | `[hanwoo-dashboard]` Dashboard schedule mutation unmount cleanup hardening. Added mounted-state guards to dashboard schedule create and completion-toggle handlers so stale server-action completions avoid schedule state and toast updates after dashboard unmount while still returning the server-action result. Strengthened home dashboard regression coverage to keep guarded schedule create/toggle success/error paths. Verified focused home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1050 | `[hanwoo-dashboard]` Dashboard inventory mutation unmount cleanup hardening. Added mounted-state guards to dashboard inventory create and quantity-update handlers so stale server-action completions avoid inventory state and toast updates after dashboard unmount while still returning the server-action result. Strengthened home dashboard regression coverage to keep guarded inventory create/update success/error paths. Verified focused home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1049 | `[hanwoo-dashboard]` Dashboard building mutation unmount cleanup hardening. Added mounted-state guards to dashboard building create/delete handlers so stale server-action completions avoid building state, selected-building, selected-pen, and toast updates after unmount while still returning the server-action result. Strengthened home dashboard regression coverage to keep guarded building create/delete success/error paths. Verified focused home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1048 | `[hanwoo-dashboard]` AI insight stale callback cleanup hardening. Added a cleanup cancellation guard to the AI insight widget so stale summary-change or unmount callbacks avoid heuristic reset, parsed-response, fallback-reason, and loading-state updates after cleanup while preserving timeout abort behavior. Strengthened AI insight regression coverage to keep the cancellation guard across microtask, success, catch, finalizer, and cleanup paths. Verified focused AI insight tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1047 | `[hanwoo-dashboard]` Login submit unmount cleanup hardening. Added a mounted-state guard to the login page submit flow so async sign-in and navigation completions release the duplicate-submit ref while avoiding login error/submitting state updates after unmount. Strengthened login/error-page regression coverage to keep the mounted cleanup guard and guarded submit finalizer. Verified focused login tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1046 | `[hanwoo-dashboard]` Dashboard cattle archive unmount cleanup hardening. Added mounted-state guards to the dashboard cattle archive flow so async delete completions avoid dashboard state and toast updates after unmount while still returning the server-action result. Strengthened home and cattle-detail regression coverage to keep guarded archive success/error/finalizer paths. Verified focused home/cattle-detail tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1045 | `[hanwoo-dashboard]` Payment widget async cleanup unmount hardening. Added a mounted-state guard to the payment checkout widget so async payment request finalizers release the duplicate-submit ref while avoiding React state updates after unmount. Strengthened payment UX regression coverage to keep the mounted cleanup guard and guarded payment finalizer. Verified focused payment tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1044 | `[hanwoo-dashboard]` Cattle detail breeding save unmount cleanup hardening. Added a mounted-state guard to the cattle detail modal so async breeding-record finalizers release the duplicate-submit ref while avoiding React state updates after unmount. Strengthened cattle form/detail regression coverage to keep the mounted cleanup guard and guarded saved-result/finalizer paths. Verified focused cattle form/detail tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1043 | `[hanwoo-dashboard]` Cattle form save unmount cleanup hardening. Extended the existing Cattle form mounted-state guard so unmount cleanup also releases the save duplicate-submit ref and async cattle-save finalizers avoid React state updates after unmount. Strengthened cattle form/detail regression coverage to keep the save cleanup guard and guarded save finalizer. Verified focused cattle form/detail tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1042 | `[hanwoo-dashboard]` Settings tab async cleanup unmount hardening. Added a mounted-state guard to the Settings tab so farm settings saves, building creates, and building deletes release their duplicate-action refs while avoiding React state/reset updates after unmount. Strengthened Settings regression coverage to keep the mounted cleanup guard and guarded saved/confirmed/finalizer paths. Verified focused Settings tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1041 | `[hanwoo-dashboard]` Calving tab async cleanup unmount hardening. Added a mounted-state guard to the Calving tab so async calving-record create finalizers release the duplicate-submit ref while avoiding React state/reset updates after unmount. Strengthened Calving regression coverage to keep the mounted cleanup guard and guarded recorded-result/finalizer paths. Verified focused Calving tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1040 | `[hanwoo-dashboard]` Feed tab async cleanup unmount hardening. Added a mounted-state guard to the Feed tab so async feed-record create finalizers release the duplicate-submit ref while avoiding React state/reset updates after unmount. Strengthened Feed/empty-state regression coverage to keep the mounted cleanup guard and guarded recorded-result/finalizer paths. Verified focused Feed tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1039 | `[hanwoo-dashboard]` Sales tab async cleanup unmount hardening. Added a mounted-state guard to the Sales tab so async sales-record create finalizers release the duplicate-submit ref while avoiding React state/reset updates after unmount. Strengthened Sales/home regression coverage to keep the mounted cleanup guard and guarded saved-result/finalizer paths. Verified focused Sales/home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1038 | `[hanwoo-dashboard]` Inventory tab async cleanup unmount hardening. Added a mounted-state guard to the Inventory tab so async inventory-create and inline quantity-update finalizers release duplicate-action refs while avoiding React state/reset updates after unmount. Strengthened Inventory/home regression coverage to keep the mounted cleanup guard, guarded saved-result paths, and guarded quantity reset finalizer. Verified focused Inventory/home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1037 | `[hanwoo-dashboard]` Schedule tab async cleanup unmount hardening. Added a mounted-state guard to the Schedule tab so async schedule-create and completion-toggle finalizers release duplicate-action refs while avoiding React state/reset updates after unmount. Strengthened Schedule and empty-state regression coverage to keep the mounted cleanup guard and tolerate the guarded saved-result path. Verified focused Schedule/empty-state tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1036 | `[hanwoo-dashboard]` QR print callback unmount cleanup hardening. Added a mounted-state guard to the QR print widget so popup load/fallback callbacks and print failure/completion paths after component unmount no longer update printing or status state. Unmounted callbacks now close the print window and release the duplicate-print lock without touching React state. Strengthened QR regression coverage to keep the mounted guard, guarded status updates, and unmounted finish-print short-circuit. Verified focused QR tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1035 | `[hanwoo-dashboard]` Market price refresh start unmount hardening. Added a mounted-state start guard to the market price widget refresh path so delayed refresh calls after component unmount return without starting a new KAPE request or setting `loading` state. Existing mounted completion guards still protect stale success/finally paths. Strengthened home/market regression coverage to keep the start guard plus completion/loading guards. Verified focused home/market tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1034 | `[hanwoo-dashboard]` Excel export async cleanup unmount hardening. Added a mounted-state guard to the Excel/CSV export button so stale async export finalizers after component unmount no longer update `isPreparing` state while still releasing the duplicate-export lock and cleaning temporary download resources. Strengthened Excel export regression coverage to keep the mounted guard and prevent direct finalizer state resets from returning. Verified focused Excel export tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1033 | `[hanwoo-dashboard]` AI chat stream unmount cleanup hardening. Added a mounted-state guard and unmount abort cleanup to the AI chat widget so stale stream chunk/done/error callbacks and aborted-stream finalizers after component unmount no longer update messages or streaming state. Strengthened AI chat regression coverage to keep the mounted guard, unmount abort, and guarded stream callbacks. Verified focused AI chat tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1032 | `[hanwoo-dashboard]` Notification modal SMS async cleanup hardening. Added a mounted-state guard to the notification modal SMS test action so stale `onTestSMS` promise completions after modal unmount no longer update `isTestingSMS` state while preserving the existing busy lock during mounted sends. Strengthened notification modal regression coverage to keep the mounted guard and prevent direct `finally { setIsTestingSMS(false); }` from returning. Verified focused notification modal tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1031 | `[hanwoo-dashboard]` Dashboard full-list preload unmount cleanup hardening. Added a dashboard mounted-state guard around full cattle registry and full sales ledger preload completions, so stale preload resolve/reject/finally paths after dashboard unmount no longer update registry, error, or loading state while still clearing in-flight refs. Strengthened home dashboard regression coverage to keep the mounted guard on cattle and sales preload success/error/loading paths. Verified focused home dashboard tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1030 | `[hanwoo-dashboard]` Field Mode full-list async cleanup hardening. Added a cleanup guard to the Field Mode background full-cattle-list loading effect so stale `ensureAllCattleLoaded()` completions after unmount or dependency replacement no longer update loading state. Strengthened FieldMode source regression coverage to keep the cleanup guard and prevent direct `.finally(() => setLoadingAllCattle(false))` from returning. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1029 | `[hanwoo-dashboard]` Dashboard ErrorBoundary reset-handler failure hardening. Wrapped custom `onReset` recovery callbacks in the dashboard runtime `ErrorBoundary` with synchronous and promise-rejection guards, routing failures through the existing fallback-state path instead of allowing the recovery button event handler to throw outside the boundary. Strengthened error-page source regression coverage to keep guarded custom reset handling and shared reset failure logging. Verified focused error-page tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1028 | `[hanwoo-dashboard]` React hooks lint suppression removal. Replaced the remaining Hanwoo `eslint-disable` hook suppressions with structural code paths: diagnostics, cattle form/detail, and payment widget effect resets now defer state updates through guarded microtasks, and CattleForm uses an event submit wrapper instead of render-time `handleSubmit(submitForm)`. Strengthened diagnostics, payment, and cattle form/detail source regression coverage to keep the no-suppression pattern. Verified focused source tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1027 | `[hanwoo-dashboard]` Weather coordinate range and React lint gate hardening. Tightened DashboardClient and useWeather weather-coordinate helpers so normalized latitude/longitude must be finite and within WGS84-style latitude/longitude bounds before building Open-Meteo requests; out-of-range saved farm or geolocation coordinates now fall back to the default Namwon weather coordinates. Also moved tab `react-hook-form` submit handler creation out of render-time JSX calls and deferred ear-tag scanner modal open-state resets through a microtask so the current React hooks lint gate passes without refs/set-state errors. Strengthened home/weather regression coverage to keep bounded coordinate validation. Verified focused home/weather tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1026 | `[hanwoo-dashboard]` Weather saved farm-coordinate hardening. Routed saved farm weather coordinates through a shared finite-coordinate helper in DashboardClient and useWeather, so malformed, stringy, or non-finite farm settings no longer build Open-Meteo requests with invalid latitude/longitude values. Invalid saved coordinates now fall back to the default Namwon weather coordinates, matching the geolocation success fallback path. Strengthened home/weather regression coverage to keep coordinate normalization in both weather entry points and prevent direct farm-coordinate fetches from returning. Verified focused home/weather tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1025 | `[hanwoo-dashboard]` Dashboard offline-sync refresh failure messaging hardening. Wrapped DashboardClient's post-offline-sync `router.refresh()` in a guarded block so successful offline sync is no longer reported as generic sync failure when route refresh fails. Refresh failures now log diagnostics and show a separate Korean warning telling the operator to manually refresh the screen to see synced results. Strengthened home dashboard regression coverage to keep the guarded refresh path and prevent direct `router.refresh()` from returning. Verified focused home dashboard tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1024 | `[hanwoo-dashboard]` Weather geolocation success-coordinate hardening. Guarded DashboardClient and useWeather geolocation success callbacks so malformed or non-finite `position.coords` values fall back to the default Namwon weather coordinates instead of building Open-Meteo requests with invalid coordinates. Strengthened home/weather regression coverage to keep coordinate normalization in both weather entry points and prevent direct `position.coords.latitude/longitude` fetches from returning. Verified focused home/weather tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1023 | `[hanwoo-dashboard]` QR print load-listener failure hardening. Wrapped QR print-window load listener registration in a guarded helper so restricted or unusual popup event APIs no longer abort an otherwise prepared QR print window. Listener registration failures now log diagnostics while the existing fallback timer path can still complete printing and release the duplicate-print lock/busy state. Strengthened QR regression coverage to keep guarded load-listener registration and prevent direct listener registration from blocking fallback scheduling. Verified focused QR tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1022 | `[hanwoo-dashboard]` QR print window open failure hardening. Wrapped QR print-window creation in a guarded helper so restricted or unusual browser popup APIs no longer leave QR print stuck in a preparing state. Window-open failures now log diagnostics, reuse the existing popup-blocked Korean guidance, and release the duplicate-print lock/busy state. Strengthened QR regression coverage to keep guarded popup opening and prevent direct window.open usage from returning. Verified focused QR tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1021 | `[hanwoo-dashboard]` Shared focus failure hardening. Added a shared safe focus helper and routed cattle form, cattle detail, AI chat panel/launcher, and notification modal focus restoration through it so restricted browser focus APIs no longer break modal/chat open-close flows. Strengthened regression coverage to keep the shared helper, guarded focus calls, and prevention of direct optional focus calls returning. Verified focused modal/chat tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1020 | `[hanwoo-dashboard]` Excel export cleanup failure hardening. Wrapped temporary download-link removal and Blob URL revocation in guarded helpers so DOM cleanup or URL revoke failures cannot prevent the export lock and busy state from being released. Strengthened Excel export regression coverage to keep best-effort cleanup helpers, finalizer ordering, and prevention of direct cleanup calls returning. Verified focused Excel tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1019 | `[hanwoo-dashboard]` Theme DOM application failure hardening. Wrapped theme document-root lookup and `data-theme`/dark-class DOM mutation in guarded paths so restricted or unusual browser DOM access no longer breaks settings/theme rendering after safe storage and system-theme fallback. Strengthened settings regression coverage to keep guarded document-root access, guarded theme DOM mutation, and prevention of direct `document.documentElement` mutation. Verified focused settings tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1018 | `[hanwoo-dashboard]` Ear-tag scanner animation frame failure hardening. Wrapped the scanner modal's `requestAnimationFrame` scheduling and `cancelAnimationFrame` cleanup in guarded helpers, reset stored frame handles after cleanup, and route missing canvas context or frame scheduling failures to the existing no-match state without synchronous effect state updates. Strengthened scanner regression coverage to keep guarded frame scheduling, cleanup, handle reset, and no-match fallback. Verified focused scanner tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1017 | `[hanwoo-dashboard]` Field Mode animation frame failure hardening. Wrapped Field Mode celebration `requestAnimationFrame` scheduling, `cancelAnimationFrame` cleanup, resize listener registration/removal, and missing canvas 2D context handling in guarded paths so restricted animation frame or resize APIs no longer break onsite celebration UI. Strengthened FieldMode regression coverage to keep guarded animation frame scheduling, cleanup, resize listener handling, and context fallback. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1016 | `[hanwoo-dashboard]` Field Mode audio/vibration browser API hardening. Wrapped audio context class access, audio context creation, suspended-context resume, and tactile vibration calls in guarded best-effort paths so restricted browser audio or vibration APIs no longer break Field Mode and scanner feedback flows. Strengthened FieldMode/audio regression coverage to keep guarded audio and vibration API access. Verified focused FieldMode/audio tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1015 | `[hanwoo-dashboard]` AI insight route timeout timer failure hardening. Wrapped the AI insight API route's Gemini timeout scheduling and cleanup in guarded blocks so restricted runtime timer APIs no longer turn timeout setup/cleanup failures into route errors. If timeout scheduling fails, the original Gemini request continues instead of crashing the route; cleanup clears timeout handles best-effort. Strengthened AI insight regression coverage to keep guarded route timeout scheduling and cleanup paths. Verified focused AI insight tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1014 | `[hanwoo-dashboard]` Subscription success timer failure hardening. Wrapped the subscription success page's invalid-amount status timer, payment-confirmation retry timer, success redirect timer, and cleanup in guarded helpers so restricted browser timer APIs no longer stall payment status updates or retry/redirect flows. If timer scheduling fails, the intended callback runs immediately instead of leaving the page in a stale state; cleanup clears timeout handles best-effort. Strengthened payment UX regression coverage to keep guarded subscription success timers. Verified focused payment UX tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1013 | `[hanwoo-dashboard]` QR print fallback timer failure hardening. Wrapped the QR label print-window fallback timer scheduling in a guarded helper so restricted popup timer APIs no longer convert print fallback scheduling failures into full print-window preparation failures. The popup load event path remains active, and scheduling failures are logged without closing the prepared print window. Strengthened QR widget regression coverage to keep guarded fallback timer scheduling. Verified focused QR tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1012 | `[hanwoo-dashboard]` Field Mode celebration timer failure hardening. Wrapped the Field Mode checklist-completion celebration timer scheduling and cleanup in guarded helpers so restricted browser timer APIs no longer break onsite celebration effects or cleanup. If timer scheduling fails, the celebration canvas can still animate through the existing particle lifecycle; cleanup clears timeout handles best-effort. Strengthened FieldMode regression coverage to keep guarded celebration timer scheduling and cleanup paths. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1011 | `[hanwoo-dashboard]` AI insight client timeout timer failure hardening. Wrapped the AI insight widget request timeout scheduling and cleanup in guarded blocks so restricted browser timer APIs no longer break AI insight fetch/fallback flows. If timeout scheduling fails, the request continues without crashing; cleanup clears timeout handles best-effort. Strengthened AI insight regression coverage to keep guarded timeout scheduling and cleanup paths. Verified focused AI insight tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1010 | `[hanwoo-dashboard]` Shared fetch timeout timer failure hardening. Wrapped `fetchWithTimeout` timeout scheduling and cleanup in guarded blocks so restricted timer APIs no longer break shared dashboard, payment, weather, KAPE, and MTRACE fetch flows before the network request starts. If timeout scheduling fails, the request continues without crashing; cleanup clears timeout handles best-effort. Added focused regression coverage for guarded timeout scheduling and cleanup. Verified focused fetch/payment/MTRACE tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1009 | `[hanwoo-dashboard]` Payment widget timeout timer failure hardening. Wrapped the payment widget load-timeout scheduling and cleanup in guarded blocks so restricted browser timer APIs no longer break Toss payment widget initialization. If timeout scheduling fails, the original widget-load promise continues without crashing; timeout cleanup clears handles best-effort. Strengthened payment UX regression coverage to keep guarded timeout scheduling and cleanup paths. Verified focused payment tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1008 | `[hanwoo-dashboard]` Pagination request timeout timer failure hardening. Wrapped cattle, sales, and shared cursor pagination request timeout scheduling and cleanup in guarded blocks so restricted browser timer APIs no longer break load-more flows. If timeout scheduling fails, pagination requests continue without crashing; cleanup now clears timeout handles best-effort. Strengthened cattle and sales pagination regression coverage to keep guarded timer scheduling and cleanup paths. Verified focused pagination tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1007 | `[hanwoo-dashboard]` Feedback toast timer failure hardening. Wrapped feedback toast dismissal timer scheduling and cleanup in guarded blocks so restricted browser timer APIs no longer break app notifications. Toasts still render when auto-dismiss scheduling fails, manual dismiss cleanup tolerates timer cleanup failures, and provider unmount cleanup clears any tracked timers best-effort. Strengthened feedback regression coverage to keep guarded timer scheduling, cleanup, and Korean dismiss semantics. Verified focused feedback tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1006 | `[hanwoo-dashboard]` Market price refresh timer failure hardening. Wrapped the market price widget's initial refresh timer, hourly polling timer, and cleanup calls in guarded blocks so restricted browser timer APIs no longer break the market price card effect. If the immediate refresh timer cannot be scheduled, the widget now falls back through a microtask fetch; polling setup and cleanup failures are logged or ignored without crashing the card. Strengthened home/market regression coverage to keep the guarded timer scheduling and cleanup paths. Verified focused home/market tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1005 | `[hanwoo-dashboard]` Offline sync refresh failure messaging hardening. Wrapped the offline queue post-sync `router.refresh()` path in a guarded block so a dashboard refresh failure after successful offline sync no longer falls into the generic sync-failure catch path. The hook now logs refresh failures separately and shows Korean guidance to manually refresh the screen to see synced results. Strengthened offline sync regression coverage to keep the guarded refresh path and Korean fallback message. Verified focused sync-manager tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1004 | `[hanwoo-dashboard]` Login success dashboard navigation failure hardening. Wrapped the login success `router.push("/")` and `router.refresh()` path in a guarded navigation block so a browser/router navigation failure no longer gets mislabeled as a generic sign-in/network failure. The login page now logs the dashboard navigation failure and shows Korean fallback copy that explains login completed but dashboard navigation failed. Strengthened login regression coverage to keep the guarded navigation path and Korean fallback message. Verified focused error-page/login tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1003 | `[hanwoo-dashboard]` Admin diagnostics dashboard-return navigation hardening. Replaced the admin diagnostics page's inline dashboard return `router.push("/")` with a guarded `handleDashboardReturn()` path that logs navigation failures and surfaces Korean feedback through the existing app feedback system. Strengthened diagnostics regression coverage to keep the guarded handler, Korean fallback message, and button wiring. Verified focused diagnostics tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1002 | `[hanwoo-dashboard]` Payment failure retry fallback-status hardening. Added a final user-visible fallback for the subscription failure retry flow: if both browser back navigation and the direct `/subscription` fallback navigation fail, the page now logs the fallback failure and announces Korean status copy instead of failing silently. Strengthened payment UX regression coverage to keep the retry status state, polite announcement, and fallback failure logging. Verified focused payment UX tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1001 | `[hanwoo-dashboard]` Payment failure retry navigation hardening. Replaced the subscription failure page's inline retry `router.back()` call with a guarded `handleRetry()` path that logs back-navigation failures and falls back to `/subscription`, so users can still return to checkout when browser history navigation fails. Strengthened payment UX regression coverage to keep the guarded retry path and direct checkout fallback. Verified focused payment UX tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-1000 | `[hanwoo-dashboard]` Payment success auto-redirect failure hardening. Wrapped the subscription success page's delayed dashboard redirect in a guarded try/catch so navigation failures are logged and replaced with Korean fallback status copy instead of leaving the user on a stale success screen that still promises automatic navigation. Strengthened payment UX regression coverage to keep the guarded redirect path and fallback copy. Verified focused payment UX tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-999 | `[hanwoo-dashboard]` Payment redirect URL browser-access hardening. Routed subscription checkout success/fail redirect URL creation through a guarded helper so restricted or unusual browser `window.location` access no longer breaks checkout request assembly. The helper builds absolute redirect URLs from the current href when available and falls back safely to the route path. Strengthened payment UX regression coverage to keep the guarded redirect helper and prevent direct `window.location.origin` URL assembly from returning. Verified focused payment UX tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-998 | `[hanwoo-dashboard]` Runtime ErrorBoundary reload failure hardening. Wrapped the dashboard runtime ErrorBoundary recovery reload in a guarded try/catch so browser reload API failures are logged and the recoverable fallback remains visible instead of clearing error state into a potentially broken dashboard render. Strengthened error-page regression coverage to keep the guarded reload path. Verified focused error-page tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-997 | `[hanwoo-dashboard]` Offline sync fallback Korean-copy consistency. Changed offline sync unsupported-action and unsuccessful-result fallback messages from English internal strings to stable Korean product copy, so retry/dead-letter metadata remains readable and consistent with operator-facing offline sync copy. Strengthened sync manager regression coverage to keep the Korean constants and prevent the English fallback strings from returning. Verified focused sync manager tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-996 | `[hanwoo-dashboard]` Offline queue storage failure hardening. Wrapped offline queue localStorage persistence and clearing in best-effort guards so restricted browser storage does not break offline-aware dashboard flows, queue rewrites, or queue clearing. Added focused source regression coverage for safe queue persistence and clear operations. Verified focused offline-queue storage tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-995 | `[hanwoo-dashboard]` Online status browser API hardening. Added guarded online-status helpers so missing or restricted `navigator.onLine`, `window.addEventListener`, and `window.removeEventListener` access falls back safely without breaking the dashboard's offline-aware flows. Added focused source regression coverage for safe online-state reads and listener cleanup. Verified focused online-status tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-994 | `[hanwoo-dashboard]` QR print-window preparation cleanup hardening. Wrapped QR print-window document preparation in guarded cleanup so DOM/document setup or `doc.close()` failures close the popup, release the duplicate-print lock, clear the busy state, and announce a Korean retry message. Strengthened QR widget regression coverage to keep the preparation `try/catch` path and prevent unguarded print-window setup from returning. Verified focused QR tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-993 | `[hanwoo-dashboard]` QR print failure-state unlock hardening. Wrapped QR label print focus/print/close completion in guarded cleanup so browser print API failures still release the duplicate-print lock, clear the busy state, and announce a Korean retry message. Strengthened QR widget regression coverage to keep the try/catch/finally path and prevent the old success-only reset path from returning. Verified focused QR tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-992 | `[hanwoo-dashboard]` Excel export temporary download cleanup hardening. Changed Excel CSV export to keep the generated Blob URL and temporary anchor in local variables, then remove the temporary DOM node and revoke the object URL from `finally`, so DOM/click failures do not leave temporary download resources behind while preserving duplicate-download lock behavior and Korean error feedback. Strengthened Excel export regression coverage to keep cleanup in `finally` and prevent the old inline remove/revoke path from returning. Verified focused Excel export tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-991 | `[hanwoo-dashboard]` Weather geolocation fallback hardening. Hardened dashboard and `useWeather` browser geolocation lookup so missing farm coordinates still fall back to the default Namwon weather coordinates when `navigator` is unavailable, permission lookup fails, or `getCurrentPosition` throws synchronously. Strengthened home dashboard regression coverage to keep the guarded geolocation path and prevent unguarded `navigator` access or inline fallback callbacks from returning. Verified focused home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-990 | `[hanwoo-dashboard]` Theme preference storage hardening. Consolidated theme preference loading, system-theme fallback, and localStorage writes in `useTheme` so settings render safely when browser storage or `matchMedia` throws. Theme initialization now falls back to `light` on server and to a guarded system preference on the client, while preference writes tolerate storage quota/security failures. Strengthened settings regression coverage to keep the safe theme-storage path and prevent direct initialization or direct effect writes from returning. Verified focused settings tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-989 | `[hanwoo-dashboard]` Widget settings persisted visibility hardening. Consolidated dashboard widget visibility defaults, persisted-value normalization, and localStorage reads/writes in `useWidgetSettings`, preserving only boolean values for known widget ids and falling back to defaults for malformed/non-object saved settings. Wrapped widget visibility writes so storage quota/security failures do not break settings toggles. Strengthened settings regression coverage to keep the safe persisted-widget-settings path and prevent raw JSON spread or direct toggle writes from returning. Verified focused settings tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-988 | `[hanwoo-dashboard]` Field Mode checklist storage access hardening. Consolidated Field Mode checklist localStorage reads and writes behind safe helper functions so initial render, daily initialization, and checklist toggles all tolerate browser storage read/write failures without breaking onsite mode. Strengthened FieldMode regression coverage to keep the safe read/write helpers and prevent direct fresh-checklist writes from returning. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-987 | `[hanwoo-dashboard]` Field Mode persisted checklist normalization. Changed Field Mode persisted checklist loading to normalize saved localStorage values through the default checklist shape, preserving checked state only for known checklist items and falling back to a fresh checklist for malformed or non-array saved data. Wrapped checklist localStorage writes during toggles so storage failures do not break onsite checklist interaction. Strengthened FieldMode regression coverage to keep the safe persisted-checklist path and prevent raw `JSON.parse(saved)` from returning directly. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-986 | `[hanwoo-dashboard]` AI route system-instruction helper-tone polish. Changed AI chat and AI insight route system instructions from command-style `말하세요` / `안내하세요` / `결정하세요` / `포함하세요` phrasing to helper-tone `말해 주세요` / `안내해 주세요` / `결정해 주세요` / `포함해 주세요`, aligning model-facing prompts with the app's operator-facing Korean tone. Strengthened AI chat and AI insight route regression coverage to keep the helper-tone prompt wording and prevent the older command-style phrases from returning. Verified focused AI route/widget tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-985 | `[hanwoo-dashboard]` Field Mode search result loading-copy polish. Changed the Field Mode search result header from `검색 매칭 개체` to `검색된 개체`, and changed the loading status from `전체 로드 중...` to `전체 목록 불러오는 중...`, making the onsite search overlay read like natural Korean product copy. Strengthened FieldMode regression coverage to keep the polished search-result wording and prevent the older mechanical phrases from returning. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-984 | `[hanwoo-dashboard]` Field Mode status counter copy accuracy polish. Changed the Field Mode counter footnotes from the over-assertive `이표 검수 100% 완료` and stiff `가축 기상 경보 확인 요망` to `이표 정보 기준 집계` and `기상 경보 확인 필요`, making the onsite counters less misleading and more consistent with operator-facing Korean copy. Strengthened FieldMode regression coverage to keep the accurate counter wording and prevent the older phrases from returning. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-983 | `[hanwoo-dashboard]` Field Mode checklist eyebrow Korean-copy polish. Changed the Field Mode checklist eyebrow from `Tactile stables list` to `축사 점검 목록`, removing a remaining visible English label from the onsite checklist card. Strengthened FieldMode regression coverage to keep the Korean checklist label and prevent the older English phrase from returning. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-982 | `[hanwoo-dashboard]` Field Mode header Korean operator-copy polish. Changed the Field Mode header eyebrow from `Smart Field Overlay` to `현장 점검 화면`, and changed the hidden search section label from `개체 초고속 검색` to `개체 빠른 검색`, removing remaining English/overstated wording from the onsite mode header and search landmark. Strengthened FieldMode regression coverage to keep the Korean restrained wording and prevent the older phrases from returning. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-981 | `[hanwoo-dashboard]` Field Mode top-card operator-tone polish. Changed the Field Mode top card labels from `현장 기동성 극대화` / `오프라인 자가생존` to `현장 점검 준비` / `오프라인 대응`, aligning visible onsite mode copy with restrained operational tool tone. Strengthened FieldMode regression coverage to keep the toned-down wording and prevent the older marketing-style phrases from returning. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-980 | `[hanwoo-dashboard]` Field Mode search placeholder cattle-terminology consistency. Changed the Field Mode search placeholder from `이표번호 4자리 또는 소 이름 입력` to `이표번호 4자리 또는 개체명 입력`, aligning onsite search copy with the dashboard's `개체명` terminology. Strengthened Field Mode regression coverage to keep the `개체명` placeholder and prevent the older `소 이름` wording from returning. Verified focused FieldMode tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-979 | `[hanwoo-dashboard]` Empty pen state Korean spacing polish. Changed the pen detail empty-state copy from `이 칸은 비어있습니다.` to `이 칸은 비어 있습니다.`, correcting the visible Korean spacing in the home pen view. Strengthened home dashboard regression coverage to keep the corrected empty-pen wording and prevent the fused spelling from returning. Verified focused home-market tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-978 | `[hanwoo-dashboard]` AI chat offline fallback formal helper-tone consistency. Changed the AI chat widget's generic offline fallback from `지금은 기본 농장 운영 질문 위주로 안내하고 있어요. ... 도와드릴게요.` to `지금은 기본 농장 운영 질문 위주로 안내합니다. ... 더 정확히 안내합니다.`, aligning the fallback response with the widget's `질문해 주세요` operator tone. Strengthened AI chat widget regression coverage to keep the formal helper wording and prevent the older casual fallback from returning. Verified focused AI chat widget tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-977 | `[hanwoo-dashboard]` Route error and not-found formal Korean-copy consistency. Changed the 404 page title/body from `페이지를 찾을 수 없어요` / `화면일 수 있어요` to `페이지를 찾을 수 없습니다` / `화면일 수 있습니다`, and changed the route error heading/body from `잠시 문제가 생겼어요` / `오류가 발생했어요` / `대시보드로 돌아가세요` to `잠시 문제가 발생했습니다` / `오류가 발생했습니다` / `대시보드로 돌아가 주세요`, aligning app-level error screens with the dashboard's formal operator tone. Strengthened error-page regression coverage to keep the polished wording and prevent the older casual wording from returning. Verified focused error-page tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-976 | `[hanwoo-dashboard]` Building delete blocker cattle-copy consistency. Changed the building deletion blocker from `이 축사에 N두의 소가 있어 삭제할 수 없습니다. 먼저 소를 이동해 주세요.` to `이 축사에 N두의 개체가 있어 삭제할 수 없습니다. 먼저 개체를 이동해 주세요.`, aligning server-action feedback with the dashboard's `개체` terminology. Strengthened server action copy regression coverage to keep the `개체` wording and prevent the older `소` wording from returning. Verified focused action-copy tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-975 | `[hanwoo-dashboard]` AI insight low-THI record-copy consistency. Changed the deterministic AI Insight low-THI condition card from `체중 측정 데이터 갱신에 적합한 시점` to `체중 측정 기록 갱신에 적합한 시점`, aligning the visible safe-condition guidance with operator-entered record terminology. Strengthened AI Insight regression coverage to keep the record wording and prevent the older data wording from returning. Verified focused AI Insight tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-974 | `[hanwoo-dashboard]` Dashboard full-list loading information-copy consistency. Changed the dashboard paginated full-list timeout and failure messages from `대시보드 데이터를 불러오는 데 시간이 오래 걸리고 있습니다.` / `대시보드 데이터를 불러오지 못했습니다.` to `대시보드 정보를 불러오는 데 시간이 오래 걸리고 있습니다.` / `대시보드 정보를 불러오지 못했습니다.`, aligning visible dashboard loading errors with the app's information-oriented failure copy. Strengthened home-market regression coverage to keep the information wording and prevent the older data wording from returning. Verified focused home-market tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-973 | `[hanwoo-dashboard]` AI insight prompt information-copy consistency. Changed the AI insight API system instruction from `제공된 농장 스냅샷 데이터를 근거로` and `데이터 기반 위급도` to `제공된 농장 스냅샷 정보를 근거로` and `농장 정보 기반 위급도`, and aligned the prompt-builder priority format hint from `데이터 기반` to `농장 정보 기반`. Strengthened AI insight regression coverage to keep the information wording and prevent the older data wording from returning. Verified focused AI insight tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-972 | `[hanwoo-dashboard]` AI chat farm-context information-copy consistency. Changed the AI chat system instruction from `제공된 농장 데이터를 근거로` and `데이터가 없거나 불확실한 경우` to `제공된 농장 정보를 근거로` and `정보가 없거나 불확실한 경우`, changed the empty status summary fallback from `상태별 개체 데이터 없음` to `상태별 개체 집계 없음`, and changed the farm-context failure fallback from `농장 데이터를 불러오지 못했습니다.` to `농장 정보를 불러오지 못했습니다.`. Strengthened AI chat route regression coverage to keep the information/aggregate wording and prevent the older data wording from returning. Verified focused AI chat tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-971 | `[hanwoo-dashboard]` AI insight record/information-copy consistency. Changed the AI insight widget description from `농장 데이터를 기반으로 우선순위 3가지 행동을 정리합니다.` to `농장 기록을 기반으로 우선순위 3가지 행동을 정리합니다.`, and changed the settings widget description from `켜면 농장 요약 데이터를 AI 분석 API로 전송합니다.` to `켜면 농장 요약 정보를 AI 분석 API로 전송합니다.`, aligning AI-visible operator copy with the app's record/information terminology. Strengthened AI insight and settings regression coverage to keep the record/information wording and prevent the older data wording from returning. Verified focused AI insight/settings tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-970 | `[hanwoo-dashboard]` Weather partial-forecast information-copy consistency. Changed the partial weather forecast degraded-state message from `일부 예보 데이터를 불러오지 못해 현재 날씨와 확인된 예보만 표시합니다.` to `일부 예보 정보를 불러오지 못해 현재 날씨와 확인된 예보만 표시합니다.`, keeping weather fallback copy aligned with the app's information-oriented weather wording. Strengthened weather-state regression coverage to keep the information wording and prevent the older data wording from returning. Verified focused weather/home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-969 | `[hanwoo-dashboard]` Profitability widget record-based copy consistency. Changed the profitability widget empty state from generic `수익성 분석 데이터가 부족합니다.` to `수익성 분석에 필요한 기록이 부족합니다.`, and changed the customized assumptions badge from `농가 데이터` to `농가 기록 기반`, aligning the widget with operator-entered record terminology. Strengthened profitability regression coverage to keep the record-based empty-state wording and prevent the older generic data wording from returning. Verified focused profitability tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-968 | `[hanwoo-dashboard]` Runtime error fallback Korean-copy polish. Changed the dashboard runtime ErrorBoundary heading from `화면 렌더링에 일시적인 에러가 발생했습니다.` to `화면 표시 중 일시적인 오류가 발생했습니다.`, and rewrote the body from the technical `데이터 형식`/`렌더링 도중` phrasing to operator-facing `화면 표시 정보`/`처리 중` wording. Strengthened error-page regression coverage to keep the polished Korean fallback copy and prevent the older technical wording from returning. Verified focused error-page tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-967 | `[hanwoo-dashboard]` AI insight market-price information-copy consistency. Changed the AI insight default data-quality card body from `체중·판매액·시세 데이터를 갱신하면 더 정확한 인사이트를 제공합니다.` to `체중·판매액 데이터와 시세 정보를 갱신하면 더 정확한 인사이트를 제공합니다.`, keeping externally sourced market-price wording aligned with the app's `시세 정보` copy while preserving the data-quality intent. Strengthened AI insight regression coverage to prevent the older bundled `시세 데이터` wording from returning. Verified focused AI insight tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-966 | `[hanwoo-dashboard]` Market price unavailable information-copy consistency. Changed the shared market-price unavailable fallback and widget fallback from `지금은 한우 시세 데이터를 확인할 수 없습니다.` to `지금은 한우 시세 정보를 확인할 수 없습니다.`, aligning visible market-price failure copy with the app's information-oriented weather/profitability wording. Strengthened market-price and home regression coverage to keep the information wording and prevent the older data wording from returning. Verified focused market/home tests, full Hanwoo tests, eslint, whitespace check, and Next production build. | Codex | 2026-05-27 |
| T-965 | `[hanwoo-dashboard]` Profitability market-price information-copy consistency. Changed profitability service operator-facing market-price errors from `수익성 시뮬레이션에 사용할 시세 데이터가 없습니다.` and `시세 데이터를 해석하지 못했습니다.` to `수익성 시뮬레이션에 사용할 시세 정보가 없습니다.` and `시세 정보를 해석하지 못했습니다.`, keeping visible profitability errors aligned with the app's information-oriented weather/market copy. Strengthened profitability regression coverage to keep the information wording and prevent the older data wording from returning. Verified focused profitability tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-964 | `[hanwoo-dashboard]` Weather unavailable information-copy consistency. Changed the shared weather unavailable fallback from `지금은 날씨 데이터를 확인할 수 없습니다. 잠시 후 다시 시도해 주세요.` to `지금은 날씨 정보를 확인할 수 없습니다. 잠시 후 다시 시도해 주세요.`, and aligned the weather widget's local fallback with the shared weather-state message. Strengthened weather and home regression coverage to keep the information wording and prevent the older data wording from returning. Verified focused weather/home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-963 | `[hanwoo-dashboard]` AI insight description action-tone consistency. Changed the AI insight widget description from `농장 데이터를 기반으로 우선순위 3가지 행동을 제안합니다.` to `농장 데이터를 기반으로 우선순위 3가지 행동을 정리합니다.`, aligning the widget with the recent move away from recommendation-style copy toward analysis and action-priority language. Strengthened AI insight widget regression coverage to keep the new action-tone wording and prevent the older proposal wording from returning. Verified focused AI insight widget tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-962 | `[hanwoo-dashboard]` Excel export empty-list copy consistency. Changed the Excel export empty-download warning title from `다운로드할 개체 데이터가 없습니다.` to `다운로드할 개체 목록이 없습니다.`, aligning the export flow with the dashboard's `개체 목록` terminology for loaded/exported cattle collections. Strengthened Excel export regression coverage to keep the list wording and prevent the older data wording from returning. Verified focused Excel export tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-961 | `[hanwoo-dashboard]` Analysis cost chart empty-state record-copy consistency. Changed the Analysis tab cost-structure empty chart message from `비용 데이터가 아직 충분히 쌓이지 않았습니다.` to `비용 기록이 아직 충분히 쌓이지 않았습니다.`, keeping the chart body aligned with the `실제 비용 기록 없음` header and the app-wide cost-record terminology. Strengthened analysis regression coverage to keep the cost-record wording and prevent the older data wording from returning. Verified focused analysis tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-960 | `[hanwoo-dashboard]` Analysis cost empty-state record-copy consistency. Changed the Analysis tab cost-structure empty label from `실제 비용 데이터 없음` to `실제 비용 기록 없음`, aligning the analysis card with the Sales tab and action copy that refer to operator-entered cost records. Strengthened analysis regression coverage to keep the cost-record wording and prevent the older data wording from returning. Verified focused analysis tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-959 | `[hanwoo-dashboard]` Profitability widget candidate-analysis copy clarity. Changed remaining profitability widget recommendation-style headers from `출하 추천 개체` and `출하 수익성 추천` to `출하 후보 개체` and `출하 수익성 분석`, and rewrote the empty state to say there is no candidate whose shipment schedule needs checking or that profitability analysis data is insufficient. Strengthened profitability regression coverage to keep the candidate-analysis copy and prevent the old recommendation framing from returning. Verified focused profitability tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-958 | `[hanwoo-dashboard]` Shipment recommendation action-copy clarity. Changed AI Insight shipment wording from `즉시 출하 권장`/`즉시 출하 권장 개체` to action-ready `출하 일정 확정 필요` and `즉시 출하 후보 개체`, and changed the profitability widget shipment badge to `출하 일정 확인 필요`. Strengthened AI Insight and profitability regression coverage to keep the new shipment action copy and prevent the older recommendation phrasing from returning. Verified focused AI Insight/profitability tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-957 | `[hanwoo-dashboard]` Market price footer metadata clarity. Changed the market price footer labels from terse `갱신` and `출처: KAPE` to `마지막 갱신` and `데이터 출처: KAPE`, so the card metadata reads as explicit operator-facing context. Strengthened home-market regression coverage to keep the clearer footer labels and prevent the terse spans from returning. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-956 | `[hanwoo-dashboard]` Market price row unit wording consistency. Changed Hanwoo market price row values from shorthand `N원 / kg` to `kg당 N원`, keeping the row value format aligned with the new `kg당 시세` panel titles. Strengthened home-market regression coverage to keep the `kg당` value wording and prevent the slash-style unit label from returning. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-955 | `[hanwoo-dashboard]` Market price panel unit-title clarity. Changed the Hanwoo market price panel titles from abbreviation-style `수소 / kg` and `암소 / kg` to `수소 kg당 시세` and `암소 kg당 시세`, so the card explains the unit and price context directly. Strengthened home-market regression coverage to keep the explicit `kg당 시세` titles and prevent the shorthand labels from returning. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-953 | `[workspace/hanwoo-dashboard]` Verified quality bundle closure. Rechecked the latest dirty worktree, confirmed the active bundle is Hanwoo quality-copy/fallback updates plus workspace tooling/session-record changes, and prepared it for commit after final gates. Verification passed for Hanwoo full QC (`335/335` tests, lint, build), Python compile for `session_orient.py` and `dependency_security_audit.py`, and code-review gate WARN (not FAIL) at risk `0.55`. Dependency audit command surfaced 73 current vulnerability alerts across 25 packages and remains a separate security backlog. | Codex | 2026-05-27 |
| T-954 | `[hanwoo-dashboard]` Analysis chart sales terminology consistency. Changed Analysis and Financial chart visible, legend, hover/title, and chart accessibility copy from `매출` to `판매액`, keeping financial charts aligned with Sales, AI Insight, and the existing `연간 총판매액` KPI. Strengthened analysis regression coverage to keep the `판매액` chart wording and prevent the older `매출` chart labels from returning. Verified focused analysis tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-952 | `[hanwoo-dashboard]` Calving alert due-date fallback specificity. Changed calving alert missing target-date copy from placeholder `-` to `분만 예정일 미등록`, so the alert chip explains which schedule field is unavailable. Strengthened alert-banner regression coverage to keep the specific fallback and prevent the dash fallback from returning. Verified focused alert-banner tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-951 | `[hanwoo-dashboard]` Sales history grade fallback specificity. Changed cattle sale history copy from `등급: -` to `등급: 등급 미등록` when a sale grade is missing, so generated cattle timeline entries explain the missing sale grade. Strengthened server-action copy regression coverage to prevent dash fallback from returning. Verified focused action-copy tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-950 | `[hanwoo-dashboard]` Subscription failure error-code fallback specificity. Changed the subscription failure page missing error-code fallback from placeholder `-` to `오류 코드 미전달`, so payment failure details explain that the gateway code was not provided. Strengthened payment UX regression coverage to prevent dash fallback from returning. Verified focused payment UX tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-949 | `[hanwoo-dashboard]` Ear-tag scanner birth-date fallback specificity. Changed the ear-tag scanner matched-cattle birth-date fallback from placeholder `-` to `생년월일 미등록`, and routed scanner birth-date rendering through a small invalid-date guard so malformed dates no longer surface as raw date output. Strengthened scanner regression coverage to prevent dash fallback from returning. Verified focused scanner tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-948 | `[hanwoo-dashboard]` Cattle card genetic-grade fallback specificity. Changed cattle row genetic-grade rendering from placeholder `-` to `유전 등급 미등록` when the grade is missing, blank, or legacy `-`, so pen/list cattle cards explain the missing data field. Strengthened card regression coverage to prevent dash fallback from returning. Verified focused card tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-947 | `[hanwoo-dashboard]` Notification time fallback specificity. Changed notification timing fallback copy from placeholder `-` to `알림 시간 확인 불가` in both the notification timing utility and notification modal rendering, so missing or malformed alert times identify the unavailable field. Strengthened notification timing and modal regression coverage to prevent dash fallbacks from returning. Verified focused notification tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-946 | `[hanwoo-dashboard]` AI Insight weather fallback specificity. Changed AI Insight prompt weather fallback copy from symbolic `?℃`/`?%` placeholders to explicit Korean signals (`기온 확인 불가`, `습도 확인 불가`) when THI is available but temperature or humidity is missing. Strengthened AI Insight regression coverage to keep the explicit weather fallback and prevent ambiguous symbol placeholders from returning. Verified focused AI Insight/chat/weather tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-945 | `[hanwoo-dashboard]` AI Insight sales-amount terminology consistency. Changed AI Insight prompt/default-card sales amount wording from `매출` to `판매액` (`판매액 N만원`, `출하·판매액 N만원`, `체중·판매액·시세 데이터`), keeping generated insight context aligned with Sales and Analysis terminology. Strengthened AI Insight regression coverage to keep `판매액` wording and prevent the older `매출` phrases from returning. Verified focused AI Insight/chat/home/Analysis tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-944 | `[hanwoo-dashboard]` Alert banner missing-pen fallback specificity. Changed estrus/calving alert banner missing-pen fallback from placeholder `-` to `칸 미지정`, so alert location chips explain that the pen assignment is missing instead of rendering a bare dash before `번`. Strengthened alert-banner regression coverage to keep the specific pen fallback and prevent the dash fallback from returning. Verified focused alert/home/field-mode tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-943 | `[hanwoo-dashboard]` Sales tab sales-terminology summary consistency. Changed the Sales tab summary heading, cumulative KPI label, and no-sales helper description from `매출` wording to `판매`/`판매액` wording (`출하 및 판매 분석`, `총 누적 판매액`, `판매액, 등급, 수익 분석 차트`), keeping the Sales tab aligned with the app-wide `판매 기록` and Analysis `연간 총판매액` terminology. Strengthened home-market regression coverage to keep the sales summary labels and prevent the older `매출` wording from returning. Verified focused Sales/home/empty/Analysis tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-942 | `[hanwoo-dashboard]` Analysis KPI sales terminology consistency. Changed the Analysis tab annual revenue KPI title from `연간 총매출` to `연간 총판매액`, keeping the management-analysis KPI aligned with the app-wide `판매` terminology already used by Sales, Today Focus, and home quick actions. Strengthened Analysis regression coverage to keep the `판매` KPI label and prevent the old `매출` KPI title from returning. Verified focused Analysis/home/Today Focus tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-941 | `[hanwoo-dashboard]` Shared short-date fallback specificity. Changed the common `formatDate()` fallback from bare `-` to `날짜 미등록`, aligning short date rendering with the existing long-date fallback and preventing detail/list date fields from surfacing meaningless dash placeholders when dates are missing or malformed. Strengthened date utility regression coverage to keep the Korean missing-date fallback and prevent the dash fallback from returning. Verified focused date/detail/alert/calving tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-940 | `[hanwoo-dashboard]` Cattle detail genetic fallback specificity. Changed the cattle detail genetic-info fallback from placeholder-style `부:- / 모:-` to `부:부계 미등록 / 모:모계 미등록`, so the genetic ability card explains which lineage field is missing instead of showing bare dashes. Strengthened cattle-detail regression coverage to keep the lineage-specific fallback copy and prevent dash fallbacks from returning. Verified focused cattle-detail tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-939 | `[hanwoo-dashboard]` Cattle detail breeding-date fallback specificity. Changed cattle detail reproductive-date fallbacks from placeholder `-` to domain-specific Korean copy (`발정일 미등록`, `최근 발정일 미등록`, `수정일 미등록`, `분만 예정일 미등록`), so breeding cards explain which date is missing instead of showing a bare dash. Strengthened cattle-detail regression coverage to keep the specific missing-date labels and prevent the dash fallback from returning. Verified focused cattle-detail tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-938 | `[hanwoo-dashboard]` Sales no-cost state copy specificity. Changed the Sales tab no-cost per-record label from generic `관련 비용 없음` to `연결된 비용 기록 없음`, so profit-unavailable rows explain that no linked cost records exist before showing `비용 기록 없어 수익 추정 불가`. Strengthened Sales regression coverage to keep the specific no-cost label and prevent the generic label from returning. Verified focused home-market/Sales tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-937 | `[hanwoo-dashboard]` Admin diagnostics unavailable-copy specificity. Changed admin diagnostics fallback copy from generic `확인 불가`/`-` to domain-specific `DB 상태 확인 불가`, `DB 응답 시간 확인 불가`, and `Node 버전 확인 불가`, so failure/empty diagnostics cards identify which system signal is unavailable. Strengthened diagnostics regression coverage to keep the specific unavailable labels and prevent generic latency fallback from returning. Verified focused diagnostics tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-936 | `[hanwoo-dashboard]` Field mode search missing-building fallback specificity. Changed Field Mode search-result missing-building copy from generic `미지정` to `축사 미지정`, so onsite cattle search results name the missing location domain consistently with alert banner location chips. Strengthened FieldModeView regression coverage to keep the specific building fallback and prevent the generic fallback from returning. Verified focused FieldModeView tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-935 | `[hanwoo-dashboard]` Alert banner missing-building fallback specificity. Changed estrus/calving alert banner missing-building copy from generic `미지정` to `축사 미지정`, so alert location chips name the missing location domain instead of showing a vague placeholder. Strengthened alert-banner regression coverage to keep the specific building fallback and prevent the generic fallback from returning. Verified focused alert-banner tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-934 | `[hanwoo-dashboard]` Weather unavailable source-label specificity. Changed weather unavailable source labels from generic `확인 불가` to `날씨 확인 불가` in the weather state model, aligning degraded/unavailable weather payloads with the visible weather widget unavailable copy. Strengthened weather-state regression coverage to keep the specific source label. Verified focused weather/home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-933 | `[hanwoo-dashboard]` Market price unavailable source-label specificity. Changed market-price unavailable source labels from generic `확인 불가` to `시세 확인 불가` in the market price state model and widget presentation fallback, so degraded/unavailable price cards name the missing data domain clearly. Strengthened market-price regression coverage to keep the specific source label. Verified focused market/home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-932 | `[hanwoo-dashboard]` Profitability recommendation missing-identity fallback clarity. Changed the profitability recommendation widget's missing tag/name fallbacks from placeholder-style `----` and `-` to Korean operator copy `이력번호 미등록` and `개체명 미등록`, aligning shipment-profit cards with Sales, AI farm context, cattle cards, and alert banner missing-identity copy. Strengthened profitability widget regression coverage to prevent placeholder fallbacks from returning. Verified focused profitability tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-931 | `[hanwoo-dashboard]` AI chat farm-context status fallback specificity. Changed the AI chat farm-context fallback for empty cattle status grouping from generic `데이터 없음` to `상태별 개체 데이터 없음`, so the model receives a domain-specific missing-data signal instead of a vague placeholder. Strengthened AI chat route regression coverage to keep the specific fallback and prevent the generic status summary fallback from returning. Verified focused AI chat API tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-930 | `[hanwoo-dashboard]` Primary data-entry submit copy consistency. Changed idle submit labels for Schedule, Feed, Inventory, Sales, and Settings forms from `등록하기`/`저장하기` variants to concise task labels (`일정 등록`, `급여 기록 저장`, `재고 등록`, `판매 기록 등록`, `농장 정보 저장`, `축사 등록`), aligning submit buttons with the already-normalized open-form CTAs while keeping pending `... 중` states intact. Strengthened form-submit, schedule, empty-state, home/market, and settings regression coverage to prevent the older `하기` submit labels from returning. Verified focused submit-copy tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-929 | `[hanwoo-dashboard]` Building-name missing fallback consistency. Changed remaining form/detail/feed building-name fallbacks from compact `축사명 미등록` to the app-wide `축사 이름 미등록`, aligning cattle forms, cattle detail location, and feed building filters with Dashboard and Settings building fallback copy. Strengthened cattle-detail and empty-state regression coverage to prevent the compact fallback from returning. Verified focused cattle-detail/empty-state tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-928 | `[hanwoo-dashboard]` Alert banner missing cattle-name fallback consistency. Changed alert banner missing cattle-name fallback from generic `이름 미등록` to Hanwoo-wide `개체명 미등록`, aligning estrus/calving alert banners with dashboard rows, sales fallbacks, cards, and analysis ranking copy. Strengthened alert-banner regression coverage to prevent the generic fallback from returning. Verified focused alert-banner tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-927 | `[hanwoo-dashboard]` Notification dropdown fallback title clarity. Changed the notification dropdown malformed-title fallback from empty-value copy `알림 제목 없음` to product-facing `운영 알림`, aligning it with the notification widget's default heading and avoiding a raw missing-title state in the UI. Strengthened notification-system regression coverage to prevent the terse fallback from returning. Verified focused notification-system tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-926 | `[hanwoo-dashboard]` Ear-tag scanner no-match empty-state copy clarity. Changed the scanner no-match heading from terse `인식된 개체 정보 없음` to sentence-style `인식된 개체 정보가 없습니다`, keeping the scanner's failure state consistent with clearer Korean empty-state copy. Strengthened ear-tag scanner regression coverage to prevent the terse heading from returning. Verified focused scanner tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-925 | `[hanwoo-dashboard]` Empty building CTA accessible helper-tone consistency. Changed the home empty-building CTA `aria-label` and `title` from noun-style `설정에서 첫 번째 축사 추가하기` to operator-facing guidance `설정에서 첫 번째 축사를 추가해 주세요`, aligning assistive-tech and hover copy with the visible first-building CTA tone. Strengthened home/market regression coverage to prevent the old label from returning. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-924 | `[hanwoo-dashboard]` Empty building CTA helper-tone consistency. Changed the home empty-building CTA title from command-style `첫 번째 축사를 추가해보세요` to operator-facing guidance `첫 번째 축사를 추가해 주세요`, keeping the first-run setup action aligned with the app-wide Korean helper tone. Strengthened home/market regression coverage for the first-building CTA copy. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-923 | `[hanwoo-dashboard]` AI chat offline greeting helper-tone consistency. Changed the offline fallback greeting reply from question-style `오늘 농장 운영에서 어떤 부분이 궁금하신가요?` to operator-facing guidance `오늘 농장 운영에서 궁금한 부분을 질문해 주세요.`, keeping the offline chat path aligned with the AI chat welcome prompt and input placeholder. Strengthened AI chat widget regression coverage for the greeting fallback copy. Verified focused AI chat widget tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-922 | `[hanwoo-dashboard]` THI level description helper-tone guidance. Changed THI warning/danger descriptions from status-style `급수량 확보와 송풍 강화가 필요한 수준입니다` and `즉시 냉방과 살수 조치가 필요한 고위험 상태입니다` to operator-facing guidance `급수량을 확보하고 송풍을 강화해 주세요` and `즉시 냉방과 살수 조치를 진행해 주세요`, keeping the weather card's THI guidance aligned with livestock weather alerts. Strengthened home/market copy regression coverage for THI descriptions. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-921 | `[hanwoo-dashboard]` Weather danger-alert helper-tone guidance. Changed livestock weather danger messages from status-style `냉방과 살수 조치가 필요합니다` and `보온 설비 점검이 필요합니다` to operator-facing guidance `냉방과 살수 조치를 진행해 주세요` and `보온 설비를 점검해 주세요`, keeping severe heat/cold alerts aligned with existing weather helper tone. Strengthened home/market copy regression coverage for danger weather alerts. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-920 | `[hanwoo-dashboard]` AI Insight default routine helper-tone guidance. Changed the deterministic AI Insight default routine recommendation from status-style `발정·분만·사료·물·축사 환기 5가지 일상 점검을 권장합니다` to operator-facing guidance `발정·분만·사료·물·축사 환기 5가지 일상 점검을 진행해 주세요`, keeping safe fallback insight cards aligned with the app-wide helper tone. Strengthened AI Insight regression coverage for the default routine card. Verified focused AI Insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-919 | `[hanwoo-dashboard]` Today Focus feed-depletion warning helper-tone guidance. Changed the non-critical feed-depletion Today Focus title from status-style `사료 잔여 N일 (점검 권장)` to operator-facing guidance `사료 잔여 N일, 재고를 점검해 주세요`, so feed stock warnings name the action before the danger threshold. Added warning-branch regression coverage for the feed-depletion item. Verified focused Today Focus tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-918 | `[hanwoo-dashboard]` AI Insight shipment schedule helper-tone guidance. Changed the deterministic AI Insight immediate-shipment recommendation from status-style `24시간 내 출고 일정 확정 권장` to operator-facing guidance `24시간 내 출고 일정을 확정해 주세요`, keeping high-priority shipment recommendations aligned with the app-wide helper tone. Strengthened AI Insight regression coverage for the shipment card. Verified focused AI Insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-917 | `[hanwoo-dashboard]` AI Insight THI heat-warning helper-tone guidance. Changed the deterministic AI Insight high-THI recommendation from status-style `환기·미스트팬 가동, 급수기 4회 이상 점검 권장` to operator-facing guidance `환기·미스트팬을 가동하고 급수기를 4회 이상 점검해 주세요`, keeping weather stress recommendations aligned with the app-wide helper tone. Strengthened AI Insight regression coverage for the heat-warning card. Verified focused AI Insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-916 | `[hanwoo-dashboard]` AI Insight calving preparation helper-tone guidance. Changed the deterministic AI Insight calving-preparation recommendation from status-style `산방 청결·보온·요오드 소독 준비 점검 권장` to operator-facing guidance `산방 청결·보온·요오드 소독 준비를 점검해 주세요`, keeping calving heuristic recommendations aligned with the app-wide helper tone. Updated AI Insight regression coverage for the calving-preparation card. Verified focused AI Insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-915 | `[hanwoo-dashboard]` AI Insight declining-margin helper-tone guidance. Changed the deterministic AI Insight declining-margin recommendation from status-style `단가·증체 추세 재검토 필요` to operator-facing guidance `단가·증체 추세를 재검토해 주세요`, keeping heuristic recommendations aligned with the app-wide helper tone. Updated AI Insight regression coverage for the declining-margin card. Verified focused AI Insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-914 | `[hanwoo-dashboard]` Sales empty-state missing-cattle action guidance. Changed the Sales tab empty-state disabled action label from terse `개체 등록 필요` to the operator-facing guidance `개체를 먼저 등록해 주세요`, so the no-cattle state explains the prerequisite action instead of showing a noun-phrase status. Updated empty-state and home-market regression coverage. Verified focused empty-state/home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-913 | `[hanwoo-dashboard]` Cattle Excel export action specificity. Changed the Excel export button labels from generic `엑셀 다운로드` / `엑셀 다운로드 준비 중` to `개체 엑셀 다운로드` / `개체 엑셀 다운로드 준비 중`, so the header export action clearly names the cattle dataset being exported in visible copy, accessible label, and hover title. Updated Excel export regression coverage. Verified focused Excel export tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-912 | `[hanwoo-dashboard]` Market price refresh busy-label specificity. Changed the market price refresh button busy accessible/title label from generic `시세 갱신 중` to `한우 시세 갱신 중`, keeping the loading action tied to the Hanwoo market-price widget context. Updated home/market regression coverage to prevent the generic busy label from returning. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-911 | `[hanwoo-dashboard]` AI chat empty-input send action label. Changed the AI chat send button accessible/title label so an empty disabled input says `질문을 입력하면 보낼 수 있습니다` instead of the actionable `질문 보내기`; streaming still says `답변 생성 중` and ready state says `질문 보내기`. Updated AI chat widget regression coverage. Verified focused AI chat widget tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-910 | `[hanwoo-dashboard]` Pen cattle preview hover context. Changed pen-card cattle preview titles from cow-name-only hover text to state-aware Korean labels (`발정 알림 있음` / `칸 배치됨`), so quick pen previews expose alert context without opening the detail view. Updated card accessibility regression coverage to keep the contextual preview titles. Verified focused card accessibility tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-909 | `[hanwoo-dashboard]` Login and not-found operations helper-tone consistency. Changed the login and not-found route guidance from `사육, 재고, 출하 업무를 이어서 관리하세요.` to `사육, 재고, 출하 업무를 이어서 관리해 주세요.`, keeping entry and recovery pages aligned with the app-wide Korean helper tone. Updated error-page regression coverage to prevent the command-style route copy from returning. Verified focused error-page tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-908 | `[hanwoo-dashboard]` Subscription page helper-tone consistency. Changed the subscription page value copy from `AI 보조 기능을 더 안정적으로 사용하세요.` to `AI 보조 기능을 더 안정적으로 사용해 주세요.`, aligning the payment entry page with the app-wide Korean helper tone. Updated payment UX regression coverage to prevent the command-style subscription copy from returning. Verified focused payment UX tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-907 | `[hanwoo-dashboard]` AI chat welcome helper-tone consistency. Changed the AI farm assistant welcome guidance from `궁금한 점을 물어보세요.` to `궁금한 점을 질문해 주세요.`, aligning the first chat prompt with the app-wide Korean helper tone and the input placeholder. Updated AI chat widget regression coverage to prevent the command-style wording from returning. Verified focused AI chat widget tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-906 | `[hanwoo-dashboard]` MTRACE timeout recovery copy consistency. Changed the 축산물이력제 timeout failure message from `축산물이력제 조회 시간이 초과되었습니다. 다시 시도해 주세요.` to `축산물이력제 조회 시간이 초과되었습니다. 잠시 후 다시 시도해 주세요.`, matching the app-wide transient retry wording used for degraded external services. Updated mtrace regression coverage to prevent the terse timeout retry copy from returning. Verified focused mtrace tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-905 | `[hanwoo-dashboard]` Today Focus sales analysis helper-tone consistency. Changed the Today Focus monthly sales analysis guidance from `판매 흐름을 분석 탭에서 확인하세요.` to `판매 흐름을 분석 탭에서 확인해 주세요.`, keeping the sales terminology fix while aligning the action guidance with the app-wide Korean helper tone. Updated Today Focus regression coverage to prevent both the old `매출` terminology and the command-style `확인하세요` wording from returning. Verified focused Today Focus tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-904 | `[hanwoo-dashboard]` Cattle form helper guidance tone consistency. Changed the cattle edit/create form helper copy from `개체 정보를 수정하고 저장하세요` and `새 개체의 기본 정보를 입력하세요` to `개체 정보를 수정하고 저장해 주세요` and `새 개체의 기본 정보를 입력해 주세요`, keeping form guidance aligned with the app-wide Korean helper tone and existing validation messages. Updated cattle form regression coverage for both edit and create helper text. Verified focused cattle detail/form tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-903 | `[hanwoo-dashboard]` AI Insight schedule fallback helper-tone consistency. Changed the deterministic AI Insight safe schedule fallback from `캘린더에서 확인하세요` to `캘린더에서 확인해 주세요`, keeping all no-signal fallback recommendations aligned with the app-wide Korean helper tone. Split regression coverage so registered-herd no-signal defaults prove the schedule card copy without assuming it appears in the empty-herd top-three slice. Verified focused AI Insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-902 | `[hanwoo-dashboard]` Weather livestock guidance helper-tone consistency. Changed THI and livestock weather warning guidance from command-style `확인하세요`/`강화하세요`/`점검하세요` to `확인해 주세요`/`강화해 주세요`/`점검해 주세요`, keeping weather recovery and livestock-care recommendations aligned with the app-wide Korean helper tone. Updated home-market regression coverage for the utility copy. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-901 | `[hanwoo-dashboard]` QR print popup-block guidance tone consistency. Changed the QR label print popup-block failure message from `브라우저 팝업 허용 후 다시 시도하세요.` to `브라우저 팝업 허용 후 다시 시도해 주세요.`, keeping retry recovery guidance aligned with the app-wide Korean helper tone. Updated QR widget regression coverage to prevent reverting to the command-style wording. Verified focused QR widget tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-900 | `[hanwoo-dashboard]` AI Insight fallback guidance tone consistency. Changed deterministic AI Insight fallback guidance from command-style `처치 일정 잡으세요` and `개체 등록을 먼저 진행하세요` to `처치 일정을 잡아 주세요` and `개체 등록을 먼저 진행해 주세요`, keeping fallback recommendations aligned with the app-wide helper tone. Updated AI Insight regression coverage for both fallback paths. Verified focused AI Insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-899 | `[hanwoo-dashboard]` Ear-tag scanner retry guidance tone consistency. Changed the scanner no-match guidance from stiff `다시 스캔해주십시오` to the app-wide `다시 스캔해 주세요`, keeping scanner failure recovery copy aligned with other retry and helper messages. Updated scanner modal regression coverage to prevent the stiff wording from returning. Verified focused scanner tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-898 | `[hanwoo-dashboard]` Analysis top-sale missing cattle-name copy consistency. Changed the Analysis tab top-sale fallback from generic `이름 없음` to the existing Hanwoo-wide `개체명 미등록`, keeping missing cattle-name copy aligned with dashboard rows, Sales tab fallbacks, cards, and AI farm context. Updated analysis copy regression coverage to prevent returning to the generic label. Verified focused analysis tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-897 | `[hanwoo-dashboard]` Analysis cost-data empty copy clarity. Changed the Analysis tab cost-structure fallback from terse `실데이터 없음` to `실제 비용 데이터 없음`, making the empty state specific to the missing cost data in that card. Updated analysis copy regression coverage to prevent returning to the terse label. Verified focused analysis tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-896 | `[hanwoo-dashboard]` Retry and move guidance spacing consistency. Changed remaining operator-facing `시도해주세요`/`이동해주세요` copy to `시도해 주세요`/`이동해 주세요` in offline sync failure toasts and the building-delete blocked message. Updated action, offline-sync, and home-market regression coverage to keep the spaced Korean helper wording. Verified focused action/sync/home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-895 | `[hanwoo-dashboard]` Month-label spacing copy consistency. Changed remaining user-facing and AI-insight copy from `이번달`/`다음달` to `이번 달`/`다음 달` in the home KPI card, AI insight prompt snapshot, and deterministic insight fallback. Updated home-market and AI insight regression coverage to keep the spaced Korean date copy. Verified focused AI insight and home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-894 | `[hanwoo-dashboard]` Today Focus sales terminology consistency. Changed the Today Focus monthly sales analysis prompt from `매출 흐름을 분석 탭에서 확인하세요.` to `판매 흐름을 분석 탭에서 확인하세요.`, keeping home guidance aligned with the Sales tab and quick-action `판매 기록` terminology. Added focused Today Focus regression coverage for the analysis-path copy. Verified focused Today Focus tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-893 | `[hanwoo-dashboard]` Cattle edit-form guidance copy clarity. Changed the cattle edit form helper text from generic `정보를 수정하고 저장하세요` to `개체 정보를 수정하고 저장하세요`, keeping the guidance aligned with the `개체 정보 수정` heading and save action. Updated cattle form regression coverage to prevent reverting to the generic edit helper. Verified focused cattle detail/form tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-892 | `[workspace]` Dependabot backlog drain (27 → 15). Admin-merged 12 patch/minor PRs whose own project's tests already passed: #79 aiohttp, #81 react-hook-form, #83 beautifulsoup4, #85 anthropic, #86 @tailwindcss/postcss (hanwoo), #88 python-dotenv, #90 @serwist/next, #94 actions/setup-node v4→v6, #99 eslint-plugin-react-refresh, #101 tailwindcss (knowledge), #105 @tailwindcss/postcss (knowledge), #106 bullmq. Confirmed `test-summary` BLOCKED state was pre-existing on main HEAD `b75e8cd3` (shorts/hanwoo/knowledge CI broken, unrelated to deps), so admin merge introduced no new regression. 15 majors deferred to user: #95 pnpm/action-setup (token workflow scope), lucide-react v1 ×3 (brand icon removal audit), eslint v10 ×3 + @eslint/js v10, vite v8 ×2, jsdom v29 ×2, notion-client v3, @types/node v25 (rebase pending), globals v17 (suspicious lockfile churn). | Claude | 2026-05-27 |
| T-892 | `[hanwoo-dashboard]` Sales profit-estimation unavailable copy clarity. Changed the Sales tab per-record no-cost profit state from terse `수익 추정 불가` to `비용 기록 없어 수익 추정 불가`, making it clearer why profit cannot be estimated when no linked cost records exist. Updated home-market regression coverage to prevent reverting to the terse old copy. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-891 | `[hanwoo-dashboard]` Notification SMS test visible copy specificity. Changed the notification modal SMS test button's idle visible text from `테스트 전송` to `문자 알림 테스트 전송`, keeping the visible action aligned with its `aria-label` and `title`. Updated notification modal regression coverage to prevent reverting to the shorter generic copy. Verified focused notification modal tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-890 | `[hanwoo-dashboard]` Excel export busy visible copy specificity. Changed the Excel export button's visible busy text from `엑셀 준비 중...` to `엑셀 다운로드 준비 중...`, keeping the on-screen state aligned with its `aria-label` and `title`. Updated Excel export regression coverage to prevent reverting to the shorter busy copy. Verified focused Excel export tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-889 | `[hanwoo-dashboard]` Home quick action sales copy consistency. Changed the home dashboard quick action detail from `매출 바로 입력` to `판매 기록 바로 입력`, keeping the quick action aligned with the Sales tab's `판매 기록` terminology and registration flow. Updated home-market regression coverage to prevent reverting the quick-action detail to `매출 바로 입력`. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-888 | `[hanwoo-dashboard]` Settings building validation copy consistency. Changed client and server building-name validation messages from `동 이름을 입력해 주세요.` to `축사 이름을 입력해 주세요.`, keeping validation feedback aligned with the Settings form label and placeholder. Updated Settings regression coverage to keep both `formSchemas.js` and `action-validation.mjs` on the shared `축사 이름` terminology. Verified focused Settings/action-validation tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-887 | `[hanwoo-dashboard]` Create-form cancel-copy regression coverage hardening. Strengthened source-level regression tests for Sales, Inventory, Settings building, and Schedule create-form toggles so they fail if the open-state button text regresses to generic `취소`. This keeps the existing task-specific Korean cancel labels (`판매 기록 등록 취소`, `재고 등록 취소`, `축사 등록 취소`, `일정 등록 취소`) protected even if only the middle branch changes. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-886 | `[hanwoo-dashboard]` Settings building delete confirmation copy consistency. Changed the Settings building-delete confirmation title from `${name} 동을 삭제할까요?` to `${name} 축사를 삭제할까요?`, keeping the destructive confirmation dialog aligned with the visible `축사 삭제`, `축사 삭제 중...`, and Settings building-management terminology. Updated Settings regression coverage to prevent reverting to the old `동` wording. Verified focused Settings test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-885 | `[hanwoo-dashboard]` Remaining-day countdown copy consistency. Replaced the remaining user-facing `D-n` countdown labels in cattle detail, schedule upcoming events, cattle row alert badges, calving alert cards, and Today Focus schedule details with operator-readable `오늘` / `n일 남음` copy. Updated focused source and behavior regression coverage so the old `D-` labels do not return. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-884 | `[hanwoo-dashboard]` AI Insight opt-in privacy copy. Added an explicit Settings widget-list description for the opt-in AI Insight widget: `켜면 농장 요약 데이터를 AI 분석 API로 전송합니다.` This keeps the existing `defaultOn: false` privacy default while telling operators what enabling the widget does before any AI request is made. Updated AI Insight and Settings regression coverage to keep the opt-in privacy copy wired through the shared widget registry. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-883 | `[hanwoo-dashboard]` Alert banner remaining-day copy clarity. Changed estrus and calving alert banner countdown labels from terse `D-n` wording to operator-readable `n일 남음`, and changed same-day labels to `오늘`. Kept the existing malformed `daysLeft` normalization intact while making livestock alerts easier to scan in Korean. Updated alert banner regression coverage to prevent reverting to `D-` countdown labels. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-703 | `[hanwoo-dashboard]` AI Insight privacy default hardening. Reviewed mixed dirty worktree and changed the new AI Insight widget to opt-in by default (`defaultOn: false`) so farm summary data is not sent to external AI unless the operator enables the widget. Updated the source-level wiring test expectation. | Codex | 2026-05-27 |
| T-882 | `[hanwoo-dashboard]` Settings farm coordinate label copy consistency. Changed the Settings tab farm coordinate labels from `위도 (Latitude)` / `경도 (Longitude)` to `위도` / `경도`, removing unnecessary English from operator-facing form labels while keeping the existing numeric placeholders and validation wiring intact. Updated Settings regression coverage to prevent reverting to `Latitude` or `Longitude`. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-881 | `[hanwoo-dashboard]` Settings building section heading copy consistency. Changed the Settings tab building section heading from `축사 동 관리` to `축사 관리`, keeping the section title aligned with the surrounding `축사 등록`, `축사 이름`, `축사 삭제`, and `축사 삭제 중...` wording. Updated Settings regression coverage to prevent reverting to the old mixed `축사 동 관리` wording. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-880 | `[hanwoo-dashboard]` Settings building form label copy consistency. Changed the Settings tab building registration form labels and placeholder from `동 이름` / `동 이름을 입력해 주세요.` / `칸 수 (Pen Count)` to `축사 이름` / `축사 이름을 입력해 주세요.` / `칸 수`, keeping the form copy aligned with the surrounding `축사 등록` wording and removing unnecessary English from the operator-facing label. Updated Settings regression coverage to prevent reverting to `동 이름` or `Pen Count`. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-879 | `[hanwoo-dashboard]` Settings building delete accessible copy consistency. Changed the Settings tab building delete button's pending and idle accessible labels from `${building.name} 동 삭제...` to `${building.name} 축사 삭제...`, keeping aria-label/title copy aligned with the visible `축사 삭제` and `축사 삭제 중...` button text. Updated Settings regression coverage to prevent reverting to the old `동 삭제` wording. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-878 | `[hanwoo-dashboard]` Sales pagination retry copy consistency. Changed the sales pagination timeout and error messages from `이전 매출 기록...` to `이전 판매 기록...`, keeping the pagination retry feedback aligned with the Sales tab load-more CTA and the broader `판매 기록` wording. Updated sales pagination regression coverage to prevent reverting to the old `이전 매출 기록` wording. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-877 | `[hanwoo-dashboard]` Dashboard full sales ledger loading copy consistency. Changed the dashboard's full sales ledger loading, retry, and load-error copy from `매출 기록` wording to `판매 기록`, keeping the Analysis/Sales loading path aligned with the Sales tab CTA, form title, submit copy, and API fallback copy. Updated home-market regression coverage to prevent reverting the retry label, loading placeholder, or error fallback to the old `매출 기록` wording. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-876 | `[hanwoo-dashboard]` Settings building add-form heading copy consistency. Changed the Settings tab building add-form heading from `새 축사 동 등록` to `새 축사 등록`, keeping the visible form title aligned with the Settings tab's `축사 등록` CTA, submit copy, and accessible label. Updated Settings regression coverage to prevent reverting to the old mixed `동 추가`/`축사 등록` wording. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-875 | `[hanwoo-dashboard]` Sales add-form heading copy consistency. Changed the Sales tab add-form heading from `새 매출 기록 등록` to `새 판매 기록 등록`, keeping the visible form title aligned with the Sales tab's `판매 기록 등록` CTA, submit copy, and empty-state action. Updated home-market regression coverage to prevent reverting to the old mixed `매출 기록` form heading. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-874 | `[hanwoo-dashboard]` Settings building add-form open visible copy specificity. Changed the Settings tab building add-form toggle's closed-state visible copy from `+ 동 추가` to `축사 등록`, keeping the visible CTA aligned with the existing `축사 등록 창 열기` accessible label and the building registration submit copy. Updated Settings regression coverage to prevent reverting to the old shorthand visible copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-873 | `[hanwoo-dashboard]` Schedule add-form open visible copy specificity. Changed the Schedule tab add-form toggle's closed-state visible copy from `새 일정` to `일정 등록`, keeping the visible CTA aligned with the existing `일정 등록 창 열기` accessible label and the schedule registration submit copy. Updated schedule regression coverage to prevent reverting to the old shorthand visible copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-872 | `[hanwoo-dashboard]` Inventory add-form open visible copy specificity. Changed the Inventory tab add-form toggle's closed-state visible copy from `+재고 등록` to `재고 등록`, keeping the visible CTA aligned with the existing `재고 등록 창 열기` accessible label and the Inventory empty-state CTA. Updated empty-state regression coverage to prevent reverting to the old shorthand visible copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-871 | `[hanwoo-dashboard]` Sales add-form open visible copy specificity. Changed the Sales tab add-form toggle's closed-state visible copy from `+매출 등록` to `판매 기록 등록`, keeping the visible CTA aligned with the existing `판매 기록 등록 창 열기` accessible label and the Sales empty-state CTA. Updated home-market regression coverage to prevent reverting to the old shorthand visible copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-870 | `[hanwoo-dashboard]` Sales empty-state action visible copy specificity. Changed the Sales tab empty-state action label from terse `매출 기록` to `판매 기록 등록`, keeping the no-sales empty-state CTA aligned with the surrounding sales registration copy and making the operator task explicit before opening the add-sale form. Updated empty-state and home-market regression coverage to prevent reverting to the old short action copy. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-869 | `[hanwoo-dashboard]` Cattle detail edit/archive visible copy specificity. Changed the cattle detail modal edit and archive action buttons' idle visible copy from generic `수정` / `보관` to `개체 정보 수정` / `개체 보관 처리`, keeping the visible actions aligned with the existing Korean accessible labels so the selected-cattle edit and archive tasks are named clearly before and during async detail operations. Updated cattle detail regression coverage to prevent reverting to the old generic action copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-868 | `[hanwoo-dashboard]` Login submit pending visible copy specificity. Changed the login form submit button's pending visible copy from generic `확인 중...` to `로그인 확인 중...`, keeping it aligned with the existing Korean `loginSubmitLabel` so the primary login action names the in-progress authentication task clearly on screen. Updated login regression coverage to prevent reverting to the old generic pending copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-867 | `[hanwoo-dashboard]` Dashboard cattle archive and move confirmation copy specificity. Changed the dashboard cattle archive confirmation buttons from generic `보관 처리` / `취소` to `개체 보관 처리` / `개체 보관 취소`, and changed cattle move confirmation buttons from generic `이동` / `취소` to `개체 이동` / `개체 이동 취소`. The confirmation actions now name the cattle archive/move tasks consistently with the surrounding Korean operator copy. Updated dashboard regression coverage to prevent reverting to the old generic confirmation labels. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-866 | `[hanwoo-dashboard]` Settings building delete visible and confirmation copy specificity. Changed the Settings tab building delete row action's idle visible copy from generic `삭제` to `축사 삭제`, and changed the destructive confirmation actions from generic `삭제` / `취소` to `축사 삭제` / `축사 삭제 취소`. The visible and confirmation actions now name the building-delete task consistently with the existing Korean accessible label/title before and during async deletion. Updated Settings regression coverage to prevent reverting to the old generic delete/cancel copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-865 | `[hanwoo-dashboard]` Settings building add-form cancel visible copy specificity. Changed the Settings tab building add/cancel toggle's idle visible copy from generic `취소` to `축사 등록 취소`, matching the existing Korean accessible label/title so the visible secondary action names the building-registration cancellation task before and during async submission. Updated Settings regression coverage to prevent reverting to the old generic cancel copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-864 | `[hanwoo-dashboard]` Schedule add-form cancel visible copy specificity. Changed the Schedule tab add/cancel toggle's idle visible copy from generic `취소` to `일정 등록 취소`, matching the existing Korean accessible label/title so the visible secondary action names the schedule-registration cancellation task before and during async submission. Updated Schedule regression coverage to prevent reverting to the old generic cancel copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-863 | `[hanwoo-dashboard]` Sales add-form cancel visible copy specificity. Changed the Sales tab add/cancel toggle's idle visible copy from generic `취소` to `판매 기록 등록 취소`, matching the existing Korean accessible label/title so the visible secondary action names the sales-record registration cancellation task before and during async submission. Updated Sales regression coverage to prevent reverting to the old generic cancel copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-862 | `[hanwoo-dashboard]` Inventory add-form cancel visible copy specificity. Changed the Inventory tab add/cancel toggle's idle visible copy from generic `취소` to `재고 등록 취소`, matching the existing Korean accessible label/title so the visible secondary action names the inventory-registration cancellation task before and during async submission. Updated inventory regression coverage to prevent reverting to the old generic cancel copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-861 | `[hanwoo-dashboard]` Cattle detail breeding cancel visible copy specificity. Changed the cattle detail breeding form cancel button's idle visible copy from generic `취소` to `번식 기록 취소`, matching the existing Korean accessible label/title so the visible secondary action names the breeding-record cancellation task before and during async submission. Updated cattle detail regression coverage to prevent reverting to the old generic cancel copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-860 | `[hanwoo-dashboard]` Calving cancel visible copy specificity. Changed the calving form cancel button's idle visible copy from generic `취소` to `분만 기록 취소`, matching the existing Korean accessible label/title so the visible secondary action names the calving-record cancellation task before and during async submission. Updated calving regression coverage to prevent reverting to the old generic cancel copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-859 | `[hanwoo-dashboard]` Cattle form cancel visible copy specificity. Changed the cattle form cancel button's idle visible copy from generic `취소` to `개체 저장 취소`, matching the existing Korean accessible label/title so the visible secondary action names the cattle-save cancellation task before and during async submission. Updated cattle form regression coverage to prevent reverting to the old generic cancel copy. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-858 | `[hanwoo-dashboard]` Feed submit visible copy regression specificity. Refactored the Feed tab submit button's visible save copy into `submitButtonText`, keeping it aligned with the existing Korean `submitButtonLabel` so the primary feed-record submission action names the task consistently before and during async submission. Updated feed and shared submit-copy regression coverage to prevent reverting to the old inline pending-copy expression. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-857 | `[hanwoo-dashboard]` Calving submit visible copy regression specificity. Refactored the Calving tab submit button's visible save copy into `submitButtonText`, keeping it aligned with the existing Korean `submitButtonLabel` so the primary calving-record submission action names the task consistently before and during async submission. Updated calving and shared submit-copy regression coverage to prevent reverting to the old inline pending-copy expression. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-856 | `[hanwoo-dashboard]` Schedule submit visible copy regression specificity. Refactored the Schedule tab submit button's visible save copy into `submitButtonText`, keeping it aligned with the existing Korean `submitButtonLabel` so the primary schedule registration action names the task consistently before and during async submission. Updated schedule and shared submit-copy regression coverage to prevent reverting to the old inline pending-copy expression. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-855 | `[hanwoo-dashboard]` Inventory quantity save visible copy specificity. Changed the inline inventory quantity save button's idle visible copy from generic `저장` to `수량 저장`, matching the existing Korean accessible label/title so the row-level quantity update action names the quantity-save task clearly before and during async submission. Updated inventory regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-854 | `[hanwoo-dashboard]` Settings building submit visible copy specificity. Changed the Settings tab building submit button's idle visible copy from generic `등록하기` to `축사 등록하기`, matching the existing Korean accessible label/title so the primary building registration action names the building task clearly before and during async submission. Updated settings regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-853 | `[hanwoo-dashboard]` Settings farm submit visible copy specificity. Changed the Settings tab farm settings submit button's idle visible copy from generic `저장하기` to `농장 정보 저장하기`, matching the existing Korean accessible label/title so the primary farm-info save action names the farm settings task clearly before and during async submission. Updated settings regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-852 | `[hanwoo-dashboard]` Sales submit visible copy specificity. Changed the Sales tab submit button's idle visible copy from generic `등록하기` to `판매 기록 등록하기`, matching the existing Korean accessible label/title so the primary sales-record create action names the sales registration task clearly before and during async submission. Updated Sales and shared submit-copy regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-851 | `[hanwoo-dashboard]` Inventory submit visible copy specificity. Changed the Inventory tab submit button's idle visible copy from generic `등록하기` to `재고 등록하기`, matching the existing Korean accessible label/title so the primary inventory-create action names the inventory registration task clearly before and during async submission. Updated shared submit-copy regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-850 | `[hanwoo-dashboard]` Cattle form submit visible copy specificity. Changed the cattle form submit button's idle visible copy from generic `저장하기` to `개체 정보 저장`, matching the existing Korean accessible label/title so the primary save action names the cattle-info save task clearly before and during async submission. Updated cattle form and shared submit-copy regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-849 | `[hanwoo-dashboard]` Cattle detail breeding submit visible copy specificity. Changed the cattle detail breeding record submit button's idle visible copy from generic `저장` to `번식 기록 저장`, matching the existing Korean accessible label/title so the button names the breeding-record save task clearly before and during async submission. Updated cattle detail regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-848 | `[hanwoo-dashboard]` AI insight loading status visible copy specificity. Changed the AI insight widget loading status visible copy from generic `분석 중…` to `AI 인사이트 분석 중…`, matching the widget task and refresh-control context so the busy status names the in-progress AI-insight analysis clearly on screen. Updated AI insight regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-847 | `[hanwoo-dashboard]` Cattle pagination button visible copy specificity. Changed the home dashboard cattle load-more button's idle visible copy from generic `개체 더 보기` to `이전 개체 더 보기`, matching the cursor-pagination behavior and existing Korean loading label so the button names the previous cattle-page load task more clearly on screen. Updated cattle pagination regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-846 | `[hanwoo-dashboard]` Notification modal close disabled label specificity. Added a state-aware close button label for the notification modal so, while SMS test sending is in progress, the disabled close action exposes `문자 알림 테스트 전송 중에는 알림 센터를 닫을 수 없습니다` through both `aria-label` and `title` instead of continuing to advertise a closable action. Updated notification modal regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-845 | `[hanwoo-dashboard]` QR print busy visible copy specificity. Changed the QR label print button's visible busy text from generic `인쇄 준비 중...` to `QR 라벨 인쇄 준비 중...`, matching the existing Korean accessible label/title so the disabled print action names the in-progress QR-label print task clearly on screen. Updated QR widget regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-844 | `[hanwoo-dashboard]` Notification SMS test busy visible copy specificity. Changed the notification modal SMS test button's visible busy text from generic `전송 중...` to `문자 알림 테스트 전송 중...`, matching the existing Korean accessible label/title so the disabled SMS test action names the in-progress task clearly on screen. Updated notification modal regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-843 | `[hanwoo-dashboard]` Sales pagination button visible copy specificity. Changed the Sales tab load-more button's visible copy from generic `이전 기록 더 보기` / `불러오는 중...` to `이전 판매 기록 더 보기` / `이전 판매 기록 불러오는 중...`, matching the existing Korean accessible label/title so the button names the sales-history pagination task clearly on screen. Updated sales pagination regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-842 | `[hanwoo-dashboard]` Settings building delete busy visible copy specificity. Changed building delete row actions' visible busy text from generic `삭제 중...` to `축사 삭제 중...`, matching the existing Korean accessible label/title so the disabled delete button names the in-progress building-delete task clearly on screen. Updated settings regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-841 | `[hanwoo-dashboard]` Schedule completion toggle busy visible copy specificity. Changed upcoming schedule completion toggles' visible busy text from generic `변경 중...` to `일정 완료 상태 변경 중...`, matching the existing Korean accessible label/title so the disabled completion toggle names the in-progress schedule-completion update clearly on screen. Updated schedule regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-840 | `[hanwoo-dashboard]` Schedule add-form toggle busy visible copy specificity. Changed the Schedule tab add/cancel toggle's visible busy text from generic `저장 중...` to `일정 저장 중...`, matching the existing Korean accessible label/title so the disabled toggle names the in-progress schedule-save task clearly on screen. Updated schedule regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-839 | `[hanwoo-dashboard]` Sales add-form toggle busy visible copy specificity. Changed the Sales tab add/cancel toggle's visible busy text from generic `저장 중...` to `판매 기록 저장 중...`, matching the existing Korean accessible label/title so the disabled toggle names the in-progress sales-record save task clearly on screen. Updated sales regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-838 | `[hanwoo-dashboard]` Inventory add-form toggle busy visible copy specificity. Changed the Inventory tab add/cancel toggle's visible busy text from generic `저장 중...` to `재고 저장 중...`, matching the existing Korean accessible label/title so the disabled toggle names the in-progress inventory-save task clearly on screen. Updated inventory regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-837 | `[hanwoo-dashboard]` Feed filter chip busy visible copy specificity. Changed Feed tab building filter chips' visible busy text from generic `저장 중...` to `급여 기록 저장 중...`, matching the existing Korean accessible label/title so disabled filter chips name the in-progress feed-record save task clearly on screen. Updated Feed tab regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-836 | `[hanwoo-dashboard]` Cattle detail action busy visible copy specificity. Changed the cattle detail edit, archive, estrus, and pregnancy action buttons' visible busy text from generic `처리 중...` to `개체 처리 중...`, matching the existing Korean accessible label/title so disabled detail actions name the in-progress cattle operation clearly on screen. Updated cattle detail regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-835 | `[hanwoo-dashboard]` Cattle breeding record busy visible copy specificity. Changed the cattle detail breeding record cancel and submit buttons' visible busy text from generic `저장 중...` to `번식 기록 저장 중...`, matching the existing Korean accessible label/title so the in-progress state names the breeding-record save task clearly on screen. Updated cattle detail regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-834 | `[hanwoo-dashboard]` Cattle form cancel busy visible copy specificity. Changed the cattle form cancel button's visible busy text from generic `저장 중...` to `개체 저장 중...`, matching the existing Korean accessible label/title so the in-progress state names the cattle-save task clearly on screen. Updated cattle form regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-833 | `[hanwoo-dashboard]` Calving cancel busy visible copy specificity. Changed the calving form cancel button's visible busy text from generic `저장 중...` to `분만 기록 저장 중...`, matching the existing Korean accessible label/title so the in-progress state names the calving-save task clearly on screen. Updated calving form regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-832 | `[hanwoo-dashboard]` Settings building toggle busy visible copy specificity. Changed the building add/cancel toggle's visible busy text from generic `저장 중...` to `축사 저장 중...`, matching the existing Korean accessible label/title so the in-progress state names the building-save task clearly on screen. Updated settings building-form regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-831 | `[hanwoo-dashboard]` Settings building submit busy visible copy specificity. Changed the building registration submit button's visible text from static `등록하기` to state-aware `축사 등록 중...` while saving, matching the existing Korean accessible label/title so the in-progress state names the building-save task clearly on screen. Updated settings building-form regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-830 | `[hanwoo-dashboard]` Settings farm save busy visible copy specificity. Changed the farm settings submit button's visible busy text from generic `저장 중...` to `농장 정보 저장 중...`, matching the existing Korean accessible label/title so the in-progress state names the farm-settings save task clearly on screen. Updated settings farm-form regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-829 | `[hanwoo-dashboard]` Inventory quantity save busy visible copy specificity. Changed the inline inventory quantity save button's visible busy text from generic `저장 중...` to `수량 저장 중...`, matching the existing Korean accessible label/title so the in-progress state names the quantity-save task clearly on screen. Updated inventory quantity edit regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-828 | `[hanwoo-dashboard]` Cattle submit busy visible copy specificity. Changed the cattle form submit button's visible busy text from generic `저장 중...` to `개체 정보 저장 중...`, matching the existing Korean accessible label/title so the in-progress state names the cattle-save task clearly on screen. Updated cattle form and primary submit pending-copy regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-827 | `[hanwoo-dashboard]` Cattle tag lookup busy visible copy specificity. Changed the cattle form tag lookup button's visible busy text from generic `조회 중...` to `이력번호 조회 중...`, matching the existing Korean accessible label/title so the in-progress state names the lookup task clearly on screen. Updated cattle form regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-826 | `[hanwoo-dashboard]` Excel export busy visible copy specificity. Changed the Excel export button's visible busy text from generic `준비 중...` to `엑셀 준비 중...`, matching the existing Korean accessible label/title so the in-progress state names the export task clearly on screen. Updated Excel export button regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-825 | `[hanwoo-dashboard]` Subscription result loading atomic status. Added `aria-atomic="true"` to the subscription success and failure Suspense fallback statuses so Korean payment loading copy is announced as coherent polite busy statuses. Updated payment UX regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-824 | `[hanwoo-dashboard]` Diagnostics loading atomic status. Added `aria-atomic="true"` to the admin diagnostics page's system-check and raw-record loading statuses so Korean operations loading copy is announced as coherent polite busy statuses. Updated diagnostics regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-823 | `[hanwoo-dashboard]` Sales pagination error atomic status. Added `aria-atomic="true"` to the Sales tab pagination load-error status so Korean retry feedback for failed "이전 기록 더 보기" requests is announced as one coherent polite status. Updated sales pagination regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-822 | `[hanwoo-dashboard]` Cattle pagination error atomic status. Added `aria-atomic="true"` to the dashboard cattle pagination load-error status so Korean retry feedback for failed "개체 더 보기" requests is announced as one coherent polite status. Updated cattle pagination regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-821 | `[hanwoo-dashboard]` Dashboard full-list loading atomic status. Added `aria-atomic="true"` to the dashboard full cattle and sales ledger loading placeholders so the Korean `개체 목록을 불러오는 중입니다...` and `매출 기록을 불러오는 중입니다...` status messages are treated as coherent polite busy announcements. Updated home market regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-820 | `[hanwoo-dashboard]` Market price loading atomic status. Added `aria-atomic="true"` to the market price widget initial loading status so the Korean `한우 시세를 불러오는 중입니다.` announcement is treated as one coherent polite busy status. Updated home market regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-819 | `[hanwoo-dashboard]` Profitability loading atomic status. Added `aria-atomic="true"` to the profitability widget loading status so the Korean `출하 수익성 예측을 불러오는 중입니다.` announcement is treated as one coherent polite busy status. Updated profitability widget regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-818 | `[hanwoo-dashboard]` AI insight loading busy status. Added `aria-busy={isLoading}` to the AI insight widget header loading status so the Korean `분석 중...` announcement exposes the busy state consistently with the widget refresh button and insight list. Updated AI insight widget regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-817 | `[hanwoo-dashboard]` Weather unavailable atomic status. Added `aria-atomic="true"` to the weather widget unavailable status region so the Korean unavailable heading, message, and location are announced as one coherent polite status. Updated home market regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-816 | `[hanwoo-dashboard]` Market price unavailable status announcement. Added polite status semantics to the market price widget unavailable fallback so missing/degraded KAPE price data is announced as a coherent Korean status instead of visual-only text. Updated home market regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-815 | `[hanwoo-dashboard]` AI insight loading status semantics. Added explicit `role="status"` and `aria-atomic="true"` to the AI insight widget's header loading copy so the Korean `분석 중...` state is announced as a coherent polite status instead of only relying on `aria-live`. Updated AI insight widget regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-814 | `[hanwoo-dashboard]` Field Mode checklist progress hover summary. Added a shared Korean checklist progress label to the Field Mode daily-check progressbar and reused it for both `aria-valuetext` and hover `title`, so the current completed-check count is exposed consistently. Updated Field Mode regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-813 | `[hanwoo-dashboard]` Setup progress track hover summary. Added a `title` to the home setup progress bar using the existing Korean progress summary, so hover/title UI exposes the same current operating-readiness percentage and completed-step count as `aria-valuetext`. Updated home dashboard regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-812 | `[hanwoo-dashboard]` Shared dialog close label specificity. Added a contextual `closeLabel` prop to the shared dialog content close control, using it for `aria-label`, hover `title`, and screen-reader copy. Applied `확인 창 닫기` to the feedback confirmation dialog while keeping a Korean default for other dialogs. Updated dialog regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-811 | `[hanwoo-dashboard]` Field Mode search input title copy. Added matching hover/title copy to the Field Mode search input so its visible tooltip aligns with the existing Korean accessible name for searching by cattle name or ear-tag number. Updated Field Mode regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-810 | `[hanwoo-dashboard]` Field Mode search placeholder Korean polish. Updated the Field Mode search input placeholder from `이표번호 4자리 또는 소이름...` to `이표번호 4자리 또는 소 이름 입력`, fixing Korean spacing and replacing trailing punctuation with action-oriented input copy. Updated Field Mode regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-809 | `[hanwoo-dashboard]` AI chat streaming placeholder Korean status. Replaced the AI chat streaming fallback from punctuation-only `...` with the Korean `답변 생성 중입니다...` constant so an empty in-progress assistant message exposes meaningful status copy. Updated AI chat widget regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-808 | `[hanwoo-dashboard]` AI chat input busy-state label. Added a state-aware `inputLabel` to the AI chat question input so, while an answer is streaming and input is disabled, its accessible label and hover title expose that questions cannot be entered yet instead of keeping the idle question label. Updated AI chat widget regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-807 | `[hanwoo-dashboard]` AI chat close action context label. Changed the AI chat panel close button from generic `채팅 닫기` to `AI 농장 비서 닫기` for both accessible label and hover title, matching the launcher/dialog naming and making the close action explicit. Updated AI chat widget regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-806 | `[hanwoo-dashboard]` Login demo credential label Korean polish. Replaced the demo account box's remaining English `ID`/`PW` labels with Korean `아이디`/`비밀번호`, keeping the login screen Korean-first while preserving the actual demo credentials. Updated login/error-page regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-805 | `[hanwoo-dashboard]` Login demo account Korean copy polish. Removed the English `Demo Accounts` parenthetical from the login page demo account box and marked the lightbulb glyph as decorative, keeping the login screen Korean-first and reducing duplicate decorative output. Updated login/error-page regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-804 | `[hanwoo-dashboard]` Profitability error alert semantics. Added `role="alert"` to the profitability widget error card so shipment-profitability failures are announced as urgent feedback instead of visual-only text, while preserving the original Korean error copy. Updated profitability widget regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-803 | `[hanwoo-dashboard]` Weather unavailable status announcement. Added `role="status"` and polite live-region semantics to the weather unavailable card so degraded/no-weather states are announced like other dashboard loading/error states instead of remaining visual-only text. Updated dashboard weather regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-802 | `[hanwoo-dashboard]` Weather forecast icon semantic title. Added an explicit `role="img"` and matching hover `title` to 3-day forecast weather icons, reusing the normalized Korean weather description instead of leaving emoji-only forecast symbols with only an aria label. Updated dashboard weather regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-801 | `[hanwoo-dashboard]` Cattle tag lookup visible copy consistency. Unified the cattle form tag lookup button's visible idle copy with its accessible label/title by changing the on-screen action from `태그 조회` to `이력번호 조회`, while preserving `조회 중...` during lookup. Updated cattle form regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-800 | `[hanwoo-dashboard]` Schedule completion toggle progress copy. Added state-aware labels and visible status copy to upcoming schedule completion toggles so the row shows `변경 중...` and exposes `일정 완료 상태 변경 중` while an async completion update is in flight instead of leaving static completion copy. Updated Schedule and dashboard regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-799 | `[hanwoo-dashboard]` Feed filter chip visible progress copy. Added state-aware visible copy to Feed tab building filter chips so disabled chips show `저장 중...` while a feed record save is in flight instead of leaving static filter names, matching their busy accessible labels and titles. Updated Feed tab regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-798 | `[hanwoo-dashboard]` Settings save controls visible progress copy. Added state-aware visible copy to the Settings tab farm save button and building add/cancel toggle so they show `저장 중...` while their async save flows are in flight instead of leaving static `저장하기`/`취소` copy, matching their busy accessible labels and titles. Updated Settings tab regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-797 | `[hanwoo-dashboard]` Schedule add-form toggle visible progress copy. Added state-aware visible copy to the Schedule tab add/cancel toggle so it shows `저장 중...` while a schedule save is in flight instead of leaving the disabled toggle on static `취소`, matching its busy accessible label and title. Updated Schedule tab regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-796 | `[hanwoo-dashboard]` Sales add-form toggle visible progress copy. Added state-aware visible copy to the Sales tab add/cancel toggle so it shows `저장 중...` while a sales record save is in flight instead of leaving the disabled toggle on static `취소`, matching its busy accessible label and title. Updated Sales tab regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-795 | `[hanwoo-dashboard]` Inventory add-form toggle visible progress copy. Added state-aware visible copy to the Inventory tab add/cancel toggle so it shows `저장 중...` while an inventory item save is in flight instead of leaving the disabled toggle on static `취소`, matching its busy accessible label and title. Updated inventory regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-27 |
| T-794 | `[hanwoo-dashboard]` Calving form cancel visible progress copy. Added state-aware visible copy to the calving form cancel button so it shows `저장 중...` while a calving record save is in flight instead of leaving the disabled action on static `취소`, matching its busy accessible label and title. Updated calving regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-793 | `[hanwoo-dashboard]` Cattle form cancel visible progress copy. Added state-aware visible copy to the cattle form bottom cancel button so it shows `저장 중...` while an async cattle save is in flight instead of leaving the disabled action on static `취소`, matching its busy accessible label and title. Updated cattle form regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-792 | `[hanwoo-dashboard]` Cattle detail breeding cancel visible progress copy. Added state-aware visible copy to the cattle detail breeding form cancel button so it shows `저장 중...` while a breeding record save is in flight instead of leaving the disabled action on static `취소`, matching its busy accessible label and title. Updated cattle detail regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-791 | `[hanwoo-dashboard]` Cattle detail breeding action visible progress copy. Added state-aware visible copy to the cattle detail breeding quick actions so 발정 기록 and 수정 기록 buttons show `처리 중...` while archive/delete or breeding-save work blocks the detail modal, matching their existing busy accessible labels and titles. Updated cattle detail regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-790 | `[hanwoo-dashboard]` Cattle detail edit visible progress copy. Added state-aware visible copy to the cattle detail edit button so archive/delete or breeding-save work in flight shows `처리 중...` instead of leaving the disabled action on static `수정` text, matching the existing busy accessible label. Updated cattle detail regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-789 | `[hanwoo-dashboard]` Cattle detail close busy label. Added a state-aware `closeButtonLabel` to the cattle detail modal close button so archive/delete or breeding-save work in flight exposes that the detail window cannot be closed yet, instead of leaving the icon-only control on static close copy. Updated cattle detail regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-788 | `[hanwoo-dashboard]` Cattle form top cancel busy label. Reused the state-aware `cancelButtonLabel` on the cattle edit form's top icon-only back/cancel button so saving in flight exposes `개체 저장 중에는 취소할 수 없습니다` through both `aria-label` and `title`, matching the bottom cancel action. Updated cattle form regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-787 | `[hanwoo-dashboard]` Cattle detail archive visible progress copy. Added state-aware visible copy to the cattle detail archive button so the destructive row action shows `처리 중...` while archive/delete or breeding-save work is blocking the detail modal, matching the existing busy state and Korean accessible labels. Updated cattle detail regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-786 | `[hanwoo-dashboard]` Settings building delete progress labels. Added state-aware item-specific labels to building delete buttons so a deletion in flight exposes `동 삭제 중` through `aria-label`, `title`, `aria-busy`, and visible `삭제 중...` copy instead of leaving the destructive action static. Updated settings regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-785 | `[hanwoo-dashboard]` Inventory inline quantity saving state labels. Added an item-specific saving-state label for inline inventory quantity updates so the input, save button `aria-label`, `title`, busy state, and visible text all switch to clear Korean progress copy while a row save is in flight. Updated inventory regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-784 | `[hanwoo-dashboard]` Analysis tab chart accessible summaries. Added Korean accessible chart summaries to the Analysis tab monthly flow bar chart and cost structure pie chart with `role="img"`, `aria-label`, and matching `title`, so both Recharts visuals communicate their purpose to assistive technology and hover/title UI. Updated analysis regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-783 | `[hanwoo-dashboard]` Cattle detail weight chart accessible summary. Added a Korean accessible chart summary to the cattle detail modal weight trend chart with `role="img"`, `aria-label`, and matching `title`, so the Recharts line chart communicates that it compares the selected cattle's weight records by date. Updated cattle detail regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-782 | `[hanwoo-dashboard]` Sales profit chart accessible summary. Added a Korean accessible chart summary to the Sales tab recent profit analysis chart with `role="img"`, `aria-label`, and matching `title`, so the Recharts bar chart communicates that it compares sale amount and profit by shipped cattle. Updated Sales tab regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-781 | `[hanwoo-dashboard]` Financial chart accessible summary. Added a Korean accessible chart summary to the farm financial flow chart with `role="img"`, `aria-label`, and matching `title`, so the Recharts bar chart communicates that it compares monthly sales, expenses, and profit over the recent six-month window. Updated financial chart regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-780 | `[hanwoo-dashboard]` Feed trend chart accessible summary. Added a Korean accessible chart summary to the Feed tab trend chart container with `role="img"`, `aria-label`, and matching `title`, so the Recharts visual communicates its purpose to assistive technology and hover/title UI. Updated Feed tab regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-779 | `[hanwoo-dashboard]` Shared decorative icon hiding. Marked shared dialog/select glyphs, schedule month arrows, the Field Mode no-result warning glyph, and the empty-pen cow emoji as decorative with `aria-hidden`, reducing duplicate visual-only output for assistive technology. Updated regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-778 | `[hanwoo-dashboard]` Secondary link action labels. Added explicit Korean `aria-label` and `title` copy to dashboard footer links and the settings diagnostics link, and added regression coverage so secondary navigation exposes clear action names. Verified focused tests, full Hanwoo tests, eslint, Next production build, and the anchor scan. | Codex | 2026-05-26 |
| T-777 | `[hanwoo-dashboard]` QR print status feedback. Added Korean QR print preparation, popup-blocked failure, and completion feedback through a polite status region so print-window failures are visible instead of silently resetting the button. Updated QR widget regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-776 | `[hanwoo-dashboard]` Unit-test warning suppression. Updated the Hanwoo `test` script to suppress Node's `MODULE_TYPELESS_PACKAGE_JSON` warning during routine `npm.cmd test` runs without switching package module semantics, and added package-script regression coverage. Verified focused test, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-775 | `[hanwoo-dashboard]` Shared empty-state CTA busy state. Wired the shared `EmptyState` CTA `disabled` prop into `aria-busy`, so disabled empty-state actions such as the sales tab's missing-cattle CTA expose their unavailable/busy state consistently with other action buttons. Updated empty-state regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-774 | `[hanwoo-dashboard]` Empty building CTA action label. Added explicit Korean `aria-label` and `title` copy to the home dashboard empty building CTA, so the button states that it opens settings to add the first building instead of relying only on the visible card copy. Updated home-market regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-773 | `[hanwoo-dashboard]` Full-list retry action labels. Added target-specific Korean `aria-label` and `title` copy to the full cattle-list and sales-ledger retry buttons shown after dashboard preload failures, so each retry action identifies whether it reloads the complete cattle list or sales records. Updated home-market regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-772 | `[hanwoo-dashboard]` Notification SMS test visible busy copy. Made the notification modal SMS test button show `전송 중...` while `isTestingSMS` is true instead of leaving the visible label static, aligning the on-screen state with the existing disabled, busy, accessible-label, and title states. Updated notification-modal regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-771 | `[hanwoo-dashboard]` Notification SMS test action accessibility. Added a state-aware `smsTestButtonLabel` to the notification modal SMS test button and reused it for `aria-label` and `title`, so idle and sending states expose the same Korean action name to assistive technology and hover/title UI. Updated notification-modal regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-770 | `[hanwoo-dashboard]` Shared empty-state CTA accessibility. Reused `actionLabel` as both `aria-label` and `title` on the shared `EmptyState` CTA button so inventory/sales/schedule empty-state actions expose the same Korean task name to assistive technology and hover/title UI. Updated empty-state regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-769 | `[hanwoo-dashboard]` Not-found and route-error home-link accessibility. Added explicit Korean `aria-label` and `title` copy to the not-found page and route error page dashboard-return links so home navigation actions are clear to assistive technology and hover/title UI. Updated error-page regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-768 | `[hanwoo-dashboard]` Legal document home-link accessibility. Added explicit Korean `aria-label` and `title` copy to the shared legal document home-return link and marked its arrow icon decorative so privacy/terms pages expose a clear home navigation action without duplicate icon semantics. Updated legal-page regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-767 | `[hanwoo-dashboard]` Route and global error retry action accessibility. Added shared reset labels to route-level and global error retry buttons and reused them for visible text, `aria-label`, and `title`, so recovery actions are explicit in assistive technology and hover/title UI. Updated error-page regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-766 | `[hanwoo-dashboard]` Subscription failure retry action accessibility. Added explicit Korean `aria-label` and `title` copy to the subscription failure page retry/back button so the control clearly communicates that it returns to the payment screen to retry. Updated payment UX regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-765 | `[hanwoo-dashboard]` Admin diagnostics return action accessibility. Added explicit Korean `aria-label` and `title` copy to the diagnostics page return-to-dashboard button and marked its arrow icon decorative so the navigation action is clear without duplicate icon semantics. Updated diagnostics regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-764 | `[hanwoo-dashboard]` Tab navigation action labels. Added a shared `tabActionLabel` for dashboard tab navigation buttons and reused it for `aria-label` and `title`, including the active-tab state in the label so each tab button exposes a clear Korean navigation action and current selection. Updated home-market regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-763 | `[hanwoo-dashboard]` Home building card action label context. Added a shared `buildingCardLabel` for home building card buttons and reused it for `aria-label` and `title`, so each building card exposes building name, pen count, and current headcount as an explicit navigation action. Updated home regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-762 | `[hanwoo-dashboard]` Notification item read-action accessibility. Allowed shared `DropdownMenuItem` to forward additional props and added item-specific Korean `aria-label`/`title` copy to notification dropdown items, so each clickable notification clearly states it marks that notification read. Updated notification-system regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-761 | `[hanwoo-dashboard]` Notification mark-all action accessibility. Added a shared unread-count-aware `markAllAsReadLabel` for the notification dropdown's mark-all button and reused it for `aria-label` and `title`, so the action clearly states how many unread notifications will be marked read. Updated notification-system regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-760 | `[hanwoo-dashboard]` Pen card hover/title context. Reused a shared pen-card accessible label for both `aria-label` and `title` so pen cards expose the same pen number, cattle count, and estrus-alert context to assistive technology and hover/title UI. Updated card accessibility regression coverage. Verified focused card tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-759 | `[hanwoo-dashboard]` Field Mode and AI launcher title specificity. Matched the dashboard Field Mode activation button and floating AI assistant launcher `title` copy to their existing Korean `aria-label` action names so hover/title UI includes the same activate/open action semantics. Updated home and AI chat regression coverage. Verified focused tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-758 | `[hanwoo-dashboard]` Dashboard header icon action title specificity. Matched the notification center and cattle-registration header icon button `title` copy to their existing Korean `aria-label` action names so hover/title UI also says the target action opens. Updated home-market regression coverage. Verified focused home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-757 | `[hanwoo-dashboard]` Feedback toast dismiss title accessibility. Reused the toast-specific Korean dismiss label for both `aria-label` and `title` so toast close controls expose the same contextual copy to assistive technology and hover/title UI. Updated regression coverage. Verified focused feedback-provider tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-756 | `[hanwoo-dashboard]` Cattle row hover/title context. Reused the existing cattle row accessible label as the button `title` so hover/title text includes the same cow detail action and alert summary as assistive technology. Updated regression coverage. Verified focused card accessibility tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-755 | `[hanwoo-dashboard]` Field Mode checklist toggle action labels. Added action-oriented checklist item labels so each field-mode checklist button exposes item title, current completion state, and that activating it changes the completion state. Updated regression coverage. Verified focused Field Mode tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-754 | `[hanwoo-dashboard]` Ear-tag scanner manual choice title specificity. Reused the manual scan-result choice label for both `aria-label` and `title` so hover/title text includes the target cow name and ear-tag suffix. Updated regression coverage. Verified focused scanner modal tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-753 | `[hanwoo-dashboard]` QR print title specificity. Reused the label-specific QR print button copy for both `aria-label` and `title` so hover/title text identifies the target label while printing and idle states remain clear. Updated regression coverage. Verified focused QR widget tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-752 | `[hanwoo-dashboard]` Sales add-form toggle accessibility. Added state-aware accessible and hover/title copy plus busy state to the sales record add/cancel toggle so operators and assistive technology receive clear feedback while a sale save is in flight. Updated regression coverage. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-751 | `[hanwoo-dashboard]` Calving card open-action accessibility. Added cow-specific accessible and hover/title copy to each calving-list open action so operators and assistive technology can identify which cow's calving form will open. Updated regression coverage. Verified focused calving tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-750 | `[hanwoo-dashboard]` Estrus alert icon wrapper decorative accessibility. Marked the estrus alert heart icon wrapper as decorative so the visual alert glyph is hidden at the same wrapper level as the calving alert icon. Updated regression coverage. Verified focused alert banner tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-749 | `[hanwoo-dashboard]` Field Mode cattle result action accessibility. Added item-specific accessible and hover/title copy to Field Mode cattle search result buttons so each result clearly opens the target cattle detail by name and ear-tag number. Updated regression coverage. Verified focused Field Mode tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-748 | `[hanwoo-dashboard]` Inventory inline quantity action title accessibility. Added matching title copy to inline inventory quantity save/edit actions so their item-specific accessible labels are also available as hover/title text. Updated regression coverage. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-747 | `[hanwoo-dashboard]` Login password toggle title accessibility. Added a shared password visibility toggle label so the login password show/hide icon button exposes matching accessible and hover/title copy. Updated regression coverage. Verified focused error-page tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-746 | `[hanwoo-dashboard]` Setup progress score value text accessibility. Added a shared setup progress label for the score badge and progressbar so the visual count, accessible score label, hover title, and progressbar value text all include percent plus completed/total context. Updated regression coverage. Verified focused home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-745 | `[hanwoo-dashboard]` Setup progress item title accessibility. Reused the existing consolidated setup item label as hover/title copy so each setup progress button exposes matching visual and assistive action text. Updated regression coverage. Verified focused home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-744 | `[hanwoo-dashboard]` Quick Action button accessible labels. Added consolidated accessible labels and titles to Quick Action buttons so each one-tap action exposes its label and detail as one clear action name. Updated regression coverage. Verified focused home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-743 | `[hanwoo-dashboard]` Today Focus action accessible labels. Added consolidated accessible labels and titles to Today Focus action buttons so each task button exposes its title, detail, and meta context as one clear action name. Updated regression coverage. Verified focused home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-742 | `[hanwoo-dashboard]` Home building section decorative icon accessibility. Hid the decorative farm icons in the home building section header and empty-state CTA from assistive technology so the heading and CTA text carry the accessible meaning without duplicate emoji output. Updated regression coverage. Verified focused home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-741 | `[hanwoo-dashboard]` Cattle detail section heading semantics. Exposed the cattle detail modal `SectionTitle` blocks as level-3 headings so assistive technology can navigate the modal's basic information, breeding, weight, timeline, memo, and QR sections. Updated regression coverage. Verified focused cattle detail tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-740 | `[hanwoo-dashboard]` Shared HeartIcon decorative accessibility. Marked the shared HeartIcon SVG as decorative and unfocusable so alert/status glyphs do not expose duplicate semantics to assistive technology. Updated regression coverage. Verified focused alert banner tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-739 | `[hanwoo-dashboard]` Shared form action icon accessibility. Marked shared Back/Edit/Trash SVG action icons as decorative and unfocusable so labeled form and modal buttons do not expose duplicate icon semantics to assistive technology. Updated regression coverage. Verified focused cattle form/detail tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-738 | `[hanwoo-dashboard]` Field Mode control title and icon accessibility. Added matching title copy to Field Mode return, clear-search, and checklist toggle controls, and hid the decorative return arrow icon from assistive technology. Updated regression coverage. Verified focused Field Mode tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-737 | `[hanwoo-dashboard]` AI chat close-button title accessibility. Added a matching Korean `title` to the AI chat dialog close button so its hover/title copy aligns with the existing accessible label. Updated regression coverage. Verified focused AI chat tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-736 | `[hanwoo-dashboard]` Ear-tag scanner header close-label accessibility. Made the scanner modal header close button use the same contextual Korean accessible label as its title instead of generic close copy. Updated regression coverage. Verified focused scanner tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-735 | `[hanwoo-dashboard]` Form add/cancel toggle busy-state accessibility. Added state-aware accessible labels, titles, and busy state to inventory, schedule, and building add/cancel form toggles while their save flows are in flight. Updated regression coverage. Verified focused tab tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-734 | `[hanwoo-dashboard]` Dashboard full-list loading placeholder accessibility. Made the dashboard's complete cattle-list and sales-ledger loading placeholders polite status regions with loading-flag based `aria-busy` state so assistive technology can perceive full-list preparation/loading states. Updated regression coverage. Verified focused home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-733 | `[hanwoo-dashboard]` AI chat conversation busy-state accessibility. Added `aria-busy={isStreaming}` to the AI chat message log so assistive technology can perceive answer generation state on the live conversation region. Updated regression coverage. Verified focused AI chat tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-732 | `[hanwoo-dashboard]` Cattle tag lookup button state labels. Added state-aware `aria-label` and `title` copy to the cattle form tag lookup button so lookup progress is exposed consistently with the visible busy state. Updated regression coverage. Verified focused cattle form/detail tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-731 | `[hanwoo-dashboard]` Payment confirmation status announcement. Added polite atomic live semantics to the subscription success page's dynamic payment confirmation heading so retry/error status changes are announced. Updated regression coverage. Verified focused payment UX tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-730 | `[hanwoo-dashboard]` Payment checkout button busy-state accessibility. Unified the payment checkout button's disabled, visual waiting, and `aria-busy` state so assistive technology sees both widget-loading and submit-in-flight states as busy. Updated regression coverage. Verified focused payment UX tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-729 | `[hanwoo-dashboard]` Subscription result fallback loading accessibility. Made the subscription success/fail Suspense fallback loading messages polite busy status regions so assistive technology announces payment result loading states. Updated regression coverage. Verified focused payment UX tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-728 | `[hanwoo-dashboard]` Market price loading busy-state accessibility. Added `aria-busy="true"` to the market price widget's initial loading status region so assistive technology receives a complete busy status contract. Updated regression coverage. Verified focused home-market tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-727 | `[hanwoo-dashboard]` Admin diagnostics loading status accessibility. Made the admin diagnostics initial loading card and raw-record loading placeholder polite busy status regions so assistive technology announces both loading states. Added regression coverage. Verified focused diagnostics tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-726 | `[hanwoo-dashboard]` Profitability loading status accessibility. Made the profitability recommendation widget loading skeleton a polite status region with busy state and screen-reader-only Korean loading copy. Added regression coverage. Verified focused profitability tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-725 | `[hanwoo-dashboard]` Setup progress item state accessibility. Added accessible names to home setup progress items so each item includes its title, completion state, and detail while keeping visual state icons decorative. Added regression coverage. Verified focused home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-724 | `[hanwoo-dashboard]` Field Mode loading status accessibility. Made the Field Mode search-result full-list loading badge a polite status announcement and hid the camera icon inside the labeled scanner button as decorative. Added regression coverage. Verified focused Field Mode tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-723 | `[hanwoo-dashboard]` Setup progress semantic accessibility. Exposed the home onboarding/setup progress track as an ARIA `progressbar` with a Korean label and bounded value attributes instead of hiding the visual track from assistive technology. Added regression coverage. Verified focused home tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-722 | `[hanwoo-dashboard]` Notification center trigger dialog relationship accessibility. Added a stable notification dialog id, wired the home notification trigger with `aria-haspopup="dialog"`, stateful `aria-expanded`, and `aria-controls`, and rendered the same id on `NotificationModal`. Added regression coverage. Verified focused home/notification tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-721 | `[hanwoo-dashboard]` AI chat launcher relationship accessibility. Added `aria-haspopup="dialog"`, `aria-expanded="false"`, and `aria-controls` to the floating AI assistant launcher, backed by a stable chat panel id on the opened dialog. Added regression coverage. Verified focused AI chat widget tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-720 | `[hanwoo-dashboard]` Notification modal close-label accessibility. Made the notification modal close control contextual by exposing `알림 센터 닫기` for both `aria-label` and `title` instead of generic `닫기`, while preserving the SMS-busy dismissal guard. Added regression coverage. Verified focused notification-modal tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-719 | `[hanwoo-dashboard]` Feedback confirmation dialog accessibility. Hardened the shared `FeedbackProvider` confirmation dialog so cancel/confirm actions expose stable Korean `aria-label` and `title` values derived from the configured action labels. Added regression coverage. Verified focused feedback-provider tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-718 | `[hanwoo-dashboard]` Dashboard runtime ErrorBoundary recovery accessibility. Hardened the premium dashboard `ErrorBoundary` fallback by hiding the decorative warning icon from assistive tech and giving the recovery action an explicit non-submit button type plus stable Korean `aria-label` and `title`. Added regression coverage. Verified focused error-page tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-717 | `[hanwoo-dashboard]` Ear-tag scanner Korean operator copy cleanup. Removed the visible English `(Click)` hint and fixed the scanner overlay action copy from typo-prone `감색` to `이표 자동 인식`, then added regression coverage to keep the action label Korean-only. Verified focused scanner tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-716 | `[hanwoo-dashboard]` AI insight live-region update announcement. Added polite live-region semantics to the AI insight card list so refreshed analysis cards are announced to assistive technology while preserving busy state. Added widget accessibility coverage. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-715 | `[hanwoo-dashboard]` AI insight stale-card reset on summary changes. Hardened `AIInsightWidget` so dashboard summary changes immediately reset visible cards to fresh heuristic insights for the new snapshot while the AI request is loading, preventing stale previous-farm cards from lingering. Added widget contract coverage. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-714 | `[hanwoo-dashboard]` AI insight fallback reason normalization. Hardened `AIInsightWidget` so heuristic API responses always show an operator-facing fallback reason even when the server omits one, while successful AI responses explicitly clear stale fallback copy. Added widget contract coverage. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-713 | `[hanwoo-dashboard]` AI insight client complete-card response guard. Reused `MAX_INSIGHTS` in `AIInsightWidget` so malformed or partial API payloads fall back to the deterministic 3-card heuristic output instead of rendering a short AI list. Added widget wiring coverage. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-712 | `[hanwoo-dashboard]` AI insight complete-card response contract. Tightened the AI insight API route so Gemini responses are accepted only when they produce all 3 insight cards promised by the UI; partial parsed responses now fall back to deterministic heuristic insights instead of showing a short AI list. Added route wiring coverage. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-711 | `[hanwoo-dashboard]` AI insight summary identity stabilization. Memoized the dashboard summary object passed into `AIInsightWidget` so unrelated dashboard re-renders do not retrigger `/api/ai/insight` calls or reset the widget. Added wiring coverage to prevent inline summary object regressions. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-710 | `[hanwoo-dashboard]` AI insight server timeout fallback control-flow fix. Moved the `InsightTimeoutError` handling from the authentication catch path into the Gemini generation catch path so slow Gemini calls return the intended timeout-specific heuristic reason and never reference `summary` before it is declared. Strengthened route wiring coverage. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-709 | `[hanwoo-dashboard]` AI insight data normalization hardening. Tightened farm summary normalization so malformed profitability rows no longer count as declining-margin risks and negative monthly sales input is clamped before operator-facing heuristic copy. Added regression coverage. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-708 | `[hanwoo-dashboard]` AI insight API Gemini timeout fallback. Added a server-side 10s timeout wrapper around Gemini insight generation, clears timeout handles, returns deterministic heuristic insights with a timeout-specific Korean reason when Gemini is slow, and added route wiring coverage. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-707 | `[hanwoo-dashboard]` AI insight timeout resilience. Bounded the AI insight client request at 12 seconds, separated timeout aborts from unmount aborts, clears timers on completion/cleanup, falls back to deterministic insights on slow responses, and announces fallback reasons with a polite status region. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-706 | `[hanwoo-dashboard]` AI insight widget refresh and accessibility polish. Added a manual refresh control to the AI insight widget so operators can retry analysis without waiting for unrelated dashboard state changes, with busy-safe disabled state, `aria-busy`, Korean accessible labels, title text, and decorative refresh icon hiding. Added regression coverage. Verified focused AI insight tests, full Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-705 | `[shorts-maker-v2/ci]` Active project matrix failure stabilization. Confirmed CI run `26443385538` failed on duplicate pnpm version config and Linux missing-font safe-zone QC behavior. Current workflow already removes `pnpm/action-setup` `version: 9`; updated `caption_pillow._load_font` to use a size-aware Pillow default font when configured font files are unavailable, and added missing-font safe-zone regression coverage. Verified CI-equivalent shorts-maker command: `1559 passed, 12 skipped`. | Codex | 2026-05-26 |
| T-704 | `[hanwoo-dashboard]` Ear-tag scanner modal accessibility and QC stabilization. Added dialog description wiring, hid visual-only scanner canvas/icons, labeled scan/retry/manual-select/detail actions, announced scan result states, added regression coverage, and fixed the current AIInsightWidget React lint failure by deferring effect loading-state updates. Verified 300/300 Hanwoo tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-410 | `[workspace]` Update and refresh all system/project-level agent skills. Manually updated 4 project-level locked skills (`accessibility`, `bash-defensive-patterns`, `find-skills`, `seo`) using raw node CLI execution to bypass Windows `spawnSync` shell-concat path space issues. Successfully updated `bash-defensive-patterns` to the latest upstream commit (hash updated in `skills-lock.json`). Cloned/copied all 9 latest `nature-*` academic, citation, polishing, and writing skills from the `nature-skills` submodule into `.agents/skills/` so they are natively available across all agent layers. | Gemini (Antigravity) | 2026-05-26 |
| T-409 | `[workspace]` 모노레포 전체 MCP 서버 통합 진단 및 헬스 체크 시스템 완료. 에이전트 환경의 Notion MCP 연동성 검사(Desk Joopark 계정 PASS), 윈도우 인코딩 오류(CP949) 방어력을 갖춘 `execution/mcp_diagnostic.py` 자동화 스크립트 신설 및 구문, 의존성 임포트, JSON-RPC Stdio Handshake를 통한 로컬 MCP 서버 6종(sqlite-multi, system-monitor, telegram, cloudinary, youtube-data, n8n-workflow)의 100% 정상 작동 검증 및 종합 진단 보고서(`docs/mcp_status_report.md`) 발급 완료. | Gemini (Antigravity) | 2026-05-26 |
| T-703 | `[hanwoo-dashboard]` Dashboard runtime resilience polish. Wired the existing premium `ErrorBoundary` around `DashboardClient` in `src/app/page.js` and added a regression test so client-render failures surface the recoverable fallback instead of blanking the dashboard. Verified 282/282 tests, eslint, and Next production build. | Codex | 2026-05-26 |
| T-702 | `[hanwoo-dashboard]` Field Mode accessibility polish. Added accessible labels for field-mode exit/search/clear controls, exposed checklist progress as ARIA progressbar, and added checklist toggle pressed/state labels without touching existing shorts-maker-v2 dirty files. | Codex | 2026-05-26 |
| T-408 | `[workspace/blind-to-x/shorts-maker-v2]` 모노레포 누적 5개 프로젝트 기술 부채(Technical Debt) 해결. .ai/ 문서들 다이어트 및 stale 로그 제거. llm_client.py 헬퍼 통합 중복 제거, shorts-maker-v2 text_engine 렌더 전략 분리 및 데모 프롬프트 JSON 외부화, blind-to-x Phase 2 품질 게이트(상투어 및 의미 중복 감지) 장착 및 best_of_n 연동. 모노레포 전체 유닛 테스트 100% 그린 패스 완료. | Gemini (Antigravity) | 2026-05-26 |

| T-372 | `[workspace]` Monorepo Migration Cleanup. Successfully resolved all pending monorepo alignment tasks: removed legacy lockfiles, restored Prisma postinstall client generation hooks, and ran Biome automatic lint auto-formatting sweeps (`biome check --write .`) in advisory mode at the root. Validated that all 1,598 project-wide unit tests are 100% green and Next.js static pages compile successfully. | Gemini (Antigravity) | 2026-05-22 |

