# TASKS - AI Kanban Board

> Read this file at session start and update it before ending the session.

## TODO

> Latest Shorts Analytics mobile output polish: 2026-06-08 T-1566 at local code commit `a8fc49c8`. `workspace/execution/pages/shorts_analytics.py` now uses Korean operator page title `쇼츠 성과 분석 - Joolife`, hides Plotly modebars with responsive chart config, and extends mobile 44px target CSS to Streamlit header/action links and anchor links. `workspace/tests/test_shorts_analytics.py` locks page config, Plotly config, no deprecated Plotly width API, and mobile touch-target selectors. Verified evidence: focused pytest (`15 passed`), Ruff check, Ruff format check, `py_compile`, diff-check, and Playwright mobile QA at `390x844` with Korean title/sections present, old visible `Shorts Analytics` absent, target count `7`, small targets `[]`, min target `44x44`, visible modebars `0`, body overflow `false`, runtime error text `false`, and console/page/network failures empty. Code-review gate WARN (`risk_score=0.30`) is covered by direct focused tests and browser QA. No related dashboard pytest, project QC, or A/B decision was claimed in this correction. No push and no T-251 retry.

> Latest launch-boundary refresh: 2026-06-08 after T-1565 context commits. Pre-context audit baseline `2528388c` was clean and graph-current; later relay/goal commits are documentation-only, so use live `python execution/session_orient.py --json` for exact current HEAD/ahead count. This supersedes older T-1565 relay snippets that still say focused Hub pytest `54 passed`, action controls `26`, or visible targets `30`: final Hub evidence is `57 passed`, code-review gate PASS (`risk_score=0.00`), and Playwright mobile QA at `390x844` with total targets `48`, app targets `44`, small targets `[]`, minimum app target `44x44`, body overflow `false`, and console/page/network failures empty. Launch audits: readiness score `96`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch manifest coverage complete; completion audit `11/15` with `4` blocked items. No adoptable local candidate remains. Remaining boundaries are explicit push/user push for current-head `root-quality-gate` and `active-project-matrix`, plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push and no T-251 retry.

> Latest Joolife Hub mobile nav-target hardening: 2026-06-08 T-1565 at local code commit `0757326c`. After T-1564 converted Hub into a Korean output-quality launcher, this follow-up extends the 44px mobile target contract to Streamlit sidebar navigation links and header action links. `workspace/execution/joolife_hub.py` now includes `a[data-testid="stSidebarNavLink"]` and `div[data-testid="stHeaderActionElements"] a` in the mobile-safe selector set, and `workspace/tests/test_joolife_hub.py` locks those selectors. Verification passed focused Hub pytest (`54 passed`), Ruff check, Ruff format check, `py_compile`, diff-check, graph refresh, code-review gate PASS (`risk_score=0.00`), and Python Playwright mobile QA at `390x844`: title `작업 허브 - Joolife`, required Korean/output terms present, old launcher copy absent, action controls `26`, visible targets `30`, small targets `[]`, horizontal overflow `false`, console errors `[]`, page errors `[]`, failed requests `[]`. No push and no T-251 retry.

> Latest Joolife Hub output launcher polish: 2026-06-08 T-1564 at local code commit `9d9b53f8`. `workspace/execution/joolife_hub.py` now uses Korean operator-facing copy, reads `.tmp/project_qc_runner_latest.json`, surfaces current QC status/project PASS count/last evidence time, and renders launcher cards as output-quality entries with result type, use case, verification signal, and next action. It keeps Hanwoo T-251 live Supabase CRUD separated from local QC, uses stretch-width Korean launch/local links, and hardens mobile Streamlit button/link/header targets to 44px. `workspace/tests/test_joolife_hub.py` locks registry schema, old English launcher removal, QC snapshot helpers, output-quality card markdown, Korean launch/link behavior, and mobile selector contract. Verification passed focused pytest (`57 passed`), Ruff check, Ruff format check after hook feedback, `py_compile`, diff-check, code-review gate advisory WARN covered by focused/browser evidence, Python Playwright mobile QA at `390x844`: required Korean/output terms present, old launcher copy absent, latest QC count `4/4`, visible targets `29`, small targets `[]`, horizontal overflow `false`, runtime error text `false`, console warnings/errors `0`, and A/B `adopt_candidate` (`score_delta=2.0925925925925926`). No push and no T-251 retry.

> Latest QA/QC Status Dashboard output polish: 2026-06-08 T-1563 at local code commit `2a2052f0`. `workspace/execution/pages/qaqc_status.py` now reads the freshest local `.tmp/project_qc_runner_latest.json` before the Knowledge Dashboard authenticated `data/qaqc_result.json` and local `public` fallback, normalizes project-QC verdict/totals, warns on stale or incomplete QA/QC results, removes the `pandas` runtime dependency from trend rendering, keeps the `인프라 상태` section visible with an empty-state, and exposes a visible Korean `프로젝트별 다음 조치` summary instead of hiding operational action guidance inside collapsed expanders. `workspace/tests/test_qaqc_status_page.py` locks latest-vs-data-vs-public loading, stale/incomplete warnings, Korean copy, no old English copy, Streamlit width contracts, Plotly config, mobile 44px CSS including heading anchors, visible next-action output including Hanwoo T-251 separation, infrastructure empty-state, and history loading. Verification passed focused pytest (`10 passed`), Ruff check, Ruff format check, `py_compile`, diff-check, graph refresh, code-review gate advisory WARN covered by focused/browser evidence, and Python Playwright mobile QA at `390x844`: title `검증 현황 - Joolife`, required Korean sections present, old English copy absent, latest QC timestamp `2026-06-07 15:32:34`, total pass count `4084`, visible targets `4`, small targets `[]`, body overflow `false`, runtime error text `false`, console errors `[]`, page errors `[]`. No push and no T-251 retry.

> Latest Debt/Health Dashboard output polish: 2026-06-08 T-1562 at local code commit `de7db3ce`. `workspace/execution/pages/debt_dashboard.py` now uses Korean operator-facing copy, responsive no-modebar Plotly charts, 44px mobile Streamlit/header/selectbox/slider/expander controls, and localized TDR/cost/trend/debtor labels. `workspace/execution/pages/health_check_dashboard.py` now turns raw health-check output into an action-oriented Korean operations view with status labels, overall notice, priority action items, category summaries, and 44px mobile/header/selectbox controls. `workspace/tests/test_debt_dashboard_page.py` and `workspace/tests/test_health_check_dashboard_page.py` lock Plotly config, Korean copy, old English rejection, mobile CSS, action sorting, and next-action guidance. Verification passed focused pytest (`11 passed, 2 skipped` on rerun), Ruff check, Ruff format check, `py_compile`, diff-check, graph refresh, code-review gate advisory WARN covered by focused/browser evidence, and Playwright CLI mobile QA at `390x844` for both pages: required Korean sections present, old English/generic copy absent except `OK` inside `GITHUB_PERSONAL_ACCESS_TOKEN`, visible targets pass 44px, horizontal overflow `false`, runtime error text `false`, console errors `0`. No push and no T-251 retry.

> Latest API/Cost Monitor mobile output polish: 2026-06-08 T-1561 at local code commit `0f785024`. `workspace/execution/pages/api_monitor.py` now uses Korean operator-facing page metadata/title/caption/metrics/empty states, responsive no-modebar Plotly config, mobile 44px Streamlit/BaseWeb control CSS, and a project-root-aware blind-to-x CostDB loader. `workspace/execution/pages/cost_dashboard.py` now uses Korean cost-operations copy, copy-ready budget action guidance, per-call cost, responsive no-modebar Plotly config, and mobile 44px toolbar/slider CSS. `workspace/tests/test_api_monitor_page.py` and `workspace/tests/test_cost_dashboard.py` lock copy, source contracts, Plotly config, mobile CSS, and BTX CostDB import behavior. Verification passed focused pytest (`12 passed`), Ruff check, Ruff format check, `py_compile`, diff-check, graph refresh, code-review gate advisory WARN covered by focused/browser evidence, and Playwright CLI mobile QA at `390x844` for both pages: required Korean sections present, old English copy absent, visible targets min `44x44`, horizontal overflow `false`, runtime error text `false`, console errors `0`. No push and no T-251 retry.

> Latest Scheduler Dashboard mobile sizing hardening follow-up: 2026-06-08 T-1560. After T-1559, `workspace/execution/pages/scheduler_dashboard.py` now forces Streamlit text/number input inner wrappers and BaseWeb input/select inner wrappers to 44px height with `!important`, and keeps default Streamlit header menu/deploy buttons at 44px height/width. `workspace/tests/test_scheduler_dashboard_page.py` locks the stronger selectors and `!important` sizing contract. Verification passed scheduler dashboard pytest (`8 passed`), scheduler/Telegram focused pytest (`45 passed` before follow-up), Ruff check, Ruff format check, `py_compile`, diff-check, graph refresh, code-review gate advisory WARN covered by focused/browser evidence, and follow-up browser QA at `390x844`: title/H1/Korean sections present, old English scheduler copy absent, visible click targets `13`, min target height `44`, min target width `44`, horizontal overflow `false`, runtime error text `false`, console errors `0`. No push and no T-251 retry.

> Latest Scheduler Dashboard operator-output polish: 2026-06-08 T-1559. `workspace/execution/pages/scheduler_dashboard.py` now uses Korean operator-facing page metadata, title/caption, worker/failure/attention/Telegram/KPI/task/log labels, localized scheduler status/error/trigger/duration formatting, mobile CSS for 44px Streamlit controls including default header menu/deploy buttons, and stretch-width task actions. `workspace/execution/telegram_notifier.py` now formats scheduler Telegram notifications as Korean copy-ready operator alerts. `workspace/tests/test_scheduler_dashboard_page.py` and `workspace/tests/test_telegram_notifier.py` lock the copy, formatter, source, and mobile CSS contracts. Verification passed focused scheduler/Telegram pytest (`45 passed` with repo-local basetemp), scheduler-only pytest (`7 passed` after CSS follow-up), Ruff check, Ruff format check, `py_compile`, diff-check, graph refresh, code-review gate advisory WARN covered by focused/browser evidence, and browser QA at `390x844`: document title/H1/Korean sections present, old English scheduler copy absent, visible click targets `13`, min target height `44`, min target width `44`, horizontal overflow `false`, runtime error text `false`, console errors `0`. No push and no T-251 retry.

> Latest ROI Dashboard mobile output polish: 2026-06-08 T-1558. `workspace/execution/pages/roi_dashboard.py` now uses compact Korean operator copy (`쇼츠 ROI`), Korean page metadata, clearer Shorts RPM copy, hidden Plotly modebars, Korean breakeven-view copy, and mobile CSS for 44px Streamlit number-input targets. `workspace/tests/test_result_roi_dashboards.py` locks the Plotly config, mobile CSS helper, and Korean copy/source contract. Verification passed focused Result/ROI dashboard pytest (`8 passed` with repo-local basetemp), Ruff check, Ruff format check, `py_compile`, diff-check, graph refresh, code-review gate advisory WARN covered by focused/browser evidence, and browser QA at `390x844`: title/caption/RPM copy present, old English title/RPM labels absent, 3 number-input controls min height `44`, min button width `44`, visible Plotly modebars `0`, horizontal overflow `false`, console errors `0`, failed requests `0`. No push and no T-251 retry.

> Latest selector inventory persistence and Shorts Manager expander touch-target hardening: 2026-06-08 T-1556/T-1557. T-1556 makes `next_experiment_selector.py` persist live GitHub/browser/dependency inventory helper outputs to the expected `.tmp` cache paths, with tests asserting `--output` for each helper. T-1557 raises Streamlit expander summaries in Shorts Manager to 44px mobile targets. Verification passed selector/Shorts focused pytest (`60 passed` with repo-local basetemp), Shorts Manager focused pytest (`46 passed`), Ruff check, Ruff format check, `py_compile`, diff-check, graph refresh at the code-change head, and browser QA at `390x844`: 6 select options min height `44`, 13 expander summaries min height `44`, no horizontal overflow, console errors `0`, failed requests `0`. Latest live audits remain readiness score `96`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`, release packet `ready_for_authorization`, selector `blocked_publish_only`, launch audit local requirements complete, completion audit `11/15` with `4` blocked items. Use live `session_orient.py --json` for exact HEAD/ahead count after relay-only commits. No push and no T-251 retry.

> Latest launch-audit evidence and Shorts Manager select-target hardening: 2026-06-08 T-1554/T-1555. T-1554 adds a direct output-quality objective item to `launch_objective_audit.py`, covering project good-output criteria, external benchmark signals, Good vs Bad output distinction, and iterative baseline/candidate/A-B evidence; focused audit tests cover complete and blocked cases. T-1555 raises Shorts Manager Streamlit select dropdown options to 44px mobile targets and locks the selector contract. Verification passed combined focused pytest (`91 passed` with repo-local basetemp), Ruff check, Ruff format check, `py_compile`, diff-check, graph refresh, and browser QA at `390x844`: 6 dropdown options, min option height `44`, display `flex`, align-items `center`, no horizontal overflow, console errors `0`, failed requests `0`. Latest product-code changes are committed locally; use live `session_orient.py --json` for exact HEAD/ahead count. Readiness score `96`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; completion audit `11/15` complete with `4` blocked items. No push and no T-251 retry.

> Latest Knowledge Dashboard skip-target hardening and final local evidence refresh: 2026-06-07 T-1553. `projects/knowledge-dashboard/src/app/page.tsx` now gives the `skip-to-main` link a mobile-safe focused target (`focus:flex focus:min-h-11 focus:items-center`) after T-1552 browser QA surfaced its hidden normal-state `sr-only` 1x1 box. `projects/knowledge-dashboard/src/mobile-touch-targets.test.mts` locks the contract. Code/QC head `4f037a69` was clean with graph current. Verification passed Knowledge test/lint/build/smoke (`64 passed`), full active-project QC (Blind-to-X `1844 passed`, `9 skipped`; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`; Hanwoo `536 passed`; Knowledge `64 passed`; all configured lint/build/smoke gates pass), browser QA at `390x844` for Knowledge/Suika/Word Chain with Knowledge focused skip target `148x44`, no overflow, no replacement chars, console errors `0`, failed requests `0`, and `browser_qa_inventory.py` fresh usable/nonblank screenshot coverage `4/4`. Readiness score `96`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; completion audit `10/14` complete with `4` blocked items. No push and no T-251 retry.

> Latest browser QA retained-screenshot evidence refresh: 2026-06-07 T-1552. Closed the auto-research selector's remaining adoptable browser-evidence gap by refreshing retained screenshots for the browser app set. Playwright CLI captured fresh ignored screenshots for Suika and Word Chain (`output/playwright/suika-game-v2-t1552-browser-refresh.png`, `output/playwright/word-chain-t1552-browser-refresh.png`), while Knowledge Dashboard already had fresh ignored evidence (`output/playwright/knowledge-t1552-debug.png` / `knowledge-dashboard-t1552-browser-refresh...`). `browser_qa_inventory.py --json` now reports browser app coverage `4/4`, missing `0`, current/fresh screenshot coverage `4/4`, fresh usable screenshot coverage `4/4`, and fresh nonblank screenshot coverage `4/4`. Reran selector, launch audit, completion audit, readiness, release packet, and session orientation at local HEAD `48759dca`: readiness score `96`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`; Knowledge Dashboard is `100/ready` with QC PASS (`62 passed`, `0 failed`); selector is `blocked_publish_only`; completion audit is `10/14` complete with `4` blocked items. No push and no T-251 retry.

> Latest Result Dashboard mobile upload-controls polish: 2026-06-07 T-1551. `workspace/execution/pages/result_dashboard.py` now injects mobile-only CSS so the upload-result registration workflow uses 44px-safe tabs, buttons, form-submit buttons, BaseWeb select/input wrappers, text areas, and date inputs on narrow screens. Baseline mobile browser QA at `390x844` found app tabs at `40px` and `URL`/`제목`/`태그` text inputs at `38px`; candidate QA removed those app controls from the small-target list while preserving `수동 업로드 + 자동 결과 관리` copy, horizontal overflow `false`, replacement characters `0`, console errors `0`, and failed requests `0`. `workspace/tests/test_result_roi_dashboards.py` locks the CSS helper, source call, 44px tab/control selectors, and existing Streamlit width helpers. Verification passed focused pytest (`6 passed` from repo root), Ruff check, Ruff format check, `py_compile`, `git diff --check`, and Shorts Maker V2 project QC test/lint (`1640 passed`, `12 skipped`, `29 warnings`, lint pass). Code commit `d67858d0` is local only. No push and no T-251 retry.

> Latest Performance Overview operator-copy polish: 2026-06-07 T-1550. `workspace/execution/pages/performance_overview.py` now presents the cross-project KPI dashboard in Korean operator copy: page title/caption, sidebar system-health metrics, KPI cards, publishing trends, API cost breakdown, platform performance, empty states, footer, missing-data labels, chart series names, and provider table columns. The follow-up removed the pipeline success KPI's tiny hidden help control by replacing `help="watchdog_history.json 기준"` with the visible caption `자동 점검 이력 기준`. `workspace/tests/test_performance_overview_page.py` locks the rendered Korean labels, visible basis caption, and old English/`N/A`/`files`/`watchdog_history.json 기준` rejection. Verification passed focused pytest (`7 passed`), Ruff check, `py_compile`, `git diff --check`, and mobile browser QA at `390x844`: required Korean labels present, forbidden English/`N/A` strings absent, horizontal overflow `false`, app-owned small controls `[]`, console warnings/errors `0`. Final screenshot: ignored `output/playwright/performance-overview-t1550-final-mobile.png`. No push and no T-251 retry.

> Latest project-dashboard output-quality handoff: 2026-06-07 T-1549. Adopted `.ai/PROJECTS.md` as a first-class session-start dashboard instead of leaving it as an untracked intermediate. The dashboard gives each project a direct status, stack, current focus, good-output criteria, verification boundary, and shared release blocker summary, so continuation agents can choose output-quality work from a usable product map rather than scanning long relay history. Wired it into AGENTS/CLAUDE/GEMINI, the `session-start` and `session-close` skills, `.agents/workflows/start.md`, and `.ai/CONTEXT.md`. Verification passed before commit: `git diff --check` returned 0 with CRLF warnings only, both rotator dry-runs returned `noop`, and `session_orient.py --json` reported latest Next Priorities `ok`, HANDOFF 200 lines, TODO 1, IN_PROGRESS 0. Separate `workspace/execution/pages/performance_overview.py` WIP remains outside this context commit. No push and no T-251 retry.

> Latest workspace hosted logging fallback polish: 2026-06-07 T-1547. Browser QA reproduced Shorts Manager failing before first render with `TypeError: Cannot log to objects of type 'NoneType'` from `execution._logging` when Streamlit hosted the app with `sys.stderr=None`. `workspace/execution/_logging.py` now selects a usable console stream from `sys.stderr`, `sys.__stderr__`, `sys.stdout`, or `sys.__stdout__` before adding loguru/stdlib console handlers, and still preserves file/JSONL logging if no console stream exists. Added `workspace/tests/test_logging_setup.py` to reload the module with `sys.stderr=None` and prove output still reaches `sys.__stderr__`. Post-fix mobile browser QA at `390x844` loaded `Shorts Manager`, screenshot `output/playwright/shorts-manager-t1545-mobile.png`, with horizontal overflow `false`, console errors `0`, failed requests `0`. Verification passed focused pytest (`1 passed`), Shorts Manager pytest (`47 passed`), Ruff check, `py_compile`, `git diff --check` with CRLF warning only, and Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass). No push was performed and T-251 was not retried.

> Latest Hanwoo ear-tag scanner close target polish: 2026-06-07 T-1546. Continued the Hanwoo browser-click/auto-research loop while selector state remained `blocked_publish_only`, so this was a distinct local UX experiment, not a release/T-251 claim. External check used W3C WCAG 2.2 target-size guidance for 44 by 44 CSS px touch targets. Browser baseline at `390x844` entered field mode, opened the virtual ear-tag scanner, ran the no-match scan path, and measured the header close button `이표 스캐너 닫기` at `34x34`. `projects/hanwoo-dashboard/src/components/widgets/EarTagScannerModal.js` now gives that icon-only close action `inline-flex min-h-11 min-w-11 items-center justify-center`, and `projects/hanwoo-dashboard/src/lib/eartag-scanner-modal-accessibility.test.mjs` locks the contract while rejecting the old `p-2` class. Browser remeasure reported `44x44`. Verification passed focused scanner test (`6 passed`), Hanwoo project test (`536 passed`), lint, build, `git diff --check`, and code-review gate advisory WARN (`risk_score=0.30`) covered by direct browser/source/project evidence. No push was performed and T-251 was not retried.

> Latest Blind-to-X output-quality loop documentation closure: 2026-06-07 T-1545. Rechecked `projects/blind-to-x/docs/output_quality_selection_gate_2026-06-07.md` against current implementation and tests. The document's previous "Next Loop" was already completed by T-1462/T-1482/T-1486: Notion reviewer output now surfaces selection quality, readiness verdicts, and concrete edit plans through `pipeline/notion/_upload.py`, with coverage in `tests/unit/test_notion_upload.py`. Updated the doc so future agents do not re-run that completed operator-facing selection-summary loop. This is documentation-only; rerun current-head release evidence after commit before any release claim. Expected remaining boundaries remain explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest launch-boundary handoff refresh: 2026-06-07 T-1544. Reconciled the post-T-1543 relay after follow-up `.ai` documentation commits. The last exact release-boundary audit before this handoff still showed readiness score `96`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`, release packet `ready_for_authorization`, selector `blocked_publish_only`, and completion audit `10/14` with `4` blocked items. This is a relay update only; because this context commit advances HEAD again, exact current-head release evidence must be rerun after the commit before any release claim. Expected remaining boundaries are explicit push/current-head GitHub Actions (`root-quality-gate`, `active-project-matrix`) plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest full canonical QC/readiness refresh: 2026-06-07 T-1543 at code baseline `a51bfa31`. Full active-project QC passed via canonical artifact `.tmp/project_qc_runner_latest.json`: Blind-to-X `1844 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `536 passed`, lint/build/smoke passed; Knowledge Dashboard `62 passed`, lint/build/smoke passed. This refresh was run after T-1540 Hanwoo AI widget polish plus T-1541/T-1542 Shorts Manager polish and before the follow-up `.ai` documentation commits. No push was performed and T-251 was not retried. Rerun final readiness/release-selector evidence after docs commits before any release claim.

> Latest Shorts Manager shortcut-nav localization: 2026-06-07 T-1542 at local code commit `58190d19`. `_render_operator_shortcuts()` now names the quick-jump navigation `쇼츠 운영 빠른 이동`, matching the T-1541 compact Korean visible title instead of leaving `Shorts Manager 빠른 이동` in the accessibility tree. `workspace/tests/test_shorts_manager.py` locks the localized nav label and rejects the old label. Verification passed focused Shorts Manager pytest (`2 passed`), targeted Ruff check, `py_compile`, `git diff --check` with CRLF warning only, advisory code-review gate WARN (`risk_score=0.30`) covered by direct shortcut regression, and current-head Shorts Maker V2 project QC `.tmp/project_qc_runner_shorts_t1542.json` (`1640 passed`, `12 skipped`, `29 warnings`, lint pass). No push was performed and T-251 was not retried. Refresh graph/current-head readiness/release-selector evidence before any release claim.

> Latest Shorts Manager compact mobile title polish: 2026-06-07 T-1541 at local code commit `003ebf9c`. `workspace/execution/pages/shorts_manager.py` keeps Streamlit metadata `page_title="Shorts Manager"` but replaces the visible first-viewport title/caption with compact Korean operator copy: `🎬 쇼츠 운영` and `생성 · 검수 · 업로드 관리`. `workspace/tests/test_shorts_manager.py` locks the compact Korean visible copy, rejects the old visible English title/caption, and preserves the metadata title. Browser A/B at `390x844` improved title height `142px -> 89px`, first title bottom `254px -> 201px`, visible English title matches `1 -> 0`, visible Korean title matches `0 -> 1`, and horizontal overflow stayed `0`; `.tmp/ab-manifest-t1541.json` selected `adopt_candidate` (`score_delta=0.39547521348563824`). Verification passed focused Shorts Manager pytest (`46 passed`), targeted Ruff check, Ruff format check, `py_compile`, Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, lint pass), `git diff --check` with CRLF warning only, graph refresh current at `003ebf9c`, and advisory code-review gate WARN (`risk_score=0.30`) covered by direct/browser/project evidence. No push was performed and T-251 was not retried. Refresh current-head readiness/release-selector evidence before any release claim because HEAD advanced after the previous launch-boundary refresh.

> Latest Hanwoo AI widget touch-target polish: 2026-06-07 T-1540 at local code commit `9027df72`. `projects/hanwoo-dashboard/src/components/widgets/AIInsightWidget.js` now gives the manual AI insight refresh action a 44px target and stable test id, while `projects/hanwoo-dashboard/src/components/widgets/AIChatWidget.js` constrains the chat panel to narrow mobile viewports and raises close/send/retry actions to 44px-safe dimensions. `projects/hanwoo-dashboard/src/lib/ai-insight-widget-copy.test.mjs` and `projects/hanwoo-dashboard/src/lib/ai-chat-widget-copy.test.mjs` lock those contracts. Browser QA artifact `.tmp/hanwoo-t1540-ai-widget-current.json` passed at 320, 390, and 1280px with `smallControls=0`, `xOverflow=false`, bad responses `0`; expected console warnings are the known external Supabase/T-251 degraded-read path. Verification passed Hanwoo source tests (`536 passed`), Hanwoo lint, `git diff --check`, graph refresh at `9027df72`, staged/pre-commit code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project evidence, and post-commit Hanwoo project QC `.tmp/project_qc_runner_hanwoo_t1540.json` (`test/lint/build/smoke` passed). No push was performed and T-251 was not retried. Current HEAD later advanced to T-1541/context commits, and the worktree still has unrelated Shorts Manager modifications in `workspace/execution/pages/shorts_manager.py` and `workspace/tests/test_shorts_manager.py`; refresh full canonical readiness only after that separate WIP is resolved.

> Latest launch-audit robustness refresh: 2026-06-07 T-1539 at local code commit `f88340ad`. `browser_qa_inventory.py` no longer accumulates full decoded PNG pixel buffers for every retained screenshot; it restores scanlines and exits nonblank detection on the first pixel difference, so the full `output/playwright` inventory now completes reliably with fresh usable/nonblank coverage `4/4`. `launch_objective_audit.py` now accepts the same top-level `gates.candidate` A/B manifest shape that `ab_decision.py` already supports, so `.tmp/ab-manifest-t1537.json` is correctly counted as required gates passed `4/4` instead of a false audit blocker. Verification passed focused auto-research pytest (`61 passed`), targeted Ruff, `py_compile`, `browser_qa_inventory.py --json`, `launch_objective_audit.py`, and `completion_audit.py --allow-incomplete`. Goal remains active because current-head Actions still require explicit push/user push and Hanwoo T-251 remains user-owned.

> Latest launch-boundary refresh: 2026-06-07 T-1538 no-code evidence update. Hanwoo project QC artifact `.tmp/project_qc_runner_hanwoo_t1537.json` passed `test/lint/build/smoke` (`535 passed`; build succeeded after one real Next build-lock retry; smoke only known accepted 405/200 warnings). `product_readiness_score.py --json` reports score `96`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`. `next_experiment_selector.py` reports `blocked_publish_only` / `current_head_release_checks_unproven`, so no adoptable local candidate remains until explicit push or user push lets GitHub Actions prove `root-quality-gate` and `active-project-matrix` on the exact current HEAD. Generate/reuse `.tmp/release-authorization-packet.json` for the final current HEAD before any push. T-251 remains user-owned and must not be retried before Supabase credential reset/resync.

> Latest Shorts Manager mobile readiness controls polish: 2026-06-07 T-1537 at local code commit `273a511a`. `workspace/execution/pages/shorts_manager.py` now gives Streamlit number stepper buttons and checkbox labels 44px mobile touch targets, keeps number steppers at least 44px wide, and localizes channel-readiness captions from `voice/style` to `음성/스타일`. `workspace/tests/test_shorts_manager.py` locks the mobile CSS selectors and the localized readiness caption contract. Verification passed focused Shorts Manager pytest (`45 passed`), targeted Ruff check, Ruff format check, `py_compile`, browser QA at `390x844` (`hasEnglishVoiceStyle=false`, `koreanVoiceStyleCount=5`, `horizontalOverflow=false`, screenshot `output/playwright/shorts-manager-t1537-channel-readiness-copy.png`), and staged/commit code-review gate exit 0 with advisory WARN (`risk_score=0.30`) covered by direct source/render/browser tests. No push was performed and T-251 was not retried.

> Latest Hanwoo dashboard content-shell scroll bound: 2026-06-07 T-1536 at local code commit `28e7946c`. This is a follow-up to T-1535. `projects/hanwoo-dashboard/src/app/globals.css` now bounds the mobile `.dashboard-content-shell` to the safe viewport height with contained vertical scrolling and resets the bound on desktop; `projects/hanwoo-dashboard/src/lib/home-market-copy.test.mjs` locks max-height, overflow, overscroll, fallback, and desktop reset contracts. Final browser-click QA artifact `.tmp/hanwoo-t1530-tabbar-safe-current.json` measured mobile and desktop `covered=0`, `small=0`, `xOverflowTabs=0`, bad responses `0`; final Hanwoo project QC artifact `.tmp/project_qc_runner_hanwoo_t1535.json` passed `test/lint/build/smoke`. No push was performed and T-251 was not retried.

> Latest Hanwoo settings tabbar-safe polish: 2026-06-07 T-1535 at local code commit `8cfc353b`. `projects/hanwoo-dashboard/src/components/tabs/SettingsTab.js` now gives farm settings fields a mobile scroll viewport, and `projects/hanwoo-dashboard/src/app/globals.css` preserves mobile spacing so settings/farm/building controls do not sit behind the fixed dashboard tab bar. `projects/hanwoo-dashboard/src/lib/home-market-copy.test.mjs` and `projects/hanwoo-dashboard/src/lib/settings-tab-accessibility.test.mjs` lock the CSS variables/classes. Browser-click QA artifact `.tmp/hanwoo-t1530-tabbar-safe-current.json` measured mobile and desktop `covered=0`, `small=0`, `xOverflowTabs=0`, bad responses `0`, with only known external Supabase/KAPE degraded-path warnings. Verification passed full Hanwoo source tests (`535 passed`), Hanwoo lint, Hanwoo project QC (`test/lint/build/smoke` passed), `git diff --check`, graph refresh, and staged code-review gate exit 0 with advisory WARN (`risk_score=0.30`) covered by direct/source/browser/project evidence. No push was performed and T-251 was not retried.

> Latest Shorts Channel Growth mobile polish: 2026-06-07 T-1532 at local code commit `8ac5b2c0`. `workspace/execution/pages/channel_growth.py` now uses a compact Korean title, mobile-only 44px touch-target CSS for Streamlit form/buttons and BaseWeb input/select controls, and Plotly chart config that hides modebars while keeping responsive charts. `workspace/tests/test_channel_growth_page.py` locks the compact title, modebar config, width API, and mobile CSS contract. Browser QA at `390x844` improved title height `194px -> 48.8px`, small app buttons `2 -> 0`, small BaseWeb controls `3 -> 0`, and Plotly modebar buttons `24 -> 0`, with no overflow, no console messages, and no failed requests; A/B adopted the candidate. Verification passed focused Channel Growth pytest (`6 passed`), related Channel Growth pytest with repo-local basetemp (`16 passed`), targeted Ruff, `py_compile`, `git diff --check`, graph refresh, and Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass). No push was performed and T-251 was not retried. Current HEAD later advanced to Hanwoo `8cfc353b`, so refresh full current-head launch evidence before release claims.

> Latest Hanwoo outbox notification copy repair: 2026-06-07 T-1530 at local code commit `9e9ec78a`. `projects/hanwoo-dashboard/scripts/outbox-worker.mjs` no longer persists broken Korean calving notification text in read-model snapshots; the final user-facing copy is now `분만 예정일이 ${daysLeft}일 남았습니다.` and corrupted section comments were removed. `projects/hanwoo-dashboard/src/lib/outbox-worker-copy.test.mjs` locks valid Korean estrus/calving notification messages and rejects replacement/mojibake text. Verification passed `rg` no replacement characters in touched files, `node --check`, Hanwoo source tests (`534 passed`), Hanwoo lint, Hanwoo project QC (`534 passed`, lint/build/smoke passed), and `git diff --check` with CRLF warning only. Code-review gate WARN (`risk_score=0.45`) is advisory and covered by direct copy guard plus full Hanwoo QC. No push was performed and T-251 was not retried. Refresh current-head readiness before release claims because HEAD advanced past T-1529.

> Latest full launch-gate refresh: 2026-06-07 T-1529 at code/QC baseline `8870fe71`, after T-1527 Hanwoo CattleForm modal polish and T-1528 Shorts Analytics channel-label/mobile-tab polish; later `.ai` commits are documentation-only and readiness reports no relevant active-project changes since the QC artifact. `python execution\project_qc_runner.py --json --artifact .tmp\project_qc_runner_latest.json --timeout-seconds 700` passed with full active-project coverage: Blind-to-X `1844 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `533 passed`, lint/build/smoke passed; Knowledge Dashboard `62 passed`, lint/build/smoke passed. `product_readiness_score.py --json` reports score `96`, clean worktree, fresh QC artifacts, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`; `session_orient.py --json` reports a current graph and no open PRs. No push was performed and T-251 was not retried. Remaining release boundaries are explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E.

> Latest Shorts Analytics channel-label/mobile-tab polish: 2026-06-07 T-1528 at local code commit `c4f26ef3`. Shorts Analytics now normalizes legacy/raw channel labels (`AI/Tech`, `space`, `history`, `health`, etc.) to canonical Korean display labels, merges production/performance summaries by display label, applies those labels across CPV tables/top-video metadata/ROI expanders, and injects mobile tab CSS so Streamlit tab controls meet the 44px target at narrow widths. Browser QA at `390x844` measured `moduleErrorCount=0`, `appContentLoaded=true`, `rawLegacyLabelCount=0`, `canonicalLabelCount=6`, `smallTabButtons=[]`, horizontal overflow false, console warnings/errors 0, and failed requests 0. Verification passed focused workspace tests (`27 passed`), Ruff, `py_compile`, `py -3.13 -m uv lock --project workspace --check`, graph refresh, Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), and advisory code-review gate WARN (`risk_score=0.40`) covered by focused/browser/project QC. No push was performed and T-251 was not retried. Refresh full canonical active-project QC/readiness before release claims because HEAD advanced past T-1527.

> Latest Hanwoo CattleForm modal polish: 2026-06-07 T-1527 at local code commit `24b6046d`. Authenticated mobile QA at `390x844` found the `새 개체 등록` modal had two sub-44px controls and the sticky save action bar covering `구입가격`/`구입일자` fields. `CattleForm.js` now separates the field scroller from the visible save action bar, raises close/history lookup/genetic controls to at least `44px`, and keeps footer action labels on one line. Candidate browser QA `.tmp/hanwoo-t1527-cattle-form-modal-candidate.json` measured initial, purchase-field scrolled, and memo scrolled states with `smallControls=0`, `coveredFields=0`, `scrollFooterOverlap=0`, horizontal overflow false, and bad responses `0`; A/B selected `adopt_candidate` (`score_delta=0.7142857142857143`). Verification passed Hanwoo source tests (`533 passed`), lint, Hanwoo project QC test/lint/build/smoke, graph refresh, and code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project evidence. No push was performed and T-251 was not retried.

> Latest current-head full launch-gate refresh: 2026-06-07 pre-T-1528 through T-1526 at code/QC head `c39bde69`; later `.ai` context/session commits are documentation-only after that artifact. Full canonical active-project QC passed via `python execution\project_qc_runner.py --json --artifact .tmp\project_qc_runner_latest.json --timeout-seconds 700`: Blind-to-X `1844 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `533 passed`, lint/build/smoke passed; Knowledge Dashboard `62 passed`, lint/build/smoke passed. T-1527/T-1528 landed after this artifact, so refresh full canonical active-project QC/readiness before release claims. No push was performed and T-251 was not retried. Remaining release boundaries are explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E.

> Latest Shorts Analytics RPM labeling/runtime dependency polish: 2026-06-07 T-1526 at local code commit `c39bde69`. The ROI surface now labels Shorts revenue estimates as RPM, not generic CPM, and explains RPM as revenue per 1,000 engaged views. `workspace/pyproject.toml` declares `plotly>=6.8.0`, `uv.lock` resolves it, and `test_shorts_analytics.py` locks the copy/dependency contract. Verification passed focused Shorts Analytics pytest (`9 passed`), Ruff, format check, `py_compile`, `py -3.13 -m uv lock --check`, staged code-review gate PASS (`risk_score=0.00`), graph refresh, and full active-project QC. No push was performed and T-251 was not retried.

> Latest Hanwoo dashboard tabbar scroll-target polish: 2026-06-07 T-1525 at local code commit `e6386296`. `globals.css` centralizes the fixed mobile tabbar offset in `--dashboard-tabbar-offset`, applies it to document/dashboard scroll padding, and gives dashboard controls matching `scroll-margin-bottom`; `home-market-copy.test.mjs` locks the contract. Verification passed focused Hanwoo source test (`57 passed`), Hanwoo lint, Hanwoo project QC (`533 passed`, lint/build/smoke passed), browser CSS check at `390x844`, staged code-review gate PASS (`risk_score=0.00`), graph refresh, and full active-project QC. No push was performed and T-251 was not retried.

> Latest Shorts Manager channel-settings label polish: 2026-06-07 T-1524 at local code commit `e1d41f20`. Baseline browser QA found the Korean operator UI still showed four English channel-settings form labels (`Voice`, `Style preset`, `Font color`, `Image style prefix`), so the settings output looked like a partial template instead of a finished Korean control surface. The form now uses `음성`, `스타일 프리셋`, `자막 색상`, and `이미지 스타일 프롬프트`; `test_shorts_manager.py` locks the localized labels and rejects the English labels. Candidate browser QA measured English labels `4 -> 0`, Korean labels `0 -> 4`, horizontal overflow `0`, browser failures `0`, and A/B adopted candidate. Verification passed Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), full active-project QC, graph refresh, and clean-worktree readiness. No push was performed and T-251 was not retried.

> Latest Hanwoo service-worker fallback polish: 2026-06-07 T-1522 at local code commit `add97c80`. Baseline inspection found committed `public/sw.js` was a stale generated Serwist/Workbox precache bundle with an old Next build id (`T0yJO_Gh6UzSU-yj3WM-R`) and an old `/manifest.json` revision (`e1df87ce...`) while the current manifest hash is `4b4e0626...`. Official Workbox docs warn not to hand-hardcode precache revisions. `public/sw.js` is now a build-agnostic fallback that skips waiting, claims clients, and clears old `serwist-`/`workbox-` caches; `NEXT_ENABLE_PWA=1` builds still replace it with the generated Serwist precache bundle. `app-metadata-copy.test.mjs` locks that the committed fallback has no `_next/static`, `precacheAndRoute`, service-worker manifest injection token, or hardcoded revision. Verification passed Hanwoo tests (`533 passed`), lint, direct HTTP/browser fetch QA for `/sw.js` and `/manifest.json`, Hanwoo project QC (`533 passed`, lint/build/smoke passed), graph refresh, code-review gate PASS (`risk_score=0.00`), and A/B `adopt_candidate` (`score_delta=0.8181818181818182`). Direct PWA build still hits the known Korean-path native crash (`-1073740791`), while ASCII temp PWA build passed. No push was performed and T-251 was not retried. T-1523 later refreshed full canonical active-project QC/readiness at this code baseline.

> Latest current-head full launch-gate refresh T-1523 is superseded by the pre-T-1528 refresh after T-1525/T-1526 landed. Earlier T-1523 covered through T-1524 at code/QC head `e1d41f20`.

> Latest Shorts Manager review-queue action polish: 2026-06-07 T-1520 at local code commit `cf3cbb68`. Baseline mobile QA at `390x844` found the `검수 큐` shortcut landed correctly but review cards exposed `0` actionable controls, forcing operators back to the content list to run/upload/retry/sync/delete the same item. Manual review queue cards can now expose the same practical item actions as the content list (`실행`, `YT 업로드`, `YT 재시도`, `📋 Notion↑/↻`, `삭제`) when generation/auth readiness context is supplied. `shorts_manager.py` now reuses `_render_item_action_buttons()` for content-list and review-queue cards while preserving generation locks, YouTube retry/upload locks, Notion sync locks, delete confirmation, and localized external-action reasons. `test_shorts_manager.py` locks the source contract and the review-queue action surface. Candidate browser QA measured `controls=7`, primary actions `5`, core actions `326x44`, no small controls, no overflow, and console/request failures `0`; A/B adopted candidate (`score_delta=3.090909090909091`). Verification passed focused Shorts Manager pytest (`44 passed`), related workspace tests (`92 passed`), Ruff check, `py_compile`, `git diff --check` with CRLF warnings only, Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), graph refresh, and code-review gate advisory WARN (`risk_score=0.40`) covered by focused/browser/project QC. No push was performed and T-251 was not retried. Current HEAD advanced past the prior full canonical artifact, so refresh full canonical active-project QC/readiness before release claims.

> Latest Hanwoo PWA manifest identity polish: 2026-06-07 T-1519 at local code commit `f1e37714`. Public mobile click QA at 320/360/390px verified login legal returns, dashboard returns, subscription failure retry, and 404 recovery with `flowsWithErrors=[]`, no small visible targets, no horizontal overflow, no replacement characters, and only the expected intentional 404 for `/privacy-missing`. PWA public resource QA confirmed `/manifest.json`, icons, `sw.js`, and Workbox script return 200 with correct content types. External MDN/web.dev manifest guidance supports explicit stable app identity and URL scope; `public/manifest.json` now declares `id: "/"`, `scope: "/"`, and `lang: "ko-KR"`, and `app-metadata-copy.test.mjs` locks the contract. A/B adopted candidate (`score_delta=0.8333333333333334`). Verification passed Hanwoo tests (`532 passed`), lint, manifest HTTP QA, Hanwoo project QC (`532 passed`, lint/build/smoke passed), `git diff --check` with CRLF warnings only, graph current at `f1e37714`, and code-review gate PASS (`risk_score=0.00`). No push was performed and T-251 was not retried. Current HEAD advanced past the prior full canonical artifact, so refresh full canonical active-project QC/readiness before release claims.

> Latest Shorts Manager delete-confirmation fixed-overlay polish: 2026-06-07 T-1518 at local code commits `dd24a7f0` and `386b9e17`. Baseline mobile QA at `390x844` found content-list deletion exposed only a flash after the first `삭제` click while actionable `삭제 확인`/`취소` controls were absent from the current viewport (`confirmCount=0`, `cancelButtonCount=0`). The confirmation is now rendered from a top-level global container and pinned as a centered fixed overlay with bounded width/height, scroll protection, and mobile sizing. Final browser QA `.tmp/shorts-manager-t1516-fixed-cancel-final.json` measured the overlay fitting the `390px` viewport (`x=12`, `y=56`, `right=378`, `width=366`), visible top confirm/cancel buttons at `334x44`, `cancelClearedPending=true`, no horizontal overflow, and console/request failures `0`; A/B adopted candidate (`score_delta=0.8571428571428571`). Verification passed focused Shorts Manager pytest (`43 passed`), related workspace tests (`91 passed`), targeted Ruff check, `py_compile`, `git diff --check` with CRLF warning only, Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), graph update, and code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project QC. No push was performed and T-251 was not retried. Current HEAD advanced past the T-1514/T-1515 full canonical artifact, so refresh current-head active-project QC/readiness before release claims.

> Latest Blind-to-X Notion/X output-quality polish: 2026-06-07 T-1517 at local code commit `2ab2a4f6`. X official counting rules and `twitter/twitter-text` v3 config confirm reviewers need weighted length, not raw `len()`: URLs count as 23 and CJK/emoji can count heavier. `_build_x_upload_check_lines()` now uses existing `x_weighted_character_count()` and surfaces `X 가중 글자 수` in the Notion X upload card. Regression tests lock Korean overflow (`가` x141 -> `282/280자 (초과)`) and long URL normalization (`27/280자 (OK)`). Verification passed focused Notion tests (`53 passed`), targeted Ruff check/format check, `git diff --check`, Blind-to-X project QC (`1844 passed`, `9 skipped`, lint pass), and code-review gate advisory WARN covered by focused/project QC. No push was performed and T-251 was not retried. Current HEAD advanced past the T-1514/T-1515 full canonical artifact, so refresh full canonical active-project QC/readiness before current-head release claims.

> Latest Hanwoo public-login touch-target polish: 2026-06-07 T-1516 at local code commit `f7fa3994`. Public mobile browser QA at `390x844` found the login password visibility button at `38x38` and login legal links at `24px` height; after checking current W3C WCAG 2.2 target-size guidance plus Android/Material touch-target guidance, `globals.css` now raises the password toggle to `48x48`, increases password input right padding, and gives login legal links/separators 44px rows. `error-pages-wiring.test.mjs` locks the CSS contract. Candidate browser QA measured `routesWithSmallTargets=[]`, no horizontal overflow, and no replacement characters; A/B adopted the candidate (`score_delta=0.7359649122807017`). Verification passed focused source test (`15 passed`), full Hanwoo source tests (`532 passed`), Hanwoo lint, browser audit, `git diff --check`, graph update, and staged/commit code-review gate advisory WARN covered by focused/browser/project evidence. No push was performed and T-251 was not retried. Current HEAD advanced past the T-1514/T-1515 full canonical artifact, so refresh full canonical active-project QC/readiness before current-head release claims.

> Latest final local verification refresh: 2026-06-07 after T-1514 launch-objective browser inventory fallback and T-1515 Shorts Manager channel alias normalization. Full canonical active-project QC passed via `python execution\project_qc_runner.py --json --artifact .tmp\project_qc_runner_latest.json`: Blind-to-X `1842 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `531 passed`, lint/build/smoke passed; Knowledge Dashboard `62 passed`, lint/build/smoke passed. Live orientation/readiness confirms clean worktree, graph current, no open PRs, product readiness score `96`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`, release packet `ready_for_authorization`, and selector `blocked_publish_only`. Remaining boundaries are explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest launch-audit robustness refresh: 2026-06-07 T-1514 at local code commit `96007240`, with current-head verification after T-1515 at `661f2c06`. `launch_objective_audit.py` now reuses a recent `.tmp/browser-qa-inventory.json` when the live browser inventory helper exceeds the default 120s timeout, so launch selection stays `blocked_publish_only` instead of falsely routing to `input_evidence_unavailable`. The fallback records path/age and keeps the live timeout stderr visible. Verification passed focused launch-audit pytest (`42 passed` with repo-local basetemp), targeted Ruff, `py_compile`, `git diff --check` with CRLF warnings only, and runtime launch audit at code HEAD `661f2c06` showing browser coverage `4/4`, fresh usable/nonblank screenshots `4/4`, release packet `ready_for_authorization`, selector `blocked_publish_only`, local blockers `0`, publish blockers `1`, and external blocker `1`. Current T-1515 Shorts Manager focused recheck also passed (`40 passed`), targeted Ruff, and `py_compile`. No push was performed and T-251 was not retried.

> Latest Shorts Manager channel-label polish: 2026-06-07 T-1515 at local code commit `661f2c06`. Mobile Streamlit QA at `390x844` found raw stored channel keys mixed into the operator UI (`AI/Tech` 5 times and `space` 5 times, horizontal overflow false). `workspace/execution/pages/shorts_manager.py` now normalizes legacy manager aliases for display, tab grouping, readiness-card merging, and manual run routing while preserving DB storage and upload metadata behavior; `workspace/tests/test_shorts_manager.py` locks alias display, readiness merge counts/issues, and canonical run-block routing. Candidate browser QA measured raw legacy label count `0`, canonical labels `18`, horizontal overflow false, and all shortcut anchors reachable. Evidence: `.tmp/ab-shorts-manager-t1514-channel-labels.json`, `output/playwright/shorts-manager-t1514-mobile-{baseline,candidate}.png`. Verification passed Ruff, focused Shorts Manager pytest (`38 passed`), related workspace tests (`63 passed`), Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), `git diff --check`, graph update current at `661f2c06`, and code-review gate advisory WARN (`risk_score=0.40`) covered by focused/browser/project QC. Current HEAD advanced past the T-1513 full canonical artifact, so refresh full canonical active-project QC/readiness before current-head release claims. No push was performed and T-251 was not retried.

> Latest current-head full launch-gate refresh: 2026-06-07 T-1513 at code/QC baseline `23b204fe` after T-1511/T-1512; later `.ai` context/goal commits are documentation-only and leave the artifact relevant to current head. T-1512 first closed a whitespace-only `SettingsTab.js` indentation diff; `git diff --ignore-all-space --exit-code` was clean, focused Settings test passed (`16 passed`), Hanwoo lint passed, and code-review gate advisory WARN was covered by focused test/lint. Full canonical active-project QC then passed via `python execution\project_qc_runner.py --json --artifact .tmp\project_qc_runner_latest.json`: Blind-to-X `1842 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `531 passed`, lint/build/smoke passed; Knowledge Dashboard `62 passed`, lint/build/smoke passed. Latest live orientation confirms clean worktree, graph current, no open PRs, no stale HANDOFF head claim, and no relevant active-project changes since that QC artifact. `product_readiness_score.py --json` remains score `96`, state `blocked`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`; `release_authorization_packet.py --json` is `ready_for_authorization`; selector is `blocked_publish_only` for current-head Actions. Remaining boundaries are explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest Hanwoo settings widget overlap polish: 2026-06-07 T-1511 at local code commit `a4e155d0`. Baseline authenticated mobile Settings QA at `390x844` found the final home-widget switch row under the fixed bottom tab bar: `lowerHitMissCount=3`, and clicking the affected switch point changed the active tab away from Settings. `SettingsTab.js` now wraps the home-widget grid in a bounded internal touch-scroll viewport (`maxHeight: 220px`, `overflowY: auto`, `overscrollBehavior: contain`, bottom scroll padding), and `settings-tab-accessibility.test.mjs` locks the source contract. Candidate QA measured actionable visible overlap `0`, actionable lower-hit misses `0`, scrolled bottom-row actionable switches `3`, `clickPreservedActiveTab=true`, horizontal overflow `0`, and failed responses `0`; expected degraded Supabase warnings only. A/B selected `adopt_candidate` (`score_delta=0.8888888888888888`). Verification passed focused settings test (`16 passed`), Hanwoo project QC (`531 passed`, lint/build/smoke passed), `git diff HEAD~1 --check`, graph current at `a4e155d0`, and code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project QC. Current HEAD advanced past the T-1510 full canonical artifact, so refresh full canonical active-project QC/readiness before current-head release claims. No push was performed and T-251 was not retried.

> Latest current-head full launch-gate refresh: 2026-06-07 T-1510 at local HEAD `45adea52` after T-1508/T-1509. Full canonical active-project QC passed via `python execution\project_qc_runner.py --json --artifact .tmp\project_qc_runner_latest.json`: Blind-to-X `1842 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `531 passed`, lint/build/smoke passed; Knowledge Dashboard `62 passed`, lint/build/smoke passed. `product_readiness_score.py --json` reports score `96`, state `blocked`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`, clean worktree, fresh current-head QC artifacts, graph current at `45adea52`, no open PRs, and `main` ahead of `origin/main` by `652`. Remaining boundaries are explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest Hanwoo secondary action/footer touch-target polish: 2026-06-07 T-1509 at local code commit `143b9559`. The existing staged Hanwoo WIP enlarged Feed filter chips, Inventory/Sales add buttons, shared empty-state CTAs, and Excel export actions to the app's 44px target. A fresh authenticated mobile dashboard QA at `390x844` then found only footer links remained below target: `uniqueSmallCount=3`, `footerSmallCount=3`, action small targets `0`, horizontal overflow `0`. `DashboardClient.js` now gives the terms/privacy/subscription footer links `inline-flex min-h-11 items-center justify-center`, and `home-market-copy.test.mjs` locks the footer contract plus existing action-target coverage. Candidate QA measured `uniqueSmallCount=0`, `footerSmallCount=0`, action small targets `0`, bad responses `0`, overflow `0`, with only expected degraded Supabase warnings. A/B selected `adopt_candidate` (`score_delta=0.6666666666666666`). Evidence uses earlier T-1508 artifact filenames: `.tmp/hanwoo-t1508-small-button-{baseline,candidate}.json`, `.tmp/ab-manifest-hanwoo-t1508-footer-tap-targets.json`, and `output/playwright/hanwoo-t1508-small-button-{baseline,candidate}.png`. Verification passed related Node tests (`78 passed`), Hanwoo project QC (`531 passed`, lint/build/smoke passed), `git diff --cached --check`, graph update, and staged code-review gate PASS (`risk_score=0.0`). Current HEAD advanced past the T-1507 full canonical artifact, so refresh full canonical active-project QC/readiness before current-head release claims. No push was performed and T-251 was not retried.

> Latest Shorts Manager form touch-target polish: 2026-06-07 T-1508 at local code commit `86ceced6`. Mobile Streamlit QA at `390x844` found the new-content/channel-settings form controls below the app's 44px target: text input `38.4px`, BaseWeb input/select controls `40px`, horizontal overflow `0`. `shorts_manager.py` now extends the existing mobile touch-target CSS to BaseWeb select/input controls and real text inputs; `test_shorts_manager.py` locks the source selectors. Candidate browser QA measured input/select controls at `44px`, body width `390`, overflow `0`, and candidate CSS present. Evidence: `.tmp/shorts-manager-t1508-ab.json` and `output/playwright/shorts-manager-t1508-form-touch-{baseline,candidate}.png`. A/B selected `adopt_candidate` (`score_delta=0.09833333333333336`). Verification passed focused Shorts Manager pytest (`33 passed` with repo-local basetemp), Ruff check/format check, `git diff --check`, Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), graph update, and staged/commit code-review gate advisory WARN (`risk_score=0.40`) covered by focused/browser/project QC. Current HEAD advanced past the T-1507 full canonical artifact, so refresh full canonical active-project QC/readiness before current-head release claims. Separate unstaged Hanwoo WIP is visible and must stay outside T-1508. No push was performed and T-251 was not retried.

> Latest current-head full launch-gate refresh: 2026-06-07 T-1507 at local HEAD `8335bb5b` after T-1505/T-1506. Full canonical active-project QC passed via `python execution/project_qc_runner.py --json --artifact .tmp/project_qc_runner_latest.json --timeout-seconds 300`: Blind-to-X `1842 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `530 passed`, lint/build/smoke passed; Knowledge Dashboard `62 passed`, lint/build/smoke passed. `product_readiness_score.py --json` reports score `96`, state `blocked`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`, clean worktree, fresh current-head QC artifacts, graph current at `8335bb5b`, no open PRs, and `main` ahead of `origin/main` by `644`. Release packet is `ready_for_authorization`; selector is `blocked_publish_only` for current-head Actions. Remaining boundaries are explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest Shorts Manager operator shortcut polish: 2026-06-07 T-1505 at local code commit `f69349c8`. The Shorts Manager review/control surface now renders internal shortcut links to new content, content list, review queue, and channel settings so operators can jump between the practical output-review sections instead of manually scrolling a long Streamlit page. `shorts_manager.py` adds escaped anchors and a 44px-min touch shortcut nav with mobile wrapping; `test_shorts_manager.py` locks the source contract, rendered links, anchor escaping, and mobile flex sizing. Verification passed focused Shorts Manager pytest (`33 passed`), targeted Ruff, `py_compile`, `git diff --check` with CRLF warnings only, and staged/commit code-review gate advisory WARN (`risk_score=0.40`) covered by direct source/helper tests. No push was performed and T-251 was not retried.

> Latest Hanwoo degraded refresh target polish: 2026-06-07 T-1506 at local code commit `0e70dc70`. Authenticated mobile tab inventory at `390x844` found the repeated degraded-state `새로고침` action was only `324x36` on all 8 dashboard tabs. `DashboardClient.js` now gives the `InitialDataStatusBanner` refresh `PremiumButton` `min-h-11`, and `initial-data-fallback.test.mjs` locks the source contract. Candidate browser QA measured all 8 refresh buttons at `324x44`, `refreshPass44=8/8`, total small mobile targets reduced from `21` to `7`, horizontal overflow `0`, bad responses `0`, and only expected degraded-state warnings. A/B selected `adopt_candidate` (`score_delta=2.3333333333333335`). Verification passed focused source test (`3 passed`), related source tests (`59 passed`), Hanwoo project QC (`530 passed`, lint/build/smoke passed), `git diff --check`, graph refresh current at `0e70dc70`, and advisory code-review gate WARN (`risk_score=0.30`) covered by focused/browser/project QC. Current HEAD also includes separate Shorts Manager T-1505 commit `f69349c8`, so refresh full canonical active-project QC/readiness before current-head release claims. No push was performed and T-251 was not retried.

> Latest current-head full launch-gate refresh: 2026-06-07 T-1504 at local HEAD `93c8c336` after the T-1503 Hanwoo schedule follow-up. Full canonical active-project QC passed via `python execution/project_qc_runner.py --json --artifact .tmp/project_qc_runner_latest.json`: Blind-to-X `1842 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, lint pass; Hanwoo `530 passed`, lint/build/smoke passed; Knowledge Dashboard `62 passed`, lint/build/smoke passed. `product_readiness_score.py --json` reports score `96`, state `blocked`, local blockers `0`, agent tasks `0`, publish blockers `1`, external blockers `1`, clean worktree, fresh current-head QC artifacts, graph current at `93c8c336`, and no open PRs. Remaining boundaries are explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest Hanwoo schedule narrow-mobile follow-up: 2026-06-07 T-1503 follow-up at local code commit `b3af6219`. The first T-1503 candidate fixed the 390px date-cell target size but could let 44px date buttons overlap on very narrow 320px screens. `ScheduleTab.js` now wraps the weekday/date grids in an internal horizontal scroll container, uses mobile `px-1 py-3 sm:p-3`, gives both grids `min-w-[314px]`, and uses `gap-px sm:gap-2` so seven 44px cells plus gaps do not overlap. `home-market-copy.test.mjs` locks the updated layout contract. Browser QA measured mobile-390, mobile-360, mobile-320, and desktop-1280 with `xOverflow=0`, `allDateButtonsPass44=true`, and `noDateButtonOverlap=true`; min date widths were `48.844`, `44.562`, `44`, and `70.562`, with screenshots in `output/playwright/hanwoo-t1503-schedule-calendar-*` and measurement JSON `.tmp/hanwoo-t1503-schedule-calendar-browser.json`. Verification passed focused home-market source test (`56 passed`), Hanwoo project QC (`530 passed`, lint/build/smoke passed), `git diff --check` with CRLF warnings only, graph refresh current at `b3af6219`, and advisory code-review gate WARN (`risk_score=0.30`) covered by focused/browser/project QC. Worktree is clean and `main` is ahead of `origin/main` by `636`. No push was performed and T-251 was not retried.

> Latest Hanwoo schedule date-cell touch-target refresh: 2026-06-07 T-1503 at local code commit `5b693ec8`. Baseline authenticated mobile Schedule tab audit at `390x844` found calendar date buttons below the 44px app target (`min_date_button_width=40.56`, `small_date_button_count=30`, `schedule_small_target_count=32`, horizontal overflow `0`). `ScheduleTab.js` now keeps the compact 7-column calendar but uses mobile `gap-1 sm:gap-2`, `min-w-11` date/placeholder cells, and a `min-h-11` schedule add button. Candidate browser QA measured date buttons at `44x78`, `small_date_button_count=0`, `schedule_small_target_count=1` (shared degraded-state refresh button only), `xOverflow=false`, and bad responses `0`. `home-market-copy.test.mjs` and `tab-header-accessibility.test.mjs` lock the source contracts. A/B selected `adopt_candidate` (`score_delta=0.6075875246548323`). Verification passed related source tests (`64 passed`), Hanwoo project QC (`530 passed`, lint/build/smoke passed), `git diff --check`, graph refresh, and advisory code-review gate WARN (`risk_score=0.30`) covered by focused/browser/project QC. Refresh current-head readiness/release-selector evidence before release claims because HEAD advanced past the T-1501 canonical full-workspace artifact. No push was performed and T-251 was not retried.

> Latest Shorts Manager runtime dependency contract: 2026-06-07 T-1502 at local code commit `84581170`. `workspace/execution/pages/shorts_manager.py` imports Streamlit, but the workspace dependency list did not explicitly include it, leaving a fresh install at risk of producing a non-runnable Shorts Manager even though generated/output workflows exist. `workspace/pyproject.toml` now declares `streamlit>=1.58.0`; `uv.lock` resolves `streamlit 1.58.0` and links it into the local `vibe-workspace` dependencies; `workspace/tests/test_shorts_manager.py` locks the dependency contract. Verification passed focused Shorts Manager pytest (`30 passed`), targeted Ruff, `py_compile`, `git diff --check` with CRLF warnings only, `py -3.13 -m uv lock --check`, A/B `adopt_candidate` (`score_delta=0.9`), and Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass). A separate unstaged Hanwoo `ScheduleTab.js` calendar-grid WIP is visible and must stay separate. Refresh graph/current readiness after the worktree is clean. No push was performed and T-251 was not retried.

> Latest current-head completion audit refresh: 2026-06-07 T-1501 at local HEAD `5ea26d84` after T-1500/T-1500b. Full canonical active-project QC passed: Blind-to-X `1842 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `530 passed`, lint/build/smoke passed; Knowledge Dashboard `62 passed`, lint/build/smoke passed. `session_orient.py --json` confirmed clean worktree, graph current at `5ea26d84`, no open PRs, and `main` ahead of `origin/main` by `625`. Product readiness remains `96`, state `blocked`, with local blockers `0`, agent tasks `0`, publish blockers `1`, and external blockers `1`. Release packet is `ready_for_authorization` but not allowed without explicit user authorization. Completion audit remains `10/14` with `4` blocked items; selector returned `blocked_publish_only`. Remaining boundaries are explicit push/current-head GitHub Actions (`root-quality-gate`, `active-project-matrix`) plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest Shorts Manager mobile touch-target refresh: 2026-06-07 T-1500 at local code commit `c439d079`. Baseline mobile Streamlit list tabs and app buttons were measured by disabling the candidate CSS in the same DOM: visible overflow `3`, `listTabAllPass44=0`, `tooSmallAppButtonCount=55`; the final `역사/고고학` tab reached `right=423` on a `390px` viewport and visible app buttons were 40px tall. `shorts_manager.py` now injects mobile-only CSS so tab lists wrap, tabs expose at least `44x44`, and Streamlit button/form-submit targets are at least 44px tall. `test_shorts_manager.py` locks the source contract and direct CSS helper rendering. Candidate browser QA measured visible overflow `0`, `listTabAllPass44=1`, `tooSmallAppButtonCount=0`, console warnings/errors `0`, and desktop tabs stayed one line with overflow `0`. A/B selected `adopt_candidate` (`score_delta=1.0`). Verification passed focused Shorts Manager pytest (`29 passed`), related Shorts Manager pytest (`49 passed`), targeted Ruff, Ruff format check, `py_compile`, `git diff --check`, Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), and code-review gate advisory WARN (`risk_score=0.30`) covered by source/helper/browser/project QC. Refresh graph/current orientation and full canonical active-project QC before current-head release claims. No push was performed and T-251 was not retried.

> Latest Hanwoo settings switch touch-target refresh: 2026-06-07 T-1500b at local code commit `410d46a6`. Baseline mobile Settings inventory found all 10 `role=switch` controls below 44px height: dark mode `52x28`, widget toggles `44x24`, `switch_pass44_count=0/10`, `min_switch_height=24`, with covered switches `0`, horizontal overflow `0`, and console errors `0`. `SettingsTab.js` now separates switch hit areas from visual tracks: theme switch button `52x44` with internal `52x28` track, widget switch buttons `44x44` with internal `44x24` tracks. `settings-tab-accessibility.test.mjs` locks the source contract. Candidate browser QA measured 10 switches, all pass 44px, min `44x44`, covered switches `0`, xOverflow `0`, and page/request/browser errors `0`. Evidence uses `.tmp/hanwoo-t1500b-*` plus `output/playwright/hanwoo-t1500b-*`. A/B selected `adopt_candidate` (`score_delta=2.8541666666666665`). Verification passed full Hanwoo Node tests (`530 passed`), postcommit Hanwoo project QC (`530 passed`, lint/build/smoke passed), `git diff --check`, and staged code-review gate advisory WARN (`risk_score=0.30`) covered by source/browser/project QC. Current HEAD later advanced to concurrent Shorts Manager T-1500 (`c439d079`), so refresh graph/current full canonical QC before current-head release claims. No push was performed and T-251 was not retried.

> Latest full launch-gate refresh: 2026-06-07 T-1499 at local HEAD `25776715` after T-1498. Full canonical active-project QC passed: Blind-to-X `1842 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `530 passed`, lint/build/smoke passed; Knowledge Dashboard `62 passed`, lint/build/smoke passed. Product readiness is `96` with local blockers `0`, agent tasks `0`, clean worktree, fresh QC artifacts, graph current, and no open PRs. Browser QA inventory coverage is `4/4` with fresh usable/nonblank screenshots `4/4`. Release packet is `ready_for_authorization`; completion audit is `10/14` with `4` blocked items; selector returned `blocked_publish_only`. Remaining release boundaries are explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest Hanwoo schedule touch-target refresh: 2026-06-07 T-1498 at local code commit `a81bf8b9`. Schedule month navigation buttons now keep compact chevrons but expose 44px touch targets (`min-h-11 min-w-11`) instead of the baseline 24px icon-only hit area. Candidate browser QA measured two previous/next targets on both mobile and desktop, each `44x44`, `allPass44=true`, horizontal overflow `0`, and no browser messages. Evidence uses `.tmp/hanwoo-t1498-*` plus `output/playwright/hanwoo-t1498-schedule-touch-{mobile,desktop}.png`. A/B selected `adopt_candidate` (`score_delta=1.611111111111111`). Verification passed Hanwoo Node tests (`530 passed`), lint, `git diff --check`, Hanwoo project QC (`530 passed`, lint/build/smoke passed), and code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project QC. No push was performed and T-251 was not retried.

> Latest Shorts Manager code wrapping refresh: 2026-06-07 T-1497 at local code commit `ac332761`, with direct helper-kwargs test follow-up `101ddde2`. Browser-click baseline found long path/code blocks wider than their card on mobile (`mobile_path_code_over_parent_count=2`, max code width `706.8px`, ratio `2.3943`). `shorts_manager.py` now renders the config path, resolved project Python path, and output video path through `_render_wrapped_code()` using `st.code(..., wrap_lines=True)`. Candidate QA measured over-parent path code blocks `0`, max code width `243.609375px`, ratio `0.7982`, horizontal overflow `0`, and no console/page/request failures. Verification passed focused Shorts Manager pytest (`3 passed`), related Shorts Manager pytest (`47 passed`), targeted Ruff, Ruff format check, `py_compile`, `git diff --check`, Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), staged/commit code-review gate advisory WARN (`risk_score=0.35`) covered by focused/related/browser/project QC, and A/B `adopt_candidate` (`score_delta=0.5767663812979826`). No push was performed and T-251 was not retried.

> Latest Hanwoo cattle registration modal refresh: 2026-06-07 T-1496 at local code commit `0eca644c`. Browser-click QA found the setup-progress cattle-registration modal focused the name input but placed save/cancel below the first mobile viewport (`save top=1143`, dialog bottom `1262` on an `844px` viewport). `CattleForm` now bounds the dialog to the viewport, makes the form fields internally scrollable, and keeps save/cancel sticky with safe-area bottom padding. Candidate QA measured dialog bottom `844`, form `overflowY=auto`, save `701..775`, `saveInitiallyVisible=true`, `saveVisibleAfterScroll=true`, horizontal overflow `0`, and console errors `0`. Verification passed focused CattleForm tests (`20 passed`), full Hanwoo tests (`530 passed`), lint, Hanwoo project QC postcommit (`530 passed`, lint/build/smoke passed), graph refresh, staged/commit code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project QC, and A/B `adopt_candidate` (`score_delta=0.785714`). No push was performed and T-251 was not retried.

> Latest Shorts Manager layout refresh: 2026-06-07 T-1495 at local code commit `4b1e03db`. Browser-click QA found content-list card action buttons squeezed to `32px` wide and up to `215px` tall because the five-button action group was nested inside a narrow side column. The card now renders header first and then the action row across the full card width, improving desktop action min width to `167px`, max height to `40px`, tall buttons to `0`, with mobile horizontal overflow `0`. Verification passed focused Shorts Manager pytest (`45 passed`), targeted Ruff, `py_compile`, `git diff --check`, Streamlit browser QA, Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), graph refresh, staged code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project QC, and A/B `adopt_candidate` (`score_delta=1.4803779069767442`). No push was performed and T-251 was not retried.

> Latest Shorts metadata coverage follow-up: 2026-06-07 T-1492 at local code commit `ece7251c`. The Shorts Manager helper-only test harness now stubs YouTube uploader with the real `build_shorts_upload_metadata` helper and expects the new copy-ready description/API-safe tag contract. Verification passed focused workspace pytest (`74 passed`), Ruff check, Ruff format check, `git diff --check`, and staged code-review gate advisory WARN (`risk_score=0.30`) covered by focused tests. No push was performed and T-251 was not retried.

> Latest Hanwoo degraded-read refresh: 2026-06-07 T-1490 at local code commit `c83d0895`. Expected read-only fallback paths no longer use `console.error` for known degraded initial reads, preventing the Next dev overlay from covering the otherwise usable authenticated mobile dashboard shell under the external Supabase degraded state. Verification passed focused Hanwoo source tests (`15 passed`), related source tests (`71 passed`), full Hanwoo tests (`529 passed`), lint, Hanwoo project QC (`529 passed`, lint/build/smoke passed with accepted smoke warnings), pre-commit code-review gate advisory WARN (`risk=0.60`) covered by focused/browser/project QC, browser A/B QA (`console_errors 9 -> 0`, dev overlay button `1 -> 0`, horizontal overflow `0`), and A/B `adopt_candidate` (`score_delta=0.9166666666666666`). No push was performed and T-251 was not retried.

> Latest Shorts YouTube metadata refresh: 2026-06-07 T-1491 at local code commit `2174fba4`. YouTube upload metadata now generates copy-ready Shorts descriptions and API-safe tags from title/topic/channel/hook/duration instead of bare hashtag-only descriptions. The helper strips forbidden `<`/`>` characters, truncates descriptions to 5000 UTF-8 bytes, dedupes tags, and counts spaced tags according to the YouTube Data API 500-character rule. Verification passed focused workspace pytest (`54 passed`), Ruff check, Ruff format check, `git diff --check`, staged code-review gate advisory WARN (`risk_score=0.35`) covered by focused tests, and graph refresh. No push was performed and T-251 was not retried.

> Latest full launch-gate refresh: 2026-06-07 T-1489 full canonical active-project QC passed at pre-context local head `c23d98f0` after T-1486/T-1487/T-1488 and closeout cleanup. Results: Blind-to-X `1842 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `529 passed`, lint/build/smoke pass with accepted smoke warnings; Knowledge Dashboard `62 passed`, lint/build/smoke pass. Product readiness is `96` with local blockers `0`, agent tasks `0`, fresh project QC artifacts, clean worktree, graph current, and no open PRs. Release packet is `ready_for_authorization`; launch objective audit local coverage is complete; completion audit is `10/14` with `4` blocked items; selector returned `blocked_publish_only`. Remaining release boundaries are explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest Hanwoo calving refresh: 2026-06-07 T-1488 at local code commit `0c1a0536`. The Calving tab now uses the shared action-oriented empty state when there are no pregnant cows and routes its registration CTA through the existing `add-cattle` quick action. Verification passed focused Hanwoo source tests (`25 passed`), `npm.cmd run lint`, `git diff --check` with CRLF warnings only, Hanwoo project QC (`529 passed`, lint/build/smoke passed with accepted 405/auth smoke warnings), staged code-review gate advisory WARN (`risk_score=0.40`) covered by focused/project QC, and A/B `adopt_candidate` (`score_delta=0.8888888888888888`). No push was performed and T-251 was not retried.

> Latest Shorts Manager refresh: 2026-06-07 T-1487 at local code commit `356e1be2`. External actions now explain their exact lock reason: YouTube setup, per-item YouTube retry, and Notion sync each get a specific disabled help/caption path instead of one broad upload flag. Verification passed focused Shorts Manager pytest (`24 passed`), Ruff, Ruff format check, `py_compile`, `git diff --check`, Streamlit mobile browser screenshot QA for both YT retry and Notion sync lock copy, staged/pre-commit code-review gate advisory WARN (`risk_score=0.40`) covered by focused/browser tests, and A/B `adopt_candidate` (`score_delta=0.7777777777777778`). No push was performed and T-251 was not retried.

> Latest Blind-to-X output-quality refresh: 2026-06-07 T-1486 at local code commit `9d94fbe9`. Notion review output now converts selection/readiness metrics into concrete reviewer edit plans. `pipeline/notion/_upload.py` builds `edit_plan` for failed gates, low publish suitability, high recent similarity, warnings, missing drafts, and clean winners, and surfaces it as `수정 플랜: ...` in both the Notion memo and top-level `검토 요약` bullets without schema churn. `test_notion_upload.py` locks warning-plan visibility, clean copy-ready next action, failed-gate repair action, high-similarity rewrite, and the guard that quality score alone is not copy-ready when selection confidence is low. Verification passed focused Notion tests (`51 passed`), related Blind-to-X quality/Best-of-N/schema tests (`113 passed`), full Blind-to-X project QC (`1842 passed`, `9 skipped`, lint pass), targeted Ruff, `git diff --check` with CRLF warnings only, staged code-review gate advisory WARN (`risk_score=0.55`) covered by focused/related/project tests, and A/B candidate B via `.tmp/ab-manifest-blind-t1486-notion-edit-plan.json`. No push was performed and T-251 was not retried. Pre-close verification returned a clean worktree after later local follow-up commits landed; refresh project/full QC artifacts before release-current claims.

> Latest Shorts Manager refresh: 2026-06-07 T-1485 at local code commit `9f73633d`. Manual review queue items now treat successful renders with failed YouTube uploads as warning work instead of healthy manual review. `content_db.py` maps `youtube_status='failed'` to `업로드 오류 확인 후 재시도` after preserving missing-video critical priority, and `shorts_manager.py` surfaces `업로드 오류: ...` inside manual review cards. Verification passed focused pytest (`2 passed`), related workspace tests (`67 passed`), Ruff, `py_compile`, `git diff --check` with CRLF warnings only, browser baseline/candidate QA with no console/request failures and no horizontal overflow (`output/playwright/shorts-manager-t1485-upload-review-baseline.png`, `output/playwright/shorts-manager-t1485-upload-review-candidate.png`, `.tmp/shorts-manager-t1485-upload-review-{baseline,candidate}.json`), Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), graph refresh, and A/B `adopt_candidate` (`score_delta=0.7777777777777778`). Code-review gate WARN (`risk_score=0.30`) is covered by focused/related/browser/project QC. No push was performed and T-251 was not retried.

> Latest Shorts Manager refresh: 2026-06-07 T-1484 at local code commit `9ce63f53`. Thumbnail preview now uses a guarded helper that ignores empty/missing paths, preserves valid previews, and reports corrupt image render failures with Korean warning copy plus the file path instead of breaking the manager view. Verification passed focused Shorts Manager pytest (`22 passed` with `--no-cov`), Ruff, `py_compile`, `git diff --check` with CRLF warnings only, browser QA on populated Streamlit data with no traceback/console/request failures and no horizontal overflow (`output/playwright/shorts-manager-t1484-thumbnail-candidate.png`, `.tmp/shorts-manager-t1484-thumbnail-candidate.json`), Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint pass), and A/B `adopt_candidate` (`score_delta=0.7777777777777778`). No push was performed and T-251 was not retried.

> Latest Hanwoo feed setup refresh: 2026-06-07 T-1483 at local code commit `580eb71e`. Feed now shows an action-oriented empty state when no buildings exist, and its add-building CTA routes through the existing add-building quick action so Settings opens the building form and focuses `#building-name`. Verification passed focused source tests (`74 passed`), full Hanwoo tests (`528 passed`), lint, Hanwoo project QC (`528 passed`, lint/build/smoke passed), browser QA for mobile Feed-to-Settings focus with no overflow/tabbar overlap, graph detect risk `0.00`, advisory code-review gate WARN covered by focused/browser/project QC, and A/B `adopt_candidate` (`score_delta=0.75`). No push was performed and T-251 was not retried.

> Latest Blind-to-X output-quality refresh: 2026-06-07 T-1482 at local code commit `9d70f2af`. Notion review `selection_quality_summary` now starts with an actionable publish-readiness verdict (`바로 게시 가능`, `검수 후 게시`, or `수정 필요`) before preserving the existing Best-of-N metrics, so reviewers can decide whether to publish, inspect, or edit without decoding raw scores. Verification passed focused Notion tests (`49 passed`), related Best-of-N/Notion/output-quality tests (`107 passed`), Blind-to-X project QC (`1840 passed`, `9 skipped`, lint pass), Ruff targeted/full project lint, `git diff --check` with CRLF warnings only, and A/B `adopt_candidate` (`score_delta=0.8`). Stage-scoped code-review gate WARN is covered by focused/related/project QC. No push was performed and T-251 was not retried.

> Latest Hanwoo settings refresh: 2026-06-07 T-1481 at local code commit `078908e9`, with final context/goal head `e23e0946`. Settings home-widget visibility toggles now use a compact responsive grid with stable tile dimensions, right-aligned toggles, and wrapped Korean label/description text so controls do not collide or overflow on narrow Settings layouts. Verification passed Hanwoo Node tests (`528 passed`), Hanwoo project QC (`528 passed`, lint/build/smoke passed), Playwright mobile hit-target QA, A/B `adopt_candidate` (`score_delta=0.8888888888888888`), `git diff --check`, and full active-project QC refreshed at `e23e0946`. Code-review gate WARN is advisory and covered by focused/browser/project/full QC. No push was performed and T-251 was not retried.

> Latest full launch-gate refresh: 2026-06-07 active-project artifact at `e23e0946`. Canonical active-project QC passed: Blind-to-X `1838 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `528 passed`, lint/build/smoke pass; Knowledge `62 passed`, lint/build/smoke pass. Product readiness is `96` with local blockers `0`; release packet is `ready_for_authorization`; launch objective audit local coverage is complete; completion audit is `10/14` with `4` blocked items. This supersedes the earlier `2e62c633` full-QC note for local release-readiness evidence. Remaining release boundaries are explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E. No push was performed and T-251 was not retried.

> Latest Hanwoo diagnostics refresh: 2026-06-07 T-1479. The diagnostics raw-data inspector no longer renders a bare `"null"` string under the known external Supabase DB failure. It now shows Korean failure/empty states with `role="status"`, disables the model selector while DB diagnostics are unavailable, and links the selector to the DB-failure note. Verification passed Hanwoo tests (`528 passed`), lint/build/smoke via Hanwoo project QC, browser QA for authenticated `/admin/diagnostics` plus dashboard-return click, code-review graph detect (`risk_score=0.00`), and A/B `adopt_candidate` (`score_delta=0.8333333333333334`). Code-review gate WARN (`risk_score=0.55`) is covered by focused tests/browser/project QC/graph detect. No push was performed and T-251 was not retried.

> Latest launch-audit refresh: 2026-06-07 T-1478 at local code commit `812a8c28`. `launch_objective_audit.py` now separates publish/current-head Actions boundaries from local product-readiness blockers when publish blockers fully account for the workspace blocker count and no local/agent blockers exist. Real local blockers still remain partial. Verification passed focused launch-audit pytest (`41 passed`), related selector/launch-audit pytest (`53 passed`), Ruff, `py_compile`, `git diff --check`, runtime launch audit generation, and A/B `adopt_candidate` (`score_delta=0.75`). The previously noted Hanwoo diagnostics WIP is now handled separately as T-1479. No push was performed and T-251 was not retried.

> Latest auto-research tool refresh: 2026-06-07 T-1477 at local code commit `900ff710`. `next_experiment_selector.py --json` no longer spends up to 120s per sequential live helper by default; it now defaults live helpers to `30s` and passes a `10s` child timeout to dependency freshness inventory, returning explicit unavailable-evidence JSON when heavy inventories are slow. Verification passed focused selector pytest (`12 passed`), related selector/launch-audit pytest (`53 passed`), Ruff, `py_compile`, `git diff --check`, default CLI smoke (`56.1s`, valid JSON), and A/B `adopt_candidate` (`score_delta=0.8870967741935484`). The previously noted Hanwoo diagnostics WIP is now handled separately as T-1479. No push was performed and T-251 was not retried.

> Latest Shorts Manager refresh: 2026-06-07 T-1476 at local code commit `be0bb1db`. After T-1475 locked unready generation runs, browser QA found the Korean run-lock UI still exposed the internal issue code `channel settings missing`; known channel-readiness issue codes now render as Korean labels before captions/run-lock reasons. Verification passed focused Shorts Manager pytest (`21 passed`), Ruff, `py_compile`, `git diff --check`, browser QA with run disabled, Korean setup copy visible, no English issue-code leak, no console/request failures, no overflow, fresh screenshot `output/playwright/shorts-manager-t1476-korean-run-lock-copy.png`, code-review gate WARN (`risk_score=0.40`) covered by focused/browser tests, and A/B `adopt_candidate` (`score_delta=0.5714285714285714`). Follow-up hygiene commits updated the active goal and ignored root `test-results/` generated by Playwright (`560d238a`, `ec3fb2dd`). No push was performed and T-251 was not retried.

> Latest full launch-gate refresh: 2026-06-07 active-project artifact at `2e62c633`; current local code head `ec3fb2dd` has only workspace Shorts Manager changes covered by T-1475/T-1476 focused tests/browser QA plus root generated-artifact ignore hygiene. Canonical active-project QC passed after T-1473/T-1474 and context commits: Blind-to-X `1838 passed`, `9 skipped`, lint pass; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`, lint pass; Hanwoo `528 passed`, lint/build/smoke pass; Knowledge `62 passed`, lint/build/smoke pass. Product readiness score is `96`, state `blocked`, no relevant active-project QC staleness, no open PRs, publish blockers `1`, external blockers `1`, and graph current. Completion remains blocked only by explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251. No push was performed and T-251 was not retried.

> Latest Hanwoo payment refresh: 2026-06-07 T-1474 at local code commit `3a3bb3c8`. The subscription page no longer falls back to a hardcoded Toss test client key; it passes `isClientKeyConfigured` into `PaymentWidget`, which now shows Korean setup/unavailable copy, disables checkout until the Toss payment-method widget fires `on("ready")`, times out stale readiness, clears timers on cleanup, and links the alert to the disabled checkout button with `aria-describedby`. Official Toss JavaScript SDK docs and local `@tosspayments/payment-widget__types` confirmed the ready-event API. Verification passed Hanwoo Node tests (`528 passed`), `npm.cmd run lint`, `git diff --check`, browser QA with setup alert visible, disabled setup-needed button, `aria-describedby=payment-widget-error`, no horizontal overflow, no fallback test key, and no console/network issues, code-review gate WARN (`risk_score=0.55`) covered by focused/browser/project QC, screenshot `output/playwright/hanwoo-t1474-subscription-payment-setup-disabled.png`, and A/B `adopt_candidate` (`score_delta=1.0`). Completion remains blocked by explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251. No push was performed and T-251 was not retried.

> Latest Shorts/Hub workflow refresh: 2026-06-07 T-1473 at local code commit `ae3655c2`. Joolife Hub now passes declared Streamlit ports through to actual `streamlit run` commands, so clicking Shorts Manager Launch opens the same `8512` endpoint that Hub registers and displays. Verification passed focused Hub pytest (`47 passed`), focused Shorts Manager pytest (`19 passed`), Ruff, `py_compile`, `git diff --check`, direct Playwright click QA from Hub `8526` to Shorts Manager `8512` with console warnings/errors `0` and screenshot `output/playwright/shorts-manager-t1473-hub-port.png`, Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint passed), graph refresh, and A/B `adopt_candidate` (`score_delta=0.8333333333333334`). Completion remains blocked by explicit push/current-head GitHub Actions plus user-owned Hanwoo T-251. A newer separate local Hanwoo commit `3a3bb3c8` exists after T-1473 and must stay separate from T-1473 claims. No push was performed and T-251 was not retried.

> Previous full launch-gate refresh: 2026-06-07 T-1472 at checked local HEAD `46eb2d4c`. This is superseded by the current local HEAD `2e62c633` full canonical QC note above. No push was performed and T-251 was not retried.

> Latest output-quality refresh: 2026-06-07 T-1471 at local code commit `998c5f6a`. Shorts Manager now launches `shorts-maker-v2` with the project `.venv`/`venv` Python when available instead of the workspace/root interpreter, preserving the dependency set used by project QC for user-triggered generation. The dashboard also shows the resolved Python path, and fallback to the current interpreter remains covered when no project venv exists. Verification passed `py_compile`, Ruff, `git diff HEAD~1 --check`, focused pytest with repo-local basetemp (`19 passed`), Shorts Maker V2 project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint passed), code-review gate pass (`risk_score=0.0`), headless Streamlit browser QA screenshot `.tmp/shorts-manager-browser-qa.png`, and A/B `adopt_candidate` (`score_delta=0.9090909090909091`). The last full canonical active-project QC remains the T-1468 refresh at checked local HEAD `ab103c6e`; refresh full canonical QC before release-current claims because T-1470/T-1471 landed later. Completion remains blocked because current-head Actions require explicit push/user push and Hanwoo T-251 remains external/user-owned. No push was performed and T-251 was not retried.

> Latest Hanwoo browser-workflow refresh: 2026-06-07 T-1470 at local code commit `e6474ff3`. Mobile baseline proved the default Next dev indicator/devtools portal could intercept the fixed bottom `사료` tab click; candidate moved `devIndicators.position` to `top-right`, and browser QA verified the `사료` tab click succeeds with selected copy `사료 탭 열기, 현재 선택됨`, `scrollY=0`, horizontal overflow `0`, dev overlay/tabbar overlap `false`, and fresh nonblank screenshot `output/playwright/hanwoo-t1470-dev-indicator-tab-click-mobile.png`. Verification passed Hanwoo Node tests (`527 passed`), Hanwoo project QC (`527 passed`, lint/build/smoke passed), `git diff --check`, browser QA inventory, and A/B `adopt_candidate` (`score_delta=1.0`). The last full canonical active-project QC remains the T-1468 refresh at checked local HEAD `ab103c6e` (Blind-to-X `1838 passed`, `9 skipped`; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`; Hanwoo `526 passed`; Knowledge `62 passed`; lint/build/smoke gates passed where applicable). Completion remains blocked because current-head Actions require explicit push/user push and Hanwoo T-251 remains external/user-owned. Current worktree cleanliness must be rechecked because unrelated unstaged Shorts Manager changes were present after T-1470. No push was performed and T-251 was not retried.

| ID | Task | Owner | Priority | Auto | Created |
| --- | --- | --- | --- | --- | --- |
| T-251 | `[hanwoo-dashboard]` Run Prisma 7 live CRUD E2E once a real Supabase `DATABASE_URL` is configured. Root cause: Supabase password desynchronization. Retried on 2026-05-20, 2026-05-28 and again during an earlier continuation with `npm.cmd run db:prisma7-test -- --live`; local Prisma/client/adapter checks passed (`15 passed`) but live connection health still failed with Prisma `P2010`, raw code `XX000`, message `(ENOTFOUND) tenant/user postgres.fuemeqmigptwfzqvrpjf not found`. 2026-06-06 launch loops through T-1385 evidence refresh did **not** retry the live check, and the 2026-06-07 T-1489 launch-gate refresh also did **not** retry it because no credential reset/resync was reported. Latest full-workspace evidence at checked local HEAD `c23d98f0` showed canonical active-project QC PASS (Blind-to-X `1842 passed`, `9 skipped`; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`; Hanwoo `529 passed`; Knowledge `62 passed`; lint/build/smoke gates passed where applicable), readiness score `96`, local blockers `0`, publish blockers `1`, external blockers `1`, clean worktree, current code-review graph, and no open PRs. Only this live Supabase CRUD check remains for Hanwoo readiness; publish/current-head Actions are a separate workspace release boundary. **Action Required**: The user MUST manually reset the database password via the Supabase Dashboard (Project Settings > Database) to resynchronize the control plane pooler credentials, then update `.env` if it changes; rerun `npm.cmd run db:prisma7-test -- --live` immediately after. | User | Medium | approval | 2026-05-28 |


## IN_PROGRESS

| ID | Task | Owner | Started | Notes |
|---|---|---|---|---|


## DONE (Latest 5)

| T-1558 | `[workspace/roi-dashboard]` Polished the ROI Dashboard mobile output surface. The page now uses compact Korean operator copy, 44px-safe number input targets, hidden Plotly modebars, and Korean breakeven-view copy; focused tests and mobile browser QA lock the behavior. No push and no T-251 retry. | Codex | 2026-06-08 |
| T-1557 | `[workspace/shorts-manager]` Hardened Streamlit expander summaries for mobile touch targets. Expander summaries now have `min-height: 44px !important`, flex display, and centered content; source/rendered CSS tests lock the selector. Browser QA at `390x844` measured 13 expander summaries with min height `44`, no horizontal overflow, console errors `0`, and failed requests `0`. No push and no T-251 retry. | Codex | 2026-06-08 |

| T-1556 | `[auto-research]` Persisted selector inventory helper outputs. `next_experiment_selector.py` now writes GitHub/browser/dependency helper outputs to expected `.tmp` cache artifacts and uses longer bounded helper timeouts, so fallback evidence is available after live helper runs. Tests assert `--output` paths for all three inventories. No push and no T-251 retry. | Codex | 2026-06-08 |

| T-1555 | `[workspace/shorts-manager]` Hardened Streamlit select dropdown options for mobile touch targets. BaseWeb virtual dropdown options now have `min-height: 44px`, `display: flex`, and centered content; source and rendered CSS tests lock the contract. Browser QA at `390x844` measured 6 options with min height `44`, no horizontal overflow, console errors `0`, and failed requests `0`. No push and no T-251 retry. | Codex | 2026-06-08 |

| T-1554 | `[auto-research]` Added direct output-quality objective coverage to the launch manifest. The audit now checks project good-output criteria, external benchmark signals, Good vs Bad output distinction, and iterative baseline/candidate/A-B evidence, with tests for complete and blocked evidence. Completion audit now includes this item and reports `11/15` complete with the same 4 publish/T-251 blockers. No push and no T-251 retry. | Codex | 2026-06-08 |


| T-1444b | `[blind-to-x/source-browser]` Added `main.py --source all` as a source-preflight alias for all configured `input_sources`, matching the documented all-source operator workflow while preserving `multi`. Baseline `main.py --source all --source-preflight` exited `1` with `Skipping unsupported source preflight targets: all` and `No supported source preflight targets resolved for source=all`; candidate expands to 4 sources and exits `0`. `pipeline/bootstrap.py` now treats `multi`/`all` as configured-source aliases, `pipeline/cli.py` documents the alias, and README/directive preflight docs distinguish main CLI `all` from standalone helper all-known-source probing. Tests cover input-source and preflight resolution for `all`. Live browser preflight verified 4 sources, ready `ppomppu`/`jobplanet`, blocked Blind `403` and FMKorea `430`; click-through preflight verified detail paths for `ppomppu` and `jobplanet` with one ready-source Ppomppu console warning. Verification passed focused tests (`41 passed`), related source/main tests (`69 passed`), `py_compile`, Ruff check/format, live preflight, live click-through preflight, blind-to-x project QC (`1832 passed`, `9 skipped`, lint passed), diff-check, staged code-review gate advisory WARN (`risk_score=0.40`) covered by focused/live/project gates, and A/B `adopt_candidate` (`score_delta=6.846153846153846`). Code commit `8f524cdb` is local only. No push was performed and T-251 was not retried. | Codex | 2026-06-07 |
| T-1438b | `[shorts-maker-v2/dashboard]` Made the generated dashboard and tracked sample browser-QA clean on mobile. Baseline Playwright CLI at 390px showed the sample overflowed horizontally (`scrollWidth=630`, `clientWidth=390`, `bodyScrollWidth=598`) and the first desktop load emitted a favicon 404. `utils/dashboard.py` now emits viewport metadata, a data favicon, constrained main layout, responsive summary cards, an accessible scroll-contained table wrapper, and the same full HTML shell for empty dashboards. `docs/dashboard.html` mirrors the runtime contract, and `tests/unit/test_dashboard.py` locks viewport/favicon/table/grid/sample contracts. Candidate browser QA verified `scrollWidth=390`, `bodyScrollWidth=390`, no horizontal overflow, grid summary cards, contained table overflow, no Korean mojibake, console warnings/errors 0, and screenshot `output/playwright/shorts-dashboard-t1438-responsive.png`. Verification passed focused dashboard pytest (`13 passed`), Ruff, `py_compile`, venv CLI dashboard generation, shorts-maker-v2 project QC (`1639 passed`, `12 skipped`, `29 warnings`, lint passed), diff-check, code-review gate advisory WARN (`risk_score=0.40`) covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=1.125`). Commit `70cb64dd` is local only; recorded as T-1438b because a concurrent Hanwoo context/code bundle was already staged. No push was performed and T-251 was not retried. | Codex | 2026-06-07 |
| T-1422b | `[blind-to-x/source-browser]` Accepted explicit all-source and JSON-compatible standalone source preflight commands. Baseline operator command `scripts/source_browser_probe.py --source all --click-through --json` failed because `--source` only accepted concrete source names and the helper rejected `--json` despite always emitting JSON. `scripts/source_browser_probe.py` now accepts `all`, `auto`, and `multi` aliases for every known source, dedupes alias-expanded sources with repeated concrete sources, and accepts `--json` as a no-op compatibility flag. README and `directives/x_content_curation.md` document the explicit all-source helper command. Live Playwright preflight verified all 4 sources with click-through: JobPlanet and Ppomppu ready, Blind `403` and FMKorea `430` blocked with problem actions, and recommended source `ppomppu`. Verification passed focused tests (`23 passed`), `py_compile`, Ruff check/format check, diff-check, live all-source click-through preflight, blind-to-x project QC (`1823 passed`, `9 skipped`, lint passed), code-review gate advisory WARN (`risk_score=0.40`) covered by focused/live/project gates, and A/B `adopt_candidate` (`score_delta=1.6`). Commit `51e4f83d` is local only. No push was performed and T-251 was not retried. | Codex | 2026-06-07 |
