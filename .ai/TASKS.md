# TASKS - AI Kanban Board

> Read this file at session start and update it before ending the session.

## TODO

| ID | Task | Owner | Priority | Auto | Created |
| --- | --- | --- | --- | --- | --- |
| T-251 | `[hanwoo-dashboard]` Run Prisma 7 live CRUD E2E once a real Supabase `DATABASE_URL` is configured. Root cause pinpointed 2026-05-11 and rechecked 2026-05-12: `projects/hanwoo-dashboard/.env` has the Supabase template URL with `YOUR_PASSWORD` placeholder still unreplaced — host/user are real, only the password literal needs substitution from Supabase console. Latest `npm run db:prisma7-test -- --live` again failed at the intended config guard. | User | Medium | 🔴 approval | 2026-05-08 |
| T-282 | `[workspace]` Human review/merge PR #35. Latest recheck after the user asked to continue: the PR branch is still `a663565`, includes the T-280 fix and `origin/main`, and all GitHub checks are green. GitHub still reports `reviewDecision: REVIEW_REQUIRED` / `mergeStateStatus: BLOCKED`; active auth is the PR author, so a non-author reviewer must approve before merge. | User | High | 🔴 approval | 2026-05-12 |

## IN_PROGRESS

| ID | Task | Owner | Started | Notes |
|---|---|---|---|---|

## DONE (Latest 5)

| ID | Task | Completed By | Completed |
|---|---|---|---|
| T-294 | `[knowledge-dashboard]` Surfaced Agent Skill health in the operations console. Feature commit `ef94a7d` adds `execution/skill_lint.py`, focused tests, a secured `/api/data/skills` dashboard route, and an Agent skill health section inside `ProductReadinessPanel`. The linter checks active `.agents/skills/**/SKILL.md` files for frontmatter, name/description quality, trigger guidance, duplicate names, and local reference drift while excluding `_archive` by default. `scripts/sync_data.py` now regenerates `data/skill_lint.json`, and README documents `python execution/skill_lint.py`. Current local skill health: `warn`, score `37`, 42 active skills, 21 healthy, 63 warnings, 0 errors. Verification: `workspace/tests/test_skill_lint.py` + product-readiness tests `6 passed`; knowledge-dashboard `npm test` `3 passed`; `npm run lint` passed; `npm run build` passed and includes `/api/data/skills`; targeted Ruff passed; Playwright smoke confirmed the UI renders the new section. Pre-commit graph gate emitted advisory WARN risk=0.40 from heuristic test-gap mapping despite direct coverage. | Codex | 2026-05-13 |
| T-293 | `[knowledge-dashboard]` Added a product operations console and deterministic product-readiness scoring. Feature commit `b81d3e2` adds `execution/product_readiness_score.py`, focused Python tests, a secured `/api/data/readiness` dashboard route, a new default `운영 콘솔` tab, and `ProductReadinessPanel` showing overall release score, per-project readiness, dirty files, open blockers, docs coverage, env checks, and next actions. `scripts/sync_data.py` now regenerates `data/product_readiness.json`, and README documents the refresh command. Latest local score after final regeneration: overall `80` / `blocked`, primarily from T-251 Supabase placeholder and T-282 PR review. Verification: product-readiness tests `3 passed`; knowledge-dashboard `npm test` `3 passed`; `npm run lint` passed; `npm run build` passed; targeted Ruff passed; `git diff --cached --check` clean. Pre-commit graph gate emitted advisory WARN risk=0.40 from heuristic test-gap mapping despite direct coverage. | Codex | 2026-05-13 |
| T-292 | `[blind-to-x]` Clarified source expansion routing and improved review-only generated artifact quality. Feature commit `20c398d` changes CLI default `--source` from ambiguous `blind` to `auto`, adds explicit `multi`, preserves explicit single-source runs, and documents the new source modes. Review-only generation now uses `quality_gate.review_only_max_retries` default `1` while normal generation uses `quality_gate.max_retries` default `2`, so the main Notion review-queue path can regenerate once when `DraftQualityGate` requests retry. Verification: focused `test_main.py`/`test_process_stages.py` `50 passed`; broader source/quality slice `119 passed`; full `blind-to-x` unit suite `1554 passed, 1 skipped`; project QC passed with `TEMP`/`TMP` redirected to project `.tmp`; lint passed. Pre-commit graph gate advisory WARN risk=0.40 was heuristic test-gap noise with direct coverage present. | Codex | 2026-05-13 |
| T-291 | `[workspace]` GitHub Actions / CI hardening pass. Fixed a 🔴 silent failure in `.github/dependabot.yml` — all `directory:` paths pointed at root-level `/blind-to-x`, `/shorts-maker-v2`, `/hanwoo-dashboard`, `/word-chain` while actual repo layout is `/projects/<name>`. Corrected every path and added the missing `/projects/knowledge-dashboard` npm entry (frontend matrix had it but Dependabot didn't). Upgraded `actions/checkout@v4 → @v5` and `actions/setup-python@v5 → @v6` across `root-quality-gate.yml`, `full-test-matrix.yml`, `dependabot-auto-merge.yml`. Added workflow-level `permissions: contents: read` (minimum privilege baseline), `concurrency` groups (`cancel-in-progress` only on `pull_request` so `main` push completes), and `timeout-minutes` on every job (15/20/25 for compute jobs, 5 for summary/auto-merge). `dependabot/fetch-metadata@v3` and `actions/setup-node@v6` left in place. Verification: PyYAML parsed all 4 files OK; manual ecosystem-to-directory mapping confirmed every entry now points to a real manifest path. Net diff: 4 files, +65 −16 lines. No commit yet; recorded under `[ai-context]` session bundle. | Claude Code (Opus 4.7 1M) | 2026-05-13 |
| T-290 | `[shorts-maker-v2]` Strengthened `gate_scene_qc` against image/audio/timing weaknesses that T-288's strict/lenient/off framework alone could not catch. Added `_check_audio_integrity` (file + size + `duration_sec >= 0.5s`) and `_check_visual_integrity` (Pillow decode + ≥540px for images / ffprobe for video) — both fold into the existing `audio_ok` / `visual_ok` essential checks so lenient mode still gates on them. Added `chars_per_sec_ok` non-essential check (1.5–10 KO chars/sec — flags TTS truncation: 200 chars in 5s = 40 c/s impossible). Expanded `_FORBIDDEN_CTA` from a 7-item English-heavy local tuple to a 14-item class constant including Korean variants (`팔로우`/`공유`/`댓글`/`지금 클릭`/`눌러주세요`); also applied to `cta` role, not just `closing`. Extracted `PipelineOrchestrator._aggregate_scene_qc_summary` as a static method and added `JobManifest.scene_qc_summary = {passed, total, unresolved, verdict}` so scenes still failing after `scene_qc_max_retries` land in `degraded_steps` with a `scene_qc_unresolved` warning instead of silently dropping. Migrated test fixtures from `_write_bytes(v.jpg, 100)` (corrupted bytes that exploited the weak visual check) to `_write_png` (real Pillow PNGs). Tests: `test_qc_step.py` 28→39 (added `TestSceneQCIntegrity` 8 cases incl. parametrized Korean CTA), `test_orchestrator_unit.py` +4 (`TestAggregateSceneQCSummary`). Verification: unit suite EXIT=0, `tests/integration` 7 passed in 113.76s, ruff clean. user_shorts_philosophy (CTA 금지 / 시간을 훔치는 이야기) preserved. `prev_scene`/`next_scene` parameters retained but still unused — left as future tone-consistency hook. | Claude Code (Opus 4.7 1M) | 2026-05-13 |

- Keep only the latest 5 items in `DONE`.
- Add newly discovered follow-up work to `TODO`.

### Smart Continue Lite - Auto Column Rules

- `🟢 safe`: can proceed immediately on a short "continue/go" command.
- `🔴 approval`: requires explicit user confirmation before execution.
- `🟢 safe` limit: at most 7 files changed, roughly 250 added lines, same feature/module boundary, and no package install, DB schema, auth/security, public API, stack, infra, or CI changes.
- If only `🔴 approval` tasks remain, stop and ask which one the user wants next.
