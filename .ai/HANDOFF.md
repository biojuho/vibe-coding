# HANDOFF - AI Context Relay

> See `SESSION_LOG.md` for recent session history and `DECISIONS.md` for stable architecture decisions.

## Current Addendum

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1785 claude-goal split-form value unknown-option boundary fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the concurrently recorded T-1784 doctor schema runtime-flag conflict fix, T-1783 doctor verify-json stale-days conflict fix, T-1782 install.sh argument-boundary fix, and root auto-research dirty-handoff work. Direct isolated CLI probes showed split-form value options followed by an unknown option-like token leaked or caused side effects: direct commands such as `contract --json-output --bogus`, `doctor --install --json-output --bogus`, `complete --audit-json --bogus`, `json --session-id --bogus`, and `pause-stale --stale-days --bogus` returned raw argparse `usage:` plus `expected one argument`; slash `invoke doctor --install --json-output --bogus` consumed `--bogus` as a literal output path and created a repo-root `--bogus` file. External reference checked: Python argparse docs describe invalid arguments/wrong argument counts as default usage-emitting parser errors and dash-leading split values as ambiguous, while `--option=value` remains the explicit value form. Root cause: direct prevalidation and custom slash parsers blocked missing split values only for known options/help tokens, so unknown dash-leading tokens either fell through to argparse or were consumed as literal values. Fixed `goal/scripts/claude_goal.py` by adding split option-token detection that preserves argparse-style negative numeric values and single `-`, then applying it to direct main value prevalidation, custom value-option parsing, and set fallback token-budget parsing. Added direct and slash regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1785-split-value-unknown-option-qa.json` records decision `adopt_candidate` and confirms all direct/slash probes return `goal error: <option> requires a value`, leave stdout empty, avoid raw argparse usage, avoid DB-open masking, avoid forbidden output-file creation, and print no traceback. Focused T-1785 regression pytest passed (`2 passed, 155 deselected`), and full isolated pytest with exported `PYTHONUTF8=1` passed (`237 passed`). Static gates passed: `python -m py_compile goal/scripts/claude_goal.py tests/test_claude_goal.py`, `python -m ruff check .`, `python -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. A `tests/test_claude_goal.py` run without exported `PYTHONUTF8` failed only on the known Korean Windows child-subprocess path mojibake assertions before the UTF-8 rerun/full run passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **Auto-research selector/debug-inventory dirty-handoff wording and publish-boundary hardening**. Continued the LLM Wiki autonomous reinforcement loop from the root workspace, but the selector correctly blocked new wiki/product edits via `dirty_worktree_handoff_current`. Hardened `.agents/skills/auto-research/scripts/debug_loop_inventory.py` and `.agents/skills/auto-research/scripts/next_experiment_selector.py` so stale dirty-handoff evidence cannot override a clean publish-only current inventory, Blind-to-X dirty blockers are emitted only when current dirty paths exist, and stale/current handoff-plan freshness is worded separately from selector acceptance. The selector blocker now says the handoff plan matches the current dirty inventory rather than saying the plan is fresh when the freshness evidence is stale-but-accepted. Added regression coverage in `workspace/tests/test_auto_research_debug_loop_inventory.py` and `workspace/tests/test_auto_research_next_experiment_selector.py`. |
| Next Priorities | Verification passed for the touched auto-research scope: focused debug-inventory pytest (`6 passed`), related auto-research/code-review tests (`72 passed`), Ruff check, Ruff format check, `py_compile`, `git diff --check` with only existing CRLF warnings, `git diff --cached --check`, code-review gate pass (`risk_score=0.1`), graph detect risk `0.00`, LLM Wiki strict audit pass (`41` pages, `40` numbered, `212` sources, unexpected manifest warnings `0`), and objective/completion audit remains expected incomplete (`8/9`) because the active goal is still waiting on explicit stop/publish authorization plus user-owned Hanwoo T-251. Current root boundary: dirty count `8`, staged `0`, branch ahead `884`, selector `dirty_worktree_handoff_current`, no adoptable candidate. No stage, commit, push, revert, `update_goal`, root product edit, or T-251 retry was performed. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1784 claude-goal doctor schema runtime-flag conflict fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1783 doctor verify-json stale-days conflict fix, T-1782 install.sh argument-boundary fix, and T-1781 completion-audit objective preflight fix. Direct isolated CLI reproduction showed `doctor --install --schema --strict`, `doctor --install --schema --stale-days 1`, and slash equivalents accepted runtime-only flags while rendering the static install-health schema with exit `0`. Root cause: `_validate_doctor_schema_conflicts()` rejected output-mode and output-sink conflicts but did not receive or check `strict` or explicit `--stale-days`, so those runtime flags were silently ignored in schema mode. Fixed `goal/scripts/claude_goal.py` by extending `_validate_doctor_schema_conflicts()` with `strict` and `stale_days_explicit` and rejecting them as `doctor --schema` conflicts. Added `tests/test_claude_goal.py::test_doctor_schema_rejects_runtime_flags_before_db_open` for direct and slash paths with a bad DB path. |
| Next Priorities | Verification passed in `claude-goal/`: red regression first rendered schema with exit `0`, then passed after the fix. After-fix QA in `.tmp/claude-goal-t1784-doctor-schema-runtime-flags-qa.json` confirms direct and slash `--strict` conflicts now return `goal error: doctor --schema cannot be combined with --strict`, direct and slash explicit `--stale-days` conflicts return `goal error: doctor --schema cannot be combined with --stale-days`, DB-open masking is absent, and plain direct/slash `doctor --install --schema` still returns JSON. Focused regression pytest passed (`1 passed`), related doctor cluster passed (`5 passed, 152 deselected`), and full isolated pytest with exported `PYTHONUTF8=1` passed (`237 passed`). Static gates passed: `python -X utf8 -m py_compile goal/scripts/claude_goal.py tests/test_claude_goal.py`, `python -X utf8 -m ruff check goal/scripts/claude_goal.py tests/test_claude_goal.py`, `python -X utf8 -m ruff format --check goal/scripts/claude_goal.py tests/test_claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1783 claude-goal doctor verify-json stale-days conflict fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1782 install.sh argument-boundary fix, T-1781 completion-audit objective preflight fix, and T-1780 install output quoting fix. Direct isolated CLI reproduction showed `doctor --install --verify-json <path> --stale-days 1` and slash `invoke doctor --install --verify-json <path> --stale-days 1` accepted `--stale-days` in install-health verification mode, then attempted artifact verification. With a missing artifact and `CLAUDE_GOAL_DB` pointing at a directory, both reported `cannot read install-health JSON artifact` instead of rejecting the meaningless verify-mode option before artifact reads and DB work. Root cause: `DoctorArgs` and `StaleCommandArgs` collapsed explicit `--stale-days` and default stale-days into the same integer, so `_validate_doctor_verify_flags()` could reject other runtime/generation flags but had no signal for explicit stale-days. Fixed `goal/scripts/claude_goal.py` by tracking `stale_days_explicit`, changing direct doctor argparse default to `None`, and adding explicit `--stale-days` to `doctor --verify-json` incompatible options. Added `tests/test_claude_goal.py::test_doctor_verify_json_rejects_stale_days_before_artifact_read`. |
| Next Priorities | Verification passed in `claude-goal/`: red regression first failed with the missing-artifact masking path, then passed after the fix. After-fix QA in `.tmp/claude-goal-t1783-doctor-verify-stale-days-conflict-qa.json` confirms direct and slash stale-days conflict cases now return `goal error: doctor --verify-json cannot be combined with --stale-days`, leave stdout empty, avoid artifact-read masking, avoid DB-open masking, and print no traceback; no-stale controls still read the missing artifact as expected. Focused regression pytest passed (`1 passed`), related doctor verifier cluster passed (`3 passed, 151 deselected`), and full isolated pytest with exported `PYTHONUTF8=1` passed (`234 passed`). Static gates passed: `python -X utf8 -m py_compile goal/scripts/claude_goal.py tests/test_claude_goal.py`, `python -X utf8 -m ruff check goal/scripts/claude_goal.py tests/test_claude_goal.py`, `python -X utf8 -m ruff format --check goal/scripts/claude_goal.py tests/test_claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1782 claude-goal install.sh help/unknown-argument side-effect boundary fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify UX loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1781 completion-audit objective preflight fix, T-1780 install output quoting fix, and T-1779 doctor strict install-required boundary fix. Direct isolated `install.sh` probes with repo-local temp `HOME` showed `./install.sh --help` and `./install.sh --dry-run` ignored their arguments, performed the normal install, wrote `.claude/settings.json`, and created the `.claude/skills/goal` symlink. Classification: functional error, loading/feedback, and copy. External reference checked: GNU command-line standards say `--help` should print invocation help and not perform the normal command function. Root cause: `install.sh` had no argument parser, so every invocation flowed straight into side-effecting install work. Fixed `install.sh` by adding `usage()`, early `-h|--help` handling with exit `0`, and explicit unknown-option/unexpected-argument errors with exit `2` before any install directories/files are created. Updated `tests/test_install_script.py` with actual Bash execution tests that set repo-local `.tmp` `HOME` and assert help/unknown cases create no settings file or skill symlink while normal install still works. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1782-install-sh-arg-boundary-qa.json` records decision `adopt_candidate`, the GNU reference URLs, and confirms `--help` exits `0`, prints usage on stdout, leaves stderr empty, and creates no side-effect files; `--dry-run` exits `2`, leaves stdout empty, reports `install.sh error: unknown option: --dry-run` plus the help hint, and creates no side-effect files; and normal no-arg install still returns `0`, prints `Installed /goal for Claude Code.`, and creates settings plus skill symlink. Focused `tests/test_install_script.py` passed (`4 passed`), `bash -n install.sh` passed, and full isolated pytest with exported `PYTHONUTF8=1` passed (`233 passed`). Static gates passed for the touched scope: `python -X utf8 -m py_compile goal/scripts/claude_goal.py goal/scripts/install_goal.py tests/test_install_script.py`, `python -X utf8 -m ruff check tests/test_install_script.py`, `python -X utf8 -m ruff format --check tests/test_install_script.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect; current debug inventory still reports root dirty-handoff, publish/current-head Actions, T-251, and launch completion audit as blocked/non-actionable boundaries. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1781 claude-goal completion-audit objective preflight fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1780 install.sh quoting fix, T-1779 doctor strict install-required boundary fix, and T-1778 restore-mode installer boundary fix. Direct isolated preflight reproduction showed `complete --audit-json <artifact>` and slash `invoke complete --audit-json <artifact>` accepted audit JSON whose `objective` was not a non-empty string through static audit preflight; with `CLAUDE_GOAL_DB` pointing at a directory, the commands failed by opening the DB first (`cannot open goal database`) instead of reporting the malformed completion-audit artifact. Root cause: `goal/docs/completion-audit-schema.json` requires `objective` to be a non-empty string, but `_read_completion_audit_json_artifact_for_preflight()` checked required keys, static identity, checklist, and gap lists without validating objective shape before the DB-dependent current-goal identity comparison. Fixed `goal/scripts/claude_goal.py` by adding `_verify_completion_audit_objective()` and invoking it during preflight before DB access. Added `tests/test_claude_goal.py::test_complete_with_invalid_audit_objective_reports_artifact_before_db_open` for direct and slash paths with a bad DB path. |
| Next Priorities | Verification passed in `claude-goal/`: red regression first failed with DB masking, then passed after the fix. After-fix QA in `.tmp/claude-goal-t1781-completion-audit-objective-preflight-qa.json` records the reproduced bug, root cause, fix, and after-fix probe: non-string, empty, and blank objectives now return `goal error: completion audit objective must be a non-empty string` for direct and slash paths before DB access; a valid-static control still reaches DB open as expected. Focused regression pytest passed (`1 passed`), audit-related cluster passed (`13 passed, 140 deselected`), and full isolated pytest with exported `PYTHONUTF8=1` passed (`231 passed`). Static gates passed: `python -X utf8 -m py_compile goal/scripts/claude_goal.py tests/test_claude_goal.py`, `python -X utf8 -m ruff check goal/scripts/claude_goal.py tests/test_claude_goal.py`, `python -X utf8 -m ruff format --check goal/scripts/claude_goal.py tests/test_claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1780 claude-goal install.sh copy-paste command quoting fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1779 doctor strict install-required boundary fix, T-1778 restore-mode installer boundary fix, and T-1777 parser-error boundary fix. Direct isolated `install.sh` reproduction in the Windows/WSL workspace showed the printed Stop hook, doctor, and restore commands used an unescaped repo path containing the space in `Vibe coding`, so copying the displayed `python3 .../Vibe coding/...` command into Bash would split the script path before reaching `doctor --install` or restore mode. Root cause: the actual installer invocation was already quoted, but the user-facing `echo` lines interpolated raw `$ROOT` and `$HOME` paths. Fixed `install.sh` by adding a Bash `shell_quote()` helper using `printf '%q'`, precomputing printable script/settings paths, and using those values only for displayed copy-paste commands. Updated `tests/test_install_script.py` to lock the shell-quoting helper, quoted display variables, and absence of the raw unescaped doctor/restore command strings. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1780-install-sh-quoted-output-qa.json` records decision `adopt_candidate` and confirms actual `install.sh` output escapes the `Vibe coding` space as `Vibe\ coding` for Stop hook, doctor, and restore copy commands; raw unescaped doctor/restore command lines are absent; the displayed doctor script path executes and returns parseable install-health JSON; and stderr stays empty. Focused `tests/test_install_script.py` passed (`2 passed`), `bash -n install.sh` passed, and full isolated pytest with exported `PYTHONUTF8=1` passed (`230 passed`). Static gates passed for the touched scope: `python -X utf8 -m py_compile goal/scripts/claude_goal.py goal/scripts/install_goal.py tests/test_install_script.py`, `python -X utf8 -m ruff check tests/test_install_script.py`, `python -X utf8 -m ruff format --check tests/test_install_script.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. A full pytest attempt without exported `PYTHONUTF8` failed only on the existing Korean Windows child-subprocess path mojibake assertions (`222 passed, 8 failed`) before the UTF-8 rerun passed. Broader `ruff format --check goal tests` currently reports existing dirty `tests/test_claude_goal.py` format drift, so it was not reformatted during this T-1780 scoped change. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1779 claude-goal doctor strict install-required boundary fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1778 restore-mode installer boundary fix, T-1777 parser-error boundary fix, and T-1776 invalid JSON path feedback fix. Direct isolated CLI reproduction showed `doctor --strict` and slash `invoke doctor --strict` were accepted without `--install`; with `CLAUDE_GOAL_DB` pointing at a directory they failed by opening the goal DB first (`cannot open goal database`) instead of reporting an install-health usage error. Root cause: `_validate_doctor_install_required_flags()` listed install-health-only output and CI flags but omitted `strict`, so `--strict` was ignored on normal local doctor paths until DB access. Fixed `goal/scripts/claude_goal.py` by threading `strict` into the install-required validation and rejecting `doctor --strict` unless `--install` is present. Updated `tests/test_claude_goal.py` to cover direct and slash `--strict` with a bad DB path so the usage error must happen before DB open. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1779-doctor-strict-install-required-qa.json` records decision `adopt_candidate` and confirms both direct and slash probes return exit `2`, leave stdout empty, report `goal error: doctor --strict requires --install`, avoid DB-open masking, avoid raw argparse `usage:`, and print no traceback. Valid `doctor --install --json --strict` still returns parseable strict JSON and exits `1` on warn status as designed. Focused regression pytest passed (`1 passed`), related doctor cluster passed (`4 passed, 148 deselected`), and full isolated pytest with exported `PYTHONUTF8=1` passed after formatting on the final state (`230 passed`). Static gates passed after formatting: `python -m py_compile goal/scripts/claude_goal.py goal/scripts/install_goal.py tests/test_claude_goal.py tests/test_install_goal.py`, `python -m ruff check .`, `python -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1778 claude-goal installer restore-mode install-only argument boundary fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1777 parser-error boundary fix, T-1776 invalid JSON path feedback fix, and T-1775 installer type-validation usage fix. Direct isolated installer reproduction showed restore mode silently accepted install-only inputs: positional `script_path`, `--no-backup`, `--python-executable`, `--stop-block-cap`, and `--max-stop-continues` all returned exit `0` with `--restore-backup`, and `--restore-backup --yes` could apply the restore while misleading install-only arguments were present. Root cause: `parse_args()` accepted installer options globally, normalized default install values before mode policy, and had no restore-mode conflict check for explicitly supplied install-only arguments. Fixed `goal/scripts/install_goal.py` by leaving install-only option defaults as `None`, rejecting explicit install-only inputs when `--restore-backup` is active, and applying python/cap defaults and validation only in install mode. Added CLI and direct `parse_args()` regression coverage in `tests/test_install_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1778-install-restore-mode-conflicts-qa.json` records decision `adopt_candidate` and confirms all five restore-mode conflict probes return exit `2`, leave stdout empty, report exact `goal installer error: ... only applies when installing` messages, avoid raw argparse `usage:` and `install_goal.py: error:`, preserve settings even with `--yes`, and print no traceback. Valid `--restore-backup --yes` still applies the backup successfully. Focused restore/parser regression pytest passed (`12 passed`), installer pytest passed with exported `PYTHONUTF8=1` (`40 passed`; the non-UTF8 attempt failed only on existing Korean-path subprocess decoding assertions), and full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`230 passed`). Static gates passed: `python -m py_compile goal/scripts/install_goal.py tests/test_install_goal.py`, `python -m ruff check .`, `python -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Unscoped `ruff format --check .` still scans ignored `.tmp` pytest artifacts and fails there, so source-scope format checking remains `goal tests`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1777 claude-goal installer parser-error boundary fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1776 invalid JSON path feedback fix, T-1775 installer type-validation usage fix, and T-1774 installer post-parse usage fix. Direct isolated installer reproduction showed parser-level errors still leaked raw argparse output: unknown options and missing values for `--python-executable`, `--stop-block-cap`, and `--max-stop-continues` returned exit `2` with `usage:` plus `install_goal.py: error:` before any settings mutation. Official Python argparse docs confirm invalid options and wrong argument counts normally print usage and exit, so this was the remaining gap against the installer's cleaner `goal installer error:` boundary. Fixed `goal/scripts/install_goal.py` by adding an installer-specific `ArgumentParser.error()` override that raises `ValueError`, letting `main()` print `goal installer error: ...` while preserving normal `--help`. Added regression coverage in `tests/test_install_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1777-installer-parser-error-boundary-qa.json` records decision `adopt_candidate` and confirms all six parser-error probes return exit `2`, leave stdout empty, report exact `goal installer error: ...` messages, avoid raw argparse `usage:` and `install_goal.py: error:`, avoid creating settings files, and print no traceback. Valid sanity probes for `--help`, custom-cap install, and restore dry-run still return `0`. Focused parser-error pytest passed (`6 passed`), full installer pytest passed (`46 passed`), and full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`230 passed`). Static gates passed: `python -X utf8 -m py_compile goal/scripts/claude_goal.py goal/scripts/install_goal.py`, `python -X utf8 -m ruff check goal/scripts/install_goal.py tests/test_install_goal.py`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. External reference checked: Python argparse docs for `parse_args()` invalid-argument usage behavior. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1776 claude-goal installer invalid JSON path feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1775 installer type-validation usage fix, T-1774 installer post-parse usage fix, and T-1773 contract alias validation-label fix. Direct isolated installer reproduction showed corrupted settings artifacts returned only raw JSON parser text without naming the broken file: install with a malformed `settings.json` returned `goal installer error: Expecting property name enclosed in double quotes...`, and restore with a malformed `settings.json.bak` returned `goal installer error: Expecting value...`. Root cause: `read_settings()` let `json.JSONDecodeError` bubble with parser-only copy, while `main()` only added the generic installer prefix. Fixed `goal/scripts/install_goal.py` by wrapping JSON decode failures as `invalid JSON in <path>: <parser detail>`. Updated `tests/test_install_goal.py` to lock the path-aware installer and restore error output without traceback, argparse usage, or file mutation. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1776-installer-invalid-json-path-qa.json` records decision `adopt_candidate` and confirms both invalid JSON probes return exit `2`, leave stdout empty, report `goal installer error: invalid JSON in <path>: ...`, include the specific settings or backup path, avoid raw argparse `usage:` and `install_goal.py: error:`, preserve or avoid creating settings files as appropriate, and print no traceback. Focused installer pytest passed after using repo-local `--basetemp` (`30 passed`), full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`214 passed`), and static gates passed: `python -X utf8 -m py_compile goal/scripts/claude_goal.py goal/scripts/install_goal.py`, `python -X utf8 -m ruff check goal/scripts/install_goal.py tests/test_install_goal.py`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Verification notes: default pytest temp setup failed with Windows `PermissionError` on `AppData\Local\Temp\pytest-of-*`, so reruns used repo-local basetemp; one full pytest attempt failed before the final rerun on stale pre-format output expectations, and the final rerun passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1775 claude-goal installer type-validation usage feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1774 installer post-parse usage fix, T-1773 contract alias validation-label fix, and T-1772 direct doctor schema validation fix. Direct isolated installer reproduction showed type-validator option errors leaked raw argparse usage and bypassed the installer error boundary: blank `--python-executable`, zero/non-integer `--stop-block-cap`, and zero/non-integer `--max-stop-continues` all returned exit `2` with `usage:` plus `install_goal.py: error:` instead of `goal installer error: ...`. Root cause: those validators were attached as argparse `type=` callbacks, so `parser.parse_args()` raised `SystemExit` with full argparse usage before `main()` could route the failure through the installer error boundary. Fixed `goal/scripts/install_goal.py` by parsing raw option strings, normalizing `python_executable`, `stop_block_cap`, and `max_stop_continues` after `parse_args()`, and letting `ValueError` flow through `main()`'s existing error handling. Updated `tests/test_install_goal.py` to lock the new no-usage installer CLI behavior. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1775-install-type-validation-usage-qa.json` records decision `adopt_candidate` and confirms all five installer type-validation probes return exit `2`, leave stdout empty, report `goal installer error: ...`, avoid raw argparse `usage:` and `install_goal.py: error:`, avoid writing settings files, and print no traceback; valid custom-cap install and restore dry-run sanity still return `0`. Focused regression pytest passed (`5 passed`), full installer pytest passed (`30 passed`), and full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`214 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1774 claude-goal installer post-parse usage feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1773 contract alias validation-label fix, T-1772 direct doctor schema validation fix, and T-1771 invoke option-command positional fix. Direct isolated installer reproduction showed post-parse policy errors leaked raw argparse usage: `python goal/scripts/install_goal.py <settings> <script> --no-backup --yes` returned exit `2` with `usage:` plus `install_goal.py: error: --yes only applies with --restore-backup`, and `python goal/scripts/install_goal.py <settings> --no-backup` returned `usage:` plus `install_goal.py: error: script_path is required unless --restore-backup is used`. Root cause: `parse_args()` used `parser.error()` for installer-specific policy checks after `parser.parse_args()` returned, so argparse printed full usage instead of the installer error boundary used by runtime validation failures. Fixed `goal/scripts/install_goal.py` by raising `ValueError` for those post-parse policy checks and running `parse_args()` inside `main()`'s existing `(OSError, ValueError)` handler. Updated `tests/test_install_goal.py` to lock CLI output and direct `parse_args()` API behavior. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1774-install-post-parse-usage-qa.json` records decision `adopt_candidate` and confirms both installer post-parse error probes return exit `2`, leave stdout empty, report `goal installer error: ...`, avoid raw argparse `usage:` and `install_goal.py: error:`, avoid writing settings files, and print no traceback; valid no-backup install and restore dry-run sanity still return `0`. Focused post-format regression pytest passed (`4 passed`), full installer pytest passed after updating two direct `parse_args()` expectation tests (`29 passed`), and full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`214 passed`). Static gates passed after formatting: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Intermediate verification notes: the first installer full run failed only because two direct `parse_args()` tests still expected `SystemExit`, and the first format check reported `tests\test_install_goal.py` needed formatting; both were corrected before the final full rerun. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1773 claude-goal contract alias validation-label feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1772 direct doctor schema validation fix, T-1771 invoke option-command positional fix, and T-1770 direct unknown-command fix. Direct isolated CLI reproduction showed contract aliases kept typed command names for unknown-option diagnostics, but deeper validation errors still used canonical `contract` copy: `invoke audit-contract --verify-schema schema.json` and `invoke operating-contract --verify-schema schema.json` returned `goal error: contract --verify-schema requires --verify-json`; conflict cases such as `invoke audit-contract --schema --json-output out.json` and `invoke operating-contract --verify-json artifact.json --markdown` had the same canonical-label mismatch. Root cause: `parse_contract_args()` passed the typed alias to the generic parser, but `_validate_contract_*()` helpers still hardcoded `contract --...` labels for dependency/conflict errors. Fixed `goal/scripts/claude_goal.py` by threading `command_name` through contract validation helpers and using it in conflict/dependency labels. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1773-contract-alias-validation-labels-qa.json` records decision `adopt_candidate` and confirms all four alias validation probes return exit `2`, leave stdout empty, report the typed alias in `goal error`, avoid canonical `goal error: contract --...` copy, avoid DB-open masking with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback. Focused regression pytest passed after narrowing an over-broad substring assertion (`3 passed, 149 deselected`), manual subprocess probes passed (`4/4`), and full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`211 passed`). Static gates passed: `python -X utf8 -m py_compile goal/scripts/claude_goal.py goal/scripts/install_goal.py`, `python -X utf8 -m ruff check goal/scripts/claude_goal.py tests/test_claude_goal.py`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Official parser references checked: Python argparse docs and Click options docs. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1772 claude-goal direct doctor schema validation feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1771 invoke option-command positional fix and T-1770 direct unknown-command fix. Direct isolated CLI reproduction showed `doctor --schema` returned exit `2` before DB access but leaked raw argparse `usage:` plus `claude_goal.py: error: doctor --schema requires --install`; the same command with `CLAUDE_GOAL_DB` pointing at a directory showed the same raw argparse boundary. Slash/custom parser `invoke doctor --schema` already returned `goal error: --schema requires --install`, and `doctor --install --schema` still printed the schema. Root cause: `validate_main_args()` converted doctor validation `ValueError` into `parser.error()`, so argparse formatted post-parse validation failures instead of letting the existing main `ValueError` handler print app-level `goal error` output. Fixed `goal/scripts/claude_goal.py` by letting direct doctor validation errors propagate through the existing app error boundary. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1772-doctor-schema-direct-validation-qa.json` records decision `adopt_candidate` and confirms five direct/slash doctor schema validation probes return expected `goal error` output, avoid raw argparse usage and `claude_goal.py: error:`, avoid `invalid choice`, avoid DB-open masking with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback; valid `doctor --install --schema` and `invoke doctor --install --schema` schema output still returns `0`. Focused regression pytest passed (`2 passed`), adjacent doctor schema/conflict cluster passed (`4 passed, 147 deselected`), and full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`210 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1771 claude-goal invoke option-command unexpected positional feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the pending relay entries for T-1770 direct unknown-command, T-1769 direct unknown-option, T-1768 invoke boolean inline-value, and T-1767 direct unexpected-positional fixes. Direct isolated slash/custom parser reproduction showed option-bearing invoke commands with bare extra tokens, including `invoke contract extra`, `invoke audit-contract extra`, `invoke operating-contract extra`, `invoke doctor extra`, `invoke complete extra`, `invoke json extra`, `invoke pause-stale extra`, `invoke contract --json-output out.json extra`, `invoke doctor --stale-days 1 extra`, `invoke json --session-id other-session extra`, and `invoke complete --audit-json missing-audit.json extra`, returned exit `2` before mutation but reported `goal error: unknown <command> option: extra`. Root cause: `_parse_flag_value_command_args()` and `_parse_stale_command_args()` classified all leftover tokens through the unknown-option branch, even when the token was a non-option positional. Fixed `goal/scripts/claude_goal.py` by adding `_reject_unexpected_positional()` before custom parser unknown-option errors, and by passing typed contract aliases into contract parser diagnostics so `audit-contract` and `operating-contract` errors name the command the user typed. Updated existing JSON invoke parity expectations to the new argument-feedback policy. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1771-invoke-option-positional-qa.json` records decision `adopt_candidate` and confirms all 11 slash/custom option-command positional probes return exit `2`, leave stdout empty, report `goal error: <command> does not accept arguments: extra`, avoid `unknown ... option`, avoid DB-open masking with `CLAUDE_GOAL_DB` pointing at a directory, avoid completion-audit file-read masking, and print no traceback. Focused regression pytest passed (`3 passed, 146 deselected`), post-fix focused rerun for the adjusted JSON parity expectation passed (`2 passed, 148 deselected`), and the three latest parser regressions passed together (`3 passed, 147 deselected`). Full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`209 passed`). Static gates passed: `python -X utf8 -m py_compile goal/scripts/claude_goal.py goal/scripts/install_goal.py`, `python -X utf8 -m ruff check goal/scripts/claude_goal.py tests/test_claude_goal.py`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. One intermediate full pytest run failed only because an existing JSON parity test expected the old `unknown json option: extra` copy; the expectation was updated and the full rerun passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1770 claude-goal direct unknown-command feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the recorded T-1769 direct unknown-option fix, T-1768 invoke boolean inline-value fix, and T-1767 direct unexpected-positional fix. Direct isolated CLI reproduction showed top-level unknown command/option tokens such as `summary`, `summary extra`, and `--bogus` returned exit `2` before mutation but leaked raw argparse `usage:` plus `invalid choice` or `unrecognized arguments` copy. Related `set`/`invoke` probes were explicitly left unchanged because dash-prefixed text can be legitimate objective text in those legacy fallback paths. Root cause: `_prevalidate_main_required_values()` returned early for unknown top-level command names, leaving argparse to format the failure before the app-level `goal error` boundary. Fixed `goal/scripts/claude_goal.py` by adding a consolidated `MAIN_COMMANDS` set and rejecting unknown top-level direct commands/options before argparse, while preserving no-arg help, `--help`/`-h`, and `set`/`invoke` objective fallback behavior. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1770-direct-unknown-command-qa.json` records decision `adopt_candidate` and confirms all three direct unknown-command probes return exit `2`, leave stdout empty, report `goal error: unknown command: <token>`, avoid raw argparse usage, avoid `invalid choice` and `unrecognized arguments`, avoid DB-open masking with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback. Focused regression pytest passed (`1 passed`), adjacent direct parser cluster passed (`7 passed, 143 deselected`), and full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`209 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. The first manual QA attempt was rejected because it reused a session across normal-flow sanity commands and hit the expected existing-goal guard; the rerun separated sessions and adopted the candidate. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1769 claude-goal direct unknown-option feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the already-recorded T-1768 invoke boolean inline-value fix and T-1767 direct unexpected-positional fix. Direct isolated CLI reproduction showed known direct commands with unknown options, including `status --bogus`, `pause --bogus`, `resume --bogus`, `clear --bogus`, `stop-hook --bogus`, `complete --bogus`, `contract --bogus`, `doctor --bogus`, `json --bogus`, `pause-stale --bogus`, `contract --json-output <path> --bogus`, and `doctor --stale-days 1 --bogus`, returned exit `2` before mutation but leaked raw argparse `usage:` plus `unrecognized arguments: --bogus`. Slash/custom parser comparison showed the intended policy: simple no-option commands report `<command> does not accept arguments: --bogus`, while option-bearing commands report `unknown <command> option: --bogus`. Root cause: the T-1767 direct prevalidation scan rejected leftover bare positionals but still let unknown dash-prefixed tokens fall through to argparse; the first candidate also revealed known boolean tokens such as `--install` needed to be consumed before the new unknown-option branch. Fixed `goal/scripts/claude_goal.py` by extending `_prevalidate_main_required_values()` so known boolean options are consumed after validation, simple commands reject dash tokens as unexpected arguments, and option-bearing commands reject unknown dash tokens as `unknown <command> option: <token>` before argparse or DB open. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1769-direct-unknown-option-qa.json` records decision `adopt_candidate` and confirms all 12 direct unknown-option probes return exit `2`, leave stdout empty, report command-specific `goal error` messages, avoid raw argparse usage and `unrecognized arguments`, avoid DB-open masking with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback. Manual sanity also confirmed normal `set`, `status`, `contract --json-output`, `doctor --install --json-output`, `json`, and help flows return `0`. Focused regression pytest passed after the boolean-consumption correction (`1 passed`), adjacent parser cluster passed (`11 passed, 137 deselected`), and full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`207 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. The first manual QA attempt caught a real candidate regression where `doctor --install --json-output` was misclassified as unknown; this was fixed and locked in the focused regression before full rerun. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1768 claude-goal invoke boolean inline-value feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the already-recorded T-1767 direct unexpected-positional fix and T-1766 invoke boolean following-value fix. Direct isolated CLI reproduction showed slash/custom parser boolean options with inline explicit values, including `invoke complete --schema=false`, `invoke contract --markdown=false`, `invoke doctor --install=false`, `invoke doctor --json=false`, `invoke pause-stale --yes=false`, and `invoke pause-stale --json=false`, returned exit `2` before mutation but reported `goal error: unknown <command> option: --flag=false` instead of the option-specific boolean-value error. Root cause: `_parse_flag_value_command_args()` and `_parse_stale_command_args()` only recognized bare boolean tokens; known boolean tokens containing `=` fell through to the custom parser unknown-option branch. Fixed `goal/scripts/claude_goal.py` by adding `_reject_inline_boolean_value()` and applying it before custom parser unknown-option errors. Added regression coverage in `tests/test_claude_goal.py`. The QA artifact was generated before the relay ID collision was noticed, so it is named `.tmp/claude-goal-t1767-invoke-boolean-inline-value-qa.json` while this relay entry uses the collision-free ID `T-1768`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1767-invoke-boolean-inline-value-qa.json` records decision `adopt_candidate` and confirms all six slash/custom boolean inline-value probes return exit `2`, leave stdout empty, report `goal error: <option> does not accept a value`, avoid `unknown ... option`, avoid DB-open masking with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback. Focused parser pytest passed (`3 passed, 144 deselected`), manual subprocess probes passed (`6/6`), and full isolated pytest with exported `PYTHONUTF8=1` passed (`206 passed`). Static gates passed: `python -X utf8 -m py_compile goal/scripts/claude_goal.py goal/scripts/install_goal.py`, `python -X utf8 -m ruff check goal/scripts/claude_goal.py tests/test_claude_goal.py`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Post-compaction sanity also passed the two latest focused regressions together (`2 passed, 146 deselected`) and nested `git diff --check` still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1767 claude-goal direct unexpected positional feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the already-recorded T-1766 invoke boolean following-value fix, T-1765 set inline value-option fix, and T-1764 direct boolean following-value fix. Direct isolated CLI reproduction showed known direct commands that do not accept positional arguments, including `status extra`, `pause extra`, `resume extra`, `clear extra`, `stop-hook extra`, `doctor extra`, `contract extra`, `json extra`, `complete extra`, `pause-stale extra`, `contract --json-output <path> extra`, and `doctor --stale-days 1 extra`, returned exit `2` before mutation but leaked raw argparse `usage:` plus `unrecognized arguments: extra` instead of the command-specific `goal error` boundary. Root cause: `_prevalidate_main_required_values()` returned early for commands with no registered value/boolean options, and it did not scan option-bearing commands for leftover bare tokens after consuming valid option values. Fixed `goal/scripts/claude_goal.py` by adding `MAIN_COMMANDS_WITHOUT_POSITIONAL_ARGS` and turning `_prevalidate_main_required_values()` into a sequential scan that skips valid value-option values, preserves normal help handling, and rejects unexpected direct positional tokens as `goal error: <command> does not accept arguments: <rest>` before argparse or DB open. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1767-direct-unexpected-positional-qa.json` records decision `adopt_candidate` and confirms all 12 direct unexpected-positional probes return exit `2`, leave stdout empty, report `goal error: <command> does not accept arguments: extra`, avoid raw argparse usage and `unrecognized arguments`, avoid DB-open masking with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback. Manual sanity also confirmed normal `set`, `status`, `contract --json-output`, `doctor --install --json-output`, and `json` flows still return `0`, while normal help remains available and `json --session-id --help` still reports `--session-id requires a value`. Focused regression pytest passed after formatting (`1 passed`), adjacent parser cluster passed (`10 passed, 136 deselected`), and full isolated pytest with exported `PYTHONUTF8=1` passed on the final state (`205 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. The first format check reported `tests\test_claude_goal.py` needed formatting; after `ruff format`, focused and full reruns passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1766 claude-goal invoke boolean following-value feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus T-1765 set inline value-option, T-1764 direct boolean following-value, and related parser parity surfaces. Direct isolated CLI reproduction showed slash/custom parser boolean options followed by non-option value tokens, such as `invoke complete --schema false`, split-argv `invoke complete --schema false`, `invoke contract --markdown false`, `invoke doctor --install false`, `invoke doctor --json false`, `invoke pause-stale --yes false`, and `invoke pause-stale --json false`, returned exit `2` before mutation but reported `goal error: unknown <command> option: false` instead of identifying the boolean option that rejected the value. Root cause: `_parse_flag_value_command_args()` and `_parse_stale_command_args()` set boolean flags and let the following token fall through to the unknown-option branch; they did not perform the direct-parser-style immediate value check. Fixed `goal/scripts/claude_goal.py` by adding `_reject_following_boolean_value()` and applying it to custom slash boolean paths, while preserving valid following option tokens such as `doctor --install --json-output path`. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1766-invoke-boolean-following-value-qa.json` records decision `adopt_candidate`, official argparse/Click references, and confirms all seven slash boolean following-value probes return exit `2`, leave stdout empty, report `goal error: <option> does not accept a value`, avoid `unknown ... option`, avoid DB-open masking with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback. Focused parser pytest passed after formatting (`3 passed, 142 deselected`), manual subprocess probes passed (`7/7`), and full isolated pytest with exported `PYTHONUTF8=1` passed (`205 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. The first format check reported `goal/scripts/claude_goal.py` needed formatting; after `ruff format`, focused and full reruns passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1765 claude-goal set inline value-option known-option guard fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the already-recorded T-1764 direct boolean following-value feedback surface, T-1763 direct split flag-like value-option feedback surface, T-1762 split-form set value-option guard, and T-1761 inline flag-like value-option guard for non-set commands. Direct isolated CLI reproduction showed `set objective --token-budget=--tokens`, slash `invoke objective --token-budget=--tokens`, split-argv slash `invoke objective --token-budget=--tokens`, `set objective --tokens=--budget`, slash `invoke objective --tokens=--budget`, and help-token variants returned `goal error: invalid token budget: '--tokens'`, `--budget`, or `--help` instead of the clearer missing-value feedback. Official argparse/Click option docs were checked; the chosen policy remains app-level blocking of known option tokens rather than blocking every dash-prefixed value. Root cause: T-1762 made split-form `set` budget values check `SET_VALUE_OPTION_BLOCKERS`, but inline `--tokens=...`, `--token-budget=...`, and `--budget=...` paths still called `_validate_set_option_value()` without blocker context before `parse_tokens()`. Fixed `goal/scripts/claude_goal.py` by letting `_validate_set_option_value()` pass optional blocked options through to `_validate_option_value()` and applying `SET_VALUE_OPTION_BLOCKERS` to all split and inline set budget value paths. Also normalized missing direct main value/boolean option groups to empty sets so the already-recorded T-1764 direct prevalidation path cannot crash on value-only commands. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1765-set-inline-option-value-qa.json` records decision `adopt_candidate`, official argparse/Click references, and confirms all seven direct/slash set inline known-option probes return exit `2`, leave stdout empty, report `goal error: <option> requires a value`, avoid `invalid token budget`, avoid DB-open masking with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback. Focused parser pytest passed (`3 passed, 140 deselected`), manual subprocess probes passed (`7/7`), adjacent repair focused pytest passed (`4 passed, 140 deselected`), and full isolated pytest with exported `PYTHONUTF8=1` passed (`203 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. One intermediate full pytest run failed on the adjacent prevalidation TypeError before the repair; the focused repair and full rerun passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1764 claude-goal direct boolean following-value feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the completed T-1763 direct split flag-like value-option feedback surface, T-1762 set value-option following-option guard, T-1761 inline flag-like value-option guard, T-1760 invoke split-form value-option flag-consumption surface, and T-1759 direct boolean explicit-value surface. Direct isolated CLI reproduction showed boolean options followed by non-option value tokens, such as `complete --schema false`, `contract --markdown false`, `doctor --install false`, `doctor --json false`, `pause-stale --yes false`, and `pause-stale --json false`, returned exit `2` before mutation but leaked raw argparse usage plus `unrecognized arguments: false` copy instead of the command-specific `goal error` boundary. Root cause: `_prevalidate_main_required_values()` rejected inline boolean values like `--schema=false`, but it did not detect split-form boolean value tokens before argparse parsed them as unexpected positionals. Fixed `goal/scripts/claude_goal.py` by extending `_prevalidate_main_required_values()` so known direct boolean options reject the immediately following non-option token as `goal error: <option> does not accept a value`, while preserving valid following option tokens such as `doctor --install --json-output path`. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1764-direct-boolean-following-value-qa.json` records score `6/6`, decision `adopt_candidate`, and confirms all direct boolean following-value probes return exit `2`, leave stdout empty, report `goal error: <option> does not accept a value`, avoid argparse usage and `unrecognized arguments`, avoid DB-open masking even with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback. Focused regression pytest passed (`1 passed`), parser cluster pytest passed after a guard correction (`10 passed, 134 deselected`; first run exposed and fixed a `boolean_options=None` TypeError in value-only commands), and full isolated pytest with `PYTHONUTF8=1` passed (`203 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1763 claude-goal direct split flag-like value-option feedback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the concurrently recorded T-1762 set value-option following-option guard, completed T-1761 inline flag-like value-option guard, T-1760 invoke split-form value-option flag-consumption surface, and T-1759 direct boolean explicit-value surface. Direct isolated CLI reproduction showed split-form direct value options followed by known flags, such as `contract --json-output --schema`, `contract --markdown-output --schema`, `doctor --install --json-output --json`, `doctor --install --markdown-output --json`, `complete --audit-json --schema`, `json --session-id --help`, and `pause-stale --stale-days --json`, returned exit `2` before mutation but leaked raw argparse usage plus `expected one argument` copy instead of the command-specific `goal error` boundary. Root cause: `_prevalidate_main_required_values()` only rejected trailing direct value options; it did not reject a following known option/help token before argparse parsed the subcommand, while the slash/custom parser and inline paths already used blocked-token validation. Fixed `goal/scripts/claude_goal.py` by extending `_prevalidate_main_required_values()` so split-form direct value options reject a missing value when the next token is a known boolean/value/help token. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1763-direct-split-flag-value-qa.json` records score `7/7`, decision `adopt_candidate`, and confirms all direct split flag-like value-option probes return exit `2`, leave stdout empty, report `goal error: <option> requires a value`, avoid argparse usage and `expected one argument`, avoid DB-open masking even with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback. Focused regression pytest passed (`1 passed`), parser cluster pytest passed (`9 passed, 133 deselected`), and full isolated pytest with `PYTHONUTF8=1` passed (`201 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1762 claude-goal set value-option following-option guard fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the already-recorded T-1761 inline flag-like value-option surface, T-1760 invoke split-form value-option flag-consumption surface, and T-1759 direct boolean explicit-value surface. Direct isolated CLI reproduction showed `set objective --token-budget --tokens 10`, slash `invoke objective --token-budget --tokens 10`, `set objective --tokens --budget 10`, slash `invoke objective --tokens --budget 10`, and help-token variants treated the following known option/help token as the budget value and returned `goal error: invalid token budget: '--tokens'`, `--budget`, or `--help`. Root cause: `parse_set_args()` only checked for a missing next token; it did not distinguish known set option/help tokens from legitimate budget values before calling `parse_tokens()`. Fixed `goal/scripts/claude_goal.py` by adding `SET_VALUE_OPTIONS` / `SET_VALUE_OPTION_BLOCKERS` and rejecting only known set value options plus `-h`/`--help` when they appear in the split value position. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1762-set-option-following-flag-qa.json` records decision `adopt_candidate` and confirms all six direct/slash set following-option probes return exit `2`, leave stdout empty, report `goal error: <option> requires a value`, avoid `invalid token budget`, avoid DB-open masking with `CLAUDE_GOAL_DB` pointing at a directory, and print no traceback. Focused parser pytest passed (`4 passed, 137 deselected`), manual subprocess probes passed (`6/6`), and full isolated pytest with exported `PYTHONUTF8=1` passed (`200 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. One full pytest attempt without exported `PYTHONUTF8` failed only on known Korean Windows path mojibake assertions in child subprocesses; the UTF-8 environment rerun passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

> T-1570 relay note: `f3f376a6` is the verified code baseline before this documentation relay. After this relay is committed, use live `python execution/session_orient.py --json` for the exact current HEAD/ahead count; the remaining boundaries should still be publish/current-head Actions plus user-owned Hanwoo T-251.

> T-1404 verification note: staged code-review gate returned advisory WARN (`risk_score=0.35`) from test-gap heuristics, covered by focused source-browser tests, CLI preflight tests, live click-through evidence, and blind-to-x project QC.

| Work | **blind-to-x ?蹂? ?紐꺿봺椰?Phase 2 ?곗뮉釉?(`/goal` ?臾먮뼗)** ??"X?癒?퐣 筌뤴뫀紐℡첎? ?蹂?????????? ???" ?????筌뤴뫚紐??4????명롥첎???놁삢/??쎈탞?룐뫂遊??닌딄퍥 ???묽) ?袁⑥쟿?袁⑹뜖??以??臾먮뼗. **A. ?袁⑨세?袁る뱜 ?????* `draft_prompts.py` `_build_comment_trigger_block` ??곗쨮 ?紐꾩맄????살쟿????뽰젟 4???袁⑥쟿?袁⑹뜖??+ "?蹂??????????疫꼲???⑤벏???癰귣똾?わ쭪袁ⓥ봺/?얜똻源뗨눧?곷옱/?臾먰돩嚥??곕뗄湲쏙쭗?녾텢 筌띾뜄龜??" ??딅뼒???쉘????밴쉐 ?袁⑨세?袁る뱜??揶쏅벡??雅뚯눘?? **B. ?癒?탵?醫듼봺???????* `editorial_reviewer.py` ???蹂? ?紐꺿봺椰?4???癒?땾(`identifiability`/`stance`/`open_loop`/`anchor`, 揶?1~10?? ?곕떽?. 5?????뇧(疫꿸퀣????4?????뇧(疫꿸퀡???袁㏉?6.0) ??**AND** 嚥??얜씈堉????????궢??곷튊 END; ??뽧걹 沃섎챶?????筌ㅼ뮆? 2???귐됱뵬??꾨뱜. `EditorialResult.comment_trigger_scores` ?袁⑤굡嚥????삸??고??癒?땾 ?紐꾪뀱. **C. 野껉퀣?숁에醫롮읅 ?????* `draft_quality_gate.py` `_is_colorless_take` ?얜똻源뗨눧?곷옱 野꺜?곗뮄由???golden 7揶?false-positive 0% ?類ㅼ뵥 ??`hedge ??2 OR (??곗뺘????꾩맯 ??1 AND ??놁삢 ??쀬겱 0揶?AND ??min_chars)` 嚥?癰귣똻????뺣뻼. twitter/threads = 疫꼲 ?袁⑷퍥, naver_blog = `<creator_take>` ??볥젃. `_extract_creator_take` ???퐣嚥??袁⑥뵭??warning. **???** ??μ맄 1669 passed + 1 skipped (282s), ?醫됲뇣 40 ?냈??곷뮞 100%, ??? ?癒?샑 4???뵬 ruff ???? **雅뚯눘??*: `pipeline/quality_gate.py` ????삘뀲 ?袁㏓럡(Gemini Workstream 1~5)揶쎛 `_check_bland_creator_take`/`_check_semantic_similarity` ?곕떽? ?臾믩씜 餓???W293(?⑤벉媛? 1椰?沃섎챷????遺욍? ??삘뀲 ?袁㏓럡??WIP????롫즲?怨몄몵嚥?沃섎챸苑ｇ㎉?(`multi_tool_git_index_race_20260520` ?類ㅼ퐠 餓Β??. |
| Next Priorities | (a) ????癒?퓠野??뚣끇而??諭???遺욧퍕 ??Phase 2 5???뵬(`draft_prompts.py`, `editorial_reviewer.py`, `draft_quality_gate.py`, `tests/unit/test_comment_trigger_uplift.py`, `docs/output_quality_uplift_2026-05-26.md`) stage ??疫? (b) Gemini Workstream 1~5 WIP ??`quality_gate.py` ??몄첒 ??`ruff --fix` ?袁⑹뒄 (1椰?. (c) Phase 3 ?袁⑤궖: Best-of-2 ????꿸숲 (LLM ??쑴??2獄쏄퀡???????野껉퀣???袁⑹뒄), ?醫뤿동 ?????쎄숲 筌ㅼ뮄??5椰???? ?醫롪텢??reroll, Notion 野꺜???遺얇늺??4???癒?땾 ??뽯뻻. |

| Next Priorities | Address Supabase E2E resync (T-251) and systematically proceed with VibeDebt RED reduction execution (T-407). |

## Notes

- **T-251 (active blocker)**: Supabase database password desynchronization. User must reset password via Supabase Dashboard (Project Settings > Database), then update `projects/hanwoo-dashboard/.env`.
- **Origin sync**: As of 2026-05-19, `main` is synchronized with `origin/main` and the worktree is clean.
- Older addenda archived in `.ai/archive/HANDOFF_archive_2026-05-15.md` and `.ai/archive/HANDOFF_archive_2026-05-19.md`.
