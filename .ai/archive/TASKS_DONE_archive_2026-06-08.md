## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1548 | `[workspace/ai-tooling]` `/goal` 시스템 개선. Enabled the 3 local-safe project MCP servers (`code-review-graph`, `sqlite-multi`, `system-monitor`) in `.claude/settings.local.json` after finding all 8 `.mcp.json` servers stuck `Pending approval` (graph tools CLAUDE.md mandates never loaded). Fixed HANDOFF/TASKS self-bloat: added `--keep-count`/`--max-lines`(200) caps to `handoff_rotator.py` (was date-only → 339 addenda/634KB no-op) plus a new `execution/tasks_done_rotator.py` honoring `## DONE (Latest 5)` (was 793 rows/672KB). Ran both: HANDOFF 2387→200 lines, TASKS DONE 793→5 rows; 1101 entries archived under `.ai/archive/`. 26 rotator unit tests pass, ruff check+format clean, MCP servers smoke-started clean, docs synced (session-close SKILL + CLAUDE/AGENTS/GEMINI). Local only; no push; T-251 untouched. | Claude | 2026-06-07 |
| T-1546 | `[hanwoo-dashboard/field-mode]` Enlarged the virtual ear-tag scanner header close button from `34x34` to `44x44` on mobile by replacing the old `p-2` icon button class with an explicit `inline-flex min-h-11 min-w-11 items-center justify-center` target. Browser-click QA at `390x844` verified field mode, scanner open, no-match scan path, and final `이표 스캐너 닫기` dimensions `44x44`. Verification passed focused scanner test (`6 passed`), Hanwoo project test (`536 passed`), lint, build, `git diff --check`, and advisory code-review gate WARN covered by focused/browser/project evidence. No push was performed and T-251 was not retried. | Codex | 2026-06-07 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1550 | `[workspace/performance-overview]` Localized the cross-project KPI dashboard to Korean operator copy across title/caption, sidebar health metrics, KPI cards, trend/cost/platform sections, empty states, and footer. Follow-up `35b2dbae` replaced the hidden `watchdog_history.json 기준` metric help with visible `자동 점검 이력 기준` caption, removing the tiny mobile help control. Added tests for rendered Korean copy, visible basis copy, old English label rejection, and old help-source rejection while preserving Streamlit width helpers. Verification passed focused pytest (`7 passed`), Ruff check, Ruff format check, `py_compile`, and diff-check. No push and no T-251 retry. | Codex | 2026-06-07 |
| T-1549 | `[workspace/ai-context]` Adopted `.ai/PROJECTS.md` as the concise project-by-project session-start dashboard. Wired it into AGENTS/CLAUDE/GEMINI, `session-start`, `session-close`, `.agents/workflows/start.md`, and `.ai/CONTEXT.md` so future agents can read status, current focus, output-quality criteria, and release boundaries before choosing a loop. Verification passed: diff-check, rotator dry-run noops, and session orientation with latest Next Priorities ok. No push and no T-251 retry. | Codex | 2026-06-07 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1552 | `[browser-apps]` Improved mobile touch targets across Knowledge Dashboard, Suika Game V2, and Word Chain. Knowledge Dashboard button/input/tab/export/search-clear controls, Suika scaled game controls, and Word Chain button variants now meet the 44px mobile target in source contracts; added focused tests in each project. Browser QA retained fresh usable nonblank screenshots for all 4 browser apps with no overflow, no console/network failures, and no remaining visible below-44 controls after the T-1553 follow-up. Verification passed focused tests, Knowledge lint/smoke, Suika/Word lint/build/full tests, full active-project QC, browser inventory `4/4`, and advisory code-review gate coverage by direct tests/browser evidence. No push and no T-251 retry. | Codex | 2026-06-07 |
| T-1551 | `[workspace/result-dashboard]` Improved the mobile upload-result registration controls for manual YouTube/X result tracking. Added mobile-only CSS for 44px-safe Streamlit tabs, buttons, form-submit buttons, BaseWeb select/input wrappers, text areas, and date inputs; candidate browser QA at `390x844` removed app tabs and `URL`/`제목`/`태그` inputs from the small-target list while preserving upload workflow copy, no horizontal overflow, no console/network errors, and no replacement characters. Verification passed focused pytest (`6 passed`), Ruff check, Ruff format check, `py_compile`, diff-check, and Shorts Maker V2 project QC test/lint (`1640 passed`, `12 skipped`, `29 warnings`, lint pass). No push and no T-251 retry. | Codex | 2026-06-07 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1553 | `[knowledge-dashboard]` Hardened the focused skip-link target after browser QA found the hidden/focused skip action remained below the 44px mobile target even after the broader T-1552 control pass. `projects/knowledge-dashboard/src/app/page.tsx` now gives the focused skip link `focus:flex focus:min-h-11 focus:items-center`, and `projects/knowledge-dashboard/src/mobile-touch-targets.test.mts` locks the source contract. Final browser QA found no below-44 visible controls in Knowledge Dashboard after T-1553; full active-project QC passed with Knowledge Dashboard `64 passed`. No push and no T-251 retry. | Codex | 2026-06-07 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1554 | `[auto-research]` Added direct output-quality objective coverage to the launch manifest. The audit now checks project good-output criteria, external benchmark signals, Good vs Bad output distinction, and iterative baseline/candidate/A-B evidence, with tests for complete and blocked evidence. Completion audit now includes this item and reports `11/15` complete with the same 4 publish/T-251 blockers. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1556 | `[auto-research]` Persisted selector inventory helper outputs. `next_experiment_selector.py` now writes GitHub/browser/dependency helper outputs to expected `.tmp` cache artifacts and uses longer bounded helper timeouts, so fallback evidence is available after live helper runs. Tests assert `--output` paths for all three inventories. No push and no T-251 retry. | Codex | 2026-06-08 |
| T-1555 | `[workspace/shorts-manager]` Hardened Streamlit select dropdown options for mobile touch targets. BaseWeb virtual dropdown options now have `min-height: 44px`, `display: flex`, and centered content; source and rendered CSS tests lock the contract. Browser QA at `390x844` measured 6 options with min height `44`, no horizontal overflow, console errors `0`, and failed requests `0`. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1557 | `[workspace/shorts-manager]` Hardened Streamlit expander summaries for mobile touch targets. Expander summaries now have `min-height: 44px !important`, flex display, and centered content; source/rendered CSS tests lock the selector. Browser QA at `390x844` measured 13 expander summaries with min height `44`, no horizontal overflow, console errors `0`, and failed requests `0`. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1558 | `[workspace/roi-dashboard]` Polished the ROI Dashboard mobile output surface. The page now uses compact Korean operator copy, 44px-safe number input targets, hidden Plotly modebars, and Korean breakeven-view copy; focused tests and mobile browser QA lock the behavior. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1573 | `[docs/llm-wiki]` Implemented the project LLM wiki at `docs/wiki/llm/` (README + 01–07, 819 lines) and collected supporting material, per `/goal`. A 16-agent workflow (7 internal code-extractors + 9 external provider web-verifiers) sourced the content: 9-provider cost-ordered fallback architecture, language bridge, two distinct caches (response `llm_cache.db` 72h vs Anthropic prompt cache), cost weights (write 1.25/2.0, read 0.10) + `workspace.db` `api_calls`/btx `btx_costs.db` + `api_usage_tracker alerts`, Langfuse v3, promptfoo eval, workspace `LLMClient` vs shorts `LLMRouter` (+mimo, no ollama/cost) vs blind-to-x async-sequential (+circuit breaker, Best-of-N), and playbooks. External 9-provider models/pricing/limits/docs were web-verified against official pages on 2026-06-08 (USD/CNY split, confidence noted). Cross-checked against live code (`max_tokens=2000` L460, 5-tuple L483, cache×0.10 L513, `workspace.db`/`api_calls` L29/L111, shorts `llm_providers` order) and confirmed the blind-to-x landmine: `config.yaml` lists `deepseek` first but `draft_providers.py` only wires anthropic/openai/xai/ollama/gemini (no `_generate_with_deepseek`). Flagged 4 stale default models for migration (`gpt-4o-mini`/`grok-3-mini-fast`/`deepseek-chat` retires 2026-07-24/`moonshot-v1-8k`). Added a wiki pointer to `workspace/directives/INDEX.md`. Docs-only, no product code change. No push and no T-251 retry. | Claude | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1574 | `[blind-to-x]` Added deterministic research-context and publish-decision gates for X publishing. Regulation failures, missing value reduction, X weighted-length overflow, forbidden AI-tone patterns, URLs, hashtag overflow, and low quality now produce HOLD/DROP instead of false `Ready to Post`; Notion defaults missing decisions to `Needs Edit`; auto-publishing requires an explicit enable flag. Verified by focused/expanded tests (`138 passed`, `228 passed`), full blind-to-x QC (`1861 passed`, `9 skipped`, lint pass), Ruff, diff-check, A/B `adopt_candidate`, and advisory code-review gate review. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1575 | `[claude-goal]` Continued a self-improvement pass in nested repo `claude-goal` branch `improve/goal-system`: checkpoint `86e2bed`, installer hardening `20b1600`, doctor diagnostics `378e5f7`, dry-run stale pause `d40ae3f`, install health doctor `6751420`, and clear-alias alignment `f73d452`. Live `~/.claude/settings.json` was backed up to `settings.json.bak` and updated to structured `command` + `args` + env caps. Verification passed py_compile, `19 passed` pytest with repo-local basetemp, diff-check, direct live hook block smoke, live install health `ok`, live doctor, and live pause-stale dry-run. No live stale pause was applied. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1577 | `[docs/llm-wiki]` Added an external-source inventory/revalidation manifest. `execution/llm_wiki_audit.py` now generates `docs/wiki/llm/source-inventory.json` with `--write-source-inventory`, audits markdown URL/manifest URL drift, page mismatches, source type/volatility metadata, and `review_after` due dates. Added `docs/wiki/llm/15-source-inventory.md`, README/14-maintenance/directive wiring, and tests for manifest generation, drift, and due-date failures. Verification passed focused pytest (`8 passed`), audit CLI pass (`16 pages`, `15 numbered`, `52` source entries, `0 issues`), Ruff check/format, `py_compile`, and diff-check with CRLF warnings only. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1576 | `[workspace/finance-tracker]` Completed a computer-use mobile UX loop on `workspace/execution/pages/finance_tracker.py`. Fixed direct Streamlit execution import root (`parents[2]`), added mobile 44px touch-target CSS/stretch controls/no-modebar Plotly charts/semantic delete action, and moved the quick-add transaction form before charts after browser QA found it at `1862px`. Verification passed focused pytest (`4 passed`), `py_compile`, Ruff check, path-limited diff-check, and Playwright mobile QA at `390x844`: import error gone, three transaction-add clicks succeeded, quick-add top `721px`, total targets `66`, small targets `[]`, console warnings/errors `0`, screenshots under `output/playwright/finance-tracker-t1576-*.png`. Code-review gate WARN came from unrelated blind-to-x/LLM Wiki WIP. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1578 | `[docs/llm-wiki]` Added a code-fact/default-model drift manifest. `execution/llm_wiki_audit.py` now generates `docs/wiki/llm/code-facts.json` with `--write-code-facts` and audits Python code facts from `workspace/execution/llm_client.py`, shorts-maker-v2 `llm_router.py`, and blind-to-x `draft_providers.py`: provider aliases, fallback order, default models, OpenAI-compatible URL keys, API-key mappings, pricing keys, class methods, constructor expressions, and `_generate_with_*` helper coverage. Added `docs/wiki/llm/16-code-facts.md`, README/14-maintenance/directive wiring, and tests for AST literal extraction, missing manifest, drift detection, healthy manifest pass, and inline-code URL normalization. Regenerated `source-inventory.json` (`54` sources) and `code-facts.json` (`3` targets). Verification passed focused pytest (`13 passed`) and audit CLI pass (`17 pages`, `16 numbered`, `54` source entries, `3` code-fact targets, `0 issues`). No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1579 | `[docs/llm-wiki]` Added a config-fact drift manifest. `execution/llm_wiki_audit.py` now generates `docs/wiki/llm/config-facts.json` with `--write-config-facts` and audits tracked YAML config facts from `projects/shorts-maker-v2/config.yaml`, `projects/blind-to-x/config.example.yaml`, and `projects/blind-to-x/config.ci.yaml`: fallback provider order, default/single models, model coverage, pricing coverage, enabled flags, retry defaults, and timeout defaults. Local `projects/blind-to-x/config.yaml` is intentionally excluded because it can carry secrets. Added `docs/wiki/llm/17-config-facts.md`, README/14-maintenance/16-code-facts/06-per-project/directive wiring, and tests for safe YAML extraction, missing manifest, drift detection, healthy manifest pass, and secret non-leakage. Regenerated source/code/config manifests. Verification passed focused pytest (`17 passed`) and audit CLI pass (`18 pages`, `17 numbered`, `56` source entries, `3` code-fact targets, `3` config-fact targets, `0 issues`). No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1581 | `[workspace/debug-history]` Removed Streamlit automatic heading action anchors from the Debug History operations page after mobile browser QA found 5 English `Link to heading` accessibility labels. The page title and four subheaders now pass `anchor=False`, and tests lock that contract. Verification passed focused pytest (`7 passed`), `py_compile`, Ruff, browser QA (`anchorCount 5 -> 0`, keyword search preserved, Korean copy present, small targets `[]`, overflow `0`, console warnings/errors `0`), and A/B `adopt_candidate` (`score_delta=0.5`). No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1580 | `[docs/llm-wiki]` Added runtime wiring cross-manifest checks. `execution/llm_wiki_audit.py` now extracts `_generate_with_*` helpers from class methods and records config-to-code runtime checks inside `docs/wiki/llm/config-facts.json`. The manifest now makes the blind-to-x mismatch explicit: tracked config includes `deepseek`, `moonshot`, and `zhipuai` without runtime helpers, while runtime has `ollama` outside tracked config. Added `docs/wiki/llm/18-runtime-wiring-checks.md`, updated README/06-per-project/14-maintenance/16-code-facts/17-config-facts/directive wiring, regenerated source/code/config manifests, and expanded `workspace/tests/test_llm_wiki_audit.py` to 19 tests. Verification passed focused pytest (`19 passed`), write/no-write audit (`19 pages`, `18 numbered`, `57` source entries, `0 issues`), `py_compile`, Ruff, and diff-check with CRLF warnings only. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1583 | `[workspace/github-dashboard]` Removed Streamlit automatic heading action anchors from the GitHub operations first screen after mobile browser QA found 2 English `Link to heading` accessibility labels on the visible title and release-boundary heading. The directly observed `GitHub 운영 현황` title and `릴리스 경계` subheader now pass `anchor=False`, and tests lock that first-screen contract. Verification passed focused pytest (`6 passed`), `py_compile`, Ruff, browser QA (`anchorCount 2 -> 0`, release boundary/push warning/token warning preserved, small targets `[]`, overflow `0`, console warnings/errors `0`), and A/B `adopt_candidate` (`score_delta=0.5`). No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1582 | `[docs/llm-wiki]` Exposed manifest check counts/status in `llm_wiki_audit.py --json` summary. JSON now reports code/config fact check counts, status-count maps, total `manifest_check_count`, `manifest_check_warning_count`, and `manifest_check_warnings`, so pass audits keep known config/runtime wiring warnings visible without turning them into failures. Updated tests/docs and regenerated `source-inventory.json` after adding Python docs sources. Verification passed focused pytest (`19 passed`), write/no-write audits (`pass`, 19 pages, 18 numbered, 59 source entries, `warning_count=0`, `manifest_check_warning_count=2`), `py_compile`, Ruff check/format, diff-check with CRLF warnings only, graph detect risk `0.00`; code-review gate WARN (`risk_score=0.30`) came from unrelated GitHub Dashboard WIP. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1585 | `[docs/llm-wiki]` Classified manifest warnings as accepted known vs unexpected new drift. Current blind-to-x runtime-helper warnings now carry `warning_classification: accepted_known` and `accepted_reason`; warnings without acceptance metadata become `unexpected`. Text output reports `2 accepted known, 0 unexpected`, JSON exposes accepted/unexpected counts, and docs record the A/B decision with official OASIS SARIF, GitHub code scanning, and Python warnings sources. Verification passed focused pytest (`21 passed`), text/JSON audits (`pass`, 62 sources, `manifest_check_accepted_warning_count=2`, `manifest_check_unexpected_warning_count=0`), source-inventory regeneration, `py_compile`, Ruff check/format, diff-check with CRLF warnings only, and graph detect risk `0.00`. Earlier full dirty-tree code-review gate passed for this slice (`risk_score=0.15`); final full dirty-tree rerun is WARN (`risk_score=0.35`) due to unrelated `performance_overview.py` WIP. No push and no T-251 retry. | Codex | 2026-06-08 |
| T-1584 | `[workspace/notion-tasks]` Recovered the Notion Tasks Streamlit first screen after mobile browser QA found a direct-run import failure (`No module named 'execution'`), English browser title/copy, and two sub-44px Streamlit header targets. The page now inserts the workspace root, localizes the Notion setup/task board, gives actionable connection/share/.env setup guidance, disables heading anchors, hardens mobile touch targets, and wraps the setup code block. Verification passed browser QA (`hasImportError true -> false`, Korean setup visible, old English absent, small targets `2 -> 0`, `anchorCount=0`, overflow `0`, over-parent code `0`, console warnings/errors `0`), related Notion pytest (`47 passed`), `py_compile`, Ruff, diff-check, graph detect risk `0.00`, code-review gate pass (`risk_score=0.15`), and A/B `adopt_candidate` (`score_delta=1.0`). No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1587 | `[docs/llm-wiki]` Added a strict/CI mode for unexpected manifest warnings while preserving default local non-failing behavior. `execution/llm_wiki_audit.py` now accepts `--fail-on-unexpected-manifest-warnings` / `--strict-manifest-warnings`, exits 1 only when that opt-in flag is set and `manifest_check_unexpected_warning_count > 0`, and exposes `strict_manifest_warning_mode` plus `strict_manifest_warning_failure` in JSON. Focused tests cover default non-failing unexpected warnings, strict exit 1, and strict JSON failure evidence. README, maintenance docs, and runtime-wiring docs document the CI command. Verification passed focused pytest (`24 passed`), default text audit (`pass`, `2 accepted known`, `0 unexpected`), strict text/JSON audits (`pass`, `strict_manifest_warning_failure=false` on current accepted debt), source-inventory regeneration, `py_compile`, Ruff check/format, diff-check with CRLF warnings only, graph detect risk `0.00`, and code-review gate pass (`risk_score=0.15`). No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1586 | `[workspace/performance-overview]` Removed Streamlit automatic heading anchors and hardened mobile touch targets on the Performance Overview page. Browser QA at `390x844` found baseline `anchorCount=5` and `smallTargets=17` while the Korean page otherwise loaded cleanly; candidate passes `anchor=False` to title/sidebar/section headings and sizes header/dataframe toolbar buttons to 44px on mobile. Verification passed focused pytest (`8 passed`), `py_compile`, Ruff check/format check, diff-check with CRLF warning only, browser QA (`anchorCount 5 -> 0`, small targets `17 -> 0`, no import error, old English copy, replacement chars, overflow, or console warnings/errors), graph detect risk `0.00`, code-review gate pass (`risk_score=0.15`), and A/B `adopt_candidate` (`score_delta=0.4117647058823529`). No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1590 | `[docs/llm-wiki]` Persisted strict LLM Wiki audit output as a reusable release/current-head evidence artifact. `execution/llm_wiki_audit.py --write-strict-release-evidence --json` now enables strict manifest-warning mode, writes `.tmp/llm-wiki-strict-audit-current.json`, and records release gate status, accepted/unexpected manifest-warning counts, full audit report, and current git HEAD SHA when available. Docs explain the artifact flow with official GitHub workflow artifact/upload-artifact references, and `source-inventory.json` now tracks 64 sources. Verification passed focused pytest (`26 passed`), default/strict/write-evidence audits (`pass`, `accepted_known=2`, `unexpected=0`, evidence status `pass`), `py_compile`, Ruff check/format, diff-check with CRLF warnings only, graph detect risk `0.00`, and code-review gate pass (`risk_score=0.15`). No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1591 | `[docs/llm-wiki]` Wired the strict LLM Wiki release evidence artifact into the broader launch/release evidence surface. `release_authorization_packet.py` now reads `.tmp/llm-wiki-strict-audit-current.json`, exposes strict evidence status/head match/unexpected warning count/source inventory plus schema/generated/command metadata in the packet, and blocks release authorization if the artifact is missing, failing, or stale. `launch_objective_audit.py` now surfaces that evidence line in the release packet item. Docs and the auto-research skill record the packet/launch-audit consumption path and A/B decision; `source-inventory.json` now tracks 65 sources. Verification passed focused auto-research pytest (`56 passed`), LLM Wiki audit/write-evidence audits (`pass`, 65 sources, `unexpected=0`), `py_compile`, Ruff check/format check, release packet CLI evidence (`status=pass`, `head_matches_current=True`, `unexpected=0`, `source_inventory_count=65`, overall `blocked_dirty_worktree` due unrelated dirty tree), launch audit evidence line, completion audit `incomplete` as expected for unrelated dirty/T-251/launch blockers, and graph detect risk `0.00`. Full dirty-tree code-review gate is advisory WARN (`risk_score=0.55`) from graph test-gap heuristics/full-tree scope, covered here by direct tests and CLI evidence. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1589 | `[workspace/result-dashboard]` Removed Streamlit automatic heading anchors, mobile sub-44px targets, and English date-picker helper text from the Result Dashboard first screen. Browser QA at `390x844` found baseline `anchorCount=2`, `smallTargets=9`, and two English date-helper strings while the Korean page otherwise loaded cleanly; candidate passes `anchor=False` to headings, extends mobile target CSS, and uses a localized `YYYY-MM-DD` text input/parser for `게시일`. Verification passed focused pytest (`10 passed`), `py_compile`, Ruff check/format check, diff-check with CRLF warning only, browser QA (`anchorCount 2 -> 0`, small targets `9 -> 0`, English date helper `2 -> 0`, no import error, old English app labels, replacement chars, overflow, or console warnings/errors), graph detect risk `0.00`, and A/B `adopt_candidate` (`score_delta=0.5714285714285714`). Full dirty-tree code-review gate WARN (`risk_score=0.55`) came from unrelated WIP/test-gap heuristics and is covered for this slice by direct evidence. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1592 | `[workspace/channel-growth]` Removed Streamlit automatic heading anchors, hardened mobile header/menu touch targets, and completed the remaining copy-localization pass on the Channel Growth page. Browser QA first verified `headingAnchorCount 5 -> 0` and actionable small targets `6 -> 0`; the follow-up reopened the page at `390x844` and removed the remaining English browser title/visible label (`Channel Growth - Joolife`, `YouTube Channel ID`) by localizing `page_title`, import failure copy, and the channel ID label. Verification passed focused pytest (`7 passed`), `py_compile`, Ruff check/format check, diff-check with CRLF warning only, browser QA (`title=채널 성장 - Joolife`, visible English copy `[]`, `headingAnchorCount=0`, `userSmallTargetCount=0`, `visibleModebarCount=0`, overflow `0`, console warnings/errors `0`), direct fill/select interaction, graph detect risk `0.00`, and A/B `adopt_candidate` (`score_delta=0.3333333333333333`). Full dirty-tree code-review gate WARN (`risk_score=0.55`) came from unrelated WIP/test-gap heuristics and is covered for this slice by direct evidence. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1593 | `[docs/llm-wiki]` Added a GitHub Actions reviewer-summary surface for LLM Wiki strict release evidence. New `llm_wiki_release_summary.py` reads `.tmp/release-authorization-packet.json`, renders `llm_wiki_strict_evidence` as a Markdown table/checklist, appends to `GITHUB_STEP_SUMMARY` or a supplied summary file, supports `--strict`, and can copy JSON/Markdown into visible `release-evidence/llm-wiki` for `actions/upload-artifact@v7` so hidden `.tmp` paths are not uploaded directly. README, maintenance/runtime docs, and the auto-research skill document the A/B decision and workflow-safe artifact path. Verification passed focused auto-research pytest (`59 passed`), LLM Wiki audits/write-evidence (`pass`, 65 sources, `unexpected=0`), `py_compile`, Ruff check/format check, release packet CLI evidence (`strict status=pass`, head current, packet still `blocked_dirty_worktree` due unrelated WIP), and summary helper CLI evidence (`status=pass`, step summary appended). No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1594 | `[docs/llm-wiki]` Added a deterministic audit guard for the LLM Wiki reviewer-summary workflow contract. `execution/llm_wiki_audit.py` now checks release-summary docs for `llm_wiki_release_summary.py`, `GITHUB_STEP_SUMMARY`, visible `release-evidence/llm-wiki`, and `actions/upload-artifact@v7`, and fails manual step-summary shell echo examples, stale upload-artifact versions, or direct `.tmp` artifact upload paths. JSON summary exposes `release_summary_contract_status` and `release_summary_contract_issue_count`. Verification passed focused pytest (`28 passed`), focused auto-research/LLM Wiki pytest (`87 passed`), `py_compile`, Ruff check/format check, source/code/config write audits plus no-write audit (`pass`, `release_summary_contract_status=pass`, `issue_count=0`), strict evidence write (`pass`, `unexpected=0`), release packet and summary-helper CLI evidence, diff-check with CRLF warning only, and selector/launch audit evidence that the next candidate is T-1595 `github_inventory_followup`. Graph detect risk `0.00` covers tracked dirty files; untracked T-1594 files are covered by direct pytest/Ruff/audit evidence. Full dirty-tree code-review gate WARN (`risk_score=0.55`) came from unrelated WIP/test-gap heuristics. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1595 | `[workspace/selector]` Fixed the auto-research selector self-loop after the GitHub inventory follow-up gates had already been run. Reproduction: `github_project_inventory.py --include-prs` reported open PRs `0` and dirty-only evidence (`dirty_count=59`), but `next_experiment_selector.py --json` reselected `github_inventory_followup`. Root cause: `_github_candidate()` treated dirty worktree count and the dirty-worktree inventory advisory as actionable GitHub follow-up evidence. The selector now filters dirty-only recommendations from GitHub follow-up, emits `dirty_worktree_handoff` for dirty/no-PR/no-actionable-recommendation state, and keeps open PRs on `github_inventory_followup`. Verification passed selector/launch pytest (`63 passed`), focused selector pytest (`16 passed`), `py_compile`, Ruff check/format check, live selector (`selected_kind=dirty_worktree_handoff`), diff-check with CRLF warnings only, and graph detect risk `0.00`. No push, no unrelated staging, and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1597 | `[workspace/roi-dashboard]` Removed the remaining internal English empty-state target and mobile sub-44px controls from the ROI Dashboard. Baseline mobile QA at `390x844` found `Result Tracker` in the empty state and 3 small Streamlit header/menu controls; candidate uses `콘텐츠 결과 관리에서 먼저 등록하세요.`, disables the title anchor, and extends mobile CSS to Streamlit number-input container/input plus header/sidebar/menu buttons. Verification passed focused Result/ROI dashboard pytest (`10 passed`), `py_compile`, Ruff check/format check, diff-check with CRLF warnings only, browser QA (`Result Tracker` absent, localized empty state present, small targets `3 -> 0`, visible heading anchors `0`, overflow `false`, visible modebars `0`, console warnings/errors `0`), A/B `adopt_candidate` (`score_delta=0.6923076923076923`), and graph detect risk `0.00`. Graph update timed out but status/detect were usable; full dirty-tree gate WARN (`risk_score=0.55`) is unrelated WIP/test-gap noise. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1596 | `[workspace/dirty-worktree-handoff]` Added deterministic dirty-worktree handoff evidence. `github_project_inventory.py` now emits `git.dirty_path_groups` with owner hints and bounded path samples; current live groups are `workspace-dashboard=20`, `llm-wiki=19`, `ai-context=6`, `auto-research=6`, `project:blind-to-x=5`, `project:hanwoo-dashboard=2`, `workspace-code-review-gate=2`, and `workspace=1`. `code_review_gate.py` default/base mode now reports graph-relevant untracked files from `git ls-files --others --exclude-standard`, while staged mode stays scoped to staged files. Verification passed inventory+gate pytest (`45 passed`), `py_compile`, Ruff check/format check, diff-check with CRLF warnings only, graph detect risk `0.00`, live inventory, live selector (`dirty_worktree_handoff`), product readiness (`score=94`), and code-review gate WARN with `9` untracked graph-relevant files surfaced. No push, no unrelated staging, and no T-251 retry. | Codex | 2026-06-08 |
| T-1584b | `[docs/llm-wiki]` Exposed non-failing manifest warnings in the non-JSON `llm_wiki_audit.py` text output. Baseline text output hid known warnings behind `0 warnings`; now it prints `0 audit warnings, 2 manifest warnings`, a `Manifest checks` summary, and each `config-facts.json` runtime-helper warning id with provider-gap details. Updated tests/docs and regenerated `source-inventory.json` after adding Python `sys` docs. Verification passed focused pytest (`20 passed`), text/JSON audits (`pass`, 60 sources, `manifest_check_warning_count=2`), `py_compile`, Ruff check/format, diff-check with CRLF warnings only, graph detect risk `0.00`, and code-review gate pass (`risk_score=0.15`). No push and no T-251 retry. | Codex | 2026-06-08 |
| T-1579b | `[workspace/finance-tracker]` Removed Streamlit automatic heading action anchors from the localized Finance Tracker page after mobile browser QA found 5 English `Link to heading` accessibility labels. The page title and four subheaders now pass `anchor=False`, and tests lock that contract. Verification passed focused pytest (`7 passed`), `py_compile`, Ruff, path-limited diff-check, browser QA (`anchorCount=0`, no `Link to heading`, Korean copy present, small targets `[]`, console warnings/errors `0`), A/B `adopt_candidate` (`score_delta=0.6666666666666666`), and advisory code-review gate WARN (`risk_score=0.30`) covered by direct Finance evidence. No push and no T-251 retry. | Codex | 2026-06-08 |
| T-1578b | `[workspace/finance-tracker]` Localized the Finance Tracker mobile operator surface and fixed the monthly trend axis after browser QA found 22 app-owned English copy leaks plus date-boundary chart labels. The page now uses Korean copy, Korean type labels over backend `income`/`expense` values, validated Korean `YYYY-MM-DD` date text input instead of English `date_input` helper copy, checkbox budget toggle instead of the expander `keyboard_arrow_right` leak, and `%Y.%m` monthly trend ticks. Verification passed focused pytest (`6 passed`), `py_compile`, Ruff, path-limited diff-check, browser QA (`foundForbidden=[]`, `missingRequired=[]`, broken time axis `false`, transaction add flow succeeded, budget toggle succeeded, small targets `[]`, console warnings/errors `0`), and A/B `adopt_candidate` (`score_delta=0.7777777777777778`). No push and no T-251 retry. | Codex | 2026-06-08 |
| T-1576b | `[docs/llm-wiki]` Added a deterministic LLM Wiki verification gate. New `execution/llm_wiki_audit.py` checks README index coverage, local markdown links, external-source evidence, and verification-date freshness; `workspace/tests/test_llm_wiki_audit.py` locks pass/fail cases. Added `docs/wiki/llm/14-maintenance-verification.md`, README index/quick-start wiring, 06-per-project source metadata, and directive index registration. Verification passed focused pytest (`5 passed` with repo-local basetemp), audit CLI pass (`15 pages`, `0 issues`), Ruff check/format, `py_compile`, diff-check, graph update/status, and graph detect risk `0.00`; code-review gate WARN came from unrelated blind-to-x WIP. No push and no T-251 retry. | Codex | 2026-06-08 |
| T-1444b | `[blind-to-x/source-browser]` Added `main.py --source all` as a source-preflight alias for all configured `input_sources`, matching the documented all-source operator workflow while preserving `multi`. Baseline `main.py --source all --source-preflight` exited `1` with `Skipping unsupported source preflight targets: all` and `No supported source preflight targets resolved for source=all`; candidate expands to 4 sources and exits `0`. `pipeline/bootstrap.py` now treats `multi`/`all` as configured-source aliases, `pipeline/cli.py` documents the alias, and README/directive preflight docs distinguish main CLI `all` from standalone helper all-known-source probing. Tests cover input-source and preflight resolution for `all`. Live browser preflight verified 4 sources, ready `ppomppu`/`jobplanet`, blocked Blind `403` and FMKorea `430`; click-through preflight verified detail paths for `ppomppu` and `jobplanet` with one ready-source Ppomppu console warning. Verification passed focused tests (`41 passed`), related source/main tests (`69 passed`), `py_compile`, Ruff check/format, live preflight, live click-through preflight, blind-to-x project QC (`1832 passed`, `9 skipped`, lint passed), diff-check, staged code-review gate advisory WARN (`risk_score=0.40`) covered by focused/live/project gates, and A/B `adopt_candidate` (`score_delta=6.846153846153846`). Code commit `8f524cdb` is local only. No push was performed and T-251 was not retried. | Codex | 2026-06-07 |
| T-1438b | `[shorts-maker-v2/dashboard]` Made the generated dashboard and tracked sample browser-QA clean on mobile. Baseline Playwright CLI at 390px showed the sample overflowed horizontally (`scrollWidth=630`, `clientWidth=390`, `bodyScrollWidth=598`) and the first desktop load emitted a favicon 404. `utils/dashboard.py` now emits viewport metadata, a data favicon, constrained main layout, responsive summary cards, an accessible scroll-contained table wrapper, and the same full HTML shell for empty dashboards. `docs/dashboard.html` mirrors the runtime contract, and `tests/unit/test_dashboard.py` locks viewport/favicon/table/grid/sample contracts. Candidate browser QA verified `scrollWidth=390`, `bodyScrollWidth=390`, no horizontal overflow, grid summary cards, contained table overflow, no Korean mojibake, console warnings/errors 0, and screenshot `output/playwright/shorts-dashboard-t1438-responsive.png`. Verification passed focused dashboard pytest (`13 passed`), Ruff, `py_compile`, venv CLI dashboard generation, shorts-maker-v2 project QC (`1639 passed`, `12 skipped`, `29 warnings`, lint passed), diff-check, code-review gate advisory WARN (`risk_score=0.40`) covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=1.125`). Commit `70cb64dd` is local only; recorded as T-1438b because a concurrent Hanwoo context/code bundle was already staged. No push was performed and T-251 was not retried. | Codex | 2026-06-07 |
| T-1422b | `[blind-to-x/source-browser]` Accepted explicit all-source and JSON-compatible standalone source preflight commands. Baseline operator command `scripts/source_browser_probe.py --source all --click-through --json` failed because `--source` only accepted concrete source names and the helper rejected `--json` despite always emitting JSON. `scripts/source_browser_probe.py` now accepts `all`, `auto`, and `multi` aliases for every known source, dedupes alias-expanded sources with repeated concrete sources, and accepts `--json` as a no-op compatibility flag. README and `directives/x_content_curation.md` document the explicit all-source helper command. Live Playwright preflight verified all 4 sources with click-through: JobPlanet and Ppomppu ready, Blind `403` and FMKorea `430` blocked with problem actions, and recommended source `ppomppu`. Verification passed focused tests (`23 passed`), `py_compile`, Ruff check/format check, diff-check, live all-source click-through preflight, blind-to-x project QC (`1823 passed`, `9 skipped`, lint passed), code-review gate advisory WARN (`risk_score=0.40`) covered by focused/live/project gates, and A/B `adopt_candidate` (`score_delta=1.6`). Commit `51e4f83d` is local only. No push was performed and T-251 was not retried. | Codex | 2026-06-07 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1598 | `[workspace/scoped-dirty-commit-plan]` Produced a scoped dirty-tree commit/handoff order without staging, committing, pushing, or reverting. Live evidence showed branch `main` ahead `833`, staged `0`, dirty `61`, open PRs `0`, readiness score `94`, selector `dirty_worktree_handoff`, and full-tree code-review gate advisory WARN `0.55`. Artifact `.tmp/scoped-dirty-commit-plan-t1598.md` orders groups as `auto-research` + `workspace-code-review-gate`, `llm-wiki`, page-level `workspace-dashboard` slices, `project:blind-to-x`, `project:hanwoo-dashboard`, then `ai-context`, with group-specific gates and a handoff-only decision until explicit commit/stage authorization. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1600 | `[workspace/code-review-gate]` Fixed the default-Python CLI failure for the graph-backed code-review gate. Reproduction: `python execution\code_review_gate.py --base HEAD --json` exited `3` with `code_review_graph is not importable from this Python interpreter`, while `py -3.13` succeeded. Root cause: direct CLI import depended on the active interpreter, but this Windows workspace has the graph package on Python 3.13. The gate now retries that exact Windows import failure once via `py -3.13`, with an env guard to avoid recursion and UTF-8 output relay. Verification passed RED/green focused gate pytest (`32 passed`), `py_compile`, Ruff check/format check, diff-check, default-Python CLI exit `0`, `py -3.13` CLI exit `0`, both advisory WARN `0.55`, and graph detect risk `0.00`. No push and no T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1599 | `[workspace/dirty-handoff-plan-generator]` Added `.agents/skills/auto-research/scripts/dirty_worktree_handoff_plan.py` to turn the T-1598 Markdown-only dirty-tree handoff into deterministic JSON/Markdown evidence with a SHA-256 dirty-group signature, freshness comparison, A/B decision, group order, required gates, and official Git source URLs. Selector `dirty_worktree_handoff` now exposes this helper as a required gate, and the auto-research skill documents the rule. Live artifact `.tmp/scoped-dirty-worktree-handoff-plan-current.json` is `handoff_required`, freshness `current`, signature `96bced5ab05aa6c62f2aa432384132d15e565f77308c9e8b2f68e4f96d6bf277`, dirty count `63`, groups `auto-research`, `workspace-code-review-gate`, `llm-wiki`, `workspace-dashboard`, `project:blind-to-x`, `project:hanwoo-dashboard`, `ai-context`, `workspace`. Verification passed focused pytest (`66 passed`), `py_compile`, Ruff check/format check, live helper freshness, live selector with the new gate, diff-check with CRLF warnings only, graph detect risk `0.00`, and full dirty-tree code-review gate advisory WARN (`risk_score=0.55`) covered for this slice by focused tests. No staging, commit, push, or T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1601 | `[workspace/selector-dirty-plan-freshness]` Taught `next_experiment_selector.py` to consume `.tmp/scoped-dirty-worktree-handoff-plan-current.json` via `--dirty-handoff-plan` or the recent default cache. When the dirty handoff plan is fresh and its dirty count/paths match current GitHub inventory, the selector now emits blocked `dirty_worktree_handoff_current` instead of another adoptable handoff-generation loop. Verification passed focused selector/dirty-plan pytest (`23 passed`), broader selector/dirty-plan/inventory/gate pytest (`69 passed`), `py_compile`, Ruff check/format check, `git diff --check` with CRLF warnings only, live dirty plan freshness (`handoff_required`, `current`, signature `0ab2fd1c7e18a07e280e248f13e155fba89b3f1567a7b108996f330be353e9c7`, dirty count `65`), live selector (`status=blocked`, `selected_kind=dirty_worktree_handoff_current`, `adoptable_candidate_count=0`), graph detect risk `0.00`, and full dirty-tree gate advisory WARN (`risk_score=0.55`) covered by focused tests. No staging, commit, push, revert, or T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1602 | `[word-chain/first-turn-continuation]` Fixed the directly clicked first-turn UX defect where the common opening word `사과` immediately ended the game because the AI dictionary had no `과` entries. Added a `과` bucket to `projects/word-chain/src/utils/dictionary.js` and a regression test in `projects/word-chain/src/utils/gameLogic.test.js`. Baseline browser QA: `사과` -> `GAME OVER`, `hasGameOver=true`. Candidate browser QA: `사과` -> AI `과...` continuation, `hasGameOver=false`, overflow `false`, small targets `[]`, failed requests `[]`. Verification passed Word Chain tests (`25 passed`), lint, build via known ASCII fallback after direct Vite Korean-path failure, path-limited diff-check with CRLF warnings only, and A/B `adopt_candidate` (`score_delta=0.5`). No staging, commit, push, or T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1604 | `[workspace/tasks-done-rotator-suffix]` Fixed `execution/tasks_done_rotator.py` so `## DONE (Latest 5)` counts suffixed task ids such as `T-1584b`, `T-1579b`, and `T-1444b` instead of leaving stale rows behind. Reproduction showed `--check --json` would report only one archived row while old suffixed DONE rows stayed visible. Root cause was `ENTRY_RE = T-\d+`; it now accepts alphabetic suffixes. Verification passed the RED focused regression, full rotator pytest (`10 passed`), `py_compile`, Ruff check/format check, dry-run archive detection (`archived=8`), and live `tasks_done_rotator.py --json` rotation (`archived=8`). No staging, commit, push, or T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1603 | `[workspace/dirty-handoff-current-refresh]` Refreshed the machine-readable dirty handoff plan after the live dirty inventory moved past T-1601/T-1602. The first selector run selected adoptable `dirty_worktree_handoff` because the cached plan still held signature `0ab2fd1c7e18a07e280e248f13e155fba89b3f1567a7b108996f330be353e9c7`; after two helper runs, `.tmp/scoped-dirty-worktree-handoff-plan-current.json` is fresh at signature `23b2e39dbf754118c0a67aee6da5359962a34c199f60aab18f77e8f025091df8`, dirty count `67`. Live selector now returns `status=blocked`, `selected_kind=dirty_worktree_handoff_current`, `adoptable_candidate_count=0`, so the next boundary remains explicit scoped staging/commit authorization or user-owned T-251 reset. No staging, commit, push, revert, or T-251 retry. | Codex | 2026-06-08 |

## Rotation 2026-06-08 (archived DONE rows beyond the newest 5)

| T-1608 | `[workspace-dashboard/format-gate]` Reproduced and repaired the next workspace-dashboard pre-stage gate failure. Initial focused pytest failed only because the active Hermes venv lacks `pytest-cov` while `workspace/pyproject.toml` auto-adds coverage; rerunning with `-o addopts=` isolated the actual gate. Ruff format then failed on five dirty workspace-dashboard files. Applied `ruff format` only to `workspace/execution/pages/debug_history.py`, `workspace/tests/test_finance_db.py`, `workspace/tests/test_finance_tracker_page.py`, `workspace/tests/test_github_dashboard_page.py`, and `workspace/tests/test_notion_client.py`. Verification passed focused pytest (`114 passed`), `py_compile`, Ruff check, Ruff format check (`16 files already formatted`), path-limited diff-check with CRLF warnings only, and graph detect risk `0.00`. No staging, commit, push, revert, product edit, or T-251 retry. | Codex | 2026-06-08 |
