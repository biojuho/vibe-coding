## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1710 claude-goal install-health environment path verifier fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, after root T-1709 became the latest handoff entry. Reproduced with a real CLI flow, temp settings, and temp goal DB that `python goal/scripts/claude_goal.py doctor --install --verify-json <json>` accepted a strict healthy install-health JSON artifact whose `settings_path` was changed to an attacker path while the rest of the artifact and canonical `contract_hash` stayed valid. Root cause: `_verify_install_health_top_level_contract()` checked environment-derived path provenance fields only for nonempty strings, not for equality with the current verifier environment (`SETTINGS_PATH`, `SKILL_PATH`, `COMMANDS_PATH`, `DB_PATH`). Fixed `goal/scripts/claude_goal.py` with `_verify_install_health_environment_paths()` and added direct CLI plus slash `invoke` regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: the original settings-path tamper reproducer now rejects with `install-health settings_path mismatch`; focused install-health verifier pytest passed (`3 passed, 90 deselected`); full claude-goal pytest passed (`134 passed`); `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next reproducible candidate if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1711 claude-goal install-health runtime provenance verifier fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`. Reproduced with a real CLI flow, temp settings, and temp goal DB that `python goal/scripts/claude_goal.py doctor --install --verify-json <json>` accepted a strict healthy install-health JSON artifact whose `runtime.python_version` was changed to `0.0.0-tampered` while the rest of the artifact and canonical `contract_hash` stayed valid. Root cause: `_verify_install_health_runtime()` checked runtime metadata only for schema keys and nonempty strings, even though install-health provenance is generated from the current verifier process via `install_health_runtime_metadata()`. Fixed `goal/scripts/claude_goal.py` by requiring the artifact runtime object to exactly match the current runtime metadata, and added direct CLI plus slash `invoke` regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: the original runtime tamper reproducer now rejects with `install-health runtime metadata mismatch`; focused runtime pytest passed (`1 passed, 93 deselected`); focused install-health verifier pytest passed (`14 passed, 80 deselected`); full claude-goal pytest passed (`135 passed`); `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next reproducible candidate if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1712 LLM Wiki credentials/secrets/API-key boundary reinforcement**. Continued the LLM Wiki autonomous reinforcement loop in the root workspace after concurrent `claude-goal` work claimed T-1711, so this wiki cycle was reassigned to T-1712. Added `docs/wiki/llm/39-credentials-secrets-api-key-boundary.md` to separate provider API keys, CI/runtime secrets, OAuth/PAT/user token files, Supabase/Postgres URLs, browser-public keys, observability secrets, redacted readiness artifacts, and live side-effect authorization. Updated README plus cross-links from security, data-retention/privacy, and conversation-state pages; regenerated `docs/wiki/llm/source-inventory.json` to include the new official credential/secret sources. |
| Next Priorities | Verification passed: `py -3.13 execution\llm_wiki_audit.py --write-source-inventory --json`, no-write audit, strict manifest audit, strict release evidence, focused LLM Wiki/objective pytest via `python -m pytest` (`31 passed`; `py -3.13` lacks pytest in this environment), objective audit expected blocked (`8/9`; active loop/dirty/T-251 boundary), completion audit expected incomplete, path-limited `git diff --check` with existing CRLF warnings only, touched-doc whitespace scan clean, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` risk `0.00`. Official refs checked OpenAI key safety/projects/authentication, Anthropic key best practices, Google API key best practices, Supabase secrets, Notion auth/PATs, GitHub Actions secrets, OWASP Secrets Management, and Twelve-Factor config. No staging, commit, push, revert, `update_goal`, product code edit, or Hanwoo T-251 retry was performed. Continue the loop from the selector after this dirty handoff/context update. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1713 claude-goal invoke help non-mutating UX fix**. Continued the `/goal` UX/UI computer-use loop in the separate `claude-goal/` repo on branch `improve/goal-system`. Direct CLI use reproduced that `python goal/scripts/claude_goal.py invoke --help` behaved like a new objective instead of help: it printed `Action: set` and stored `Objective: --help` in the active goal DB. This was a real state-mutation UX defect; the accidental default DB row created during reproduction was cleared, and all follow-up reproduction used isolated temp goal DBs. Root cause: `invoke` uses `argparse.REMAINDER`, and the legacy `invoke` dispatch path passed `--help` through to the slash-command parser before argparse help handling, so unknown command text fell through to `set_goal()`. Fixed `goal/scripts/claude_goal.py` by adding non-mutating `INVOKE_HELP` handling for `invoke -h/--help` in both main and legacy invoke paths; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct temp-DB CLI QA now reports `invoke --help` and `invoke -h` both show `usage: /goal`, do not print `Action: set`, and leave `status` at `No goal is currently set`; evidence is `.tmp/claude-goal-t1713-cli-qa.json`. Focused pytest passed (`1 passed, 94 deselected`); full claude-goal pytest passed (`136 passed`); `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. External refs checked Python argparse help behavior, GitHub CLI subcommand help, and Vercel CLI `--help` global option. Removed this loop's temp DB and pytest basetemp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next directly reproduced CLI friction point if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1714 claude-goal install-health current-check evidence verifier fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving concurrent T-1712/T-1713 updates. Reproduced with a real CLI flow, temp settings, and temp goal DB that `python goal/scripts/claude_goal.py doctor --install --verify-json <json>` accepted a strict healthy install-health JSON artifact whose first `checks` item kept `name=settings_json` and `status=ok` but changed `detail` to `tampered check detail`. Root cause: `_verify_install_health_checks()` checked schema shape, allowed names/statuses, and detail type, while summary/count verification only recomputed from the artifact's own checks; it never compared the evidence array to checks recomputed from the current verifier environment. Fixed `goal/scripts/claude_goal.py` with `_verify_install_health_checks_match_current_environment()` and added direct CLI plus slash `invoke` regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: the original check-detail tamper reproducer now rejects with `install-health checks do not match current environment`; focused check-detail pytest passed (`1 passed, 95 deselected`); focused install-health verifier pytest passed (`15 passed, 81 deselected`); full claude-goal pytest passed (`137 passed`); `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next reproducible candidate if requested. |
