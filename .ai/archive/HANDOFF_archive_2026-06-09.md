## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1710 claude-goal install-health environment path verifier fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, after root T-1709 became the latest handoff entry. Reproduced with a real CLI flow, temp settings, and temp goal DB that `python goal/scripts/claude_goal.py doctor --install --verify-json <json>` accepted a strict healthy install-health JSON artifact whose `settings_path` was changed to an attacker path while the rest of the artifact and canonical `contract_hash` stayed valid. Root cause: `_verify_install_health_top_level_contract()` checked environment-derived path provenance fields only for nonempty strings, not for equality with the current verifier environment (`SETTINGS_PATH`, `SKILL_PATH`, `COMMANDS_PATH`, `DB_PATH`). Fixed `goal/scripts/claude_goal.py` with `_verify_install_health_environment_paths()` and added direct CLI plus slash `invoke` regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: the original settings-path tamper reproducer now rejects with `install-health settings_path mismatch`; focused install-health verifier pytest passed (`3 passed, 90 deselected`); full claude-goal pytest passed (`134 passed`); `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next reproducible candidate if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1711 claude-goal install-health runtime provenance verifier fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`. Reproduced with a real CLI flow, temp settings, and temp goal DB that `python goal/scripts/claude_goal.py doctor --install --verify-json <json>` accepted a strict healthy install-health JSON artifact whose `runtime.python_version` was changed to `0.0.0-tampered` while the rest of the artifact and canonical `contract_hash` stayed valid. Root cause: `_verify_install_health_runtime()` checked runtime metadata only for schema keys and nonempty strings, even though install-health provenance is generated from the current verifier process via `install_health_runtime_metadata()`. Fixed `goal/scripts/claude_goal.py` by requiring the artifact runtime object to exactly match the current runtime metadata, and added direct CLI plus slash `invoke` regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: the original runtime tamper reproducer now rejects with `install-health runtime metadata mismatch`; focused runtime pytest passed (`1 passed, 93 deselected`); focused install-health verifier pytest passed (`14 passed, 80 deselected`); full claude-goal pytest passed (`135 passed`); `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next reproducible candidate if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1712 LLM Wiki credentials/secrets/API-key boundary reinforcement**. Continued the LLM Wiki autonomous reinforcement loop in the root workspace after concurrent `claude-goal` work claimed T-1711, so this wiki cycle was reassigned to T-1712. Added `docs/wiki/llm/39-credentials-secrets-api-key-boundary.md` to separate provider API keys, CI/runtime secrets, OAuth/PAT/user token files, Supabase/Postgres URLs, browser-public keys, observability secrets, redacted readiness artifacts, and live side-effect authorization. Updated README plus cross-links from security, data-retention/privacy, and conversation-state pages; regenerated `docs/wiki/llm/source-inventory.json` to include the new official credential/secret sources. |
| Next Priorities | Verification passed: `py -3.13 execution\llm_wiki_audit.py --write-source-inventory --json`, no-write audit, strict manifest audit, strict release evidence, focused LLM Wiki/objective pytest via `python -m pytest` (`31 passed`; `py -3.13` lacks pytest in this environment), objective audit expected blocked (`8/9`; active loop/dirty/T-251 boundary), completion audit expected incomplete, path-limited `git diff --check` with existing CRLF warnings only, touched-doc whitespace scan clean, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` risk `0.00`. Official refs checked OpenAI key safety/projects/authentication, Anthropic key best practices, Google API key best practices, Supabase secrets, Notion auth/PATs, GitHub Actions secrets, OWASP Secrets Management, and Twelve-Factor config. No staging, commit, push, revert, `update_goal`, product code edit, or Hanwoo T-251 retry was performed. Continue the loop from the selector after this dirty handoff/context update. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1713 claude-goal invoke help non-mutating UX fix**. Continued the `/goal` UX/UI computer-use loop in the separate `claude-goal/` repo on branch `improve/goal-system`. Direct CLI use reproduced that `python goal/scripts/claude_goal.py invoke --help` behaved like a new objective instead of help: it printed `Action: set` and stored `Objective: --help` in the active goal DB. This was a real state-mutation UX defect; the accidental default DB row created during reproduction was cleared, and all follow-up reproduction used isolated temp goal DBs. Root cause: `invoke` uses `argparse.REMAINDER`, and the legacy `invoke` dispatch path passed `--help` through to the slash-command parser before argparse help handling, so unknown command text fell through to `set_goal()`. Fixed `goal/scripts/claude_goal.py` by adding non-mutating `INVOKE_HELP` handling for `invoke -h/--help` in both main and legacy invoke paths; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct temp-DB CLI QA now reports `invoke --help` and `invoke -h` both show `usage: /goal`, do not print `Action: set`, and leave `status` at `No goal is currently set`; evidence is `.tmp/claude-goal-t1713-cli-qa.json`. Focused pytest passed (`1 passed, 94 deselected`); full claude-goal pytest passed (`136 passed`); `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. External refs checked Python argparse help behavior, GitHub CLI subcommand help, and Vercel CLI `--help` global option. Removed this loop's temp DB and pytest basetemp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next directly reproduced CLI friction point if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1714 claude-goal install-health current-check evidence verifier fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving concurrent T-1712/T-1713 updates. Reproduced with a real CLI flow, temp settings, and temp goal DB that `python goal/scripts/claude_goal.py doctor --install --verify-json <json>` accepted a strict healthy install-health JSON artifact whose first `checks` item kept `name=settings_json` and `status=ok` but changed `detail` to `tampered check detail`. Root cause: `_verify_install_health_checks()` checked schema shape, allowed names/statuses, and detail type, while summary/count verification only recomputed from the artifact's own checks; it never compared the evidence array to checks recomputed from the current verifier environment. Fixed `goal/scripts/claude_goal.py` with `_verify_install_health_checks_match_current_environment()` and added direct CLI plus slash `invoke` regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: the original check-detail tamper reproducer now rejects with `install-health checks do not match current environment`; focused check-detail pytest passed (`1 passed, 95 deselected`); focused install-health verifier pytest passed (`15 passed, 81 deselected`); full claude-goal pytest passed (`137 passed`); `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next reproducible candidate if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1715 claude-goal slash subcommand help parity UX fix**. Continued the `/goal` UX/UI computer-use loop in the separate `claude-goal/` repo on branch `improve/goal-system`. Direct CLI use reproduced that direct subcommand help worked (`complete --help`), but slash subcommand help failed for `invoke complete --help`, `invoke contract --help`, `invoke doctor --help`, and `invoke pause-stale --help` with `unknown <command> option: --help`. Classification: functional error plus copy/feedback inconsistency. Root cause: slash subcommands use custom parsers (`parse_complete_args`, `parse_contract_args`, `parse_doctor_args`, `parse_pause_stale_args`) and `argparse.REMAINDER`, so `--help` reached custom option validation instead of the direct subparser help renderer. Fixed `goal/scripts/claude_goal.py` by routing known slash subcommand `-h/--help` requests through the existing direct subcommand argparse help, including aliases such as `operating-contract`; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct temp-DB CLI QA now reports `invoke complete/contract/doctor/pause-stale --help` all match the corresponding direct subcommand help, do not print `Action: set`, and leave `status` at `No goal is currently set`; evidence is `.tmp/claude-goal-t1715-cli-qa.json`. Focused pytest passed (`2 passed, 95 deselected`); full claude-goal pytest passed (`138 passed`); `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. External refs checked Python argparse help behavior, GitHub CLI subcommand help, and Vercel CLI `--help` global option. Removed this loop's temp DB and pytest basetemp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next directly reproduced CLI friction point if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1716 claude-goal direct set help non-mutating UX fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving concurrent T-1715 slash-help changes. A direct CLI help mutation scan reproduced that `python goal/scripts/claude_goal.py set --help` returned `0` but printed `Action: set` and stored an active goal whose objective was `--help`; `set -h` had the same state-mutation risk. Root cause: `_run_legacy_main_command()` intercepts legacy `set` before argparse, and because `set` uses raw/remainder-style objective text, `--help` was treated as objective content instead of being passed to the direct `set` argparse help renderer. Fixed `goal/scripts/claude_goal.py` by letting legacy `set -h/--help` fall through to the existing argparse parser, and added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: the original `set --help`/`set -h` repro now prints `usage: claude_goal.py set` and leaves `status` at `No goal is currently set`; focused help pytest passed (`3 passed, 95 deselected` after formatting rerun); full claude-goal pytest passed (`139 passed`); `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next directly reproduced CLI friction point if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1717 root LLM Wiki dirty-boundary closure**. Adopted the verified T-1712 LLM Wiki credentials/secrets/API-key boundary group and the shared AI relay group in scoped local commits while preserving concurrent `claude-goal` T-1715/T-1716 relay entries. Created `5bb475f5` (`docs(llm-wiki): add credential boundary`) for `docs/wiki/llm/39-credentials-secrets-api-key-boundary.md`, README/cross-links, and `source-inventory.json`; created `c3dbd025` (`[ai-context] 세션 로그 업데이트`) for the relay/context group. No push, revert, `update_goal`, product code edit, or Hanwoo T-251 retry was performed. |
| Next Priorities | Verification before adoption passed: `py -3.13 execution\llm_wiki_audit.py --json` (`40` pages, `39` numbered, `201` sources, pass), path-limited `git diff --check` with only existing CRLF warnings, graph detect risk `0.00`, staged `code_review_gate.py --staged --json` pass for the docs group, and staged non-code gate pass for `.ai`. Post-commit `session_orient.py --json` reported a clean worktree, branch `main` ahead `848`, open PRs `0`; `next_experiment_selector.py` moved from `dirty_worktree_handoff_current` to `blocked_publish_only/current_head_release_checks_unproven`; readiness score is `96`, with local blockers `0`, publish blockers `1`, external blockers `1`. Remaining boundaries: do not push without explicit authorization; after a push/user push, required current-HEAD Actions are `root-quality-gate` and `active-project-matrix`; do not retry Hanwoo T-251 until Supabase credentials are reset. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1718 claude-goal simple command help copy UX fix**. Continued the `/goal` UX/UI computer-use loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving concurrent T-1716/T-1717 updates. Direct CLI use reproduced that `status --help`, `pause --help`, and `clear --help` showed only `usage` plus `-h/--help`, with no description of whether the command reads state, pauses continuation, resumes it, or clears state. Classification: copy/feedback UX gap, directly observed from the help surfaces. Root cause: `_add_simple_parser()` created simple subcommands without `help` or `description`, so argparse had no command-purpose text to render. Fixed `goal/scripts/claude_goal.py` by adding `SIMPLE_COMMAND_DESCRIPTIONS` for `status`, `pause`, `resume`, and clear aliases and wiring those descriptions into the simple subparsers; added regression coverage in `tests/test_claude_goal.py` for direct and slash help. |
| Next Priorities | Verification passed in `claude-goal/`: direct temp-DB CLI QA now reports `status/pause/resume/clear --help` include command-effect descriptions, slash `invoke <command> --help` matches direct help, and no `Action: set` appears; evidence is `.tmp/claude-goal-t1718-cli-qa.json`. Focused pytest passed (`3 passed, 96 deselected`); first full pytest in a parallel batch had a transient child `SyntaxError` while `py_compile` and Ruff passed; isolated full pytest rerun passed (`140 passed`). `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. External refs checked Python argparse help behavior, GitHub CLI subcommand help, and Vercel CLI `--help` global option. Removed this loop's temp DB and pytest basetemp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next directly reproduced CLI friction point if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1719 claude-goal trailing help-token non-mutating UX fix**. Continued the `/goal` CLI UX loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving the live T-1718 simple-command help-copy relay and existing WIP. Reproduced earlier with isolated temp goal DBs that `set --help extra`, `set -h extra`, and `invoke --help extra` could be treated as goal/objective text, while `invoke complete --help extra` could fall into custom option validation instead of help. Root cause: help handling covered exact `-h`/`--help` forms but not a first help token followed by extra args across legacy set/invoke dispatch and slash subcommand help routing. Fixed `goal/scripts/claude_goal.py` by parsing the leading raw invoke token with `shlex.split()`, checking first positional help flags for direct `invoke`, routing slash subcommand trailing help through direct argparse help, and letting legacy `set -h/--help ...` fall through to argparse; updated `tests/test_claude_goal.py` to cover trailing help args for direct set, direct invoke, and slash subcommands. |
| Next Priorities | Verification passed in `claude-goal/`: direct isolated CLI QA in workspace `.tmp/claude-goal-t1719-trailing-help-qa.json` confirms `set --help extra`, `set -h extra`, `invoke --help extra`, and `invoke complete --help extra` all return `0`, show help, avoid `Action: set`, and leave `status` at `No goal is currently set`; focused help pytest passed (`3 passed, 96 deselected`); full pytest had transient/untrusted runs first (parallel basetemp loss, then isolated child `SyntaxError`/temp-path flakes whose failed cases passed in isolation), and the final isolated rerun passed (`141 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`; diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's pytest basetemp dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next directly reproduced CLI friction point if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1720 LLM Wiki side-effect/idempotency replay boundary reinforcement**. Continued the LLM Wiki autonomous reinforcement loop from live workspace state after `next_experiment_selector.py` was publish-blocked without push authorization. Selected a distinct local docs gap: external side-effect writes can be retried independently from publish approval, credentials, or provider retry timing. Added `docs/wiki/llm/40-side-effect-idempotency-replay-boundary.md`, covering Notion page creation, Cloudinary uploads, X post creation, provider-native idempotency, operation keys, side-effect ledgers, unknown-write reconciliation, replay artifacts, and A/B operating choices. Updated README and cross-links from rate-limit/retry, tool-calling/harness, safety/publish gates, and credential boundaries; regenerated `docs/wiki/llm/source-inventory.json` to 212 sources. |
| Next Priorities | Verification passed: `py -3.13 execution\llm_wiki_audit.py --write-source-inventory --json`, no-write audit, strict manifest audit, strict release evidence regenerated after the relay commit, focused LLM Wiki/objective pytest (`31 passed`), path-limited `git diff --check`, graph detect risk `0.00`, staged docs scope guard low risk, and commit hook code-review gate pass. The docs commit is `docs(llm-wiki): add side-effect replay boundary`, followed by this `.ai` relay commit. Release packet has strict evidence pass/head-match/unexpected `0`; the remaining blockers are explicit push/user push for current-head `root-quality-gate`/`active-project-matrix`, plus user-owned Hanwoo T-251 until Supabase credentials are reset. No push, product code edit, `update_goal`, or T-251 retry was performed. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1720b claude-goal operation command help UX fix**. Continued the `/goal` CLI UX loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving the existing `T-1720` LLM Wiki record by using the task-id collision fallback (`next_task_id.py --json` suggested `T-1720`, already present). Direct CLI use reproduced that operation-oriented commands such as `set --help`, `json --help`, `pause-stale --help`, and `stop-hook --help` were thin or inconsistent: direct help lacked practical command descriptions/argument copy for several commands, and slash help did not route `set/json/stop-hook` through the matching direct argparse help. Fixed `goal/scripts/claude_goal.py` by adding `set`, `json`, and `stop-hook` to slash subcommand help routing, adding useful descriptions/argument help for `set`, `json`, and `pause-stale`, and documenting `stop-hook` in the simple command description map; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct isolated CLI QA in `.tmp/claude-goal-t1720b-operation-help-qa.json` confirms `set/json/pause-stale/stop-hook --help` return `0`, direct and slash help match, no `Action: set` appears, and status remains `No goal is currently set`; focused help pytest passed (`5 passed, 95 deselected`); full pytest passed (`141 passed`) after rerunning an earlier transient child `SyntaxError` with `py_compile` still clean; `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` passed with risk `0.00` and only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp SQLite/pytest basetemp dirs after preserving the JSON evidence; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next directly reproduced CLI friction point if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1721 claude-goal direct pause/resume no-goal traceback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving the existing T-1720/T-1720b relay records and WIP. Reproduced with isolated temp state that direct `python goal/scripts/claude_goal.py pause` and `resume` returned exit `1` and printed a traceback for the normal no-goal condition, while slash `invoke pause`/`invoke resume` already returned clean `goal error: no goal is set for this Claude session` with exit `2`. Root cause: `_handle_main_status_update()` let `update_status()` `ValueError` bubble to the generic top-level exception handler, which prints tracebacks for unexpected errors; slash invoke catches the same `ValueError` as a user-facing command error. Fixed `goal/scripts/claude_goal.py` by catching `ValueError` in the direct status-update handler and returning exit `2` without traceback; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct isolated CLI QA in workspace `.tmp/claude-goal-t1721-pause-no-goal-qa.json` confirms direct `pause`/`resume`, slash `invoke pause`/`invoke resume`, and direct `complete` all return clean exit `2` no-goal errors with no traceback; focused pytest passed (`2 passed, 99 deselected`); final isolated full pytest passed (`142 passed`, after one untrusted transient child `SyntaxError` run); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp repro/pytest dirs; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging from the next directly reproduced CLI or verifier defect if requested. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1722 claude-goal Stop-hook malformed continuation cap fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving the existing T-1721/T-1720b relay records and WIP. Reproduced with isolated temp state that an active goal plus `CLAUDE_GOAL_MAX_STOP_CONTINUES=not-an-int` made `python goal/scripts/claude_goal.py stop-hook` exit `1` and print a Python traceback from bare `int(...)` parsing. Root cause: `_stop_hook_max_continues()` parsed the optional hook env var without handling `ValueError`, so a local config typo escaped into the generic top-level traceback handler and prevented the normal active-goal Stop-hook block JSON. Fixed `goal/scripts/claude_goal.py` by adding `STOP_HOOK_MAX_CONTINUES_ENV`/`DEFAULT_STOP_HOOK_MAX_CONTINUES`, parsing the env var with fallback to `500`, and emitting a clear stderr warning; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct isolated Stop-hook QA in workspace `.tmp/claude-goal-t1722-stop-hook-bad-cap-qa.json` confirms malformed `CLAUDE_GOAL_MAX_STOP_CONTINUES` now returns exit `0`, emits normal active-goal block JSON on stdout, preserves the active goal objective in the block reason, warns `goal warning: invalid CLAUDE_GOAL_MAX_STOP_CONTINUES='not-an-int'; using 500` on stderr, and prints no traceback; focused pytest passed (`2 passed, 100 deselected`); full isolated pytest passed (`143 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp DB/pytest dirs after preserving JSON evidence; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1723 claude-goal install doctor Stop-hook cap env verifier fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving the existing T-1722/T-1721 relay records and WIP. Refreshed the 0-stage inventory from current evidence: completed T-1703 through T-1722 were excluded; the highest-priority new candidate was Stop-hook cap configuration because it can affect automatic continuation. Reproduced with isolated temp settings and goal DB that `doctor --install --json` returned overall `ok` and `goal_stop_hook_env: ok` when the installed structured hook had `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP=0` and `CLAUDE_GOAL_MAX_STOP_CONTINUES=not-an-int`. Root cause: `_goal_stop_hook_env_health()` only checked truthiness/presence of cap env vars, not whether they were valid positive integers. Fixed `goal/scripts/claude_goal.py` by adding `STOP_HOOK_BLOCK_CAP_ENV`, validating both Stop-hook cap env vars as positive integers, and warning with a hook-refresh action for invalid values; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct isolated doctor QA in workspace `.tmp/claude-goal-t1723-doctor-cap-env-qa.json` confirms invalid cap env now returns overall `warn`, marks `goal_stop_hook_env` as `warn`, names both invalid env vars, includes `refresh_stop_hook` and `rerun_install_doctor` next actions, and emits no stderr; focused/cluster pytest passed (`5 passed, 98 deselected` after formatting); full isolated pytest passed (`144 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp repro/QA-state/pytest dirs after preserving JSON evidence; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1724 claude-goal runtime Stop-hook non-positive continuation cap fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving the existing T-1723/T-1722 relay records and WIP. Refreshed the 0-stage inventory from current evidence: completed T-1703 through T-1723 were excluded; the highest-priority new candidate was runtime Stop-hook non-positive cap handling because it directly affects automatic continuation. Reproduced with isolated active-goal temp DBs that `CLAUDE_GOAL_MAX_STOP_CONTINUES=0` and `-1` made `python goal/scripts/claude_goal.py stop-hook` return a limit payload instead of the active-goal block; `-1` produced the impossible message `auto-continuation stopped after -1 Stop-hook continuations`. Root cause: `_stop_hook_max_continues()` parsed the env var as an integer but did not validate that it was positive. Fixed `goal/scripts/claude_goal.py` so non-positive values follow the same warning plus default `500` fallback path as malformed values; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct isolated Stop-hook QA in workspace `.tmp/claude-goal-t1724-stop-hook-nonpositive-cap-qa.json` confirms caps `0` and `-1` now return exit `0`, emit normal active-goal block JSON on stdout, preserve the objective, warn `goal warning: invalid CLAUDE_GOAL_MAX_STOP_CONTINUES='<value>'; using 500` on stderr, do not emit the limit payload, and print no traceback; focused Stop-hook pytest passed (`7 passed, 97 deselected`); full isolated pytest passed (`145 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. Removed this loop's temp repro/QA-state/pytest dirs after preserving JSON evidence; no staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1725 claude-goal installer invalid Stop-hook cap argument fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the T-1724/T-1723 relay records. Direct CLI reproduction showed `python goal/scripts/install_goal.py <settings> <script> --stop-block-cap 0 --max-stop-continues not-an-int --no-backup` exited `0`, created `settings.json`, and wrote invalid structured hook env values: `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP=0` and `CLAUDE_GOAL_MAX_STOP_CONTINUES=not-an-int`. The current install doctor immediately reported overall `warn` with `goal_stop_hook_env: warn`, so the installer could produce a known-unhealthy config. Root cause: installer CLI options and the direct hook builder accepted cap values as unchecked strings. Fixed `goal/scripts/install_goal.py` so both cap values are validated as positive integers at argparse entry and again before hook construction; added regression coverage in `tests/test_install_goal.py` for CLI rejection and direct API rejection without writing settings. |
| Next Priorities | Verification passed in `claude-goal/`: direct after-fix repro returns exit `2`, reports `argument --stop-block-cap: --stop-block-cap must be a positive integer`, and does not create settings; focused installer pytest passed (`13 passed`); install doctor cluster pytest passed (`43 passed, 61 deselected`); full isolated pytest passed (`149 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\install_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1725-installer-invalid-cap-args-qa.json`; nested temp repro dirs were removed. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1726 claude-goal installer restore-backup CLI traceback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and T-1725/T-1724 relay records. Direct CLI reproduction showed `python goal/scripts/install_goal.py <settings> --restore-backup` with no `settings.json.bak` printed a full Python `FileNotFoundError` traceback. A second repro with malformed `settings.json.bak` and `--restore-backup --yes` printed a full `json.decoder.JSONDecodeError` traceback. Root cause: installer `main()` called `restore_settings_backup()` without translating expected backup/settings validation failures into CLI errors. Fixed `goal/scripts/install_goal.py` so expected `FileNotFoundError`/`ValueError` failures print `goal installer error: ...` on stderr and return exit `2`; added subprocess regressions in `tests/test_install_goal.py` for missing and invalid backup restore failures. |
| Next Priorities | Verification passed in `claude-goal/`: direct after-fix repro now emits clean `goal installer error: backup does not exist: ...` or `goal installer error: Expecting value...` with no traceback; focused installer pytest passed (`15 passed`); full isolated pytest passed (`151 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\install_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1726-restore-backup-cli-errors-qa.json`; nested temp repro/full-test dirs and root pytest basetemps were removed after locks cleared. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1726b claude-goal installer help-copy UX fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and using the suffix fallback because `T-1726` was already present as the installer restore-backup CLI traceback fix. Direct CLI use showed `python goal/scripts/install_goal.py --help` listed `settings_path`, `script_path`, `--python-executable`, cap options, and backup options without explaining what each input controls. Classification: copy/feedback UX gap on the installer command surface, directly observed from the help screen. Root cause: `parse_args()` supplied no `help` text for the positional arguments and most options. Fixed `goal/scripts/install_goal.py` by adding practical help copy for the settings path, script path, Python executable, Stop-hook caps, backup skip, and restore dry-run options; added regression coverage in `tests/test_install_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct after-fix CLI QA in workspace `.tmp/claude-goal-t1726b-install-help-copy-qa.json` confirms `install_goal.py --help` returns exit `0`, emits no stderr, writes no settings file, and describes the settings path, script path, Python executable, Stop-hook caps, no-backup behavior, and restore-backup behavior. Focused installer pytest passed with repo-local basetemp (`16 passed` after the first run hit the known Windows temp permission issue); full isolated pytest passed (`152 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\install_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1728 claude-goal output sink write-error traceback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and T-1727 relay records. Direct isolated CLI reproduction showed `contract --markdown-output <existing-directory>`, `contract --json-output <existing-directory>`, `doctor --install --markdown-output <existing-directory>`, and `doctor --install --json-output <existing-directory>` returned exit `1` and printed Python tracebacks from `PermissionError` while trying to open a directory as an artifact file. Root cause: `write_markdown_file()` and `write_json_file()` let `OSError` escape directly, and direct `doctor` handling did not translate expected writer `ValueError` failures into user-facing CLI errors. Fixed `goal/scripts/claude_goal.py` so markdown/json output writes report `cannot write ...` as `ValueError`, and direct `doctor` returns exit `2` with `goal error` for those expected failures; added contract and install-doctor output directory regressions in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct after-fix repro for the four original output-directory commands now returns exit `2`, leaves stdout empty, includes `goal error: cannot write ...` on stderr, and prints no traceback. Focused pytest passed (`5 passed, 102 deselected`); full isolated pytest passed (`155 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1728-output-sink-errors-qa.json`; T-1728 repro/pytest temp dirs were removed. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1727 claude-goal cp949 output UnicodeEncodeError fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the T-1726/T-1726b relay records. Direct isolated CLI reproduction with objective `alpha \u2014 beta` and cp949 output showed `set` exited `1` after mutating state and `status` exited `1` with `UnicodeEncodeError`/`Traceback` from `print(render_invoke_result(...))`. Root cause: CLI rendering produced Unicode text successfully, but stdout/stderr used the process text stream's strict cp949 error handler, so unencodable goal text crashed during output. Fixed `goal/scripts/claude_goal.py` by reconfiguring stdout/stderr to keep the active encoding while using `backslashreplace` for unencodable characters; added cp949 subprocess regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct after-fix repro with `PYTHONIOENCODING=cp949` now returns exit `0` for `set` and `status`, renders `alpha \\u2014 beta`, emits empty stderr, and prints no traceback. Focused pytest passed (`3 passed, 102 deselected`); full isolated pytest passed (`153 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1727-cp949-output-qa.json`; nested T-1727 repro dirs and root pytest basetemps were removed. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1730 claude-goal direct pause-stale negative stale-days fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and T-1729 relay records. Direct isolated CLI reproduction showed `pause-stale --stale-days -1` and `pause-stale --stale-days -1 --json` returned exit `0` and could treat current active goals as stale, while slash `invoke pause-stale --stale-days -1` correctly returned exit `2` with `--stale-days must be zero or greater`. Root cause: slash `parse_pause_stale_args()` validated negative stale-day thresholds through `_parse_stale_command_args()`, but direct `_pause_stale_args_from_main_namespace()` copied argparse's integer without the same non-negative check. Fixed `goal/scripts/claude_goal.py` by validating direct `pause-stale` stale_days and returning clean `goal error` exit `2`; added regression coverage in `tests/test_claude_goal.py` proving direct and slash negative values reject without mutating the active goal. |
| Next Priorities | Verification passed in `claude-goal/`: direct after-fix repro for `pause-stale --stale-days -1`, slash invoke, and JSON variants now returns exit `2`, leaves stdout empty, emits `goal error: --stale-days must be zero or greater`, and prints no traceback. Focused pytest passed (`3 passed, 105 deselected`); full isolated pytest passed (`158 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1730-pause-stale-negative-days-qa.json`; T-1730 pytest temp dirs were removed. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1729 claude-goal installer settings_path filesystem-error traceback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and T-1728 relay records. Direct isolated CLI reproduction showed `python goal/scripts/install_goal.py <settings-directory> --restore-backup --yes` and `python goal/scripts/install_goal.py <settings-directory> <script-path> --no-backup` returned exit `1` and printed Python tracebacks from expected filesystem `OSError`/`PermissionError` paths. Root cause: installer `main()` converted `FileNotFoundError` and `ValueError` to `goal installer error` but let expected `OSError` failures from reading or replacing a directory `settings_path` escape to the top-level traceback. Fixed `goal/scripts/install_goal.py` by handling `OSError` alongside `ValueError`; added restore and install CLI regressions in `tests/test_install_goal.py` for directory `settings_path` inputs. |
| Next Priorities | Verification passed in `claude-goal/`: direct after-fix repro for both original installer directory-path commands now returns exit `2`, leaves stdout empty, includes `goal installer error` on stderr, and prints no traceback. Focused installer pytest passed (`8 passed, 10 deselected`); full isolated pytest passed (`157 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\install_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1729-installer-settings-path-errors-qa.json`; T-1729 pytest temp dirs were removed. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1730b claude-goal contract/doctor artifact-help copy UX fix**. Continued the `/goal` direct CLI UX loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving the existing T-1730 pause-stale relay record and all nested WIP. Direct CLI use showed `python goal/scripts/claude_goal.py contract --help` and `python goal/scripts/claude_goal.py doctor --install --help` rendered high-risk reviewer/artifact flags (`--schema`, `--markdown-output`, `--json-output`, `--verify-json`, `--verify-schema`, `--github-step-summary`) with little or no option-purpose copy. Classification: copy/accessibility UX gap on reviewer evidence commands, not a traceback. External references checked: Python argparse help descriptions, GitHub CLI subcommand help, and Vercel CLI detailed command help. A/B: baseline flags-only help vs candidate descriptive option copy; adopted the candidate because it lowers reviewer/operator error without changing command behavior. Fixed `goal/scripts/claude_goal.py` by adding command descriptions and practical option help for `contract` and `doctor`; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct after-fix QA confirms `contract --help`, `invoke contract --help`, `doctor --install --help`, and `invoke doctor --help` return exit `0`, emit no stderr/traceback, keep slash help identical to direct help, include reviewer/artifact option descriptions, and do not mutate goal state. Focused help pytest passed (`4 passed, 106 deselected`); full isolated pytest passed (`160 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1730b-review-help-copy-qa.json`. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1731 claude-goal direct doctor negative stale-days fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the T-1730/T-1730b relay records. Direct isolated CLI reproduction showed `doctor --stale-days -1` and `doctor --stale-days -1 --json` returned exit `0` and rendered stale reporting as though the threshold were `0` days, while slash `invoke doctor --stale-days -1` correctly returned exit `2` with `--stale-days must be zero or greater`. Root cause: slash `parse_doctor_args()` validated negative stale-day thresholds through `_parse_stale_command_args()`, but direct `_doctor_args_from_main_namespace()` copied argparse's integer without the same non-negative check; the first fix attempt also showed namespace conversion had to be inside `handle_main_doctor()`'s `ValueError` boundary. Fixed `goal/scripts/claude_goal.py` by validating direct doctor stale_days and moving doctor namespace conversion inside the existing clean CLI error handler; added direct/slash regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct after-fix repro for `doctor --stale-days -1`, slash invoke, and JSON variants now returns exit `2`, leaves stdout empty, emits `goal error: --stale-days must be zero or greater`, and prints no traceback. Focused pytest passed (`3 passed, 107 deselected`); an initial full-suite run had a transient help-wrap assertion failure, then the focused help test passed (`1 passed`) and full isolated rerun passed (`160 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1731-doctor-negative-stale-days-qa.json`; T-1731 pytest temp dirs were removed. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-08 |
| Tool | Codex |
| Work | **T-1732 claude-goal slash simple-command extra-arg guard**. Continued the `/goal` CLI UX loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the T-1731 relay record. Direct isolated CLI reproduction showed direct simple state commands such as `status extra`, `pause extra`, `resume extra`, and clear aliases returned argparse exit `2`, while slash `invoke status extra` silently ran status and slash `invoke pause/resume/clear-alias extra` performed the state mutation. This could pause or clear an active goal from a trailing typo. External reference checked: Python argparse documents invalid arguments as parser errors with usage/error output. Root cause: `_render_invoke_db_command()` handled simple invoke commands without checking `InvokeCommand.rest`; help routing already ran earlier, so trailing non-help args reached DB state changes. Fixed `goal/scripts/claude_goal.py` by adding `INVOKE_SIMPLE_COMMANDS_WITHOUT_ARGS` plus a pre-DB `_reject_invoke_simple_command_args()` guard, and added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: direct after-fix QA in workspace `.tmp/claude-goal-t1732-simple-invoke-extra-args-qa.json` confirms slash `invoke status/show/get/menu/pause/resume/cancel/clear/none/off/reset/stop extra` now returns exit `2`, leaves stdout empty, emits `goal error: <command> does not accept arguments: extra`, prints no traceback, and preserves the active goal; direct simple commands still reject extra args through argparse; `invoke clear --help extra` still renders help and remains non-mutating. Focused pytest passed (`3 passed, 108 deselected`); full isolated pytest passed (`162 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1732b claude-goal DB path directory traceback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the concurrently recorded T-1732 slash simple-command guard by using the suffix fallback. Direct isolated CLI reproduction with `CLAUDE_GOAL_DB` pointing at an existing directory showed `status`, `json`, `doctor`, `set objective`, `invoke status`, and `stop-hook` all returned exit `1` and printed `sqlite3.OperationalError` tracebacks. Root cause: `sqlite_connect()` let expected local DB setup/connect/init failures (`OSError`/`sqlite3.Error`) escape to generic exception handlers; parsed commands printed tracebacks, and legacy `set` did not distinguish storage setup failures from command logic errors. Fixed `goal/scripts/claude_goal.py` by wrapping storage failures in `GoalStorageError`, returning exit `2` for legacy storage failures, and handling parsed-command `ValueError` without traceback; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix repro for `status`, `json`, `doctor`, `set objective`, `invoke status`, and `stop-hook` with `CLAUDE_GOAL_DB` set to a directory now returns exit `2`, leaves stdout empty, emits `goal error: cannot open goal database: ... unable to open database file`, and prints no traceback or `sqlite3.OperationalError`. Focused pytest passed (`3 passed, 109 deselected`); full isolated pytest passed (`162 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1732b-db-path-directory-qa.json`; this loop's temp dirs were removed. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **auto-research debug-loop inventory publish-boundary classifier fix**. Continued the LLM Wiki/auto-research loop from the live selector state. Found that `.agents/skills/auto-research/scripts/debug_loop_inventory.py` could report a clean `blocked_publish_only/current_head_release_checks_unproven` state as `Dirty Handoff Boundary Blocks New Product Edits`, and could keep a Blind-to-X handoff blocker even when `dirty_paths` was empty. Fixed the helper to derive the first blocker from selector kind plus dirty count, report the publish/current-head Actions boundary when the worktree is clean, and only emit the Blind-to-X dirty-path blocker when dirty paths exist. Added focused regression coverage in `workspace/tests/test_auto_research_debug_loop_inventory.py`. |
| Next Priorities | Verification passed: focused debug inventory pytest (`4 passed`) with repo-local `--basetemp`, related auto-research pytest (`72 passed`), Ruff check, Ruff format check, `py_compile`, path-limited `git diff --check` with CRLF warnings only, code-review graph detect risk `0.00`, `execution/code_review_gate.py --base HEAD --json` pass, and A/B `adopt_candidate` (`score_delta=0.9047619047619049`). Official refs checked: Git status porcelain/branch ahead metadata and GitHub Actions event/ref behavior. Current root remains dirty and handoff-only: auto-research code/test files plus existing `.ai` relay files; selector is `dirty_worktree_handoff_current`; release packet is `blocked_dirty_worktree`; T-251 remains user-owned; no stage, commit, push, revert, `update_goal`, or T-251 retry was performed. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1733 claude-goal slash operation-command parity fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP. Direct isolated CLI reproduction showed direct `json` returned `null` and direct `stop-hook` without a goal was non-mutating, while slash `invoke json`, `invoke json extra`, `invoke stop-hook`, and `invoke stop-hook extra` fell through to `set_goal()` and created accidental objectives. Root cause: slash invoke dispatch treated operation commands without explicit handlers as objective text; `json` had no invoke route and `stop-hook` was not covered by the no-extra-args guard. Fixed `goal/scripts/claude_goal.py` by adding explicit invoke routing for `json` and `stop-hook`, argument validation, and empty-output-safe printing for both main and legacy invoke paths; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1733-invoke-operation-commands-qa.json` confirms `invoke json` matches direct `json` without mutation, `invoke stop-hook` matches direct Stop-hook behavior, empty Stop-hook output stays empty, and `invoke json extra` / `invoke stop-hook extra` return clean exit `2` errors while preserving active goals. Focused pytest passed (`3 passed, 111 deselected`); focused order-dependency diagnosis passed (`3 passed, 111 deselected`); `doctor_install` group passed (`44 passed, 70 deselected`); full `tests/test_claude_goal.py` passed (`114 passed`); final full isolated pytest passed (`165 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1733b claude-goal installer cp949 Unicode output fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the concurrently recorded T-1733 invoke-operation work by using the suffix fallback. Direct isolated CLI reproduction with `PYTHONIOENCODING=cp949` and an installer `settings_path` containing U+2014 showed `goal/scripts/install_goal.py` wrote `settings.json`, then returned exit `1` with a `UnicodeEncodeError` traceback while printing `Updated Stop hook in ...`. Root cause: installer stdout/stderr still used strict process encoding, unlike `claude_goal.py` after T-1727. Fixed `goal/scripts/install_goal.py` by reconfiguring stdout/stderr to preserve the active encoding while using `backslashreplace`; added subprocess regression coverage in `tests/test_install_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix cp949 repro now returns exit `0`, prints `Updated Stop hook in .tmp\t1733-install-cp949-after\path-\u2014-case\settings.json` and `Goal Stop hooks: 1`, emits empty stderr, writes settings, and prints no traceback or `UnicodeEncodeError`. Focused installer pytest passed (`5 passed, 14 deselected`); cp949-only installer pytest passed (`1 passed`); a stale-child full-suite run initially failed with transient/concurrent old-source errors, then focused stale-child probe passed (`1 passed`) and full isolated rerun passed (`165 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\install_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1733b-install-cp949-output-qa.json`; this loop's temp dirs were removed. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1734 claude-goal static schema DB-boundary fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and avoiding the completed T-1733/T-1733b surfaces. Direct isolated reproduction with `CLAUDE_GOAL_DB` pointing at an existing directory showed static schema commands that should not need goal state failed before rendering schema: `complete --schema`, `invoke complete --schema`, and `invoke contract --schema` returned exit `2` with `goal error: cannot open goal database: ... unable to open database file`. Direct `contract --schema`, direct `doctor --install --schema`, and `invoke doctor --install --schema` already worked, proving this was a routing inconsistency rather than a global schema-output failure. Root cause: direct `handle_main_complete()` and slash `invoke()` entered `sqlite_connect()` before checking schema-only `complete`/`contract` paths. Fixed `goal/scripts/claude_goal.py` by returning completion-audit schema before opening DB and routing slash `complete/contract --schema` through non-DB schema output; extended regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix repro with `CLAUDE_GOAL_DB` set to a directory now returns exit `0`, emits schema JSON, leaves stderr empty, and prints no traceback for `complete --schema`, `contract --schema`, `doctor --install --schema`, `invoke complete --schema`, `invoke contract --schema`, and `invoke doctor --install --schema`. Focused schema pytest passed (`3 passed, 111 deselected`); full isolated pytest passed (`165 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1734-static-schema-db-boundary-qa.json`; this loop's temp dirs were removed. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1735 claude-goal installer blank python executable fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1734 schema DB-boundary work. Direct isolated installer reproduction showed `python goal/scripts/install_goal.py <settings> <script> --no-backup --python-executable "   "` returned exit `0`, wrote `settings.json`, and installed a structured Stop hook with `"command": "   "`. Running `doctor --install --json` against that settings file immediately reported overall `fail` with `goal_stop_hook_command: command does not resolve:    `. Empty-string `--python-executable ""` fell back to `sys.executable`, so the confirmed bug is whitespace-only values. Root cause: the installer validated Stop-hook cap arguments but treated explicit `python_executable` as trusted text; whitespace-only strings are truthy and were written directly as the hook command. Fixed `goal/scripts/install_goal.py` by normalizing explicit Python executable values with `strip()` and rejecting blank results through both argparse and `build_stop_hook()`/`install_settings()`; added direct API and CLI regression coverage in `tests/test_install_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix repro for `--python-executable "   "` now returns exit `2`, leaves stdout empty, emits `argument --python-executable: python_executable must not be blank`, writes no settings file, and prints no traceback. Focused installer pytest passed (`5 passed, 16 deselected`); full isolated pytest passed (`167 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\install_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1735-installer-blank-python-executable-qa.json`; this loop's temp dirs were removed. No staging, commit, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1735b claude-goal contract verify-json artifact preflight fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and using the suffix fallback because T-1735 was already recorded as the installer blank-python-executable fix. Direct isolated reproduction showed `contract --verify-json missing-contract.json` with a normal DB reported the actionable missing artifact error, but the same direct/slash command with `CLAUDE_GOAL_DB` pointing at an existing directory reported `cannot open goal database` before validating the input artifact. Root cause: direct and slash contract verification opened SQLite before preflighting verify-json artifact/schema inputs. Fixed `goal/scripts/claude_goal.py` by adding a contract verify-json preflight before DB-dependent current-goal comparison; added regression coverage in `tests/test_claude_goal.py`. Official refs checked: Python argparse invalid-argument behavior and pathlib file-read/FileNotFoundError behavior. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1735b-contract-verify-json-preflight-qa.json` confirms direct and slash `contract --verify-json <missing>` with `CLAUDE_GOAL_DB` set to a directory return exit `2`, leave stdout empty, report `cannot read goal operating contract JSON artifact`, do not show `cannot open goal database`, and print no traceback. Focused regression pytest passed (`1 passed`); contract cluster pytest passed (`15 passed, 100 deselected`); diagnostic pair passed (`2 passed, 113 deselected`); final full isolated pytest passed (`168 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. An earlier full-suite attempt failed due the known Windows `--basetemp` directory-loss pattern, then focused diagnosis and fresh full rerun passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1736 claude-goal Stop-hook non-object JSON payload fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1735/T-1735b surfaces. Direct isolated Stop-hook reproduction with an active goal showed `python goal/scripts/claude_goal.py stop-hook` tolerated empty stdin, malformed JSON, `{}`, `[]`, and `null`, but valid non-object JSON payloads such as `"hello"` and `123` returned exit `1` with `AttributeError: '<type>' object has no attribute 'get'` and a Python traceback; a non-empty array has the same root cause. Root cause: `_read_stop_hook_data()` caught JSON decode failures but returned any parsed JSON value, while `candidate_session_ids()` assumes dict-like hook data and calls `.get()`. Fixed `goal/scripts/claude_goal.py` by normalizing parsed Stop-hook stdin to `{}` unless it is a JSON object; added regression coverage in `tests/test_claude_goal.py` for string, number, and non-empty array payloads. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix repro for `"hello"`, `123`, and `["session_id", "t1736-session"]` stdin now returns exit `0`, emits normal active-goal block JSON, leaves stderr empty, and prints no traceback. Focused Stop-hook pytest passed (`5 passed, 111 deselected`); full isolated pytest passed (`169 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1736-stop-hook-non-object-payload-qa.json`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1737b claude-goal Stop-hook non-string hook payload field fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1736 surface. Used suffix fallback after finding an existing `.tmp/claude-goal-t1737-complete-audit-preflight-qa.json` artifact for a distinct complete-audit preflight loop. Direct isolated Stop-hook reproduction with an active goal showed object payloads with malformed internal field types still crashed: `{"cwd":123}`, `{"cwd":["x"]}`, and `{"cwd":{"path":"x"}}` returned exit `1` with `AttributeError: '<type>' object has no attribute 'encode'`; `{"session_id":["t1737-session"]}` and `{"session_id":{"id":"t1737-session"}}` returned exit `1` with `sqlite3.ProgrammingError` while binding the session candidate. Root cause: `candidate_session_ids()` trusted `hook_data.session_id` and `hook_data.cwd` as strings; `cwd_session_id()` hashes `.encode()` and `get_goal()` binds session candidates into SQLite. Fixed `goal/scripts/claude_goal.py` by normalizing hook payload fields through `_hook_string()` before deriving candidate session IDs; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix repro for non-string `cwd` and `session_id` hook fields now returns exit `0`, emits normal active-goal block JSON, leaves stderr empty, and prints no traceback. Focused Stop-hook pytest passed (`6 passed, 111 deselected`); full isolated pytest passed (`170 passed`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. QA evidence is `.tmp/claude-goal-t1737b-stop-hook-non-string-fields-qa.json`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1737 claude-goal completion-audit artifact preflight fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1736 Stop-hook surface. Direct isolated reproduction showed `complete --audit-json <missing>` and `invoke complete --audit-json <missing>` normally report the actionable missing completion-audit artifact error when a DB can open, but with `CLAUDE_GOAL_DB` pointing at an existing directory both reported `cannot open goal database` before validating the audit artifact. Root cause: direct and slash completion-audit verification opened SQLite before preflighting artifact/schema/static completion-audit inputs. Fixed `goal/scripts/claude_goal.py` by splitting static completion-audit identity checks, adding a DB-free audit JSON preflight, reusing it in the full verifier, and calling it before `sqlite_connect()` for direct and slash `complete --audit-json`; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1737-complete-audit-preflight-qa.json` confirms direct and slash `complete --audit-json <missing>` with `CLAUDE_GOAL_DB` set to a directory return exit `2`, leave stdout empty, report `cannot read completion audit JSON artifact` plus the existing recovery hint, do not show `cannot open goal database`, and print no traceback. Focused regression pytest passed (`1 passed`); completion-audit cluster pytest passed (`9 passed, 109 deselected`); focused diagnosis for the one first-full-run failure passed (`1 passed`); fresh full isolated pytest rerun passed (`171 passed`). Static gates passed after scoped Ruff formatting: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. The first full-suite attempt failed only on the known order-sensitive `test_install_action_catalog_ids_are_reachable` missing `start_or_resume_goal`, then focused diagnosis and fresh full rerun passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1738 claude-goal invoke-json argument preflight fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1737/T-1737b surfaces. Direct isolated CLI reproduction showed direct `json --unknown` and `json --session-id` returned immediate argparse input errors, and slash `invoke json --unknown` / `invoke json --session-id` returned clean `goal error` input messages when the DB could open; however, with `CLAUDE_GOAL_DB` pointing at an existing directory, slash `invoke json ...` reported `cannot open goal database` before validating the bad JSON command arguments. Root cause: slash `invoke` routed `json` through `_render_invoke_db_command()`, where `parse_json_args()` ran only after `sqlite_connect()`. Fixed `goal/scripts/claude_goal.py` by adding a DB-free `json` argument preflight in `invoke()` before opening SQLite; added regression coverage in `tests/test_claude_goal.py`. Official reference checked: Python argparse invalid-argument and missing-value behavior. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1738-invoke-json-preflight-qa.json` confirms slash `invoke json --unknown` and `invoke json --session-id` with `CLAUDE_GOAL_DB` set to a directory return exit `2`, leave stdout empty, report `goal error: unknown json option: --unknown` / `goal error: --session-id requires a value`, do not show `cannot open goal database`, and print no traceback. A/B decision recorded in the QA artifact: baseline masked input errors behind DB open failure; candidate returns actionable input errors first, so adopted. Focused regression pytest passed (`1 passed`); JSON command cluster pytest passed (`4 passed, 115 deselected`); full isolated pytest passed (`172 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1739b claude-goal missing schema docs no-traceback fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and avoiding the already-recorded T-1738 invoke-json preflight surface plus a pre-existing nested T-1739 pause-stale artifact. Reproduced by copying `goal/scripts/claude_goal.py` into a temp install layout without `goal/docs/`: direct and slash schema commands `complete --schema`, `contract --schema`, and `doctor --install --schema` returned exit `1` with `FileNotFoundError` tracebacks. Root cause: the three schema renderers used raw `Path.read_text()` while other JSON/text artifact readers converted `OSError` into user-facing `ValueError`. Fixed `goal/scripts/claude_goal.py` to read schema files through `_read_utf8_text_file()`, added missing-docs CLI regression coverage in `tests/test_claude_goal.py`, and hardened `tests/test_claude_goal.py` plus `tests/test_install_goal.py` subprocess decoding with UTF-8/error replacement for Korean Windows paths. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1739b-schema-missing-docs-qa.json` confirms all six missing-docs schema cases return exit `2`, leave stdout empty, emit `goal error: cannot read <schema>`, and print no traceback. Focused regression pytest passed (`1 passed, 119 deselected`); schema cluster passed (`5 passed, 115 deselected`); cp949 helper regression passed (`1 passed, 119 deselected`); full `tests/test_claude_goal.py` passed (`120 passed`); installer decoding focused pytest passed (`3 passed, 18 deselected`); final full isolated pytest passed (`174 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1740 claude-goal invoke pause-stale argument preflight fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and avoiding the already-recorded T-1739b missing schema-docs surface. Direct isolated reproduction showed direct `pause-stale --stale-days -1` with `CLAUDE_GOAL_DB` pointing at a directory still returned the actionable `--stale-days must be zero or greater` input error, and slash `invoke pause-stale --stale-days -1` returned that same clean error when the DB could open; however, slash `invoke pause-stale --stale-days -1` and `invoke pause-stale --stale-days` with `CLAUDE_GOAL_DB` pointing at an existing directory reported `cannot open goal database` before validating bad `pause-stale` arguments. Root cause: slash `invoke` routed `pause-stale` through `_render_invoke_db_command()`, where `parse_pause_stale_args()` ran only after `sqlite_connect()`. Fixed `goal/scripts/claude_goal.py` by adding a DB-free `pause-stale` argument preflight in `invoke()` before opening SQLite; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1740-pause-stale-preflight-qa.json` confirms slash `invoke pause-stale --stale-days -1` and `invoke pause-stale --stale-days` with `CLAUDE_GOAL_DB` set to a directory return exit `2`, leave stdout empty, report `goal error: --stale-days must be zero or greater` / `goal error: --stale-days requires a value`, do not show `cannot open goal database`, and print no traceback. A/B decision recorded in the QA artifact: baseline masked input errors behind DB open failure; candidate returns actionable input errors first, so adopted. Focused regression pytest passed (`1 passed`); pause-stale cluster pytest passed (`3 passed, 118 deselected`); static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Initial full pytest without UTF-8 forcing failed only on Korean Windows path mojibake in existing path-output assertions (`5 failed, 169 passed`); rerun with `PYTHONUTF8=1` passed (`174 passed`). Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1741 claude-goal invoke set fallback argument preflight fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1740 `pause-stale` preflight surface. Direct isolated reproduction showed direct `set objective --token-budget nope` with `CLAUDE_GOAL_DB` pointing at a directory returned the actionable `invalid token budget: 'nope'` input error, and slash `invoke objective --token-budget nope` returned the same clean input error when the DB could open; however, slash `invoke objective --token-budget nope` and `invoke objective --token-budget` with `CLAUDE_GOAL_DB` pointing at an existing directory reported `cannot open goal database` before validating bad set fallback arguments. Root cause: slash `invoke` parsed objective-setting fallback arguments only inside `_render_invoke_db_command()` after `sqlite_connect()`. Fixed `goal/scripts/claude_goal.py` by adding a DB-free set fallback preflight in `invoke()` before opening SQLite; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1741-invoke-set-preflight-qa.json` confirms slash `invoke objective --token-budget nope` and `invoke objective --token-budget` with `CLAUDE_GOAL_DB` set to a directory return exit `2`, leave stdout empty, report `goal error: invalid token budget: 'nope'` / `goal error: --token-budget requires a value`, do not show `cannot open goal database`, and print no traceback. A/B decision recorded in the QA artifact: baseline masked input errors behind DB open failure; candidate returns actionable input errors first, so adopted. Focused regression pytest passed (`1 passed`); invoke/set cluster pytest passed (`5 passed, 117 deselected`); full isolated pytest with `PYTHONUTF8=1` passed (`175 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1742 claude-goal legacy SQLite schema migration fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1741 invoke set preflight surface. Direct isolated reproduction with a legacy `goals` table missing `active_started_at`/`metadata_json` and an `events` table missing `goal_id`/`detail` showed user-facing commands `status`, `json`, `contract`, `pause`, and `doctor` could exit `1` with `IndexError: No item with that key` tracebacks. Root cause: `init_db()` only used `CREATE TABLE IF NOT EXISTS`, so existing older SQLite tables were never migrated before readers accessed newer columns. Fixed `goal/scripts/claude_goal.py` by adding idempotent `goals`/`events` column migrations during DB initialization; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1742-legacy-schema-migration-qa.json` confirms the legacy DB repro now returns exit `0` for `status`, `json`, `contract`, `doctor --json`, and `pause`, adds missing columns, keeps `metadata` as `{}`, records the pause event, and prints no traceback. Focused regression/adjacent pytest passed (`3 passed`); full isolated pytest with `PYTHONUTF8=1` passed (`176 passed`). Static gates passed: `python -m ruff check .`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Initial pytest without repo-local `--basetemp` failed before test execution on Windows temp `PermissionError`; initial `ruff format --check .` failed only because it scanned old `.tmp` pytest copies, while `goal tests` passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1742b claude-goal contract output sink preflight fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1741 set fallback preflight surface while avoiding the already-recorded T-1742 legacy schema migration surface. Direct isolated reproduction showed `contract --json-output <existing-dir>` and slash `invoke contract --json-output <existing-dir>` with `CLAUDE_GOAL_DB` pointing at an existing directory reported `cannot open goal database` before validating the bad output sink. Root cause: default contract rendering needs current goal state, so `_render_goal_contract_args_command()` opened SQLite before any output writer saw already-invalid `--json-output` / `--markdown-output` paths. Fixed `goal/scripts/claude_goal.py` by preflighting known-bad contract markdown/json output sinks before DB open; added direct and slash regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1742b-contract-output-preflight-qa.json` confirms direct and slash `contract --markdown-output <existing-dir>` / `contract --json-output <existing-dir>` with `CLAUDE_GOAL_DB` set to a directory return exit `2`, leave stdout empty, report `goal error: cannot write markdown output` / `goal error: cannot write json output`, name the output directory leaf, do not show `cannot open goal database`, and print no traceback. A/B decision recorded in the QA artifact: baseline masked output errors behind DB open failure; candidate returns actionable output path errors first, so adopted. Focused regression pytest passed (`1 passed`); contract cluster passed under `PYTHONUTF8=1` (`5 passed, 119 deselected`) after the non-UTF8 run hit known Korean path mojibake in existing full-path assertions (`2 failed, 3 passed`); full isolated pytest with `PYTHONUTF8=1` passed (`177 passed`). Static gates passed after scoped Ruff formatting: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1743 claude-goal legacy set usage-error exit-code fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1742 legacy schema migration surface. Direct isolated reproduction showed legacy direct `set objective --token-budget nope`, `set objective --token-budget`, and unmatched-quote `set` usage errors returned exit `1` even though equivalent slash `invoke` set fallback and other argparse-style command-line usage errors return exit `2`. Root cause: the legacy set dispatcher caught all `ValueError` instances as runtime set failures and mapped them to exit `1`, even when the error came from argument parsing. Fixed `goal/scripts/claude_goal.py` by introducing `GoalUsageError` for set argument parsing failures and mapping that class to legacy exit `2`; preserved exit `1` for empty objective and duplicate active-goal state errors. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1743-set-usage-exit-code-qa.json` confirms legacy set token-budget and quote parsing usage errors now return exit `2`, leave stdout empty, preserve no-mutation behavior, and print no traceback, while empty objective still returns exit `1`. Focused regression/adjacent pytest passed (`3 passed`); full isolated pytest with `PYTHONUTF8=1` passed (`178 passed`). Static gates passed: `python -m ruff check .`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1744 claude-goal installer malformed Stop entry count fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1743 legacy set exit-code surface. Direct isolated installer reproduction with `settings.json` containing `hooks.Stop: [{ "matcher": "", "hooks": null }]` showed `python goal/scripts/install_goal.py <settings> goal/scripts/claude_goal.py --no-backup` wrote the new hook, then exited `1` with `TypeError: 'NoneType' object is not iterable` traceback while counting goal hooks. Root cause: `_remove_existing_goal_stop_hooks()` intentionally preserved malformed unrelated Stop entries, but `_count_goal_stop_hooks()` later iterated `entry.get("hooks", [])` without checking that the value was a list. Fixed `goal/scripts/install_goal.py` by making the counter skip non-object Stop entries and non-list entry hook values; hardened the test helper and added CLI regression coverage in `tests/test_install_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1744-installer-malformed-stop-entry-qa.json` confirms the malformed Stop entry install now returns exit `0`, prints `Goal Stop hooks: 1`, leaves stderr empty, preserves the malformed unrelated entries, and prints no traceback. Focused installer/adjacent pytest passed (`3 passed`); full isolated pytest with `PYTHONUTF8=1` passed (`179 passed`). Static gates passed: `python -m ruff check .`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1745b claude-goal installer backup-directory guard fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1744 installer malformed Stop entry surface. Direct isolated installer reproduction with an existing `settings.json` and pre-created `settings.json.bak` directory showed `python goal/scripts/install_goal.py <settings> goal/scripts/claude_goal.py` returned exit `0`, printed `Backup: <settings.json.bak>`, and modified `settings.json` even though no real backup file existed. Root cause: `backup_settings()` checked only `backup_path.exists()`, so any existing non-file path was accepted as an existing backup. Fixed `goal/scripts/install_goal.py` by rejecting an existing backup path unless it is a file, causing the installer to stop before writing settings; added CLI regression coverage in `tests/test_install_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1745b-installer-backup-directory-qa.json` confirms the backup-directory case now returns exit `2`, leaves stdout empty, reports `goal installer error: backup path exists but is not a file`, keeps `settings.json` unchanged, leaves the backup directory intact, and prints no traceback. Focused installer/adjacent pytest passed (`3 passed`); full isolated pytest with `PYTHONUTF8=1` passed (`181 passed`). Static gates passed: `python -m ruff check .`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1746 claude-goal doctor install output sink preflight fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the concurrently recorded T-1744/T-1745 installer surfaces. Used `execution/next_task_id.py --json` after discovering T-1744 and T-1745 were already present in shared `.ai` context. Direct isolated CLI reproduction showed `doctor --install --json-output <existing-dir>` and direct/slash `doctor --install --github-step-summary` / `doctor --install --ci` with `GITHUB_STEP_SUMMARY` pointing at an existing directory returned clean exit `2` but exposed raw Windows `Permission denied` write errors instead of the actionable `output path is a directory` preflight used by `contract`. Root cause: doctor validation checked conflicts and missing summary env, but never preflighted markdown/json output sink paths after applying the CI summary preset. Fixed `goal/scripts/claude_goal.py` by adding `_preflight_doctor_output_sinks()` and calling it for both parsed main `doctor` and slash `invoke doctor` paths after CI preset resolution; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1746-doctor-output-preflight-qa.json` confirms direct and slash `doctor --install --json-output <existing-dir>`, explicit `--github-step-summary`, and CI-enabled `GITHUB_STEP_SUMMARY` directory cases return exit `2`, leave stdout empty, report `output path is a directory`, do not show any DB/sqlite error while `CLAUDE_GOAL_DB` points at a directory, and print no traceback. Focused regression pytest passed (`2 passed, 124 deselected`); doctor/install-health cluster passed (`55 passed, 81 deselected`); full isolated pytest with `PYTHONUTF8=1` passed (`181 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1747b claude-goal invoke stale-days non-integer usage error fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1745b/T-1746 plus concurrent T-1748 installer script-path surfaces. Direct isolated slash/invoke reproduction showed `invoke doctor --stale-days nope --json`, `invoke doctor --stale-days= --json`, `invoke pause-stale --stale-days nope --json`, and `invoke pause-stale --stale-days= --json` returned exit `2` without traceback, but exposed Python conversion text (`invalid literal for int()`) instead of an option-specific usage error. Root cause: `_parse_stale_days_value()` called `int(value)` directly and let conversion failures bubble as raw Python messages. Fixed `goal/scripts/claude_goal.py` by wrapping non-integer stale-day values as `--stale-days must be an integer`; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1747b-stale-days-usage-qa.json` confirms all four direct slash/invoke stale-days non-integer cases now return exit `2`, leave stdout empty, report `goal error: --stale-days must be an integer`, avoid `invalid literal for int()`, do not mask the input error behind `cannot open goal database` even when `CLAUDE_GOAL_DB` is a directory, and print no traceback. Focused stale-days pytest passed (`4 passed`); full isolated pytest with `PYTHONUTF8=1` passed (`186 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1748 claude-goal installer script path validation fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1746 doctor output sink surface. Direct isolated installer reproduction showed `python goal/scripts/install_goal.py <settings> <missing-script> --no-backup` and the same command with `<script>` as a directory returned exit `0`, wrote `settings.json`, and only surfaced later as doctor install-health `fail`/`warn` because the Stop hook targeted an invalid script. Root cause: `install_settings()` trusted the provided hook script path and wrote settings before proving the target existed and was a file. Fixed `goal/scripts/install_goal.py` by adding a pre-write `validate_script_path()` guard; updated installer tests so valid-path cases create a real script fixture and added missing-script/directory-script regression coverage in `tests/test_install_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1748-installer-script-path-qa.json` confirms missing and directory script paths now return exit `2`, leave stdout empty, create no new settings or backup files, preserve an existing settings file without creating a backup, and print no traceback. Focused installer pytest passed (`27 passed`); full isolated pytest with `PYTHONUTF8=1` passed (`186 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\install_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1749 claude-goal goal metadata_json object-shape guard fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the completed T-1747b/T-1748 surfaces. 0-step refresh prioritized a structured-output data-integrity bug over lower-impact message-order differences: direct isolated DB reproduction showed `goals.metadata_json = '{broken'` made `json`/`contract` return exit `2` with raw JSON parser text and no `metadata_json` context, while `metadata_json = '[]'` or `"string"` made `json` and `contract` return exit `0` with schema-invalid `metadata` array/string values; `contract --verify-json` accepted those artifacts when the same corrupt DB value was present. Root cause: `row_to_dict()` trusted `metadata_json` and passed any decoded JSON value through, while operating-contract verification only checked that `current_goal` was an object and did not enforce `current_goal.metadata` object shape. Fixed `goal/scripts/claude_goal.py` by adding `goal_metadata()` validation and rejecting non-object `current_goal.metadata`; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1749-metadata-json-shape-qa.json` confirms malformed `metadata_json` now returns contextual `goal metadata_json is invalid`, non-object JSON returns `goal metadata_json must be a JSON object`, `json`/direct `contract`/slash `invoke contract`/`doctor --json` leave stdout empty and print no traceback, and operating-contract verification rejects `current_goal.metadata` array values. Focused metadata/contract pytest passed (`4 passed`), post-format focused rerun passed (`2 passed`), and full isolated pytest with `PYTHONUTF8=1` passed (`189 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1750 claude-goal empty inline completion-audit value guard fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing WIP and the already-recorded T-1749 metadata_json surface by using the next task ID. Direct isolated CLI reproduction showed `complete --audit-json=` and slash `invoke complete --audit-json=` returned exit `0`, printed `Action: complete`, and marked an active goal complete without any completion-audit artifact, while the missing-value forms without `=` already returned clean usage errors. Root cause: empty inline option values were treated as present-but-falsy and then normalized away (`values["--audit-json"] or None`), so completion audit validation never saw an invalid artifact path. Fixed `goal/scripts/claude_goal.py` by adding `_validate_option_value()` for direct and slash value options and validating direct `complete --audit-json`; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1750-empty-audit-json-qa.json` confirms direct and slash empty-inline `--audit-json=` now return exit `2`, leave stdout empty, report `goal error: --audit-json requires a value`, print no traceback, and keep the goal active. Focused regression pytest passed (`7 passed, 123 deselected`), command cluster pytest passed (`97 passed, 33 deselected`), and full isolated pytest with `PYTHONUTF8=1` passed (`189 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1751 claude-goal goal-record schema guard fix**. Continued the `/goal` reproduce -> isolate -> root-cause -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP and the completed T-1750 empty audit-json surface. Direct isolated DB reproduction showed corrupted goal rows such as `tokens_used = -1`, `time_used_seconds = -5`, `token_budget = -1`, negative timestamps, empty `source`, and empty `objective` all returned exit `0` from `json`, `contract`, and `doctor --json`; saved operating-contract artifacts with the same invalid `current_goal` shape also passed `contract --verify-json` when the DB was corrupted to match. Root cause: `row_to_dict()` only validated `metadata_json`, while the operating-contract verifier only compared `current_goal` to the DB row and did not enforce the goal-record schema shape. Fixed `goal/scripts/claude_goal.py` by adding shared goal-record validation for key surface, non-empty string fields, status, non-negative numeric fields, nullable timestamps, positive-or-null token budget, and metadata object shape; `row_to_dict()` and operating-contract `current_goal` verification now use that validator. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1751-goal-record-shape-qa/summary.json` confirms the corrupted goal-row cases now return exit `2` for `json`, direct `contract`, slash `invoke contract`, and `doctor --json`, leave stdout empty, report contextual `goal record ...` errors, and print no traceback; direct and slash `contract --verify-json` also reject a saved artifact whose `current_goal.tokens_used` is invalid even when the DB row is corrupted to match. Focused adjacent pytest passed (`4 passed, 128 deselected`), command-cluster pytest passed (`95 passed, 37 deselected`), and full isolated pytest with `PYTHONUTF8=1` passed (`192 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1752 claude-goal direct artifact-option empty-value parity fix**. Continued the `/goal` computer-use reproduce -> research -> A/B -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the concurrently recorded T-1751 goal-record schema guard. Direct CLI interaction showed `contract --verify-json=`, `contract --json-output=`, `contract --markdown-output=`, `contract --verify-schema=`, `doctor --install --verify-json=`, `doctor --install --json-output=`, `doctor --install --markdown-output=`, and `doctor --install --verify-markdown=` treated empty inline values as omitted and returned normal output or later dependency behavior instead of a value-required usage error; equivalent slash `invoke` paths already returned `goal error: <option> requires a value`. Classification: 기능 오류 + 로딩/피드백 + 카피. Official references checked: Python argparse value options/invalid-argument behavior, Click option value behavior, and GitHub CLI reference style. Fixed `goal/scripts/claude_goal.py` by validating direct `doctor` and `contract` namespace value options with the same `_validate_option_value()` policy used by slash parsing; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1752-direct-empty-artifact-options-qa.json` records baseline score `0/8`, candidate score `8/8`, decision `adopt_candidate`, and confirms all eight direct empty artifact-option cases return exit `2`, leave stdout empty, report `goal error: <option> requires a value`, print no traceback, and keep the active goal unchanged. Focused regression/adjacent pytest passed (`36 passed, 97 deselected`) before and after formatting, and full isolated pytest with `PYTHONUTF8=1` passed (`192 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1753 claude-goal direct json empty session-id parity fix**. Continued the `/goal` computer-use reproduce -> research -> A/B -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP and the completed T-1752 artifact-option parity surface. Direct CLI interaction showed `json --session-id=` returned exit `0` with `null`, silently treating an explicitly empty session id as a different missing goal, while slash `invoke json --session-id=` and `invoke json --session-id` already returned `goal error: --session-id requires a value`. Classification: 기능 오류 + 로딩/피드백 + 카피. Official references checked: Python argparse value options/invalid-argument behavior, Click option behavior, and GitHub CLI reference style. Fixed `goal/scripts/claude_goal.py` by adding direct JSON namespace validation through `JsonArgs` and `_validate_option_value("--session-id", ...)`; added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1753-json-empty-session-id-qa.json` records baseline score `2/3`, candidate score `3/3`, decision `adopt_candidate`, and confirms direct plus slash empty `--session-id=` JSON flows return exit `2`, leave stdout empty, report `goal error: --session-id requires a value`, print no traceback, and keep the active goal unchanged. Focused JSON pytest passed before and after formatting (`4 passed, 131 deselected` after formatting), and full isolated pytest with `PYTHONUTF8=1` passed (`194 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1754 claude-goal stale-days empty-value guard fix**. Continued the `/goal` computer-use reproduce -> research -> A/B -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP plus the concurrently recorded T-1753 direct JSON empty session-id parity surface. Direct CLI reproduction showed `doctor --stale-days= --json`, `pause-stale --stale-days= --json`, and split-empty `--stale-days ""` variants reached inconsistent empty-value feedback: direct commands exposed argparse `invalid int value: ''`, while slash `invoke` paths classified the same empty value as `--stale-days must be an integer`. The correct contract is a missing-value usage error before DB open. Root cause: `_parse_stale_days_value()` called `int(value)` without first validating blank values, and direct `doctor`/`pause-stale` argparse definitions used `type=int`, so empty strings were consumed before shared option validation. Fixed `goal/scripts/claude_goal.py` by routing direct stale-days values through `_parse_stale_days_value()`, validating blank values with `_validate_option_value("--stale-days", ...)`, and removing direct argparse `type=int` for the affected options; updated `tests/test_claude_goal.py` so non-integer and empty-value stale-days cases are distinct regressions. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1753-stale-days-empty-value-qa/summary.json` records score `8/8`, decision `adopt_candidate`, and confirms direct plus slash `doctor`/`pause-stale` empty `--stale-days` flows return exit `2`, leave stdout empty, report `goal error: --stale-days requires a value`, avoid argparse `invalid int value`, avoid integer-format errors, avoid DB errors, and print no traceback. Focused stale-days pytest passed (`6 passed, 129 deselected`), command-cluster pytest passed (`67 passed, 68 deselected`), and full isolated pytest with `PYTHONUTF8=1` passed (`194 passed`). Static gates passed: `python -m ruff check goal tests`, `python -m ruff format --check goal tests`, `python -m py_compile goal\scripts\claude_goal.py`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |

## Rotation 2026-06-09 (archived addenda older than 2026-06-02)

| Field | Value |
|---|---|
| Date | 2026-06-09 |
| Tool | Codex |
| Work | **T-1755 claude-goal direct value-option missing-value feedback fix**. Continued the `/goal` computer-use reproduce -> research -> A/B -> fix -> verify loop in the separate `claude-goal/` repo on branch `improve/goal-system`, preserving existing nested WIP and the completed T-1754 stale-days empty-value guard surface. Direct CLI reproduction showed `pause-stale --stale-days` emitted argparse usage plus `expected one argument` while slash `invoke pause-stale --stale-days` already returned `goal error: --stale-days requires a value`; direct `set objective --token-budget=` also reported `goal error: invalid token budget: ''` instead of the clearer missing-value copy used by split missing values. Classification: loading-feedback + copy + functional-validation. Official references checked: Python argparse invalid-argument behavior and Click option/value modeling. Fixed `goal/scripts/claude_goal.py` by wrapping empty set budget values as `GoalUsageError` through `_validate_set_option_value()` and adding a narrow `MAIN_VALUE_OPTIONS_BY_COMMAND` / `_prevalidate_main_required_values()` preflight that rejects only trailing direct value options before argparse prints usage. Added regression coverage in `tests/test_claude_goal.py`. |
| Next Priorities | Verification passed in `claude-goal/`: after-fix QA in `.tmp/claude-goal-t1755-direct-value-option-missing-value-qa.json` records the direct `pause-stale`/`doctor`/`json`/`contract` missing-value probes and inline-empty `set` probes all returning exit `2`, empty stdout, `goal error: <option> requires a value`, and no traceback. Focused pytest passed (`3 passed, 133 deselected`), and full isolated pytest with `PYTHONUTF8=1` passed (`195 passed`). Static gates passed: `python -X utf8 -m py_compile goal\scripts\claude_goal.py goal\scripts\install_goal.py`, `python -X utf8 -m ruff check goal tests`, `python -X utf8 -m ruff format --check goal tests`, `git diff --check`, and `py -3.13 -m code_review_graph detect-changes --base HEAD --brief` with risk `0.00`. The first full-file pytest attempt timed out at 120s before completion; rerun with a 600s timeout passed. The first Ruff command used a nonexistent `scripts` path and failed with `E902`; rerun on `goal tests` passed. Diff-check still reports only existing CRLF warnings for `.gitignore`, `README.md`, and `goal/SKILL.md`. No nested code staging, push, revert, `update_goal`, root product edit, or Hanwoo T-251 retry was performed. Continue `/goal` debugging only from the next directly reproduced CLI or verifier defect. |
