# Vibe Coding Context

> Local-only multi-project workspace. Do not push, pull, or deploy unless the user explicitly asks.

## Workspace Summary

- Root runtime: Python `venv`, `pytest`, `ruff`, root `.env`
- Canonical path contract:
  - `workspace/...` for shared automation and docs
  - `projects/<name>/...` for product repos
  - `infrastructure/...` for services and MCP helpers

## Active Projects

| Project | Status | Stack | Canonical Path |
|---|---|---|---|
| `blind-to-x` | Active | Python pipeline, Notion, Cloudinary | `projects/blind-to-x` |
| `shorts-maker-v2` | Active | Python, MoviePy, Edge TTS, OpenAI, Google GenAI, Pillow | `projects/shorts-maker-v2` |
| `hanwoo-dashboard` | Active | Next.js, React, Prisma 7, PostgreSQL/Supabase, Redis/BullMQ, Tailwind | `projects/hanwoo-dashboard` |
| `knowledge-dashboard` | Maintenance | Next.js, React, TypeScript, Tailwind | `projects/knowledge-dashboard` |
| `suika-game-v2` | Frozen | Vite, Vanilla JS, Matter.js | `projects/suika-game-v2` |
| `word-chain` | Frozen | React, Vite, Tailwind | `projects/word-chain` |

## Technology Adoption Policy

- Workspace stack policy is documented in `docs/technology-stack.md`.
- Current frontend default is React/Next.js with JavaScript or TypeScript.
- Current backend/runtime default is Python pipelines plus Next.js server routes/actions.
- Current SaaS data direction is PostgreSQL/Supabase; `hanwoo-dashboard` uses Prisma with `@prisma/adapter-pg`.
- Current queue/cache direction is Redis/BullMQ for single-app internal async work.
- Current HTTP client direction is native Fetch API via local helpers.

## Current Reliability Notes

- **2026-06-06 (Codex)**: T-1418 Hanwoo payment failure code-specific recovery copy completed in local commit `f0b534cc`. External TossPayments docs say payment `failUrl` carries `code`, `message`, and `orderId`, and should show user-friendly copy based on `code` plus a retry path. Baseline browser QA on `/subscription/fail` at 320px found the missing-code case rendered `오류 코드: 오류 코드 미전달`, while known codes still used generic copy. `projects/hanwoo-dashboard/src/app/subscription/fail/page.js` now normalizes missing codes to `미전달`, maps `PAY_PROCESS_CANCELED`, `PAY_PROCESS_ABORTED`, and `REJECT_CARD_COMPANY` to safe Korean recovery messages, and still avoids rendering the provider `message` query directly. `projects/hanwoo-dashboard/src/lib/payment-ux-copy.test.mjs` locks the contract. Browser QA verified clean missing-code copy, cancellation-specific copy, no provider-message leak, retry to `/login?.../subscription#login`, no horizontal overflow or replacement characters, and console warnings/errors `0`. Verification passed Hanwoo Node tests (`510 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.30`) covered by source/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.5`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1417 Hanwoo public 404 recovery login fragment preservation completed in local commit `b5de11cb`. Baseline mobile browser QA on `/privacy-missing` found the public 404 page copy/layout was good, but clicking `대시보드로 돌아가기` used Next client navigation to protected `/` and landed on `/login?callbackUrl=.../` without the auth-owned `#login` fragment; direct document navigation preserved `/login?...#login`. `projects/hanwoo-dashboard/src/app/not-found.js` and `projects/hanwoo-dashboard/src/app/error.js` now use intentional plain `<a href="/">` document navigation for protected dashboard recovery links, with a narrow ESLint exception documenting why `next/link` is not used there. `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the contract and exception rationale. Browser QA verified `/privacy-missing` recovery click lands on `/login?...#login`, preserves same-origin callback `/`, renders the login section/form, has no horizontal overflow or replacement characters, and reports console warnings/errors `0`. Verification passed Hanwoo Node tests (`510 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.30`) covered by direct/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.7`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1416 Performance Overview single-row chart fallback completed in local commit `cdd941eb`. `workspace/execution/pages/performance_overview.py` now uses `_has_multiple_rows()` to render publishing/API-cost trend charts only when at least two rows exist; one-row datasets show `st.info()` plus the raw dataframe. `workspace/tests/test_performance_overview_page.py` locks the helper and both source branches. Browser QA verified `HTTP 200`, visible Performance Overview, KPI Summary, API Cost Breakdown, and Platform Metrics sections, one single-row guidance message, console/page/network errors `0`, no horizontal overflow, and screenshot `output/playwright/performance-overview-t1416-single-row-guard.png`. Verification passed `py_compile`, focused/related pytest (`11 passed`), Ruff, path-limited diff-check, no deprecated width usage, shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), and code-review gate advisory WARN (`risk_score=0.35`) covered by direct/browser/project tests. No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1415 Blind-to-X source preflight operator action summary completed in local commit `f875b1da`. `projects/blind-to-x/scripts/source_browser_probe.py` now emits `summary.recommended_command` for the recommended ready source and `summary.problem_actions` for blocked, click, browser, login-wall, timeout, and empty-page failures. Live Chromium click-through preflight produced ready `jobplanet`/`ppomppu`, blocked Blind `403`, blocked FMKorea `430`, `recommended_source=ppomppu`, a guarded Ppomppu command, and fallback actions for both blocked sources. Verification passed focused source-browser pytest (`19 passed`), CLI preflight pytest (`11 passed`, `26 deselected`), Ruff, `py_compile`, diff-check, live click-through preflight, blind-to-x project QC (`1817 passed`, `9 skipped`, lint passed), code-review gate advisory WARN (`risk_score=0.35`) covered by focused/live/project gates, and A/B `adopt_candidate` (`score_delta=1.0666666666666667`). MCP browser direct navigation was blocked by an existing Playwright profile lock, so the accepted browser evidence is the script-backed Chromium click-through plus screenshots. No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1414 Performance Overview Streamlit width API refresh completed in local commit `6b1e9005`. `workspace/execution/pages/performance_overview.py` now conditionally inserts the workspace root before importing `execution.*`, centralizes line chart, bar chart, and dataframe rendering through helpers, and uses Streamlit `width="stretch"` instead of deprecated `use_container_width=True`. `workspace/tests/test_performance_overview_page.py` locks import-path, helper, no-deprecated-source, and watchdog-history parsing behavior. Browser baseline loaded the page but emitted deprecated-width Streamlit warnings; candidate browser QA verified `HTTP 200`, visible title/KPI/API-cost/platform sections, console/page/network errors `0`, no horizontal overflow, no deprecated-width server warning, and nonblank screenshot `output/playwright/performance-overview-t1412-width-refresh.png`. Verification passed `py_compile`, focused/related pytest (`10 passed`), Ruff, path-limited diff-check, no deprecated Performance Overview width usage, shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), code-review gate advisory WARN covered by focused/browser/project tests, and A/B `adopt_candidate` (`score_delta=0.5`). No push was performed and T-251 was not retried. A later Hanwoo T-1413 commit `c2a13a3a` is now live `HEAD`; keep that separate from T-1414 and preserve current blind-to-x dirty WIP unless scope switches.

- **2026-06-06 (Codex)**: T-1413 Hanwoo password visibility toggle pressed-state polish completed in local commit `c2a13a3a`. Baseline mobile browser QA on `/login#login` found `.login-password-toggle` changed the password input `type` and Korean label/title copy correctly, but exposed no toggle state: `aria-pressed` and `Element.ariaPressed` were `null` before, after show, and after hide. Official WAI-ARIA APG/MDN guidance supports `aria-pressed` for toggle-button state, so `projects/hanwoo-dashboard/src/app/login/page.js` now binds `aria-pressed={showPassword}` and `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the source contract. Candidate browser QA verified `aria-pressed`/`ariaPressed` as `false -> true -> false`, preserved `password -> text -> password`, preserved Korean label/title copy, no horizontal overflow, console warnings/errors `0`, and screenshots `output/playwright/hanwoo-t1413-login-password-toggle-{baseline,candidate}.png`. Verification passed Hanwoo Node tests (`510 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.50`) covered by direct/browser/project gates, and A/B `adopt_candidate` (`score_delta=1.3333333333333333`). No push was performed and T-251 was not retried. Preserve unrelated current blind-to-x WIP outside Hanwoo commits.

- **2026-06-06 (Codex)**: T-1412 Hanwoo mobile login keyboard hint polish completed in local commit `c8efef0b`. Baseline mobile browser QA on `/login#login` clicked and filled the username/password fields and found both fields preserved `autocomplete` but had no `enterkeyhint` attribute or DOM `enterKeyHint` value. `projects/hanwoo-dashboard/src/app/login/page.js` now sets `enterKeyHint="next"` on the username field and `enterKeyHint="go"` on the password field, preserving existing labels, autocomplete, and submit readiness. `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the source contract. Candidate browser QA verified username `next`, password `go`, autocomplete preserved, submit enabled after fill, no horizontal overflow, console warnings/errors `0`, and screenshots `output/playwright/hanwoo-t1412-login-enterkeyhint-{baseline,candidate}.png`. Verification passed Hanwoo Node tests (`510 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.50`) covered by direct/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.8888888888888888`). No push was performed and T-251 was not retried. Preserve unrelated current workspace performance-overview WIP outside Hanwoo commits.

- **2026-06-06 (Codex)**: T-1411 Hanwoo payment failure long-code wrapping completed in local commit `aaccbb1a`. Baseline mobile browser QA on `/subscription/fail` with a long unbroken gateway error code produced horizontal overflow on a 390px viewport: body/document `scrollWidth=666`, error-code paragraph `scrollWidth=642/clientWidth=342`, and computed `overflowWrap=normal`. `projects/hanwoo-dashboard/src/app/subscription/fail/page.js` now applies `overflowWrap: "anywhere"` to the error-code paragraph, and `projects/hanwoo-dashboard/src/lib/payment-ux-copy.test.mjs` locks the source contract. Candidate browser QA verified body/document `scrollWidth=390`, `horizontalOverflow=false`, error-code `scrollWidth/clientWidth=342`, computed `overflowWrap=anywhere`, retry CTA still visible, console warnings/errors `0`, and screenshot `output/playwright/hanwoo-t1411-payment-fail-long-code-wrap-mobile.png`. Verification passed focused payment UX tests (`10 passed`), related payment/error-page tests (`23 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.6995687276061108`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1410 Debug History import and Plotly width refresh completed in local commit `7443583e`. Browser baseline on `http://127.0.0.1:8777` showed the page rendered only `Debug History 모듈을 불러올 수 없습니다: No module named 'execution'`. `workspace/execution/pages/debug_history.py` now inserts `Path(__file__).resolve().parents[2]` into `sys.path`, imports `execution.debug_history_db` successfully, and routes both Plotly pie charts through `_render_plotly_chart()` with Streamlit `width="stretch"` instead of deprecated `use_container_width=True`. `workspace/tests/test_debug_history_page.py` locks the import-path, helper, and source guard contracts. Candidate browser QA on `http://127.0.0.1:8778` verified `HTTP 200`, title visible, module error absent, alerts `0`, console/page/serious-network errors `0`, and nonblank screenshot `output/playwright/debug-history-t1410-width-import-refresh.png`; actual workspace DB had no debug entries, so chart count was `0`. Verification passed `py_compile`, focused/related pytest (`34 passed`), Ruff, path-limited diff-check, no deprecated Debug History Plotly width usage, shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), code-review gate advisory WARN (`risk_score=0.30`) covered by direct/browser/project tests, and A/B `adopt_candidate` (`score_delta=3.75`). No push was performed and T-251 was not retried. A separate Hanwoo commit `aaccbb1a` (`T-1411 wrap payment failure codes`) landed after T-1410; keep scopes separate.

- **2026-06-06 (Codex)**: T-1409 Blind-to-X recommended source continuation completed. `projects/blind-to-x/pipeline/cli.py` now exposes `--source-preflight-use-recommended` and rewrites `args.source` to `summary.recommended_source` only when `--require-source-ready` is active and the preflight report provides a non-empty recommendation, allowing guarded multi-source runs to continue with a ready source instead of aborting on unrelated blocked sources. `projects/blind-to-x/tests/unit/test_main.py` locks parser, helper guardrails, async command behavior, and single-command lock cleanup. README plus `projects/blind-to-x/directives/x_content_curation.md` document the guarded command. Verification passed focused CLI preflight pytest (`15 passed`, `22 deselected`), full `test_main.py` (`37 passed`), source preflight related pytest (`16 passed`, `40 deselected`), Ruff, `py_compile`, diff-check, live guarded CLI smoke (`recommended_source=ppomppu`, ready `ppomppu`/`jobplanet`, blocked Blind `403` and FMKorea `430`, lock removed), blind-to-x project QC (`1817 passed`, `9 skipped`, lint passed), code-review gate advisory WARN (`risk_score=0.35`) covered by focused/live/project gates, and A/B `adopt_candidate` (`score_delta=0.9247594096222289`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1408 Hanwoo login legal link separation completed in local commit `6757d373`. Browser QA found `/login` legal links rendered as adjacent inline text when the current dev stylesheet omitted the semantic `.login-legal-links` rule: computed layout was block/normal gap, link rectangles touched, and `nav.innerText` collapsed to `이용약관개인정보처리방침`. `projects/hanwoo-dashboard/src/app/login/page.js` now adds fallback `flex flex-wrap justify-center gap-3.5` classes, wraps legal-link mapping in `Fragment`, and renders an aria-hidden ` · ` separator. `projects/hanwoo-dashboard/src/app/globals.css` adds inline-flex/min-height styling for links and separator, and `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the contract. Browser QA verified flex gap, separator hiding from assistive tech, 31.499992px adjacent visual gap, no concatenated legal text, no replacement chars, no horizontal overflow, and console warnings/errors `0`. Verification passed focused Node tests (`14 passed`), `node --check`, ESLint with CSS ignore warning only, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.35`) covered by focused/browser/project gates, W3C WCAG 2.2 Target Size review, and A/B `adopt_candidate` (`score_delta=7.941174675436581`). No push was performed and T-251 was not retried. T-1409 is now committed separately as `6c9e044e`.

- **2026-06-06 (Codex)**: T-1406 API Monitor Plotly width API refresh completed in local commit `b280e50a`. `workspace/execution/pages/api_monitor.py` now centralizes all 7 Plotly charts through `_render_plotly_chart()` and uses Streamlit `width="stretch"` instead of deprecated `use_container_width=True`, clearing the 2 remaining deprecated Plotly width calls in the blind-to-x cost sections. `workspace/tests/test_api_monitor_page.py` locks the helper and source guard. Verification passed focused/related pytest (`2 passed`, `47 passed`), Ruff, `py_compile`, no-deprecated API Monitor Plotly width usage, direct Streamlit browser QA (`HTTP 200`, title visible, 2 Plotly charts, console/page/serious-network errors `0`, server warning/error/deprecated matches `0`, nonblank screenshot `output/playwright/api-monitor-t1404-width-refresh.png`), shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), code-review gate advisory WARN (`risk_score=0.35`) covered by direct tests/browser/QC, and A/B `adopt_candidate` (`score_delta=1.8148148148148149`). No push was performed and T-251 was not retried. Next remaining Streamlit deprecated-width surfaces include `debug_history.py`, `performance_overview.py`, `debt_dashboard.py`, and `health_check_dashboard.py`.

- **2026-06-06 (Codex)**: T-1405 Hanwoo legal document Korean eyebrow copy completed in local commit `0336fecf`. Baseline browser click QA from `/login` confirmed `/terms` and `/privacy` links worked but found English eyebrow copy on otherwise Korean legal documents. `projects/hanwoo-dashboard/src/app/terms/page.js` now uses `서비스 이용약관`, `projects/hanwoo-dashboard/src/app/privacy/page.js` now uses `개인정보 보호 안내`, and `projects/hanwoo-dashboard/src/lib/legal-pages-copy.test.mjs` locks the Korean eyebrow contract while rejecting old English strings. Verification passed focused Node tests (`14 passed`), `node --check`, ESLint, path-limited diff-check, direct mobile Playwright click QA (`390x844`, Korean eyebrows present, English legal eyebrows absent, no replacement characters, no horizontal overflow, console warnings/errors `0`), Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.35`) with no fail and covered by focused/browser/project tests, W3C WCAG 2.2 Language of Parts review, and A/B `adopt_candidate` (`score_delta=1.0`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1404 Blind-to-X source preflight recommendation evidence ranking completed. `projects/blind-to-x/scripts/source_browser_probe.py` now ranks `summary.recommended_source` by strongest successful ready-source detail evidence (`click_through.body_chars`, falling back to listing body size only without click evidence) instead of first-ready order, while preserving `summary.ready_sources` order for operator visibility. `projects/blind-to-x/tests/unit/test_source_browser_probe.py` locks the JobPlanet API detail vs Ppomppu HTML detail case; README plus `projects/blind-to-x/directives/x_content_curation.md` document the recommendation contract. Verification passed focused pytest (`19 passed`), CLI preflight pytest (`9 passed`, `23 deselected`), Ruff, `py_compile`, live all-source click-through preflight (`ready_count=2`, `problem_count=2`, `recommended_source=ppomppu`, existing Blind `403` and FMKorea `430` blocked responses), blind-to-x project QC (`1812 passed`, `9 skipped`, lint passed), and A/B `adopt_candidate` (`score_delta=1.0055555555555555`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1403 Channel Growth import and Plotly width refresh completed in local commit `d7ba1ec2`. Browser QA found `workspace/execution/pages/channel_growth.py` rendered only `No module named 'execution.channel_growth_tracker'` because it inserted `workspace/execution` instead of the workspace root before importing `execution.channel_growth_tracker`. The page now inserts `Path(__file__).resolve().parents[2]`, routes all 3 Plotly charts through `_render_plotly_chart()`, and uses Streamlit `width="stretch"` instead of deprecated `use_container_width=True`. `workspace/tests/test_channel_growth_page.py` locks import-path, helper, and source guard behavior. Verification passed focused/related pytest (`3 passed`, `13 passed`, `12 passed`), Ruff, `py_compile`, no-deprecated-width `rg`, direct Streamlit/Playwright QA with a UI-added probe channel (`3` Plotly charts rendered, console warnings/errors `0`, server warning/error/deprecated matches `0`, nonblank screenshot `output/playwright/channel-growth-t1403-import-width-refresh.png`), shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), code-review gate PASS (`risk_score=0.0`), and A/B `adopt_candidate` (`score_delta=1.6`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1402 Blind-to-X JobPlanet API detail source preflight completed. Live all-source `scripts/source_browser_probe.py --click-through` showed JobPlanet feed API returned HTTP 200/readable JSON but was falsely classified as `click_error` because it has no HTML post anchors. `projects/blind-to-x/scripts/source_browser_probe.py` now handles JobPlanet as an API-backed source: extract first `data.items[].id`, record the public `/community/posts/{id}` evidence URL, verify `/api/v5/community/posts/{id}` detail content, and keep JobPlanet `ready` when that detail API is readable. HTML sources still use visible-anchor click-through. `projects/blind-to-x/tests/unit/test_source_browser_probe.py` covers JobPlanet candidate extraction, API detail success, and missing-id failure; README plus `projects/blind-to-x/directives/x_content_curation.md` document the API-backed detail behavior. Verification passed focused pytest (`18 passed`), CLI preflight pytest (`10 passed`, `22 deselected`), Ruff, `py_compile`, live all-source click-through preflight (`ready_count=2`, `problem_count=2`, `ready_sources=["jobplanet","ppomppu"]`, `recommended_source=jobplanet`, JobPlanet detail `ok=true`, `body_chars=77`), blind-to-x project QC (`1811 passed`, `9 skipped`, lint passed), and A/B `adopt_candidate` (`score_delta=1.0999999999999999`). No push was performed and T-251 was not retried. Preserve unrelated workspace channel-growth WIP outside this task.

- **2026-06-06 (Codex)**: T-1401 Hanwoo login legal document links completed. `projects/hanwoo-dashboard/src/app/login/page.js` now renders `/terms` and `/privacy` links inside a `nav.login-legal-links` with Korean visible labels and explicit accessible labels/titles; `projects/hanwoo-dashboard/src/app/globals.css` adds compact wrapping/focus styling for the login card; `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the source contract. Verification passed full Hanwoo Node tests via the focused command (`510 passed`), ESLint, path-limited diff-check, direct Playwright browser QA on desktop and mobile (`/terms` and `/privacy` navigation verified, no overlap, no horizontal overflow, console warnings/errors `0`, page errors `0`), Hanwoo project QC (`test`, `lint`, `build`, `smoke` passed), staged code-review gate advisory WARN (`risk_score=0.50`) covered by source/browser/project tests, and A/B `adopt_candidate` from `.tmp/ab-manifest-hanwoo-t1401.json` (`score_delta=1.25`). No push was performed and T-251 was not retried. Preserve unrelated current WIP outside this task: blind-to-x source preflight files and workspace channel-growth files.

- **2026-06-06 (Codex)**: T-1400 Blind-to-X source preflight actionable summary completed. `projects/blind-to-x/scripts/source_browser_probe.py` now adds `summary.ready_sources`, `summary.problem_sources`, and `summary.recommended_source` to browser preflight reports while preserving existing count/status and exit-code behavior. `projects/blind-to-x/tests/unit/test_source_browser_probe.py` locks the summary contract, and README plus `projects/blind-to-x/directives/x_content_curation.md` document that operators should read these fields before paid/LLM runs. Verification passed focused pytest (`15 passed`) with repo-local basetemp after default Windows temp returned `WinError 5`, CLI preflight pytest (`10 passed`, `22 deselected`), Ruff, `py_compile`, live Ppomppu click-through preflight (`ready_count=1`, `problem_count=0`, `recommended_source=ppomppu`, click-through `ok=true`, body_chars `801`), blind-to-x project QC (`1808 passed`, `9 skipped`, lint passed), staged code-review gate advisory WARN (`risk_score=0.40`) covered by tests/live/project QC, and A/B `adopt_candidate` (`score_delta=1.1764705882352942`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1398 Hanwoo payment retry login anchor preservation completed in local commit `beaf361e`. Browser QA found `/subscription/fail` retry reached `/login?callbackUrl=.../subscription` without `#login`; `projects/hanwoo-dashboard/src/app/subscription/fail/page.js` now uses `window.location.assign(PAYMENT_RETRY_PATH)` instead of App Router `router.push`, letting the protected-route redirect supply the anchored login URL. `projects/hanwoo-dashboard/src/lib/payment-ux-copy.test.mjs` locks the source contract. Browser QA verified `/login?...#login`, login anchor target `SECTION`, target/hash match `true`, callback `/subscription`, console warnings/errors `0`, and screenshot `output/playwright/hanwoo-t1398-payment-retry-login-anchor.png`. Verification passed focused Node tests (`25 passed`), `node --check`, ESLint, full Hanwoo tests (`510 passed`), Hanwoo project QC (`test`, `lint`, `build`, `smoke`), code-review gate advisory WARN (`risk_score=0.40`) covered by focused/full/browser/project tests, and A/B `adopt_candidate` (`score_delta=0.7692307692307693`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1399 Cost Dashboard Plotly width API refresh completed. Local Streamlit `1.54.0` and current official docs expose `st.plotly_chart(..., width="stretch", ...)`, so `workspace/execution/pages/cost_dashboard.py` now uses `_render_plotly_chart()` for all 7 Plotly charts and has no remaining `use_container_width=True` chart calls, including the shorts-maker-v2 cost chart. `workspace/tests/test_cost_dashboard.py` locks the helper contract and source guard. Verification passed focused/related pytest (`6 passed`), Ruff, `py_compile`, direct Streamlit/Playwright Cost Dashboard QA with console warnings/errors `0` and no added deprecation warnings after rerun, full shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), and A/B `adopt_candidate` (`score_delta=2.3333333333333335`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1397 Hanwoo login fragment anchor target completed in local commit `46f37d61`. T-1394 redirected protected-route auth failures to `/login?...#login`, but browser QA found the login page did not expose an element with `id="login"`. `projects/hanwoo-dashboard/src/app/login/page.js` now adds `id="login"` to the login card `section` while preserving `aria-labelledby="login-title"`, and `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the source contract. Browser QA verified `/admin/diagnostics?tab=health#db` lands on `/login?...#login`, `document.getElementById("login")` returns the `SECTION`, the label target is `login-title`, hash/target match is `true`, console warnings/errors are `0`, and screenshot `output/playwright/hanwoo-t1397-login-anchor.png` is committed. Verification passed focused Node tests (`15 passed`), ESLint, `node --check`, full Hanwoo tests (`510 passed`), Hanwoo project QC (`test`, `lint`, `build`, `smoke`), staged code-review gate advisory WARN (`risk_score=0.50`) covered by focused/full/browser/project tests, WHATWG fragment/id target research, and A/B `adopt_candidate` (`score_delta=0.8333333333333334`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1396 Blind-to-X source preflight evidence hardening completed. `projects/blind-to-x/scripts/source_browser_probe.py` now writes ASCII-escaped JSON reports so Korean titles/paths survive default PowerShell `Get-Content`/JSON handling, waits for readable clicked-detail text, and canonicalizes Ppomppu `/zboard/zboard.php?...&no=...` links to `/zboard/view.php?...&no=...` before retrying when a clicked page remains on a `Loading ...` interstitial. `projects/blind-to-x/tests/unit/test_source_browser_probe.py` covers ASCII report output, canonical link resolution, and the retry path. Verification passed focused pytest (`15 passed`), Ruff, `py_compile`, live Ppomppu CLI preflight (`ready_count=1`, `problem_count=0`, clicked detail `body_chars=1753`), ASCII evidence check (`AsciiOnly=true`, `HasUnicodeEscapes=true`), blind-to-x project QC (`1808 passed`, `9 skipped`, lint passed), staged code-review gate advisory WARN (`risk_score=0.50`) covered by focused/live/project tests, and A/B `adopt_candidate` (`score_delta=59.11025641025641`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1395 Shorts Analytics Plotly width API refresh completed. Local Streamlit `1.54.0` exposes `st.plotly_chart(..., width="stretch", ...)`, so `workspace/execution/pages/shorts_analytics.py` now uses `_render_plotly_chart()` for all Plotly charts and has no remaining `use_container_width=True` chart calls. `workspace/tests/test_shorts_analytics.py` locks both the helper contract and a source-level guard against deprecated chart-width reintroduction. Verification passed focused/related pytest (`52 passed`), Ruff, `py_compile`, direct Streamlit/Playwright tab QA with console warnings/errors `0`, full shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), and A/B `adopt_candidate` (`score_delta=1.5`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1394 Hanwoo protected-route login fragment guard completed in local commit `eec15103`. Browser QA found `http://127.0.0.1:3001/admin/diagnostics?tab=health#db` redirected to `/login?...#db`, because a redirect `Location` without a fragment can inherit the original request fragment. `projects/hanwoo-dashboard/src/auth.js` now returns `${loginUrl.href}#login` for unauthenticated protected-route redirects, preserving the same-origin `callbackUrl` while preventing protected-route hashes from leaking onto the login page. `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the source contract. Browser QA verified the final login URL ended with `#login`, console warnings/errors `0`, and screenshot `output/playwright/hanwoo-t1394-fragment-cleared.png`. Verification passed focused Node tests (`15 passed`), ESLint, full Hanwoo tests (`510 passed`), Hanwoo project QC (`test`, `lint`, `build`, `smoke`), diff-check, RFC 9110 redirect-fragment research, and A/B `adopt_candidate` (`score_delta=1.2727272727272727`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1391 Hanwoo login callback target preservation completed. After T-1389 fixed protected-route callback origin, the login success handler still discarded safe callbacks by always pushing `/`. `projects/hanwoo-dashboard/src/lib/login-redirect.mjs` now resolves `callbackUrl` against the current origin, accepts only same-origin non-login/non-internal paths, preserves query/hash, and falls back to `/` for unsafe or malformed values. `projects/hanwoo-dashboard/src/app/login/page.js` now pushes that sanitized target after successful credential sign-in. Browser QA verified `/subscription/success` keeps a same-host callback through invalid-login recovery, shows the Korean invalid-credential alert, leaves submit enabled, and logs console warnings/errors `0`; screenshot `output/playwright/hanwoo-t1391-login-callback-failure.png`. Verification passed focused Node tests (`15 passed`), ESLint, full Hanwoo tests (`510 passed`), Hanwoo project QC (`510` tests, lint, build, smoke), diff-check, staged code-review gate advisory WARN (`risk_score=0.50`) covered by focused/full/browser/project tests, and A/B `adopt_candidate` (`score_delta=0.7142857142857143`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1389 Hanwoo protected-route callback origin preservation completed. Direct Playwright QA found `http://127.0.0.1:3001/subscription/success` redirected to `/login` with `callbackUrl=http://localhost:3001/subscription/success` because Auth.js rewrites `request.nextUrl` to configured `AUTH_URL`/`NEXTAUTH_URL`. `projects/hanwoo-dashboard/src/auth.js` now returns an explicit `NextResponse.redirect()` from `callbacks.authorized()` for unauthenticated protected routes and reconstructs the callback origin from `x-forwarded-proto`, `x-forwarded-host`, and `host` headers. `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the source contract. Browser QA verified `callbackHost=127.0.0.1:3001`, protected-route login redirect, console warnings/errors `0`, and screenshot `output/playwright/hanwoo-t1389-callback-origin.png`. Verification passed focused source test (`13 passed`), full Hanwoo tests (`508 passed`), ESLint, Hanwoo project QC (`508` tests, lint, build, smoke), path-limited diff-check, staged code-review gate advisory WARN (`risk_score=0.50`) covered by tests/browser QA, and A/B `adopt_candidate` (`score_delta=0.7142857142857143`). No push was performed and T-251 was not retried. Preserve unrelated blind-to-x WIP outside T-1389.

- **2026-06-06 (Codex)**: T-1388 ROI/Result Tracker Streamlit workflow repair completed. Direct Streamlit/Playwright QA found the ROI dashboard failed to import workspace modules with `No module named 'execution'`; `workspace/execution/pages/roi_dashboard.py` now inserts the workspace root before importing `execution.*`. ROI and Result Tracker dashboards now use current Streamlit `width="stretch"` arguments instead of deprecated `use_container_width=True` for Plotly charts, dataframes, buttons, and form submit buttons. `workspace/tests/test_result_roi_dashboards.py` covers the import-path and width-API contracts. Verification passed focused/related pytest (`56 passed` with repo-local basetemp), Ruff check, Ruff format check, `py_compile`, path-limited diff-check, no deprecated width usage in the two dashboards, ROI and Result Tracker browser QA with console warnings/errors `0`, nonblank screenshots, staged code-review gate pass (`risk_score=0.0`), and A/B `adopt_candidate` (`score_delta=0.7628205128205128`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1387 Hanwoo login password remask on submit completed. Direct Playwright QA on `http://127.0.0.1:3001/login` found and fixed a failed-login recovery edge case where a revealed password could remain visible after invalid credentials. `projects/hanwoo-dashboard/src/app/login/page.js` now calls `setShowPassword(false)` as submission starts, and `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the source contract. Browser QA verified pre-submit `type=text`/`비밀번호 숨기기`, post-failure `type=password`/`비밀번호 보기`, Korean invalid-credentials alert, console warnings/errors `0`, and screenshot `output/playwright/hanwoo-t1387-login-remask.png`. Verification passed Hanwoo `507 passed`, ESLint, Hanwoo project QC (`507` tests, lint, build, smoke; smoke passed after one transient Next build-lock retry), path-limited diff-check, staged code-review gate exit `0` advisory WARN (`risk_score=0.50`) covered by tests/browser QA, and A/B `adopt_candidate` (`score_delta=64.125`). No push was performed and T-251 was not retried. Preserve unrelated dirty blind-to-x/workspace WIP currently outside T-1387.

- **2026-06-06 (Codex)**: Post-T-1385 launch evidence refresh completed. Full active-project QC passed and rewrote `.tmp/project_qc_runner_latest.json`: blind-to-x `1795 passed`, `9 skipped`; shorts-maker-v2 `1637 passed`, `12 skipped`, `29 warnings`; Hanwoo `507 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. `session_orient.py --json` reported clean worktree, open PRs `0`, and code-review graph freshness `current`. `product_readiness_score.py --json` reported score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`. Release packet is `ready_for_authorization`; selector is `blocked_publish_only`; launch audit coverage is complete; completion audit remains `incomplete` with `9/14` complete and `5` blocked issues. Remaining blockers are explicit push/current-head Actions and external/user-owned Hanwoo T-251.

- **2026-06-06 (Codex)**: T-1385 Shorts Analytics helper split and dataframe API refresh completed. `workspace/execution/pages/shorts_analytics.py` now extracts YouTube KPI summary, CPV math/table rows, CPV chart rows, Shorts revenue/ROI math, hook pattern table rows, and dataframe rendering into focused helpers while preserving dashboard calculations. `_render_dataframe()` uses current Streamlit `width="stretch"` and `hide_index=True`. `workspace/tests/test_shorts_analytics.py` covers empty/missing metric values, CPV display labels, zero-view/cost filtering plus sorting, revenue/ROI helpers, and dataframe render arguments. Verification passed focused/related pytest (`66 passed` across analytics/content DB/manager), Ruff check, Ruff format check, `py_compile`, path-limited diff-check, code-review gate PASS (`risk_score=0.0`), Playwright CLI tab-click QA for all Analytics tabs with console warnings/errors `0`, nonblank screenshot `output/playwright/shorts-analytics-t1385-empty.png`, and A/B `adopt_candidate` (`score_delta=2.375`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1384 Blind-to-X required source readiness gate completed. `projects/blind-to-x/pipeline/cli.py` now adds `--require-source-ready`, which runs the existing browser source preflight before normal pipeline work, forces fail-on-problem semantics, exits before pipeline execution on source failure, and continues only when preflight succeeds. Standalone `--source-preflight` remains report-and-exit, and README documents the guarded run example. `projects/blind-to-x/tests/unit/test_main.py` covers parser flags, request/continue helpers, success continuation into `execute_pipeline()`, failure before pipeline work, and lock cleanup. Verification passed focused pytest (`41 passed`), Ruff check, `py_compile`, path-limited diff-check, CLI unsupported-source smoke exit `1` with no leftover lock, and A/B `adopt_candidate` (`score_delta=0.833333`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1383 Shorts Manager delete-confirmation browser-QA polish completed. Direct Streamlit/Playwright QA found one-click queue deletion; `workspace/execution/pages/shorts_manager.py` now stores pending deletion in `st.session_state`, shows `삭제 확인`/`취소`, clears stale confirmation state, and only deletes after confirmation. `workspace/tests/test_shorts_manager.py` covers confirmation/cancel/stale-state helpers. Verification passed focused pytest (`61 passed`), Ruff check/format, `py_compile`, path-limited diff-check, browser QA add/delete/cancel/confirm flow with DB count `1 -> 1 -> 0`, console warnings/errors `0`, screenshot `output/playwright/shorts-manager-delete-confirm-final.png`, and A/B `adopt_candidate` (`score_delta=0.833333`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1376 shorts-maker-v2 whisper aligner helper split completed after concurrent T-1374/T-1375 cycles. Code commit `976fd51b` splits `transcribe_to_word_timings()` in `projects/shorts-maker-v2/src/shorts_maker_v2/providers/whisper_aligner.py` into focused word-timing, WhisperX, and faster-whisper helpers while preserving the TTS fallback contract; `tests/unit/test_whisper_aligner.py` adds direct WhisperX alignment success and segment fallback coverage. Verification passed focused pytest (`14 passed`), related provider pytest (`62 passed`), shorts-maker-v2 project QC (`1634 passed`, `12 skipped`, lint passed), ruff/format/diff checks, code-review WARN covered by tests, VibeDebt (`35.7 -> 14.0`, complexity `26 -> 9`), and A/B `adopt_candidate` (`score_delta=0.829837`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: T-1369 local auto-research cycle completed after the publish/T-251 boundary. Code commit `aa0f7819` splits `FMKoreaScraper.scrape_post()` in `projects/blind-to-x/scrapers/fmkorea.py` into focused fetch/load/container/title/content/count/screenshot/result helpers while preserving scrape/error behavior; `projects/blind-to-x/tests/unit/test_scrapers_fmkorea.py` adds helper and screenshot-timeout coverage. Verification passed `py_compile`, focused FMKorea pytest (`37 passed`), related scraper pytest (`103 passed`), ruff check, ruff format check, diff-check, blind-to-x project QC test (`1773 passed`, `9 skipped`) and lint, code-review gate advisory WARN `risk_score=0.50` covered by focused/related/project tests, VibeDebt (`fmkorea.py` score `33.0 -> 30.1`; `scrape_post` complexity/length `38/208 -> 4/36`), and A/B `adopt_candidate` (`score_delta=0.568856`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: Current launch evidence needs T-1370 cleanup before green closeout. A final full active-project QC refresh at current HEAD was attempted, but foreground/background runner attempts crossed the shell/session process boundary and left orphaned pytest processes; the interrupted `.tmp/project_qc_runner_latest.json` now reports false `returncode 4294967295` failures for blind/shorts while still showing Hanwoo (`505`) and Knowledge (`61`) pass and a completed blind parse count (`1774`). `python execution/session_orient.py --json` currently reports dirty unrelated files `execution/project_qc_runner.py` and `workspace/tests/test_project_qc_runner.py`; preserve those changes and inspect them under T-1370, then regenerate full active-project QC before claiming launch evidence is green.

- **2026-06-06 (Codex)**: Post-T-1366 launch evidence refresh. Full active-project QC passed and rewrote `.tmp/project_qc_runner_latest.json`: total `3953 passed`, `21 skipped`; blind-to-x `1769 passed`, `9 skipped`; shorts-maker-v2 `1618 passed`, `12 skipped`; Hanwoo `505 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence stayed blocked only on publish/current-head Actions and external/user-owned T-251: readiness score `96`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage with current code-review graph; completion audit `incomplete` with `9/14` complete and 5 blocked issues. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1366 local auto-research cycle completed after the publish/T-251 boundary. Code commit `18527ad8` splits `JobplanetScraper.scrape_post()` in `projects/blind-to-x/scrapers/jobplanet.py` into focused helpers for post-id extraction, detail fetch, title fallback, payload validation, screenshot capture, and result classification; `projects/blind-to-x/tests/unit/test_scrapers_jobplanet.py` adds helper and failure-classification coverage. Verification passed focused JobPlanet pytest (`14 passed`), related FMKorea/JobPlanet dry-run pytest (`28 passed`), combined focused/related pytest (`42 passed`), blind-to-x project QC (`1769 passed`, `9 skipped`) and lint, code-review gate advisory WARN `risk_score=0.40` covered by focused/related/project tests, AST metrics (`scrape_post` `96` lines/`20` branches -> `30` lines/`5` branches; max function length `96 -> 39`; max branches `20 -> 13`), and A/B `adopt_candidate` (`score_delta=0.597414`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: Post-T-1364/T-1365 launch evidence refresh. Full active-project QC passed and rewrote `.tmp/project_qc_runner_latest.json`: total `3945 passed`, `21 skipped`; blind-to-x `1761 passed`, `9 skipped`; shorts-maker-v2 `1618 passed`, `12 skipped`; Hanwoo `505 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence stayed blocked only on publish/current-head Actions and external/user-owned T-251: readiness score `96`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1364/T-1365 local auto-research cycles completed after the publish/T-251 boundary. T-1364 concurrent/local commit `e3145c11` split Blind feed URL helpers in `projects/blind-to-x/scrapers/blind.py` with tests in `projects/blind-to-x/tests/unit/test_scrapers_blind.py` and was covered by the final full QC refresh. T-1365 code commit `d04e612d` splits `TechVSShortsGenerator._render()` in `projects/shorts-maker-v2/tools/ai_tech_shorts.py` into focused layer/intro/points/verdict/phase/composition helpers while preserving byte-identical demo frames; verification passed focused pytest (`12 passed`), frame hashes (`6/6`), shorts-maker-v2 project QC (`1618 passed`, `12 skipped`) and lint, VibeDebt target score `36.9 -> 11.6`, and A/B `adopt_candidate` (`score_delta=0.386542`). No push was performed and T-251 was not retried.

- **2026-06-06 (Codex)**: Post-T-1362/T-1363 launch evidence refresh. Full active-project QC passed and rewrote `.tmp/project_qc_runner_latest.json`: total `3932 passed`, `21 skipped`; blind-to-x `1760 passed`, `9 skipped`; shorts-maker-v2 `1606 passed`, `12 skipped`; Hanwoo `505 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence stayed blocked only on publish/current-head Actions and external/user-owned T-251: readiness score `96`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. Code-review graph is stale only across context/doc changes and the launch audit reports no graph-relevant stale-range file changes. Use live `session_orient.py --json` for exact SHA/ahead count after context-only commits. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1362/T-1363 local auto-research cycles completed after the publish/T-251 boundary. T-1362 code commit `ecd4e5b5` splits `QuoteShortsGenerator._render()` in `projects/shorts-maker-v2/tools/psychology_quote.py` into focused timeline/alpha/layer/tag/quote/author helpers while preserving byte-identical demo frames; verification passed focused pytest (`6 passed`), related renderer pytest (`14 passed`), frame hashes (`6/6`), shorts-maker-v2 project QC test (`1606 passed`, `12 skipped`) and lint, and A/B `adopt_candidate` (`score_delta=108.867444`). T-1363 code commit `ce765cc5` deduplicates Hanwoo today-focus feed depletion and generic low-stock cards for the same feed row while preserving non-feed low-stock warnings; verification passed focused today-focus test (`17 passed`), Hanwoo project test (`505 passed`), lint, smoke, build retry after one concurrent Next build lock, and A/B `adopt_candidate` (`score_delta=41.384615`). No push was performed and T-251 was not retried. Post-cycle full active-project QC and launch evidence are recorded in the reliability note above.

- **2026-06-06 (Codex)**: Post-T-1361 current-head launch evidence refresh at the clean local head immediately before the latest context-only relay commits. Use live `session_orient.py --json` for exact SHA/ahead count. The refreshed checks reported local `main` ahead of `origin/main`, open PR count `0`, clean worktree, and a current code-review graph. Full active-project QC canonical artifact is current for the latest product-code head and passed with total `3925 passed`, `21 skipped`: blind-to-x `1760 passed`, `9 skipped`; shorts-maker-v2 `1600 passed`, `12 skipped`; Hanwoo `504 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Product readiness remains score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1361 shorts-maker-v2 space scale renderer helper split. `projects/shorts-maker-v2/tools/space_scale.py` now delegates `SpaceScaleGenerator._render()` into focused helpers for phase bounds, frame layers, scale-step timing, warp speed, previous/current transition state, scale/outro/rewind phase drawing, scaled image paste, and scale labels while preserving visual output. `projects/shorts-maker-v2/tests/unit/test_space_scale.py` covers timeline boundaries, transition helper curves, and nonblank scale/outro/rewind frames. Verification passed pre/post frame-hash comparison at 6 timestamps (`6/6` byte-identical), focused pytest (`3 passed`), shorts-maker-v2 project QC test (`1600 passed`, `12 skipped`, `1` warning) and lint, ruff check, ruff format check, `py_compile`, diff-check, staged code-review gate advisory WARN `risk_score=0.45` covered by frame hashes/focused/project tests, VibeDebt (`space_scale.py` score `38.9 -> 10.8`, max complexity `18 -> 8`, max function length `103 -> 34`, shorts-maker-v2 TDR `32.77 -> 32.47`), and A/B `adopt_candidate` (`score_delta=90.44647867564534`). Code commit `a27525e6` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: Post-relay launch audit and external dependency check at checked head `38a181d4`. Worktree was clean, `main` ahead of `origin/main` by `219`, open PR count `0`, and no local auto-research candidate remained. `shorts-maker-v2` direct readiness is `100` with QC `1597 passed`, `12 skipped`, docs/env complete, and `FEATURE` checklist `7/7`; browser QA inventory reports `4/4` fresh usable nonblank screenshots; GitHub inventory found `7` projects and `3` workflow files; dependency freshness reports direct candidates `0`. External npm metadata confirms ESLint latest is `10.4.1`, but `eslint-plugin-import@2.32.0`, `eslint-plugin-jsx-a11y@6.10.2`, and `eslint-plugin-react@7.37.5` still do not peer-allow ESLint 10; only `eslint-plugin-react-hooks@7.1.1` does, so do not force ESLint 10 yet. Release packet remains `ready_for_authorization`, selector remains `blocked_publish_only`, and completion audit remains `incomplete` with `9/14` complete and 5 blocked issues. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: Post-T-1360 launch evidence refresh. The checked head before this relay update was `0e71ae64`; use live `session_orient.py --json` for exact latest HEAD/ahead count after context-only commits. Full active-project QC passed and `.tmp/project_qc_runner_latest.json` was replaced with the passed full-run artifact: total `3922 passed`, `21 skipped`; blind-to-x `1760 passed`, `9 skipped`; shorts-maker-v2 `1597 passed`, `12 skipped`; Hanwoo `504 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence: readiness score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. No local auto-research candidate remains. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1360 blind-to-x Ppomppu feed URL helper split. `projects/blind-to-x/scrapers/ppomppu.py` now delegates `PpomppuScraper._fetch_post_urls()` into focused helpers for link selector selection, page URL collection, curl/session HTML fetch, intercept-mode URL collection, and direct-navigation fallback while preserving scraper behavior. `projects/blind-to-x/tests/unit/test_scrapers_ppomppu.py` covers hot-board selector filtering, duplicate/board URL filtering, any-board hot feed collection, and direct fallback after empty intercept results. Verification passed focused Ppomppu pytest (`15 passed`), related scraper pytest (`86 passed`), blind-to-x project QC (`1760 passed`, `9 skipped` plus lint), ruff check, ruff format check, `py_compile`, diff-check, code-review gate advisory WARN `risk_score=0.50` covered by direct/related/project tests, VibeDebt (`ppomppu.py` score `40.9 -> 35.2`, max complexity `19 -> 11`, max function length `98 -> 85`, `_fetch_post_urls` length/branches `98/19 -> 42/3`), and A/B `adopt_candidate` (`score_delta=7.386747627889143`). Code commit `eb1e51f0` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: Post-T-1359 launch evidence refresh. The checked project-code head before this relay update was `c44791b0`; use live `session_orient.py --json` for exact latest HEAD/ahead count after context-only commits. Full active-project QC passed and rewrote `.tmp/project_qc_runner_latest.json`: total `3918 passed`, `21 skipped`; blind-to-x `1756 passed`, `9 skipped`; shorts-maker-v2 `1597 passed`, `12 skipped`; Hanwoo `504 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence: readiness score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. No local auto-research candidate remains. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1359 blind-to-x Notion property payload helper split. `projects/blind-to-x/pipeline/notion/_upload.py` now delegates `_prepare_property_payload()` into focused helpers for property target resolution, scalar payloads, multi-select payloads, and date payloads while preserving Notion upload behavior. `projects/blind-to-x/tests/unit/test_notion_upload.py` adds direct regressions for date values and empty/missing/unsupported properties. Verification passed focused Notion upload pytest (`45 passed`), broader Notion pytest (`69 passed`), focused ruff check, focused ruff format check, blind-to-x project QC (`1756 passed`, `9 skipped` plus lint), diff-check, code-review gate advisory WARN `risk_score=0.40` covered by direct/project tests, structure metrics (`_prepare_property_payload` length `40 -> 10`, branches `17 -> 2`, helper count `0 -> 5`), and A/B `adopt_candidate` (`score_delta=3.271493212669683`). Code commit `0a2fd4f1` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1358 Knowledge Dashboard and word-chain Radix dependency freshness. `projects/knowledge-dashboard/package.json` now refreshes Radix dialog `1.1.16`, separator `1.1.9`, slot `1.2.5`, and tooltip `1.2.9`; `projects/word-chain/package.json` refreshes Radix slot `1.2.5`. The project npm lockfiles and root `pnpm-lock.yaml` are synchronized. Official npm metadata confirmed the targets match latest dist-tags. Verification passed root pnpm frozen lockfile check, Knowledge test/lint/build (`61 passed`), word-chain test/lint/build (`23 passed`, build via existing ASCII fallback after direct Vite `3221226505`), dependency freshness with direct candidates `0`, npm audit review, diff-check, code-review gate PASS `risk_score=0.0`, and A/B `adopt_candidate` (`score_delta=3.5454545454545454`). Code commit `0147a0f1` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1357 Hanwoo Radix dependency freshness. `projects/hanwoo-dashboard/package.json` now refreshes 12 direct Radix UI packages to the current npm `latest` patch/minor releases, with `projects/hanwoo-dashboard/package-lock.json` and root `pnpm-lock.yaml` synchronized. Official npm metadata confirmed the targets match latest dist-tags and React peer ranges include React 19. Verification passed `npm.cmd install --package-lock-only --ignore-scripts`, root `pnpm.cmd install --lockfile-only --frozen-lockfile --ignore-scripts`, Hanwoo project QC (`504 passed`, lint, build, smoke), `git diff --check`, code-review gate PASS `risk_score=0.0`, dependency freshness with Hanwoo direct candidates `0`, and A/B `adopt_candidate` (`score_delta=0.9632352941176471`). npm audit still reports `5` moderate existing advisories whose force fixes cross unsuitable major/downgrade boundaries, so no broad audit fix was included. Code commit `f3defece` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1356 shorts-maker-v2 RenderStep orchestration helper split. `projects/shorts-maker-v2/src/shorts_maker_v2/pipeline/render_step.py` now delegates scene render and output orchestration into focused helpers for warning recording, role style selection, scene color grading, TTS postprocess/audio attach, static/karaoke caption composition, hook animation, B-roll PiP, closing fade, bookend append, scene sequence rendering, short-limit trimming, BGM/SFX layering, video write kwargs/output, benchmark logging, and resource cleanup while preserving render behavior. `projects/shorts-maker-v2/tests/unit/test_render_step_core.py` covers role-style mapping and static-caption prefix/resource tracking. Verification passed focused render-step pytest (`32 passed`), related `test_render_step_*.py` (`132 passed`, `1` upstream warning), shorts-maker-v2 project QC (`1597 passed`, `12 skipped`, `1` warning plus lint), full active-project QC (`3916 passed`, `21 skipped`), ruff, format check, `py_compile`, diff-check, staged code-review gate advisory WARN `risk_score=0.40` covered by direct/related/project tests, VibeDebt (`render_step.py` score `35.8`, max complexity `14`, max function length `116`, shorts-maker-v2 TDR `32.77`), and A/B `adopt_candidate` (`score_delta=0.7191674605064697`). Code commit `2c0bbfb4` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1355 shorts-maker-v2 history countdown render helper split. `projects/shorts-maker-v2/tools/history_fact_shorts.py` now caches `HistoryCountdownGenerator`'s static countdown background gradient and delegates `_render()` to focused helpers for ember drawing, phase timing, hook title, item card, description card, and CTA drawing while preserving visual output. `projects/shorts-maker-v2/tests/unit/test_history_fact_shorts.py` covers timing and cached gradient formula. Verification passed pre/post frame-hash comparison at 4 timestamps (`4/4` byte-identical), `compileall`, focused pytest (`2 passed`), ruff, format check, diff-check, VibeDebt (`history_fact_shorts.py` score `45.3 -> 31.8`, max function length `79 -> 50`, shorts-maker-v2 TDR `33.09 -> 32.99`), staged code-review gate advisory WARN `risk_score=0.35` covered by tests/frame hashes, and A/B `adopt_candidate` (`score_delta=0.22411434598396104`). Unrelated dirty WIP in `projects/shorts-maker-v2/src/shorts_maker_v2/pipeline/render_step.py` is preserved and excluded. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: Post-T-1354 checked launch evidence refresh. At checked head `c7e2de49` before the final relay/doc commits, the worktree was clean, `main` was ahead of `origin/main` by `188`, open PR count was `0`, and `session_orient.py --json` reported the graph current for `c7e2de49` after one transient parallel graph-update SQLite lock. Product readiness accepted the canonical full active-project QC as current because no relevant project changes occurred since artifact head `92cc7af6`: blind-to-x `1754 passed`, `9 skipped`; shorts-maker-v2 `1590 passed`, `12 skipped`, `1 warning`; Hanwoo `504 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence remained readiness score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. No local auto-research candidate remained. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1354 blind-to-x DraftQualityGate helper split. `projects/blind-to-x/pipeline/draft_quality_gate.py` now delegates `DraftQualityGate.validate()` quality groups to focused helpers for length, Korean ratio, social style, creator take, format, duplicate sentences, cliches, repetition/hook checks, vague expressions, and strict mode while preserving behavior. Verification passed focused quality-gate pytest (`235 passed`), blind-to-x project QC (`1754 passed`, `9 skipped` plus lint), `ruff check`, `ruff format --check`, `py_compile`, `git diff --check`, code-review gate advisory WARN `risk_score=0.55` covered by focused/project tests, and A/B `adopt_candidate` (`score_delta=2.4833293186234364`; `validate` branches `55 -> 4`, length `333 -> 41`). Code commit `ad6cd7c0` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: Post-T-1353 launch evidence refresh. After T-1352/T-1353 and before this relay update, the checked state had a clean worktree, a local `main` branch ahead of `origin/main`, and a current code-review graph. Full active-project QC passed and refreshed the canonical artifact: blind-to-x `1754 passed`, `9 skipped`; shorts-maker-v2 `1590 passed`, `12 skipped`, `1 warning`; Hanwoo `504 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence stayed at readiness score `96`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; browser QA `4/4`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. No push was performed, and T-251 was not retried. The only remaining boundaries are explicit push authorization/user push plus current-head Actions, and external/user-owned Hanwoo T-251 Supabase credential reset/live Prisma CRUD E2E.

- **2026-06-06 (Codex)**: T-1352 Hanwoo market price freshness status announcement. `projects/hanwoo-dashboard/src/components/widgets/MarketPriceWidget.js` now builds a shared Korean freshness label from the last update time and exposes the last-updated/source footer as a polite `role="status"` region with matching `aria-label` and `title`, so manual market-price refresh completion is announced without changing the visible layout. `projects/hanwoo-dashboard/src/lib/home-market-copy.test.mjs` locks the label and live-region contract. Verification passed Hanwoo node tests (`504 passed`, `0 failed`), Hanwoo project QC (`test`, `lint`, `build`, `smoke`), diff-check, staged code-review gate advisory WARN `risk_score=0.40` covered by Hanwoo tests/project QC, and A/B `adopt_candidate` (`score_delta=2.272727272727273`). Code commit `82d061dd` is local only. After T-1352, concurrent local shorts-maker-v2 commit `c2960d9d` (`refactor(shorts): T-1353 split llm bridge helpers`) appeared and must be preserved. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1351 blind-to-x BaseScraper browser-open helper split and final evidence refresh. `projects/blind-to-x/scrapers/base.py` now delegates pool launch, single-browser guard checks, proxy config, Camoufox launch, Chromium fallback, and partial Chromium cleanup to focused helpers while preserving browser lifecycle behavior. `projects/blind-to-x/tests/unit/test_scrapers_base.py` covers failed Camoufox launch falling back to Chromium without closing an unentered Camoufox context. Verification passed focused base scraper pytest `15/15`, related scraper pytest `37/37`, full blind-to-x project QC (`1754 passed, 9 skipped` plus lint), full active-project QC (`3909 passed, 21 skipped`), ruff, format check, `py_compile`, diff-check, graph current at `964c82ba`, VibeDebt (`base.py` score `36.3`, `open` branches `27 -> 4`, length `95 -> 14`), code-review gate advisory WARN covered by direct/related/project tests, release packet `ready_for_authorization`, selector `blocked_publish_only`, completion audit `incomplete` with `9/14` complete and 5 blocked issues, and A/B `adopt_candidate` (`score_delta=2.620346807188912`). Code commit `64cae0cf` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1350 workspace CI analyzer helper split. `workspace/execution/_ci_analyzers.py` now delegates Python analyzer naming, large-literal, public type-hint, and string-concat loop checks to focused helpers while preserving analyzer behavior. `workspace/tests/test_ci_analyzers.py` covers string-ish augassign detection, naming exemptions, and public missing type-hint filtering. Verification passed focused analyzer pytest `6/6`, related workspace pytest `30/30` with repo-local `--basetemp` after a Windows temp permission error, ruff, format check, `py_compile`, diff-check, VibeDebt (`_ci_analyzers.py` score `36.9 -> 28.5`, max complexity `14 -> 8`, duplicate blocks `22 -> 16`, workspace TDR `28.51 -> 28.41`), code-review gate advisory WARN `risk_score=0.55` covered by direct/related tests, and A/B `adopt_candidate` (`score_delta=1.889754540706912`). Code commit `b8b488ad` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: Post-T-1349 evidence refreshed after the Knowledge Dashboard `@types/node` `^25.9.1 -> ^25.9.2` dependency commit. Latest checked launch evidence at that point was worktree clean, graph current, canonical full active-project QC PASS (`3908 passed, 21 skipped`; blind-to-x `1753 passed, 9 skipped`, shorts-maker-v2 `1590 passed, 12 skipped`, Hanwoo `504 passed`, Knowledge `61 passed`), readiness score `96` with local blockers `0`, release packet `ready_for_authorization`, selector `blocked_publish_only`, launch-objective audit complete coverage, and completion audit `incomplete` with `9/14` complete and 5 blocked issues. Use live `session_orient.py --json` for the exact current commit/ahead/graph state after later relay commits. Remaining boundaries are explicit push authorization/user push plus current-head Actions, and external/user-owned T-251.

- **2026-06-06 (Codex)**: Post-T-1347 pre-relay evidence refreshed at `d1f4b4d0`. Worktree clean, graph current, canonical full active-project QC PASS (`3908 passed, 21 skipped`; blind-to-x `1753 passed, 9 skipped`, shorts-maker-v2 `1590 passed, 12 skipped`, Hanwoo `504 passed`, Knowledge `61 passed`), readiness score `96` with local blockers `0`, release packet `ready_for_authorization`, selector `blocked_publish_only`, and completion audit `incomplete` with `9/14` complete and 5 blocked issues. Remaining boundaries are explicit push authorization/user push plus current-head Actions, and external/user-owned T-251.

- **2026-06-06 (Codex)**: T-1347 blind-to-x filter/profile stage helper split. `projects/blind-to-x/pipeline/process_stages/filter_profile_stage.py` now delegates shared filter skip/result handling, daily queue-floor overrides, profile/review decision wiring, emotion/profile side effects, editorial gate rejection, viral scoring, content-calendar diversity, and daily budget checks to focused helpers while preserving `run_filter_profile_stage()` behavior. `tests/unit/test_process_stages.py` covers calendar diversity rejection, daily-floor calendar override, and budget failure handling. Verification passed blind-to-x project QC (`1753 passed, 9 skipped` plus lint), focused ruff, `git diff --check` (CRLF warning only), VibeDebt (`project TDR 34.02 -> 33.79`, target file moved from rank 5/score 36.7 to outside top 80 with direct score 22.5), code-review gate advisory WARN `risk_score=0.40` covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=2.658607248209018`). Code commit `bc77355d` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1345/T-1346 blind-to-x Notion query and persist-stage helper splits. `projects/blind-to-x/pipeline/notion/_query.py` now delegates property decoding, status-aware option values, recency checks, sort keys, database matching, and recent search-result filtering into focused helpers; `tests/unit/test_notion_query_mixin.py` covers multi-select and malformed Notion payloads. `projects/blind-to-x/pipeline/process_stages/persist_stage.py` now delegates missing-uploader failure, NotebookLM task scheduling, Blind image prompt/A-B variant handling, AI image scheduling, media await handling, Notion upload, and draft analytics recording while preserving `run_persist_stage()` behavior. Verification passed focused query pytest `13/13`, related Notion pytest `67/67`, feedback pytest `28/28`, persist/process-stage pytest `50/50`, ruff, format checks, `py_compile`, diff-checks, blind-to-x project QC (`1750 passed, 9 skipped` plus lint), graph refresh current at `c2f6f885`, code-review gate advisory WARNs (`risk_score=0.60/0.40`) covered by direct/project tests, and A/B adoption for both (`score_delta` `2.552950` and `2.584363`). Worktree was clean/ahead `146` before this relay update. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1340 shorts-maker-v2 provider optional import hardening and exact-head launch evidence refresh. `providers.__getattr__` now restores already-loaded provider submodules from `sys.modules` before `find_spec()`, and `edge_tts_client.py` imports without the optional `edge-tts` package while raising a clear lazy `ImportError` only if fallback `Communicate()` is used. `tests/unit/test_tts_providers.py` covers provider submodule recovery and missing Edge TTS dependency import. Verification passed focused provider pytest `3/3`, related provider/Edge pytest `102/102`, full active-project QC at `47d2fb48` (blind-to-x `1744 passed, 9 skipped`; shorts-maker-v2 `1590 passed, 12 skipped, 1 warning`; Hanwoo `500 passed` plus lint/build/smoke; Knowledge `61 passed` plus lint/build/smoke), ruff, format check, `py_compile`, diff checks, VibeDebt project TDR `33.22`, code-review gate `risk_score=0.0` after direct/full coverage, and A/B `adopt_candidate` (`score_delta=0.6684382871536524`). Final local launch evidence is clean/ahead `130`, readiness score `96`, release packet `ready_for_authorization`, selector `blocked_publish_only`, and completion audit `9/14` complete with 5 blocked issues. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1336/T-1337/T-1338 shorts-maker-v2 CosyVoice and lazy import boundary cycle. `projects/shorts-maker-v2/src/shorts_maker_v2/providers/cosyvoice_client.py` now splits `generate_tts()` into focused helpers for generation modes, tensor collection, audio output saving, MP3 fallback, and word timing while preserving zero-shot/ref-audio/instruct/cross-lingual/SFT behavior. Package/provider/pipeline imports are lazy or local where appropriate; top-level and pipeline `__getattr__` now restore already-loaded `sys.modules` submodules before `find_spec()` so preloaded modules and test doubles survive optional dependency discovery failures. `tests/unit/test_tts_providers.py` and entrypoint tests cover provider routing plus preloaded-submodule recovery. Verification passed provider/entrypoint pytest `53/53`, full shorts-maker-v2 project QC (`1584 passed, 12 skipped, 1 warning` plus lint), ruff, format check, `py_compile`, diff checks, VibeDebt (`cosyvoice_client.py` score `40.1 -> 20.9`, max complexity `20 -> 6`, max function length `115 -> 37`, project TDR `33.39 -> 33.24`), code-review gate advisory WARNs `risk_score=0.40/0.30` covered by focused/full tests, and A/B `adopt_candidate` (`score_delta=6.71426596552098`). Commits `7694b14d`, `8efb3556`, and `8cd6fe7c` are local only. Current workspace is clean/ahead `124`; code-review graph is stale (`built_at_commit e0a67597`, current `8cd6fe7c`). Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1334 shorts-maker-v2 media fallback chain tightening. `projects/shorts-maker-v2/src/shorts_maker_v2/pipeline/media/fallback_mixin.py` now extracts `_try_image_provider()` and `_try_dalle_image()` so `_try_image_chain()` builds ordered Imagen/Gemini/Pollinations attempts while preserving DALL-E policy retry, stock-video fallback, cache, and placeholder behavior. `tests/unit/test_media_step_branches.py` covers skipped stock-mix attempts not recording failures and stock-video recovery after image-provider failures. Verification passed focused media-step pytest `27/27`, related media fallback pytest `6/6`, full shorts-maker-v2 project QC (`1584 passed, 12 skipped, 1 warning` plus lint), ruff, format check, `py_compile`, diff checks, VibeDebt (`fallback_mixin.py` score `40.5 -> 36.3`, max complexity `19 -> 13`, max function length `103 -> 93`, duplicate blocks `2 -> 2`, project TDR `33.47 -> 33.39`), code-review gate advisory WARN `risk_score=0.35` covered by focused/related/full tests, and A/B `adopt_candidate` (`score_delta=1.3466666666666667`). Commit `bd2fa637` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: Post-T-1333 launch evidence refresh. At clean local head `4c7c61c4`, `product_readiness_score.py --json` stayed score `96`, state `blocked`, with `local_blocker_count=0`, `publish_blocker_count=1`, and `external_blocker_count=1`. Current direct target states: blind-to-x `100/ready`, shorts-maker-v2 `100/ready`, knowledge-dashboard `100/ready`, hanwoo-dashboard `86/blocked` only by T-251. Release packet was `ready_for_authorization` with ahead `99`, dirty `0`, and unproven `root-quality-gate`/`active-project-matrix`; selector was `blocked_publish_only`; browser QA evidence was fresh usable nonblank `4/4`; dependency inventory had no direct patch/minor candidates. Completion audit remained `incomplete` at `9/14` with 5 explicit blockers: publish/Actions, release authorization, selector publish boundary, external/user-owned T-251, and direct Hanwoo readiness T-251. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1333 blind-to-x 6D scoring helper split. `projects/blind-to-x/pipeline/content_intelligence/scoring_6d.py` now splits freshness, social signal, hook strength, trend relevance, audience targeting, viral potential, 6D weight loading, weighted rank calculation, source quality boost, calibration row fetch, engagement extraction, dimension proxies, correlations, and normalized calibration weights into focused helpers while preserving `calculate_6d_score()` / `calibrate_weights()` behavior. `tests/unit/test_content_intelligence.py` covers weighted dimension scoring and source quality boost behavior. Verification passed focused pytest `6/6`, related unit pytest `52/52`, integration pytest `23/23`, blind-to-x project QC (`1743 passed, 9 skipped` plus lint), ruff, format check, `py_compile`, diff checks, AST metrics (`scoring_6d.py` max complexity `22 -> 7`, max function length `143 -> 30`, `calculate_6d_score` `22/143 -> 5/27`, `calibrate_weights` `19/83 -> 3/30`), VibeDebt project TDR `34.97 -> 34.8`, code-review gate advisory WARN `risk_score=0.45` covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=0.6502586776752242`). Commit `7492047f` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1332 blind-to-x editorial scoring helper split. `projects/blind-to-x/pipeline/content_intelligence/scoring_editorial.py` now splits editorial signal construction, dimension scoring, weighted score, hard-reject checks, reason labels, and publishability brief/reason assembly into focused helpers while preserving `evaluate_candidate_editorial_fit()` / `calculate_publishability_score()` behavior. `tests/unit/test_content_intelligence.py` covers preserved scoring dimensions and publishability brief wiring. Verification passed focused pytest `5/5`, related pytest `74/74`, full blind-to-x project QC (`1742 passed, 9 skipped` plus lint), ruff, format check, `py_compile`, diff checks, AST metrics (`scoring_editorial.py` max complexity `50 -> 8`, max function length `169 -> 49`), VibeDebt project TDR `34.97`, code-review gate advisory WARN `risk_score=0.45` covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=1.4935507`). Commit `c6bed861` is local only. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1331 shorts-maker-v2 script prompt locale override split. `projects/shorts-maker-v2/src/shorts_maker_v2/pipeline/script_prompts.py` now splits locale override normalization/merging for tone presets, CTA forbidden words, channel persona, persona keywords, prompt field names, and channel review criteria into focused helpers while preserving prompt/review behavior. `tests/unit/test_script_step_i18n.py` covers malformed locale bundle entries so bad override fragments do not corrupt defaults while valid entries still apply. Verification passed focused script pytest `30/30` with one upstream Google GenAI deprecation warning, full shorts-maker-v2 project QC (`1582 passed, 12 skipped, 1 warning` plus lint), ruff, format check, `py_compile`, diff checks, VibeDebt (`script_prompts.py` score `40.6 -> 34.9`, max complexity `39 -> 11`, max function length `99 -> 93`, project TDR `33.53 -> 33.47`), code-review gate advisory WARN `risk_score=0.45` covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=0.2579619`). Commit `d4ca6be4` is local only; unrelated blind-to-x dirty WIP remains and must be preserved. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1330 Knowledge Dashboard NotebookLM fetch helper split. `projects/knowledge-dashboard/scripts/sync_data.py` now splits NotebookLM dependency availability, token loading, client construction, and notebook/source payload simplification into focused helpers. `scripts/test_sync_data.py` covers fake-client payload simplification, empty source lists, and client credential argument wiring. Verification passed Knowledge sync pytest `16/16`, Knowledge project QC (`61 passed` plus lint/build/smoke), ruff, format check, `py_compile`, diff checks, VibeDebt (`sync_data.py` score `21.0 -> 12.0`, max complexity `11 -> 9`, max function length `50 -> 45`, project TDR `25.06 -> 20.88`), code-review gate advisory WARN `risk_score=0.55` covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=0.2237566`). Commit `da6381c1` is local only; unrelated blind-to-x and shorts-maker-v2 dirty WIP remains and must be preserved.

- **2026-06-06 (Codex)**: T-1328/T-1329 launch fallback guard cycle. Knowledge Dashboard `scripts/sync_data.py` now splits NotebookLM token-template checks into focused helpers and covers non-string cookie values plus identity placeholder fields in script/app tests. shorts-maker-v2 `providers/tts_factory.py` now splits TTS provider routing/fallback behavior and covers premium provider success, stale-output unlink plus Edge fallback, OpenAI signature routing, and missing OpenAI client failure in direct factory tests. Focused verification passed Knowledge sync pytest `15/15`, Shorts TTS/provider pytest `17/17` with one upstream Google GenAI deprecation warning, ruff, format check, `py_compile`, diff checks, and A/B `adopt_candidate` for both (`score_delta` `1.1957691`, `1.5712114`). Full active-project QC passed on the current code commit: blind-to-x `1740 passed, 9 skipped`; shorts-maker-v2 `1581 passed, 12 skipped, 1 warning`; Hanwoo `500 passed` plus lint/build/smoke; Knowledge `61 passed` plus lint/build/smoke. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1327 blind-to-x generate-review-stage helper split. `generate_review_stage.py` now delegates missing-generator failure, screenshot upload, draft splitting, Twitter reply fallback, generation failure, quality-gate retries, post-generation components, Twitter quality failure, and completion into focused helpers while preserving orchestration behavior. `run_generate_review_stage()` shrank from 230 to 47 lines and 46 to 7 branch points; private helpers increased from 4 to 18. `test_process_stages.py` covers readable Korean Twitter reply fallback copy. Verification passed process-stage pytest `42/42`, full blind-to-x project QC (`1740 passed, 9 skipped` plus lint), ruff, format check, `py_compile`, diff checks, code-review gate advisory WARN `risk_score=0.30` covered by direct/full tests, and A/B `adopt_candidate` (`score_delta=1.0158915`). Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-06 (Codex)**: T-1325 Knowledge Dashboard direct launch-readiness audit evidence. `launch_objective_audit.py` now includes a direct `knowledge-dashboard` target item requiring score `100`, ready state, nonstale project QC `PASS`, README/package/page artifacts, complete env checks, no unresolved tasks, and no dirty target paths. Regressions cover complete Knowledge evidence and stale/failing Knowledge readiness blockers. Verification passed focused launch/completion pytest `45/45`, ruff, format check, `py_compile`, code-review graph refresh on the T-1325 feature commit, code-review gate advisory WARN `risk_score=0.40` covered by direct tests, and A/B `adopt_candidate` (`score_delta=0.918273`). Feature-head evidence remained locally green except publish/Actions and external/user-owned T-251: readiness score `96`, local blockers `0`, publish blockers `1`, external blockers `1`, release packet `ready_for_authorization`, selector `blocked_publish_only`, completion audit `9/14` complete with 5 blocked issues. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-05 (Codex)**: T-1322/T-1323/T-1324 launch evidence freshness and graph audit refinement. Launch audits now include current-code triage evidence from `session_orient.py --json` and `code_review_gate.py`; canonical project-QC artifacts are schema `3` and record branch/head/dirty metadata; readiness treats project QC as stale only when relevant project/global QC files changed since the artifact head; and stale code-review graph evidence blocks launch completion only when graph-relevant files changed or the stale range cannot be inspected. Verification passed T-1322 focused/related tests `37/37` and `56/56` with A/B `adopt_candidate` (`score_delta=1.155124`), T-1323 focused readiness/QC tests `44/44` with A/B `score_delta=1.503968`, T-1324 focused launch-audit tests `39/39` with ruff/format/py_compile/diff-check and A/B `score_delta=1.232601`, plus full active-project QC: blind-to-x `1739 passed, 9 skipped`; shorts-maker-v2 `1577 passed, 12 skipped, 1 warning`; Hanwoo `500 passed` plus lint/build/smoke; Knowledge `61 passed` plus lint/build/smoke. Current product launch evidence is locally green except for publish/Actions and external/user-owned T-251: readiness score `96`, local blockers `0`, publish blockers `1`, external blockers `1`, release packet `ready_for_authorization`, selector `blocked_publish_only`, completion audit `8/13` complete with 5 blocked issues. Do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-05 (Codex)**: T-1321 blind-to-x Ppomppu post helper extraction. `PpomppuScraper.scrape_post()` now delegates post fetch failure classification, route fulfillment, post container discovery, direct-navigation fallback, title extraction, content extraction, count extraction, image URL extraction, and screenshot saving into focused helpers while preserving behavior. Added regressions for comment-span title cleanup, content selector fallback, comment-line count fallback, image URL normalization/filtering, and fetch-failure reason preservation. Verification passed focused Ppomppu pytest `11/11`, related scraper pytest `43/43`, ruff, format check, `py_compile`, `git diff --check`, full blind-to-x project QC `1739 passed, 9 skipped` plus lint, VibeDebt (`ppomppu.py` debt `44.8 -> 40.9`, max complexity `53 -> 19`, `scrape_post` complexity `53 -> 9`, length `255 -> 85`), code-review gate advisory WARN `risk_score=0.50` covered by direct/full tests, and A/B `adopt_candidate` (`score_delta=1.827920`). The workspace remains ahead of `origin/main`; do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-05 (Codex)**: T-1320 blind-to-x Ppomppu scraper helper extraction. `PpomppuScraper` now delegates feed URL selection, feed navigation, row title/comment/view/like/image extraction, feed candidate construction, and feed candidate collection into focused helpers while preserving ranking, dedupe, and metadata behavior. Added regression coverage for candidate sort/filter/dedupe plus image metadata. Verification passed focused Ppomppu scraper pytest `19/19`, related scraper pytest `71/71`, ruff, format check, `py_compile`, `git diff --check`, staged code-review gate advisory WARN `risk_score=0.45` covered by direct tests, and A/B `adopt_candidate` (`score_delta=2.314408`). The workspace remains ahead of `origin/main`; do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-05 (Codex)**: T-1319 blind-to-x Blind scraper helper extraction. `BlindScraper` now delegates feed URL selection, feed row metadata, feed candidate construction/collection, post content/count extraction, fetch failure classification, post navigation, post container discovery, Crawl4AI fallback handling, screenshot saving, and category keyword matching into focused helpers while preserving behavior. Added regressions for feed candidate engagement sorting/deduplication and category fallback coverage. Verification passed graph detect-changes `risk_score=0.0`, focused Blind scraper pytest `11/11`, related scraper pytest `75/75`, ruff, format check, `py_compile`, `git diff --check`, full blind-to-x project QC `1733 passed, 9 skipped` plus lint, staged code-review gate advisory WARN `risk_score=0.65` covered by direct/full tests, and A/B `adopt_candidate` (`score_delta=0.528523`). The workspace remains ahead of `origin/main`; do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-05 (Codex)**: T-1318 blind-to-x selector repair maintainability. `BaseScraper._suggest_selectors()` now delegates selector part scoring, scored dedupe, unique append, class fallback, similar-id fallback, and tag content fallback into focused helpers while preserving CSS repair ordering and max-3 behavior. Regression tests cover duplicate class/id/tag candidates and fallback ordering. Verification passed focused selector/scraper pytest `46/46`, ruff, format check, `py_compile`, VibeDebt audit (`base.py` debt `43.1 -> 43.0`, `_suggest_selectors` complexity `35 -> 7`, length `92 -> 50`), `git diff --check`, code-review gate advisory WARN `risk_score=0.40` covered by direct tests, full blind-to-x project QC `1732 passed, 9 skipped` plus lint, and A/B `adopt_candidate` (`score_delta=0.693839`). The workspace remains ahead of `origin/main`; do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-05 (Codex)**: T-1317 blind-to-x draft generator best-of-N maintainability. `generate_drafts()` now delegates cache writes, recent-failure provider filtering, best-of-N candidate generation, and reviewer selection into focused helpers while preserving behavior. Added regressions for selected comment-trigger persistence, single-candidate cache writes, and reviewer-failure fallback cache writes. Verification passed project `.venv` best-of-N tests `15/15`, related draft generation tests `36/36`, blind-to-x project QC `1729 passed, 9 skipped` plus lint, ruff, format check, `py_compile`, `git diff --check`, code-review gate advisory WARN `risk_score=0.40` covered by direct tests, and A/B `adopt_candidate` (`score_delta=0.773171`). The workspace remains ahead of `origin/main`; do not push without explicit user authorization, and do not retry T-251 before Supabase credentials are reset.

- **2026-06-05 (Codex)**: T-1316 session-orient graph freshness evidence. `session_orient.py --json` now compares code-review graph `built_at_commit` with the current git head and reports `current_head`, `freshness`, `stale`, and `stale_reason`; text/Rich diagnostics also show stale or unknown graph freshness. This matters because the code-review graph can remain built on an older commit while local launch commits are ahead of origin. Verification passed focused session-orient pytest `33/33`, ruff, format check, `py_compile`, `git diff --check`, runtime JSON stale-graph evidence, staged code-review gate advisory WARN `risk_score=0.60` covered by direct tests, and A/B `adopt_candidate` (`score_delta=1.472549`). Do not push without explicit user authorization; T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1313/T-1314/T-1315 launch-loop stabilization. The local branch is clean and ahead of `origin/main`; regenerate the release authorization packet for the exact current HEAD before any push request. Product readiness is score `96` with `local_blocker_count=0`, `publish_blocker_count=1`, and `external_blocker_count=1`; release packet is `ready_for_authorization`; selector is `blocked_publish_only`; completion audit is `8/13` complete with 5 explicit blockers (publish/Actions, release authorization, selector publish boundary, external/user-owned T-251, direct Hanwoo readiness). T-1314 centralized blind-to-x Notion review optional-signal formatting helpers and passed full blind-to-x QC. T-1315 changed `project_qc_runner.py` so partial/focused runs do not overwrite `.tmp/project_qc_runner_latest.json`; only complete active-project coverage may update the canonical latest artifact. Use `.tmp/project_qc_runner_partial_latest.json` for focused QC artifacts.

- **2026-06-05 (Codex)**: T-1312 release authorization packet and Hanwoo direct launch evidence. `release_authorization_packet.py` now produces a no-push authorization packet for clean-ahead current HEADs, including branch/head, ahead commits, dirty paths, required workflow status, post-push gates, suggested push command only when clean, blockers, and guardrails. `next_experiment_selector.py` now reports clean-ahead unproven Actions as `blocked_publish_only` and routes dirty states back to GitHub inventory follow-up. `launch_objective_audit.py` now includes direct `hanwoo-dashboard` target readiness evidence, so T-251 appears as complete coverage plus explicit target blockers. Clean-state release packet is `ready_for_authorization`; selector is `blocked_publish_only`; completion audit remains `incomplete` with `8/12` complete and 4 blocked issues: publish/Actions, selector publish boundary, external/user-owned T-251, and direct Hanwoo readiness T-251. Verification passed focused selector/launch/completion/release-packet pytest `51/51`, workflow-hygiene plus packet pytest `6/6`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.998026`), and graph gate warning `risk_score=0.40` covered by direct tests. Do not push without explicit user authorization; T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1310/T-1311 publish-boundary launch evidence cleanup. `product_readiness_score.py` now places the current-head publish/Actions blocker before user-owned project waits in `next_actions`; `launch_objective_audit.py` now treats current-head publish boundaries as complete evidence with explicit blockers instead of partial evidence gaps. Clean-state readiness is score `96` with `local_blocker_count=0`, `publish_blocker_count=1`, and the first next action requiring explicit push authorization/user push plus Actions. Completion audit remains `incomplete` for the right blockers only: publish/Actions, selector publish boundary, and external/user-owned T-251. Verification passed focused readiness/selector/launch/completion tests, ruff, `py_compile`, `git diff --check`, A/B `adopt_candidate` for both candidates, and graph gate warning for T-1311 was covered by direct tests. Do not push without explicit user authorization; T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1309 clean-ahead relay closeout note. `session_orient.py` now adds `latest_next_priority_note` when the latest HANDOFF `Next Priorities` contains a committed relay closeout step, the worktree is clean, and the branch is ahead of `origin/main`. The status stays `ok`, but the note makes clear that the local commit step is already satisfied while publish/CI/user blockers may remain. Verification passed focused session-orient pytest `30/30`, broader session/selector/launch pytest `68/68`, ruff, pre-commit ruff format check, `py_compile`, `git diff --check`, staged code-review gate advisory WARN `risk_score=0.55` covered by direct tests, and A/B `adopt_candidate` (`score_delta=0.949127`). Do not push without explicit user authorization; T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1308 shorts-maker-v2 FEATURE checklist launch-audit guard. `launch_objective_audit.py` now parses Markdown checkbox items and requires `projects/shorts-maker-v2/FEATURE.md` to contain a non-empty, fully closed checklist before the direct `shorts-maker-v2` target product readiness requirement can be complete. `FEATURE.md` now records implemented status and all seven acceptance criteria are checked, so launch audits expose `shorts-maker-v2 FEATURE checklist complete 7/7, open=0`. Verification passed focused launch-audit pytest `28/28`, broader auto-research/readiness pytest `69/69`, full active-project QC (blind-to-x `1723 passed, 9 skipped`; shorts-maker-v2 `1577 passed, 12 skipped`; Hanwoo `500/500` plus lint/build/smoke; Knowledge `61/61` plus lint/build/smoke), ruff, `py_compile`, `git diff --check`, staged code-review gate `risk_score=0.0`, runtime launch-audit evidence, and A/B `adopt_candidate` (`score_delta=0.994212`). Do not push without explicit user authorization; T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1307 credential exposure hardening and selector snapshot reuse. Hanwoo seed now refuses to create the admin user unless `ADMIN_PASSWORD` is configured and at least 12 characters long, so public `admin/admin123` credentials are no longer seeded. The login page no longer shows demo credentials and instead shows operator credential guidance. `launch_objective_audit.py` now derives next-experiment selection from the same collected readiness/GitHub/browser/dependency snapshot, preventing clean-audit/dirty-selector evidence races. Verification passed Hanwoo Node tests `500/500`, Hanwoo project QC test/lint/build/smoke, focused auto-research pytest `37/37`, ruff, format check, `py_compile`, `git diff --check`, browser login click QA with demo credentials absent and console `0/0`, and A/B `adopt_candidate` (`score_delta=1.124169`). Do not push without explicit user authorization; T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1306 release publish-blocker classification. `product_readiness_score.py` now separates a clean local branch ahead of `origin/main` with missing current-head required Actions from actual repo-side local blockers. In that state GitHub release status reports `ahead_count`, `publish_required`, and `blocker_type=publish`; overall readiness reports `publish_blocker_count=1` while `local_blocker_count` stays `0` unless there is dirty worktree, open PR, failed/in-progress pushed Actions, or environment work. `launch_objective_audit.py` now records `workspace/local/publish/agent blockers=...`. Do not push without explicit user authorization; after push, wait for current-head `root-quality-gate` and `active-project-matrix`.

- **2026-06-05 (Codex)**: T-1305 shorts-maker-v2 launch evidence routing. `launch_objective_audit.py` now includes direct `shorts-maker-v2` target readiness evidence instead of relying only on workspace-level launch proxy signals. Runtime audit verifies `shorts-maker-v2` score `100`, state `ready`, QC `PASS` (`1577 passed`, `12 skipped`), docs `5/5`, env checks `1/1`, tasks `0`, and dirty target paths `0`. `.playwright-mcp/` is ignored so Playwright MCP console/page snapshots do not become false GitHub inventory dirty blockers. `next_experiment_selector.py` now reports a clean local branch ahead of origin with missing required Actions as `current_head_release_checks_unproven`; do not push without explicit user authorization, then wait for current-head `root-quality-gate` and `active-project-matrix` after push.

- **2026-06-05 (Codex)**: T-1304 launch evidence refresh. Current local launch evidence is still green on clean/synced `main` at `a6535544`: open PRs `0`, current-head `root-quality-gate` and `active-project-matrix` success, readiness score `96`, workspace/local/agent blockers `0/0/0`, GitHub inventory dirty count `0`, browser QA coverage `4/4` with fresh usable/nonblank screenshots `4/4`, and direct dependency candidates `0`. The launch completion audit remains `8/9` complete with one blocker, external/user-owned Hanwoo T-251. Do not retry T-251 until the Supabase Dashboard database credential reset/resync is done; use `py -3.13 execution\code_review_gate.py` for graph-backed review gate runs in this Windows environment.

- **2026-06-05 (Codex)**: T-1303 launch audit selector required-gate evidence. `launch_objective_audit.py` now carries `selected.required_gates` from `next_experiment_selector.py` into the selector requirement evidence as `Selector required gates: ...`. This makes launch audits preserve the exact resume gates for external-only T-251 and for any future local selector candidate directly, instead of requiring readers to open the separate selector artifact. Verification passed focused launch audit pytest `21/21`, broader auto-research launch/completion/selector pytest `34/34`, ruff, format check, `py_compile`, `git diff --check`, runtime selector required-gate evidence, A/B `adopt_candidate` (`score_delta=0.9881422924901185`), T-1303 completion audit `complete` (`3/3`), code-review gate exit `0` with advisory WARN (`risk_score=0.4`) from graph heuristics covered by direct tests, and required Actions on `dc0d11e8` (`root-quality-gate` `26997046879`, `active-project-matrix` `26997046881`). T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1302 launch audit selector guardrail evidence. `launch_objective_audit.py` now sentence-normalizes selector action evidence so action strings ending in punctuation do not render with duplicate terminal punctuation, and carries `selected.guardrails` from `next_experiment_selector.py` into the selector requirement evidence. This makes launch audits preserve the external T-251 retry boundary directly, instead of requiring readers to open the separate selector artifact. Verification passed focused launch audit pytest `21/21`, broader auto-research launch/completion/selector pytest `34/34`, ruff, format check, `py_compile`, `git diff --check`, runtime launch audit guardrail evidence, A/B `adopt_candidate` (`score_delta=0.9849618229539489`), T-1302 completion audit `complete` (`3/3`), code-review gate exit `0` with advisory WARN (`risk_score=0.4`) from graph heuristics covered by direct tests, and required Actions on `ae9d9e59`. T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1301 launch audit selector-state evidence. `launch_objective_audit.py` now collects live `next_experiment_selector.py --root . --json` output during CLI runs and adds a selector requirement item to the launch manifest. Clean `blocked_external_only` selector state is recorded as complete evidence without duplicating the known user-owned T-251 blocker, while local selector candidates such as `local_readiness_blocker` become explicit completion blockers with status/kind/project evidence. Verification passed focused launch audit pytest `20/20`, broader auto-research launch/completion/selector pytest `33/33`, ruff, format check, `py_compile`, `git diff --check`, runtime launch audit selector evidence, A/B `adopt_candidate` (`score_delta=0.9910837857979347`), T-1301 completion audit `complete` (`4/4`), code-review gate exit `0` with advisory WARN (`risk_score=0.35`) from graph test-gap heuristics covered by direct tests, and required Actions on `43bd0f8e`. T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1300 deterministic launch-audit A/B manifest evidence. `launch_objective_audit.py` no longer relies only on `.tmp/ab-manifest-*.json` mtime when task identity is present; it extracts T-IDs from filenames/manifest fields, ranks task-id-bearing artifacts by highest T-ID, falls back to mtime/name only when no task ID is available, and accepts `--ab-manifest` to pin final launch evidence. Regression coverage proves `ab-manifest-t1300.json` wins over a newer-touched `ab-manifest-t1299.json`, while explicit override can still select a requested manifest. Verification passed focused launch audit pytest `18/18`, broader auto-research launch/completion/selector pytest `31/31`, ruff, format check, `py_compile`, `git diff --check`, code-review gate `risk_score=0.0`, A/B `adopt_candidate` (`score_delta=1.2387226869238648`), T-1300 completion audit `complete` (`4/4`), runtime launch audit citing T-1300 metrics `5` and gates `8/8`, and required Actions on `b59e3006`. T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1299 CI-aware stale HANDOFF closeout-priority detection. Follow-up to T-1298: `session_orient.py` now marks post-push closeout `Next Priorities` stale only when git is clean/synced **and** the current HEAD required release checks (`root-quality-gate`, `active-project-matrix`) are both completed successfully. This prevents a clean/synced but still-CI-pending context head from hiding actionable final live-check work. Added `_required_release_checks_green()` current-head matching and regressions for green current-head checks, in-progress current-head checks, and clean/synced closeout priorities that must remain actionable until release checks are green. Verification passed focused session-orient pytest `28/28`, broader session/auto-research audit pytest `57/57`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.8818502202643175`), T-1299 completion audit `complete` (`4/4`), and required Actions on `0bac5859` (`root-quality-gate` `26994295341`, `active-project-matrix` `26994295347`). T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1298 stale HANDOFF closeout-priority detection. `session_orient.py` now extracts the newest `Next Priorities` row and reports `latest_next_priorities`, `latest_next_priority_status`, and `stale_next_priority_reason`. It marks post-push closeout instructions stale only when git is clean/synced, so continuation agents can see that a closeout has already been committed/pushed without suppressing active dirty-work closeout steps. Runtime proof after feature push caught the prior T-1297 closeout instruction as `latest_next_priority_status=stale`; this context closeout replaces that instruction with live-selector routing. Verification passed focused session-orient pytest `25/25`, broader auto-research selector/launch/session pytest `50/50`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.9451472980300019`), and required Actions on `d7719bbf` (`root-quality-gate` `26994128473`, `active-project-matrix` `26994128458`). T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1297 post-push release-gate confirmation. Feature commit `7984696c` is pushed to `origin/main`; required Actions passed on that feature head (`root-quality-gate` `26993403449`, `active-project-matrix` `26993403451`). The feature head includes skill-lint warning cleanup and next-selector input-evidence hardening. Expected final steady state after this context closeout is local launch gates green, selector `blocked_external_only`, and only external/user-owned T-251 unresolved.

- **2026-06-05 (Codex)**: T-1297 skill evidence lint cleanup and selector input-evidence guard. `skill_lint.py` now treats generated `input/` and `output/` examples as workflow artifacts, and accessibility/SEO/shorts TTS skill docs no longer point at unavailable local skills or stale test paths. Runtime skill lint is `pass`, score `100`, warning_count `0`. `next_experiment_selector.py` now turns missing, unreadable, invalid, non-object, or schema-incomplete readiness/GitHub/browser/dependency inputs into an explicit `input_evidence_unavailable` candidate with regeneration gates, preventing missing evidence from falling through to completion audit; UTF-16 JSON input artifacts are accepted for Windows resilience. Verification passed focused selector+skill-lint pytest `18/18`, broader auto-research/GitHub/skill-lint pytest `75/75`, ruff, format check, `py_compile`, A/B `adopt_candidate` (`score_delta=1.154267543156432`), and T-1297 completion audit `complete` (`4/4`). Continue to keep T-251 separate as an external/user-owned blocker.

- **2026-06-05 (Codex)**: T-1296 post-push release-gate confirmation. Feature commit `4a1d1dbc` is pushed to `origin/main`; required Actions passed on that feature head (`root-quality-gate` `26992731140`, `active-project-matrix` `26992731162`). Final clean checks confirm `session_orient.py --json` clean/synced state, open PRs `0`, `head_claim_status=none`, product readiness score `96`, workspace/local/agent blockers `0/0/0`, selector `blocked_external_only` for external/user-owned T-251, and launch-objective completion audit `7/8` complete with only T-251 blocked. Continue to avoid retrying T-251 until Supabase Dashboard credentials are reset.

- **2026-06-05 (Codex)**: T-1296 next-experiment selector and blocker-detail audit. `.agents/skills/auto-research/scripts/next_experiment_selector.py` now deterministically ranks local readiness blockers, GitHub follow-ups, browser QA refreshes, dependency candidates, stale QC refreshes, and external/user-owned blockers before a new auto-research cycle is chosen. Regression coverage locks the clean external-only T-251 state to `blocked_external_only` with the guardrail not to retry until Supabase Dashboard credentials are reset. Runtime selector during the candidate dirty worktree correctly selected `local_readiness_blocker` while preserving T-251 as a blocked ranked candidate. `SKILL.md` documents the selector, `launch_objective_audit.py` requires it as the 8th auto-research artifact, `completion_audit.py` blocked issues now include requirement/blocker details, and root CI workspace-quality includes the selector script/test. Verification passed selector pytest `6/6`, completion audit pytest `4/4`, launch audit pytest `16/16`, broader auto-research/GitHub pytest `63/63`, ruff, format check, `py_compile`, runtime selector, runtime launch audit, A/B `adopt_candidate` (`score_delta=1.468470559113834`), and T-1296 completion audit `complete` (`4/4`).

- **2026-06-05 (Codex)**: T-1295 post-push release-gate confirmation. Feature commit `2c5ad69e` is pushed to `origin/main`; required Actions passed on that feature head (`root-quality-gate` `26992093689`, `active-project-matrix` `26992093691`). Live closeout checks confirmed active product-launch GOAL routing, browser QA fresh valid usable nonblank screenshot coverage `4/4`, A/B `adopt_candidate` (`score_delta=0.9822995235306066`), and T-1295 completion audit `complete` (`3/3`). Continue from live `session_orient.py --json` for exact current head after context commits; T-251 remains external/user-owned and should not be retried until Supabase credentials are reset.

- **2026-06-05 (Codex)**: T-1295 launch evidence routing and screenshot validity. `.ai/GOAL.md` now reflects the active product-launch auto-research loop instead of the stale completed Hanwoo quality goal, and `launch_objective_audit.py` now requires GOAL existence, active launch-loop status, and product-launch/auto-research objective mapping before the resumability item is complete. `browser_qa_inventory.py` now parses retained PNG screenshots for validity, dimensions, and nonblank pixels, reports valid/usable/nonblank summary counts, and recommends refreshing invalid or blank retained screenshots. `launch_objective_audit.py` now gates browser QA completion on fresh usable and fresh nonblank screenshot coverage. Runtime browser inventory reports browser coverage `4/4`, fresh valid usable nonblank screenshots `4/4`, and stale `0`; runtime launch audit reports GOAL mapped true, fresh usable/nonblank `4/4`, and latest A/B `.tmp/ab-manifest-t1295.json` with candidate metrics `10` and required gates `6/6`. Verification passed launch audit pytest `16/16`, browser inventory pytest `13/13`, broader auto-research/GitHub pytest `57/57`, ruff, format check, `py_compile`, runtime browser inventory, runtime launch audit, A/B `adopt_candidate` (`score_delta=0.9822995235306066`), T-1295 completion audit `complete` (`3/3`), and `git diff --check` with CRLF warnings only.

- **2026-06-05 (Codex)**: T-1294 post-push release-gate confirmation. Feature commit `680c5224` and context count correction `b898ded2` were pushed; live closeout checks reported clean/synced `main`, open PRs `0`, `head_claim_status=none`, and required workflows green for the checked state (`root-quality-gate` `26991455712`, `active-project-matrix` `26991455721`). Final `product_readiness_score.py --json` remains score `96` with workspace/local/agent blockers `0/0/0` and external blocker count `1`. Final launch-objective completion audit on `.tmp/launch-objective-audit-t1294-final.json` is `7/8` complete with `issue_count=1`, `blocked_count=1`, and only external/user-owned Hanwoo T-251 unresolved. Continue to use `session_orient.py --json` for exact live head/CI state and do not retry T-251 until the Supabase Dashboard password/control-plane credential reset is done.

- **2026-06-05 (Codex)**: T-1294 dependency peer metadata evidence. `dependency_freshness_inventory.py` now checks latest npm `version` plus `peerDependencies` metadata for installed peer blockers on deferred major migrations, annotates each blocker with latest peer support fields, and summarizes latest-supported/blocked/unavailable counts. `launch_objective_audit.py` carries those counts into dependency evidence, and dependency recommendations now show whether blockers already have upstream support or still block the target major. Current runtime inventory reports direct dependency candidates `0`, outdated count `3`, deferred dependencies `2`, latest peer support `1`, latest blocked `6`, unavailable `0`: Hanwoo ESLint 10 has `1/4` latest-supported blockers, while Knowledge ESLint 10 remains `0/3`. Verification passed dependency freshness pytest `11/11`, launch-objective pytest `14/14`, broader auto-research pytest `53/53`, ruff, format check, `py_compile`, runtime dependency inventory, launch-objective audit generation, A/B `adopt_candidate` (`score_delta=0.9919931369745496`), and expected pre-commit launch completion audit incomplete only from dirty candidate worktree plus external/user-owned T-251.

- **2026-06-05 (Codex)**: T-1293 post-push release-gate confirmation. Feature commit `dd890834` and context closeout `0746521d` are pushed/synced; `session_orient.py --json` reports clean/synced worktree, open PRs `0`, `head_claim_status=none`, no stale latest HANDOFF head claims, and required Actions green for the pushed context state (`root-quality-gate` `26990896929`, `active-project-matrix` `26990896922`). `product_readiness_score.py --json` remains score `96` with workspace/local/agent blockers `0/0/0` and external blocker count `1`. Fresh inventories report GitHub dirty count `0`, browser QA coverage/fresh screenshots `4/4`, direct dependency candidates `0`, and deferred ESLint peer blockers only. Launch-objective audit remains `7/8` complete with only external/user-owned Hanwoo T-251 unresolved; do not retry T-251 until the Supabase Dashboard password/control-plane credential reset is done.

- **2026-06-05 (Codex)**: T-1293 GitHub inventory dirty-state confirmation hardening. `github_project_inventory.py` now refreshes the Git index before status collection, keeps raw `status_dirty_count`, confirms tracked dirty status with `git diff --quiet` plus `git diff --cached --quiet`, preserves untracked files as real dirty work, and exposes `dirty_paths` plus `dirty_confirmation`. This prevents stale tracked status from becoming a false launch-audit workspace blocker while keeping real candidate edits visible. Verification passed focused GitHub inventory pytest `12/12`, broader auto-research pytest `51/51`, ruff, format check, `py_compile`, runtime GitHub inventory generation, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.9966832504145935`), and completion audit `complete` (`3/3`).

- **2026-06-05 (Codex)**: T-1292 post-push release-gate confirmation. The corrected T-1292 evidence is pushed, local state is clean/synced, open PRs are `0`, and the newest HANDOFF addendum has `head_claim_status=none` with no stale latest head claims. Required GitHub Actions passed for the pushed context state checked in this closeout: `root-quality-gate` `26990435906` and `active-project-matrix` `26990435930`. `product_readiness_score.py --json` remains score `96` with workspace/local/agent blockers `0/0/0` and external blocker count `1`. Launch-objective completion audit on `.tmp/launch-objective-audit-t1292-final.json` is `7/8` complete with `issue_count=1`, `blocked_count=1`, and only external/user-owned Hanwoo T-251 unresolved.
- **2026-06-05 (Codex)**: T-1292 relay head-claim detection and inventory artifact persistence. `session_orient.py` now inspects only the newest HANDOFF addendum for explicit live-head claims, compares them with live git evidence, and reports `latest_head_claims`, `stale_head_claims`, `current_head`, and `head_claim_status` in JSON plus a text advisory. This caught the T-1291 closeout wording that became stale after a context commit, so future tools should trust `session_orient.py --json` for exact head state instead of prose. `github_project_inventory.py` and `browser_qa_inventory.py` now accept `--output` for retained ASCII-safe JSON artifacts, and `auto-research/SKILL.md` documents `.tmp/github-project-inventory.json` and `.tmp/browser-qa-inventory.json`. Verification passed focused pytest `42/42`, ruff, format check, `py_compile`, runtime inventory output generation, runtime stale-claim detection, A/B `adopt_candidate` (`score_delta=1.1248447204968943`), and T-1292 completion audit `complete` (`3/3`).

- **2026-06-05 (Codex)**: T-1291 post-push release-gate confirmation. Current `main`/`origin/main` head is `6fbc74ae` (`fix(auto-research): T-1291 clarify dependency channel evidence`) and required GitHub Actions passed on that head: `root-quality-gate` `26989894129` and `active-project-matrix` `26989894138`. Final `session_orient.py --json` reports clean/synced worktree, open PRs `0`, HANDOFF `archivable_addendum_count=0`, and `rotation_suggested=false`. Final `product_readiness_score.py --json` reports score `96` with workspace/local/agent blockers `0/0/0` and only external/user-owned Hanwoo T-251 remaining. Launch-objective audit `.tmp/launch-objective-audit-t1291-head.json` is `7/8` complete and blocked only by T-251; dependency evidence now cleanly distinguishes direct candidates `0`, outdated count `3`, peer-blocked ESLint majors `2`, and Hanwoo `next-auth` current on beta.

- **2026-06-05 (Codex)**: T-1291 launch audit dependency channel evidence clarity. `launch_objective_audit.py` now includes `outdated_dependency_count` in dependency freshness evidence and reports `current_prerelease_channel` packages with channel/current/stable-latest versions. This explains the current dependency inventory shape: direct candidates `0`, outdated count `3`, deferred peer-blocked ESLint majors `2`, and Hanwoo `next-auth` current on beta `5.0.0-beta.31` while npm stable latest remains `4.24.14`. Regression coverage locks this evidence without turning current prerelease packages into blockers. Verification passed focused launch audit pytest `14/14`, broader launch/completion audit pytest `18/18`, ruff, format check, `py_compile`, `git diff --check`, code-review gate, runtime launch audit generation, A/B `adopt_candidate` (`score_delta=0.705308775731311`), and completion audit `complete` (`3/3`).

- **2026-06-05 (Codex)**: T-1290 launch audit unreadable/BOM A/B manifest resilience. `launch_objective_audit.py` now uses `_ab_manifest_sort_key()` to guard A/B manifest `stat()` access, ignores unreadable local `.tmp/ab-manifest-*.json` artifacts with explicit evidence instead of letting transient filesystem issues abort the launch audit, and reads local manifest JSON with `utf-8-sig`. `ab_decision.py` also reads manifests with `utf-8-sig`, fixing the observed Windows/PowerShell BOM JSON failure. Regression coverage locks stat-failure selection, BOM launch-audit evidence, and BOM A/B decision parsing. Verification passed focused A/B/launch/completion audit pytest `18/18`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.847887323943662`), completion audit `complete` (`3/3`), and runtime launch-audit generation with BOM-written `.tmp/ab-manifest-t1290.json` cited in A/B evidence with candidate metrics `5` and required gates `4/4`. Feature commit `f3391ffe` is pushed; required Actions passed (`root-quality-gate` `26989496985`, `active-project-matrix` `26989497031`).

- **2026-06-05 (Codex)**: T-1289 launch audit A/B manifest evidence hardening. `launch_objective_audit.py` now validates the newest valid local `.tmp/ab-manifest-*.json` as concrete A/B evidence instead of relying only on `.ai` relay prose. The A/B item includes the manifest artifact, records candidate metric count and required-gate pass count, and keeps coverage partial when metrics are missing or required gates fail. Regression coverage locks local A/B manifest inclusion and failed-gate blocking. Verification passed focused launch audit pytest `11/11`, broader launch/completion audit pytest `15/15`, ruff, format check, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.6666666666666666`), T-1289 completion audit `complete` (`4/4`), and runtime candidate launch audit generation with `.tmp/ab-manifest-t1289.json` cited in A/B evidence with required gates `7/7`.

- **2026-06-05 (Codex)**: T-1288 post-push closeout. Feature commit `9025f5fc` (`fix(auto-research): T-1288 clarify external blocker coverage`) is pushed to `origin/main`; required GitHub Actions passed on that head (`root-quality-gate` `26988553084`, `active-project-matrix` `26988553088`). Final `session_orient.py --json` reports clean/synced `main`, open PRs `0`, HANDOFF `archivable_addendum_count=0`, and `rotation_suggested=false`. Final `product_readiness_score.py --json` reports score `96` with workspace/local/agent blockers `0/0/0` and only external/user-owned Hanwoo T-251 remaining. Launch-objective completion audit on `.tmp/launch-objective-audit-t1288-feature-head.json` is `7/8` complete with `issue_count=1`, `blocked_count=1`, and only T-251 unresolved; known external blocker evidence is now complete coverage and fails only on `blocked`.

- **2026-06-05 (Codex)**: T-1288 launch audit external blocker coverage precision. `launch_objective_audit.py` now treats known user-owned external blockers as fully mapped evidence (`coverage: complete`) while still preserving the blocker, so the T-251 launch audit failure is only `blocked` instead of `blocked` plus generic `incomplete_coverage`. Unknown external blockers remain `coverage: partial` with `unknown` blocker text, and user-owner matching is case-insensitive. Verification passed focused launch/completion audit pytest `13/13`, ruff, format check, `py_compile`, runtime candidate launch audit generation, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.6071428571428571`), and completion audit `complete` (`4/4`).

- **2026-06-05 (Codex)**: T-1287 post-push release-gate confirmation. Commit `31faa96e` is on `origin/main`; required GitHub Actions passed on that head (`root-quality-gate` 26988113562, `active-project-matrix` 26988113556). Final orientation is clean/synced with open PRs `0`, HANDOFF `archivable_addendum_count=0`, and `rotation_suggested=false`. Product readiness remains score `96` with workspace/local/agent blockers `0/0/0`, external blocker count `1`, and first next action `Wait for 1 user-owned task blocker(s) before rerunning local launch checks: T-251.` Launch-objective completion audit on `.tmp/launch-objective-audit-t1287-final-head.json` remains `7/8` complete and incomplete only for external/user-owned T-251.

- **2026-06-05 (Codex)**: T-1287 mixed-owner readiness regression coverage. Follow-up on pushed T-1286 head `04d13af4`: `workspace/tests/test_product_readiness_score.py` now covers the mixed blocker path where a project has both agent-owned and user-owned active tasks. Product readiness is locked to recommend resolving the agent-owned blocker first, then waiting on user-owned T-251. Shared T-1286 evidence was updated to focused pytest `25/25`, A/B `adopt_candidate` (`score_delta=0.9090909090909091`), and completion audit `complete` (`5/5`). Verification passed focused pytest, ruff, format check, `py_compile`, runtime readiness JSON, `git diff --check`, and staged code-review gate exit `0` with advisory WARN (`risk_score=0.35`) from graph helper-gap heuristics covered by the focused tests.

- **2026-06-05 (Codex)**: T-1286 owner-aware readiness blocker routing. `execution/product_readiness_score.py` now distinguishes user-owned task blockers from local/agent work in project recommendations. Current Hanwoo T-251 now appears as `Wait for 1 user-owned task blocker(s) before rerunning local launch checks: T-251.` in both the project recommendation and `next_actions`, instead of generic local resolution wording. Mixed user-owned and agent-owned project blockers keep agent-owned work first, then wait on user-owned blockers. This preserves the Supabase credential reset boundary and avoids auto-research retry loops while keeping readiness score/blocker counts stable. Verification passed focused product-readiness pytest `25/25`, ruff, format check, `py_compile`, runtime readiness JSON, `git diff --check`, A/B `adopt_candidate` (`score_delta=0.9090909090909091`), and completion audit `complete` (`5/5`).

- **2026-06-05 (Codex)**: T-1285 post-push release-gate confirmation. Feature commit `491b7782` and context head `7d5f633b` are pushed to `origin/main`; required GitHub Actions passed on `7d5f633b` (`root-quality-gate` 26987547889, `active-project-matrix` 26987547880). Final orientation is clean/synced with open PRs `0`, HANDOFF `archivable_addendum_count=0`, and `rotation_suggested=false`. Product readiness remains score `96` with workspace/local/agent blockers `0/0/0`; launch-objective audit remains `7/8` complete and incomplete only for external/user-owned Hanwoo T-251.

- **2026-06-05 (Codex)**: T-1285 session orientation HANDOFF rotation signal alignment. `execution/session_orient.py` now mirrors the deterministic `execution/handoff_rotator.py` seven-day cutoff instead of suggesting rotation only because `HANDOFF.md` is long. The handoff snapshot now exposes `rotation_cutoff` and `archivable_addendum_count`; current runtime output reports `line_count=593`, `current_addendum_count=83`, `oldest_age_days=1`, `rotation_cutoff=2026-05-29`, `archivable_addendum_count=0`, and `rotation_suggested=false`, matching `handoff_rotator.py --check --json` noop (`kept=83`, `archived=0`). Regression coverage keeps stale addenda actionable while preventing long recent HANDOFF false positives. Verification passed focused pytest `31/31`, ruff, format check, `py_compile`, runtime session-orient/rotator checks, `git diff --check`, A/B `adopt_candidate` (`score_delta=1.0`), and completion audit `complete` (`4/4`).

- **2026-06-05 (Codex)**: T-1284 auto-research JSON ASCII evidence hardening. Auto-research JSON evidence files now stay parseable when Windows PowerShell reads them with default `Get-Content` under `C:\Users\박주호`: all six JSON emitters (`ab_decision.py`, `browser_qa_inventory.py`, `completion_audit.py`, `dependency_freshness_inventory.py`, `github_project_inventory.py`, `launch_objective_audit.py`) use ASCII-escaped JSON output. Added a Korean-path launch-audit CLI regression and documented the rule in `auto-research/SKILL.md`. Verification passed focused launch-audit pytest `7/7`, broader auto-research/GitHub pytest `38/38`, ruff, format check, `py_compile`, skill lint score `100`, runtime default-`Get-Content` parse checks for launch/dependency JSON, `git diff --check`, A/B `adopt_candidate` (`score_delta=3.0604133545310015`), and completion audit `complete` (`3/3`). No app package manifests changed; T-251 and Hanwoo/Knowledge ESLint 10 peer blockers remain separate.

- **2026-06-05 (Codex)**: T-1283 post-push release-gate confirmation. Feature commit `37a68afe` is pushed to `origin/main`; required GitHub Actions passed on that head (`root-quality-gate` `26986800302`, `active-project-matrix` `26986800326`). Clean post-push readiness is score `96` with workspace/local/agent blockers `0/0/0`, external blocker count `1`, and only user-owned Hanwoo T-251 remaining. Final launch objective audit `.tmp/launch-objective-audit-t1283-actions-green.json` is complete on `7/8` requirements and incomplete only for T-251. Current direct patch/minor dependency candidates remain `0`; Hanwoo/Knowledge ESLint 10 are upstream-peer-blocked; browser QA retained screenshot evidence is `4/4` fresh and `0` stale.

- **2026-06-05 (Codex)**: T-1283 launch evidence routing clarity. `dependency_freshness_inventory.py` now routes fully peer-blocked deferred major migrations to an upstream-peer-support wait recommendation instead of generic major-migration retry work. Current runtime `.tmp/dependency-freshness-t1283-candidate.json` still reports direct dependency candidates `0` and deferred dependencies `2`, but recommends waiting for upstream peer support before Hanwoo ESLint 10 (`4` peer blockers) and Knowledge ESLint 10 (`3` peer blockers). `browser_qa_inventory.py` now records retained screenshot `modified_at`, `age_days`, fresh/stale counts, and stale refresh recommendations; current runtime browser QA inventory reports coverage `4/4`, fresh screenshots `4/4`, stale `0`. `launch_objective_audit.py` now reports peer-blocked deferred dependency evidence plus fresh screenshot coverage. Auto-research pytest passed `37/37`; ruff, format check, `py_compile`, runtime inventories, skill lint score `100`, `git diff --check`, A/B `adopt_candidate` (`score_delta=1.4835309617918315`), and completion audit `complete` (`5/5`) passed. Do not force Hanwoo/Knowledge ESLint 10 until upstream lint plugin peer ranges include ESLint 10, and do not retry T-251 until the Supabase Dashboard credential reset is done.

- **2026-06-05 (Codex)**: T-1282 dependency freshness peer blocker evidence. Baseline `.tmp/dependency-freshness-t1282-baseline.json` still reported direct candidates `0`, deferred dependencies `2`, and generic ESLint 10 major deferrals for Hanwoo and Knowledge. The dependency freshness helper now inspects each project's `package-lock.json` peer ranges for deferred major migrations and annotates blocker evidence without modifying dashboard package manifests. Runtime `.tmp/dependency-freshness-t1282-candidate-rerun.json` reports Hanwoo ESLint 10 `peer_blocker_count=4` (`eslint-plugin-import@2.32.0`, `eslint-plugin-jsx-a11y@6.10.2`, `eslint-plugin-react@7.37.5`, `eslint-plugin-react-hooks@7.0.1`) and Knowledge ESLint 10 `peer_blocker_count=3` under `eslint-config-next`. Focused dependency freshness pytest passed `9/9`; broader auto-research/GitHub inventory pytest passed `33/33`; ruff, format check, `py_compile`, runtime inventory, and `git diff --check` passed. A/B `.tmp/ab-manifest-t1282.json` returned `adopt_candidate` (`score_delta=6.222222222222222`), and completion audit `.tmp/completion-audit-t1282.json` was complete (`4/4`). Do not force ESLint 10 until upstream plugin peer support changes.

- **2026-06-05 (Codex)**: T-1281 post-push closeout. Feature commit `97c62c64` is on `origin/main`; required GitHub Actions passed on that head (`root-quality-gate` 26985740619, `active-project-matrix` 26985740597). Final checks show a clean/synced worktree, product readiness score `96`, workspace/local/agent blockers `0/0/0`, and only external/user-owned Hanwoo T-251 remaining. `.tmp/dependency-freshness-t1281-final.json` reports direct patch/minor candidates `0`, deferred dependencies `2`, and Hanwoo `next-auth` as `current_prerelease_channel` on npm `beta`; `.tmp/launch-objective-audit-t1281-final.json` is complete on `7/8` launch requirements and incomplete only for T-251.

- **2026-06-05 (Codex)**: T-1281 dependency inventory prerelease dist-tag context. `dependency_freshness_inventory.py` now checks npm dist-tags for prerelease/stable channel mismatches and reclassifies packages already current on their matching channel as `current_prerelease_channel`. Live metadata shows `next-auth` `beta=5.0.0-beta.31` while `latest=4.24.14`, so Hanwoo `next-auth@5.0.0-beta.31` is no longer a false deferred blocker; current inventory deferred dependency count is `2` and the remaining items are Hanwoo ESLint 10 and Knowledge ESLint 10 major migrations. Knowledge ESLint 10 was tested and rejected because current Next transitive plugins still peer only through ESLint 9 and `npm.cmd run lint` fails under ESLint 10 with `react/display-name` `TypeError: contextOrFilename.getFilename is not a function`; Knowledge was restored to valid `eslint@9.39.4`. Verification passed focused pytest `8/8`, broader auto-research/GitHub inventory pytest `32/32`, ruff, format check, `py_compile`, runtime inventory, valid Knowledge `npm ls`, Knowledge lint, audit high/critical `0`, skill lint no-error status, diff-check, completion audit `complete` (`4/4`), and A/B `adopt_candidate` (`score_delta=1.2549019607843137`). Feature commit `97c62c64` is pushed and required GitHub Actions passed (`root-quality-gate` 26985740619, `active-project-matrix` 26985740597); clean post-push readiness remains score `96` with workspace/local/agent blockers `0/0/0` and only external/user-owned T-251 blocked.

- **2026-06-05 (Codex)**: T-1280 auto-research launch audit evidence correction. `launch_objective_audit.py` now reports `SKILL.md` documentation for the launch objective audit command, and `workspace/tests/test_auto_research_launch_objective_audit.py` locks that evidence string. T-1279 relay records were corrected to remove an unsupported Knowledge Dashboard ESLint 10 recheck claim, use A/B `score_delta=5.25`, and keep the launch-objective completion audit incomplete while dirty work or external/user-owned T-251 remains. Verification passed launch-audit pytest `4/4`, broader auto-research/GitHub inventory pytest `30/30`, ruff, format check, `py_compile`, runtime manifest generation with `7/7` skill artifacts, expected incomplete completion audit before clean commit, A/B `adopt_candidate` (`score_delta=2.0`), and `git diff --check` with CRLF warnings only.

- **2026-06-05 (Codex)**: T-1279 auto-research launch objective audit helper. `.agents/skills/auto-research/scripts/launch_objective_audit.py` now builds a current-evidence completion manifest for the long-running product-launch objective, covering auto-research artifacts, GitHub inventory, browser-click QA, dependency freshness, readiness, external blockers, A/B evidence, and `.ai` relay continuity. `SKILL.md` documents the command and both required workflows include the helper/test. Verification passed launch-audit pytest `4/4`, broader auto-research/GitHub inventory pytest `30/30`, ruff, `py_compile`, runtime manifest generation to `.tmp/launch-objective-audit-t1279.json` / `.tmp/launch-objective-audit-t1280.json`, and A/B `adopt_candidate` (`score_delta=5.25`). The generated launch-objective completion audit intentionally stays incomplete while dirty local work or external/user-owned T-251 remains, preventing false launch-complete claims. No Knowledge Dashboard ESLint 10 recheck was run in this T-1279 cycle; Knowledge ESLint 10 remains a separate deferred migration.

- **2026-06-05 (Codex)**: T-1278 post-push closeout. Commit `7b649d9f` is on `origin/main`; required GitHub Actions passed on that head (`root-quality-gate` 26983546992, `active-project-matrix` 26983546977). Current verified steady state is readiness score `96`, clean worktree, open PRs `0`, workspace/local blockers `0`, direct patch/minor dependency candidates `0`, and only external/user-owned Hanwoo T-251 blocked. Remaining deferred dependency items are Hanwoo ESLint 10, Hanwoo next-auth beta/stable channel mismatch, and Knowledge ESLint 10.

- **2026-06-05 (Codex)**: T-1278 Hanwoo Dashboard TypeScript 6 migration. `projects/hanwoo-dashboard` now declares `typescript` `^6.0.3`, with `projects/hanwoo-dashboard/package-lock.json` and root `pnpm-lock.yaml` synchronized. Current npm metadata reports `typescript@6.0.3` with Node `>=14.17`; `typescript-eslint@8.60.1` peers on `typescript >=4.8.4 <6.1.0`; `eslint-config-next@16.2.7` accepts ESLint 9 and TypeScript; Prisma/Serwist peers accept TypeScript 6. Hanwoo has no `tsconfig.json` and no tracked `.ts/.tsx` source files, so the migration risk stayed package/build/lint focused. Verification passed package listing, Hanwoo tests `499/499`, lint, offline Prisma 7 test (`14` passed, live skipped), build, smoke, Hanwoo project QC, frozen root pnpm lockfile check, `npm.cmd outdated typescript --json` `{}`, audit high/critical `0`, dependency inventory `.tmp/dependency-freshness-t1278-candidate.json` with total deferred count `4 -> 3`, direct Node Playwright browser-click QA screenshot `output/playwright/hanwoo-t1278-typescript6-browser-qa.png` with manifest/API health `200` and no console/page/request/status issues, A/B `adopt_candidate` (`score_delta=0.4375`), and completion audit `complete` (`7/7`). ESLint 10, next-auth channel changes, and live T-251 Supabase verification remain separate.

- **2026-06-05 (Codex)**: T-1277 full active-project QC evidence refresh. After T-1276, the latest `.tmp/project_qc_runner_latest.json` contained only a Knowledge Dashboard single-project run, so readiness temporarily showed blind-to-x, shorts-maker-v2, and Hanwoo QC as unavailable. `python execution/project_qc_runner.py --json` regenerated the full active-project artifact and passed: blind-to-x tests `1723` passed / `9` skipped plus ruff, shorts-maker-v2 tests `1577` passed / `12` skipped plus ruff, Hanwoo tests `499/499` plus lint/build/smoke, and Knowledge tests `61/61` plus lint/build/smoke. `python execution/product_readiness_score.py --json` returned score `96` with blind-to-x, shorts-maker-v2, and Knowledge ready, Hanwoo blocked only by user-owned T-251, and no project-QC refresh recommendations. Dependency freshness inventory `.tmp/dependency-freshness-t1277.json` still reports zero direct patch/minor candidates and four deferred major/channel items.

- **2026-06-05 (Codex)**: T-1276 Knowledge Dashboard TypeScript 6 migration. `projects/knowledge-dashboard` now declares `typescript` `^6.0.3`, with `projects/knowledge-dashboard/package-lock.json` and root `pnpm-lock.yaml` synchronized. Current npm metadata reports `typescript@6.0.3` with Node `>=14.17`; `typescript-eslint@8.60.1` peers on `typescript >=4.8.4 <6.1.0`; local `npm.cmd ls typescript eslint eslint-config-next typescript-eslint @typescript-eslint/parser --depth=2` reports `typescript@6.0.3`, `eslint@9.39.4`, `eslint-config-next@16.2.7`, `typescript-eslint@8.60.1`, and `@typescript-eslint/parser@8.60.1`. Verification passed Knowledge `npm.cmd test` (`61/61`), `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run smoke`, project QC for Knowledge, frozen root pnpm lockfile check, `npm.cmd outdated typescript --json` `{}`, browser-click QA screenshot `output/playwright/knowledge-t1276-typescript6-browser-qa.png` with console errors/warnings `0`, dependency inventory `.tmp/dependency-freshness-t1276-candidate.json` with total deferred dependency count `5 -> 4`, A/B `adopt_candidate` (`score_delta=0.5625`), and completion audit `complete` (`7/7`); post-push required GitHub Actions also passed on `0c42f201` (`root-quality-gate` 26981857496, `active-project-matrix` 26981857489). Knowledge ESLint 10 remains a separate deferred major migration.

- **2026-06-05 (Codex)**: T-1275 Word Chain plugin-react 6 migration. `projects/word-chain` now declares `@vitejs/plugin-react` `^6.0.2`, with `projects/word-chain/package-lock.json` and root `pnpm-lock.yaml` synchronized. Current official plugin-react changelog says v6 removes Babel-related features and drops Vite 7-and-below support, while 6.0.2 is type-only; Word Chain uses plain `react()` with no Babel options and already runs Vite `8.0.16`, matching npm metadata for 6.0.2. Verification passed `npm.cmd ls @vitejs/plugin-react vite --depth=0`, Word Chain `npm.cmd test` (`23/23`), `npm.cmd run lint`, `npm.cmd run build` through the known ASCII fallback after direct Vite `3221226505`, frozen lockfile-only pnpm, `npm.cmd audit --json` total `0`, `npm.cmd outdated @vitejs/plugin-react --json` `{}`, dependency freshness inventory with Word Chain `clean` and total deferred dependencies reduced `6 -> 5`, Playwright production browser-click QA with Korean input `사과`, console errors/warnings `0`, all requests `200`, screenshot `output/playwright/word-chain-t1275-plugin-react6-qa.png`, `git diff --check`, A/B `adopt_candidate`, and completion audit `complete`. Remaining deferred npm items are Hanwoo/Knowledge major/channel migrations only; Hanwoo T-251 remains external/user-owned.

- **2026-06-05 (Codex)**: T-1274 root Biome/Turbo patch freshness. Root `package.json` now declares `@biomejs/biome` `^2.4.16` and `turbo` `^2.9.16`, `pnpm-lock.yaml` is synchronized, and `biome.json` points to the Biome 2.4.16 schema. Verification passed root package list and Biome/Turbo version commands, lockfile-only frozen pnpm, root-filtered frozen pnpm, root `pnpm outdated --json` `{}`, dependency freshness inventory `.tmp/dependency-freshness-t1274.json` with root `clean` and direct patch/minor candidates `0`, workspace-quality pytest (`162/162`), workspace-quality ruff, Turbo Word Chain test (`23/23`) and lint, `git diff --check`, A/B `adopt_candidate`, and completion audit `complete`. Local full-workspace `pnpm install --frozen-lockfile --ignore-scripts` still exits `-1073740791` during Windows node_modules linking even though lockfile/root-filtered installs pass; use required Linux GitHub Actions as the full workspace install gate after push. Deferred major/channel migrations remain separate.

- **2026-06-05 (Codex)**: T-1273 auto-research dependency freshness inventory. `.agents/skills/auto-research/scripts/dependency_freshness_inventory.py` now deterministically discovers npm package projects, runs `npm outdated --json`, separates safe patch/minor candidates from `defer_major_migration` and `defer_channel_mismatch` items, and writes JSON artifacts with `--output`. `SKILL.md` documents the helper, and both required workspace-quality workflows include the helper plus `workspace/tests/test_auto_research_dependency_freshness_inventory.py`. Runtime inventory wrote `.tmp/dependency-freshness-t1273.json`, reporting root patch candidates `@biomejs/biome 2.4.15 -> 2.4.16` and `turbo 2.9.14 -> 2.9.16`; Hanwoo/Knowledge/Word Chain only show major or channel-deferred items. Verification passed focused pytest (`6/6`), combined auto-research pytest (`18/18`) with repo-local basetemp, ruff, A/B `adopt_candidate` (`score_delta=1.325`), and completion audit `complete` (`3/3`). Next dependency cycle should be T-1274 for the root patch candidates, not deferred major/channel migrations.

- **2026-06-05 (Codex)**: T-1272 Hanwoo Recharts 3 responsive chart hardening. All six Hanwoo `ResponsiveContainer` chart surfaces now include explicit `minWidth={0}`, `minHeight={0}`, and `initialDimension={{ width: 1, height: 1 }}` guards after browser QA exposed Recharts 3's initial `width(-1)/height(-1)` sizing warning without those guards. Updated source tests lock the guard contract for monthly/cost/financial, cattle weight, feed trend, and sales profit charts. Verification passed Hanwoo `npm.cmd test` (`499/499`), `npm.cmd run lint`, `npm.cmd run smoke`, `npm.cmd run build`, top-level Recharts/React-Is checks, direct outdated/audit review, browser QA `.tmp/hanwoo-t1272-browser-qa.json` with production login click QA plus Recharts 3 render harness console/page/network/server errors `0`, A/B `adopt_candidate` (`score_delta=3.090909090909091`), and completion audit `complete` (`5/5`). Existing audit findings remain moderate-only with unsuitable Next/Prisma downgrade fixes. Hanwoo T-251 remains separate as the only external/user-owned launch blocker.

- **2026-06-05 (Codex)**: T-1271 dashboard safe dependency freshness. Hanwoo now declares `bullmq` `^5.78.0`, `ioredis` `^5.11.1`, `react-hook-form` `^7.77.0`, and `zod` `^4.4.3`; Knowledge's existing `eslint` `^9` lock is refreshed to `9.39.4`; and npm/pnpm locks are synchronized. Verification passed root frozen pnpm lock, target `npm ls` checks, Hanwoo test/lint/build/smoke (`499` tests), Knowledge test/lint/build/smoke (`61` tests), selected outdated checks, audit review with no high/critical findings, `git diff --check`, staged code-review gate pass (`risk_score=0.0`), A/B `adopt_candidate` (`score_delta=4.0`), completion audit `complete`, feature commit `7383776b`, and GitHub Actions on that head passed (`root-quality-gate` 26978670413, `active-project-matrix` 26978670027). Remaining audit findings are moderate-only with unsuitable downgrade fixes; deferred major/incompatible migrations stay separate. Hanwoo T-251 remains the only external/user-owned launch blocker.

- **2026-06-05 (Codex)**: T-1270 frontend React/Next patch alignment. `projects/word-chain` now uses `react`/`react-dom` `^19.2.7` and `@types/react` `^19.2.16`; `projects/hanwoo-dashboard` now uses `next`/`eslint-config-next` `^16.2.7` plus `react`/`react-dom` `^19.2.7`; `projects/knowledge-dashboard` now uses `next`/`eslint-config-next` `16.2.7`. Root `pnpm-lock.yaml` and the three npm package locks were synchronized. Verification passed selected npm metadata/`npm ls`/`npm outdated` checks, root frozen pnpm lock, Word Chain tests `23/23`, lint, audit `0`, production build through the known ASCII-workspace fallback after direct Vite `3221226505`, Hanwoo/Knowledge project QC test/lint/build/smoke (`499` and `61` tests), direct browser-click QA for Word Chain submit/retry plus Hanwoo and Knowledge login interactions, A/B `adopt_candidate` (`score_delta=5.777777777777778`), and completion audit `complete`. Dashboard audit output still has existing moderate transitive advisories with unsuitable downgrade fixes; no broad audit fix was applied. Hanwoo T-251 remains separate as the only external/user-owned launch blocker.

- **2026-06-05 (Codex)**: T-1267 browser QA screenshot retention closeout. After T-1266 found Suika and Word Chain had verified browser logs but no current retained `output/playwright` screenshots, both apps were built for production and QA'd through `vite preview`. Direct Vite build still hit the known Korean-path `3221226505`, and both projects completed through their ASCII-workspace fallback. A temporary Playwright Node script using the npx Playwright package cache clicked Suika `Game Start`, sent ArrowLeft/ArrowRight/Space, verified nonblank canvas pixels, submitted Korean input `사과` in Word Chain, and saved `output/playwright/suika-t1267-browser-click-qa.png` plus `output/playwright/word-chain-t1267-browser-click-qa.png`. Console warnings/errors and failed requests were `0` for both apps; screenshot checks reported `1280x900` nonblank images. Browser QA inventory now reports `browser_project_count=4`, `covered_count=4`, `missing_count=0`, `current_screenshot_project_count=4`, and `recommendations=[]`. A/B selected `adopt_candidate` (`score_delta=1.25`) and T-1267 completion audit is `complete`. No app source changes were needed; Hanwoo T-251 remains separate as the only external/user-owned launch blocker.

- **2026-06-05 (Codex)**: T-1266 auto-research browser QA evidence inventory. `.agents/skills/auto-research/scripts/browser_qa_inventory.py` now deterministically discovers browser app package projects, matches verified direct browser/app-click QA evidence from `.ai` logs and current `output/playwright` screenshots, filters generic-token and mixed-task false positives, and reports covered/missing browser projects plus screenshot-retention recommendations. `SKILL.md` documents the helper, and root GitHub Actions workflow lists include the new helper/test. Runtime inventory reports browser QA coverage `4/4` covered and `0` missing; Hanwoo and Knowledge have current screenshots, while Suika and Word Chain are covered by verified logs but lack current `output/playwright` screenshots. Verification passed focused pytest (`8` passed), workflow-focused pytest (`14` passed), ruff, `py_compile`, auto-research skill lint score `100`/pass, diff-check, staged code-review gate `pass` (`risk_score=0.0`), A/B `adopt_candidate` (`score_delta=1.6666666666666667`), completion audit `complete`, pushed feature commit `94abd514` plus ai-context head `b0765352`, and GitHub Actions on `b0765352` passed (`root-quality-gate` 26974181977, `active-project-matrix` 26974182044). Current readiness reports score `96`, workspace/local blockers `0`, open PRs `0`, required Actions green, and only external/user-owned Hanwoo T-251 blocked.

- **2026-06-05 (Codex)**: T-1265 GitHub Actions checkout/submodule hygiene. Added `.gitmodules` for the tracked `nature-skills` gitlink, upgraded required workflow checkout steps to `actions/checkout@v6`, and set `persist-credentials: false` to keep checkout read-only. `workspace/tests/test_github_workflow_hygiene.py` locks the workflow/submodule contract and is wired into both required workflow lists. Verification passed submodule status/cleanup checks, focused pytest (`2` passed), root-quality equivalent (`140` passed), ruff, diff-check, staged code-review gate `pass`, A/B `adopt_candidate`, pushed `a94136b2`, and GitHub Actions on that head passed (`root-quality-gate` 26973845848, `active-project-matrix` 26973845884). Follow-up log greps found no repeated `No url found`, `fatal`, or git exit `128` checkout cleanup warnings.

- **2026-06-05 (Codex)**: T-1264 knowledge-dashboard local sync fallback and browser-click QA. `projects/knowledge-dashboard/scripts/sync_data.py` now falls back to `.agents/skills/auto-research/scripts/github_project_inventory.py` when GitHub credentials/API access are unavailable, mapping local workspace projects into GitHub-card-compatible rows with stable ids, local/tree URLs, inferred language, README/test/workflow metadata, and safe descriptions. `build_product_readiness()` now uses the current default project-QC readiness contract instead of forcing the legacy public QA/QC artifact. Regression coverage in `projects/knowledge-dashboard/tests/test_sync_data.py` covers remote URL normalization, local repo mapping, no-token fallback, and readiness invocation. Direct Playwright CLI QA clicked login, operations, knowledge search/clear, notebook modal, export menu, QA/QC, activity, refresh, and logout with console warnings/errors 0 and tracked auth/data requests 200; screenshot artifact `output/playwright/knowledge-t1264-browser-click-qa.png`. Verification passed focused pytest (`8` passed), ruff, `py_compile`, diff-check, Knowledge `npm.cmd test` (`61` passed), Knowledge project QC test/lint/build/smoke, full active-project QC (`3860` passed / `21` skipped), A/B `adopt_candidate` (`score_delta=4.666666666666667`), completion audit `complete`, and GitHub Actions on `7b656d3a` passed (`root-quality-gate` 26973233204, `active-project-matrix` 26973233352). Current readiness remains score `96` with workspace/local blockers `0` and only external/user-owned Hanwoo T-251 blocked.

- **2026-06-05 (Codex)**: T-1263 project-QC artifact schema contract. `execution/project_qc_runner.py` now writes `schema_version=2` into `.tmp/project_qc_runner_latest.json`, and `execution/product_readiness_score.py` imports the current runner contract before trusting `project_qc_runner` artifacts. Artifacts missing the current schema are downgraded to `PARTIAL` with `contract_mismatches=["artifact_schema_version"]`, preventing stale parser-era evidence from staying complete after runner/counting changes. Verification passed project-QC runner pytest (`12` passed), product-readiness pytest (`24` passed), combined focused pytest (`36` passed), ruff, ruff format check, `py_compile`, diff-check, full active-project QC (`3860` passed / `21` skipped; Hanwoo `499`, Knowledge `61` now reflected), A/B `adopt_candidate` (`score_delta=1.5105105105105106`), completion audit `complete`, and GitHub Actions on `3df8642c` passed (`root-quality-gate` 26971365112, `active-project-matrix` 26971365069). Current readiness remains score `96` with workspace/local blockers `0` and only external/user-owned Hanwoo T-251 blocked.

- **2026-06-05 (Codex)**: T-1262 GitHub inventory and QC test-evidence accuracy. `.agents/skills/auto-research/scripts/github_project_inventory.py` now detects colocated JS/TS/MJS test files and usable `package.json` test scripts, skips generated/dependency directories, handles disappearing directories during live scans, and avoids double-counting child project tests in the root summary. This fixed false `has_tests=false` inventory results for Hanwoo, Suika, and Word Chain. `execution/project_qc_runner.py` now parses Node test-runner summary lines with non-word prefixes so readiness artifacts retain real passed/skipped counts. Verification passed focused inventory pytest (`5` passed), project-QC runner pytest (`12` passed), live inventory, Hanwoo/Knowledge runner test checks, ruff, ruff format check, `py_compile`, diff-check, A/B `adopt_candidate` (`score_delta=3.4285714285714284`), and GitHub Actions on `fb0268eb` passed (`root-quality-gate` 26970050492, `active-project-matrix` 26970050500). Current local readiness still separates T-251 as the only external/user-owned blocker.

- **2026-06-05 (Codex)**: T-1261 project-QC contract drift guard. `execution/product_readiness_score.py` now revalidates stored `project_qc_runner` artifacts against the current `execution/project_qc_runner.py` expected check IDs, so older artifacts missing newly required checks such as web-app `smoke` are downgraded to `PARTIAL` instead of treated as complete launch evidence. Readiness refresh guidance now names `test/lint/build/smoke`. Verification passed focused readiness pytest (`23` passed), ruff, ruff format check, `py_compile`, diff-check, full active-project QC with smoke, A/B `adopt_candidate` (`score_delta=0.8939393939393939`), completion audit `complete`, and GitHub Actions on `c72e0ce8` passed (`root-quality-gate` 26969298890, `active-project-matrix` 26969298914). Current readiness remains score `96` with workspace blockers `0`, local blockers `0`, and only external/user-owned Hanwoo T-251 blocked.

- **2026-06-05 (Codex)**: T-1260 workspace release-readiness clarity and runtime smoke gates. `execution/product_readiness_score.py` now emits per-project `blocker_breakdown` and overall `external_blocker_count`, `local_blocker_count`, `agent_task_count`, `environment_blocker_count`, and nested `blocker_breakdown`. Treat `external_blocker_count` as user-owned/manual work in current launch loops: after the T-1260 commit lands, the expected steady state is local blockers `0` and external blockers `1` for Hanwoo T-251 until the Supabase Dashboard DB password/control-plane credential reset is done. `execution/project_qc_runner.py` now includes `smoke` checks for `hanwoo-dashboard` and `knowledge-dashboard`, and CLI `--check smoke` is supported. Verification passed readiness pytest (`22` passed), project QC runner pytest (`11` passed), direct T-251 ownership regressions (`2` passed), `py_compile`, ruff, diff-check, direct Hanwoo/Knowledge `npm.cmd run smoke`, runner smoke, and A/B `adopt_candidate` (`score_delta=7.25`). Code-review gate returned advisory WARN only for a stale graph helper-gap heuristic covered by direct tests.

- **2026-06-05 (Codex)**: T-1256..T-1259 workspace release-gate hardening. `execution/task_id_gate.py` is now wired into `.githooks/commit-msg` to block normal commits that reuse exact existing `T-####` IDs while allowing `[ai-context]` follow-ups, new suffixed fallback IDs, and no-edit amend commits whose subject exactly matches HEAD. `execution/product_readiness_score.py` now includes `workspace_gates` for clean worktree, open GitHub PRs, and required GitHub Actions (`root-quality-gate`, `active-project-matrix`) on current HEAD; non-Git temporary roots return unavailable/watch without invoking `gh`. Current readiness reports score `96`, clean worktree, open PRs `0`, required workflows green on `a1956ec6`, workspace blocker count `0`, and only Hanwoo T-251 blocked. Verification passed focused readiness tests (`21` passed), task-id gate tests (`8` passed), ruff, py_compile, live task-id allow/block CLI repro, A/B `adopt_candidate`, completion audits `complete`, and GitHub Actions `root-quality-gate` run `26967317966` plus `active-project-matrix` run `26967317897` passed.

- **2026-06-05 (Codex)**: T-1255 blind-to-x/workspace google-genai migration. After checking official Google GenAI SDK docs, `projects/blind-to-x/pyproject.toml` now declares `google-genai>=1.0.0` instead of deprecated `google-generativeai>=0.8.0`, and the active root workspace `uv.lock` resolves `google-genai 2.8.0` without the legacy package in the active lock. `workspace/execution/content_writer.py` now uses `google.genai`/`google.genai.types`, constructs `genai.Client`, calls `client.models.generate_content(...)`, and closes the client; `workspace/tests/test_content_writer.py` covers the new Client path. Verification passed workspace content writer tests (`8` passed), ruff, py_compile, `uv lock --project projects/blind-to-x --check`, project venv import smoke (`google.genai` available, legacy `google.generativeai` absent), focused blind-to-x FutureWarning repro with `-W error::FutureWarning`, blind-to-x lint, blind-to-x project QC (`1723` passed, `9` skipped), full active-project QC (`blind-to-x` 1723 passed/9 skipped, `shorts-maker-v2` 1577 passed/12 skipped, Hanwoo 499 tests + lint/build, Knowledge 61 tests + lint/build), readiness score `96` with only Hanwoo T-251 blocked, A/B `adopt_candidate` (`score_delta=1.4`), completion audit `complete`, pushed `401b77b7`, and GitHub Actions `root-quality-gate` run `26965986775` plus `active-project-matrix` run `26965986181` passed. That feature commit message says T-1254 due a numbering collision; shared context records the cycle as T-1255.

- **2026-06-05 (Codex)**: T-1253 Hanwoo browser QA no-adopt cycle. Playwright CLI browser QA against local Hanwoo at `127.0.0.1:4316` confirmed `/login` readable Korean copy and demo credentials, admin/admin123 submit showing the expected Korean invalid-credentials alert, browser console errors/warnings 0, auth provider/csrf/callback requests returning 200, `/manifest.json` returning 200 JSON, `/` redirecting to `/login`, and `/privacy` returning 200 with readable policy copy. Screenshot artifact: `output/playwright/hanwoo-t1252-privacy.png`. `/api/health` returned 200 JSON with the known T-251 Supabase `P2010 / XX000 / ENOTFOUND tenant/user postgres.fuemeqmigptwfzqvrpjf not found` warning. No local code defect was found; dev server and browser session were stopped.

- **2026-06-05 (Codex)**: T-1252 knowledge-dashboard runtime auth readiness gate. `execution/product_readiness_score.py` now includes a `dashboard_runtime_auth` check for `projects/knowledge-dashboard`, reads the first existing `.env.local` or `.env`, blocks missing/empty/placeholder `DASHBOARD_API_KEY`, reports `DASHBOARD_SESSION_SECRET` as either configured or optional fallback, and never exposes secret values. The gate first caught the missing local env file; a gitignored `projects/knowledge-dashboard/.env.local` was generated and confirmed ignored. Current readiness reports overall score `96` and `knowledge-dashboard` score `100` with Dashboard API key plus dedicated session secret configured; only Hanwoo T-251 remains blocked. Regression coverage in `workspace/tests/test_product_readiness_score.py` locks the requirement plus placeholder API-key blocker path. Verification passed focused pytest (`16` tests), ruff, `ruff format`, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=2.875`), completion audit `complete`, pushed `53563abf`, and GitHub Actions `root-quality-gate` plus `active-project-matrix` passed.

- **2026-06-05 (Codex)**: T-1251 blind-to-x launch env readiness gate. `execution/product_readiness_score.py` now includes a `blind_to_x_launch_env` check for `projects/blind-to-x`, validates Notion review queue credentials (`NOTION_API_KEY`, `NOTION_DATABASE_ID`), validates at least one usable LLM provider key, strips quoted env values, treats empty/placeholder values as blockers, and reports only configured-key counts. Stale QC now downgrades readiness state to `needs-review` even when the numeric score reaches the ready threshold. Current readiness reports overall score `96` and `blind-to-x` score `100` with Notion review queue keys and `8` LLM provider key(s) configured. Regression coverage in `workspace/tests/test_product_readiness_score.py` locks the requirement plus Notion/provider missing-key blocker paths. Verification passed focused pytest (`14` tests), ruff, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=2.514473684210526`), completion audit `complete`, pushed `694491a2`, and GitHub Actions `root-quality-gate` plus `active-project-matrix` passed.

- **2026-06-05 (Codex)**: T-1250 shorts-maker-v2 provider-key readiness gate. `execution/product_readiness_score.py` now includes a `shorts_provider_keys` env check for `projects/shorts-maker-v2`, parses `.env` assignments without printing secret values, treats empty/placeholder provider keys as blockers, and reports only the configured generation-provider key count. Current readiness reports overall score `95` and `shorts-maker-v2` score `100` with `8` provider key(s) configured. Regression coverage in `workspace/tests/test_product_readiness_score.py` locks the requirement and placeholder/missing-key blocker behavior. Verification passed focused pytest (`11` tests), ruff, `py_compile`, `git diff --check`, A/B `adopt_candidate` (`score_delta=1.8894876819708846`), and completion audit `complete`.

- **2026-06-05 (Codex)**: T-1249 shorts-maker-v2 root launch README/readiness gate. `projects/shorts-maker-v2/README.md` now exists as the GitHub-visible launch entrypoint with quick start, generation, operations, verification, artifact hygiene, and documentation links. `.agents/skills/auto-research/scripts/github_project_inventory.py` now recommends adding a root README for projects missing one, and `execution/product_readiness_score.py` requires `README.md` in the `shorts-maker-v2` launch docs. Verification passed focused workspace pytest (`11` tests), inventory confirmed `projects/shorts-maker-v2 has_readme=true`, readiness confirmed `projects/shorts-maker-v2/README.md` present with `shorts-maker-v2` ready, A/B selected `adopt_candidate` (`score_delta=1.0`), completion audit returned `complete`, and post-push GitHub Actions passed on `848e83a0` for both `root-quality-gate` and `active-project-matrix`.

- **2026-06-04 (Codex)**: T-1248 knowledge-dashboard browser QA and Korean copy guard hardening. Playwright CLI browser QA against the local Knowledge Dashboard at `localhost:3103` confirmed API-key login, operations/readiness rendering, knowledge, QA/QC, and activity tab clicks, `/api/auth/session` plus all four data routes returning 200, console/page/network errors 0, and `/api/data/readiness` reporting score 94 with only the existing Hanwoo T-251 blocker. `projects/knowledge-dashboard/src/encoding-guard.test.mts` now rejects common Korean mojibake fragments across source files and asserts core operations/readiness Korean copy remains readable in `app/page.tsx` and `ProductReadinessPanel.tsx`. `.gitignore` now ignores `.playwright-cli/` and `output/playwright/` so browser QA screenshots/snapshots do not pollute future git status. Verification passed Knowledge Dashboard project QC (`61` tests plus lint/build), A/B `adopt_candidate` (`score_delta=0.4666666666666667`), and completion audit `complete`. Temporary Playwright spec files were removed before staging.

- **2026-06-04 (Codex)**: T-1247 product-readiness QC artifact bridge. `execution/project_qc_runner.py` now writes `.tmp/project_qc_runner_latest.json` after real runs, with per-project PASS/FAIL/TIMEOUT, parsed test counts, expected/observed/missing check IDs, and `coverage` so partial `--project`/`--check` runs cannot be over-scored. `execution/product_readiness_score.py` now reads that artifact first, falls back to legacy knowledge-dashboard QA/QC JSON, and treats project-QC artifacts as available only when coverage is complete. Verification passed focused workspace tests (`19 passed`), ruff, py_compile, `git diff --check`, full active-project QC (`blind-to-x` 1723 passed/9 skipped, `shorts-maker-v2` 1577 passed/12 skipped, Hanwoo test/lint/build, Knowledge test/lint/build), readiness score improved from 71 to 94, A/B selected `adopt_candidate` (`score_delta=1.9964788732394365`), completion audit returned `complete`, and post-push GitHub Actions passed on `8b6322ef` for both `root-quality-gate` and `active-project-matrix`.

- **2026-06-04 (Codex)**: T-1246 blind-to-x packaging metadata repair. `projects/blind-to-x/pyproject.toml` now declares `ruff>=0.11.0` in dev extras because the standard project QC lint command is `python -m ruff check .`, and adds `[tool.setuptools.packages.find] include = ["pipeline*", "scrapers*"]` so `pip install -e ".[dev]"` no longer fails on flat-layout auto-discovery. The local blind-to-x `.venv` was rebuilt with Python 3.11.15 after the previous Python 3.14 venv hit an `lxml 5.4.0` wheel/build-tools failure; the old venv was moved under `.tmp/venv-backups`. Verification passed for editable install, project-root import smoke (`pipeline`, `scrapers`, `ruff`), A/B `adopt_candidate` (`score_delta=1.0`), completion audit `complete`, and full `project_qc_runner.py --json` across active projects.

- **2026-06-04 (Codex)**: T-1245 project QC runner hardening. `execution/project_qc_runner.py` now uses `-o addopts=` plus unique repo-local `--basetemp` for Python project pytest checks, sets repo-local `TMP`/`TEMP` on Windows and non-Windows, and skips Python candidates that do not provide the required `-m` module. This avoids coverage-addopts pollution, Windows temp permission collisions, and wrong-venv pytest failures during product-readiness loops. The same cycle fixed `shorts-maker-v2` `JsonlLogger` so it recreates `log_path.parent` under the write lock before opening; actual project QC passed for `blind-to-x` test (`1723 passed, 9 skipped`) and `shorts-maker-v2` test (`1577 passed, 12 skipped`). CI workflow lists now include `workspace/tests/test_project_qc_runner.py` and `execution/project_qc_runner.py` ruff coverage, and post-push GitHub Actions passed on `8a1f3d18` for both `root-quality-gate` and `active-project-matrix`.

- **2026-06-04 (Codex)**: T-1244 auto-research completion-audit hardening. The existing `auto-research` skill now includes `scripts/completion_audit.py` plus `references/completion-audit.md`, so product-launch/self-improvement cycles can gate completion claims on explicit requirement-to-artifact evidence instead of proxy signals. The helper returns `complete` only when every checklist item has artifacts, evidence, `verified: true`, complete coverage, and no blockers, and accepts UTF-8/UTF-16 manifests for Windows PowerShell redirection. `execution/skill_lint.py` also now resolves relative `--skills-root` values from the repo root after verification exposed that bug. The new skill/tool tests and ruff targets are wired into root-quality-gate and active-project-matrix workspace-quality. CI-equivalent focused workspace pytest passed 113/113, focused helper pytest passed 12/12, ruff passed, compileall passed, auto-research-only skill lint passed with score 100, completion audit returned complete for the cycle, A/B selected `adopt_candidate` (`score_delta=0.78125`), and post-push GitHub Actions passed on `d136a91a` for both `root-quality-gate` and `active-project-matrix`.

- **`hanwoo-dashboard`**:
  - **데이터 견고성 확보**: API 및 로컬 캐시/스토리지가 오염되거나 빈 값을 가질 때 발생할 수 있는 크래시(NaN 누출, Invalid Date 노출)를 차단하기 위해 `toFiniteNumber()`, `toValidDate()`, `normalizePaginationItems()` 등의 방어적 데이터 정규화(Normalization) 도우미 함수들을 대거 도입했습니다. (2026-05-21)
  - **UX 및 안정성 강화**: 폼 제출 버튼에 로딩 상태 및 바쁜 표시(`aria-busy`, pending copy)를 일관되게 적용하여 이중 제출을 방지하고, 모든 `<button>`에 명시적인 `type`을 지정하는 회귀 방지 테스트를 추가했습니다. (2026-05-21)
- **`shorts-maker-v2`**:
  - **테스트 안정성**: 딥러닝 무거운 의존성(`torch`, `torch.cuda`) 없이도 로컬에서 100% 테스트할 수 있도록 `test_openvoice_client.py`에 가벼운 런타임 `MagicMock`을 주입하여 602개 단위 테스트 100% 그린 패스를 달성했습니다. `ShortsFactory/engines/text_engine.py`에서 발생하던 `unexpected indent` 인덱스 에러를 해결했습니다. (2026-05-22)
  - **미디어 스텝 개선**: `media_step.py`에서 13개의 중복 중첩 메서드를 공통 Mixin 구조(`MediaAudioMixin`, `MediaVisualMixin`, `MediaFallbackMixin`)로 정리하여 Ruff 린트를 통과하고 유지 보수성을 높였습니다. (2026-05-22)
- **`blind-to-x`**:
  - **생성 품질 통제 (Phase 1)**: `docs/output_quality_uplift_2026-05-26.md`에 따라 어휘 필터링(zero-tolerance 어휘 12개), CTA 종결 규칙, 이모지 제한, 의존성 표현 억제, 12자 연속 중복 감지 등의 결정론적 heuristic 품질 검사를 `quality_gate.py`에 장착했습니다. 단위 테스트 49/49 통과 완료. (2026-05-26)

## Recent Verification

- **2026-06-05 (Codex)**: T-1269 browser-app Vitest dependency freshness loop. Applied `vitest` and `@vitest/coverage-v8` `^4.1.8` to both `projects/suika-game-v2` and `projects/word-chain`, synchronized both npm locks plus root `pnpm-lock.yaml`, added `.gitignore` coverage-folder ignores, and confirmed local Node `24.13.0` plus Vite `8.0.16` satisfy current npm metadata for Vitest 4.1.8. Verification passed `npm ls` at 4.1.8 in both apps, Suika coverage tests 61/61, Word Chain coverage tests 23/23, lint for both apps after removing generated coverage output, builds for both apps via the known ASCII-workspace Vite fallback after direct `3221226505`, root frozen pnpm lock, npm audit 0 for both apps, selected-package outdated checks returning `{}`, A/B `adopt_candidate` (`score_delta=1.4285714285714286`), and completion audit `complete`.

- **2026-06-05 (Codex)**: T-1268 auto-research GitHub inventory CLI compatibility. `.agents/skills/auto-research/scripts/github_project_inventory.py` now accepts `--json` while preserving JSON as the default output, the skill command example includes the flag, workflow quality lists cover the helper/test, and `workspace/tests/test_github_project_inventory.py` has a parseable-JSON CLI regression. Verification passed focused inventory pytest 8/8 with repo-local basetemp, ruff, py_compile, and required Actions on `5849691e`.

- **2026-06-04 (Codex)**: T-1242 knowledge-dashboard ESLint 10 non-adoption closeout. Dependabot PR #117 was investigated and closed without code adoption because ESLint 10 currently breaks the Next lint plugin stack: npm metadata shows latest `eslint-plugin-import@2.32.0`, `eslint-plugin-jsx-a11y@6.10.2`, and `eslint-plugin-react@7.37.5` peer only through ESLint 9, and a local `eslint@10.4.1` trial failed `npm run lint` with `react/display-name` / `contextOrFilename.getFilename is not a function` from bundled `eslint-plugin-react`. Failed local lockfile churn was restored; `knowledge-dashboard` remains on `eslint@9.39.4`, and `npm run lint` passes there.

- **2026-06-04 (Codex)**: T-1241 post-push closeout. `e94faddb` is on `main`, PR #115 was commented and closed as superseded, and both `root-quality-gate` plus `active-project-matrix` passed for that head. The matrix still emitted known checkout annotations (`/usr/bin/git` exit 128) on several jobs, but every job concluded success and the workflow result was green.

- **2026-06-04 (Codex)**: T-1241 word-chain paired ESLint dependency freshness loop. Applied `eslint` `^9.39.1` -> `^10.4.1`, `@eslint/js` `^9.39.1` -> `^10.0.1`, and `eslint-plugin-react-hooks` `^7.0.1` -> `^7.1.1` with synced npm and root pnpm lockfiles after confirming current npm metadata and peer contracts; local Node `24.13.0` satisfies the ESLint 10 engine range. ESLint 10 surfaced `no-useless-assignment` in `scripts/prebuild-check.js`, fixed by removing the unused fallback `status` initializer while preserving catch-path failure status. Verified root lock sync, root frozen lock, `npm ci`, `npm ls` at ESLint 10 stack versions, lint pass after cleanup, tests 23/23, audit 0, build via ASCII fallback after direct Vite `3221226505`, Playwright preview QA with Korean input `사과`, score 60/game-over/high-score state, screenshot `.tmp/word-chain-t1241-eslint10.png`, console/network issues 0, and A/B `adopt_candidate` (`score_delta=1.352941`).

- **2026-06-04 (Codex)**: T-1240 suika-game-v2 paired ESLint dependency freshness loop. Paired `@eslint/js` `^9.39.1` -> `^10.0.1` with `eslint` `^9.39.1` -> `^10.4.1` because `@eslint/js@10.0.1` peers on `eslint ^10.0.0`; local Node `24.13.0` satisfies both engine ranges. ESLint 10 surfaced `no-useless-assignment` in `drawStar()`, fixed by replacing unused final `x/y` assignments with block-scoped outer/inner coordinates. Verified root frozen lock, `npm ls @eslint/js eslint` at `10.0.1`/`10.4.1`, lint pass, tests 61/61, audit 0, build via ASCII fallback after direct Vite `3221226505`, Chrome CDP preview QA with start hidden, 480x800 canvas, sampled nonzero pixels increasing from 144 to 1089 after Space drop, `/manifest.webmanifest`, `/icon.svg`, and `/sw.js` 200, same-origin network/status failures 0, console issues 0, A/B `adopt_candidate` (`score_delta=0.263158`), PR #102/#96 commented and closed as superseded, and main `root-quality-gate` plus `active-project-matrix` passed for `6b77a424`.

- **2026-06-04 (Codex)**: T-1239 suika-game-v2 Vite dependency freshness loop. Applied `vite` `^7.2.4` / lock `7.3.3` -> `^8.0.16` with synced npm and root pnpm lockfiles after confirming npm latest/current engine metadata. PR #104 changelog highlights Vite 8.0.16 Windows UNC/alternate-path hardening and Vite 8.0.15 Rolldown 1.0.3/request-timeout fixes; local Node `24.13.0` satisfies Vite's `^20.19.0 || >=22.12.0` engine. Verified root frozen lock, `npm ls vite` 8.0.16, suika tests 61/61, lint, audit 0, build via ASCII fallback after direct Vite `3221226505`, Chrome CDP preview QA with start overlay hidden, 480x800 canvas, sampled nonzero pixels increasing from 694 to 1102 after Space drop, `/manifest.webmanifest`, `/icon.svg`, and `/sw.js` 200, same-origin network/status failures 0, console issues 0, and A/B `adopt_candidate` (`score_delta=0.559626`).

- **2026-06-04 (Codex)**: T-1238 knowledge-dashboard React dependency freshness loop. Applied `react`/`react-dom` `19.2.3` -> `19.2.7` and `@types/react` `^19` -> `^19.2.16` with synced npm and root pnpm lockfiles after confirming npm latest/current peer metadata; `@types/react-dom` remained latest at `19.2.3`. `react-dom@19.2.7` peers on `react ^19.2.7`, so the runtime pair was updated together instead of taking Dependabot PR #114's partial runtime patch. Verified root frozen lock, `npm ls` React/ReactDOM 19.2.7 plus React type packages, knowledge-dashboard project QC 59 tests/lint/build after a transient local Next build lock cleared, standalone smoke, Chrome CDP browser QA with API-key login, knowledge/qaqc/activity/operations tab clicks, all four data routes plus `/api/health` 200, SVG count 13, console/log issues 0, network failures 0, unchanged audit total 7, and A/B `adopt_candidate` (`score_delta=0.486486`).

- **2026-06-04 (Codex)**: T-1237 word-chain Vite dependency freshness loop. Applied `vite` `^7.2.4` / lock `7.3.3` -> `^8.0.16` with synced npm and root pnpm lockfiles after confirming npm latest/current engine metadata. PR #87 changelog highlights Vite 8.0.16 Windows UNC/alternate-path hardening and Vite 8.0.15 Rolldown 1.0.3/request-timeout fixes; local Node `24.13.0` satisfies Vite's `^20.19.0 || >=22.12.0` engine. Verified root frozen lock, `npm ls vite` 8.0.16, word-chain tests 23/23, lint, audit 0, build via ASCII fallback after direct Vite `3221226505`, Python CDP browser QA with Korean input/submit/max score 60/SVG render/same-origin network failures 0/console issues 0, and A/B `adopt_candidate` (`score_delta=0.5625`).

- **2026-06-04 (Codex)**: T-1236 knowledge-dashboard @types/node dependency freshness loop. Applied `@types/node` `^20`/20.19.x -> `^25.9.1` with synced npm and root pnpm lockfiles after confirming npm latest/current dist-tags; `@types/node@25.9.1` is the TypeScript 5.9 tag and depends on `undici-types >=7.24.0 <7.24.7`. Verification: root frozen lock passed, `npm ls @types/node` reported 25.9.1, `project_qc_runner.py --project knowledge-dashboard --json` passed 59 tests/lint/build, `npm.cmd run smoke` passed standalone runtime smoke, Chrome CDP QA passed API-key login, operations/knowledge/qaqc/activity tab clicks, all four data routes plus `/api/health` 200, SVG count 13, console issues 0, and network failures 0. Existing audit total stayed 7, and A/B selected `adopt_candidate` (`score_delta=0.333333`).

- **2026-06-04 (Codex)**: T-1235 hanwoo-dashboard public health proxy fix. Browser QA found `/api/health` was still protected by the Auth.js proxy matcher, producing a 307 login redirect and browser fetch failure under non-default local ports. Added `api/health` to the proxy matcher exclusion list and covered the exception with a regression test. Verification: focused `src/lib/app-metadata-copy.test.mjs` passed 2/2, full Hanwoo tests passed 499/499, lint passed, production build passed, direct health curl returned 200 JSON, and Chrome CDP QA passed `/login` render, password-toggle coordinate click, `/api/health` 200, `/privacy` navigation, console issues 0, and network failures 0. A/B selected `adopt_candidate` (`score_delta=0.636364`). T-251 live Supabase credential reset remains separate because health JSON still reports the existing `P2010 / XX000 / ENOTFOUND tenant/user ... not found` DB warning.

- **2026-06-04 (Codex)**: T-1243 hanwoo-dashboard ESLint 10 non-adoption. Do not retry Hanwoo ESLint 10 by only bumping `eslint`: current `eslint-config-next@16.2.6` pulls plugin versions whose peer ranges still exclude ESLint 10, `npm.cmd run lint` fails under `eslint@10.4.1`, and Hanwoo's existing `brace-expansion` override also breaks ESLint 10's `minimatch@10.2.5` unless a narrower override is added. A local rescue trial with `minimatch@10.2.5 -> brace-expansion@5.0.6` and `typescript-eslint@8.60.1` still failed lint with `scopeManager.addGlobals is not a function`. Baseline remains `eslint@9.39.4`, and `npm.cmd run lint` passes.

- **2026-06-04 (Codex)**: T-1234 hanwoo-dashboard Prisma dependency freshness loop. Applied `prisma`, `@prisma/client`, and `@prisma/adapter-pg` `^7.6.0` -> `^7.8.0` with synced npm and root pnpm lockfiles after confirming current npm metadata and reviewing official Prisma 7.7.0/7.8.0 release notes. The relevant 7.8.0 changes are `queryPlanCacheMaxSize` plus Prisma Client/schema-engine fixes for PostgreSQL JSON, enum parameterization, parameter-limit checks, concurrent index migration replay, and sequence-default introspection. Verification: root frozen lock passed, npm install with scripts ignored passed, Prisma/client version 7.8.0 plus adapter import smoke passed, Prisma Client 7.8.0 generation passed, offline `db:prisma7-test` passed 14/14 with live CRUD skipped, Hanwoo QC passed 498 tests/lint/build, Hanwoo smoke passed, Playwright CLI login/input/auth-callback browser QA had console errors/warnings 0, audit count stayed at the existing 7 advisories, and A/B selected `adopt_candidate` (`score_delta=0.285714`). T-251 live Supabase credential reset remains separate.

- **2026-06-04 (Codex)**: T-1233 suika-game-v2 jsdom dependency freshness loop. Applied `jsdom` `^28.1.0` -> `^29.1.1` with synced npm and root pnpm lockfiles after confirming npm latest/current engine metadata and reviewing official jsdom v29.0.0, v29.1.0, and v29.1.1 release notes. The relevant compatibility change is the Node 22 minimum moving to `22.13.0+`; local Node `24.13.0` remains supported. Verified root frozen lock, suika tests 61/61, lint, build via ASCII fallback after direct Vite `3221226505`, jsdom `29.1.1` import and DOM smoke, audit 0, Playwright CLI browser QA with Game Start/nonblank 480x800 canvas/drop pixel increase/static requests 200/no console errors, and A/B `adopt_candidate` (`score_delta=0.4`).

- **2026-06-04 (Codex)**: T-1232 blind-to-x Notion SDK dependency freshness loop. Applied `notion-client==3.1.0` with the root workspace `uv.lock` synced, after checking local package metadata (`Requires-Python >=3.8, <4`) and official notion-sdk-py v2.7.0, v3.0.0, and v3.1.0 release pages. The relevant breaking change removes private/helper APIs `is_full_page_or_database` and `is_api_error_code`, which blind-to-x does not use; v3.1.0 adds endpoint support and automatic retry support. Verification: `uv lock --project projects/blind-to-x --check` passed, import/API smoke reported Notion 3.1.0 and confirmed the used `AsyncClient` namespaces/methods, focused Notion upload/schema/persist tests passed 163/163 with one unrelated deprecated `google.generativeai` warning, extra Notion analytics/query/backfill tests passed 99/99, blind-to-x lint passed, and A/B selected `adopt_candidate` (`score_delta=0.55`).

- **2026-06-04 (Codex)**: T-1231 word-chain jsdom dependency freshness loop. Applied `jsdom` `^28.1.0` -> `^29.1.1` with synced npm and root pnpm lockfiles after confirming npm latest/current engine metadata and reviewing official jsdom v29.0.0, v29.1.0, and v29.1.1 release notes. The relevant compatibility change is the Node 22 minimum moving from `22.12.0+` to `22.13.0+`; local Node `24.13.0` remains supported. Verified root frozen lock, word-chain tests 23/23, lint, build via ASCII fallback after direct Vite `3221226505`, jsdom `29.1.1` import and DOM smoke, audit 0, and A/B `adopt_candidate` (`score_delta=0.5`).

- **2026-06-04 (Codex)**: T-1230 blind-to-x OpenAI SDK dependency freshness loop. Applied `openai==2.41.0` with the root workspace `uv.lock` synced, after checking official PyPI metadata and openai-python v2.38.0..v2.41.0 release notes. The release range is additive API/spec, workload identity/additional Responses tools, Bedrock Responses support, and `responses.moderation` / `chat_completions.moderation` surface updates. Verification: `uv lock --project projects/blind-to-x --check` passed, import smoke reported OpenAI 2.41.0 and `AsyncOpenAI`, focused OpenAI provider/image/runtime tests passed 152/152, blind-to-x lint passed, and A/B selected `adopt_candidate` (`score_delta=0.55`).

- **2026-06-04 (Codex)**: T-1229 blind-to-x Anthropic SDK dependency freshness loop. Applied `anthropic==0.105.2` with the root workspace `uv.lock` synced, after checking official anthropic-sdk-python v0.105.0, v0.105.1, and v0.105.2 release pages. The relevant changes are additive support for `claude-opus-4-8`, mid-conversation system blocks, `usage.output_tokens_details`, custom file size caps, plus internal publishing/changelog-only follow-ups. Verification: `uv run` installed anthropic 0.105.2, `AsyncAnthropic` import smoke passed, focused Anthropic provider/cost/prompt-cache tests passed 70/70, blind-to-x lint passed, and A/B selected `adopt_candidate` (`score_delta=0.9`).

- **2026-06-04 (Codex)**: T-1228 blind-to-x pytest-asyncio dependency freshness loop. Applied `pytest-asyncio==1.4.0` with the root workspace `uv.lock` synced, after checking the official pytest-asyncio v1.4.0 release notes. The release deprecates overriding `event_loop_policy`, adds `pytest_asyncio_loop_factories`, improves an unset fixture-loop-scope warning, and raises the minimum pytest to `8.4.0`; blind-to-x has no `event_loop_policy`/`loop_factories`/`asyncio_default_fixture_loop_scope`/`pytest_asyncio.fixture` usage and currently runs pytest `8.4.2`. Verification: `uv run` installed pytest-asyncio 1.4.0, full unit+integration without coverage passed 1791/1791 with 5 skipped when the known unrelated `tests/integration/test_curl_cffi.py` local CAfile test was ignored, blind-to-x lint passed, and A/B selected `adopt_candidate` (`score_delta=0.9`).

- **2026-06-04 (Codex)**: T-1227 suika-game-v2 `globals` dependency freshness plus runtime asset 404 fix. Applied `globals` `^16.5.0` -> `^17.6.0` with synced npm and root pnpm lockfiles after confirming npm latest/current engine metadata. Browser QA exposed missing runtime asset requests for `background.png` and fruit PNG sprites, so CSS now uses gradients only and renderer image loading only requests explicitly registered bundled fruit assets before using emoji fallback. Verified root frozen lock, suika tests 61/61, lint, build via ASCII fallback after direct Vite `3221226505`, `globals.browser`/`globals.node` smoke, audit 0, Python CDP browser QA with free-play start/nonblank canvas/drop/pause and no serious console/network issues, clean temp root pnpm install plus suika `npm ci`, and A/B `adopt_candidate` (`score_delta=0.7`).

- **2026-06-04 (Codex)**: T-1226 word-chain `globals` dependency freshness plus PWA icon runtime fix. Applied `globals` `^16.5.0` -> `^17.6.0` with synced npm and root pnpm lockfiles after confirming npm latest/current engine metadata. Browser QA exposed missing manifest icon files, so added `public/icon.svg`, changed manifest icons to that real SVG asset, and added `mobile-web-app-capable` meta. Verified root frozen lock, word-chain tests 23/23, lint, build via ASCII fallback after direct Vite `3221226505`, `globals.browser` smoke for browser globals, audit 0, Python CDP browser QA with Korean input/submit/score/SVG render and no serious console/network issues, clean temporary root pnpm install, and A/B `adopt_candidate` (`score_delta=0.7`).

- **2026-06-04 (Codex)**: T-1225 blind-to-x Playwright dependency freshness loop. Applied `playwright==1.60.0` with the root workspace `uv.lock` synced, after checking the official Playwright Python v1.60.0 release notes and confirming blind-to-x has no `expose_binding` or `handle=` usage affected by the listed breaking change. Verified `uv run` installed Playwright 1.60.0, async import smoke passed, focused scraper/config unit tests passed 16/16 with repo-local basetemp, blind-to-x lint passed via QC runner, and A/B selected `adopt_candidate`. Full unit suite via `uv run` exceeded the 5-minute local turn timeout; main CI must be rechecked after push.

- **2026-06-04 (Codex)**: T-1224 knowledge-dashboard standalone smoke/data-dir fix. Final `active-project-matrix` failed only in knowledge-dashboard runtime smoke because the missing-file assertion removed `skill_lint.json` from source `data/` while T-1223's standalone helper had copied the same file into the standalone server cwd, so `/api/data/skills` still returned 200. Added `KNOWLEDGE_DASHBOARD_DATA_DIR`, routed authenticated data routes and health through `dashboardDataFile()`, changed standalone start to set the runtime data dir instead of copying stale data, documented Docker `/app/data`, and made smoke remove/restore fixtures across source/root/nested standalone data candidates. Verified standalone smoke, knowledge-dashboard project QC test/lint/build, and standalone smoke again after build.

- **2026-06-04 (Codex)**: T-1223 knowledge-dashboard standalone deploy/start hardening. Found production runtime drift where `output: "standalone"` still used `next start`; Next warns this path is not the standalone server path, and local output can be nested at `.next/standalone/projects/knowledge-dashboard/server.js`. Added `scripts/start-standalone.mjs`, changed `npm run start` and smoke to use it, copied current static/data assets into the generated server cwd, hardened Docker for root/nested standalone output plus `/app/data` mount visibility, and cleaned deploy docs/logs to ASCII-safe operator copy. Verified missing-key/keyed `verify-deploy`, project QC test/lint/build, standalone smoke, Playwright login + QA/QC + Knowledge tab clicks with console errors/warnings 0 and data APIs 200, and A/B `adopt_candidate`.

- **2026-06-04 (Codex)**: T-1222 word-chain `lucide-react` dependency freshness loop. Applied `lucide-react` `^0.563.0` -> `^1.17.0` with synced npm and root pnpm lockfiles, fixed `prebuild-check.js` lint by replacing the control-regex ASCII path test with `isAsciiPath()`, superseded and closed Dependabot PR #112, and verified current npm metadata (`latest=1.17.0`, React 19 peer compatible), root frozen lock, word-chain tests 23/23, lint, build via ASCII fallback after direct Vite `3221226505`, 3 lucide imports with 0 missing exports, audit 0, Chrome/Edge CDP browser QA with SVG render/input/submit/score and no console/network serious issues, clean temporary root pnpm install, A/B `adopt_candidate`, and main CI `root-quality-gate` plus `active-project-matrix` pass.

- **2026-06-04 (Codex)**: T-1219 knowledge-dashboard repo-display fallback hardening. Browser QA after T-1218 exposed malformed GitHub rows rendering `undefined 저장소 열기` and risking search crashes on `repo.name.toLowerCase()`. Added `getGithubRepoDisplayName()` and routed tag/search text plus repo card title/aria-label through safe display copy. Verified focused dashboard-view regression, lint, `project_qc_runner.py --project knowledge-dashboard --json`, root frozen lock, production smoke, Playwright login plus Knowledge tab QA with console errors/warnings 0, data APIs 200, SVG render, selected tab `지식 현황`, `hasUndefinedText=false`, and A/B `adopt_candidate`.

- **2026-06-04 (Codex)**: T-1218 knowledge-dashboard `lucide-react` dependency freshness loop. Applied `lucide-react` `^0.563.0` -> `^1.17.0` with synced npm and root pnpm lockfiles, superseding and closing Dependabot PR #119. Verified current npm metadata (`latest=1.17.0`, React 19 peer compatible), root frozen lock, 29 knowledge-dashboard lucide named imports with 0 missing exports, `project_qc_runner.py --project knowledge-dashboard --json` test/lint/build pass, `npm.cmd run smoke` pass, Chrome CDP login and tab-click smoke with lucide SVG render and no console/network serious issues, clean temporary worktree `pnpm install --frozen-lockfile`, A/B `adopt_candidate`, and main CI `root-quality-gate` plus `active-project-matrix` pass.

- **2026-06-04 (Codex)**: T-1217 Hanwoo `lucide-react` dependency freshness loop. Applied `lucide-react` `^0.563.0` -> `^1.17.0` with synced npm and root pnpm lockfiles. Verified current npm metadata (`latest=1.17.0`, React 19 peer compatible), root frozen lock, 58 Hanwoo lucide named imports with 0 missing exports, `project_qc_runner.py --project hanwoo-dashboard --json` test/lint/build pass, Chrome CDP login click smoke with lucide SVG render and no console/network issues, clean temporary worktree `pnpm install --frozen-lockfile`, and A/B `adopt_candidate`.

- **2026-05-26 (Gemini)**: 모노레포 전역 스페이싱/포맷팅 테스트 하드닝 및 QC 스윕 완료. `hanwoo-dashboard`에서 Biome 포맷터 도입에 대응해 29개 정규식 검사를 하드닝하여 282/282 Node 테스트 100% 그린 패스 및 Next 빌드 통과. `shorts-maker-v2` Ruff 린트 에러(UP035, I001) 및 `blind-to-x` coverage 70% 제한 우회 처리(`--no-cov` 격리)를 통해 모노레포 전체 QC 올-그린 확인.
- **2026-05-22 (Gemini)**: `media_step.py` 리팩토링 및 Ruff 린트 클린. `shorts-maker-v2` 602개 단위 테스트 100% 그린 패스 달성.
- **2026-05-21 (Codex)**: `hanwoo-dashboard` 전역 데이터 정규화(Data Normalization) 하드닝 및 263개 테스트 스위트 그린 패스 완료.

## Minefield

- **`blind-to-x`**:
  - **Ppomppu browser QA context**: Source preflight and manual click QA should share `scripts/source_browser_probe.py` context assumptions: `locale=ko-KR`, desktop viewport, and the configured desktop Chrome user agent. A vanilla Chromium context can return a false `403 Forbidden`/empty-anchor result on `https://www.ppomppu.co.kr/hot.php`, while the probe context loads the same page as HTTP `200` with readable HOT links.
  - **Source click-through gate**: Before paid/LLM-backed live runs, prefer `main.py --source <source> --source-preflight --source-preflight-click-through ...` so the gate proves both listing readability and first-post detail readability. A listing-only `ready` result can miss broken detail navigation; click-through failures classify as `click_error`.
  - **Ppomppu click interstitial**: Ppomppu HOT links can expose `/zboard/zboard.php?...&no=...` and leave a clicked page on a `Loading ...` interstitial even though the canonical `/zboard/view.php?...&no=...` detail URL is readable. Keep the source preflight canonicalization/retry behavior before changing scraper selectors.
- **`hanwoo-dashboard`**:
  - **Next dev loopback QA**: Next 16 dev resources block cross-origin dev asset/HMR requests unless `allowedDevOrigins` includes the extra host. Hanwoo intentionally allows `127.0.0.1` in `next.config.mjs` so Codex/Playwright browser QA can use loopback without repeated `/_next/webpack-hmr` console errors. When testing protected Auth routes on a non-default port, align `NEXTAUTH_URL`/`AUTH_URL` with that port or use the default `3001`; otherwise the redirect may point at `localhost:3001`.
  - **소프트 삭제 계약**: 개체 삭제는 하드 딜리트가 아닌 소프트 아카이브(`isArchived`)로 동작합니다. 문구 및 기능 개발 시 파괴적인 '삭제' 대신 '보관 처리'를 일관되게 적용하십시오.
  - **태그 유니크 제한**: `Cattle.tagNumber`는 고유값입니다. 중복 예외 발생 시 Prisma `P2002` 에러를 한국어 문구로 깔끔하게 맵핑하십시오.
  - **개인정보 유출 주의**: 공인 약관 및 법적 페이지(`/privacy`, `/terms`)에 개인 휴대폰 번호나 주소가 유출되지 않도록 `legal-pages-copy.test.mjs` 검증 규칙을 유지하십시오.
  - **데이터 Normalization 정책**: API, 캘린더, 날짜 정렬, 재고 수량, 차트 데이터 등 외부 유입/로컬 캐시 컬렉션은 렌더링 혹은 연산 전에 반드시 `safe*` 또는 normalizer 도우미를 거쳐 가공하십시오. (NaN, Invalid Date 원천 차단)
  - **웹 접근성(A11y)**: 모달 다이얼로그 개방 시 자동 포커스 캡처 및 `role="dialog"`, `aria-modal="true"`, Escape dismissal이 올바르게 동작하도록 캡션 및 위젯 포커스 관리를 수행하십시오.
- **`shorts-maker-v2`**:
  - **Edge TTS 채널 컨텍스트**: `MediaStep`은 TTS 호출 시 반드시 `AppConfig._channel_key`를 넘겨야 채널별 고유 피치/속도 진동(prosody jitter)이 정상 적용됩니다. 그렇지 않으면 디폴트 속도로 무음 처리됩니다.
  - **Pytest 모의 오염**: Pytest 런타임에서 특정 테스트 모듈이 `sys.modules`에 MagicMock을 직접 주입하면 다른 테스트에 영향을 주므로, 반드시 setup/teardown 격리 또는 mock wrapper를 사용하십시오.
- **Windows 실행 장벽**:
  - 파이썬 및 Node 프로세스 실행 시 Windows 환경에서는 쉘 실행 바이너리(`.cmd`, `.bat`) 확장자를 명시하거나 `PATHEXT`를 존중하도록 코딩하십시오.
  - `Get-ChildItem -Recurse` 등을 수행할 때 존재하지 않는 디렉토리를 탐색하면 에러가 발생하므로, 탐색 전 타깃 존재 여부를 반드시 체크하십시오.
  - Windows의 CP949 인코딩 콘솔 한글 깨짐 현상이 있어도 실제 파일 및 데이터는 항상 UTF-8 클린하게 저장해야 합니다.
  - auto-research 증거 JSON처럼 PowerShell `Get-Content`로 다시 읽는 파일은 UTF-8 저장만으로 부족할 수 있습니다. 한글 프로필 경로가 포함되는 JSON은 `ensure_ascii=True` 등으로 ASCII-escape하여 기본 PowerShell 읽기에서도 `ConvertFrom-Json`이 깨지지 않게 하십시오.
  - 터미널이나 CLI 도구에서 비ASCII 유니코드나 이모지를 stdout/stderr로 직접 출력하면 `UnicodeEncodeError` 예외가 발생하므로, 윈도우 실행 시 `sys.stdout.reconfigure(encoding='utf-8')`을 통해 스트림을 강제 재구성하거나 이모지 사용을 피하십시오.
  - Codex/PowerShell 세션에서 `Get-Content`, `Join-Path`, `Test-Path`, `Select-Object` 같은 core cmdlet이 다시 로드되지 않을 수 있습니다. MCP/launcher 스크립트는 가능한 한 `[System.IO.*]`, `[System.Environment]`, `[System.Text.RegularExpressions.Regex]` 같은 .NET API로 작성하십시오.
  - For word-chain browser QA on this Windows/Korean-path setup, a Node-native WebSocket CDP script crashed with `-1073740791` after connecting to DevTools. Use Python `websockets` CDP or the Playwright wrapper for browser-click smoke tests, and keep the Vite build ASCII-workspace fallback for direct `3221226505` failures.
  - For focused workspace pytest on this Windows/Korean-path setup, use `-o addopts= --basetemp .tmp\<purpose>` when root addopts or user-temp permissions interfere. Coverage-producing Node test commands can leave generated `coverage/` folders that lint will scan; remove those generated folders before app lint.
  - Full workspace `pnpm install --frozen-lockfile --ignore-scripts` can exit `-1073740791` during Windows node_modules linking even when `pnpm install --lockfile-only --frozen-lockfile` and root-filtered install pass. When this appears, verify package-manager metadata, root-filtered install, actual tool versions, and required Linux GitHub Actions before treating it as a package/lock regression.
  - For ad-hoc Playwright browser QA, `npx.cmd --yes playwright` can run the CLI, but packages installed through npx are not visible to local `.tmp` Node scripts or Playwright config via normal `require('@playwright/test')`. Use the cached `node_modules/playwright` package path via `PLAYWRIGHT_PACKAGE_DIR` or use an installed project dependency; also keep `Start-Process` stdout/stderr redirects in separate files on PowerShell.
  - 이 "Windows 실행 장벽" 규칙 전체는 `windows-safe-scripting` 스킬로 인코딩되어 있습니다(T-1221) — PowerShell 런처/MCP 스크립트/`execution/` 파이썬을 작성·수정할 때 트리거하십시오.
- **MCP / connector startup**:
  - `@notionhq/notion-mcp-server` v2.2.1은 `NOTION_TOKEN`을 권장 env로 사용합니다. `infrastructure/notion-mcp/start_notion_mcp.ps1`는 기존 `.env`의 `NOTION_API_KEY`도 읽어 `NOTION_TOKEN`과 양방향 동기화한 뒤 `--transport stdio`로 실행해야 합니다.
  - sandbox 안에서 Notion MCP npm 검증은 `EACCES` 또는 npm cache/log 쓰기 실패가 날 수 있습니다. 실제 stdio initialize 검증은 승인된 network/cache 권한에서 통과하는지 확인하십시오.
  - standalone `mcp_servers.figma` remote URL은 OAuth client credentials 없이는 token refresh가 실패합니다. 현재는 `C:\Users\박주호\.codex\config.toml`에서 비활성화했고, Figma 작업은 plugin/app connector를 우선 사용하십시오.

## Multi-Tool Coordination — Task ID Allocation

여러 AI 도구(Claude Code, Codex, Gemini, ...)가 `.ai/TASKS.md` 를 동시에 편집하다 보니
같은 `T-####` 가 두 번 사용되는 충돌이 반복 발생합니다(T-1107×2, T-1108×2, T-1195×2,
T-1199×2 — `git log` 에서 직접 확인 가능). 각 도구가 자기 스냅샷의 `max(T-####)+1`
을 독립적으로 고르기 때문입니다.

**새 task ID 가 필요할 때는 반드시 `execution/next_task_id.py` 의 출력을 사용하십시오**:

```bash
py -3 execution/next_task_id.py
# → T-1201

# automation:
py -3 execution/next_task_id.py --json
```

이 스크립트는 `.ai/TASKS.md`, `.ai/HANDOFF.md`, 최근 30개 git commit 의 subject + body
세 곳을 모두 스캔해 `T-####` 참조 합집합의 최대값 + 1 부터 충돌 없는 첫 ID 를
제안합니다. 다른 도구가 자기 커밋에 이미 사용했지만 `TASKS.md` 에는 아직 반영 안 된
ID 도 git log 에서 잡히므로, 도구 간 race window 가 "수 분(스냅샷 갱신 주기)"에서
"수 초(커밋 직전 재실행)" 로 좁아집니다.

**완전 동시(1초 이내) 충돌 폴백 규칙**: 그래도 충돌이 발생하면 *나중에 커밋하는*
도구가 ID 뒤에 알파벳 접미사를 붙이고(`T-1201b`) 커밋 본문에서 충돌을 언급하십시오.
이미 유기적으로 정착된 패턴이며(예: commit `e940de77` 의 "ID 충돌 노트:" 섹션) 변경
이력 보존을 위해 과거 commit message 의 ID 는 절대 다시 쓰지 마십시오.

> 이 절차는 `task-id` 스킬로 트리거됩니다(T-1221).

## AI Context Rotation Tools

When `.ai/HANDOFF.md` or `.ai/SESSION_LOG.md` grows large during session close,
run the deterministic rotators before committing context updates:

```bash
python execution/handoff_rotator.py --check --json
python execution/handoff_rotator.py --json

python execution/session_log_rotator.py --check --json
python execution/session_log_rotator.py --json
```

Both tools keep entries dated within the last 7 days by default and archive
older material under `.ai/archive/`.

> 세션 시작은 `session-start` 스킬, 세션 종료(이 로테이터 호출 포함)는 `session-close` 스킬로 트리거됩니다(T-1221).
