# GOAL - Active Workspace Goal

> Shared current goal for Claude Code, Codex, Gemini, and other AI tools.

## Active Goal

- Status: active
- Goal: Product launch-ready workspace through the auto-research self-improvement loop across GitHub projects, browser-click QA, A/B adoption, current dependency/code triage, and local readiness gates.
- Owner: Codex, with user-owned T-251 external Supabase credential reset
- Started: 2026-06-05
- Latest: 2026-06-07 completed T-1459 Hanwoo empty building card quick-action recovery in local commit `376acc2e`. The home empty building card now routes through the existing `add-building` quick-action path, opening the Settings add-building form and focusing `#building-name` instead of only switching tabs. Browser QA at 390px verified Settings selected, `#building-name` active, `축사 등록 취소` visible, input above the bottom tab bar, horizontal overflow `0`, no API error resources, and screenshots `output/playwright/hanwoo-t1459-empty-building-card-baseline.png` plus `output/playwright/hanwoo-t1459-empty-building-card-candidate.png`. Verification passed focused home/settings tests (`70 passed`), Hanwoo project QC (`524 passed`, lint/build/smoke passed), diff-check, code-review gate advisory WARN covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.7272727272727273`). No push was performed and T-251 was not retried. Keep release boundaries as explicit push/current-head Actions plus external/user-owned T-251 Supabase credential reset/live Prisma CRUD E2E.
- Success: All launch-audit requirements are complete after user-owned Hanwoo T-251 Supabase credential reset and live Prisma CRUD E2E pass; until then keep local launch evidence green and do not retry T-251.

## Progress Notes

- 2026-06-07: Completed T-1459 Hanwoo empty building card quick-action recovery in local commit `376acc2e`. Baseline mobile QA at 390px clicked the home `첫 번째 축사를 추가해 주세요` empty building card and found Settings selected but focus stayed on `BODY`, the add-building form stayed closed, and only the `축사 등록` toggle was visible. `DashboardClient.js` now routes that CTA through `handleQuickAction({ id: "add-building", targetTab: "settings" })`, reusing SettingsTab's existing open/scroll/focus behavior. Candidate browser QA verified Settings selected, `#building-name` active, form toggle `축사 등록 취소`, input above the fixed bottom nav, horizontal overflow `0`, `badResponses=[]`, and screenshots `output/playwright/hanwoo-t1459-empty-building-card-baseline.png` plus `output/playwright/hanwoo-t1459-empty-building-card-candidate.png`. Verification passed focused home/settings tests (`70 passed`), Hanwoo project QC (`524 passed`, lint/build/smoke passed), diff-check, code-review gate advisory WARN covered by focused/browser/project gates, W3C WCAG 2.4.3 Focus Order review, and A/B `adopt_candidate` (`score_delta=0.7272727272727273`). No push was performed and T-251 was not retried.

- 2026-06-07: Refreshed current-head local launch gates at clean local HEAD `c73e8427` after T-1458. `python execution/project_qc_runner.py --json --artifact .tmp/project_qc_runner_latest.json` passed with full workspace coverage: Blind-to-X `1834 passed`, `9 skipped`; Shorts Maker V2 `1640 passed`, `12 skipped`, `29 warnings`; Hanwoo `524 passed`; Knowledge `62 passed`; lint/build/smoke gates passed where applicable. `python execution/product_readiness_score.py --json` reported score `96`, local blockers `0`, publish blockers `1`, external blockers `1`; `launch_objective_audit.py` reported complete local coverage and `completion_audit.py --allow-incomplete` reported incomplete `9/14` with `5` blocked issues. `session_orient.py --json` confirmed clean worktree and current graph. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1458 Hanwoo AI chat prompt/fallback guidance in local commit `2ada2543`. `/api/ai/chat` now has a structured Korean prompt contract, explicit farm-context connection state, UTF-8 SSE responses, and stable provider/key error copy. `AIChatWidget.js` now turns recoverable AI/provider failures into a practical fallback guide with checklist-style next steps and a retry button that restores the original question to the focused input, while keeping auth/validation failures non-recoverable. Browser QA at 390px verified empty send disabled, typed estrus question enabled send, fallback guide/checklist/retry rendered after provider failure, retry restored the exact question, input stayed focused, send stayed enabled, horizontal overflow `0`, and screenshots `output/playwright/hanwoo-t1458-ai-chat-prompt-baseline-error.png` plus `output/playwright/hanwoo-t1458-ai-chat-prompt-fallback-retry.png`. Verification passed focused AI chat tests (`14 passed`), Hanwoo project QC (`524 passed`, lint/build/smoke passed), diff-check, code-review gate advisory WARN covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=1.1904761904761907`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1457 Hanwoo field mode quick-search focus recovery in local commit `d29d2622`. `FieldModeView.js` now keeps a `searchInputRef`, focuses it on mount via `scheduleFieldModeAnimationFrame()`, cancels the frame on cleanup, and attaches the ref to the field-mode quick-search input. Browser QA at 390px verified active `INPUT`, active aria label `개체 이름 또는 이표번호로 검색`, placeholder `이표번호 4자리 또는 개체명 입력`, visible input rect `top=205/bottom=261`, horizontal overflow `0`, no bad responses, and screenshot `output/playwright/hanwoo-t1457-field-mode-search-focus.png`; console DB/KAPE messages matched known external T-251. Verification passed focused FieldMode tests (`15 passed`), direct Hanwoo build, Hanwoo project QC (`524 passed`, lint/build/smoke passed), diff-check, code-review gate advisory WARN covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.4`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1456 Shorts Maker V2 lazy render-pipeline import in local commit `04a835d3`. `cli.py` now lazily imports `PipelineOrchestrator` through `_get_pipeline_orchestrator_class()` only when generation/batch execution needs it. `dashboard` and `costs` tests assert the render pipeline stays lazy. Verification passed focused CLI tests (`16 passed`), Ruff, `py_compile`, Shorts project QC (`1640 passed`, `12 skipped`, `29 warnings`, lint passed), and A/B `adopt_candidate` (`score_delta=1.2678571428571428`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1455 Hanwoo AI chat input focus recovery in local commit `b9c13a2c`. `AIChatWidget.js` now keeps an `inputRef`, focuses `inputRef.current || panelRef.current` when the dialog opens, and preserves launcher focus restoration on close. Browser QA at 390px verified active `INPUT`, input visible above the fixed bottom nav, horizontal overflow `0`, no bad responses, and screenshot `output/playwright/hanwoo-t1455-ai-chat-input-focus.png`; console DB errors matched known external T-251. Verification passed related AI Node tests (`14 passed`), Hanwoo project QC (`523 passed`, lint/build/smoke passed), diff-check, and A/B `adopt_candidate` (`score_delta=0.4444444444444444`). No push was performed and T-251 was not retried. A separate local Shorts Maker V2 commit `04a835d3` is newer than this Hanwoo code commit and was not part of this Hanwoo verification.

- 2026-06-07: Completed T-1453 Hanwoo Today Focus sales quick action in local commit `47a87c3f`. The no-sales Today Focus prompt now routes through `record-sale`, opens the Sales form, scrolls it into view, and focuses `sale-date` while preserving React Hook Form refs. Browser QA at 390px verified active `INPUT#sale-date`, visible input above the bottom nav, horizontal overflow `0`, and screenshot `output/playwright/hanwoo-t1453-today-focus-sale-form-focus.png`; console DB errors matched known external T-251. Verification passed focused Node tests (`71 passed`), diff-check, Hanwoo project QC (`523 passed`, lint/build/smoke passed), full active-project QC, A/B `adopt_candidate` (`score_delta=0.9230769230769231`), and readiness score `96` with local blockers `0`, publish blockers `1`, external blockers `1`. No push was performed and T-251 was not retried.

- 2026-06-07: Refreshed full active-project QC and readiness at clean local HEAD `f440c0d1`. Canonical project QC PASS: Blind-to-X `1834 passed`, `9 skipped`; Shorts Maker V2 `1639 passed`, `12 skipped`, `29 warnings`; Hanwoo `522 passed`; Knowledge `62 passed`; lint/build/smoke gates passed where applicable. Readiness stayed score `96` with local blockers `0`, publish blockers `1`, external blockers `1`; worktree clean and graph current. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1451 Blind-to-X source preflight summary viewport evidence in local commit `e28f697f`. `source_browser_probe.py` now includes `summary.viewport` in preflight reports while preserving T-1449's viewport-aware `summary.recommended_command`. Live mobile Ppomppu probe verified `summary.viewport="mobile"` with readable click-through detail. Verification passed focused source-browser tests (`30 passed`), related source/main tests (`71 passed`), `py_compile`, Ruff check, live mobile probe, Blind-to-X project QC (`1834 passed`, `9 skipped`, lint passed), diff-check, code-review gate advisory WARN covered by focused/live/project gates, and A/B `adopt_candidate`. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1450 Hanwoo setup-progress inventory/schedule focus recovery in local commit `f5c3fdd1`. `buildSetupProgressItems()` now attaches `add-inventory` and `add-schedule` action IDs to missing inventory/schedule items; `InventoryTab` and `ScheduleTab` respond to quick-action nonce updates, open the add form, scroll it into view, and focus the first useful input while preserving React Hook Form registration refs. Browser QA at 390px confirmed `INPUT#inventory-name` and `INPUT#schedule-title` as active elements, visible forms/inputs, and horizontal overflow `0`. Verification passed focused tests (`32 passed`), Hanwoo project QC (`522 passed`, lint/build/smoke passed), diff-check, and code-review gate advisory WARN covered by focused/browser/project gates. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1449 Blind-to-X viewport-aware recommended command in local commit `eb7b8681`. `source_browser_probe.py` now threads source preflight `viewport` through report summary generation so mobile preflight recommendations include `--source-preflight-viewport mobile` while desktop recommendations stay unchanged. Live mobile Ppomppu probe verified the mobile viewport flag and readable click-through detail. Verification passed focused source-browser tests (`30 passed`), related source/main tests (`71 passed`), `py_compile`, Ruff check/format, live mobile probe, Blind-to-X project QC (`1834 passed`, `9 skipped`, lint passed), diff-check, and A/B `adopt_candidate`. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1448 Hanwoo new-cattle modal first-field focus in local commit `1a0a9869`. `CattleForm` now composes a local `cattleNameInputRef` with React Hook Form's `name` registration ref and focuses `cattleNameInputRef.current || dialogRef.current` when the modal opens. Browser QA at 390px clicked the home setup-progress `개체 등록` item and confirmed `activeElement=INPUT#cattle-name`, the input visible above the bottom nav, and horizontal overflow `0`. Verification passed focused cattle form/detail tests (`17 passed`), direct build, Hanwoo project QC (`521 passed`, lint/build/smoke passed), diff-check, and code-review gate advisory WARN covered by focused/browser/project gates. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1447 Blind-to-X obstructed click detail fallback in local commit `f9adaf08`. `source_browser_probe.py` now keeps the first visible post's canonical detail URL and retries it when click/scroll/detail-wait failures obstruct normal HTML click-through. README, `directives/x_content_curation.md`, and CLI help document the fallback. Verification passed focused source-browser tests (`29 passed`), related source/main tests (`70 passed`), `py_compile`, Ruff check/format, live Ppomppu standalone click-through probe, Blind-to-X project QC (`1833 passed`, `9 skipped`, lint passed), diff-check, code-review gate advisory WARN covered by focused/related/live/project gates, and A/B `adopt_candidate`. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1446 Hanwoo setup building-form focus recovery in local commit `eedbf3d8`. `SettingsTab` now scrolls the opened building form into view and focuses `building-name` when the setup quick action opens the add-building form. Browser QA at 390px clicked `축사 구조 미완료`, confirmed Settings selected, active element `building-name`, input visible above the bottom nav, and horizontal overflow `0`. Verification passed focused settings tests (`16 passed`), Hanwoo project QC (`521 passed`, lint/build/smoke passed), diff-check, and code-review gate advisory WARN covered by focused/browser/project gates. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1445 Hanwoo notification SMS status feedback in local commit `cae0ebee`. `NotificationModal` now exposes local SMS test success/failure feedback through `role="status"`, `aria-live="polite"`, and `aria-atomic="true"` and links the SMS button through `aria-describedby` when status text exists. `FeedbackProvider` toasts now render at `z-[360]`, above modal overlays. Browser QA at 390px authenticated `/`, opened the notification center, verified the SMS button was not intercepted by the fixed tab bar, clicked SMS test, confirmed the Korean success status in the modal, and found no horizontal overflow. Verification passed focused Node tests (`13 passed`), Hanwoo project QC (`520 passed`, lint/build/smoke passed), diff-check, and code-review gate advisory WARN covered by focused/browser/project gates. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1444 Hanwoo notification modal/tab bar overlap hardening in the T-1444 local commit. `DashboardClient.js` now renders `NotificationModal` after `TabBar`, and `globals.css` scopes the dashboard child stacking context to `.dashboard-container > :not(.tab-bar):not(.modal-overlay)` so the modal overlay keeps its fixed positioning and z-index above the mobile tab bar. `.gitignore` now ignores generated `projects/hanwoo-dashboard/test-results/`. Production browser QA at 390px verified `hasOverlay=true`, `hasDialog=true`, `overlayPosition=fixed`, `overlayZ=300`, `tabBarZ=200`, `bottomHitInOverlay=true`, `bottomHitInTabBar=false`, no horizontal overflow, and `badResponses=[]`; screenshot `output/playwright/hanwoo-t1444-notification-tabbar-overlap.png`. Verification passed focused Node tests (`62 passed`), Hanwoo project QC (`519 passed`, lint/build/smoke passed), diff-check, and A/B `adopt_candidate` (`score_delta=0.666828`). No push was performed and T-251 was not retried. Separate FeedbackProvider WIP was preserved outside this task.

- 2026-06-07: Completed T-1443 Hanwoo mobile tab bar fixed-position recovery in local commit `8ac0beec`. Baseline mobile QA at 390px showed `.dashboard-container > *` overrode `.tab-bar { position: fixed }`, pushing the tab bar near `y=2970` and preventing first-viewport Settings activation; forced diagnostics navigation also surfaced `Received true for a non-boolean attribute glow`. `globals.css` now scopes the dashboard stacking context to `.dashboard-container > :not(.tab-bar)`, and `PremiumButton` consumes `glow` before spreading DOM props. Browser QA verified fixed bottom tab metrics, normal Settings click activation, authenticated `/admin/diagnostics`, no horizontal overflow, `badResponses=[]`, and `unexpectedConsoleErrors=[]`. Verification passed focused Node tests (`56 passed`), ESLint, Hanwoo project QC (`518 passed`, lint/build/smoke passed), diff-check, code-review gate PASS (`risk_score=0.0`), and A/B `adopt_candidate` (`score_delta=0.7692307692307693`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1442 Blind-to-X ready-source warning CLI visibility in local commit `93ba07bd`. After T-1440, `summary.ready_warning_count` and `summary.ready_warnings` made usable-source diagnostics visible in JSON but not in normal CLI operator logs. `pipeline/cli.py` now logs compact ready-source warning summaries immediately after source preflight returns, including source, warning type, count, sample, and action while preserving exit-code and recommended-source behavior. Live CLI preflight with `--source multi --source-preflight --source-preflight-click-through` verified `ready_count=2`, `problem_count=2`, `ready_warning_count=1`, `recommended_source=ppomppu`, and emitted `Source preflight ready warning: source=ppomppu ...`. Verification passed focused main tests (`39 passed`), related source/main tests (`67 passed`), `py_compile`, Ruff check/format, live CLI preflight, blind-to-x project QC (`1830 passed`, `9 skipped`, lint passed), diff-check, code-review gate advisory WARN (`risk_score=0.35`) covered by focused/related/live/project gates, and A/B `adopt_candidate` (`score_delta=0.7142857142857143`). No push was performed and T-251 was not retried. Unrelated Hanwoo WIP in `globals.css`, `premium-button.js`, `home-market-copy.test.mjs`, and `premium-button-semantics.test.mjs` was preserved outside the Blind-to-X commit.

- 2026-06-07: Completed T-1441 Hanwoo degraded dashboard tab preload guard in local commit `1d450f75`. Baseline authenticated dashboard tab-order QA at 390px showed all eight tabs still activated and stayed within the viewport, but tab preloading emitted four `/api/dashboard/cattle?limit=100` 500 responses in the known external Supabase degraded state. External/current research checked WAI-ARIA APG tabs guidance: tab panels should display without noticeable latency, typically through preloaded content; in this degraded case the correct preload is the already-known empty fallback, not another failed request. The candidate seeds `allCattleRegistry` and `allSalesLedger` as `[]` when their initial sections are degraded, preserving usable empty tab panels while eliminating repeated failing full-list fetches. Browser QA verified 9 tab snapshots, active tab progression, no click failures, no horizontal overflow, `badResponses=[]`, and screenshot `output/playwright/hanwoo-t1440-dashboard-tabs-candidate.png`. Verification passed focused Node tests (`55 passed`), Hanwoo project QC (`516 passed`, lint/build/smoke passed), diff-check, code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.5882352941176471`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1440 Blind-to-X ready-source preflight warnings in local commit `736e548b`. Baseline source-browser preflight hid a Ppomppu console `424 Failed Dependency` inside per-result details even though the source was usable and recommended. The candidate adds `summary.ready_warning_count` and `summary.ready_warnings` for ready sources with console/page errors while preserving `ready_count=2`, `problem_count=2`, and `recommended_source=ppomppu` in live all-source click-through proof. Verification passed focused source-browser tests (`28 passed`), related source/main tests (`66 passed`), `py_compile`, Ruff check/format, live all-source preflight, blind-to-x project QC (`1829 passed`, `9 skipped`, lint passed), diff-check, code-review gate advisory WARN (`risk_score=0.35`) covered by focused/live/project gates, and A/B `adopt_candidate` (`score_delta=0.42857142857142855`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1438b Shorts Maker V2 responsive dashboard browser QA in local commit `70cb64dd` (commit subject uses T-1438 due a concurrent Hanwoo context/code bundle). Baseline CLI browser QA showed the tracked dashboard sample overflowed horizontally at 390px and emitted a favicon 404; the candidate adds viewport metadata, a data favicon, constrained main layout, responsive summary-card grid, accessible scroll-contained table behavior, and a full empty-dashboard HTML shell to both the generator and tracked sample. Verification passed focused dashboard pytest (`13 passed`), Ruff check/format, `py_compile`, venv CLI dashboard generation, shorts-maker-v2 project QC (`1639 passed`, `12 skipped`, `29 warnings`, lint passed), browser QA with no horizontal overflow/no mojibake/console warnings-errors 0, diff-check, code-review gate advisory WARN (`risk_score=0.40`) covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=1.125`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1439 Hanwoo authenticated home DB-degraded shell in local commit `0a9a817c`. Baseline browser QA showed authenticated `/` dropped into the generic error boundary on the known external Supabase credential signature. The candidate adds `initial-data-fallback.mjs`, wraps initial home loaders in `page.js`, passes `initialDataLoadStatus` to `DashboardClient`, renders a degraded-storage status banner with refresh, skips field-mode full-cattle preload when cattle data is degraded, and fixes mobile overflow in the header controls plus the 8-item bottom tab bar. Browser QA verified the dashboard shell and field mode remain usable at 390px with no horizontal overflow and no extra cattle API 500 on field-mode entry. Verification passed focused Node tests (`67 passed`), Hanwoo project QC (`516 passed`, lint/build/smoke passed), diff-check, and A/B `adopt_candidate` (`score_delta=0.9476762080640198`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1437 Blind-to-X recommended source fallback guard in local commit `e8dd0c47`. A baseline regression showed `_apply_recommended_source_fallback()` would apply a `recommended_source` not present in `ready_sources`; the candidate now rejects that malformed report while preserving valid recommended-source continuation. Verification passed focused main tests (`38 passed`), related source/main tests (`65 passed`), live CLI source preflight with click-through (`ready_sources=ppomppu,jobplanet`; blocked Blind `403` and FMKorea `430`; recommended Ppomppu), Ruff, `py_compile`, diff-check, blind-to-x project QC (`1828 passed`, `9 skipped`, lint passed), code-review gate advisory WARN (`risk_score=0.40`) covered by focused/related/live/project gates, and A/B `adopt_candidate` (`score_delta=0.4559659090909091`). No push was performed and T-251 was not retried; unrelated Hanwoo/shorts WIP was preserved.

- 2026-06-07: Completed T-1436 Knowledge Dashboard smoke fixture isolation follow-up in local commit `9867c58d`, after T-1435 landed as `108c2350`. `scripts/smoke.mjs` now keeps smoke fixtures out of `.next/standalone` build output and writes only to the runtime `data/` directory, while `smoke-script.test.mts` rejects `BASE_URL` globals and standalone fixture writes. Verification passed `node --check`, focused smoke-script test (`1 passed`), Knowledge project QC (`62 passed`, lint/build/smoke passed), full active-project QC at `9867c58d`, and readiness score `96` with local blockers `0`, publish blockers `1`, external blockers `1`. No push was performed and T-251 was not retried.

- 2026-06-07: T-1435 Knowledge Dashboard smoke port isolation landed in local commit `108c2350`. The smoke script now uses an OS-assigned free localhost port unless `SMOKE_PORT` is explicitly set and uses the resolved per-run base URL for all smoke requests. The added smoke source guard prevents reintroducing a fixed default shared port. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1434 Blind-to-X source preflight command and JobPlanet detail fallback in local commit `6af1959c`. The candidate makes `summary.recommended_command` use the active Python interpreter plus explicit project/config/output paths and makes JobPlanet API click-through skip short first candidates in favor of the first readable detail payload. Verification passed focused source-browser tests (`27 passed`), `py_compile`, Ruff check, diff-check, staged code-review gate PASS (`risk_score=0.0`) plus pre-commit advisory WARN (`risk_score=0.55`) covered by focused/live/project gates, live JobPlanet click-through preflight (`ready_count=1`), and current-head full active-project QC at `6fef602a` (blind-to-x `1827 passed`, `9 skipped`; shorts-maker-v2 `1637 passed`, `12 skipped`, `29 warnings`; Hanwoo `513 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable). Product readiness stayed score `96` with local blockers `0`, publish blockers `1`, and external blockers `1`. No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1433 Hanwoo payment success callback identifier validation in local commit `8f999a6f`. Current TossPayments docs confirm success redirects carry `paymentKey`, `orderId`, and `amount`; `orderId` is merchant-generated 6-64 `[A-Za-z0-9_-]`, and `paymentKey` max length is 200. The candidate adds shared payment identifier normalization, blocks invalid success callback identifiers before the confirm API call, and normalizes direct confirm POST identifiers before DB/Toss work. Browser QA verified the invalid-id recovery message, retry href `/subscription`, zero `/api/payments/confirm` requests, no mobile horizontal overflow, and screenshot `output/playwright/hanwoo-t1433-invalid-success-callback.png`. Verification passed `node --check`, focused payment tests (`27 passed`), Hanwoo project QC (`513 passed`, lint/build/smoke passed), diff-check, code-review gate advisory WARN (`risk_score=0.55`) covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.8571428571428571`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1432 Hanwoo payment failure support order id in local commit `a2e6c17c`. Current TossPayments docs confirm failed payment redirects can include `code`, `message`, and `orderId`; the candidate validates optional `orderId` values and displays a support order number only for safe IDs while keeping provider `message` text hidden. Browser QA verified the valid support order-id label renders, unsafe order IDs stay hidden, provider messages stay hidden, retry remains visible, no horizontal overflow, no replacement characters, console warnings/errors `0`, and screenshot `output/playwright/hanwoo-t1432-payment-fail-order-id-candidate.png`. Verification passed `node --check`, Hanwoo Node tests (`512 passed`), ESLint, diff-check, Hanwoo project QC (`512 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.35`) covered by source/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.5`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1431 Blind-to-X missing Playwright source-preflight action in local commit `dc041372`. Baseline root-Python all-source click-through preflight failed all 4 sources as generic `browser_error` with `No module named 'playwright'`. The candidate classifies missing `playwright` package/submodule imports as `browser_unavailable` and gives operators a concrete virtualenv/install recovery path. README and SOP docs now point standalone source preflight to the project venv and `python -m playwright install chromium`. Candidate root-Python preflight reports 4 actionable setup failures, while project-venv live preflight still keeps JobPlanet/Ppomppu ready, Blind `403` and FMKorea `430` blocked, and recommends Ppomppu. Verification passed focused source-browser tests (`26 passed`), related source/main tests (`63 passed`), `py_compile`, Ruff check/format check, diff-check, live all-source click-through preflight, blind-to-x project QC (`1826 passed`, `9 skipped`, lint passed), and A/B `adopt_candidate` (`score_delta=2.9033816425120773`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1430 Hanwoo legal document top return action in local commit `21725a4e`. Baseline mobile QA found the only legal return link at `1632px`, outside the first viewport. The candidate adds a top `LegalReturnLink` under the legal header and preserves the bottom return link. Browser QA verified first-viewport top link at `260px`, two return links, safe login callback preservation, unsafe callback downgrade to `/login`, top-link click to `/login?callbackUrl=%2Fsubscription#login`, no horizontal overflow, no replacement characters, console warnings/errors `0`, and screenshot `hanwoo-t1430-legal-top-return-candidate.png`. Verification passed `node --check`, Hanwoo Node tests (`512 passed`), ESLint, diff-check, Hanwoo project QC (`512 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.30`) covered by source/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.55`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1429 Health Check Dashboard literal detail rendering in local commit `9cfd11a1`. Health-check detail values now escape embedded backticks and render as inline code so filesystem paths and diagnostics stay literal in `st.markdown()`. Browser QA on `http://127.0.0.1:8787` verified HTTP `200`, visible page, no horizontal overflow, no replacement characters, console/page/request errors `0`, screenshot `output/playwright/health-dashboard-t1429-detail-literal-candidate.png`, and empty server stderr. Verification passed `py_compile`, focused pytest (`4 passed`), Ruff check/format check, diff-check, and code-review gate PASS (`risk_score=0.0`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1428 Health Check Dashboard Streamlit width API refresh in local commit `c7754abb`. `health_check_dashboard.py` now uses guarded workspace-root path setup and `st.button(..., width="stretch")` through `_stretch_button_kwargs()` instead of deprecated `use_container_width=True`; `test_health_check_dashboard_page.py` covers the path guard, width helper, and source guard. Browser QA on `http://127.0.0.1:8786` verified HTTP `200`, nonblank page text, `3` buttons, no mobile overflow, console/page errors `0`, and no deprecated-width log matches. Verification passed `py_compile`, focused pytest (`3 passed`), Ruff check/format check, diff-check, and code-review gate PASS (`risk_score=0.0`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1427 Hanwoo login legal callback preservation in local commit `9ea59841`. Login-origin legal links now preserve safe protected callbacks through `/terms` and `/privacy`, and legal return links validate the callback again before returning to `/login?callbackUrl=%2Fsubscription#login`; unsafe external callbacks fall back to `/login`. Browser QA verified preserved legal hrefs, terms return href, unsafe callback blocking, no horizontal overflow, no replacement characters, and no new console warnings/errors after clearing edit-time Fast Refresh logs. Verification passed `node --check`, Hanwoo Node tests (`512 passed`), ESLint, cached diff-check, Hanwoo project QC (`512 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.50`) covered by source/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.45`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1425 VibeDebt Dashboard trend chart hardening in local commit `6b7341d4`. Followed T-1423 by adding `_build_trend_chart_frame()` to coerce timestamps, drop malformed/null trend rows, and skip empty chart renders while preserving `_render_line_chart(..., width="stretch")`. `test_debt_dashboard_page.py` now has 4 focused tests covering width API, empty chart skip, datetime-index frame building, and the source guard. Final browser QA screenshot `output/playwright/debt-dashboard-t1423-width-refresh-followup.png` is nonblank (`1280x720`, `1659` colors), candidate HTTP was `200`, and server warning/error output was `0`. Verification passed `py_compile`, focused pytest (`3 passed, 1 skipped`), Ruff check/format check, path-limited diff-check, no-deprecated-width source scan, shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.7272727272727273`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1424 Hanwoo payment failure retry pending state in local commit `d73b338e`. `subscription/fail/page.js` now gives the retry CTA a Korean pending status, duplicate-click guard, disabled/busy semantics, dynamic label/title, wait cursor, reduced opacity, and visible pending copy while preserving protected `/subscription` document navigation. `payment-ux-copy.test.mjs` locks the source contract. Browser QA on `/subscription/fail?code=PAY_PROCESS_ABORTED&message=network%20interrupted&orderId=order_retry_busy` verified no horizontal overflow or replacement characters, no visible provider-message leak, pre-click `aria-busy="false"`, retry URL `/login?callbackUrl=http%3A%2F%2F127.0.0.1%3A3001%2Fsubscription#login`, and console warnings/errors `0`. Verification passed `node --check`, Hanwoo Node tests (`512 passed`), ESLint, diff-check, Hanwoo project QC (`512 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.30`) covered by source/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.6`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1423 VibeDebt Dashboard Streamlit line chart width refresh in local commit `80d6b0ea`. `debt_dashboard.py` now routes all line charts through `_render_line_chart()` with `width="stretch"`, removing the remaining deprecated `use_container_width=True` call from the technical-debt dashboard surface. `test_debt_dashboard_page.py` locks helper behavior and the source guard. CLI browser QA captured nonblank baseline/candidate screenshots (`1280x720`, candidate `146` colors), candidate HTTP `200`, and server warning/error output `0`. Verification passed `py_compile`, focused pytest (`2 passed`), Ruff check/format check, path-limited diff-check, no-deprecated-width source scan, shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), code-review gate PASS (`risk_score=0.0`), and A/B `adopt_candidate` (`score_delta=1.0`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1422 Hanwoo subscription success missing-parameter recovery in local commit `b9e7fe3c`. `subscription/success/page.js` now derives missing Toss redirect parameters into a visible Korean recovery state and protected `/subscription` retry link without synchronous effect `setState`; `payment-ux-copy.test.mjs` locks the source contract. Browser QA on direct no-query `/subscription/success` verified protected login callback integrity, no horizontal overflow, no replacement characters, and console warnings/errors `0`. Verification passed `node --check`, Hanwoo Node tests (`512 passed`), ESLint, diff-check, Hanwoo project QC (`512 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.40`) covered by source/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.7`). No push was performed and T-251 was not retried.

- 2026-06-07: Completed T-1422b Blind-to-X source preflight all/json CLI compatibility in local commit `51e4f83d`. `source_browser_probe.py` now accepts explicit all-source aliases `all`, `auto`, and `multi`, dedupes alias-expanded sources, and accepts `--json` as a no-op compatibility flag. README and `directives/x_content_curation.md` document `python scripts/source_browser_probe.py --source all ...`. Live preflight with `--source all --click-through --json` verified 4 source targets, kept JobPlanet/Ppomppu ready, classified Blind `403` and FMKorea `430` as blocked with problem actions, and recommended `ppomppu`. Verification passed focused tests (`23 passed`), `py_compile`, Ruff check/format check, diff-check, live all-source click-through preflight, blind-to-x project QC (`1823 passed`, `9 skipped`, lint passed), code-review gate advisory WARN (`risk_score=0.40`) covered by focused/live/project gates, and A/B `adopt_candidate` (`score_delta=1.6`). No push was performed and T-251 was not retried. The concurrent Hanwoo WIP from that cycle was later completed as T-1422.

- 2026-06-07: Completed T-1421 Hanwoo legal dashboard return document navigation in local commit `9ab08546`. `LegalReturnLink.js` now marks dashboard return targets with `requiresDocumentNavigation: true` and renders them as plain `<a href="/">` controls so protected-dashboard redirects preserve `/login?...#login`; login return targets still use `next/link`. `legal-pages-copy.test.mjs` locks the contract. Browser QA on `/terms?returnTo=dashboard` at 390px verified `tagName=A`, `href=/`, final URL `/login?callbackUrl=http%3A%2F%2F127.0.0.1%3A3001%2F#login`, login section and username field present, no horizontal overflow or replacement characters, console warnings/errors `0`, and screenshot `output/playwright/hanwoo-t1421-legal-return-document-nav-candidate.png`. Verification passed Hanwoo Node tests (`511 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`511 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.30`) covered by source/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.55`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1420 Hanwoo login credential error focus recovery in local commit `8dd1c59f`. `login/page.js` now keeps a username input ref, focuses `#login-username` after credential/network login failures, and links both credential inputs to `#login-error-message` through `aria-errormessage` while preserving `aria-describedby`. `error-pages-wiring.test.mjs` locks the source contract. Browser QA on `/login#login` at 320px verified `activeElement.id=login-username`, both fields expose `aria-describedby`/`aria-errormessage`, both `aria-invalid=true`, no horizontal overflow or replacement characters, auth network endpoints `200`, console warnings/errors `0`, and screenshot `output/playwright/hanwoo-t1420-login-error-focus-candidate.png`. Verification passed Hanwoo Node tests (`511 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`511 passed`, lint/build/smoke passed), browser QA inventory fresh/nonblank, code-review gate advisory WARN (`risk_score=0.50`) covered by source/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.8000000000000002`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1419 Blind-to-X X weighted character length in local commit `626acbb3`. `regulation_checker.py` now validates X draft length with `x_weighted_character_count()` instead of raw `len()`, covering Korean/CJK, non-Latin defaults, URL 23-unit weights, NFC normalization, emoji modifiers, ZWJ emoji sequences, and regional-indicator pairs. `test_regulation_checker.py` locks the official X edge cases and rejects 141 Korean characters as 282 weighted characters. Verification passed focused regulation tests (`24 passed`), related tests (`61 passed`), `py_compile`, Ruff check/format check, path-limited diff-check, blind-to-x project QC (`1819 passed`, `9 skipped`, lint passed), code-review gate advisory WARN (`risk_score=0.65`) covered by focused/related/project gates, and A/B `adopt_candidate` (`score_delta=3.6`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1418 Hanwoo payment failure code-specific recovery copy in local commit `f0b534cc`. `subscription/fail/page.js` now normalizes missing codes to `미전달`, maps known Toss failure codes to safe Korean recovery messages, and keeps provider `message` query text out of the UI. `payment-ux-copy.test.mjs` locks the source contract. Browser QA on `/subscription/fail` at 320px verified `오류 코드: 미전달`, `PAY_PROCESS_CANCELED` cancellation-specific Korean copy, no provider-message leak, retry to `/login?callbackUrl=http%3A%2F%2F127.0.0.1%3A3001%2Fsubscription#login`, no horizontal overflow or replacement characters, and console warnings/errors `0`. Verification passed Hanwoo Node tests (`510 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.30`) covered by source/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.5`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1417 Hanwoo public 404 recovery login fragment preservation in local commit `b5de11cb`. `not-found.js` and `error.js` now use intentional document navigation for protected dashboard recovery links so Auth.js proxy redirects own the final `/login?...#login` URL. `error-pages-wiring.test.mjs` locks no `next/link` regression plus the ESLint exception rationale. Browser QA on `/privacy-missing` verified the recovery click lands on `/login?callbackUrl=http%3A%2F%2F127.0.0.1%3A3001%2F#login`, preserves same-origin callback `/`, renders the login section/form, has no horizontal overflow or replacement characters, and reports console warnings/errors `0`. Verification passed Hanwoo Node tests (`510 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.30`) covered by direct/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.7`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1416 Performance Overview single-row chart fallback in local commit `cdd941eb`. Added `_has_multiple_rows()` and guarded publishing/API-cost trend chart rendering so a single data point shows an explanatory `st.info()` plus dataframe instead of a misleading one-point line/bar chart. `workspace/tests/test_performance_overview_page.py` covers the row-range helper and source guards for both branches. Verification passed `py_compile`, focused/related pytest (`11 passed`), Ruff check/format check, path-limited diff-check, no deprecated width usage, browser QA on `http://127.0.0.1:8783` (`HTTP 200`, title/KPI/API-cost/platform visible, one single-row message, console/page/network errors `0`, no horizontal overflow, nonblank screenshot `output/playwright/performance-overview-t1416-single-row-guard.png`), shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), and code-review gate advisory WARN (`risk_score=0.35`) covered by direct/browser/project gates. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1415 Blind-to-X source preflight operator action summary in local commit `f875b1da`. Added `summary.recommended_command` and `summary.problem_actions` to source browser preflight reports so operators get a guarded next-run command and per-source next steps without scanning raw results. Live click-through preflight kept `jobplanet`/`ppomppu` ready, kept Blind `403` and FMKorea `430` blocked, recommended `ppomppu`, and emitted fallback actions for both blocked sources. Verification passed focused source-browser pytest (`19 passed`), CLI source-preflight pytest (`11 passed`, `26 deselected`), Ruff check/format check, `py_compile`, path-limited diff-check, live click-through preflight, blind-to-x project QC (`1817 passed`, `9 skipped`, lint passed), code-review gate advisory WARN (`risk_score=0.35`) covered by focused/live/project gates, and A/B `adopt_candidate` (`score_delta=1.0666666666666667`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1414 Performance Overview Streamlit width API refresh in local commit `6b1e9005`. `workspace/execution/pages/performance_overview.py` now uses a conditional workspace-root `sys.path` insert before `execution.*` imports and helper wrappers for `st.line_chart`, `st.bar_chart`, and `st.dataframe` with `width="stretch"`. Added `workspace/tests/test_performance_overview_page.py` for import-path, helper, source-guard, and watchdog-history parsing regressions. Browser baseline showed the page loaded but emitted Streamlit `use_container_width` deprecation warnings; candidate browser QA kept the page usable with visible title/KPI/API-cost/platform sections, no console/page/network errors, no horizontal overflow, nonblank screenshot, and no deprecated-width warning. Verification passed `py_compile`, focused/related pytest (`10 passed`), Ruff check/format check, path-limited diff-check, no-deprecated-width `rg`, shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), code-review gate advisory WARN covered by direct/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.5`). No push was performed and T-251 was not retried. Note: live `HEAD` later moved to Hanwoo T-1413 commit `c2a13a3a`.

- 2026-06-06: Completed T-1413 Hanwoo password visibility toggle pressed-state polish. Added `aria-pressed={showPassword}` to `.login-password-toggle`, preserving existing Korean label/title copy and password input type toggling. Browser QA verified `aria-pressed`/`Element.ariaPressed` changed `false -> true -> false`, `type` stayed `password -> text -> password`, no horizontal overflow, and console warnings/errors `0`. Verification passed Hanwoo Node tests (`510 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.50`) covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=1.3333333333333333`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1412 Hanwoo mobile login keyboard hint polish. Added `enterKeyHint="next"` to the username input and `enterKeyHint="go"` to the password input, preserving autocomplete and existing validation behavior. Browser QA verified the candidate DOM values, submit readiness, no horizontal overflow, and console warnings/errors `0`. Verification passed Hanwoo Node tests (`510 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.50`) covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.8888888888888888`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1411 Hanwoo payment failure long-code wrapping. Added `overflowWrap: "anywhere"` to the public payment failure error-code paragraph and source-contract coverage. Browser QA verified the long-code case no longer expands the document beyond a 390px viewport, keeps the retry CTA visible, and reports console warnings/errors `0`. Verification passed focused payment UX tests (`10 passed`), related payment/error-page tests (`23 passed`), `node --check`, ESLint, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.30`) covered by focused/browser/project gates, and A/B `adopt_candidate` (`score_delta=0.6995687276061108`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1410 Debug History import and Plotly width refresh. Browser baseline rendered only `No module named 'execution'`; the page now inserts `Path(__file__).resolve().parents[2]`, imports `execution.debug_history_db` successfully, and uses `_render_plotly_chart()` with `st.plotly_chart(fig, width="stretch")` for both pie charts. Added `test_debug_history_page.py` with fake Streamlit/Plotly/DB modules to lock import-path, helper, and source guards. Verification passed `py_compile`, focused/related pytest (`34 passed`), Ruff check/format check, path-limited diff-check, no deprecated Debug History Plotly width usage, browser QA with module error absent and nonblank screenshot, shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), code-review gate advisory WARN (`risk_score=0.30`) covered by direct/browser/project gates, and A/B `adopt_candidate` (`score_delta=3.75`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1409 Blind-to-X recommended source continuation. `pipeline/cli.py` adds `--source-preflight-use-recommended` and applies `summary.recommended_source` only when `--require-source-ready` is active and a non-empty recommendation exists, preserving report-only preflight behavior. `test_main.py` covers parser wiring, helper guardrails, the async multi-source preflight path that continues with `ppomppu` when Blind is blocked but the report recommends a ready source, and lock cleanup after a handled single command. README and SOP docs now show the guarded multi-source command. Verification passed focused CLI preflight pytest (`15 passed`, `22 deselected`), full `test_main.py` (`37 passed`), source preflight related pytest (`16 passed`, `40 deselected`), Ruff check/format check, `py_compile`, diff-check, live guarded CLI smoke (`recommended_source=ppomppu`, ready `ppomppu`/`jobplanet`, blocked Blind `403` and FMKorea `430`, lock removed), blind-to-x project QC (`1817 passed`, `9 skipped`, lint passed), code-review gate advisory WARN (`risk_score=0.35`) covered by focused/live/project gates, and A/B `adopt_candidate` (`score_delta=0.9247594096222289`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1408 Hanwoo login legal link separation in local commit `6757d373`. Baseline browser QA found `/login` legal links could render as adjacent text when `.login-legal-links` CSS was missing from the current dev stylesheet. Added fallback flex/gap utility classes, an aria-hidden middle-dot separator, and inline-flex/min-height link styling; browser QA verified separated text, flex gap, no overflow, no replacement chars, and console warnings/errors `0`. Verification passed focused Node tests (`14 passed`), `node --check`, ESLint with CSS ignore warning only, path-limited diff-check, Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.35`) covered by focused/browser/project gates, W3C WCAG 2.2 Target Size review, and A/B `adopt_candidate` (`score_delta=7.941174675436581`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1406 API Monitor Plotly width API refresh in local commit `b280e50a`. Baseline source scan showed `workspace/execution/pages/api_monitor.py` still had 2 deprecated Plotly `use_container_width=True` calls in blind-to-x cost sections while earlier charts already used `width="stretch"`. The page now centralizes all 7 Plotly calls through `_render_plotly_chart()` with `st.plotly_chart(fig, width="stretch")`, and `workspace/tests/test_api_monitor_page.py` covers helper behavior plus a source guard. Verification passed focused pytest (`2 passed`), related pytest (`47 passed` across API Monitor/API usage/Cost Dashboard), Ruff check, Ruff format check, `py_compile`, path-limited diff-check, direct Python Playwright Streamlit QA (`HTTP 200`, title visible, 2 Plotly charts rendered, console/page/serious-network errors `0`, server warning/error/deprecated matches `0`, nonblank screenshot), shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), official Streamlit docs check, code-review gate advisory WARN (`risk_score=0.35`) covered by direct tests/browser/QC, and A/B `adopt_candidate` (`score_delta=1.8148148148148149`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1405 Hanwoo legal document Korean eyebrow copy in local commit `0336fecf`. Baseline browser click QA from `/login` verified the T-1401 legal links worked, then found `/terms` and `/privacy` still used English eyebrow copy (`Terms of Service`, `Privacy Policy`) above Korean legal documents. Updated the terms eyebrow to `서비스 이용약관`, the privacy eyebrow to `개인정보 보호 안내`, and the legal-page source contract test to require Korean eyebrows and reject the old English strings. Verification passed focused Node tests (`14 passed`), `node --check`, ESLint, path-limited diff-check, direct mobile Playwright click QA (`390x844`, both links navigated, Korean eyebrows present, English legal eyebrows absent, no replacement characters, no horizontal overflow, console warnings/errors `0`), Hanwoo project QC (`510 passed`, lint/build/smoke passed), code-review gate advisory WARN (`risk_score=0.35`) with no fail, W3C WCAG 2.2 Language of Parts review, and A/B `adopt_candidate` (`score_delta=1.0`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1404 Blind-to-X source preflight recommendation evidence ranking. `source_browser_probe.py` now computes `summary.recommended_source` from the ready result with the strongest successful click-through detail body, falling back to listing `body_chars` only when no successful click evidence is available, while preserving `summary.ready_sources` order for operator visibility. Added regression coverage for the JobPlanet API detail vs Ppomppu HTML detail case, and updated README/SOP wording. Verification passed focused pytest (`19 passed`), CLI preflight pytest (`9 passed`, `23 deselected`), Ruff check/format check, `py_compile`, path-limited diff-check, live all-source click-through preflight (`recommended_source=ppomppu`, `ready_count=2`, `problem_count=2`, existing Blind `403` and FMKorea `430` blocked responses), blind-to-x project QC (`1812 passed`, `9 skipped`, lint passed), and A/B `adopt_candidate` (`score_delta=1.0055555555555555`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1403 Channel Growth import and Plotly width refresh. Baseline browser QA on `http://127.0.0.1:8772` rendered only `Channel Growth 모듈을 불러올 수 없습니다: No module named 'execution.channel_growth_tracker'`. `workspace/execution/pages/channel_growth.py` now uses `WORKSPACE_ROOT = Path(__file__).resolve().parents[2]`, inserts that root into `sys.path`, and centralizes Plotly rendering in `_render_plotly_chart()` with `st.plotly_chart(fig, width="stretch")`, removing all 3 deprecated Plotly `use_container_width=True` calls. `workspace/tests/test_channel_growth_page.py` covers import-path, helper, and source guard behavior. Verification passed focused/related pytest (`3 passed`, `13 passed`, `12 passed`), Ruff check, Ruff format check, `py_compile`, `rg` no deprecated Plotly width calls in `channel_growth.py`, path-limited diff-check, direct Streamlit/Playwright QA with a UI-added probe channel (`3` Plotly charts rendered, console warnings/errors `0`, server warning/error/deprecated matches `0`, nonblank screenshot `output/playwright/channel-growth-t1403-import-width-refresh.png`), shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), official Streamlit docs check, code-review gate PASS (`risk_score=0.0`), and A/B `adopt_candidate` (`score_delta=1.6`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1402 Blind-to-X JobPlanet API detail source preflight. Live all-source `scripts/source_browser_probe.py --click-through` showed JobPlanet feed API was HTTP 200/readable but incorrectly failed as `click_error` because the API feed has no HTML anchors. `projects/blind-to-x/scripts/source_browser_probe.py` now routes JobPlanet click-through verification through the same source-specific detail API used by the scraper: parse first `data.items[].id`, record the public `/community/posts/{id}` candidate URL, navigate to `/api/v5/community/posts/{id}`, and require readable detail content before keeping the source ready. HTML sources still use visible-anchor click verification. `projects/blind-to-x/tests/unit/test_source_browser_probe.py` covers JobPlanet candidate extraction, API detail success, and missing-id failure. README and `directives/x_content_curation.md` document the API-backed detail endpoint behavior. Verification passed focused pytest (`18 passed`), CLI source-preflight pytest (`10 passed`, `22 deselected`), Ruff check, Ruff format check, `py_compile`, path-limited diff-check, live all-source click-through preflight (`ready_count=2`, `problem_count=2`, `ready_sources=["jobplanet","ppomppu"]`, `recommended_source=jobplanet`, JobPlanet `ok=true`, `body_chars=77`, screenshot `screenshots/source_preflight_t1401_jobplanet_api/jobplanet-click.png`), blind-to-x project QC (`1811 passed`, `9 skipped`, lint passed), and A/B `adopt_candidate` (`score_delta=1.0999999999999999`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1401 Hanwoo login legal document links. `projects/hanwoo-dashboard/src/app/login/page.js` now renders `/terms` and `/privacy` with `next/link` inside `nav.login-legal-links`, preserving concise visible Korean labels while exposing explicit accessible labels/titles. `projects/hanwoo-dashboard/src/app/globals.css` adds compact wrapping login-card styling and focus-visible treatment. `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` covers the source contract. Verification passed full Hanwoo Node tests via the focused command (`510 passed`), ESLint, path-limited diff-check, direct Playwright browser QA on desktop `1280x900` and mobile `390x844` (`/terms` and `/privacy` navigation paths verified, links inside card, no overlap, no horizontal overflow, console warnings/errors `0`, page errors `0`), Hanwoo project QC (`test`, `lint`, `build`, `smoke` passed), staged code-review gate advisory WARN (`risk_score=0.50`) covered by source/browser/project tests, and A/B `adopt_candidate` from `.tmp/ab-manifest-hanwoo-t1401.json` (`score_delta=1.25`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1400 Blind-to-X source preflight actionable summary. `projects/blind-to-x/scripts/source_browser_probe.py` now builds summary payloads through `_build_summary()` and adds `ready_sources`, `problem_sources`, and `recommended_source` so operators do not need to scan raw `results` to pick a usable source. `projects/blind-to-x/tests/unit/test_source_browser_probe.py` covers mixed ready/problem reports, click-error problem summaries, and preflight output recommended source. README and `directives/x_content_curation.md` now document the JSON fields. Verification passed focused pytest (`15 passed` with repo-local basetemp after default temp `WinError 5`), CLI preflight pytest (`10 passed`, `22 deselected`), Ruff check, Ruff format check, `py_compile`, path-limited diff-check, direct Ppomppu source preflight with click-through (`ready_count=1`, `problem_count=0`, `recommended_source=ppomppu`, click body_chars `801`, screenshot `screenshots/source_preflight_t1400_actionable/ppomppu-click.png`), blind-to-x project QC (`1808 passed`, `9 skipped`, lint passed), staged code-review gate advisory WARN (`risk_score=0.40`) covered by tests/live/project QC, and A/B `adopt_candidate` (`score_delta=1.1764705882352942`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1398 Hanwoo payment retry login anchor preservation in local commit `beaf361e`. Browser baseline showed `/subscription/fail` rendered correct Korean failure copy, but clicking retry reached `/login?callbackUrl=.../subscription` without `#login`. `src/app/subscription/fail/page.js` now uses `window.location.assign(PAYMENT_RETRY_PATH)` instead of `router.push(PAYMENT_RETRY_PATH)`, and `src/lib/payment-ux-copy.test.mjs` guards against reintroducing `useRouter`/`router.push` on this retry path. Browser QA verified final URL `/login?...#login`, login anchor target exists, hash/target match is `true`, callback remains `/subscription`, console warnings/errors `0`, and committed screenshot `output/playwright/hanwoo-t1398-payment-retry-login-anchor.png`. Verification passed focused Node tests (`25 passed`), `node --check`, ESLint, diff-check, full Hanwoo tests (`510 passed`), Hanwoo project QC (`test`, `lint`, `build`, `smoke`), code-review gate advisory WARN (`risk_score=0.40`) covered by focused/full/browser/project tests, and A/B `adopt_candidate` (`score_delta=0.7692307692307693`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1399 Cost Dashboard Plotly width API refresh. `workspace/execution/pages/cost_dashboard.py` now centralizes Plotly rendering in `_render_plotly_chart()` and removes all 7 deprecated `use_container_width=True` chart calls, including the shorts-maker-v2 cost trend chart. `workspace/tests/test_cost_dashboard.py` covers the helper contract and source-level deprecated-width guard. Verification passed focused/related pytest (`6 passed` across `test_cost_dashboard.py` and `test_result_roi_dashboards.py`), Ruff check, Ruff format check, `py_compile`, `rg` no deprecated chart-width calls in `cost_dashboard.py`, path-limited diff-check, direct Streamlit/Playwright Cost Dashboard QA (`HTTP 200`, title visible, `10` Plotly charts, console warnings/errors `0`, Streamlit log deprecation/warning/error lines `0`, nonblank screenshot `output/playwright/cost-dashboard-t1399-width-refresh.png`), full shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), and A/B `adopt_candidate` (`score_delta=2.3333333333333335`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1397 Hanwoo login fragment anchor target. `projects/hanwoo-dashboard/src/app/login/page.js` now adds `id="login"` to the login card section while preserving `aria-labelledby="login-title"`, completing the fragment target introduced by T-1394 protected-route redirects. `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` covers the source contract. Browser QA verified `/admin/diagnostics?tab=health#db` redirects to `/login?...#login`, `document.getElementById("login")` returns the `SECTION`, target/hash match is `true`, console warnings/errors are `0`, and screenshot `output/playwright/hanwoo-t1397-login-anchor.png` is committed. Verification passed focused Node tests (`15 passed`), ESLint, `node --check`, full Hanwoo tests (`510 passed`), Hanwoo project QC (`test`, `lint`, `build`, `smoke`), staged code-review gate advisory WARN (`risk_score=0.50`) covered by focused/full/browser/project tests, and A/B `adopt_candidate` (`score_delta=0.8333333333333334`). Code commit `46f37d61` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1396 Blind-to-X source preflight evidence hardening. `projects/blind-to-x/scripts/source_browser_probe.py` now emits ASCII-escaped JSON reports, waits for readable clicked-detail text, and retries Ppomppu click-throughs with canonical `/zboard/view.php?...&no=...` URLs if the clicked page remains on a `Loading ...` interstitial. `projects/blind-to-x/tests/unit/test_source_browser_probe.py` covers ASCII report output, canonical link resolution, and retry behavior. Verification passed focused pytest (`15 passed`), Ruff check, Ruff format check, `py_compile`, path-limited diff-check, live Ppomppu CLI preflight (`ready_count=1`, `problem_count=0`, clicked detail `body_chars=1753`, screenshot `screenshots/source_preflight_t1396/ppomppu-click.png`), ASCII evidence check (`AsciiOnly=true`, `HasUnicodeEscapes=true`), blind-to-x project QC (`1808 passed`, `9 skipped`, lint passed), staged code-review gate advisory WARN (`risk_score=0.50`) covered by focused/live/project tests, and A/B `adopt_candidate` (`score_delta=59.11025641025641`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1395 Shorts Analytics Plotly width API refresh. `workspace/execution/pages/shorts_analytics.py` now centralizes Plotly rendering in `_render_plotly_chart()` and calls `st.plotly_chart(fig, width="stretch")` for every chart, removing 11 deprecated `use_container_width=True` usages. `workspace/tests/test_shorts_analytics.py` adds helper-level and source-level regression coverage. Verification passed focused/related pytest (`52 passed` across `test_shorts_analytics.py` and `test_content_db.py`), Ruff check, Ruff format check, `py_compile`, `rg` no deprecated chart-width calls, path-limited diff-check, direct Streamlit/Playwright tab-click QA with console warnings/errors `0`, nonblank screenshot `output/playwright/shorts-analytics-t1395-plotly-width-refresh.png`, full shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, `29 warnings`, lint passed), and A/B `adopt_candidate` (`score_delta=1.5`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1394 Hanwoo protected-route login fragment guard in local commit `eec15103`. `projects/hanwoo-dashboard/src/auth.js` now returns a protected-route login redirect ending in `#login` so browser redirect fragment inheritance cannot carry a protected-route hash onto `/login`; `callbackUrl` still uses `getRequestHref(request, requestOrigin)` and remains hash-free. `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the source contract. Verification passed manual header check (`307`, `Location: /login?...#login`), focused Node tests (`15 passed`), ESLint, full Hanwoo tests (`510 passed`), direct Playwright browser QA for `/admin/diagnostics?tab=health#db` (`#login`, no `#db` leak, console warnings/errors `0`, screenshot `output/playwright/hanwoo-t1394-fragment-cleared.png`), Hanwoo project QC (`test`, `lint`, `build`, `smoke`), diff-check, staged code-review gate advisory WARN (`risk_score=0.40`) covered by focused/full/browser/project tests, RFC 9110 redirect-fragment research, and A/B `adopt_candidate` (`score_delta=1.2727272727272727`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1393 Blind-to-X source preflight click-through gate. `projects/blind-to-x/scripts/source_browser_probe.py` now supports optional `--click-through`, selects a real post link after a ready listing page, skips nav/ad/policy links, captures `<source>-click.png`, records a nested `click_through` result, and reports unreadable detail navigation as `click_error`. `projects/blind-to-x/pipeline/cli.py` exposes `--source-preflight-click-through`, and README/SOP docs recommend it before paid or LLM-backed runs. Verification passed focused pytest (`44 passed`), Ruff check, Ruff format check, `py_compile`, path-limited diff-check, browser QA inventory, live Ppomppu CLI preflight with detail click (`ready_count=1`, `problem_count=0`, detail `body_chars=1153`, screenshot `projects/blind-to-x/screenshots/source_preflight_t1393_cli/ppomppu-click.png`), blind-to-x project QC (`1805 passed`, `9 skipped`, lint passed), staged code-review gate advisory WARN (`risk_score=0.40`) covered by focused/live/project tests, official Playwright docs check, and A/B `adopt_candidate` (`score_delta=1.7073170731707314`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1392 Shorts Manager Streamlit width API refresh. `workspace/execution/pages/shorts_manager.py` now centralizes stretch button/form-submit sizing in `_stretch_button_kwargs()` and uses `width="stretch"` instead of deprecated `use_container_width=True` across the Shorts Manager controls. `workspace/tests/test_shorts_manager.py` locks both the helper return value and source-level absence of the deprecated API. Verification passed focused pytest (`63 passed` across `test_shorts_manager.py` and `test_content_db.py`), Ruff check, Ruff format check, `py_compile`, `rg` no deprecated width calls, path-limited diff-check, direct Streamlit/Playwright CLI QA on `http://127.0.0.1:8769` with content-tab clicks and empty-form submit, console warnings/errors `0`, and nonblank screenshot `output/playwright/shorts-manager-t1392-width-refresh.png`. A/B decision `.agents/skills/auto-research/scripts/ab_decision.py .tmp\ab-manifest-t1392.json --json` returned `adopt_candidate` with `score_delta=1.1666666666666667`. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1390 Blind-to-X scrape-quality integrity signal. `projects/blind-to-x/scrapers/base.py` now runs the existing `classify_scrape_integrity()` inside `BaseScraper.assess_quality()`, returns the verdict as `quality["integrity"]`, promotes exact scrape failure reasons such as `scrape_blocked_page` to the first quality reason, scores blocked pages as `0`, and caps non-article pages below the quality threshold. `projects/blind-to-x/tests/unit/test_scrapers_base.py` and `projects/blind-to-x/tests/unit/test_scrape_quality_rules.py` lock blocked-page, disabled-policy, default-policy, and BlindScraper export-path behavior. `projects/blind-to-x/directives/x_content_curation.md` records the browser QA lesson that manual click checks should reuse source preflight context options (`ko-KR`, desktop viewport, Chrome UA). Verification passed focused pytest (`22 passed`), adjacent scraper pytest (`75 passed`), Ruff check, Ruff format check, `py_compile`, blind-to-x project QC (`1803 passed`, `9 skipped`, lint passed), path-limited diff-check, live Ppomppu source preflight (`ready_count=1`, `problem_count=0`), direct Playwright post-link click QA (`HTTP 200`, detail URL with `no=709586`, `body_chars=1907`, screenshot `.tmp/t1390_browser_click/post_detail.png`), staged code-review gate advisory WARN (`risk_score=0.50`) covered by focused/adjacent/project tests, and A/B `adopt_candidate` (`score_delta=2.0225563909774436`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1389 Hanwoo protected-route callback origin preservation. `projects/hanwoo-dashboard/src/auth.js` now returns an explicit `NextResponse.redirect()` from `callbacks.authorized()` for unauthenticated protected routes and builds `/login?callbackUrl=...` from `x-forwarded-proto`, `x-forwarded-host`, and `host` headers instead of the Auth.js `AUTH_URL`-normalized `request.nextUrl.origin`. `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` covers the source contract. Browser QA on `http://127.0.0.1:3001/subscription/success` changed the callback host from `localhost:3001` to `127.0.0.1:3001`, with console warnings/errors `0` and screenshot `output/playwright/hanwoo-t1389-callback-origin.png`. Verification passed focused source test (`13 passed`), full Hanwoo tests (`508 passed`), ESLint, Hanwoo project QC (`508` tests, lint, build, smoke), path-limited diff-check, staged code-review gate advisory WARN (`risk_score=0.50`) covered by tests/browser QA, and A/B `adopt_candidate` (`score_delta=0.7142857142857143`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1388 ROI/Result Tracker Streamlit workflow repair. `workspace/execution/pages/roi_dashboard.py` now inserts the workspace root into `sys.path`, so `execution.roi_calculator` and `execution.result_tracker_db` resolve when Streamlit runs the page directly. `workspace/execution/pages/result_dashboard.py` and ROI rendering helpers now use Streamlit `width="stretch"` for Plotly charts, dataframes, buttons, and form submit buttons instead of deprecated `use_container_width=True`. `workspace/tests/test_result_roi_dashboards.py` covers the import-path contract and current Streamlit width API calls. Verification passed focused/related pytest (`56 passed` with repo-local basetemp), Ruff check, Ruff format check, `py_compile`, path-limited diff-check, `rg` no deprecated width usage in the two dashboards, direct Streamlit/Playwright QA on ROI and Result Tracker tab navigation with console warnings/errors `0`, nonblank screenshots `output/playwright/roi-dashboard-t1388-import-fixed.png` and `output/playwright/result-dashboard-t1388-width-refresh.png`, staged code-review gate pass (`risk_score=0.0`), and A/B `adopt_candidate` (`score_delta=0.7628205128205128`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1387 Hanwoo login password remask on submit. `projects/hanwoo-dashboard/src/app/login/page.js` now hides a revealed password as soon as login submission starts, while preserving the pre-submit visibility toggle. `projects/hanwoo-dashboard/src/lib/error-pages-wiring.test.mjs` locks the source contract. Browser QA on `http://127.0.0.1:3001/login` verified pre-submit `type=text`/`비밀번호 숨기기`, post-failure `type=password`/`비밀번호 보기`, Korean invalid-credentials alert, console warnings/errors `0`, and screenshot `output/playwright/hanwoo-t1387-login-remask.png`. Verification passed Hanwoo `npm.cmd test -- src/lib/error-pages-wiring.test.mjs` (`507 passed`), ESLint, Hanwoo project QC (`507` tests, lint, build, smoke; smoke passed after one transient Next build-lock retry), path-limited diff-check, staged code-review gate exit `0` advisory WARN (`risk_score=0.50`) covered by tests/browser QA, and A/B `adopt_candidate` (`score_delta=64.125`). No push was performed and T-251 was not retried.

- 2026-06-06: Refreshed post-T-1385 launch evidence. Full active-project QC passed and rewrote `.tmp/project_qc_runner_latest.json`: blind-to-x `1795 passed`, `9 skipped`; shorts-maker-v2 `1637 passed`, `12 skipped`, `29 warnings`; Hanwoo `507 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. `session_orient.py --json` reported clean worktree, open PRs `0`, and current code-review graph. `product_readiness_score.py --json` reported score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`. Release packet is `ready_for_authorization`, selector is `blocked_publish_only`, launch audit coverage is complete, and completion audit remains `incomplete` with `9/14` complete and `5` blocked issues. Remaining blockers are explicit push/current-head Actions and external/user-owned Hanwoo T-251.

- 2026-06-06: Completed T-1385 Shorts Analytics helper split and dataframe API refresh. `workspace/execution/pages/shorts_analytics.py` now extracts YouTube KPI summary, CPV math/table rows, CPV chart rows, Shorts revenue/ROI math, hook pattern table rows, and dataframe rendering into tested helpers while preserving the dashboard calculations. The shared dataframe helper uses Streamlit `width="stretch"` with `hide_index=True`, matching the current official `st.dataframe` API guidance. `workspace/tests/test_shorts_analytics.py` covers empty/missing metric values, CPV display labels, zero-view/cost filtering plus sorting, revenue/ROI helpers, and dataframe render arguments. Verification passed focused/related pytest (`66 passed` across analytics/content DB/manager), Ruff check, Ruff format check, `py_compile`, path-limited diff-check, code-review gate PASS (`risk_score=0.0`), Playwright CLI tab-click QA for all Analytics tabs with console warnings/errors `0`, nonblank screenshot `output/playwright/shorts-analytics-t1385-empty.png`, and A/B `adopt_candidate` (`score_delta=2.375`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1384 Blind-to-X required source readiness gate. `pipeline/cli.py` now exposes `--require-source-ready` for normal runs that must prove browser source availability before expensive pipeline work; it reuses the shared source preflight, forces fail-on-problem semantics, exits before pipeline execution on failure, and continues only after a successful preflight. Standalone `--source-preflight` still exits immediately after writing/reporting preflight evidence. `tests/unit/test_main.py` now covers the parser contract, preflight request/continue helpers, successful continuation into `execute_pipeline()`, and failure exit before pipeline work with lock cleanup. Verification passed focused pytest (`41 passed`), Ruff check, `py_compile`, path-limited diff-check, CLI unsupported-source smoke exit `1` with no leftover lock, and A/B `adopt_candidate` (`score_delta=0.833333`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1383 Shorts Manager delete-confirmation polish. Direct Streamlit/Playwright click QA on `workspace/execution/pages/shorts_manager.py` found the content queue `삭제` action was a one-click destructive DB delete. Official Streamlit state/button docs support storing persistent confirmation state in `st.session_state`, so `shorts_manager.py` now tracks `_DELETE_CONFIRM_KEY`, opens a `삭제 확인`/`취소` state on first click, clears stale pending-delete ids, and calls `delete_item()` only after explicit confirmation. `workspace/tests/test_shorts_manager.py` covers pending confirmation, cancel mismatch preservation, confirmed deletion, and stale-state cleanup. Browser QA verified add -> first delete leaves DB count `1` and shows confirmation controls, cancel keeps DB count `1` and restores `삭제`, final `삭제 확인` returns DB count `0`, console warnings/errors `0`, and screenshot `output/playwright/shorts-manager-delete-confirm-final.png`. Verification passed focused pytest (`61 passed` with repo-local `--basetemp`), Ruff check, Ruff format check, `py_compile`, path-limited diff-check, and A/B `adopt_candidate` (`score_delta=0.833333`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1382 blind-to-x CLI source preflight command. `projects/blind-to-x/pipeline/cli.py` now exposes `--source-preflight` plus fail-on-problem, timeout, output, screenshot-dir, headed, and viewport options, resolves configured sources through the existing input-source resolver, filters unsupported browser probe targets, runs the shared source preflight, releases the lock, and exits before pipeline work. `projects/blind-to-x/scripts/source_browser_probe.py` now exposes reusable `run_source_preflight()` so the standalone script and main CLI share probe/report logic. Verification passed focused pytest (`37 passed`), Ruff check, `py_compile`, path-limited diff-check, live CLI preflight for Ppomppu (`ready_count=1`, `problem_count=0`, screenshot `projects/blind-to-x/screenshots/source_preflight_t1382/ppomppu.png`), and A/B `adopt_candidate` (`score_delta=1.0`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1381 Shorts Manager empty KPI browser-QA polish. Direct Streamlit/Playwright QA on `workspace/execution/pages/shorts_manager.py` found an empty queue rendering `None / None` and `0 / 0 대기`. SQLite official docs confirm `sum()` returns `NULL` for no non-NULL rows, so `workspace/execution/content_db.py` now uses `COALESCE(SUM(...), 0)` for KPI counters; `workspace/execution/pages/shorts_manager.py` now uses `_fmt_youtube_upload_metric()` for spaced YouTube upload copy. `workspace/tests/test_content_db.py` and `workspace/tests/test_shorts_manager.py` cover the regressions. Verification passed focused pytest (`59 passed` with repo-local `--basetemp`), Ruff check, `py_compile`, path-limited diff-check, browser QA screenshot `output/playwright/shorts-manager-kpi-clean-final.png` with console warnings/errors `0`, staged code-review gate advisory WARN `risk_score=0.40` covered by focused tests/browser QA, and A/B `adopt_candidate` (`score_delta=0.8`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1380 Hanwoo legal return-link routing polish. `DashboardClient.js` now sends dashboard footer legal links to `/terms?returnTo=dashboard` and `/privacy?returnTo=dashboard`; `LegalDocumentLayout.js` renders a Suspense-wrapped client `LegalReturnLink`, and `LegalReturnLink.js` maps `returnTo=dashboard` to `/` while defaulting public/legal visitors to `/login`. `home-market-copy.test.mjs` and `legal-pages-copy.test.mjs` lock the routing and copy contract. Browser QA verified direct `/privacy` returns to `/login` without protected-root callback, dashboard-origin `/terms?returnTo=dashboard`, mobile `390x844` label layout, and console warnings/errors `0`. Verification passed full Hanwoo `npm test` (`507 passed`), Hanwoo project QC (`507` tests, lint, build, smoke), path-limited diff-check, staged code-review gate advisory WARN `risk_score=0.35` covered by browser/source/project tests, and A/B `adopt_candidate` (`score_delta=0.666667`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1379 blind-to-x source browser preflight. Added `projects/blind-to-x/scripts/source_browser_probe.py`, unit coverage, and README usage so operators can run a Playwright source-access preflight before `--source multi --popular --review-only`. Live probe captured Blind `403 blocked`, FMKorea `430 blocked`, JobPlanet Cloudflare `blocked`, and Ppomppu `ready`; independent Playwright click smoke opened a Ppomppu HOT post. Verification passed focused pytest (`9 passed`), ruff/format/py_compile, blind-to-x project QC (`1784 passed`, `9 skipped`, lint passed), VibeDebt `33.04 -> 32.88`, and A/B `adopt_candidate` (`score_delta=2.857373`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1378 shorts-maker-v2 Edge TTS timing helper split. `projects/shorts-maker-v2/src/shorts_maker_v2/providers/edge_tts_client.py` now delegates Edge stream collection, WordBoundary conversion, audio chunk writes, whisper/approximate fallback timing persistence, padding offset shifting, and plain-text audit output into focused helpers while preserving `EdgeTTSClient.generate_tts()` and `_generate_async_with_timing()` behavior. `projects/shorts-maker-v2/tests/unit/test_edge_tts_timing.py` adds direct helper coverage for WordBoundary tick conversion and saved timing offset handling. Verification passed `py_compile`, focused Edge TTS pytest (`53 passed`), related TTS pytest (`78 passed`), Ruff check, Ruff format check, path-limited diff-check, shorts-maker-v2 project QC (`1637 passed`, `12 skipped`, lint passed), staged code-review gate advisory WARN `risk_score=0.35` covered by focused/related/project tests, VibeDebt target score `35.3 -> 33.7`, and A/B `adopt_candidate` (`score_delta=0.927004`). T-251 was not retried and no push was performed.

- 2026-06-06: Completed T-1377 Hanwoo payment-failure recovery browser-QA polish. Browser baseline showed `/subscription/fail?code=PAY_PROCESS_CANCELED` redirecting to login before users could see the failure state; the adopted candidate keeps `/subscription/fail` public, preserves `/subscription` as protected, and changes retry from `router.back()` to `router.push("/subscription")`. Verification passed direct Playwright desktop/mobile QA with console warnings/errors `0`, full Hanwoo `npm test` (`507 passed`), Hanwoo project QC (`507` tests, lint, build, smoke), diff-check, staged code-review gate advisory WARN `risk_score=0.30` covered by browser/source/project tests, and A/B `adopt_candidate` (`score_delta=0.8`). T-251 was not retried and no push was performed.

- 2026-06-06: Refreshed local launch evidence after T-1374/T-1375/T-1376 and context normalization. `session_orient.py --json` reported clean worktree, open PRs `0`, and code-review graph current at the evidence head; `product_readiness_score.py --json` reported score `96`, local blockers `0`, publish blockers `1`, external blockers `1`; browser QA inventory reported `4/4` fresh usable nonblank screenshots; release packet reported `ready_for_authorization`; selector reported `blocked_publish_only`; launch audit reported complete coverage; completion audit remains `incomplete` with `9/14` complete and `5` blockers. Remaining blockers are explicit push/current-head Actions and external/user-owned T-251.

- 2026-06-06: Completed T-1376 shorts-maker-v2 whisper aligner helper split. `projects/shorts-maker-v2/src/shorts_maker_v2/providers/whisper_aligner.py` now delegates word timing normalization, WhisperX segment parsing, faster-whisper word parsing, WhisperX transcription/alignment, and faster-whisper fallback transcription to focused helpers while preserving `transcribe_to_word_timings()` behavior. `tests/unit/test_whisper_aligner.py` adds direct WhisperX alignment success and alignment failure segment fallback coverage. Verification passed `py_compile`, focused pytest (`14 passed`), related provider pytest (`62 passed`), ruff check, ruff format check, path-limited diff-check, shorts-maker-v2 project QC (`1634 passed`, `12 skipped`, lint passed), staged code-review gate advisory WARN `risk_score=0.50` covered by focused/related/project tests, VibeDebt target score `35.7 -> 14.0`, and A/B `adopt_candidate` (`score_delta=0.829837`). Commit `976fd51b` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1375 blind-to-x content-profile builder helper split. `projects/blind-to-x/pipeline/content_intelligence/builder.py` now delegates classification, optional viral boost, ML/heuristic performance scoring, rank scoring, 6D scoring, rationale merging, and profile construction into focused helpers while preserving `build_content_profile()` output. Verification passed `py_compile`, focused pytest (`34 passed`), ruff check, ruff format check, diff-check, blind-to-x project QC test (`1775 passed`, `9 skipped`) and lint; VibeDebt improved target score `32.9 -> 20.5`; A/B adopted (`score_delta=0.406027`). Code commit `05b626cf` is local only. Concurrent Hanwoo T-1374 commit `1063ee92` and shorts-maker-v2 T-1376 commit `976fd51b` were preserved as separate cycles.

- 2026-06-06: Completed T-1374 Hanwoo login/browser-QA stabilization. `next.config.mjs` now allows `127.0.0.1` as a Next dev origin for Codex/Playwright loopback QA, and `app/login/page.js` now exposes stable username/password `id`/`name`/`aria-label` selectors with explicit labels so the password field no longer inherits the visibility-toggle copy in its accessible name. Browser QA verified desktop and mobile login with console errors/warnings `0`, HMR connected, stable field labels/selectors, invalid-credentials feedback, `/privacy`, `/terms`, `/manifest.json`, and protected `/admin/diagnostics` redirect. Hanwoo project QC passed (`507` tests, lint, build, smoke); A/B selected `adopt_candidate` (`score_delta=1.454905`). Commit `1063ee92` is local only. No push was performed and T-251 was not retried. Concurrent blind-to-x/shorts WIP was preserved during T-1374 and later completed separately as T-1375/T-1376.

- 2026-06-06: Closed T-1370 and T-1373 by adopting the QC runner/test WIP, T-1372 shorts-maker-v2 live-provider test isolation, and deterministic retry handling for transient Next build locks, then regenerating the canonical active-project QC artifact and refreshing launch evidence. `.tmp/project_qc_runner_latest.json` is a clean PASS artifact with total `3973 passed`, `21 skipped`, `0` failures/errors: blind-to-x `1775 passed`, `9 skipped`; shorts-maker-v2 `1632 passed`, `12 skipped`; Hanwoo `505 passed`; Knowledge `61 passed`. Focused runner pytest passed `15/15`; `product_readiness_score.py --json` reports score `96`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet is `ready_for_authorization`; selector is `blocked_publish_only`; launch audit has complete coverage with current code-review graph; completion audit remains `incomplete` with `9/14` complete and `5` blocked issues. Remaining blockers are explicit push/current-head Actions and external/user-owned T-251.

- 2026-06-06: Completed T-1369 blind-to-x FMKorea scraper helper split. `projects/blind-to-x/scrapers/fmkorea.py` now delegates `FMKoreaScraper.scrape_post()` into focused fetch/load/container/title/content/count/screenshot/result helpers while preserving scrape/error behavior, and `projects/blind-to-x/tests/unit/test_scrapers_fmkorea.py` adds helper and screenshot-timeout coverage. Verification passed `py_compile`, focused FMKorea pytest (`37 passed`), related scraper pytest (`103 passed`), ruff check, ruff format check, diff-check, blind-to-x project QC test (`1773 passed`, `9 skipped`) and lint, code-review gate advisory WARN `risk_score=0.50` covered by focused/related/project tests, VibeDebt (`fmkorea.py` score `33.0 -> 30.1`; `scrape_post` complexity/length `38/208 -> 4/36`), and A/B `adopt_candidate` (`score_delta=0.568856`). Code commit `aa0f7819` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Current-head launch evidence cleanup is needed under T-1370. A full active-project QC refresh at current HEAD was attempted after T-1368/T-1369, but repeated foreground/background runner attempts were interrupted by orphaned runner/pytest processes; those interruptions left `.tmp/project_qc_runner_latest.json` with false `returncode 4294967295` failures for blind/shorts. Current `product_readiness_score.py --json` reports score `79`, local blocker `1`, publish blocker `1`, external blocker `1`; the local blocker is the dirty unrelated `execution/project_qc_runner.py` and `workspace/tests/test_project_qc_runner.py` WIP plus the interrupted QC artifact. Preserve those dirty files and resolve/adopt them before rerunning full active-project QC.

- 2026-06-06: Refreshed current launch evidence after T-1366 and before context-only relay updates, without pushing or retrying user-owned T-251. Full active-project QC passed and rewrote `.tmp/project_qc_runner_latest.json`: blind-to-x `1769 passed`, `9 skipped`; shorts-maker-v2 `1618 passed`, `12 skipped`; Hanwoo `505 passed`; Knowledge `61 passed`; total `3953 passed`, `21 skipped`. Runtime evidence: readiness score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage with current code-review graph; completion audit `incomplete` with `9/14` complete and 5 blocked issues. Remaining blockers are explicit push authorization/user push plus current-head Actions, and external/user-owned T-251.

- 2026-06-06: Completed T-1366 blind-to-x JobPlanet scraper helper split. `projects/blind-to-x/scrapers/jobplanet.py` now delegates `JobplanetScraper.scrape_post()` into focused URL/id extraction, detail fetch, title resolution, payload validation, screenshot capture, success result, and error result helpers while preserving scrape classification; `projects/blind-to-x/tests/unit/test_scrapers_jobplanet.py` covers helper and failure-classification paths. Verification passed focused JobPlanet pytest (`14 passed`), related FMKorea/JobPlanet dry-run pytest (`28 passed`), combined focused/related pytest (`42 passed`), ruff check, ruff format check, diff-check, code-review gate advisory WARN `risk_score=0.40` covered by focused/related/project tests, blind-to-x project QC (`1769 passed`, `9 skipped`) and lint, AST metrics (`scrape_post` `96` lines/`20` branches -> `30` lines/`5` branches; max function length `96 -> 39`; max branches `20 -> 13`), and A/B `adopt_candidate` (`score_delta=0.597414`). Code commit `18527ad8` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Refreshed current launch evidence after T-1364/T-1365 and before context-only relay updates, without pushing or retrying user-owned T-251. Full active-project QC passed and rewrote `.tmp/project_qc_runner_latest.json`: blind-to-x `1761 passed`, `9 skipped`; shorts-maker-v2 `1618 passed`, `12 skipped`; Hanwoo `505 passed`; Knowledge `61 passed`; total `3945 passed`, `21 skipped`. Runtime evidence: readiness score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. Remaining blockers are explicit push authorization/user push plus current-head Actions, and external/user-owned T-251.

- 2026-06-06: Completed T-1365 shorts-maker-v2 AI tech comparison renderer helper split. `projects/shorts-maker-v2/tools/ai_tech_shorts.py` now delegates `TechVSShortsGenerator._render()` into focused layer, intro, points, verdict, phase dispatch, and composition helpers while preserving visual output; `projects/shorts-maker-v2/tests/unit/test_ai_tech_shorts.py` covers phase boundaries and nonblank representative frames. Verification passed `py_compile`, focused pytest (`12 passed`), frame-hash parity (`6/6` byte-identical), ruff check, ruff format check, diff-check, shorts-maker-v2 project QC (`1618 passed`, `12 skipped`) and lint, staged code-review gate advisory WARN `risk_score=0.4` covered by tests/frame hashes/project QC, VibeDebt target score `36.9 -> 11.6`, and A/B `adopt_candidate` (`score_delta=0.386542`). Code commit `d04e612d` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Recorded concurrent/local T-1364 Blind feed URL helper split commit `e3145c11`. It changed `projects/blind-to-x/scrapers/blind.py` and `projects/blind-to-x/tests/unit/test_scrapers_blind.py`; final full active-project QC after T-1364/T-1365 covered it with blind-to-x `1761 passed`, `9 skipped` plus lint. No push was performed and T-251 was not retried.

- 2026-06-06: Refreshed current launch evidence after T-1362/T-1363 and context updates, without pushing or retrying user-owned T-251. The checked evidence run had a clean worktree, no open PRs, and no direct dependency adoption candidates; code-review graph is stale only across context/doc changes and the launch audit reports no graph-relevant stale-range file changes. Full active-project QC passed and rewrote `.tmp/project_qc_runner_latest.json`: blind-to-x `1760 passed`, `9 skipped`; shorts-maker-v2 `1606 passed`, `12 skipped`; Hanwoo `505 passed`; Knowledge `61 passed`; total `3932 passed`, `21 skipped`. Runtime evidence: readiness score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. Remaining blockers are explicit push authorization/user push plus current-head Actions, and external/user-owned T-251.

- 2026-06-06: Completed T-1363 Hanwoo today-focus feed-card dedupe. `projects/hanwoo-dashboard/src/lib/dashboard/today-focus.mjs` now builds the feed-depletion projection once and suppresses the same feed row from the generic low-stock card, while preserving non-feed low-stock warnings. `today-focus.test.mjs` covers the duplicate-card regression. Verification passed focused today-focus test (`17 passed`), Hanwoo project test (`505 passed`), lint, smoke, build retry after one real concurrent Next build lock, diff-check, staged code-review gate advisory WARN `risk_score=0.45` covered by focused/project tests, and A/B `adopt_candidate` (`score_delta=41.384615`). Code commit `ce765cc5` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1362 shorts-maker-v2 psychology quote renderer helper split. `projects/shorts-maker-v2/tools/psychology_quote.py` now delegates `QuoteShortsGenerator._render()` into focused timeline, alpha, layer, tag, quote-line, and author helpers while preserving visual output, and `test_psychology_quote.py` adds timeline/alpha/render phase coverage. Verification passed focused pytest (`6 passed`), related renderer pytest (`14 passed`), pre/post frame-hash comparison at 6 timestamps (`6/6` byte-identical), shorts-maker-v2 project QC test (`1606 passed`, `12 skipped`, `23` warnings) and lint, ruff check, ruff format check, `py_compile`, diff-check, VibeDebt target score `9.8`, staged code-review gate advisory WARN `risk_score=0.45` covered by frame hashes/tests, and A/B `adopt_candidate` (`score_delta=108.867444`). Code commit `ecd4e5b5` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Refreshed current-head launch evidence after T-1361 and evidence-correction commits. The checked state before context-only relay commits had a clean worktree, local `main` ahead of `origin/main`, open PR count `0`, and a current graph; use live `session_orient.py --json` for exact SHA/ahead count. Full active-project QC canonical artifact is current for the latest product-code head and passed with total `3925 passed`, `21 skipped`: blind-to-x `1760 passed`, `9 skipped`; shorts-maker-v2 `1600 passed`, `12 skipped`; Hanwoo `504 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence: readiness score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only` with selected kind `current_head_release_checks_unproven`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1361 shorts-maker-v2 space scale renderer helper split without pushing or retrying user-owned T-251. `projects/shorts-maker-v2/tools/space_scale.py` now delegates `SpaceScaleGenerator._render()` into focused phase/timing/drawing helpers while preserving visual output, and `projects/shorts-maker-v2/tests/unit/test_space_scale.py` adds timing, transition, and nonblank frame coverage. Verification passed pre/post frame-hash comparison at 6 timestamps (`6/6` byte-identical), focused pytest (`3 passed`), shorts-maker-v2 project QC test (`1600 passed`, `12 skipped`, `1` warning) and lint, ruff check, ruff format check, `py_compile`, diff-check, staged code-review gate advisory WARN `risk_score=0.45` covered by frame hashes/focused/project tests, VibeDebt (`space_scale.py` score `38.9 -> 10.8`, max complexity `18 -> 8`, max function length `103 -> 34`, shorts-maker-v2 TDR `32.77 -> 32.47`), and A/B `adopt_candidate` (`score_delta=90.44647867564534`). Code commit `a27525e6` is local only.

- 2026-06-06: Continued post-relay launch audit at checked head `38a181d4` without pushing or retrying user-owned T-251. `session_orient.py --json` reported clean worktree, `main` ahead by `219`, open PRs `0`, and graph stale only because context-only commits moved HEAD after graph build at `0e71ae64`. Product readiness stayed score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; `shorts-maker-v2` readiness is `100` with QC `1597 passed`, `12 skipped`, docs/env complete, and FEATURE checklist `7/7`. GitHub inventory found `7` projects, `3` workflow files, and no open PRs. Browser QA inventory reports `4/4` fresh usable nonblank screenshots. Dependency freshness reports direct candidates `0`; external npm metadata confirms ESLint `10.4.1` is latest but import/jsx-a11y/react plugin peer ranges still block ESLint 10, so no forced ESLint 10 migration was adopted. Release packet is `ready_for_authorization`; selector is `blocked_publish_only`; completion audit remains `incomplete` with `9/14` complete and 5 blocked issues.

- 2026-06-06: Refreshed launch evidence after T-1360 and its context commits. The checked head before this relay update was `0e71ae64`; use live `session_orient.py --json` for the exact latest HEAD/ahead count after context-only commits. Full active-project QC passed and `.tmp/project_qc_runner_latest.json` was replaced with the passed full-run artifact: total `3922 passed`, `21 skipped`; blind-to-x `1760 passed`, `9 skipped`; shorts-maker-v2 `1597 passed`, `12 skipped`; Hanwoo `504 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence: readiness score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. No local auto-research candidate remains. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1360 blind-to-x Ppomppu feed URL helper split. `projects/blind-to-x/scrapers/ppomppu.py` now delegates `_fetch_post_urls()` into focused helpers for feed link selectors, page URL collection, curl/session HTML fetch, intercepted feed URL collection, and direct fallback collection while preserving scraper behavior. `projects/blind-to-x/tests/unit/test_scrapers_ppomppu.py` adds hot-board selector, duplicate/board filter, any-board hot feed, and direct fallback regressions. Verification passed focused Ppomppu pytest (`15 passed`), related scraper pytest (`86 passed`), blind-to-x project QC (`1760 passed`, `9 skipped` plus lint), ruff check, ruff format check, `py_compile`, diff-check, code-review gate advisory WARN `risk_score=0.50` covered by direct/related/project tests, VibeDebt (`ppomppu.py` score `40.9 -> 35.2`, max complexity `19 -> 11`, max function length `98 -> 85`, `_fetch_post_urls` length/branches `98/19 -> 42/3`), and A/B `adopt_candidate` (`score_delta=7.386747627889143`). Code commit `eb1e51f0` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Refreshed launch evidence after T-1359 and its context commits. The checked project-code head before this relay update was `c44791b0`; use live `session_orient.py --json` for the exact latest HEAD/ahead count after context-only commits. Full active-project QC passed and rewrote `.tmp/project_qc_runner_latest.json`: total `3918 passed`, `21 skipped`; blind-to-x `1756 passed`, `9 skipped`; shorts-maker-v2 `1597 passed`, `12 skipped`; Hanwoo `504 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence: readiness score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. No local auto-research candidate remains. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1359 blind-to-x Notion property payload helper split. `projects/blind-to-x/pipeline/notion/_upload.py` now delegates `_prepare_property_payload()` into focused helpers for resolving property targets, scalar payloads, multi-select payloads, and date payloads while preserving Notion API payload behavior. `projects/blind-to-x/tests/unit/test_notion_upload.py` adds direct regressions for date values and empty/missing/unsupported properties. Verification passed focused Notion upload pytest (`45 passed`), broader Notion pytest (`69 passed`), focused ruff check, focused ruff format check, blind-to-x project QC (`1756 passed`, `9 skipped` plus lint), diff-check, code-review gate advisory WARN `risk_score=0.40` covered by direct/project tests, structure metrics (`_prepare_property_payload` length `40 -> 10`, branches `17 -> 2`, helper count `0 -> 5`), and A/B `adopt_candidate` (`score_delta=3.271493212669683`). Code commit `0a2fd4f1` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1358 Knowledge Dashboard and word-chain Radix patch dependency refresh. `projects/knowledge-dashboard/package.json` now updates `@radix-ui/react-dialog` to `^1.1.16`, `@radix-ui/react-separator` to `^1.1.9`, `@radix-ui/react-slot` to `^1.2.5`, and `@radix-ui/react-tooltip` to `^1.2.9`; `projects/word-chain/package.json` updates `@radix-ui/react-slot` to `^1.2.5`. `projects/knowledge-dashboard/package-lock.json`, `projects/word-chain/package-lock.json`, and root `pnpm-lock.yaml` were synchronized. Verification passed official npm metadata checks, `pnpm.cmd install --lockfile-only --frozen-lockfile --ignore-scripts`, Knowledge `npm.cmd test` (`61 passed`), Knowledge lint/build, word-chain `npm.cmd test` (`23 passed`), word-chain lint/build with the existing ASCII fallback after direct Vite `3221226505`, dependency freshness (`candidate_dependency_count=0`), npm audit review (Knowledge `2` moderate, word-chain `0`; no force fix because the suggested Next fix is an unsuitable major downgrade), `git diff --cached --check`, code-review gate PASS `risk_score=0.0`, and A/B `adopt_candidate` (`score_delta=3.5454545454545454`). Code commit `0147a0f1` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1357 Hanwoo Radix patch/minor dependency refresh. `projects/hanwoo-dashboard/package.json` now updates 12 direct Radix UI packages to current npm latest patch/minor releases: avatar `1.1.12`, dialog `1.1.16`, dropdown-menu `2.1.17`, label `2.1.9`, progress `1.1.9`, scroll-area `1.2.11`, select `2.3.0`, separator `1.1.9`, slot `1.2.5`, tabs `1.1.14`, toast `1.2.16`, and tooltip `1.2.9`. `projects/hanwoo-dashboard/package-lock.json` and root `pnpm-lock.yaml` were synchronized. Verification passed `npm.cmd install --package-lock-only --ignore-scripts`, root `pnpm.cmd install --lockfile-only --frozen-lockfile --ignore-scripts`, Hanwoo project QC (`504 passed`, lint, build, smoke), `git diff --check`, code-review gate PASS `risk_score=0.0`, dependency freshness artifact with Hanwoo direct candidates `0`, and A/B `adopt_candidate` (`score_delta=0.9632352941176471`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1356 shorts-maker-v2 RenderStep orchestration helper split. `projects/shorts-maker-v2/src/shorts_maker_v2/pipeline/render_step.py` now delegates scene render and output orchestration into focused helpers for warning recording, role style selection, scene color grading, TTS postprocess/audio attach, static/karaoke caption composition, hook animation, B-roll PiP, closing fade, bookend append, scene sequence rendering, short-limit trimming, BGM/SFX layering, video write kwargs/output, benchmark logging, and resource cleanup. `projects/shorts-maker-v2/tests/unit/test_render_step_core.py` covers role-style mapping and static-caption prefix/resource tracking. Verification passed focused render-step pytest (`32 passed`), related `test_render_step_*.py` (`132 passed`, `1` upstream warning), `ruff check`, `ruff format --check`, `py_compile`, `git diff --check`, staged code-review gate advisory WARN `risk_score=0.40` covered by direct/related/project tests, VibeDebt (`render_step.py` score `35.8`, max complexity `14`, max function length `116`, project TDR `32.77`), A/B `adopt_candidate` (`score_delta=0.7191674605064697`), shorts-maker-v2 project QC (`1597 passed`, `12 skipped`, `1` warning plus lint), and full active-project QC (`3916 passed`, `21 skipped`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1355 shorts-maker-v2 history countdown renderer helper split. `projects/shorts-maker-v2/tools/history_fact_shorts.py` now caches the countdown background gradient and splits `HistoryCountdownGenerator._render()` into helpers for embers, phase timing, hook title, item card, description card, and CTA drawing. `projects/shorts-maker-v2/tests/unit/test_history_fact_shorts.py` covers countdown timing and cached gradient formula. Verification passed pre/post `DEMO_COUNTDOWN` frame-hash comparison at `0.0`, `4.2`, `12.0`, and `36.5` seconds (`4/4` byte-identical), `compileall`, focused pytest (`2 passed`), `ruff check`, `ruff format --check`, `git diff --check`, VibeDebt (`history_fact_shorts.py` score `45.3 -> 31.8`, max function length `79 -> 50`, project TDR `33.09 -> 32.99`), staged code-review gate advisory WARN `risk_score=0.35` covered by direct tests/frame hashes, and A/B `adopt_candidate` (`score_delta=0.22411434598396104`). No push was performed and T-251 was not retried. Unrelated dirty WIP in `projects/shorts-maker-v2/src/shorts_maker_v2/pipeline/render_step.py` was preserved and excluded.

- 2026-06-06: Refreshed checked launch evidence after T-1354 and relay commits at `c7e2de49`, before the final relay/doc commits moved HEAD again. Worktree was clean, `main` was ahead of `origin/main` by `188`, open PR count was `0`, and `session_orient.py --json` reported the graph current for `c7e2de49` after one transient SQLite lock from a parallel graph update attempt. Product readiness accepted the canonical full active-project QC as current because there were no relevant project changes since artifact head `92cc7af6`: blind-to-x `1754 passed`, `9 skipped`; shorts-maker-v2 `1590 passed`, `12 skipped`, `1 warning`; Hanwoo `504 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence: readiness score `96`, state `blocked`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1354 blind-to-x DraftQualityGate helper split. `projects/blind-to-x/pipeline/draft_quality_gate.py` now delegates `DraftQualityGate.validate()` quality groups to focused helpers for length, Korean ratio, social style, creator take, format, duplicate sentences, cliches, repetition/hook checks, vague expressions, and strict mode while preserving behavior. Verification passed focused quality-gate pytest (`235 passed`), blind-to-x project QC (`1754 passed`, `9 skipped` plus lint), `ruff check`, `ruff format --check`, `py_compile`, `git diff --check`, code-review gate advisory WARN `risk_score=0.55` covered by focused/project tests, and A/B `adopt_candidate` (`score_delta=2.4833293186234364`; `validate` branches `55 -> 4`, length `333 -> 41`). Code commit `ad6cd7c0` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Refreshed latest local launch evidence after T-1352/T-1353 and context commits. Worktree was clean, `main` was ahead of `origin/main`, and the code-review graph was current for the evidence head. Full active-project QC passed: blind-to-x `1754 passed`, `9 skipped`; shorts-maker-v2 `1590 passed`, `12 skipped`, `1 warning`; Hanwoo `504 passed`; Knowledge `61 passed`; lint/build/smoke gates passed where applicable. Runtime evidence stayed at readiness score `96`, local blockers `0`, publish blockers `1`, external blockers `1`; release packet `ready_for_authorization`; selector `blocked_publish_only`; browser QA `4/4`; launch audit complete coverage; completion audit `incomplete` with `9/14` complete and 5 blocked issues. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1353 shorts-maker-v2 LLM bridge helper split. `LLMRouter.generate_text_bridged()` and `generate_json_bridged()` now delegate bridge provider selection, shadow-mode returns, validation-error formatting, repair attempts, validated JSON payload handling, and per-provider retry loops to focused helpers while preserving text/json bridge behavior. Verification passed focused `test_llm_router.py` (`17 passed`, `1` upstream warning), `ruff check`, `ruff format --check`, `py_compile`, `git diff --check`, prior full active-project QC over the same code contents, and A/B `adopt_candidate` (`score_delta=1.5051935580497946`). Code commit `c2960d9d` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Completed T-1352 Hanwoo market-price freshness status announcement. `MarketPriceWidget.js` now builds a shared Korean freshness label and exposes the last-updated/source footer as a polite status region with matching `aria-label` and `title`, so manual refresh completion is announced without changing the visible layout. `home-market-copy.test.mjs` locks the label and live-region contract. Verification passed Hanwoo node tests (`504 passed`, `0 failed`), Hanwoo project QC (`test`, `lint`, `build`, `smoke`), `git diff --cached --check`, staged code-review gate advisory WARN `risk_score=0.40` covered by Hanwoo tests/project QC, and A/B `adopt_candidate` (`score_delta=2.272727272727273`). Code commit `82d061dd` is local only. No push was performed and T-251 was not retried.

- 2026-06-06: Completed a bounded blind-to-x BaseScraper browser-open maintainability cycle. `projects/blind-to-x/scrapers/base.py` now delegates pool launch, single-browser guard checks, proxy config, Camoufox launch, Chromium fallback, and partial Chromium cleanup to focused helpers while preserving lifecycle behavior. `projects/blind-to-x/tests/unit/test_scrapers_base.py` adds a regression for failed Camoufox launch falling back to Chromium without closing an unentered Camoufox context. Verification passed focused base scraper pytest `15/15`, related scraper pytest `37/37`, full blind-to-x project QC (`1754 passed, 9 skipped` plus lint), full active-project QC (`3909 passed, 21 skipped`), ruff, format check, `py_compile`, diff-check, graph current at `964c82ba`, VibeDebt (`base.py` score `36.3`, `open` branches `27 -> 4`, length `95 -> 14`), code-review gate advisory WARN covered by direct/related/project tests, release packet `ready_for_authorization`, selector `blocked_publish_only`, completion audit `incomplete` with `9/14` complete and 5 blocked issues, and A/B `adopt_candidate` (`score_delta=2.620346807188912`). No push was performed and T-251 was not retried.

- 2026-06-06: Completed a bounded workspace CI analyzer maintainability cycle. `workspace/execution/_ci_analyzers.py` now delegates naming, large-literal, public type-hint, and string-concat loop checks to focused helpers while preserving behavior. `workspace/tests/test_ci_analyzers.py` adds regressions for string-ish augassign detection, naming exemptions, and public missing type-hint filtering. Verification passed focused analyzer pytest `6/6`, related workspace pytest `30/30` with repo-local `--basetemp` after a Windows temp permission error, ruff, format check, `py_compile`, `git diff --check`, VibeDebt (`_ci_analyzers.py` score `36.9 -> 28.5`, max complexity `14 -> 8`, duplicate blocks `22 -> 16`, workspace TDR `28.51 -> 28.41`), code-review gate advisory WARN `risk_score=0.55` covered by direct/related tests, and A/B `adopt_candidate` (`score_delta=1.889754540706912`). No push was performed and T-251 was not retried.

- 2026-06-06: T-1349 refreshed Knowledge Dashboard Node type definitions from `@types/node@^25.9.1` to `^25.9.2` and synchronized `projects/knowledge-dashboard/package-lock.json`. Exact-head evidence at `221c093f` passed full active-project QC (`3908 passed, 21 skipped`; blind-to-x `1753/9`, shorts-maker-v2 `1590/12`, Hanwoo `504/0`, Knowledge `61/0`), graph refresh current, readiness score `96` with local blockers `0`, release packet `ready_for_authorization`, selector `blocked_publish_only`, launch-objective audit complete coverage, and completion audit `incomplete` with `9/14` complete and 5 blocked issues. No push was performed and T-251 was not retried.

- 2026-06-06: T-1347 completed a bounded blind-to-x filter/profile helper split. `pipeline/process_stages/filter_profile_stage.py` now delegates shared filter skip/result handling, daily queue-floor overrides, profile/review decision wiring, emotion/profile side effects, editorial gate rejection, viral scoring, content-calendar diversity, and daily budget checks to focused helpers while preserving `run_filter_profile_stage()` behavior. Verification passed focused process-stage pytest `45/45`, blind-to-x project QC (`1753 passed, 9 skipped` plus lint), full active-project QC at exact head `d1f4b4d0` (`3908 passed, 21 skipped`), graph refresh current, readiness score `96` with local blockers `0`, release packet `ready_for_authorization`, selector `blocked_publish_only`, code-review gate advisory WARN `risk_score=0.40` covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=2.658607248209018`). No push was performed and T-251 was not retried.

- 2026-06-06: T-1345/T-1346 completed two bounded blind-to-x maintainability cycles. T-1345 splits `pipeline/notion/_query.py` property decoding, status-aware options, date/multi-select decoding, recency checks, sort keys, database matching, and recent search-result filtering into focused helpers while preserving `NotionQueryMixin`; `tests/unit/test_notion_query_mixin.py` now covers multi-select and malformed Notion payloads. T-1346 splits `pipeline/process_stages/persist_stage.py` missing-uploader failure, NotebookLM scheduling, Blind image prompt/A-B variant handling, AI image scheduling, screenshot/image await handling, Notion upload, and draft analytics recording helpers while preserving `run_persist_stage()`. Verification passed query pytest `13/13`, related Notion pytest `67/67`, feedback pytest `28/28`, persist/process-stage pytest `50/50`, ruff, format checks, `py_compile`, diff-checks, blind-to-x project QC (`1750 passed, 9 skipped` plus lint), graph refresh current at `c2f6f885`, code-review gate advisory WARNs (`risk_score=0.60/0.40`) covered by direct/project tests, VibeDebt (`_query.py` score `38.3 -> 32.0`; persist-stage candidate score `29.9`), and A/B `adopt_candidate` for both (`score_delta` `2.552950` and `2.584363`). No push was performed and T-251 was not retried.

- 2026-06-06: T-1343 completed a bounded blind-to-x runner execution helper split. `pipeline/runner.py` now delegates base/item service construction, dry-run/live item processing, sequential/parallel fan-out, cross-source insight append, trend spike recording, metric calculation, execution summary, notification, all-failed exit, and digest dispatch while preserving the public `execute_pipeline()` contract. `tests/unit/test_runner.py` adds direct dry-run and parallel-path regressions. Verification passed runner pytest `5/5`, related runner/main/process pytest `32/32`, ruff, ruff format check, `py_compile`, `git diff --check`, full blind-to-x project QC (`1750 passed, 9 skipped` plus lint), VibeDebt (`runner.py` score `41.3 -> 23.4`, max complexity `31 -> 8`, max function length `190 -> 66`), code-review gate advisory WARN (`risk_score=0.60`) covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=3.979945265462507`). Concurrent Hanwoo health commit `4028a8fe` was preserved and verified separately with Hanwoo project QC (`504 passed`, lint, build, smoke). No push was performed and T-251 was not retried.

- 2026-06-06: Post-T-1342 exact-head launch evidence refresh completed before this relay update. A local/concurrent `e28b042c chore(deps): T-1342 refresh React types` commit updated `@types/react` to `^19.2.17` for knowledge-dashboard and word-chain plus lockfiles. Knowledge Dashboard was covered by full active-project QC; word-chain was verified separately with `npm.cmd test` (`23 passed`), `npm.cmd run lint`, and `npm.cmd run build` using the existing ASCII fallback after direct Vite `3221226505`. Current launch evidence stayed stable: readiness score `96`, local blockers `0`, publish blockers `1`, external blockers `1`, release packet `ready_for_authorization`, selector `blocked_publish_only`, and completion audit `incomplete` with `9/14` complete and 5 blocked issues. No push was performed and T-251 was not retried.

- 2026-06-06: T-1341 completed a bounded blind-to-x draft response parser helper split that was discovered as dirty shared work during final evidence refresh. `draft_validation.py` now delegates `_parse_response()` to focused helpers for thinking-tag stripping, tag matching/text extraction, JSON payload application, thread/platform/reply/auxiliary tags, Twitter fallback, and single-format fallback while preserving behavior. `test_draft_validation.py` adds direct coverage for JSON payload mapping, thread compatibility, auxiliary tags, image-prompt fallback stripping, and single-format plain text fallback. Verification passed blind-to-x project QC (`1748 passed, 9 skipped` plus lint), graph update, and full active-project QC: blind-to-x `1748 passed, 9 skipped`; shorts-maker-v2 `1590 passed, 12 skipped, 1 warning`; Hanwoo `500 passed` plus lint/build/smoke; Knowledge `61 passed` plus lint/build/smoke. Code commit `61865f87` is local only; no push was performed and T-251 was not retried.

- 2026-06-06: Post-T-1340 current-head QC refresh closed the selector's temporary `project_qc_refresh` candidate. Focused Hanwoo project QC passed (`500 passed`, lint, build, smoke with accepted CI smoke warnings), then full active-project QC was rerun and wrote the canonical `.tmp/project_qc_runner_latest.json`: blind-to-x `1744 passed, 9 skipped`; shorts-maker-v2 `1590 passed, 12 skipped, 1 warning`; Hanwoo `500 passed` plus lint/build/smoke; Knowledge `61 passed` plus lint/build/smoke. Fresh readiness is score `96` with local blockers `0`, publish blockers `1`, external blockers `1`; release packet is `ready_for_authorization`; selector is `blocked_publish_only`; completion audit remains `9/14` complete with 5 blockers: publish/Actions, release authorization, selector publish boundary, external/user-owned T-251, and direct Hanwoo readiness T-251. No push was performed and T-251 was not retried.

- 2026-06-06: T-1340 completed a bounded shorts-maker-v2 provider optional import hardening cycle. `providers.__getattr__` now restores already-loaded provider submodules from `sys.modules` before `find_spec()`, and `edge_tts_client.py` imports without the optional `edge-tts` package while raising a clear lazy `ImportError` only when fallback `Communicate()` is used. `test_tts_providers.py` adds direct regressions for provider submodule recovery and missing Edge TTS dependency import. Verification passed focused provider pytest `3/3`, related provider/Edge pytest `102/102`, full active-project QC at exact head `47d2fb48` (blind-to-x `1744 passed, 9 skipped`; shorts-maker-v2 `1590 passed, 12 skipped, 1 warning`; Hanwoo `500 passed` plus lint/build/smoke; Knowledge `61 passed` plus lint/build/smoke), ruff, format check, `py_compile`, `git diff --check`, VibeDebt project TDR `33.22`, code-review gate `risk_score=0.0` after direct/full coverage, and A/B `adopt_candidate` (`score_delta=0.6684382871536524`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-06: T-1336/T-1337/T-1338 completed a bounded shorts-maker-v2 CosyVoice and lazy import boundary cycle. `cosyvoice_client.py` now delegates generation modes, tensor collection, audio saving, MP3 fallback, and timing behavior into focused helpers while preserving zero-shot/ref-audio/instruct/cross-lingual/SFT behavior. Package/provider/pipeline imports are lazy or local where appropriate, and follow-up commits make top-level and pipeline `__getattr__` restore already-loaded `sys.modules` submodules before `find_spec()` so preloaded modules and test doubles survive optional dependency discovery failures. Verification passed provider/entrypoint pytest `53/53`, full shorts-maker-v2 project QC (`1584 passed, 12 skipped, 1 warning` plus lint), ruff, format check, `py_compile`, `git diff --check`, VibeDebt (`cosyvoice_client.py` score `40.1 -> 20.9`, max complexity `20 -> 6`, max function length `115 -> 37`, project TDR `33.39 -> 33.24`), code-review gate advisory WARNs (`risk_score=0.40/0.30`) covered by focused/full tests, graph refresh current at code head `8cd6fe7c`, and A/B `adopt_candidate` (`score_delta=6.71426596552098`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-06: T-1335 completed a bounded blind-to-x Notion upload helper split. `pipeline/notion/_upload.py` now delegates upload memo construction, X publish properties, draft body properties, analysis properties, base page properties, initial media children, regulation status, and child block assembly to focused helpers while preserving `upload()` behavior. `test_notion_upload.py` adds direct regression coverage for `_build_upload_properties()` review/X/draft/analysis payloads. Verification passed Notion upload pytest `43/43` with repo-local basetemp, blind-to-x project QC (`1744 passed, 9 skipped` plus lint), ruff, format check, `py_compile`, `git diff --check`, VibeDebt (`_upload.py` score `43.2 -> 34.4`, max complexity `24 -> 20`, max function length `128 -> 49`, project TDR `34.8 -> 34.6`), code-review gate advisory WARN (`risk_score=0.40`) covered by direct/project tests, graph current at `a1c5a000`, and A/B `adopt_candidate` (`score_delta=0.25058404887338076`). Later T-1336/T-1337/T-1338 adopted the shorts-maker-v2 WIP, so the current blocker state is explicit push/current-head Actions plus T-251, not dirty local work.

- 2026-06-06: T-1334 completed a bounded shorts-maker-v2 media fallback chain tightening. `fallback_mixin.py` now extracts `_try_image_provider()` and `_try_dalle_image()` so `_try_image_chain()` builds ordered Imagen/Gemini/Pollinations attempts while preserving DALL-E policy retry, stock-video fallback, cache, and placeholder behavior. `test_media_step_branches.py` adds regressions for skipped stock-mix attempts not recording failures and for stock-video recovery after image-provider failures. Verification passed focused pytest `27/27`, related media fallback pytest `6/6`, full shorts-maker-v2 project QC (`1584 passed, 12 skipped, 1 warning` plus lint), ruff, format check, `py_compile`, `git diff --check`, VibeDebt (`fallback_mixin.py` score `40.5 -> 36.3`, max complexity `19 -> 13`, max function length `103 -> 93`, duplicate blocks `2 -> 2`, project TDR `33.47 -> 33.39`), code-review gate advisory WARN (`risk_score=0.35`) covered by focused/related/full tests, and A/B `adopt_candidate` (`score_delta=1.3466666666666667`). Do not mark the launch goal complete until explicit push authorization/user push and current-head Actions are complete, and T-251 is resolved.

- 2026-06-06: Post-T-1333 launch evidence refresh confirmed there are no remaining local auto-research candidates. `product_readiness_score.py --json` reported score `96`, state `blocked`, local blockers `0`, publish blockers `1`, and external blockers `1`; target readiness is blind-to-x `100`, shorts-maker-v2 `100`, knowledge-dashboard `100`, and hanwoo-dashboard `86` only because T-251 remains unresolved. `release_authorization_packet.py --json` reported `ready_for_authorization`, ahead `99`, dirty `0`, and unproven `root-quality-gate`/`active-project-matrix`. `next_experiment_selector.py` reported `blocked_publish_only`, and `completion_audit.py` on the launch manifest stayed incomplete with `9/14` complete and 5 blocked issues. Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-06: T-1333 completed a bounded blind-to-x 6D scoring helper split. `scoring_6d.py` now delegates freshness, social signal, hook strength, trend relevance, audience targeting, viral potential, weight loading, weighted score, source quality boost, calibration cutoff/row fetch, engagement extraction, proxy construction, correlation, and normalized calibration weighting to focused helpers while preserving `calculate_6d_score()` and `calibrate_weights()` behavior. Added direct regression coverage in `test_content_intelligence.py` for weighted dimension scoring and source quality boost behavior. Verification passed focused pytest `6/6`, related unit pytest `52/52`, integration pytest `23/23`, blind-to-x project QC (`1743 passed, 9 skipped` plus lint), ruff, format check, `py_compile`, `git diff --check`, AST metrics (`scoring_6d.py` max complexity `22 -> 7`, max function length `143 -> 30`, `calculate_6d_score` `22/143 -> 5/27`, `calibrate_weights` `19/83 -> 3/30`), VibeDebt project TDR `34.97 -> 34.8`, code-review gate advisory WARN (`risk_score=0.45`) covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=0.6502586776752242`). Do not mark the launch goal complete until explicit push authorization/user push and current-head Actions are complete, and T-251 is resolved.

- 2026-06-06: T-1332 completed a bounded blind-to-x editorial scoring helper split. `scoring_editorial.py` now delegates editorial signal construction, dimension scoring, weighted score calculation, hard-reject checks, reason-label assembly, and publishability brief/reason assembly to focused helpers while preserving public scoring and publishability brief behavior. Added direct regression coverage in `test_content_intelligence.py`. Verification passed focused pytest `5/5`, related blind-to-x pytest `74/74`, full blind-to-x project QC (`1742 passed, 9 skipped` plus lint), ruff, format check, `py_compile`, `git diff --check`, AST metrics (`scoring_editorial.py` max complexity `50 -> 8`, max function length `169 -> 49`), VibeDebt project TDR `34.97`, code-review gate advisory WARN (`risk_score=0.45`) covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=1.4935507`). Do not mark the launch goal complete until explicit push authorization/user push and current-head Actions are complete, and T-251 is resolved.

- 2026-06-06: T-1331 completed a bounded shorts-maker-v2 script prompt locale override split. `script_prompts.py` now delegates tone preset normalization, string tuple normalization, channel persona merging, persona keyword merging, prompt field-name merging, and channel review-criteria merging to focused helpers while preserving prompt/review behavior. Added malformed locale bundle regression coverage in `test_script_step_i18n.py`. Verification passed focused script pytest `30/30`, full shorts-maker-v2 project QC (`1582 passed, 12 skipped, 1 warning` plus lint), ruff, format check, `py_compile`, `git diff --check`, VibeDebt (`script_prompts.py` score `40.6 -> 34.9`, max complexity `39 -> 11`, max function length `99 -> 93`, project TDR `33.53 -> 33.47`), code-review gate advisory WARN (`risk_score=0.45`) covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=0.2579619`). Do not mark the launch goal complete until unrelated dirty blind-to-x WIP is resolved/adopted, graph/current-head evidence is refreshed, explicit push authorization/user push and current-head Actions are complete, and T-251 is resolved.

- 2026-06-06: T-1330 completed a bounded Knowledge Dashboard NotebookLM fetch helper split. `fetch_notebooklm_notebooks()` now delegates dependency availability, token JSON loading, client construction, source payload simplification, notebook payload simplification, and list simplification into focused helpers. Added fake-client regression coverage for simplified notebook/source payloads, `sources=None -> []`, and client credential argument wiring. Verification passed Knowledge sync pytest `16/16`, Knowledge project QC (`61 passed` plus lint/build/smoke), ruff, format check, `py_compile`, `git diff --check`, VibeDebt (`sync_data.py` score `21.0 -> 12.0`, max complexity `11 -> 9`, max function length `50 -> 45`, project TDR `25.06 -> 20.88`), code-review gate advisory WARN (`risk_score=0.55`) covered by direct/project tests, and A/B `adopt_candidate` (`score_delta=0.2237566`). Do not mark the launch goal complete until unrelated dirty WIP is resolved/adopted, graph/current-head evidence is refreshed, explicit push authorization/user push and current-head Actions are complete, and T-251 is resolved.

- 2026-06-06: T-1328/T-1329 completed a bounded launch fallback guard cycle. T-1328 split Knowledge Dashboard NotebookLM token-template detection into focused helpers and added script/app regressions for non-string cookie values and identity placeholder fields. T-1329 split shorts-maker-v2 TTS provider routing/fallback behavior and added direct regressions for premium provider success, stale-output unlink plus Edge fallback, OpenAI signature routing, and missing OpenAI client failure. Focused verification passed Knowledge sync pytest `15/15`, Shorts TTS/provider pytest `17/17` with one upstream Google GenAI deprecation warning, ruff, format check, `py_compile`, `git diff --check`, and A/B `adopt_candidate` for both candidates (`score_delta` `1.1957691` and `1.5712114`). Full active-project QC passed on the current code commit: blind-to-x `1740 passed, 9 skipped`; shorts-maker-v2 `1581 passed, 12 skipped, 1 warning`; Hanwoo `500 passed` plus lint/build/smoke; Knowledge `61 passed` plus lint/build/smoke. Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-06: T-1327 completed a bounded `blind-to-x` generate-review-stage maintainability cycle. `generate_review_stage.py` now delegates missing-generator failure, screenshot upload, draft splitting, Twitter reply fallback, generation failure, quality-gate retries, editorial/fact/readability/validator post-generation components, Twitter quality failure, and completion into focused helpers. `run_generate_review_stage()` shrank from 230 to 47 lines and 46 to 7 branch points; private helpers increased from 4 to 18. Added regression coverage for readable Korean Twitter reply fallback copy. Verification passed process-stage pytest `42/42`, full blind-to-x project QC (`1740 passed, 9 skipped` plus lint), ruff, format check, `py_compile`, `git diff --check`, code-review gate advisory WARN (`risk_score=0.30`) covered by direct/full tests, and A/B `adopt_candidate` (`score_delta=1.0158915`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-06: T-1326 completed a GitHub release workflow definition guard. `workspace/tests/test_github_workflow_hygiene.py` now maps required release gates to local workflow files and asserts the expected `name:` values plus `push` and `pull_request` triggers, preventing local workflow name/trigger drift before the publish authorization boundary. Verification passed workflow hygiene pytest `3/3`, release-boundary related pytest `67/67`, ruff, format check, `py_compile`, `git diff --check`, staged code-review gate advisory WARN (`risk_score=0.30`) with no impacted nodes for the staged workflow-test file, and A/B `adopt_candidate` (`score_delta=2.294872`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-06: T-1325 completed direct Knowledge Dashboard launch-readiness evidence. `launch_objective_audit.py` now includes a `knowledge-dashboard` target item requiring score `100`, ready state, nonstale project QC `PASS`, README/package/page launch artifacts, env checks complete, no unresolved tasks, and no dirty target paths; regressions cover complete and stale/failing readiness cases. Verification passed focused launch/completion tests `45/45`, ruff, format check, `py_compile`, code-review graph refresh on the T-1325 feature commit, code-review gate advisory WARN `risk_score=0.40` covered by direct tests, A/B `adopt_candidate` (`score_delta=0.918273`), release packet `ready_for_authorization`, selector `blocked_publish_only`, readiness score `96`, and completion audit `incomplete` with `9/14` complete and 5 blocked issues. Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-05: T-1322/T-1323/T-1324 completed launch evidence freshness work. `launch_objective_audit.py` now records current-code triage evidence from `session_orient.py --json` and `code_review_gate.py`, `project_qc_runner.py` schema `3` records git artifact metadata, `product_readiness_score.py` keeps full-QC evidence fresh across `.ai` context-only commits, and launch graph freshness blocks only on graph-relevant stale changes or unverifiable stale ranges. Verification passed focused launch-audit tests `37/37` and `39/39`, related auto-research tests `56/56`, readiness/QC focused tests `44/44`, ruff/format/py_compile/diff-check gates, A/B `adopt_candidate` for all three candidates (`score_delta` `1.155124`, `1.503968`, `1.232601`), and full active-project QC: blind-to-x `1739 passed, 9 skipped`; shorts-maker-v2 `1577 passed, 12 skipped, 1 warning`; Hanwoo `500 passed` plus lint/build/smoke; Knowledge `61 passed` plus lint/build/smoke. Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-05: T-1321 extracted blind-to-x Ppomppu post helpers. `PpomppuScraper.scrape_post()` now delegates post fetch failure classification, route fulfillment, post container discovery, direct-navigation fallback, title/content/count/image extraction, and screenshot saving while preserving behavior. Verification passed Ppomppu tests `11/11`, related scraper tests `43/43`, full blind-to-x QC `1739 passed, 9 skipped` plus lint, ruff, format check, `py_compile`, `git diff --check`, VibeDebt (`ppomppu.py` debt `44.8 -> 40.9`, max complexity `53 -> 19`, `scrape_post` complexity `53 -> 9`, length `255 -> 85`), code-review gate advisory WARN (`risk_score=0.50`) covered by direct/full tests, and A/B `adopt_candidate` (`score_delta=1.827920`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-05: T-1320 extracted blind-to-x Ppomppu feed helpers. `PpomppuScraper` now delegates feed URL selection, feed navigation, row title/comment/view/like/image extraction, feed candidate construction, and feed candidate collection while preserving ranking, dedupe, and metadata behavior. Verification passed focused Ppomppu scraper tests `19/19`, related scraper tests `71/71`, ruff, format check, `py_compile`, `git diff --check`, staged code-review gate advisory WARN (`risk_score=0.45`) covered by direct tests, and A/B `adopt_candidate` (`score_delta=2.314408`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-05: T-1319 extracted blind-to-x Blind scraper feed/post helpers. `BlindScraper` now delegates feed URL selection, feed row metadata, feed candidate construction/collection, post content/count extraction, fetch failure classification, navigation, container discovery, Crawl4AI fallback handling, screenshot saving, and category keyword matching while preserving behavior. Verification passed graph detect-changes `risk_score=0.0`, Blind scraper tests `11/11`, related scraper tests `75/75`, full blind-to-x QC `1733 passed, 9 skipped` plus lint, ruff, format check, `py_compile`, `git diff --check`, staged code-review gate advisory WARN (`risk_score=0.65`) covered by direct/full tests, and A/B `adopt_candidate` (`score_delta=0.528523`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-05: T-1318 extracted blind-to-x selector repair helpers. `BaseScraper._suggest_selectors()` now delegates selector part scoring, scored dedupe, unique append, class fallback, similar-id fallback, and tag content fallback while preserving candidate order and max-3 behavior. Verification passed focused selector/scraper tests `46/46`, ruff, format check, `py_compile`, VibeDebt audit (`base.py` debt `43.1 -> 43.0`, `_suggest_selectors` complexity `35 -> 7`, length `92 -> 50`), full blind-to-x project QC `1732 passed, 9 skipped` plus lint, code-review gate advisory WARN (`risk_score=0.40`) covered by direct tests, and A/B `adopt_candidate` (`score_delta=0.693839`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-05: T-1317 extracted blind-to-x best-of-N draft generation helpers. `generate_drafts()` now delegates cache writes, recent-failure provider filtering, candidate generation, and reviewer selection while preserving behavior. Verification passed project `.venv` best-of-N tests `15/15`, related draft generation tests `36/36`, blind-to-x project QC `1729 passed, 9 skipped` plus lint, ruff, format check, `py_compile`, `git diff --check`, code-review gate advisory WARN (`risk_score=0.40`) covered by direct tests, and A/B `adopt_candidate` (`score_delta=0.773171`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-05: T-1316 surfaced code-review graph freshness in session orientation. `session_orient.py --json` now compares graph `built_at_commit` with the current git head and reports `current_head`, `freshness`, `stale`, and `stale_reason`; text and Rich diagnostics show stale/unknown graph freshness instead of leaving agents to infer it manually. Verification passed focused session-orient tests `33/33`, ruff, format check, `py_compile`, `git diff --check`, runtime JSON stale-graph evidence, staged code-review gate advisory WARN (`risk_score=0.60`) covered by direct tests, and A/B `adopt_candidate` (`score_delta=1.472549`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-05: T-1315 preserved full-workspace project-QC evidence. `project_qc_runner.py` now writes partial `--project/--check` runs to `.tmp/project_qc_runner_partial_latest.json` instead of overwriting the canonical `.tmp/project_qc_runner_latest.json` unless the artifact has complete coverage for all active projects. This prevents focused QC from making `product_readiness_score.py` regress to stale/missing project coverage. Verification passed `workspace/tests/test_project_qc_runner.py` `14/14`, ruff, format check, `py_compile`, `git diff --check`, full active-project QC (`blind-to-x 1726 passed, 9 skipped`; `shorts-maker-v2 1577 passed, 12 skipped`; Hanwoo `500/500` plus lint/build/smoke; Knowledge `61/61` plus lint/build/smoke), and A/B `adopt_candidate` (`score_delta=0.993639`).

- 2026-06-05: T-1314 consolidated blind-to-x Notion review formatting helpers. `projects/blind-to-x/pipeline/notion/_upload.py` now centralizes metric pairs, fact-check warning counts/previews, and emotion-profile lines while preserving reviewer-facing Notion output. Added focused helper/output regressions in `tests/unit/test_notion_upload.py`. Verification passed Notion upload tests `42/42`, related Notion review/schema tests `51/51`, ruff, format check, `py_compile`, `git diff --check`, full blind-to-x QC (`1726 passed, 9 skipped` plus lint), and A/B `adopt_candidate` (`score_delta=1.346602`).

- 2026-06-05: T-1313 made current-head release authorization a direct launch-audit requirement. `launch_objective_audit.py` now includes the no-push release packet as first-class evidence so completion audits cannot hide the clean-ahead push authorization boundary behind selector proxy evidence. Verification passed focused release/launch audit tests, related auto-research tests, ruff, format check, `py_compile`, `git diff --check`, dirty/clean runtime audits, and A/B `adopt_candidate` (`score_delta=0.981177`). Do not mark the launch goal complete until explicit push authorization/user push, current-head Actions, and T-251 are resolved.

- 2026-06-05: T-1312 added `.agents/skills/auto-research/scripts/release_authorization_packet.py` and documented/wired it into required workflow test and ruff lists. Clean-ahead selector state is now `blocked_publish_only` with `release_authorization_packet.py --json` as the first required gate before explicit push authorization. `launch_objective_audit.py` now includes direct `hanwoo-dashboard` target readiness evidence, keeping T-251 as a complete-coverage direct blocker. Verification passed focused selector/launch/completion/release-packet pytest `51/51`, workflow-hygiene plus packet pytest `6/6`, ruff, format check, `py_compile`, `git diff --check`, clean release packet `ready_for_authorization`, selector `blocked_publish_only`, completion audit `incomplete` (`8/12` complete, 4 blocked), A/B `adopt_candidate` (`score_delta=0.998026`), and code-review gate advisory WARN `risk_score=0.40` covered by direct tests. Do not mark the launch goal complete: unpushed commits still need explicit push authorization plus current-head Actions, and T-251 remains external/user-owned.

- 2026-06-05: T-1311 classified current-head publish/Actions boundaries in `launch_objective_audit.py` as complete evidence with explicit blockers, removing noisy `incomplete_coverage` issues while keeping completion audit `incomplete` for the actual blockers. Clean-state launch audit now reports `workspace/local/publish/agent blockers=1/0/1/0` with complete coverage, selector `current_head_release_checks_unproven` with complete coverage and a publish-boundary blocker, and completion audit `8/11` complete with only three blocked issues. Verification passed launch+completion pytest `34/34`, auto-research related pytest `44/44`, ruff, `py_compile`, `git diff --check`, code-review gate advisory WARN `risk_score=0.35` covered by direct tests, and A/B `adopt_candidate` (`score_delta=0.695612`). Do not mark the launch goal complete: unpushed commits still need explicit push authorization plus current-head Actions, and T-251 remains external/user-owned.

- 2026-06-05: T-1310 prioritized the current-head publish/Actions blocker in `product_readiness_score.py` `next_actions` before user-owned project waits, so the next approval boundary appears before Hanwoo T-251. Clean-state readiness remains score `96`, `local_blocker_count=0`, `publish_blocker_count=1`, with the first next action requiring explicit push authorization/user push plus current-head Actions. Verification passed product-readiness pytest `28/28`, readiness+selector pytest `38/38`, ruff, `py_compile`, `git diff --check`, code-review gate `pass`, and A/B `adopt_candidate` (`score_delta=0.725932`).

- 2026-06-05: T-1309 added `latest_next_priority_note` to `session_orient.py` for clean-but-unpushed relay closeout states. The note clarifies that a "commit this relay update" step can already be locally satisfied while publish/CI/user blockers remain. Verification passed focused session-orient pytest `30/30`, broader session/selector/launch pytest `68/68`, ruff, pre-commit ruff format check, `py_compile`, `git diff --check`, staged code-review gate advisory WARN `risk_score=0.55` covered by direct tests, and A/B `adopt_candidate` (`score_delta=0.949127`). Do not mark the launch goal complete: unpushed commits still need explicit push authorization plus current-head Actions, and T-251 remains external/user-owned.

- 2026-06-05: T-1308 added direct `shorts-maker-v2` FEATURE checklist evidence to the launch-objective audit. Runtime dirty launch audit evidence showed `shorts-maker-v2 FEATURE checklist complete 7/7, open=0`, and regression coverage proves open checklist items block target readiness completion. Verification passed focused launch-audit pytest `28/28`, broader auto-research/readiness pytest `69/69`, full active-project QC (blind-to-x `1723 passed, 9 skipped`; shorts-maker-v2 `1577 passed, 12 skipped`; Hanwoo `500/500` plus lint/build/smoke; Knowledge `61/61` plus lint/build/smoke), ruff, `py_compile`, `git diff --check`, staged code-review gate `risk_score=0.0`, and A/B `adopt_candidate` (`score_delta=0.994212`). Do not mark the launch goal complete: unpushed commits still need explicit push authorization plus current-head Actions, and T-251 remains external/user-owned.

- 2026-06-05: T-1307 hardened credential surfaces and launch selector snapshot reuse. `projects/hanwoo-dashboard/prisma/seed.js` now requires configured `ADMIN_PASSWORD` length `>= 12` instead of seeding `admin/admin123`; `src/app/login/page.js` no longer exposes demo credentials and shows operator credential guidance; `launch_objective_audit.py` calls the selector in-process using the already-collected snapshot. Verification passed Hanwoo Node tests `500/500`, Hanwoo project QC test/lint/build/smoke, focused auto-research pytest `37/37`, ruff, format check, `py_compile`, `git diff --check`, browser `/login` click QA with demo credentials absent and console errors/warnings `0/0`, and A/B `adopt_candidate` (`score_delta=1.124169`). Do not mark the launch goal complete: unpushed commits still need explicit push authorization plus current-head Actions, and T-251 remains external/user-owned.

- 2026-06-05: T-1306 classified clean unpublished current-head Actions as a publish blocker instead of a local code blocker. `product_readiness_score.py` now exposes GitHub release `ahead_count`, `publish_required`, `blocker_type=publish`, and overall `publish_blocker_count`; `launch_objective_audit.py` records `workspace/local/publish/agent blockers=...`. Verification passed focused product-readiness + launch-audit pytest `53/53`, broader auto-research/readiness pytest `67/67`, active-project QC evidence complete in readiness, ruff, format check, `py_compile`, `git diff --check`, and A/B `adopt_candidate` (`score_delta=0.998045`). Post-context clean-state reruns confirmed readiness score `96`, `local_blocker_count=0`, `publish_blocker_count=1`, selector `current_head_release_checks_unproven`, and completion audit `incomplete` (`8/11` complete). Do not mark the launch goal complete: unpushed commits still need explicit push authorization plus current-head Actions, and T-251 remains external/user-owned.

- 2026-06-05: T-1305 added direct `shorts-maker-v2` target readiness evidence to `launch_objective_audit.py` and ignored `.playwright-mcp/` local artifacts. Verification passed focused auto-research pytest `35/35`, broader auto-research/GitHub pytest `76/76`, ruff, format check, `py_compile`, Hanwoo Node tests `499/499`, `shorts-maker-v2` QC (`1577 passed`, `12 skipped`, lint passed), diff-check, check-ignore, A/B `adopt_candidate`, and code-review gate exit `0` with advisory WARN `risk_score=0.4`. Do not mark the launch goal complete: T-251 remains external/user-owned, and any unpushed local commits need explicit push authorization plus current-head Actions.

- 2026-06-05: Completion audit against the active `complete` thread goal. Pre-recording `session_orient.py --json` reported clean/synced `main` at `b036e6a2`, open PRs `0`, and required GitHub Actions green on that HEAD. `product_readiness_score.py --json` reported score `96`, workspace/local/agent blockers `0/0/0`, and external blocker count `1`. `next_experiment_selector.py --root . --json` wrote `.tmp/next-experiment-completion-check.json` and returned `blocked_external_only` for user-owned T-251. `launch_objective_audit.py` wrote `.tmp/launch-objective-audit-completion-check.json`; `completion_audit.py ... --allow-incomplete` returned `status=incomplete`, `complete_count=8/9`, `issue_count=1`, `blocked_count=1`, with only external/user-owned T-251 blocked. After recording context commits, rerun `session_orient.py --json`; if `main` is ahead of `origin/main`, current-HEAD Actions are not proven until those commits are pushed and required Actions pass. Do not mark the product launch goal complete until Supabase credentials are reset and Hanwoo live Prisma CRUD E2E passes.

- 2026-06-05: T-1303 exposed next-experiment selector required gates in launch-objective audit evidence. The candidate passed focused pytest `21/21`, broader auto-research launch/completion/selector pytest `34/34`, ruff, format check, `py_compile`, diff-check, runtime selector required-gate evidence, A/B `adopt_candidate`, completion audit `3/3`, code-review gate exit `0` with advisory WARN `risk_score=0.4` covered by direct tests, and required Actions on `dc0d11e8` (`root-quality-gate` `26997046879`, `active-project-matrix` `26997046881`). Full product launch remains blocked only by external/user-owned Hanwoo T-251.

- 2026-06-05: T-1302 exposed next-experiment selector guardrails in launch-objective audit evidence and normalized selector action punctuation. The candidate passed focused pytest `21/21`, broader auto-research launch/completion/selector pytest `34/34`, ruff, format check, `py_compile`, diff-check, runtime launch audit guardrail evidence, A/B `adopt_candidate`, completion audit `3/3`, code-review gate exit `0` with advisory WARN `risk_score=0.4` covered by direct tests, and required Actions on `ae9d9e59`. Full product launch remains blocked only by external/user-owned Hanwoo T-251.

- 2026-06-05: T-1301 added live next-experiment selector-state evidence to launch-objective audits. The candidate passed focused pytest `20/20`, broader auto-research launch/completion/selector pytest `33/33`, ruff, format check, `py_compile`, diff-check, runtime launch audit selector evidence, A/B `adopt_candidate`, completion audit `4/4`, code-review gate exit `0` with advisory WARN `risk_score=0.35` covered by direct tests, and required Actions on `43bd0f8e`. Full product launch remains blocked only by external/user-owned Hanwoo T-251.

- 2026-06-05: T-1300 stabilized launch-objective A/B manifest evidence selection so older `.tmp/ab-manifest-*` artifacts touched after newer experiments do not displace the intended latest evidence. The candidate passed focused pytest `18/18`, broader auto-research launch/completion/selector pytest `31/31`, ruff, format check, `py_compile`, diff-check, code-review gate `risk_score=0.0`, A/B `adopt_candidate`, completion audit `4/4`, and required Actions on `b59e3006`. Full product launch remains blocked only by external/user-owned Hanwoo T-251.

- 2026-06-05: T-1295 aligned the shared active goal with the product launch auto-research loop so `session_orient.py --json` no longer reports the stale completed Hanwoo quality goal. The current local state remains launch audit 7/8, readiness score 96, workspace/local/agent blockers 0/0/0, and only external/user-owned Hanwoo T-251 blocked.

- 2026-06-05: T-1296 added `next_experiment_selector.py` so future auto-research loops deterministically rank local readiness, GitHub, browser QA, dependency, stale QC, and external/user-owned blocker candidates before choosing work. Feature commit `4a1d1dbc` is pushed and required Actions passed; final clean selector output is `blocked_external_only` for T-251. Do not retry T-251 until Supabase Dashboard credentials are reset.

- 2026-06-05: T-1297 cleared skill-lint evidence warnings and hardened `next_experiment_selector.py` against missing/invalid explicit input artifacts plus UTF-16 PowerShell JSON artifacts. Feature commit `7984696c` is pushed and required Actions passed; runtime missing-input selection routes to `input_evidence_unavailable`, while prepared clean artifacts still route to `blocked_external_only` for T-251.

- 2026-06-05: T-1298 hardened session orientation against stale relay closeout instructions. `session_orient.py` now reports `latest_next_priority_status` and marks post-push closeout instructions stale only when git is already clean/synced, preventing continuation agents from repeating completed context closeouts while preserving actionable dirty-work closeouts. Feature commit `d7719bbf` is pushed and required Actions passed; full product launch remains blocked only by external/user-owned T-251.

- 2026-06-05: T-1299 refined T-1298 with current-HEAD CI awareness. `session_orient.py` now requires both `root-quality-gate` and `active-project-matrix` to be completed successfully on the current head before stale post-push closeout `Next Priorities` are flagged; focused pytest `28/28`, broader related pytest `57/57`, A/B `adopt_candidate`, completion audit `4/4`, and required Actions on `0bac5859` passed. Full product launch remains blocked only by external/user-owned T-251.

- 2026-05-21: T-625 normalized Hanwoo pagination item payloads. `useCattlePagination()` and `useSalesPagination()` now route initial SSR items and load-more API `json.data.items` through `normalizePaginationItems()`, ignore non-array input and malformed/non-object rows, and append from safe arrays, preventing malformed pagination/cache payloads from crashing through spread access. Verification passed with focused pagination source tests (`4 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 263, lint, build), and staged `code_review_gate --json` JSON pass (`risk_score 0.0`; cp949 reader-thread noise only). Commit `676f7453`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-624 normalized Hanwoo notification modal payloads. `NotificationModal` now routes its `notifications` prop through `normalizeModalNotifications()`, ignores non-array input and malformed/non-object rows, and renders empty/list states from `visibleNotifications`, preventing direct modal reuse from crashing through raw `.length` or `.map()` access. Verification passed with focused notification modal source test (`8 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 261, lint, build), and staged `code_review_gate --json` JSON pass (`risk_score 0.0`; cp949 reader-thread noise only). Commit `64ee6b6f`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-623 normalized Hanwoo dashboard notification payloads. `DashboardClient` now routes `initialNotifications` and refreshed `getNotifications()` results through `normalizeDashboardNotifications()`, ignores non-array input and malformed/non-object rows, and feeds safe arrays into the header critical badge, notification modal, notification widget, and estrus/calving alert banners, preventing malformed caller/cache data from crashing home notification rendering through raw `.some()` or modal/widget render paths. Verification passed with focused home-market source test (`29 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 260, lint, build), and staged `code_review_gate --json` JSON pass (`risk_score 0.0`; cp949 reader-thread noise only). Commit `81c6c267`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-622 normalized Hanwoo cattle CSV export payloads. `buildCattleCsvRows()` now routes caller input through `normalizeCattleCsvRows()`, ignores non-array input and malformed/non-object rows, and exports only safe object rows, preventing malformed caller/cache data from crashing CSV generation through raw `.map()` access. Verification passed with focused cattle CSV export tests (`4 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 259, lint, build), and staged `code_review_gate --json` JSON pass (`risk_score 0.0`; cp949 reader-thread noise only). Commit `3e2ef7a5`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-621 normalized Hanwoo weather forecast payloads. `WeatherWidget` now routes `weather.forecast` through `normalizeWeatherForecast()`, ignores malformed/non-object rows, and renders forecast cards plus livestock weather alerts from `safeForecast`, preventing malformed caller/cache weather data from crashing the home weather card through raw `.map()` access. Verification passed with focused home-market/weather source test (`28 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 258, lint, build), and staged `code_review_gate --json` JSON pass (`risk_score 0.0`; cp949 reader-thread noise only). Commit `5e7362ea`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-620 normalized Hanwoo financial chart payloads. `FinancialChartWidget` now routes `saleRecords`, `expenseRecords`, and `seriesData` through `normalizeFinancialChartItems()`, ignores malformed/non-object rows, and renders from `safeSaleRecords`, `safeCostRecords`, and `safeSeriesData`, preventing malformed caller/cache data from crashing the home financial chart through raw `.forEach()` or `.map()` access. Verification passed with focused analysis/financial source test (`3 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 258, lint, build), and staged `code_review_gate --json` JSON pass (`risk_score 0.0`; cp949 reader-thread noise only). Commit `2f0bd804`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-619 normalized Hanwoo dashboard building and cattle collection payloads. `DashboardClient` now routes `initialBuildings`/state through `normalizeDashboardBuildings()`, derives `safeBuildings`, routes paged/full cattle through `normalizeDashboardCattleList()`, filters malformed rows, supplies Korean missing-name copy, and passes safe collections into home cards, pen grids, drag/drop/export, setup progress, alert banners, forms, detail modal, Feed/Calving/Sales/Analysis/Settings tabs. Verification passed with focused home-market source test (`28 passed`), empty-state source test (`16 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 257, lint, build), and `code_review_gate --base HEAD~1 --json` JSON pass (`risk_score 0.0`; cp949 reader-thread noise only). Commit `1dd1f846`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-618 normalized Hanwoo pen card payloads. `PenCard` now routes incoming `cattle` through `normalizePenCattle()`, filters malformed rows, supplies stable fallback ids plus Korean missing-name copy, and renders from `visibleCattle`, preventing malformed caller/cache data from crashing the pen card through raw `.some()`, `.length`, or `.map()` access. Verification passed with focused cards accessibility/source test (`3 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC after one real concurrent Next build lock retry (`test` 255, lint, build), and staged `code_review_gate --json` (`risk_score 0.0`; cp949 reader-thread noise only). Commit `bd5adf04`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-617 normalized Hanwoo notification-system payloads. `NotificationSystem.js` and the tracked `NotificationSystem.tsx` mirror now route `initialNotifications` through `normalizeSystemNotifications()`, ignore malformed rows, supply stable fallback ids plus Korean title/message defaults, and use functional state updates for mark-read actions, preventing malformed caller/cache data from crashing the notification dropdown through raw `.filter()`, `.length`, or `.map()` access. Verification passed with focused notification-system source test (`9 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 255, lint, build), and staged `code_review_gate --json` (`risk_score 0.0`; cp949 reader-thread noise only). Commit `3411278f`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-616 normalized Hanwoo profitability widget payloads. `ProfitabilityWidget` now routes incoming `data` through `normalizeProfitabilityItems()`, filters malformed rows, supplies stable fallback ids, and renders from `visibleData`, preventing malformed caller/cache data from crashing the profitability recommendation widget through raw `.length` or `.map()` access. Verification passed with focused profitability copy/source test (`7 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 253, lint, build), and staged `code_review_gate --json` (`risk_score 0.0`; cp949 reader-thread noise only). Commit `e4097a36`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-615 normalized Hanwoo settings building payloads. `SettingsTab` routes incoming `buildings` through `normalizeSettingsBuildings()`, filters malformed rows, supplies safe fallback building names and pen counts, and renders from `safeBuildings`, preventing malformed caller/cache data from crashing the settings building list through raw `.map()` access. Verification passed with focused settings plus home-market source tests (`37 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 252, lint, build), and staged `code_review_gate --json` (`risk_score 0.0`). Commit `0272bd19`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-614 normalized Hanwoo sales tab payloads. `SalesTab` now routes incoming `saleRecords`, `cattleList`, and `expenseRecords` through `normalizeSalesItems()`, then uses `safeSaleRecords`, `safeCattleList`, and `safeExpenseRecords` for processed sales, cattle lookup, expense aggregation, select options, submit disabled state, and empty-state actions, preventing malformed caller/cache data from crashing the tab through raw spread, `.find()`, `.filter()`, `.map()`, or `.length` access. Verification passed with focused home-market copy/source test (`26 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 252, lint, build), and staged `code_review_gate --json` (`risk_score 0.0`; cp949 reader-thread noise only). Commit `ab0183f2`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-613 labeled Hanwoo settings submit states. `SettingsTab` now derives `farmSubmitButtonLabel` and `buildingSubmitButtonLabel` from `isSavingFarm`/`isSavingBuilding` and applies them to the farm settings and building create submit `PremiumButton` `aria-label` and `title`, so settings save actions expose their in-flight state while async saves are locked. Verification passed with focused settings accessibility test (`10 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 250, lint, build), and staged `code_review_gate --json` (`risk_score 0.0`; cp949 reader-thread noise only). Commit `1b4e16e7`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-612 normalized Hanwoo analysis tab payloads. `AnalysisTab` now routes incoming `saleRecords`, `feedHistory`, `cattleList`, and `expenseRecords` through `normalizeAnalysisItems()`, then uses `safeSaleRecords`, `safeFeedHistory`, `safeCattleList`, and `safeExpenseRecords` for monthly chart aggregation, cost structure, top sales, feed average, and cattle count, preventing malformed caller/cache data from crashing the tab through raw `.forEach()`, spread, `.reduce()`, or `.length` access. Verification passed with focused analysis copy/source test (`3 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 250, lint, build), and staged `code_review_gate --json` (`risk_score 0.0`; cp949 reader-thread noise only). Commit `1e72ad25`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-611 normalized Hanwoo calving tab payloads. `CalvingTab` now routes incoming `cattle` and `buildings` props through safe collection normalizers, ignores malformed rows, derives `pregnantCows` from `safeCattle`, and uses `safeCattle`/`safeBuildings` for submit and building lookups, preventing stale cache or caller data from crashing the tab through raw `.filter()` or `.find()` access. Verification passed with focused Calving accessibility test (`5 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC test/lint (`test` 250, lint), targeted build retry after a real concurrent Next build lock, and staged `code_review_gate --json` (`risk_score 0.0`; cp949 reader-thread noise only). Commit `4535546c`; commit hook WARN was the known graph/test-gap heuristic while direct tests and build covered the changed files.

- 2026-05-21: T-610 labeled Hanwoo sales submit state. `SalesTab` now derives `submitButtonLabel` from `isSaving` and applies it to the sales submit `PremiumButton` `aria-label` and `title`, matching the visible sales record submit state so the action exposes its in-flight state. Verification passed with focused home-market copy/source test (`25 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 250, lint, build), and staged `code_review_gate --json` (`risk_score 0.0`; cp949 reader-thread noise only). Commit `4974e238`; commit hook WARN was the known graph/test-gap heuristic while direct tests and full QC covered the changed files.

- 2026-05-21: T-609 labeled Hanwoo schedule submit state. `ScheduleTab` now derives `submitButtonLabel` from `isSaving` and applies it to the schedule submit button `aria-label` and `title`, matching the visible schedule registration state so the action exposes its in-flight state. Verification passed with focused Schedule accessibility test (`7 passed`), current Cattle/Schedule focused regression pass (`18 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC test/lint (`test` 249, lint), targeted build retry after a real concurrent Next build lock, and staged `code_review_gate --json` (`risk_score 0.0`; cp949 reader-thread noise only). Commit `69c19b22`; commit hook WARN was the known graph/test-gap heuristic while direct tests and build covered the changed files.

- 2026-05-21: T-608 normalized Hanwoo cattle form building payloads. `CattleForm` routes incoming `buildings` props through `normalizeCattleFormBuildings()`, ignores malformed payloads, supplies stable fallback ids plus Korean missing-building copy, and renders/reset/defaults from `safeBuildings`. Verification passed with focused cattle detail/form wiring test (`11 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC (`test` 249, lint, build), and staged `code_review_gate --json` (`risk_score 0.0`; cp949 reader-thread noise only). Commit `d76f1058`.

- 2026-05-21: T-607 labeled Hanwoo inventory submit state. `InventoryTab` derives `submitButtonLabel` from `isSaving` and applies it to the create submit `PremiumButton` `aria-label` and `title`. Verification passed with focused empty-state/home-market source tests (`41 passed`), targeted ESLint, path-limited `git diff --check`, full Hanwoo QC test/lint (`test` 248, lint), targeted build retry, and staged `code_review_gate --json`. Commit `d1a2b83f`.
